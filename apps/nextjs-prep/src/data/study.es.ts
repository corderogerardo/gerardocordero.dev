// Study guide — Next.js App Router sections (01–20) — español
export type StudySection = { id: string; num: string; title: string; html: string };

export const STUDY_INTRO_HTML =
  "<span class=\"lbl\">Cómo usar esto</span> Cada tema explica el concepto a la profundidad que una entrevista senior de Next.js / React espera, y luego da una línea de <b>'cómo decirlo'</b> — la oración concisa para entregar en voz alta. Lee para entender primero; ensaya las líneas de una sola línea al final. Todo es actual a <b>Next.js 16.2</b> y <b>React 19.2</b>.";

export const STUDY_SECTIONS: StudySection[] = [
  {
    id: "st-1",
    num: "01",
    title: "01 · El modelo mental del App Router",
    html:
      "<p><b>Lo que quieren:</b> entiendas que el App Router invierte el defecto. Cada componente bajo <code>app/</code> es un <b>Server Component</b> a menos que opte por <code>\"use client\"</code>. Esa es una inversión deliberada del Pages Router, donde todo se enviaba al navegador por defecto.</p>" +
      "<p><b>Por qué existe RSC:</b> tres ventajas. Se envía menos JavaScript — la lógica de un Server Component y sus dependencias nunca llegan al bundle del cliente. Los secretos permanecen del lado del servidor — puedes leer una base de datos o una clave API directamente en el cuerpo de un componente sin riesgo de que se filtre a una etiqueta script. Y los datos se obtienen cerca de la fuente — sin cascada del lado del cliente de \"montar, luego obtener\", sin impuesto de spinner de carga para cada hoja que necesita datos.</p>" +
      "<p>El formato de transmisión es el <b>Payload RSC</b>: no es HTML, un árbol serializado compacto que describe la salida del Server Component renderizado, con <i>marcadores</i> para dónde van los Client Components más las props serializables que necesitan. En la primera carga, el servidor también renderiza ese árbol a HTML para una pintura rápida. La secuencia es: HTML llega y pinta inmediatamente → el payload RSC reconcilia el árbol (React lo empareja) → el JS del cliente hidrata solo las islas de Client Components, conectando manejadores de eventos y estado.</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Los Server Components son el defecto — se renderizan a un payload serializado con marcadores para Client Components, así que solo las islas interactivas envían JS e hidratan.\"</div>",
  },
  {
    id: "st-2",
    num: "02",
    title: "02 · Enrutamiento por sistema de archivos y estructura de proyecto",
    html:
      "<p><b>Núcleo:</b> el directorio <code>app/</code> es el router — las carpetas definen segmentos de URL, y archivos con nombres especiales dentro de una carpeta definen el comportamiento para ese segmento. <code>page.tsx</code> hace que un segmento sea públicamente alcanzable y renderiza la UI. <code>layout.tsx</code> envuelve un segmento y sus hijos, <b>preserva estado entre navegaciones</b> (no se remonta), y se anida automáticamente con layouts padres.</p>" +
      "<p><code>template.tsx</code> parece un layout pero <b>sí</b> se remonta en cada navegación — útil para animaciones de entrada/salida o resetear estado por visita. <code>loading.tsx</code> envuelve automáticamente el segmento en un límite <code>&lt;Suspense&gt;</code> y se muestra mientras los datos del segmento se resuelven. <code>error.tsx</code> es un límite de errores de Client Component con alcance en ese segmento (debe ser un Client Component porque los límites de errores usan estado de componente). <code>not-found.tsx</code> se renderiza cuando se lanza <code>notFound()</code> o una ruta no puede coincidir. <code>default.tsx</code> es la UI de fallback para un slot de ruta paralela cuando Next no puede recuperar el estado activo en una navegación dura.</p>" +
      "<p>Organiza sin cambiar la URL: los <b>grupos de rutas</b> <code>(marketing)</code> envuelven segmentos en paréntesis para que el nombre de la carpeta se elimine del camino — útil para dar a una sección su propio layout sin agregar un segmento de URL. Las <b>carpetas privadas</b> <code>_components</code> (guión bajo al inicio) sacan una carpeta <i>del</i> enrutamiento por completo, para que puedas colocar ayudantes, pruebas y archivos de no-ruta junto a las rutas que los usan.</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"El árbol de carpetas es el árbol de rutas — page.tsx es lo público, layout.tsx persiste entre navegaciones, y los grupos de rutas me permiten organizar o agregar un límite de layout sin tocar la URL.\"</div>",
  },
  {
    id: "st-3",
    num: "03",
    title: "03 · Server Components vs Client Components",
    html:
      "<p><b>Predetermina Server Components</b> para cualquier cosa que obtenga datos, lea secretos, o no necesite interactividad — lo cual en la mayoría de aplicaciones es la mayoría del árbol. Recurre a un <b>Client Component</b> (<code>\"use client\"</code>) solo para la hoja que realmente necesita <code>useState</code>, <code>useEffect</code>, manejadores de eventos, o APIs solo-navegador.</p>" +
      "<p><b>La directiva es un límite del grafo de módulos, no un flag por componente.</b> <code>\"use client\"</code> al inicio de un archivo marca ese archivo como el <i>punto de entrada</i> al bundle del cliente — todo lo que ese archivo importa y renderiza directamente también se envía al cliente, incluso si esos módulos no tienen directiva propia. No significa \"solo este componente es un Client Component\"; significa \"aquí es donde se divide servidor y cliente.\"</p>" +
      "<p>La <b>intercalación</b> es el truco que mantiene a los Server Components renderizados en servidor incluso dentro de un árbol de Client Components: pasa un Server Component como <code>children</code> u otra prop a un Client Component, en lugar de importarlo y renderizarlo directamente. <code>&lt;Modal&gt;&lt;Cart/&gt;&lt;/Modal&gt;</code> — <code>Modal</code> es un Client Component que provee la interactividad de abrir/cerrar, pero <code>Cart</code> se compone desde un padre Server Component y permanece renderizado en servidor; <code>Modal</code> solo recibe su output ya renderizado como un slot.</p>" +
      "<p>React Context necesita un wrapper cliente porque el contexto depende de <code>createContext</code>/<code>useContext</code>, que requieren reactividad del lado del cliente — un Server Component no puede suscribirse a nada. La convención es un pequeño archivo de proveedor <code>\"use client\"</code> que envuelve <code>children</code>, para que todo lo que esté debajo aún pueda ser un Server Component por defecto.</p>" +
      "<p><code>server-only</code> y <code>client-only</code> son paquetes de protección: importar <code>server-only</code> a un módulo lanza un error de compilación si ese módulo es alguna vez incluido en un bundle del cliente (y viceversa) — seguro barato contra un ayudante que lee secretos filtrándose accidentalmente al navegador.</p>" +
      "<div class=\"callout warn\"><span class=\"lbl\">Trampa</span> Agregar <code>\"use client\"</code> a un layout o página de nivel superior \"solo para arreglar un error\" arrastra todo lo que importa al bundle del cliente. Empuja la directiva tan abajo como sea posible — a la hoja interactiva específica, no a sus ancestros.</div>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"'use client' marca un límite de módulo, no un solo componente — así que lo empujo a la hoja más pequeña que necesita interactividad y paso Server Components como children para mantener el resto del árbol renderizado en servidor.\"</div>",
  },
  {
    id: "st-4",
    num: "04",
    title: "04 · Estrategias de renderizado",
    html:
      "<p><b>Núcleo:</b> el App Router colapsa la vieja taxonomía SSG/ISR/SSR en dos ejes — renderizado <b>estático</b> y <b>dinámico</b> — decidido por ruta en tiempo de compilación/solicitud en lugar de declararse de antemano. El renderizado estático se ejecuta en tiempo de compilación (o una vez, luego se cachea), produciendo HTML que se reutiliza para cada visitante — esto es lo que SSG e ISR se convierten efectivamente. El renderizado dinámico se ejecuta en cada solicitud, porque la ruta necesita algo que solo se puede conocer en tiempo de solicitud.</p>" +
      "<p><b>Cómo decide el router:</b> una ruta se vuelve dinámica cuando toca una API en tiempo de solicitud — leer <code>cookies()</code>, <code>headers()</code>, un <code>searchParams</code> sin caché, o llamar un <code>fetch</code> sin caché. En el modelo anterior (pre-Cache Components), esto se hacía explícito con la configuración de segmento de ruta <code>export const dynamic = 'force-dynamic' | 'force-static'</code>, que fija el comportamiento en lugar de dejar que Next lo infiera del uso de API. <b>ISR</b> en este marco es solo renderizado estático con una ventana de <code>revalidate</code> — servir el HTML cacheado y regenerarlo en segundo plano después de que la ventana expire.</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Pienso en estático vs dinámico, no en SSG vs SSR — una ruta es dinámica en el momento que toca una API en tiempo de solicitud, e ISR es solo renderizado estático con una ventana de revalidate.\"</div>",
  },
  {
    id: "st-5",
    num: "05",
    title: "05 · Cache Components y Prerenderizado Parcial",
    html:
      "<p><b>Núcleo:</b> Cache Components es el nuevo modelo de caché, habilitado con <code>cacheComponents: true</code> en <code>next.config.ts</code>. Reemplaza \"¿es esta ruta estática o dinámica?\" con una pregunta más granular: ¿qué <i>partes</i> de una ruta son cacheables, marcadas explícitamente en lugar de inferidas?</p>" +
      "<p>La directiva <code>\"use cache\"</code> marca una función, componente, o archivo completo como cacheable. Ponla al inicio de una función asíncrona y su valor de retorno se cachea; ponla al inicio de un archivo y cada función exportada en ese archivo es cacheable. La clave de caché se deriva <b>automáticamente</b> de los argumentos de la función y cualquier valor capturado de su clausura — no creas una clave manualmente.</p>" +
      "<p><code>cacheLife(profile)</code> y <code>cacheTag(tag)</code>, ambos importados de <code>next/cache</code>, ahora son <b>estables</b> — sin más prefijo <code>unstable_</code>. <code>cacheLife</code> establece cuánto tiempo una entrada de caché se considera fresca (perfiles incorporados como <code>'seconds'</code>, <code>'minutes'</code>, <code>'hours'</code>, <code>'days'</code>, u un objeto personalizado); <code>cacheTag</code> adjunta una etiqueta de invalidación que puedes apuntar después con <code>revalidateTag</code>/<code>updateTag</code>.</p>" +
      "<p>El <b>Prerenderizado Parcial</b> es el mecanismo de entrega: el shell estático de una ruta — todo lo que no está envuelto en acceso a datos dinámicos — se prerenderiza en tiempo de compilación, y un límite <code>&lt;Suspense&gt;</code> alrededor de una pieza dinámica crea un \"hueco\" en ese shell que se transmite en tiempo de solicitud. El shell estático se sirve instantáneamente desde el borde del CDN; el hueco dinámico se llena un latido después.</p>" +
      "<p>Bajo Cache Components, acceder a una API en tiempo de solicitud (como <code>cookies()</code>) o una fuente de datos sin caché <b>fuera</b> de un límite <code>&lt;Suspense&gt;</code> o una función <code>\"use cache\"</code> es un error de compilación/dev — el framework se rehusa a adivinar si esa pieza debería ser estática o dinámica y te fuerza a ser explícito. <code>connection()</code> es la ruta de escape para llamadas no determinísticas (<code>Math.random()</code>, <code>Date.now()</code>) que no son APIs en tiempo de solicitud pero aún no deberían hornearse en el shell estático — esperarla difiere el resto de la función al tiempo de solicitud.</p>" +
      "<div class=\"callout warn\"><span class=\"lbl\">Trampa</span> Cache Components es opt-in y cambia las reglas — no asumas que está activo en cada proyecto de Next 16. Verifica <code>next.config.ts</code> para <code>cacheComponents: true</code> antes de razonar sobre el comportamiento de caché.</div>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Con Cache Components, el caché es explícito — 'use cache' marca lo que es cacheable con una clave derivada automáticamente, cacheLife/cacheTag controlan frescura e invalidación, y los límites de Suspense tallan los huecos dinámicos de un shell de lo demás estático y prerenderizado.\"</div>",
  },
  {
    id: "st-6",
    num: "06",
    title: "06 · El modelo de caché anterior",
    html:
      "<p><b>Núcleo:</b> a menos que una ruta opte por Cache Components, se ejecuta en el modelo de caché <i>original</i> del App Router — y ese modelo es importante conocerlo porque aún es el defecto. Esto <b>no es una capa debajo de Cache Components</b>; son dos modelos mentales diferentes para el mismo problema, y mezclar su vocabulario en una respuesta de entrevita es una señal de que no realmente sabes cuál está ejecutando un proyecto dado.</p>" +
      "<p>En este modelo, <code>fetch()</code> es <b>sin caché por defecto</code> en el App Router (un cambio deliberado del Pages Router, donde la obtención de datos no tenía caché incorporado en absoluto). Optas por ello por llamada: <code>fetch(url, { cache: 'force-cache' })</code> cachea indefinidamente hasta invalidarse manualmente, y <code>fetch(url, { next: { revalidate: 60, tags: ['posts'] } })</code> cachea con una ventana de revalidación basada en tiempo y/o una etiqueta que puedes invalidar bajo demanda.</p>" +
      "<p>Para acceso a datos que no es una llamada <code>fetch</code> — una consulta ORM, un cliente de base de datos directo — <code>unstable_cache()</code> da el mismo comportamiento de caché (TTL, etiquetas) para una función asíncrona arbitraria. La configuración de segmento de ruta aún aplica aquí: <code>export const dynamic</code>, <code>export const revalidate</code>, y <code>export const fetchCache</code> al inicio de un <code>page.tsx</code>/<code>layout.tsx</code> fijan el comportamiento de caché de toda la ruta en lugar de decidirlo por fetch.</p>" +
      "<p>La función <code>cache()</code> de React es una herramienta completamente diferente — no persiste datos entre solicitudes, deduplica llamadas a la misma función <b>dentro de una sola solicitud</b>, así que si tres componentes en una página todos llaman <code>getUser(id)</code>, el trabajo subyacente se ejecuta una vez.</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Sin Cache Components, fetch es sin caché por defecto y opto por ello con force-cache o una opción revalidate/tags; para datos no-fetch uso unstable_cache, y el cache() de React es solo dedup por solicitud, no caché persistente.\"</div>",
  },
  {
    id: "st-7",
    num: "07",
    title: "07 · Streaming y Suspense",
    html:
      "<p><b>Núcleo:</b> envolver una pieza lenta de UI en <code>&lt;Suspense fallback={...}&gt;</code> permite que el resto del shell HTML de la ruta se transmita al navegador inmediatamente, con el fallback mostrado en lugar de la parte lenta, luego el contenido real se transmite y reemplaza el fallback una vez que se resuelve — sin necesidad de bloquear toda la página detrás de la dependencia de datos más lenta.</p>" +
      "<p>La sutileza que vale la pena decir en voz alta: estar <i>envuelto</i> en Suspense no es lo mismo que ser <i>dinámico</i>. Si el componente dentro del límite es síncrono y no toca ninguna API en tiempo de solicitud, aún se resuelve completamente en tiempo de compilación — el límite de Suspense solo está presente estructuralmente en el JSX, pero no hay nada en lo que realmente se suspenda, así que no contribuye a un hueco dinámico bajo Prerenderizado Parcial. Suspense solo crea comportamiento de streaming útil cuando algo dentro de él es realmente asíncrono y lento (un fetch real, una API real en tiempo de solicitud).</p>" +
      "<div class=\"callout warn\"><span class=\"lbl\">Trampa</span> No asumas que envolver algo en Suspense hace que una ruta sea dinámica o mejore su comportamiento en tiempo de compilación — verifica si el componente envuelto realmente espera algo dependiente de la solicitud, o el límite es peso muerto.</div>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Suspense transmite el shell primero y llena la parte lenta después — pero solo si lo que está dentro realmente se suspende en algo asíncrono; un componente síncrono en un límite de Suspense aún se resuelve en tiempo de compilación.\"</div>",
  },
  {
    id: "st-8",
    num: "08",
    title: "08 · Patrones de obtención de datos",
    html:
      "<p><b>Núcleo:</b> los Server Components obtienen datos directamente en el cuerpo del componente con <code>await</code> — sin <code>useEffect</code>, sin boilerplate de estado de carga para el caso común. El patrón que eliges controla si las obtenciones independientes se ejecutan en paralelo o se serializan accidentalmente.</p>" +
      "<p><b>Paralelo:</b> inicia múltiples llamadas <code>fetch</code>/consulta sin esperar cada una inmediatamente, luego espéralas juntas — ya sea esperando dos promises juntas o explícitamente con <code>Promise.all([getUser(id), getPosts(id)])</code>. Ambas solicitudes se disparan al mismo tiempo en lugar de que una espere a la otra.</p>" +
      "<p><b>Secuencial (una cascada):</b> a veces inevitable y a veces intencional — necesitas el resultado de una obtención (el id del equipo de un usuario) antes de poder hacer la siguiente (los miembros de ese equipo). La habilidad no es \"nunca cascada\", es reconocer qué dependencias son reales y cuáles son accidentales (por ejemplo, dos componentes esperando independientemente dentro de JSX anidado cuando podrían haber juntos más arriba en el árbol).</p>" +
      "<p><b>Precarga:</b> un idioma común es una utilidad <code>preload()</code> — una llamada no esperada a una función de datos envuelta en <code>cache()</code>, disparada temprano (frecuentemente al inicio de un padre) para que la solicitud ya esté en vuelo cuando un componente hijo realmente la espere más profundo en el árbol. Porque <code>cache()</code> deduplica por argumentos dentro de la solicitud, el <code>await getUser(id)</code> posterior del hijo solo resuelve el promise ya iniciado en lugar de disparar una segunda solicitud.</p>" +
      "<p><code>use()</code> es la API para la dirección opuesta — canalizar un promise de un Server Component a un Client Component y leerlo allí. El Server Component inicia la obtención y pasa el <i>promise mismo</i> (no el valor esperado) como prop; el Client Component llama <code>use(promise)</code> dentro de un límite <code>&lt;Suspense&gt;</code> para suspender hasta que se resuelva, lo que permite que el shell renderizado por servidor se transmita inmediatamente mientras ese valor específico se transmite después.</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Inicio obtenciones independientes juntas para que se ejecuten en paralelo, uso un preload() respaldado por cache() para iniciar solicitudes temprano, y use() cuando necesito transmitir un promise de un Server Component a un Client Component sin bloquear el shell.\"</div>",
  },
  {
    id: "st-9",
    num: "09",
    title: "09 · Server Functions y Server Actions",
    html:
      "<p><b>Núcleo:</b> la directiva <code>\"use server\"</code> marca una función como Server Function — código que se define dondequiera que lo escribas pero siempre se ejecuta en el servidor, invocable desde Client Components como si fuera una función asíncrona local. Ponla al inicio del cuerpo de la función para una ocasional, o al inicio de un archivo para marcar cada exportación en ese archivo. Cuando una Server Function se pasa a un formulario, convencionalmente se llama Server Action.</p>" +
      "<p>La invocación idiomática es <code>&lt;form action={myServerFunction}&gt;</code>, o <code>formAction</code> en un botón de envío individual cuando un formulario necesita soportar múltiples acciones. Internamente, Next genera una referencia estable a la función y convierte el envío del formulario en un POST.</p>" +
      "<p><b>El hecho crítico de seguridad:</b> cada Server Function es <b>siempre un endpoint POST y siempre directamente alcanzable</b> desde fuera de tu UI — cualquiera que tenga la firma de solicitud puede llamarla con <code>curl</code>, evadiendo tu formulario, el estado deshabilitado de tu botón, tus verificaciones del lado del cliente completamente. Esto significa que <b>debes verificar autenticación y autorización dentro de la función misma</b>, cada vez, nunca confiando en \"el botón solo se renderiza para administradores\" como un límite de seguridad. El control del lado del cliente es UX, no defensa.</p>" +
      "<p>Múltiples llamadas a Server Functions despachadas desde el cliente se envían <b>secuencialmente, una a la vez</b> — no en paralelo — lo cual importa si estás disparando varias acciones en respuesta a una interacción y esperas que compitan.</p>" +
      "<p><code>useActionState</code> conecta una Server Function a estado pendiente/resultado para un formulario (reemplazando el viejo patrón de rastrear manualmente un booleano de carga y un resultado). <code>useOptimistic</code> te permite renderizar el resultado esperado de una acción inmediatamente, antes de que el servidor lo confirme, luego reconcilia una vez que la respuesta real regresa.</p>" +
      "<div class=\"callout warn\"><span class=\"lbl\">Trampa</span> \"Solo se llama desde un formulario que controlo\" no es un argumento de seguridad — las Server Functions son alcanzables por POST independientemente de tu UI. Verifica auth dentro de la acción, no solo alrededor del disparador.</div>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Las Server Functions son directamente alcanzables por POST independientemente de la UI que las dispara, así que verifico auth dentro de cada acción — y uso useActionState para pendiente/resultado y useOptimistic para la versión de retroalimentación instantánea.\"</div>",
  },
  {
    id: "st-10",
    num: "10",
    title: "10 · Mutaciones y revalidación",
    html:
      "<p><b>Núcleo:</b> después de una mutación, algo tiene que decirle al caché que sus datos están obsoletos. <code>revalidatePath(path)</code> invalida todo lo cacheado para una ruta específica — grueso pero simple. <code>revalidateTag(tag, profile)</code> invalida cada entrada de caché que carry esa etiqueta, dondequiera que viva en la aplicación — más granular, ya que una etiqueta puede abarcar muchas rutas.</p>" +
      "<table><tr><th>API</th><th>Comportamiento</th></tr>" +
      "<tr><td><code>revalidatePath</code></td><td>Invalidar por ruta</td></tr>" +
      "<tr><td><code>revalidateTag(tag, profile)</code></td><td>Invalidar por etiqueta — estilo SWR: el próximo visitante puede aún recibir datos obsoletos brevemente mientras se regenera</td></tr>" +
      "<tr><td><code>updateTag(tag)</code></td><td>Invalidar por etiqueta, solo para Server Actions — read-your-writes, refresca inmediatamente, sin ventana obsoleta</td></tr>" +
      "<tr><td><code>refresh()</code></td><td>Solo para Server Actions — refresca UI sin caché/dinámica en pantalla sin tocar el caché en absoluto</td></tr></table>" +
      "<p><b>Cambio de ruptura en Next 16:</b> <code>revalidateTag</code> ahora toma un segundo argumento requerido de perfil <code>cacheLife</code>, porque se comporta con semántica stale-while-revalidate — el perfil le dice cómo tratar la ventana de transición. Esto es diferente de <code>updateTag</code> (nuevo en 16, solo para Server Actions), que te da frescura inmediata, read-your-writes, sin ventana obsoleta, y <code>refresh()</code> (también nuevo en 16, también solo para Server Actions), que refresca solo las partes sin caché de la UI actual sin invalidar nada en el caché compartido.</p>" +
      "<p><code>redirect()</code> funciona lanzando una excepción de control de flujo especial que Next atrapa internamente — esto significa que el código después de una llamada <code>redirect()</code> nunca se ejecuta, y cualquier revalidación que necesites debe ocurrir <b>antes</b> de que lo llames, no después.</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"revalidatePath/revalidateTag son estilo SWR — revalidateTag ahora necesita un perfil cacheLife en Next 16 — mientras updateTag y refresh son las nuevas primitivas solo para Server Actions para frescura inmediata, read-your-writes. Y siempre revalide antes de redirigir, porque redirect lanza una excepción.\"</div>",
  },
  {
    id: "st-11",
    num: "11",
    title: "11 · Enrutamiento dinámico",
    html:
      "<p><b>Núcleo:</b> los corchetes en un nombre de carpeta crean un segmento dinámico. <code>[slug]</code> coincide exactamente con un segmento de ruta (<code>/blog/hello-world</code> → <code>slug: 'hello-world'</code>). <code>[...catchAll]</code> coincide con uno o más segmentos y los captura como un array (<code>/docs/a/b/c</code> → <code>['a','b','c']</code>) — pero <code>/docs</code> solo no coincidirá. <code>[[...optionalCatchAll]]</code> es lo mismo, pero además coincide con la ruta base con cero segmentos, haciendo que todo el array sea opcional.</p>" +
      "<p><code>generateStaticParams</code> es una función asíncrona exportada desde el <code>page.tsx</code> de una ruta dinámica que retorna la lista de valores de parámetros para prerenderizar en tiempo de compilación — el reemplazo del App Router para <code>getStaticPaths</code>. Los parámetros no retornados por ella dan 404 o se renderizan dinámicamente en la primera solicitud, dependiendo de la configuración.</p>" +
      "<p><code>params</code> (y <code>searchParams</code>) son <b>Promises</b> que deben esperarse (<code>await</code>) antes de usarse — esto aplica a cada segmento dinámico y cada página/layout que los lea.</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"[slug] es un segmento, [...catchAll] es uno-o-más capturado como un array, y la versión de doble corchete hace que ese array sea opcional — y generateStaticParams es cómo le digo a la compilación qué valores prerenderizar.\"</div>",
  },
  {
    id: "st-12",
    num: "12",
    title: "12 · Grupos de rutas, rutas paralelas y rutas interceptadas",
    html:
      "<p><b>Grupos de rutas</b> <code>(marketing)</code> organizan archivos y pueden definir <b>múltiples layouts raíz</b> — pon dos grupos de rutas directamente bajo <code>app/</code>, cada uno con su propio <code>layout.tsx</code> que contenga <code>&lt;html&gt;</code>/<code>&lt;body&gt;</code>, y obtienes, digamos, un shell completamente diferente para un sitio de marketing versus un dashboard, sin compartir nada en la raíz.</p>" +
      "<p><b>Rutas paralelas</b> usan una carpeta <code>@slot</code> para renderizar más de una página independiente en el mismo layout simultáneamente — un dashboard con un slot <code>@analytics</code> y un slot <code>@team</code> renderizados lado a lado, cada uno con su propio estado de carga/error y su propia sub-navegación. El layout recibe cada slot como una prop y lo coloca en JSX como cualquier otro hijo.</p>" +
      "<div class=\"callout warn\"><span class=\"lbl\">Cambio de ruptura en Next 16</b> Cada slot de ruta paralela ahora <b>requiere un <code>default.js</code> explícito</b> — anteriormente Next caía gracefully en algunos casos, pero ahora un slot sin uno causa un fallo de compilación. Agrega un <code>default.tsx</code> a cada carpeta <code>@slot</code>, aunque solo renderice <code>null</code>.</div>" +
      "<p><b>Rutas interceptadas</b> permiten que una ruta se renderice en el contexto del layout <i>actual</i> mientras aún actualiza la URL — el caso de uso clásico es una cuadrícula de fotos donde hacer clic en una miniatura la abre como modal (interceptada), pero una actualización dura o enlace directo a esa URL renderiza la página completa independiente en su lugar. La convención son prefijos de estilo de ruta relativa en el nombre de la carpeta: <code>(.)</code> intercepta un segmento en el <b>mismo</b> nivel, <code>(..)</code> un nivel <b>arriba</b>, <code>(..)(..)</code> dos niveles arriba, y <code>(...)</code> desde la <b>raíz</b>.</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Las rutas paralelas con @slots renderizan páginas independientes en un layout — y cada slot necesita su propio default.js ahora en Next 16 — mientras las rutas interceptadas con (.)/(..)/(...) me dan URLs reales para modales que aún caen a la página completa en actualización.\"</div>",
  },
  {
    id: "st-13",
    num: "13",
    title: "13 · proxy.ts (anteriormente middleware.ts)",
    html:
      "<p><b>Núcleo:</b> Next 16 renombró el archivo y la exportación — <code>middleware.ts</code> es ahora <code>proxy.ts</code>, y la función exportada <code>middleware</code> ahora se llama <code>proxy</code>. El renombrado es intencional: \"middleware\" implicaba que podía sentarse en cualquier parte del ciclo de solicitud/respuesta haciendo trabajo arbitrario, cuando en realidad esta capa es específicamente un límite de red que se ejecuta antes de que una solicitud llegue a tus rutas — <code>proxy</code> nombra eso con precisión.</p>" +
      "<p>El cambio funcional junto al renombrado: <code>proxy.ts</code> se ejecuta <b>solo en el runtime de Node.js</b> ahora — no hay opción de edge runtime para él. La ruta obsoleta <code>middleware.ts</code> aún existe y aún soporta edge, mantenido para proyectos que específicamente necesitan ejecución en edge, pero el código nuevo debería usar <code>proxy.ts</code> y aceptar el runtime de Node.js.</p>" +
      "<p><b>Usos comunes:</b> proteger rutas con auth (verificando una cookie de sesión antes de permitir que una solicitud pase y redirigiendo a <code>/login</code> si falta), pruebas A/B (reescribiendo una solicitud a una ruta variante basada en una cookie o encabezado), y reescritura/inyección de encabezados (agregando un id de solicitud, un nonce CSP, o normalizando un encabezado antes de que llegue a un route handler).</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Ahora es proxy.ts y export function proxy, no middleware — el renombrado hace explícito que es un límite de red, y en Next 16 es solo Node.js, sin más opción de edge para código nuevo.\"</div>",
  },
  {
    id: "st-14",
    num: "14",
    title: "14 · Route Handlers",
    html:
      "<p><b>Núcleo:</b> un archivo <code>route.ts</code> dentro de <code>app/</code> exporta funciones nombradas por verbo HTTP — <code>GET</code>, <code>POST</code>, <code>PUT</code>, <code>DELETE</code>, etc. — convirtiendo ese segmento en un endpoint API en lugar de una página. Una carpeta puede tener <code>page.tsx</code> o <code>route.ts</code> para un segmento dado, no ambos.</p>" +
      "<p><b>Cuándo usar cuál:</b> una Server Action / Server Function es la herramienta correcta cuando el llamador es tu propia UI y estás realizando una mutación desencadenada por interacción del usuario — te da ergonomía de llamada a función directa sin cableado manual de fetch/JSON. Un Route Handler es la herramienta correcta cuando necesitas un endpoint HTTP real — un objetivo de webhook para un servicio de terceros, una API consumida por un cliente no-Next (una aplicación móvil, otro servicio), o cuando necesitas control explícito sobre encabezados/códigos de estado/respuestas de streaming. Una simple llamada <code>fetch</code>/datos de Server Component es correcta cuando solo estás leyendo datos para renderizar — sin handler necesario en absoluto, ya que el componente mismo puede hablar con la base de datos o un servicio interno directamente.</p>" +
      "<p><b>Caché para handlers GET bajo Cache Components:</b> el output de un handler <code>GET</code> no se cachea implícitamente como podría haberlo estado bajo algunas convenciones en el viejo modelo — optas por ello explícitamente, de la misma manera que lo harías dentro de cualquier otra función: envuelve la lógica que produce datos en <code>\"use cache\"</code> (con <code>cacheLife</code>/<code>cacheTag</code> según sea necesario) si quieres que la respuesta se cachee, en lugar de depender del caché de configuración de ruta a nivel de framework.</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Server Actions para mutaciones desencadenadas desde mi propia UI, Route Handlers cuando necesito un endpoint HTTP real para un webhook o un cliente externo, y simples fetches de Server Component cuando solo estoy leyendo datos para renderizar.\"</div>",
  },
  {
    id: "st-15",
    num: "15",
    title: "15 · Metadatos y SEO",
    html:
      "<p><b>Núcleo:</b> exporta un objeto <code>metadata</code> estático desde un <code>page.tsx</code>/<code>layout.tsx</code> cuando los títulos/descripciones/etiquetas OG no dependen de datos — Next fusiona metadatos hacia abajo en el árbol de layouts, con segmentos hijos sobrescribiendo campos padres. Cuando los metadatos dependen de datos obtenidos (el título de una publicación de blog, el precio de un producto en la descripción), exporta una función asíncrona <code>generateMetadata()</code> en su lugar, que recibe los <code>params</code> de la ruta y puede esperar lo que necesite.</p>" +
      "<p><code>next/font</code> auto-hospeda archivos de fuente en tiempo de compilación — Google Fonts o archivos locales se descargan una vez durante la compilación y se sirven desde tu propio dominio, así que no hay solicitud de red externa a una CDN de fuentes en tiempo de ejecución (mejor privacidad, una ida y vuelta DNS/TLS menos) y no hay cambio de diseño por una fuente que se cambia tarde, ya que las métricas de la fuente se conocen y reservan de antemano.</p>" +
      "<p>Las imágenes de Open Graph y Twitter card pueden ser archivos estáticos (<code>opengraph-image.png</code> en una carpeta de ruta) o generarse dinámicamente con un archivo <code>opengraph-image.tsx</code> usando <code>ImageResponse</code>. <code>sitemap.ts</code> y <code>robots.ts</code> son archivos especiales que generan <code>/sitemap.xml</code> y <code>/robots.txt</code> programáticamente, para que puedan derivarse de tus datos de ruta reales (por ejemplo, cada publicación de blog) en lugar de mantenerse manualmente.</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Exportaciones de metadatos estáticos para contenido fijo, generateMetadata cuando depende de datos obtenidos, y next/font para que no haya cambio de diseño ni solicitud externa para tipografía.\"</div>",
  },
  {
    id: "st-16",
    num: "16",
    title: "16 · Optimización de imágenes y fuentes",
    html:
      "<p><b>Núcleo:</b> <code>next/image</code> reemplaza un <code>&lt;img&gt;</code> simple con un componente que genera un <code>srcset</code> responsivo automáticamente (para que el navegador descargue un tamaño apropiado para el viewport, no siempre la versión más grande), carga diferidamente imágenes fuera de pantalla por defecto, y previene cambios de diseño requiriendo width/height (o <code>fill</code>) de antemano para que el navegador reserve espacio antes de que la imagen cargue.</p>" +
      "<p>Las imágenes remotas deben estar en la lista de permitidos vía <code>images.remotePatterns</code> en <code>next.config.ts</code> — un patrón estructurado (protocolo, hostname, puerto, pathname) en lugar del viejo <code>images.domains</code> más grueso (obsoleto), que solo ponía en la lista de permitidos un hostname entero sin granularidad de ruta/protocolo.</p>" +
      "<p><b>Cambios de defecto de Next 16 que vale la pena conocer de memoria:</b> <code>minimumCacheTTL</code> (cuánto tiempo se cachea una variante de imagen optimizada) pasó de 60 segundos a <b>4 horas</b> — un defecto mucho más sensato para imágenes que rara vez cambian. Y la opción <code>qualities</code> se redujo a solo <code>[75]</code> por defecto, reduciendo el número de variantes de calidad distintas generadas (y cacheadas) por imagen a menos que configures explícitamente más.</p>" +
      "<p><code>next/font</code> (cubierto en la sección de metadatos) aplica aquí también — es la mitad de fuentes de la misma filosofía \"optimizar assets estáticos en tiempo de compilación, servir desde tu propio dominio\" que <code>next/image</code> aplica a imágenes.</p>" +
      "<div class=\"callout warn\"><span class=\"lbl\">Trampa</span> Olvidar <code>images.remotePatterns</code> para un nuevo host de imagen externo es uno de los errores más comunes de \"¿por qué mi imagen está rota en producción\" — el servidor de desarrollo a veces lo enmascara dependiendo de la configuración, pero una compilación de producción estricta se rehusará a optimizar un host no listado.</div>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"next/image me da srcset responsivo, carga diferida, y cero cambio de diseño gratis, y pongo en la lista de permitidos hosts remotos con remotePatterns en lugar de la lista obsoleta de domains.\"</div>",
  },
  {
    id: "st-17",
    num: "17",
    title: "17 · APIs Asíncronas de Solicitud",
    html:
      "<p><b>Núcleo:</b> <code>cookies()</code>, <code>headers()</code>, <code>draftMode()</code>, y los <code>params</code> y <code>searchParams</code> de cada ruta son todos <b>Promises</b> — esto solía ser una migración gradual (el acceso síncrono fue obsoleto con una advertencia por un tiempo), pero a partir de Next 16 <b>el acceso síncrono ha sido completamente removido</b>. Cada uno de estos debe esperarse (<code>await</code>) antes de poder leerse, tanto en Server Components como en Route Handlers.</p>" +
      "<div class=\"code\">export default async function Page({\n  params,\n  searchParams,\n}: PageProps&lt;'/blog/[slug]'&gt;) {\n  const { slug } = await params;\n  const { q } = await searchParams;\n  const store = await cookies();\n}</div>" +
      "<p>El razonamiento se conecta con Cache Components: hacer que estas APIs sean asíncronas, en lugar de sincronizarlas desde algún objeto de solicitud ambiental, es lo que le permite al framework razonar sobre un componente como \"esto espera algo dependiente de la solicitud\" versus \"esto no\" — que es exactamente la señal que el Prerenderizado Parcial necesita para decidir qué pertenece al shell estático versus un hueco dinámico.</p>" +
      "<p><code>next typegen</code> genera los tipos auxiliares para las rutas reales del proyecto — <code>PageProps</code>, <code>LayoutProps</code>, y <code>RouteContext</code> — parametrizados por el patrón de ruta literal (como <code>PageProps&lt;'/blog/[slug]'&gt;</code> arriba), para que <code>params</code>/<code>searchParams</code> regresen tipados correctamente para esa ruta específica en lugar de un genérico <code>Record&lt;string,string&gt;</code>.</p>" +
      "<div class=\"callout warn\"><span class=\"lbl\">Trampa</span> Código copiado de un ejemplo anterior de Next.js que lee <code>params.slug</code> síncronamente lanzará un error o se comportará silenciosamente mal en Next 16 — siempre <code>await params</code> primero.</div>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"cookies, headers, draftMode, params, y searchParams son todos asíncronos ahora — completamente asíncronos, sin fallback síncrono en Next 16 — y uso los PageProps/LayoutProps de next typegen para que regresen tipados correctamente por ruta.\"</div>",
  },
  {
    id: "st-18",
    num: "18",
    title: "18 · Patrones de autenticación y autorización",
    html:
      "<p><b>Núcleo:</b> la forma común es basada en sesión o cookie — al hacer login, establece una cookie HTTP-only (un id de sesión o un token firmado/cifrado) que el servidor lee en solicitudes subsiguientes para identificar al usuario. HTTP-only lo mantiene fuera del alcance del JavaScript del lado del cliente, lo cual importa para resistencia a XSS.</p>" +
      "<p><b>Defensa en profundidad en dos capas:</b> verificar la sesión en <code>proxy.ts</code> se trata de <b>UX y enrutamiento</b> — redirigir a un visitante no autenticado a <code>/login</code> antes de que siquiera vea el shell de una página protegida, o reescribir basado en rol. Pero verificar auth <i>otra vez</i> dentro del Server Component real, Route Handler, o Server Action es lo que provee <b>seguridad real</b> — porque, por la sección de Server Functions, esos son directa e independientemente alcanzables independientemente de cualquier lógica de proxy que esté frente a la UI. Trata las verificaciones a nivel de <code>proxy.ts</code> como una redirección agradable de tener, nunca como la única puerta.</p>" +
      "<p>En la práctica, la mayoría de aplicaciones de producción no crean gestión de sesiones manualmente — recurren a <b>Auth.js</code> (anteriormente NextAuth.js) para integración de proveedores OAuth, manejo de sesión/JWT, y persistencia basada en adaptadores, porque obtener rotación de tokens, protección CSRF, y particularidades de proveedores bien desde cero es mucho área superficial para poseer.</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Las verificaciones de auth a nivel de proxy son para UX y redirecciones; la verificación de seguridad real ocurre otra vez dentro de cada Server Component, acción, y route handler, porque esos son independientemente alcanzables — y en la práctica uso Auth.js en lugar de crear sesiones manualmente.\"</div>",
  },
  {
    id: "st-19",
    num: "19",
    title: "19 · Rendimiento y Turbopack",
    html:
      "<p><b>Núcleo:</b> Turbopack ahora es <b>estable y el empaquetador por defecto</b> tanto para <code>next dev</code> como <code>next build</code> en Next 16 — sin flag necesario para optar por ello. Es un empaquetador desde cero, basado en Rust, construido para computación incremental, y los números principales son aproximadamente <b>2–5x compilaciones de producción más rápidas</b> y hasta <b>10x Fast Refresh más rápido</b> comparado con webpack en aplicaciones grandes. Aún puedes optar por salir a webpack con <code>--webpack</code> si un proyecto depende de un plugin específico de webpack que no ha sido portado.</p>" +
      "<p><b>React Compiler</b> es estable y opt-in vía <code>reactCompiler: true</code> en <code>next.config.ts</code>. Analiza estáticamente el código del componente en tiempo de compilación e inserta memorización automáticamente — el <code>useMemo</code>/<code>useCallback</code>/<code>React.memo</code> que habrías escrito manualmente para evitar re-renderizados innecesarios se generan por ti, lo que tanto reduce boilerplate como captura oportunidades de memorización que un desarrollador probablemente omitiría o haría sutilmente mal.</p>" +
      "<p><b>Core Web Vitals</b> — LCP (carga), INP (interactividad, reemplazó FID), CLS (estabilidad visual) — permanecen como el marco estándar para rendimiento de usuario real, y la mayoría de lo que se cubre en otra parte de esta guía (Prerenderizado Parcial para shells rápidos, <code>next/image</code> para CLS, streaming para tiempo de carga percibido) está a su servicio.</p>" +
      "<p><b>Next 16 eliminó la métrica \"First Load JS\"</b> del output de compilación — era un proxy aproximado para el tamaño del bundle que no contaba bien para streaming, matices de división de código, o Cache Components. El reemplazo recomendado es medir lo que los usuarios realmente experimentan: <b>Lighthouse</b> para datos de laboratorio, <b>Vercel Analytics</b> (u otra herramienta RUM) para datos de campo.</p>" +
      "<p>Dos mecanismos adicionales de rendimiento de navegación que vale la pena nombrar: la <b>deduplicación de layouts</b> significa que un layout compartido no se re-obtiene/re-renderiza en cada navegación entre rutas hermanas — el router reconoce que no ha cambiado y lo reutiliza. La <b>precarga incremental</b> significa que el router precarga solo lo que se necesita para renderizar la próxima navegación probable (no todo el árbol de páginas ansiosamente), manteniendo el tráfico de precarga proporcional a lo que el usuario probablemente hará clic.</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Turbopack es el defecto ahora — estable, varias veces más rápidas las compilaciones y Fast Refresh — React Compiler automatiza la memorización si opto por ello, y como Next 16 eliminó First Load JS del output de compilación mido rendimiento real con Lighthouse y datos de campo en lugar de un proxy de compilación.\"</div>",
  },
  {
    id: "st-20",
    num: "20",
    title: "20 · Testing y despliegue",
    html:
      "<p><b>Núcleo:</b> los Client Components se testean como cualquier componente React — Jest o Vitest con React Testing Library, renderizando el componente, interactuando con él, y asertando sobre el DOM. Son unidades aisladas sin dependencia del servidor, así que esta capa es rápida y barata.</p>" +
      "<p>Los Server Components no pueden testearse unitariamente de la misma manera — no hay una historia simple de \"renderirlo en jsdom\" para un componente que está destinado a ejecutarse en un entorno servidor, esperar datos, y producir un payload RSC. La respuesta práctica es empujar la cobertura de Server Components un nivel más arriba, hacia <b>pruebas de integración o extremo a extremo</b> (Playwright es la elección común) que realmente ejecutan la aplicación — enrutamiento real, obtención de datos real (contra una base de datos de prueba o capa de red mockeada) — y asertar sobre la página renderizada, en lugar de intentar montar un Server Component de forma aislada.</p>" +
      "<p>La <b>forma de despliegue</b> es una elección de arquitectura real, no una idea posterior: <code>output: \"export\"</code> produce una exportación completamente estática — HTML/CSS/JS plano sin servidor Node.js requerido, desplegable a cualquier host estático o CDN — pero renuncia a cualquier cosa que necesite un servidor vivo: Server Actions, Route Handlers que hacen trabajo de servidor real, ISR/revalidación bajo demanda, y las APIs de solicitud asíncronas que dependen de una solicitud real. La alternativa es desplegar a una plataforma capaz de Node.js o Edge (Vercel, o un servidor Node auto-hospedado) que pueda ejecutar el conjunto completo de características.</p>" +
      "<p><b>Variables de entorno:</b> cualquier cosa que necesite llegar al navegador debe tener el prefijo <code>NEXT_PUBLIC_</code> — Next las inlinea en tiempo de compilación al bundle del cliente. Todo lo demás permanece solo-servidor por defecto y nunca se expone al cliente. El viejo mecanismo <code>serverRuntimeConfig</code>/<code>publicRuntimeConfig</code> de la era del Pages Router ya no existe — las variables de entorno son el único mecanismo soportado ahora.</p>" +
      "<p><b>Fundamentos de seguridad:</b> Next realiza una <b>verificación de encabezado Origin</b> en solicitudes de Server Actions como protección CSRF — una solicitud cuyo <code>Origin</code> no coincide con el origen esperado del despliegue se rechaza antes de que tu código de acción siquiera se ejecute. El paquete <code>server-only</code> (de la sección de Server/Client Components) protege contra un módulo solo-servidor — uno que lee secretos o habla con una base de datos — que termine accidentalmente en un bundle del cliente. Y la disciplina general es: nunca pases un secreto como prop a un Client Component, nunca registres un secreto donde herramientas visibles por el cliente puedan capturarlo, y trata <code>NEXT_PUBLIC_</code> como una puerta de un solo sentido — cualquier cosa con ese prefijo debe tratarse como pública para siempre.</p>" +
      "<div class=\"callout tip\"><span class=\"lbl\">Cómo decirlo</span> \"Los Client Components obtienen pruebas unitarias rápidas con RTL; los Server Components obtienen cobertura de integración/e2e con Playwright ya que no hay una manera significativa de renderizarlos unitariamente; y elijo exportación estática versus un servidor Node vivo basado en si la aplicación realmente necesita Server Actions o revalidación bajo demanda.\"</div>",
  },
];
