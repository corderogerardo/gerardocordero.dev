// Batch 11 — Writing a native module (Swift/Kotlin) and the Expo Modules API, bridging callbacks/promises/events to JS, and codegen specs for TurboModules/Fabric components.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED11_FLASHCARDS: Flashcard[] = [
  {
    id: "a11-native-modules-1",
    category: "native",
    categoryLabel: "Native",
    question: `In the New Architecture, why does the Expo Modules API let you write a synchronous native function callable from JS without a Promise, while legacy native modules could not?`,
    answerHtml: `<p>Sync calls matter because a Promise round trip adds a JS-thread tick you can't avoid — for a hot-path getter called every frame, that's the difference between smooth and janky. Legacy modules only had the async Bridge: every call serialized arguments to JSON, queued a message, and returned later, so even trivial getters had to be Promise-based. Expo Modules API sync functions run over <b>JSI</b>: a <code>Function("getX") { ... }</code> definition is exposed as a JS function backed by a C++ HostObject that calls into Swift/Kotlin directly on the same thread, no serialization, no round trip. This requires the New Architecture (JSI) to exist at all — Expo SDK 54+ and React Native 0.81+ dropped legacy-architecture support entirely, so on current tooling sync <code>Function</code> isn't an opt-in fallback over an old Bridge, it's simply the only architecture available; on older SDKs that still supported the legacy Bridge, a sync <code>Function</code> definition without JSI was not callable synchronously. <b>Sync Function works because JSI lets JS call directly into native on the same thread — no serialization, no Promise round trip.</b></p>`,
  },
  {
    id: "a11-native-modules-2",
    category: "native",
    categoryLabel: "Native",
    question: `What is the role of \`ModuleDefinition\` / the \`Module\` protocol's DSL (\`Name\`, \`Function\`, \`AsyncFunction\`, \`Events\`) in the Expo Modules API, and how does it differ from manually writing \`RCT_EXPORT_METHOD\`?`,
    answerHtml: `<p>The DSL's role is a declarative registry, not a binding mechanism you hand-write per method — that's what removes an entire category of casting bugs. The DSL (<code>ModuleDefinition</code> in Swift, the Kotlin builder in <code>Module()</code>) records metadata each block (<code>Name</code>, <code>Function</code>, <code>AsyncFunction</code>, <code>Property</code>, <code>Events</code>, <code>View</code>) declares, which the runtime introspects at module install time to auto-generate the JS-facing object and marshal argument/return types via Expo's <b>Convertibles</b> system. <code>RCT_EXPORT_METHOD</code>'s role, by contrast, was imperative glue: macros hand-generate Objective-C selectors that the legacy bridge invoked by name with manually unwrapped <code>NSDictionary</code>/<code>NSArray</code> args — there's no shared type-conversion layer, so each method does its own casting and validation. <b>The DSL trades hand-written bridging for a declarative schema the runtime marshals for you.</b></p>`,
  },
  {
    id: "a11-native-modules-3",
    category: "native",
    categoryLabel: "Native",
    question: `When would you choose \`AsyncFunction\` over \`Function\` in an Expo module, and what does the runtime do differently under the hood for each?`,
    answerHtml: `<p>Use <code>AsyncFunction</code> for any call that does real I/O, blocks, or must not run on the JS thread (file access, network, camera) — blocking the JS thread even briefly drops frames and freezes gesture handling app-wide, not just in your module. The Expo Modules API automatically wraps the native call's result in a JS Promise and dispatches the native body onto a background queue by default (Swift: chainable <code>.runOnQueue(.main)</code> to force main-thread execution; Kotlin: supports coroutines via <code>AsyncFunction("x") Coroutine { ... }</code>), then resolves/rejects back via JSI. <code>Function</code> (sync) executes immediately on the calling thread over JSI and blocks the JS thread until it returns — appropriate only for cheap, non-blocking work (it also supports at most 8 arguments).</p><p><b>Red flag:</b> reaching for sync <code>Function</code> because it "feels simpler" and calling a slow operation through it — that visibly freezes the UI since there's no thread hop to hide behind.</p><p><b>I use Function only for cheap, non-blocking reads; anything that touches disk, network, or a device API goes through AsyncFunction so it can't stall the JS thread.</b></p>`,
  },
  {
    id: "a11-native-modules-4",
    category: "native",
    categoryLabel: "Native",
    question: `How does an Expo Module emit an event to JS, and what's the JS-side contract for subscribing to it?`,
    answerHtml: `<p>The event contract exists so native code never has to know whether JS is even listening — that's what lets a module avoid burning battery on listeners nobody's using. Native side: declare <code>Events("onChange")</code> in the module definition, then call <code>sendEvent("onChange", ["value": x])</code> (Swift) or <code>sendEvent("onChange", bundleOf("value" to x))</code> (Kotlin) whenever the underlying system fires. The native layer also gets <code>OnStartObserving</code>/<code>OnStopObserving</code> hooks so you only attach expensive native listeners (e.g. \`CLLocationManager\`) while JS actually has at least one subscriber. JS side: use the \`useEvent\` hook from \`expo\` (recommended) or call \`addListener("onChange", cb)\` on the native module directly and clean up via the returned subscription's \`.remove()\` — this is the same emitter pattern \`expo-av\`/\`expo-location\` use instead of hand-rolled \`NativeEventEmitter\`. <b>Events are opt-in on both sides: native only fires while JS is subscribed, and JS always cleans up its subscription on unmount.</b></p>`,
  },
  {
    id: "a11-native-modules-5",
    category: "native",
    categoryLabel: "Native",
    question: `What problem does \`Codegen\` solve for TurboModules, and what's the actual build-time pipeline from a TS spec file to native code?`,
    answerHtml: `<p>Codegen exists to move a category of bugs from runtime crashes to build failures — hand-written bridging glue had no compiler checking that native code actually matched the JS-declared shape. The pipeline: 1. Author a \`NativeModuleName.ts\` spec using the restricted \`TurboModuleRegistry\`-compatible Flow/TS types. 2. At build time (Gradle task / Xcode build phase invoking \`react-native-codegen\`) the spec is parsed into a schema (\`schema.json\`). 3. The schema generates C++ <b>JSI</b> binding interfaces — \`NativeModuleNameCxxSpecJSI\` (an abstract base) and Java/Obj-C spec protocols. 4. Your native module class extends/conforms to that generated spec, so a mismatch between implementation and declared TS types fails the build, not a production call. <b>Codegen's job is to make a spec/implementation mismatch a compile-time error instead of a runtime crash.</b></p>`,
  },
  {
    id: "a11-native-modules-6",
    category: "native",
    categoryLabel: "Native",
    question: `Why must a TurboModule spec file use plain object types and avoid index signatures, unions of arbitrary shapes, or generics?`,
    answerHtml: `<p>The restriction exists because Codegen has to generate a fixed native struct for every type ahead of time — it can't defer to a runtime type check the way plain JS would. Codegen statically parses the spec's AST into its own schema (not a real TS compiler) and can only represent a fixed subset of types it knows how to marshal across JSI: primitives, plain objects (translated to C++ structs), specific array/tuple forms, function types, and a few sanctioned utility types (\`Partial\`, nullable \`?\`). Index signatures and open-ended generics have no fixed native struct shape, so Codegen can't generate a stable C++/Java/ObjC representation for them — using one fails codegen at build time, not at runtime, which is intentional: it pushes type errors to the earliest possible point. <b>If Codegen can't map a type to a fixed native struct ahead of time, it rejects the spec at build time rather than risk an unmarshalable value at runtime.</b></p>`,
  },
  {
    id: "a11-native-modules-7",
    category: "native",
    categoryLabel: "Native",
    question: `What is \`TurboModuleRegistry.getEnforcing<Spec>('ModuleName')\` doing differently from the legacy \`NativeModules.ModuleName\` lookup?`,
    answerHtml: `<p><code>NativeModules.ModuleName</code>'s role was an eager, always-on proxy: it loaded and wrapped every registered native module at bridge init, even ones a screen never used, paying that setup cost on every app launch. <code>TurboModuleRegistry.getEnforcing</code>'s role is lazy resolution: it resolves the named module through \`global.__turboModuleProxy\` only on first access, instantiating the native object on demand via JSI (falling back to the legacy \`NativeModules\` lookup if no Turbo proxy entry exists) — which is what makes startup faster under the New Architecture. The \`Enforcing\` suffix means it throws immediately (via an \`invariant\`) if the module isn't found by either path, instead of silently returning \`undefined\`/\`null\` like the legacy lookup could. <b>Turbo modules load on first use, not at bridge init — that's the startup win.</b></p>`,
  },
  {
    id: "a11-native-modules-8",
    category: "native",
    categoryLabel: "Native",
    question: `How does Fabric's codegen for native UI components differ from TurboModule codegen, and what native artifact does a \`codegenNativeComponent\` call ultimately produce?`,
    answerHtml: `<p>TurboModule codegen's role is binding a singleton-like native object's imperative methods/properties to JSI. Fabric component codegen's role is different: describing a tree node's props so the renderer can diff and lay it out off the JS thread. Concretely, Fabric codegen generates a <b>ComponentDescriptor</b> and a C++ <b>ShadowNode</b> subclass from the props interface declared via <code>codegenNativeComponent&lt;Props&gt;('ComponentName')</code>, plus the platform view manager scaffolding (\`...ComponentView\` on iOS conforming to \`RCTComponentViewProtocol\`, a \`ViewManager\`/\`ViewGroupManager\`-replacing Fabric view on Android). This lets Fabric build/diff/commit shadow trees and compute layout off the JS thread, whereas the legacy \`requireNativeComponent\` had no compile-time prop schema and relied on each \`ViewManager\`'s manually written \`@ReactProp\` setters. <b>TurboModule codegen binds methods to an object; Fabric codegen describes a node's props so layout can happen off the JS thread.</b></p>`,
  },
  {
    id: "a11-native-modules-9",
    category: "native",
    categoryLabel: "Native",
    question: `When implementing an Expo Module native View (\`View(MyViewType.self) { ... }\`), why does the Prop closure receive both the view instance and the new value, and what's a common mistake when handling prop updates that involve async work?`,
    answerHtml: `<p>The closure receives the view instance because it's invoked once per prop diff Fabric (or the legacy view manager) detects, and it has to mutate that concrete view directly (e.g. \`view.tintColor = value\`) — there's no other handle to it. \`Prop("propName") { view, value in ... }\` is that per-diff callback.</p><p><b>Red flag:</b> kicking off async work (image decode, network fetch) inside the prop setter without guarding against the view being recycled or unmounted before it completes. Fabric view recycling means the same native view instance can be reused for a different React element, so an unguarded completion handler can write stale data onto a now-different logical view.</p><p><b>Any async work started in a Prop setter has to check the view is still the one it was started for before writing the result.</b></p>`,
  },
  {
    id: "a11-native-modules-10",
    category: "native",
    categoryLabel: "Native",
    question: `What's the difference between \`OnCreate\`, \`OnDestroy\`, and \`OnAppContextDestroys\` lifecycle hooks in an Expo Module, and when would a module need \`OnAppContextDestroys\` specifically?`,
    answerHtml: `<p>The distinction matters because a resource scoped to the JS runtime outlives any single module instance — releasing it at the wrong hook leaks it across reloads. <code>OnCreate</code>/<code>OnDestroy</code> fire with the module instance's own lifecycle (e.g. tied to a \`Fragment\`/\`Activity\` in Expo Go's multi-app-context world, or simply module init/teardown in a standalone app). <code>OnAppContextDestroys</code> fires when the entire \`AppContext\` (the JS runtime/bridge instance) is torn down — relevant mainly in Expo Go or \`expo-dev-client\`, where multiple JS apps can be loaded/unloaded in the same native process over the host's lifetime; a module holding a native resource scoped to the whole runtime (a shared cache, a global listener) must release it here, not in \`OnDestroy\`, or it leaks across app reloads. <b>Anything scoped to the runtime, not the module instance, gets released in OnAppContextDestroys.</b></p>`,
  },
  {
    id: "a11-native-modules-11",
    category: "native",
    categoryLabel: "Native",
    question: `Why does the Expo Modules API require config plugins for modules that touch \`Info.plist\`, \`AndroidManifest.xml\`, or native build files, and how does this differ from manually editing those files in a bare workflow?`,
    answerHtml: `<p>Config plugins exist because CNG treats \`ios/\` and \`android/\` as disposable build output, not source — Expo's managed/Continuous Native Generation (CNG) workflow regenerates them from \`app.json\` plus installed config plugins on every \`prebuild\`. A module that needs a permission string, URL scheme, or Gradle dependency ships an \`app.plugin.js\` config plugin using \`withInfoPlist\`/\`withAndroidManifest\`/\`withDangerousMod\` (from \`@expo/config-plugins\`) so the change is declarative and replayed on every prebuild. In a fully bare (non-CNG) workflow this isn't strictly required since the native projects are hand-maintained and committed, but most production Expo apps use CNG, making the plugin the only durable place to put the change.</p><p><b>Red flag:</b> hand-editing \`Info.plist\` or \`AndroidManifest.xml\` directly in a CNG project — it works until the next \`prebuild\` silently wipes it.</p><p><b>Any native config a module needs ships as a config plugin, not a manual edit, so it survives the next prebuild.</b></p>`,
  },
  {
    id: "a11-native-modules-12",
    category: "native",
    categoryLabel: "Native",
    question: `What does \`expo-module.config.json\` do, and why does autolinking need it instead of just scanning \`node_modules\` for any package with native code?`,
    answerHtml: `<p>Autolinking needs an explicit manifest because folder-convention guessing breaks the moment a monorepo, scoped package, or platform-limited module shows up — \`expo-module.config.json\` exists to make the registration explicit instead of inferred. It declares which platforms a module supports (\`platforms\`) and which native module/view classes to register — iOS by bare class name under \`apple.modules\`, Android by fully-qualified class name (package + class) under \`android.modules\` — plus other platform-specific keys. Expo's autolinking CLI (\`expo-modules-autolinking\`) reads this file across \`node_modules\` to build the dependency graph and generate the \`ExpoModulesProvider\`/registry glue (a generated \`Podfile\`/Gradle \`settings.gradle\` include list and a generated Swift/Kotlin registry file) at prebuild/pod-install time. <b>The manifest makes autolinking explicit and declarative instead of a folder-convention guess that breaks in monorepos.</b></p>`,
  },
  {
    id: "a11-native-modules-13",
    category: "native",
    categoryLabel: "Native",
    question: `In a Kotlin Expo Module, why is \`appContext.currentActivity\` potentially null inside an \`AsyncFunction\`, and what's the correct pattern to handle a function that needs an Activity (e.g. to launch an intent)?`,
    answerHtml: `<p><code>currentActivity</code> can be null because it tracks the foreground Activity via the host's lifecycle listeners, and that tracking can legitimately come up empty — the call races app backgrounding, a process restart, or runs from a background JS task/headless context where no Activity is attached.</p><p><b>Red flag:</b> force-unwrapping it (\`appContext.currentActivity!!\`). A crash here takes down the whole native process, not just the JS layer, because it's outside the catchable-JS-exception boundary. The correct pattern is to null-check and reject the promise (or throw a \`CodedException\`): \`val activity = appContext.currentActivity ?: throw MissingActivity()\` — \`MissingActivity\` is a real \`CodedException\` subclass expo-modules-core provides for exactly this (message: "The current activity is no longer available").</p><p><b>Anything that can be null gets a null-check and a rejected promise, never a force-unwrap, when a crash there takes down the whole app.</b></p>`,
  },
  {
    id: "a11-native-modules-14",
    category: "native",
    categoryLabel: "Native",
    question: `How do you bridge a native error to JS so it shows up as a rejected Promise with a meaningful \`code\` and \`message\` in both legacy and Expo Modules API code?`,
    answerHtml: `<p>Expo Modules API's role here is automatic: throw a \`Swift.Error\`/Kotlin \`CodedException\` subclass from inside \`AsyncFunction\` and the runtime auto-catches it, rejecting the JS Promise with \`.code\` and \`.message\` derived from the exception — no manual \`reject()\` plumbing. Legacy bridge modules' role was manual: you call the \`RCTPromiseRejectBlock\` directly — \`reject(@"E_CODE", @"message", error)\` (Obj-C) or \`promise.reject("E_CODE", "message", throwable)\` (Kotlin) on every code path.</p><p><b>Red flag:</b> a legacy code path that returns early without calling either \`resolve\` or \`reject\` — the JS-side Promise hangs permanently pending, a classic legacy-bridge bug that Codegen/Expo's auto-handling eliminates by construction.</p><p><b>Throwing a CodedException from AsyncFunction is enough — the runtime turns it into a rejected promise for you.</b></p>`,
  },
  {
    id: "a11-native-modules-15",
    category: "native",
    categoryLabel: "Native",
    question: `What's the practical difference between \`JavaScriptObject\`/\`JavaScriptFunction\` types available in Expo Modules API Kotlin/Swift code and calling back into JS via a stored callback in the legacy bridge?`,
    answerHtml: `<p>The legacy callback's role was a one-shot delivery mechanism: \`RCTResponseSenderBlock\` / a stored \`Callback\` arg sends a single serialized, queued message back across the bridge — fire it more than once and React Native throws, and there's no way to hold a long-lived reference to a JS function. Expo's JSI-backed \`JavaScriptObject\`/\`JavaScriptFunction\` types have a different role: a live, reusable handle. Native code holds a real reference to an actual JS object/function (e.g. as a sync \`Function\` argument: \`Function("callback") { fn: JavaScriptFunction<String> in fn("arg1", "arg2") }\`) and can invoke it repeatedly, synchronously, without the serialization/queueing the legacy callback model required — closer to how JSI lets C++ hold a \`jsi::Function\` directly. <b>Legacy callbacks fire once; JSI function references can be called as many times as you need.</b></p>`,
  },
  {
    id: "a11-native-modules-16",
    category: "native",
    categoryLabel: "Native",
    question: `Why does Fabric require old-architecture view managers to migrate \`@ReactProp\`/\`@ReactPropGroup\` Java annotations into Codegen-derived prop structs instead of just continuing to read props from a generic \`ReactStylesDiffMap\`?`,
    answerHtml: `<p>Migration is forced because Fabric's \`ShadowNode\`/\`ComponentDescriptor\` model computes and diffs props off the JS thread inside C++, before the view is even created on the UI thread — that requires a strongly-typed, codegen-known prop struct (\`MyComponentProps\` derived from the TS \`Props\` interface) to populate during the commit phase, not a runtime map to reflect over. \`ReactStylesDiffMap\`'s role was the opposite: a loosely-typed runtime map the legacy \`ViewManager.updateProperties\` walked reflectively via \`@ReactProp\` annotations on the UI thread. That per-prop reflection and lack of a compile-time-known prop schema is exactly what made the legacy path slow, and what Fabric's interop layer (\`fabric-interop\`) has to paper over for unmigrated components. <b>Fabric needs a typed prop struct before the view exists; the legacy path could get away with reflecting over props after the fact.</b></p>`,
  },
  {
    id: "a11-native-modules-17",
    category: "native",
    categoryLabel: "Native",
    question: `What does the Expo Modules API's \`Convertibles\`/\`ConvertibleArgument\` type-conversion layer actually do when a JS object is passed into a \`Record\`-typed Swift struct argument?`,
    answerHtml: `<p>Convertibles exist to move validation to the call boundary instead of letting a malformed value crash deep inside your function body on an unguarded force-unwrap. When you declare a Swift \`Record\` (conforming to \`Record\` protocol with \`@Field\` property wrappers) as a \`Function\`/\`AsyncFunction\` parameter type, the Expo Modules API's Convertibles layer (\`Convertible\`/\`ConvertibleArgument\` in \`expo-modules-core\`) inspects the JSI value at the call boundary, validates each field against its declared type (with optional/default handling), and constructs the Swift struct automatically — equivalent to what Codegen generates for TurboModule structs, but resolved dynamically at runtime via reflection-like field wrappers rather than ahead-of-time C++ generation. <b>Convertibles validate at the boundary, so a malformed JS object throws a clear type error there instead of crashing deep inside the function.</b></p>`,
  },
  {
    id: "a11-native-modules-18",
    category: "native",
    categoryLabel: "Native",
    question: `Why is it dangerous for a TurboModule's native implementation to hold a strong reference to the \`CallInvoker\` or JSI \`Runtime\` past the lifetime of the bridge/instance, and what's the safe pattern for an event emitter held by long-lived native code (e.g. a singleton SDK wrapper)?`,
    answerHtml: `<p>The danger is scope mismatch: the JSI \`Runtime\` and its \`CallInvoker\` are scoped to one JS instance and torn down on JS reload (fast refresh, RN bridge reload, or backgrounding on some platforms), but a singleton SDK wrapper outlives any single JS instance by design.</p><p><b>Red flag:</b> caching a raw \`jsi::Function\` or \`Runtime*\` on that singleton. Calling into a destroyed runtime from native code crashes the whole process — it's not a catchable JS exception. The safe pattern: hold a <i>weak</i> reference to the runtime/CallInvoker (or none at all, going through Expo's \`sendEvent\` which already guards this) and check liveness/dispatch through the invoker's thread-safe \`invokeAsync\` before touching JSI objects.</p><p><b>A long-lived native singleton never owns a strong reference to the Runtime — it dispatches through a CallInvoker that can tell if the runtime is still alive.</b></p>`,
  },
  {
    id: "a11-native-modules-19",
    category: "native",
    categoryLabel: "Native",
    question: `What's the distinction between \`expo-modules-core\`'s \`SharedObject\` and a plain native class wrapped in an Expo Module, and what problem does \`SharedObject\` solve for passing native handles (e.g. a GL texture, a file handle) to JS?`,
    answerHtml: `<p>The problem \`SharedObject\` solves is identity: a plain native class exposed only through module functions has no independent JS-side handle, so every call has to re-look-up the resource by some ID you manage yourself. \`SharedObject\` gives a native object a corresponding lightweight JS-side instance Expo manages, so you can return it from one function and pass it into another (e.g. a \`Class("Context", ImageContext.self) { Constructor { ... } }\` factory returns a shared instance, later functions take it back as an argument) with the JSI plumbing and lifecycle (the instance is released once neither JS nor native code holds a reference — \`sharedObjectDidRelease()\` on Kotlin for cleanup) handled for you — this is the mechanism \`expo-av\`/\`expo-camera\`-style APIs that hand back "native instances" to JS are built on. <b>SharedObject gives a native handle a real JS-side identity, so you pass the object itself instead of managing an ID yourself.</b></p>`,
  },
  {
    id: "a11-native-modules-20",
    category: "native",
    categoryLabel: "Native",
    question: `When a New Architecture app still has some libraries only providing legacy native modules, how does interop work, and what's the actual performance implication of leaving a module un-migrated?`,
    answerHtml: `<p>Interop exists so adoption isn't all-or-nothing: React Native ships a Turbo/Fabric interop layer that wraps legacy \`NativeModules\`-registered modules so \`TurboModuleRegistry\` lookups still resolve them, and wraps legacy \`ViewManager\`s so Fabric can mount them inside the new renderer — this is why most apps could adopt the New Architecture before every dependency had migrated.</p><p><b>Red flag:</b> assuming interop means migration is optional for performance, not just correctness. An interop-wrapped legacy module still pays the JSON-serialization tax for its calls (it's bridged through the old async path internally), and an interop-wrapped view loses Fabric's synchronous layout/measurement benefits for that subtree — the New Architecture's perf gains for that piece aren't realized until it's actually migrated. As of Expo SDK 54+/RN 0.81+, the legacy architecture itself has been removed, so this interop layer is now the only path for any remaining un-migrated legacy module rather than one of two supported architectures.</p><p><b>Interop makes an un-migrated module work under the New Architecture; it doesn't make it fast under it.</b></p>`,
  },
  {
    id: "a11-native-modules-21",
    category: "native",
    categoryLabel: "Native",
    question: `Why does Codegen need a dedicated \`codegenConfig\` block in \`package.json\` (with \`name\`, \`type\`, \`jsSrcsDir\`), and what breaks if a library's \`NativeFoo.ts\` spec file isn't under the declared \`jsSrcsDir\`?`,
    answerHtml: `<p>\`codegenConfig\` exists because codegen runs per-dependency at app build time and can't afford to walk every file in \`node_modules/<pkg>\` looking for specs — it tells the CLI exactly where to look for \`Native*.ts\`/\`*NativeComponent.ts\` files.</p><p><b>Red flag:</b> a spec file living outside the declared \`jsSrcsDir\`. Codegen simply never discovers it — no error is thrown at build time, the module just silently has no generated spec — and \`TurboModuleRegistry.getEnforcing\` fails at runtime with "could not be found" even though the package and types look correct. It's a notoriously confusing failure mode when authoring a new native module, precisely because the failure surfaces far from the misconfiguration.</p><p><b>If a Turbo module can't be found at runtime despite correct types, check jsSrcsDir before anything else.</b></p>`,
  },
  {
    id: "a11-native-modules-22",
    category: "native",
    categoryLabel: "Native",
    question: `In Swift, why might you implement \`definition()\` to call \`OnStartObserving\`/\`OnStopObserving\` rather than always running a native listener (e.g. \`CMMotionManager\`) from \`OnCreate\`?`,
    answerHtml: `<p>The hooks exist because sensors and OS-level listeners (motion, location, NFC) carry a real power/CPU cost and, on iOS, can trigger permission-usage indicators (e.g. the location arrow) even when no JS code is actually consuming the data. \`OnStartObserving\`/\`OnStopObserving\` fire exactly when the JS-side listener count goes from zero to one and back to zero, so the native sensor is only active while something in JS is actually subscribed.</p><p><b>Red flag:</b> starting the sensor unconditionally from \`OnCreate\` instead — that keeps it (and its battery/privacy cost) alive for the module's entire lifetime regardless of whether anyone's listening.</p><p><b>A sensor only runs while OnStartObserving says JS is actually listening — never from module init.</b></p>`,
  },
  {
    id: "a11-native-modules-23",
    category: "native",
    categoryLabel: "Native",
    question: `What's the difference between a TurboModule's \`getConstants()\` (legacy bridge concept) and how the Expo Modules API exposes static/constant values to JS?`,
    answerHtml: `<p>Legacy bridge modules' role: implement \`getConstants()\` (Obj-C \`+ (NSDictionary *)constantsToExport\`, or its Java \`getConstants()\` equivalent), which the bridge called once at module init and froze onto the JS-side module object — useful for config values that never change per call, avoiding a round trip per access. TurboModules replaced this with a \`getConstants\` method directly on the generated spec (still called once, but now JSI-typed). The Expo Modules API's role is the same intent, declarative syntax: \`Constant("PI") { 3.14159 }\` inside the module definition DSL — computed once on first access and then cached. <b>Constants are computed once and cached, never re-derived per JS access, on both the legacy and Expo Modules paths.</b></p>`,
  },
];

export const ADVANCED11_QUIZ: QuizQuestion[] = [
  {
    id: "a11q-native-modules-1",
    category: "native",
    categoryLabel: "Native",
    question: `A TurboModule spec (\`NativeFoo.ts\`) declares a method \`doThing(options: {id: string, count?: number}): Promise<void>\`. At build time, what does react-native-codegen actually do with this signature?`,
    options: [`It type-checks the implementation file against the spec using the TypeScript compiler`, `It parses the spec's AST into an internal schema and generates JSI-binding C++/Java/ObjC interfaces the native implementation must conform to`, `It transpiles the TS file directly into a native Swift/Kotlin module so no manual implementation is needed`, `It generates only TypeScript .d.ts files for editor autocomplete; native binding is still hand-written`],
    answer: 1,
    explanationHtml: `<p>Codegen uses its own restricted parser (not \`tsc\`) to read the spec file into a schema, then emits generated native scaffolding (e.g. \`NativeFooCxxSpecJSI\`, Java/ObjC spec interfaces) that your real native module implementation must conform to — it generates the binding layer, not the business logic, so it isn't doing general TS type-checking (ruling out option 0), and it isn't transpiling your TS into a working Swift/Kotlin implementation either (ruling out option 2) — you still write that by hand against the generated spec.</p>`,
  },
  {
    id: "a11q-native-modules-2",
    category: "native",
    categoryLabel: "Native",
    question: `Why does the Expo Modules API distinguish \`Function\` from \`AsyncFunction\` rather than always wrapping native calls in a Promise like the legacy bridge did?`,
    options: [`AsyncFunction is only available on Android; iOS modules must use Function exclusively`, `Function calls go over JSI synchronously on the calling thread, so they're for cheap non-blocking work; AsyncFunction returns a Promise and runs off the JS thread for I/O or blocking work`, `Function and AsyncFunction are purely stylistic aliases with identical runtime behavior`, `Function is for void-returning calls and AsyncFunction is required whenever a value is returned`],
    answer: 1,
    explanationHtml: `<p>\`Function\` is invoked synchronously through JSI on the calling (JS) thread — good only for fast, non-blocking work (and limited to 8 arguments), since it stalls JS until it returns. \`AsyncFunction\` dispatches the native body to a background queue by default and resolves a JS Promise, which is required for any I/O-bound or slow native call so the JS thread isn't blocked. Neither is platform-restricted (ruling out option 0) or a mere stylistic alias (option 2) — they run on genuinely different threads/paths — and the split is about blocking behavior, not whether a value is returned (ruling out option 3).</p>`,
  },
  {
    id: "a11q-native-modules-3",
    category: "native",
    categoryLabel: "Native",
    question: `A native module singleton wrapper caches a raw pointer to the JSI \`Runtime\` the first time it's initialized, intending to push events into JS later from a background thread. What's the main risk with this design?`,
    options: [`JSI Runtime objects are immutable, so caching a pointer to them has no risk at all`, `The Runtime can be torn down on JS reload/reinstantiation; calling into a stale Runtime from native code crashes the process rather than throwing a catchable JS error`, `JSI doesn't allow background-thread access under any circumstances, so the code won't compile`, `The Runtime pointer changes value on every render, making the cache immediately stale but harmless`],
    answer: 1,
    explanationHtml: `<p>The JSI Runtime is scoped to a JS instance and gets destroyed on reload/fast refresh or instance teardown — it isn't immutable or crash-proof to reference indefinitely (ruling out option 0), and nothing about JSI prevents compiling background-thread code (ruling out option 2) or makes a stale pointer "harmless" (ruling out option 3). Native code holding a stale pointer and dispatching into it later causes a hard native crash, not a recoverable JS-side exception — the safe pattern is to route through a CallInvoker that checks liveness, or to use Expo's managed \`sendEvent\` which guards this for you.</p>`,
  },
  {
    id: "a11q-native-modules-4",
    category: "native",
    categoryLabel: "Native",
    question: `Under the New Architecture, how does Fabric obtain a compile-time-known prop schema for a native view component, in contrast to the legacy \`requireNativeComponent\`/\`ViewManager\` approach?`,
    options: [`Fabric reflects on the Java/Swift ViewManager class at runtime to discover @ReactProp-annotated setters, same as before`, `Codegen parses a TS Props interface passed to codegenNativeComponent and generates a ComponentDescriptor/ShadowNode/typed prop struct used during the commit phase`, `Fabric requires all props to be passed as a single untyped JSON string parsed natively at render time`, `Props are inferred at runtime from the first set of values passed from JS, with no static schema`],
    answer: 1,
    explanationHtml: `<p>Fabric component codegen turns the \`Props\` TS interface given to \`codegenNativeComponent<Props>('Name')\` into a generated \`ComponentDescriptor\` and C++ \`ShadowNode\` with a typed prop struct, so props are diffed/known ahead of view creation — not reflected over at runtime the way the legacy \`@ReactProp\`-annotated setters were (ruling out option 0), not passed as an untyped JSON blob (option 2), and not inferred from whatever values happen to arrive first (option 3). The legacy path instead reflected over \`@ReactProp\`-annotated setters on the UI thread with a loosely-typed \`ReactStylesDiffMap\`, which had no compile-time prop schema.</p>`,
  },
  {
    id: "a11q-native-modules-5",
    category: "native",
    categoryLabel: "Native",
    question: `An Expo Module's Kotlin \`AsyncFunction\` calls \`appContext.currentActivity!!.startActivity(intent)\` and occasionally crashes the whole app in production, though it works fine in dev. What's the most likely root cause and correct fix?`,
    options: [`Kotlin's !! operator is deprecated and should be replaced with .let{} for all nullable types`, `currentActivity can legitimately be null (e.g. call races backgrounding or a process restart); force-unwrapping crashes the native process, so it should be null-checked and a CodedException thrown/promise rejected instead`, `AsyncFunction bodies cannot call startActivity at all; it must be moved into a Function (sync) block`, `The crash is unrelated to currentActivity and is actually caused by the Intent object being non-Parcelable`],
    answer: 1,
    explanationHtml: `<p><code>currentActivity</code> is tracked via lifecycle callbacks and can be null when no Activity is currently in the foreground (background task, process restart racing the call, etc.) — that's the actual root cause, not a deprecated-operator style issue (option 0), a restriction on what AsyncFunction can call (option 2 — calling \`startActivity\` from AsyncFunction is fine), or a Parcelable issue with the Intent (option 3). Force-unwrapping with \`!!\` crashes the entire native process rather than failing just the JS call — the fix is to null-check and reject the promise or throw a \`CodedException\` subclass such as expo-modules-core's \`MissingActivity\`.</p>`,
  },
  {
    id: "a11q-native-modules-6",
    category: "native",
    categoryLabel: "Native",
    question: `A team adopts the New Architecture but one dependency still only ships a legacy \`NativeModules\`-registered module with no TurboModule spec. What actually happens when JS calls \`TurboModuleRegistry.getEnforcing('LegacyLib')\`?`,
    options: [`The call throws immediately at app startup because legacy modules are incompatible with the New Architecture and must be removed`, `The Turbo interop layer wraps the legacy module so the lookup still resolves (getEnforcing falls back to the legacy NativeModules registry when no Turbo proxy entry exists), but calls still pay the old JSON-serialization/async-bridge cost internally rather than getting JSI's direct-call benefit`, `The module is automatically and transparently recompiled into a TurboModule at build time with zero performance difference`, `getEnforcing silently returns undefined, requiring a manual fallback to the legacy NativeModules export`],
    answer: 1,
    explanationHtml: `<p>\`TurboModuleRegistry\`'s internal lookup tries the Turbo proxy first and falls back to the legacy \`NativeModules\` registry if no Turbo entry exists, so legacy modules keep resolving under the New Architecture — they don't throw at startup (ruling out option 0), get silently and transparently recompiled into a real TurboModule (option 2 — no such auto-conversion exists), or resolve to \`undefined\` (option 3). This is why incremental adoption was possible, but it's not free — the wrapped module's calls are still bridged through the old async/serialized path internally, so the New Architecture's perf benefit for that specific module isn't realized until it's actually migrated to a TurboModule spec.</p>`,
  },
  {
    id: "a11q-native-modules-7",
    category: "native",
    categoryLabel: "Native",
    question: `Why does authoring a native module for an Expo managed/CNG (Continuous Native Generation) app typically require an \`app.plugin.js\` config plugin to add an Info.plist key or AndroidManifest permission, instead of just editing those files directly?`,
    options: [`Expo CNG regenerates ios/ and android/ from app.json plus installed config plugins on every prebuild, so direct edits to Info.plist/AndroidManifest.xml are overwritten; the plugin makes the change declarative and replayed each prebuild`, `Direct XML/plist editing is technically impossible in any Expo project regardless of workflow`, `Config plugins are required only for App Store submission metadata, not for native module functionality`, `Info.plist and AndroidManifest.xml are auto-generated purely from package.json dependencies with no plugin involvement`],
    answer: 0,
    explanationHtml: `<p>In the managed/CNG workflow, \`ios/\` and \`android/\` are treated as build artifacts regenerated from \`app.json\` and installed config plugins on every \`expo prebuild\`. A manual edit to \`Info.plist\` or \`AndroidManifest.xml\` survives only until the next prebuild wipes it — direct editing isn't technically impossible (ruling out option 1), config plugins aren't limited to App Store metadata (option 2), and these files aren't generated purely from \`package.json\` with no plugin involvement (option 3) — a config plugin using \`withInfoPlist\`/\`withAndroidManifest\` makes the modification declarative so it's correctly reapplied every time.</p>`,
  },
];
