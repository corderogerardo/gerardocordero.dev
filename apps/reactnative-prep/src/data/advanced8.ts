// Batch 8 — New Architecture internals: JSI direct calls, Fabric's shadow tree and concurrent rendering, TurboModules lazy loading & Codegen, and migrating a legacy bridge module.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED8_FLASHCARDS: Flashcard[] = [
  {
    id: "a8-new-arch-1",
    category: "rn",
    categoryLabel: "RN",
    question: `Why is JSI's elimination of JSON serialization more than a perf win — what new capability does it unlock that the bridge couldn't support?`,
    answerHtml: `<p>The legacy bridge marshalled every cross-language call into a JSON string queued on an async message loop, so native calls were inherently <b>async and batched</b> — there was no way to call native and get a value back in the same tick. JSI exposes C++ <code>HostObject</code>s directly as live references in the JS heap holding real function pointers, so JS can call native <b>synchronously and read a return value immediately</b>. That's what makes <code>react-native-reanimated</code> running worklets on the UI thread, or <code>react-native-mmkv</code>'s synchronous storage reads, possible — neither could exist on the old bridge.</p>`,
  },
  {
    id: "a8-new-arch-2",
    category: "rn",
    categoryLabel: "RN",
    question: `A JSI HostObject holds native state reachable from JS. What's the classic lifecycle bug this creates, and how do TurboModule authors avoid it?`,
    answerHtml: `<p>If a native object holds a strong reference to a JS callback (e.g. a retained <code>jsi::Function</code>) and that JS side also indirectly keeps the native object alive, you get a <b>cross-boundary retain cycle</b> that neither the JS GC nor C++ refcounting can see in full, since each only owns its half. The fix is to hold callbacks as <code>jsi::WeakObject</code> where the native side doesn't need a strong hold, and to give native objects an explicit <code>invalidate()</code>/teardown hook tied to a deterministic lifecycle event (e.g. host destruction) rather than trusting JS garbage collection timing.</p>`,
  },
  {
    id: "a8-new-arch-3",
    category: "rn",
    categoryLabel: "RN",
    question: `Fabric's shadow tree is described as immutable. What does that mean precisely, and why does it make concurrent rendering safe?`,
    answerHtml: `<p>Each <code>ShadowNode</code> is immutable once committed — any prop or layout change produces a <b>new node (and new ancestor chain up to the root)</b> rather than mutating in place, following React's own "clone on write" fiber model. Because the render phase only ever reads from a snapshot tree and writes to a fresh one, React can pause, abandon, resume, or run renders <b>concurrently/off the JS thread without ever exposing a half-mutated tree</b> to the layout or commit phases. The committed tree is swapped atomically, so there's no lock contention and no torn reads between the JS, UI (layout), and main (mounting) threads.</p>`,
  },
  {
    id: "a8-new-arch-4",
    category: "rn",
    categoryLabel: "RN",
    question: `Name the three Fabric tree stages and what each one is for.`,
    answerHtml: `<p><ul><li><b>Render (current) tree</b> — the last tree actually committed and visible on screen.</li><li><b>Work-in-progress tree</b> — built during a render pass off-thread; can be thrown away if React decides to abandon/restart the work (e.g. a higher-priority update interrupts it).</li><li><b>Committed tree</b> — the work-in-progress tree promoted atomically once rendering finishes, becoming the new current tree, then diffed for mounting/layout updates.</li></ul>This three-stage pipeline (matching React Native's documented Render → Commit → Mount pipeline) is what lets Fabric do React 18 concurrent features (e.g. transitions) without blocking the UI thread.</p>`,
  },
  {
    id: "a8-new-arch-5",
    category: "rn",
    categoryLabel: "RN",
    question: `Where does layout actually happen in Fabric, and why was moving it off the JS thread architecturally significant?`,
    answerHtml: `<p>Layout is computed by <b>Yoga running in C++</b> directly against the shadow tree on a background thread, not by JS and not on the bridge. Under the legacy architecture, layout measurements (e.g. <code>onLayout</code>, <code>measure()</code>) had to round-trip through the async bridge, adding a frame or more of latency. With Fabric, the shadow tree and Yoga live in the same C++ layer that JSI exposes, so layout can be computed synchronously relative to a commit and results are available to native views without waiting on a JS tick.</p>`,
  },
  {
    id: "a8-new-arch-6",
    category: "rn",
    categoryLabel: "RN",
    question: `What is the C++ "MountingCoordinator" responsible for in Fabric, and why can't the UI thread just apply the work-in-progress tree directly?`,
    answerHtml: `<p>The <code>MountingCoordinator</code> (in <code>ReactCommon/react/renderer/mounting/</code>) computes the diff between the previous committed tree and the newly committed tree and produces an ordered list of <b>mutation instructions</b> (create/insert/remove/update/delete view) that the platform-specific mounting layer applies to actual <code>UIView</code>/<code>View</code> instances. It can't apply the WIP tree directly because rendering happens off the main thread for concurrency, while mounting (touching real native views) must happen on the main/UI thread — the coordinator is the synchronization point that hands off a safe, ordered diff once a commit is finalized.</p>`,
  },
  {
    id: "a8-new-arch-7",
    category: "rn",
    categoryLabel: "RN",
    question: `What problem does TurboModules' lazy initialization solve, and what's the mechanism that makes it lazy rather than eager?`,
    answerHtml: `<p>Under the legacy bridge, <b>every native module was instantiated at app startup</b> regardless of whether it was ever used, because the bridge built a single global module registry up front — this directly hurt TTI on apps with dozens of modules (camera, maps, payments, etc.) that a given screen never touches. TurboModules instead register a <b>lazy lookup</b>: calling <code>TurboModuleRegistry.getEnforcing('ModuleName')</code> from JS triggers the native side to construct that single module's C++/Obj-C/Java instance on first access via a <code>TurboModuleProvider</code>, and it's cached after that — unused modules are simply never constructed.</p>`,
  },
  {
    id: "a8-new-arch-8",
    category: "rn",
    categoryLabel: "RN",
    question: `TurboModules still cross the JS/native boundary on every call. What part of the legacy bridge's overhead is actually eliminated, versus what's just reshaped?`,
    answerHtml: `<p>What's eliminated: the <b>JSON serialization/deserialization</b> of arguments and return values, and the <b>async message-queue hop</b> — calls go through JSI as direct C++ function invocations, synchronously when needed. What's reshaped, not eliminated: there's still a JS-to-native <b>call boundary</b> with marshalling cost (converting JS values to C++ types via JSI's <code>jsi::Value</code> conversions), and badly designed TurboModules can still be chatty and slow if you call them in a tight loop — JSI removes the serialization tax, not the cost of crossing languages at all.</p>`,
  },
  {
    id: "a8-new-arch-9",
    category: "rn",
    categoryLabel: "RN",
    question: `What does Codegen actually generate, and from what source of truth?`,
    answerHtml: `<p>Codegen parses <b>Flow or TypeScript type specs</b> you write in a module's JS/TS file (e.g. a file matching <code>NativeMyModule.ts</code> implementing the <code>TurboModule</code> interface, or a Fabric component's <code>codegenNativeComponent</code> call) and generates the <b>C++ glue code, Objective-C++/Swift protocol stubs, and Java/Kotlin interfaces</b> that bind the typed JS API to native implementations. The hand-written native code only has to implement the generated interface — Codegen guarantees the JS-declared signature and the native-side signature can't silently drift apart.</p>`,
  },
  {
    id: "a8-new-arch-10",
    category: "rn",
    categoryLabel: "RN",
    question: `Why does Codegen matter more for type safety than it did under the old \`NativeModules\` API, beyond just "less boilerplate"?`,
    answerHtml: `<p>Under the legacy <code>NativeModules.MyModule.someMethod()</code> API, the JS side had <b>no compile-time guarantee</b> that the native method existed, took the right argument types, or returned the right shape — a typo or signature mismatch only surfaced as a runtime crash or silent <code>undefined</code>. Because Codegen generates native interfaces directly from the JS/TS type spec, a mismatched native implementation now fails to <b>compile</b> (missing protocol conformance in Swift, unimplemented interface method in Kotlin) instead of failing at runtime in production.</p>`,
  },
  {
    id: "a8-new-arch-11",
    category: "rn",
    categoryLabel: "RN",
    question: `When does Codegen actually run in the build pipeline — is it a one-time codegen step or does it happen on every build?`,
    answerHtml: `<p>It runs as part of the <b>native build process</b> (a Gradle task on Android, a build phase / <code>react-native-codegen</code> script invoked by CocoaPods on iOS), not a one-off CLI command you run manually and commit. Every time you do a native build, Codegen re-scans your JS/TS specs (your own app's plus every linked library's) and regenerates the glue code into a build output directory — so the generated code is never committed to source control and always reflects your current type specs.</p>`,
  },
  {
    id: "a8-new-arch-12",
    category: "rn",
    categoryLabel: "RN",
    question: `What's the difference between the old \`requireNativeComponent\` and the New Architecture's \`codegenNativeComponent\` for building a Fabric native view?`,
    answerHtml: `<p><code>requireNativeComponent('RCTMyView')</code> looked up a native view manager <b>by string name at runtime</b> with no compile-time prop validation — props were passed through loosely typed and any mismatch was a runtime issue. <code>codegenNativeComponent&lt;Props&gt;('MyView')</code> takes a TypeScript/Flow props interface, and Codegen generates the native <b>ComponentDescriptor</b>, prop-parsing code, and view manager scaffolding from that type, so the props contract between JS and the native view is enforced at build time and the view participates correctly in Fabric's shadow tree (measurement, mounting) instead of being bolted on.</p>`,
  },
  {
    id: "a8-new-arch-13",
    category: "rn",
    categoryLabel: "RN",
    question: `Your app has a legacy native module written against \`RCTBridgeModule\` (iOS) / \`ReactContextBaseJavaModule\` (Android) using \`NativeModules\`. What's the actual migration path to TurboModules, step by step?`,
    answerHtml: `<p><ul><li>Write a JS/TS spec file (e.g. <code>NativeMyModule.ts</code>) that extends <code>TurboModule</code> and declares every method's exact types — Codegen requires this to be statically analyzable (no unions of arbitrary shape, no <code>any</code>).</li><li>Run Codegen (via build, or <code>npx react-native codegen</code> for inspection) to generate the native interface/protocol.</li><li>On iOS, make the existing class conform to the generated Objective-C++ protocol (often by adding a small adapter rather than rewriting the whole module); on Android, implement the generated <code>Spec</code> abstract class instead of extending <code>ReactContextBaseJavaModule</code> directly.</li><li>Register the module through the New Architecture's <code>TurboReactPackage</code> / module provider instead of the old package list, so it's looked up lazily instead of eagerly constructed.</li><li>Replace any synchronous-looking JS usage that relied on bridge batching quirks — TurboModules can genuinely be sync, so double-checked async assumptions (e.g. manual promise wrapping for ordering) should be re-verified.</li></ul></p>`,
  },
  {
    id: "a8-new-arch-14",
    category: "rn",
    categoryLabel: "RN",
    question: `A legacy module exposes a method via \`RCT_EXPORT_METHOD\` that returns data through a callback, not a Promise. What has to change for the TurboModule version, and why does it matter for Codegen?`,
    answerHtml: `<p>Codegen's type spec format supports <b>Promises and direct return values</b> as first-class typed results, but raw success/failure callback pairs (the old <code>RCTResponseSenderBlock</code> pattern) aren't expressible the same way in the typed spec — you generally convert the method to return a <code>Promise&lt;T&gt;</code> in the JS spec. This isn't just stylistic: Codegen needs a <b>statically typed return shape</b> to generate the native interface, and an untyped callback signature can't be checked at compile time the way a typed Promise resolution value can.</p>`,
  },
  {
    id: "a8-new-arch-15",
    category: "rn",
    categoryLabel: "RN",
    question: `Why can a legacy (non-Fabric) native UI component still work inside a New Architecture app, and what's the performance cost of that interop path?`,
    answerHtml: `<p>React Native ships an <b>interop layer</b> that lets old-style native view managers (registered via the legacy <code>requireNativeComponent</code> path) render inside a Fabric tree by wrapping them so Fabric can still mount/measure them, which is why third-party libraries don't all break overnight on upgrade. The cost: the wrapped component doesn't get Fabric's synchronous layout/measurement guarantees or the direct JSI prop-passing path — it still goes through a bridging shim, so it loses some of the New Architecture's latency wins and can become the slow path in an otherwise-migrated screen.</p>`,
  },
  {
    id: "a8-new-arch-16",
    category: "rn",
    categoryLabel: "RN",
    question: `What's \`react-native-codegen\`'s actual constraint on TypeScript types you can use in a TurboModule spec, and why does that constraint exist?`,
    answerHtml: `<p>Spec files are restricted to a <b>subset of TypeScript</b> that Codegen's static parser can fully resolve without running the TS compiler's type inference — no generics beyond the supported set, no complex conditional or mapped types, no importing arbitrary types from node_modules. The constraint exists because Codegen has to deterministically turn the type into a native (C++/Obj-C/Java/Kotlin) representation at build time using its own lightweight parser, not a full TypeScript type-checker, so anything that requires real type inference to resolve simply isn't expressible.</p>`,
  },
  {
    id: "a8-new-arch-17",
    category: "rn",
    categoryLabel: "RN",
    question: `What does "backpressure" mean in the context of Fabric's mounting phase, and why is it a real concern even with an immutable shadow tree?`,
    answerHtml: `<p>If JS produces shadow tree commits faster than the native UI thread can apply the resulting mutation instructions (e.g. rapid state updates during a fast scroll or animation), <b>unapplied mounting work queues up</b>, and the visible UI lags behind the logical state even though each individual commit was internally consistent. Fabric mitigates this by allowing the mounting layer to coalesce or skip intermediate commits and jump straight to the latest one when it falls behind, since each committed tree is a complete, valid snapshot rather than a delta that must be applied in order.</p>`,
  },
  {
    id: "a8-new-arch-18",
    category: "rn",
    categoryLabel: "RN",
    question: `A TurboModule constructor does expensive work (e.g. opening a database connection) in its native init. What's the New Architecture tradeoff this creates compared to the old eager-load model?`,
    answerHtml: `<p>Lazy loading means that expensive work is deferred from <b>app startup</b> to <b>first call site</b>, which speeds up TTI — but it also means the <b>first user interaction that touches that module pays the latency spike</b> instead of it being absorbed during splash/boot when the user expects a brief wait. For modules with genuinely expensive init (e.g. a native SDK doing disk I/O), this can turn a hidden startup cost into a visible jank on first use, so some teams deliberately "warm" a known-needed TurboModule early (e.g. during splash) by touching it once rather than relying purely on lazy semantics.</p>`,
  },
  {
    id: "a8-new-arch-19",
    category: "rn",
    categoryLabel: "RN",
    question: `What is \`RCTHost\` (iOS) / equivalent host abstraction, and why did the New Architecture need to introduce it instead of reusing the old \`RCTBridge\`?`,
    answerHtml: `<p><code>RCTHost</code> is the New Architecture's top-level coordinator that owns the JS runtime (via JSI), the Fabric surface registry, and TurboModule lookup, replacing <code>RCTBridge</code>'s role as the single object apps reached for. It was introduced because <code>RCTBridge</code>'s identity was tightly coupled to the <b>message-queue model</b> (it literally was the bridge) — the New Architecture has no message queue to own, so a new abstraction was needed that represents "the running React instance" without implying bridge semantics, and that can support things like <b>bridgeless mode</b> where no \`RCTBridge\` instance exists at all.</p>`,
  },
  {
    id: "a8-new-arch-20",
    category: "rn",
    categoryLabel: "RN",
    question: `What is "bridgeless mode" and how does it relate to, but differ from, simply "having Fabric and TurboModules enabled"?`,
    answerHtml: `<p>Bridgeless mode is the configuration where <b>no <code>RCTBridge</code> object is instantiated at all</b> — JS-to-native communication goes purely through JSI/<code>RCTHost</code> from the start, rather than constructing a bridge instance for backward compatibility and routing around it. You can run Fabric + TurboModules <i>with</i> a legacy bridge object still present for interop with modules that reach into bridge internals; bridgeless mode is the further step of removing that object entirely, which is what unlocks the full startup-time and memory savings since you're not paying for a bridge you don't use. Bridgeless has been the default since React Native 0.76.</p>`,
  },
  {
    id: "a8-new-arch-21",
    category: "rn",
    categoryLabel: "RN",
    question: `How does prop diffing change between the legacy architecture and Fabric in terms of where and how often it happens?`,
    answerHtml: `<p>On the legacy architecture, every prop update from a JS re-render was serialized and sent over the bridge to the native view manager, which had to apply props one at a time with no built-in diff against the shadow tree (diffing happened in JS's reconciler, but the native side just received "set these props"). Fabric computes the diff <b>once, in C++, between two immutable <code>ShadowNode</code> trees</b> during the commit, producing a minimal set of prop-update mutations that the native mounting layer applies directly — removing both the serialization step and a layer of redundant native-side prop bookkeeping.</p>`,
  },
  {
    id: "a8-new-arch-22",
    category: "rn",
    categoryLabel: "RN",
    question: `If you call a TurboModule method from JS and pass a large typed array (e.g. for image bytes), how does the data actually cross the boundary compared to the legacy bridge?`,
    answerHtml: `<p>On the legacy bridge, binary data had to be <b>base64-encoded into a JSON string</b> to cross the serialization boundary — a meaningful CPU and memory cost for large payloads. With JSI, an <code>ArrayBuffer</code> can be exposed as a <code>jsi::ArrayBuffer</code> backed by the <b>same underlying memory</b> the native side allocated (or vice versa), so native code can get a raw pointer to the bytes without copying through a string encoding at all — this is one of the concrete wins libraries like image/crypto/ML modules rely on.</p>`,
  },
];

export const ADVANCED8_QUIZ: QuizQuestion[] = [
  {
    id: "a8q-new-arch-1",
    category: "rn",
    categoryLabel: "RN",
    question: `What architectural property of JSI is the actual enabler of synchronous native calls, not just faster ones?`,
    options: [`JSI uses a faster JSON parser written in C++`, `JSI exposes native objects as direct references in the JS heap with real function pointers, removing the async message queue entirely`, `JSI batches multiple bridge messages into a single frame`, `JSI runs all native modules on the JS thread`],
    answer: 1,
    explanationHtml: `<p>The synchronicity comes from JSI giving JS a live reference to a C++ object it can call into directly — there's no queue to wait on. A faster JSON parser (option A) would still be async by construction; the queue itself is what's removed, not sped up.</p>`,
  },
  {
    id: "a8q-new-arch-2",
    category: "rn",
    categoryLabel: "RN",
    question: `During a commit, the Fabric mounting layer falls behind because JS is producing shadow tree updates faster than the UI thread can apply them. What does Fabric do, and why is it safe to do?`,
    options: [`It blocks the JS thread until mounting catches up on every commit`, `It applies every queued mutation in order, even if several frames behind`, `It can skip directly to the latest committed tree because each committed tree is a complete, self-consistent snapshot, not a delta`, `It drops the update entirely and keeps the previous tree forever`],
    answer: 2,
    explanationHtml: `<p>Because each commit represents a full immutable tree rather than an incremental patch, the mounting layer can safely jump to the newest commit and compute a single diff against the last applied one — it doesn't need to replay every intermediate commit in sequence.</p>`,
  },
  {
    id: "a8q-new-arch-3",
    category: "rn",
    categoryLabel: "RN",
    question: `A team adds a new native module and writes its spec as a \`.ts\` file extending \`TurboModule\`. At what point does this spec actually get turned into native interface code?`,
    options: [`At runtime, the first time the module is required from JS`, `During the native build (Gradle task on Android, build phase on iOS), which regenerates glue code from the spec on every build`, `Only once, when \`npx react-native codegen\` is manually run and the output is committed to git`, `When Metro bundles the JS, Codegen runs as a Babel plugin`],
    answer: 1,
    explanationHtml: `<p>Codegen runs as part of the native build pipeline and regenerates output every build from the current spec files — it's not a manual one-time step, and generated code isn't normally committed to source control.</p>`,
  },
  {
    id: "a8q-new-arch-4",
    category: "rn",
    categoryLabel: "RN",
    question: `Why does lazy TurboModule initialization improve startup time compared to the legacy \`NativeModules\` system?`,
    options: [`TurboModules run on a separate startup thread so init never blocks the JS thread`, `The legacy bridge constructed every registered native module at app launch regardless of use; TurboModules construct a module only on its first JS access`, `TurboModules skip native initialization entirely and use JS-only fallbacks`, `Codegen precompiles modules into the JS bundle so no native construction is needed`],
    answer: 1,
    explanationHtml: `<p>The legacy bridge eagerly built its entire module registry at startup. TurboModules defer construction to first use via the registry lookup, so modules a given session never touches are never paid for — a direct TTI win on module-heavy apps.</p>`,
  },
  {
    id: "a8q-new-arch-5",
    category: "rn",
    categoryLabel: "RN",
    question: `A legacy native view manager (not yet migrated to a Fabric component) is rendered inside an app running the New Architecture. What actually happens?`,
    options: [`The app crashes at runtime because Fabric requires \`codegenNativeComponent\``, `It silently falls back to rendering nothing`, `An interop layer wraps it so Fabric can still mount and measure it, at the cost of losing Fabric's direct synchronous layout/prop path for that component`, `React Native automatically rewrites it to a Fabric component at build time`],
    answer: 2,
    explanationHtml: `<p>The interop layer is what lets the ecosystem upgrade incrementally — unmigrated native views keep working but go through a bridging shim, missing out on Fabric's full performance benefits for that specific component.</p>`,
  },
  {
    id: "a8q-new-arch-6",
    category: "rn",
    categoryLabel: "RN",
    question: `Why are large binary payloads (e.g. image bytes) cheaper to pass to a TurboModule than they were over the legacy bridge?`,
    options: [`TurboModules compress binary data automatically before sending it`, `JSI lets an ArrayBuffer be backed by the same underlying memory on both sides, avoiding the base64-into-JSON-string encoding the bridge required`, `TurboModules only support passing references to files on disk, never raw bytes`, `The legacy bridge already passed binary data as raw bytes, so there's no difference`],
    answer: 1,
    explanationHtml: `<p>The legacy bridge had to base64-encode binary data into JSON strings to cross the serialization boundary. JSI's \`jsi::ArrayBuffer\` can share the same underlying memory between JS and native, skipping that copy/encode step entirely.</p>`,
  },
  {
    id: "a8q-new-arch-7",
    category: "rn",
    categoryLabel: "RN",
    question: `What is \`RCTHost\` (and equivalent host abstractions on the New Architecture) primarily responsible for, and why couldn't \`RCTBridge\` simply be reused for that role?`,
    options: [`It manages app navigation state; RCTBridge was never designed for navigation`, `It owns the JS runtime, Fabric surface registry, and TurboModule lookup; RCTBridge's identity was tied to the message-queue model the New Architecture removes`, `It replaces Metro as the JS bundler; RCTBridge only worked with the old bundler`, `It is a debugging-only tool with no role in production apps`],
    answer: 1,
    explanationHtml: `<p>RCTBridge's whole identity was the message-queue bridge object. Since the New Architecture (especially bridgeless mode, the default since RN 0.76) has no message queue to own, a new top-level coordinator was needed that represents the running React instance without implying bridge semantics.</p>`,
  },
];
