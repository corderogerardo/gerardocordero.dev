// Batch 14 — Jest + RNTL async patterns, mocking native modules, snapshot testing pitfalls, and E2E with Detox vs Maestro tradeoffs.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED14_FLASHCARDS: Flashcard[] = [
  {
    id: "a14-testing-1",
    category: "test",
    categoryLabel: "TEST",
    question: `Why does RNTL v14 require await on render() and fireEvent calls, when older versions didn't need it?`,
    answerHtml: `<p>RNTL v14 switched to an <b>async-by-default</b> API to match React 18+ concurrent rendering. <code>render()</code> and <code>fireEvent</code> now internally flush microtasks and effects before returning. Skipping <code>await</code> means assertions can run before a <code>useEffect</code> or state update has committed, producing flaky tests that pass locally but fail under different timing, or silently check stale output instead of throwing.</p><p><b>Red flag:</b> assuming <code>fireEvent</code> is still synchronous like pre-v14 RNTL — that habit is exactly what produces CI-only flakiness.</p><p><b>I await render() and fireEvent in RNTL v14 so my assertions run after React has actually committed the update, not before.</b></p>`,
  },
  {
    id: "a14-testing-2",
    category: "test",
    categoryLabel: "TEST",
    question: `What's the practical difference between findBy* queries and wrapping getBy* in waitFor in RNTL?`,
    answerHtml: `<p><code>findBy*</code> is sugar for <code>waitFor(() =&gt; getBy*(...))</code> with a default 1000ms timeout polling on a fixed interval. They're equivalent for presence checks, but <code>waitFor</code> is the general primitive: use it directly when waiting on something that isn't a query, e.g. a mock having been called N times, or a piece of derived state. For waiting on an element's <i>removal</i>, neither is correct on its own — use <code>waitForElementToBeRemoved</code>, since polling <code>getBy*</code> would throw immediately once the node is gone.</p><p><b>I reach for findBy* for simple presence checks and drop to waitFor directly when I'm waiting on something that isn't a query, like a mock call count.</b></p>`,
  },
  {
    id: "a14-testing-3",
    category: "test",
    categoryLabel: "TEST",
    question: `RNTL wraps render, fireEvent, and waitFor in act() automatically. When do you still need to call act() manually?`,
    answerHtml: `<p>Any state update that happens outside RNTL's own helpers still needs a manual <code>act()</code>. The common triggers: 1) a timer callback fired via <code>jest.advanceTimersByTime()</code>, 2) a promise resolving from code you invoked directly rather than through <code>fireEvent</code>, 3) a subscription callback from a native event emitter mock firing asynchronously. Wrap the trigger in <code>await act(async () =&gt; { ... })</code> so React flushes the resulting re-render before your assertions run — otherwise you get the 'An update to Component inside a test was not wrapped in act(...)' warning and a race on the assertion.</p><p><b>If I see the act() warning, my first question is what fired this update, and whether it went through fireEvent or waitFor.</b></p>`,
  },
  {
    id: "a14-testing-4",
    category: "test",
    categoryLabel: "TEST",
    question: `Why does mixing fake timers with await waitFor() in RNTL often deadlock or time out?`,
    answerHtml: `<p><code>waitFor</code> polls using real <code>setInterval</code>/microtask ticks internally. If <code>jest.useFakeTimers()</code> is active and you never manually advance the clock, <code>waitFor</code>'s own polling can stall waiting on fake time that nothing is advancing, while the code under test is waiting on a timer that's also fake and frozen. The fix is either to avoid fake timers for that test, or to interleave <code>jest.advanceTimersByTimeAsync()</code> calls with flush points so both the app code and waitFor's polling loop progress.</p><p><b>Red flag:</b> reaching for jest.useFakeTimers() by default — it's the wrong tool the moment a test also needs waitFor, since both then compete for the same clock.</p><p><b>I only use fake timers when I'm also driving them manually with advanceTimersByTimeAsync — otherwise I let waitFor run on real time.</b></p>`,
  },
  {
    id: "a14-testing-5",
    category: "test",
    categoryLabel: "TEST",
    question: `When mocking a native module that exposes an event emitter (e.g. NetInfo or a Bluetooth library), what's the most common test bug?`,
    answerHtml: `<p>Mocking the emitter's <code>addEventListener</code> to immediately invoke the callback synchronously, when the real native bridge always delivers events asynchronously (next tick at minimum, often after a native round-trip). A synchronous mock makes a test pass that would fail on-device because the component assumed an initial render before the first event — e.g. a loading state that the mock skips entirely. Mock emitters should fire via <code>Promise.resolve().then()</code> or <code>setImmediate</code> to preserve the async contract.</p><p><b>Red flag:</b> a mock that fires synchronously "just to make the test pass" — it deletes the exact async gap where a real loading-state bug lives.</p><p><b>I fire mock emitters via Promise.resolve().then() or setImmediate specifically to preserve the bridge's async contract.</b></p>`,
  },
  {
    id: "a14-testing-6",
    category: "test",
    categoryLabel: "TEST",
    question: `What's the risk of using jest.mock() with a manual factory for a native module versus relying on Jest's automock for react-native?`,
    answerHtml: `<p>react-native's own jest preset already ships mocks for many native modules (e.g. <code>NativeModules</code>, <code>Animated</code>, <code>Dimensions</code>). Writing your own <code>jest.mock('react-native', ...)</code> factory that doesn't spread the preset's existing mock can silently strip out behavior other code in the tree depends on (like <code>Platform.OS</code> or <code>NativeEventEmitter</code> internals), causing unrelated tests to fail with cryptic 'undefined is not a function' errors. Prefer mocking the specific native module path (e.g. <code>jest.mock('react-native/Libraries/Components/Switch/Switch')</code>) over re-mocking the whole package.</p><p><b>Red flag:</b> re-mocking the whole 'react-native' package instead of spreading the preset's mock — it silently breaks unrelated tests elsewhere in the suite.</p><p><b>I mock the specific native module path, not the whole react-native package, so I don't strip mocks other tests rely on.</b></p>`,
  },
  {
    id: "a14-testing-7",
    category: "test",
    categoryLabel: "TEST",
    question: `Why is jest.spyOn often preferable to jest.mock() for a native module you only need to override in one or two tests?`,
    answerHtml: `<p><code>jest.mock()</code> is hoisted and module-scoped — it replaces the module for the entire test file, which can break other tests in the same file that needed the real (or differently-mocked) behavior. <code>jest.spyOn(module, 'method').mockImplementation(...)</code> is scoped to the spy and can be restored per-test with <code>mockRestore()</code> in <code>afterEach</code>, giving finer-grained control without polluting sibling tests in the same suite.</p><p><b>jest.mock() is a file-scoped module swap; jest.spyOn() is a test-scoped override I can restore — I use spyOn for a one- or two-test override so I don't affect sibling tests in the same file.</b></p>`,
  },
  {
    id: "a14-testing-8",
    category: "test",
    categoryLabel: "TEST",
    question: `How should you mock a native module wrapped by Expo (e.g. expo-haptics or expo-file-system) rather than a bare React Native native module?`,
    answerHtml: `<p>Expo modules are typically backed by the Expo Modules API and exposed through <code>requireNativeModule</code>/<code>NativeModulesProxy</code>. jest-expo ships mocks for the common first-party Expo SDK modules (haptics, file-system, constants) so you usually get a working stub for free; for ones it doesn't cover, mock at the package's public JS entry point (<code>jest.mock('expo-haptics')</code>) rather than trying to mock the underlying native proxy, since the proxy's shape is an implementation detail that changes between SDK versions.</p><p><b>For Expo SDK modules I mock the public JS entry point, not the native proxy, because the proxy's shape is an implementation detail that changes across SDK versions.</b></p>`,
  },
  {
    id: "a14-testing-9",
    category: "test",
    categoryLabel: "TEST",
    question: `What's the failure mode of snapshot testing a component that renders a FlatList or any list with dynamically generated keys/timestamps?`,
    answerHtml: `<p>Any non-deterministic value baked into the rendered tree — a <code>Date.now()</code> timestamp, a <code>Math.random()</code> id, or list virtualization metadata that can differ run-to-run — makes the snapshot fail on every CI run even though nothing meaningfully changed. The fix is either to mock the non-deterministic source (freeze <code>Date</code>, stub the id generator) before snapshotting, or to avoid snapshotting components that embed such values and assert on stable, derived output instead.</p><p><b>Red flag:</b> snapshotting a component before checking whether anything inside it is non-deterministic — that's the diff that reappears every CI run for no real reason.</p><p><b>Before I snapshot anything, I check for Date.now(), Math.random(), or virtualization metadata baked into the tree, and freeze or stub it first.</b></p>`,
  },
  {
    id: "a14-testing-10",
    category: "test",
    categoryLabel: "TEST",
    question: `Why are large component-tree snapshots considered an anti-pattern by many senior RN teams, even though Jest supports them natively?`,
    answerHtml: `<p>A full-tree snapshot changes on almost any unrelated styling or prop tweak, training engineers to run <code>jest --ci=false -u</code> reflexively without reading the diff — at which point the snapshot stops catching real regressions and just becomes a rubber stamp. They also produce huge, low-signal diffs in PRs that are hard to review. Senior teams favor targeted assertions (<code>getByText</code>, <code>toHaveStyle</code>, prop assertions on a shallow render) or small, intentional snapshots of a stable leaf component, not whole screens.</p><p><b>Red flag:</b> running jest -u reflexively without reading the diff — once that becomes habit, the snapshot suite stops catching real regressions.</p><p><b>I reserve snapshots for small, stable leaf components and use targeted assertions for anything that changes often.</b></p>`,
  },
  {
    id: "a14-testing-11",
    category: "test",
    categoryLabel: "TEST",
    question: `What does toMatchInlineSnapshot offer over toMatchSnapshot, and what's its tradeoff at scale?`,
    answerHtml: `<p><code>toMatchInlineSnapshot()</code> writes the expected output directly into the test file instead of a separate <code>__snapshots__/*.snap</code> file, so reviewers see the expected value in the same diff hunk as the test change — better review signal, no separate file to cross-reference. The tradeoff is file bloat for anything beyond a small string/object: a serialized component tree inline makes the test file unreadable, so it's best reserved for small, stable values (a formatted string, a small computed object), not full render trees.</p><p><b>I use toMatchInlineSnapshot for small, stable values reviewers need to see in the diff, and toMatchSnapshot for anything larger than a few lines.</b></p>`,
  },
  {
    id: "a14-testing-12",
    category: "test",
    categoryLabel: "TEST",
    question: `What's a common cause of 'act warning' noise specifically tied to React Navigation or expo-router screens under test?`,
    answerHtml: `<p>Navigators mount child screens lazily and often run their own internal animations/state transitions (e.g. <code>react-native-screens</code> native stack transitions, or expo-router's deferred route resolution) outside the test's render call. These fire state updates after the initial <code>render()</code> resolves. Wrapping the navigator in a test-specific minimal config (disabling animations via <code>NavigationContainer</code> test props, or using <code>renderRouter</code> from <code>expo-router/testing-library</code>) avoids relying on real native transition timers that the JS test environment can't actually run.</p><p><b>When I see act warnings tied to navigation, I disable animations in the test config or use expo-router's renderRouter rather than trying to out-wait real native transition timers the JS environment can't run.</b></p>`,
  },
  {
    id: "a14-testing-13",
    category: "test",
    categoryLabel: "TEST",
    question: `Why does mocking AsyncStorage with a synchronous in-memory object break tests that rely on persisted state across remounts, and what's the correct mock?`,
    answerHtml: `<p>The real <code>AsyncStorage</code> API is promise-based and the official Jest mock (<code>@react-native-async-storage/async-storage/jest/async-storage-mock</code>) preserves that contract — it returns promises backed by an in-memory store that persists for the lifetime of the test module, so a component that reads on mount and writes on unmount behaves correctly across multiple <code>render</code>/<code>unmount</code> cycles within one test file. A naive synchronous stub that returns values directly instead of promises will break any code awaiting the read, and resetting it incorrectly between tests (or not at all) causes state to leak across tests in the same file.</p><p><b>Red flag:</b> hand-rolling a synchronous in-memory stub for AsyncStorage — it breaks any code that awaits the read and can pass tests that would fail on a real device.</p><p><b>I always use the official async-storage-mock so the promise contract and cross-render persistence match production.</b></p>`,
  },
  {
    id: "a14-testing-14",
    category: "test",
    categoryLabel: "TEST",
    question: `How do you test a component that depends on Reanimated 4 worklets without crashing on the native thread bridge that doesn't exist in Jest's Node environment?`,
    answerHtml: `<p>Reanimated ships a Jest mock (now via <code>react-native-worklets/src/mock</code> after worklets split out of <code>react-native-reanimated</code> in v4) that replaces the worklet runtime with synchronous JS execution, so shared values and animations resolve immediately instead of running on the UI thread. It's wired up via <code>jest-setup.ts</code> calling the mock's setup function before tests run; without it, any component touching <code>useSharedValue</code> or <code>useAnimatedStyle</code> throws because there's no native UI runtime in the Jest/Node environment.</p><p><b>I wire up react-native-worklets' mock in jest-setup.ts before any test runs, so useSharedValue and useAnimatedStyle resolve synchronously instead of reaching for a UI thread that doesn't exist in Jest.</b></p>`,
  },
  {
    id: "a14-testing-15",
    category: "test",
    categoryLabel: "TEST",
    question: `What's the core architectural difference between how Detox and Maestro drive the app under test?`,
    answerHtml: `<p>Detox is a <b>gray-box</b> framework: it injects a native sync mechanism into your app binary that monitors the JS event loop, native animations, network requests, and timers, then blocks each test action until the app is genuinely idle — this needs an instrumented build and tight coupling to your app's bundler/native config. Maestro is <b>black-box</b>: it drives the OS accessibility tree from outside the app (like a human tapping), uses its own heuristic/polling-based waiting instead of an idle-detection hook, and needs zero in-app instrumentation — it works on any installed build, including one you didn't build yourself.</p><p><b>Red flag:</b> treating Detox and Maestro as interchangeable — the moment you need to test a build you didn't compile, gray-box instrumentation is off the table and black-box is the only option.</p><p><b>Detox needs my build pipeline; Maestro needs only the installed binary — that's the first question I ask when picking between them.</b></p>`,
  },
  {
    id: "a14-testing-16",
    category: "test",
    categoryLabel: "TEST",
    question: `Given Detox's automatic synchronization, why do teams still hit flaky tests, and what's the standard escape hatch?`,
    answerHtml: `<p>Detox's idle-detection only tracks resources it knows how to watch — native timers, the JS run loop, XHR/fetch, animations. A <code>setInterval</code> polling loop, a WebSocket, or certain third-party native SDKs with their own background threads are invisible to it, so Detox can report 'idle' while the app is still mid-async-operation, causing flaky early assertions. The escape hatch is <code>device.disableSynchronization()</code> around the offending operation paired with an explicit <code>waitFor(element).toBeVisible().withTimeout(ms)</code>, trading automatic sync for a manual, explicit wait.</p><p><b>When Detox reports idle but the test is still flaky, I scope device.disableSynchronization() to the specific untracked operation and pair it with an explicit waitFor().withTimeout(), rather than disabling sync globally.</b></p>`,
  },
  {
    id: "a14-testing-17",
    category: "test",
    categoryLabel: "TEST",
    question: `Why can Maestro test an app built by someone else (e.g. a QA team testing a release .apk with no source access), while Detox fundamentally cannot?`,
    answerHtml: `<p>Detox's idle-sync mechanism has to be compiled into the binary at build time (it patches native code and requires specific build configuration), so you must control the build pipeline. Maestro only needs the binary installed on a device/simulator and drives it through the OS's accessibility/UI-automation layer (XCUITest/UIAutomator under the hood), the same surface any human tester uses — no special build flag, no SDK dependency in your app at all.</p><p><b>If a build isn't mine to instrument, Maestro is the only option — Detox's sync hooks have to be compiled in at build time.</b></p>`,
  },
  {
    id: "a14-testing-18",
    category: "test",
    categoryLabel: "TEST",
    question: `What's the tradeoff of Maestro's black-box waiting model versus Detox's synchronization when an app does heavy custom rendering (e.g. Skia/Canvas-based UI)?`,
    answerHtml: `<p>Detox's idle-sync doesn't understand custom GPU-drawn content either — neither framework can 'see' inside a Skia canvas to assert pixel-level state, both rely on accessibility labels or testIDs exposed by the app. But Maestro's explicit, timeout-based waits (<code>waitForAnimationToEnd</code>, retry-with-timeout assertions) are arguably a better fit here because they don't pretend to know when custom rendering settles — you write the wait condition yourself, which is more honest than relying on Detox's idle-tracker silently mis-reporting 'idle' for content it can't actually monitor.</p><p><b>For Skia/Canvas-heavy screens I write explicit, timeout-based waits rather than trusting either framework's idle detection to understand content it can't see into.</b></p>`,
  },
  {
    id: "a14-testing-19",
    category: "test",
    categoryLabel: "TEST",
    question: `Why is YAML-based flow authoring in Maestro considered lower-maintenance than Detox's JS test files, and what's the cost of that simplicity?`,
    answerHtml: `<p>Maestro flows are declarative YAML — a flat sequence of steps like <code>tapOn</code>, <code>assertVisible</code>, <code>inputText</code> — with no test framework, no async/await, no custom matchers to learn, so non-engineers (QA, PM) can read and even author flows. The real cost is expressiveness: complex conditional logic, custom assertions beyond what Maestro's command set provides, or tight integration with app-internal state (e.g. asserting on a Redux store value) isn't possible in pure YAML. But the alternative isn't free either — Detox's JS test files need engineers to write and maintain them, and QA can't extend a suite without learning the framework. For a fast, low-maintenance smoke gate on every PR, that tradeoff is worth it; reserve Detox's expressiveness for the regression suite that actually needs app-internal assertions.</p><p><b>Maestro's YAML ceiling isn't a bug — it's the price of a smoke test any teammate can read and extend without touching JS.</b></p>`,
  },
  {
    id: "a14-testing-20",
    category: "test",
    categoryLabel: "TEST",
    question: `What does it mean that RNTL v14's render() flushes effects, and why doesn't that make manual act() calls entirely unnecessary for testing custom hooks?`,
    answerHtml: `<p><code>render()</code> flushes the <i>initial</i> mount's effects (layout effects then passive effects) before returning, covering the common case of a hook's setup logic running on mount. But a hook that schedules further state updates from within an effect — a polling <code>useEffect</code>, a debounce, a subscription that fires later — produces updates after that initial flush, outside any RNTL-wrapped call, so those still need an explicit <code>await act(async () =&gt; {})</code> or a <code>waitFor</code> around the resulting assertion to avoid the act-warning and a race condition in the test.</p><p><b>render() only guarantees the initial mount's effects are flushed — anything a hook schedules later, from a poll or a subscription, still needs its own act() or waitFor.</b></p>`,
  },
  {
    id: "a14-testing-21",
    category: "test",
    categoryLabel: "TEST",
    question: `Why do many senior RN teams keep Maestro for local/PR-gate smoke testing but still bring in Detox for deep regression suites, instead of picking just one?`,
    answerHtml: `<p>Maestro's setup cost and per-test reliability (no source instrumentation, simple YAML, works on any build) make it ideal for a fast 'does the app boot and can I tap through the tabs' gate that runs on every PR. Detox's deeper native synchronization and ability to assert on app-internal state pays off for large, assertion-heavy regression suites where flaky black-box timing would be more costly than the upfront instrumentation cost — it's a coverage-vs-setup-cost tradeoff, not a strict either/or, and some teams run both at different stages of the pipeline.</p><p><b>I run Maestro on every PR for a cheap boot-and-tap-through gate, and reserve Detox for the deep regression suite where its native sync and internal-state access earn back the setup cost.</b></p>`,
  },
  {
    id: "a14-testing-22",
    category: "test",
    categoryLabel: "TEST",
    question: `When testing a component using NativeWind classNames, why might toHaveStyle assertions fail in Jest even though the style renders correctly on-device?`,
    answerHtml: `<p>NativeWind resolves Tailwind classes into RN style objects via a metro/babel transform and (in NativeWind v4+) a CSS-interop runtime that can apply styles through a different code path (e.g. a <code>StyleSheet</code> reference or CSS variables resolved at render time) than a plain inline <code>style</code> prop. <code>toHaveStyle</code> matches against the resolved style object RNTL sees in the test renderer's output, so if the interop hasn't fully resolved in the jsdom-less Jest environment (no real layout pass, no CSS variable cascade), the assertion can see unresolved class names or partial styles. Testing by behavior (testID, visible text) rather than exact computed style is more robust across NativeWind's resolution pipeline.</p><p><b>For NativeWind components I assert on behavior — testID, visible text — not toHaveStyle, since the CSS-interop resolution path doesn't fully run in a layout-less Jest environment.</b></p>`,
  },
  {
    id: "a14-testing-23",
    category: "test",
    categoryLabel: "TEST",
    question: `What's the difference between RNTL's queryBy*, getBy*, and findBy* in terms of failure semantics, and why does picking the wrong one hide bugs?`,
    answerHtml: `<p><code>getBy*</code> throws synchronously if the element isn't present right now — use it to assert something must already exist. <code>queryBy*</code> returns <code>null</code> instead of throwing — use it only to assert <i>absence</i> (<code>expect(queryByText(...)).toBeNull()</code>); using <code>queryBy*</code> to assert presence silently turns a real 'element missing' bug into a less obvious 'received null' assertion failure with a worse error message. <code>findBy*</code> is the async, polling version of <code>getBy*</code> for elements that appear after a delay; using it for something synchronous just adds unnecessary timeout latency to the test.</p><p><b>Red flag:</b> using queryBy* to assert presence — a real "element missing" bug degrades into a vague "received null" failure instead of a clear thrown error.</p><p><b>getBy* is for "this must already be there," queryBy* is only for asserting absence, and findBy* is for anything that shows up later.</b></p>`,
  },
];

export const ADVANCED14_QUIZ: QuizQuestion[] = [
  {
    id: "a14q-testing-1",
    category: "test",
    categoryLabel: "TEST",
    question: `A test calls fireEvent.press(button) without await in RNTL v14, then immediately asserts the resulting text changed, and the assertion intermittently fails in CI but passes locally. What's the most likely root cause?`,
    options: [`The testID on the button is duplicated elsewhere in the tree`, `fireEvent in RNTL v14 is async and the state update from the press hasn't been flushed before the assertion runs`, `Jest's CI environment uses a different React reconciler than local runs`, `The component is missing a key prop, causing nondeterministic reconciliation`],
    answer: 1,
    explanationHtml: `<p>RNTL v14 made <code>fireEvent</code> async to align with React 18's effect flushing. Without <code>await</code>, the assertion can race the state update — it sometimes wins the race locally (faster machine, warm cache) and loses in CI, which is the classic signature of this exact mistake. A duplicate testID or a missing key prop would throw a deterministic error on every run, not an intermittent one, and Jest doesn't swap reconcilers between local and CI — the tempting distractors describe bugs that fail consistently, not flakily.</p>`,
  },
  {
    id: "a14q-testing-2",
    category: "test",
    categoryLabel: "TEST",
    question: `A component subscribes to a native event emitter in useEffect and updates state when an event fires. The test mock invokes the listener callback synchronously inside addEventListener. What real-world bug can this mock hide?`,
    options: [`Memory leaks from not calling removeEventListener`, `A loading or initial state the component renders before the first real (asynchronously delivered) event arrives`, `Incorrect typing on the event payload`, `The component re-rendering too many times`],
    answer: 1,
    explanationHtml: `<p>Real native bridges always deliver events asynchronously (at least a tick later). A synchronous mock skips straight to the post-event state, so a test can pass while the component's actual initial/loading render path — which only shows up with real async timing — is never exercised or verified. A missing removeEventListener would leak across tests rather than hide a render-order bug, and payload typing or re-render count are unrelated to when the callback fires — both are plausible-sounding but address the wrong layer of the bug.</p>`,
  },
  {
    id: "a14q-testing-3",
    category: "test",
    categoryLabel: "TEST",
    question: `Why is Detox described as a 'gray-box' E2E framework while Maestro is 'black-box'?`,
    options: [`Detox only supports Android while Maestro only supports iOS`, `Detox injects in-app synchronization hooks that observe JS/native idle state, requiring an instrumented build; Maestro drives the OS accessibility layer externally with no app instrumentation`, `Detox tests are written in TypeScript while Maestro tests are written in JavaScript`, `Detox can only assert on visible text while Maestro can assert on internal app state`],
    answer: 1,
    explanationHtml: `<p>The gray-box/black-box distinction is about visibility into the app's internals. Detox patches the binary to track the JS run loop, timers, and animations for automatic synchronization — it needs your build pipeline. Maestro taps and reads the screen like a human via the OS automation layer, working on any installed build. The language distractor (TS vs JS) and the platform-support distractor are both factually wrong and orthogonal to what gray-box/black-box actually measures — implementation language and OS support don't determine how much a framework can see inside the app.</p>`,
  },
  {
    id: "a14q-testing-4",
    category: "test",
    categoryLabel: "TEST",
    question: `A snapshot test for a list component fails on every CI run with a one-character diff in a generated id, even though the rendered UI is unchanged. What's the most targeted fix?`,
    options: [`Delete the snapshot file and never snapshot test that component again`, `Switch from toMatchSnapshot to toMatchInlineSnapshot`, `Mock or stub the id-generating function so it returns a deterministic value before snapshotting`, `Increase the Jest test timeout`],
    answer: 2,
    explanationHtml: `<p>The root cause is non-deterministic data (a random or time-based id) baked into the rendered tree. Stubbing the generator to a fixed value removes the nondeterminism at the source; inline snapshots or longer timeouts don't address why the value changes between runs.</p>`,
  },
  {
    id: "a14q-testing-5",
    category: "test",
    categoryLabel: "TEST",
    question: `Why might a Jest test using fake timers (jest.useFakeTimers()) hang or time out when combined with await waitFor(...)?`,
    options: [`waitFor requires real network access which fake timers block`, `waitFor's internal polling relies on real time progressing, so if fake time is never advanced, both the app's timer and waitFor's poll loop stall`, `Fake timers are incompatible with async/await syntax entirely`, `waitFor automatically disables fake timers, causing a configuration conflict`],
    answer: 1,
    explanationHtml: `<p><code>waitFor</code> polls on real clock ticks/microtasks under the hood. With fake timers frozen and never advanced, neither the code under test's timer-driven logic nor waitFor's own polling can progress, so the test hangs until Jest's outer timeout fires.</p>`,
  },
  {
    id: "a14q-testing-6",
    category: "test",
    categoryLabel: "TEST",
    question: `Which scenario is the best fit for Maestro over Detox, given their respective design tradeoffs?`,
    options: [`Asserting on a Redux store's internal state after a complex multi-step user flow`, `Running a smoke test against a release build an engineer didn't compile themselves, with no app source instrumentation available`, `Writing JS-level custom matchers that inspect component props directly`, `Synchronizing test assertions precisely with native timer-based animations via automatic idle detection`],
    answer: 1,
    explanationHtml: `<p>Maestro needs no in-app instrumentation and drives any installed binary through the OS accessibility layer, making it the right tool when you don't control the build (e.g. testing a downloaded release artifact). The other options play to Detox's strengths: build-time instrumentation, JS-level access to app internals, and native idle-sync.</p>`,
  },
  {
    id: "a14q-testing-7",
    category: "test",
    categoryLabel: "TEST",
    question: `A test does expect(queryByText('Saved')).toBeNull() immediately after a save action, intending to confirm the success message hasn't appeared too early, but the assertion passes even when there's a real bug delaying the message indefinitely. What's the issue with this test design?`,
    options: [`queryByText cannot be used with toBeNull, only getByText can`, `The assertion only proves absence at one instant; it doesn't verify the message ever appears, so a regression that prevents it from ever rendering goes undetected`, `toBeNull() is not a valid RNTL/Jest matcher`, `queryByText triggers a console warning that fails the test in strict mode`],
    answer: 1,
    explanationHtml: `<p>This test only checks a negative at a single point in time. A complete test needs a positive assertion later (e.g. <code>await findByText('Saved')</code>) to confirm the message eventually shows; otherwise a bug that permanently suppresses the message is indistinguishable from correct 'not yet visible' behavior.</p>`,
  },
];
