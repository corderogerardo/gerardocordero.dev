// Batch 10 — Context API pitfalls (re-render storms), Redux Toolkit vs Zustand vs Jotai tradeoffs, async state with React Query/SWR (caching, invalidation, optimistic updates).
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED10_FLASHCARDS: Flashcard[] = [
  {
    id: "a10-state-mgmt-1",
    category: "state",
    categoryLabel: "State",
    question: `Why does a single Context.Provider value object cause every consumer to re-render, even ones reading unrelated fields?`,
    answerHtml: `<p><code>useContext</code> subscribes to the entire context value reference, not individual fields. If the provider passes <code>{state, setState}</code> or any object literal, React creates a new reference on every render of the provider, so <b>all</b> consumers re-render regardless of which slice they actually read. There's no built-in selector mechanism — Context is an all-or-nothing broadcast, unlike a store with fine-grained subscriptions.</p>`,
  },
  {
    id: "a10-state-mgmt-2",
    category: "state",
    categoryLabel: "State",
    question: `Splitting one Context into multiple Contexts is a common fix for re-render storms. What's the limit of this technique, and when does it stop being enough?`,
    answerHtml: `<p>Splitting by <i>update frequency</i> (e.g. separate <code>UserContext</code> and <code>ThemeContext</code>) helps because consumers only re-subscribe to what changes together. It breaks down when: (1) a single piece of state is large and consumed partially across many components (you'd need dozens of contexts), or (2) updates are high-frequency (scroll position, animation values, form input on every keystroke). At that point you need external state with selectors (Zustand/Jotai) or memoized selector hooks, because Context still can't subscribe to a derived slice without re-rendering on every change to the source object.</p>`,
  },
  {
    id: "a10-state-mgmt-3",
    category: "state",
    categoryLabel: "State",
    question: `Does wrapping a context consumer in React.memo prevent it from re-rendering when the context value changes?`,
    answerHtml: `<p>No. <code>React.memo</code> only blocks re-renders triggered by a parent re-rendering with the same props — it has no effect on re-renders triggered by a subscribed context changing. A memoized component that calls <code>useContext</code> still re-renders whenever that context's value reference changes, because the subscription bypasses the props/memo comparison entirely.</p>`,
  },
  {
    id: "a10-state-mgmt-4",
    category: "state",
    categoryLabel: "State",
    question: `What's the standard pattern for preventing Context-driven re-renders when you need to share frequently-changing state (e.g. a counter or scroll offset) across deeply nested components?`,
    answerHtml: `<p>Split the provider into a state context and a stable dispatch/setter context, and push the value down so each leaf only subscribes to what it needs — or better, move the high-frequency value out of React state entirely (e.g. a ref + imperative updates, or Reanimated shared values) and only use Context to pass the stable updater function. The key idea: Context is fine for low-frequency, structurally stable data (auth, theme, locale); it's the wrong tool for hot-path values.</p>`,
  },
  {
    id: "a10-state-mgmt-5",
    category: "state",
    categoryLabel: "State",
    question: `In React Query/TanStack Query, what's the practical difference between staleTime and gcTime (formerly cacheTime), and what bug results from confusing them?`,
    answerHtml: `<p><code>staleTime</code> controls how long data is considered fresh — within that window, refetch triggers (refocus, remount, reconnect) are skipped and the cached data is served as-is. <code>gcTime</code> controls how long an <i>unused/inactive</i> query stays in memory before being garbage-collected after all observers unmount. Setting <code>staleTime: 0</code> (the default) with a long <code>gcTime</code> causes a common bug: the cache holds data indefinitely but every component mount triggers a background refetch anyway, producing visible loading flicker even though data was 'cached'.</p>`,
  },
  {
    id: "a10-state-mgmt-6",
    category: "state",
    categoryLabel: "State",
    question: `How does React Query's optimistic update pattern (onMutate/onError/onSettled) avoid leaving the cache in an inconsistent state if the mutation fails?`,
    answerHtml: `<p><code>onMutate</code> cancels in-flight queries for that key (<code>cancelQueries</code>) to prevent a race where a stale response overwrites the optimistic value, snapshots the previous cache value via <code>getQueryData</code>, then writes the optimistic value via <code>setQueryData</code> and returns the snapshot as context. <code>onError</code> receives that context and rolls back with <code>setQueryData(key, context.previousValue)</code>. <code>onSettled</code> (runs on both success and failure) calls <code>invalidateQueries</code> to reconcile with the server's actual state, so any drift between the optimistic guess and reality self-corrects.</p>`,
  },
  {
    id: "a10-state-mgmt-7",
    category: "state",
    categoryLabel: "State",
    question: `Why is invalidateQueries generally preferred over manually calling setQueryData after a mutation succeeds, in a codebase with many consumers of that data?`,
    answerHtml: `<p><code>setQueryData</code> requires you to know and correctly reconstruct the exact shape the server would have returned, and only updates the keys you explicitly target — easy to miss related queries (e.g. updating a single todo but forgetting the filtered list/count queries that include it). <code>invalidateQueries</code> marks matching queries stale and triggers a refetch from the source of truth, so it's self-healing against shape drift and partial-key coverage at the cost of an extra network round-trip. The tradeoff is correctness/simplicity vs. perceived speed — many teams do optimistic <code>setQueryData</code> for instant UI feedback, then <code>invalidateQueries</code> in <code>onSettled</code> to reconcile.</p>`,
  },
  {
    id: "a10-state-mgmt-8",
    category: "state",
    categoryLabel: "State",
    question: `In SWR, what does the dedupingInterval option do, and how does it differ conceptually from React Query's staleTime?`,
    answerHtml: `<p><code>dedupingInterval</code> (default 2s) collapses multiple requests for the same key fired within that window into a single network call, regardless of whether the data is considered 'fresh' for UI purposes — it's purely a request-coalescing guard against duplicate fetches from rapid remounts or rerenders. React Query's <code>staleTime</code> is broader: it gates whether <i>any</i> refetch trigger fires at all (not just deduping concurrent ones) and persists for as long as you configure, not just a short debounce window. SWR is stale-while-revalidate by default — it almost always shows cached data immediately and revalidates in the background, with deduping as the main collision guard rather than a staleness policy.</p>`,
  },
  {
    id: "a10-state-mgmt-9",
    category: "state",
    categoryLabel: "State",
    question: `What does React Query's query key array actually do, beyond acting as a cache lookup key?`,
    answerHtml: `<p>The key array is hashed (deterministically, ignoring key order in objects) to identify a cache entry, but it's also the unit of <i>partial matching</i> for invalidation and refetching. <code>invalidateQueries({queryKey: ['todos']})</code> matches every query whose key starts with <code>['todos']</code> — including <code>['todos', {status: 'done'}]</code> — using prefix matching. This is why key structure is a design decision: putting filter/pagination params inside the key array (rather than baking them into a string) lets you invalidate broad families of related queries with one call instead of tracking every variant manually.</p>`,
  },
  {
    id: "a10-state-mgmt-10",
    category: "state",
    categoryLabel: "State",
    question: `Why can naive use of useQuery with a paginated/infinite list cause a full-screen loading spinner on every page change, and what's the fix?`,
    answerHtml: `<p>Each page number is part of the query key, so changing pages is a <i>different</i> query with no cached predecessor — React Query unmounts the old data and shows <code>isLoading</code> for the new key by default, causing a flash. The fix is <code>placeholderData: keepPreviousData</code> (formerly <code>keepPreviousData: true</code> pre-v5), which keeps rendering the previous page's data (with <code>isPlaceholderData: true</code>) while the new page fetches in the background, avoiding the spinner. For true infinite scroll, <code>useInfiniteQuery</code> is the better primitive since it appends pages into one growing cache entry instead of key-switching.</p>`,
  },
  {
    id: "a10-state-mgmt-11",
    category: "state",
    categoryLabel: "State",
    question: `Zustand stores don't require a Provider wrapping the component tree by default. What does that buy you, and what do you lose compared to Context-based or Redux-with-Provider state?`,
    answerHtml: `<p>You gain: no provider nesting/wrapper hell, the store is a plain module-level object so it can be read/written from outside React (event handlers, non-component utility code, even before first render), and there's zero re-render cost from a Provider value changing since there isn't one. You lose: per-subtree isolation — by default it's a singleton module, so SSR (multiple concurrent requests) or multiple independent instances of the same feature (e.g. two embedded widgets on one screen) need the separate <code>createStore</code> + context-binding pattern Zustand also ships, which reintroduces a Provider for that specific case.</p>`,
  },
  {
    id: "a10-state-mgmt-12",
    category: "state",
    categoryLabel: "State",
    question: `How does Zustand achieve selective re-rendering without requiring memoization wrappers like React.memo on every consumer?`,
    answerHtml: `<p>The <code>useStore(selector)</code> hook subscribes to the store via <code>useSyncExternalStore</code> and runs the selector on every store update, but only triggers a component re-render if the selector's return value changed by reference (default) or by your custom equality function. So <code>useStore(s => s.count)</code> only re-renders when <code>count</code> changes — other state mutations in the same store are invisible to that component. This is the same shape of optimization Context can't give you for free, since Context has no concept of a selector.</p>`,
  },
  {
    id: "a10-state-mgmt-13",
    category: "state",
    categoryLabel: "State",
    question: `What's a common Zustand selector pitfall that causes infinite re-renders or unnecessary re-renders, and how do you avoid it?`,
    answerHtml: `<p>Selecting multiple fields by returning a new object/array literal from the selector (e.g. <code>useStore(s => ({a: s.a, b: s.b}))</code>) creates a new reference every call, defeating the default reference-equality check and causing a re-render on <i>every</i> store update — in React 18 strict/concurrent paths this can also trigger 'getSnapshot should be cached' warnings or update loops. Fix: select primitives individually with separate hook calls, use Zustand's <code>useShallow</code> utility for shallow-equality on the returned object, or pass a custom equality function as the second argument to the store hook.</p>`,
  },
  {
    id: "a10-state-mgmt-14",
    category: "state",
    categoryLabel: "State",
    question: `Jotai's model is described as 'bottom-up' atomic state versus Zustand/Redux's 'top-down' store model. What does that distinction mean in practice for re-renders?`,
    answerHtml: `<p>In Jotai, each atom is its own independent subscription unit by construction — a component using <code>useAtom(countAtom)</code> only ever re-renders when that specific atom (or atoms derived from it) changes, with no selector needed because there's no larger object to select from. In Zustand/Redux, all state lives in one store object and you opt into granularity via selectors; without a selector, the default behavior (especially in Redux's plain <code>useSelector(state => state)</code> misuse) re-renders on any store change. Jotai's tradeoff: composing many small atoms requires more deliberate dependency wiring (derived atoms, atom families) versus reasoning about one centralized shape.</p>`,
  },
  {
    id: "a10-state-mgmt-15",
    category: "state",
    categoryLabel: "State",
    question: `What problem do Jotai atom families solve, and what's the memory-leak risk specific to them?`,
    answerHtml: `<p><code>atomFamily(param => atom(initialValue))</code> generates a parameterized atom per key (e.g. one atom per todo ID, or per list-item) instead of one atom holding a collection, which keeps re-renders scoped to the single item that changed rather than the whole list re-rendering. The risk: the family caches every atom it has ever created keyed by parameter, so atoms for items that no longer exist (deleted todos, unmounted list entries) stay referenced forever unless you call <code>atomFamily.remove(param)</code> explicitly — an unbounded cache that grows with churn if cleanup is forgotten.</p>`,
  },
  {
    id: "a10-state-mgmt-16",
    category: "state",
    categoryLabel: "State",
    question: `Redux Toolkit's createSlice uses Immer under the hood so reducers can 'mutate' state directly. What's the actual mechanism, and what's the failure mode when a reducer accidentally mutates state outside of Immer's draft?`,
    answerHtml: `<p>Immer wraps the draft state in a Proxy; mutations recorded against the proxy are translated into a new immutable state tree via structural sharing, leaving the original untouched until the producer finishes. If code escapes the proxy — e.g. holds a reference to a nested object captured before the reducer ran, or mutates state retrieved from outside the slice (a 'selector result' object) — those mutations bypass Immer's tracking and corrupt the actual previous state in place, breaking time-travel debugging, reference-equality-based selectors (a previously-passing <code>===</code> check that should have changed now silently passes), and memoized selectors that assumed immutability.</p>`,
  },
  {
    id: "a10-state-mgmt-17",
    category: "state",
    categoryLabel: "State",
    question: `RTK Query and React Query/SWR solve overlapping problems. What's a concrete reason a team already using Redux Toolkit might still prefer RTK Query over bolting on React Query, beyond 'one less dependency'?`,
    answerHtml: `<p>RTK Query's cache lives inside the Redux store itself, so server data is visible to Redux DevTools time-travel, can be read by plain <code>useSelector</code> calls anywhere (including outside components, via <code>store.getState()</code>), and composes naturally with existing Redux middleware (e.g. an analytics middleware that taps every dispatched action also sees query lifecycle actions for free). It also reuses the existing slice/selector mental model for the team instead of introducing a second, parallel state paradigm — the real cost is RTK Query's API surface for things like manual cache patching (<code>updateQueryData</code>) is more verbose than React Query's <code>setQueryData</code>.</p>`,
  },
  {
    id: "a10-state-mgmt-18",
    category: "state",
    categoryLabel: "State",
    question: `When is normalized state (entity adapters, normalized shape with IDs) actually worth the complexity in Redux Toolkit, versus when is it overkill?`,
    answerHtml: `<p>Worth it when the same entity appears in multiple places in the UI/state simultaneously and must stay in sync (e.g. a user object referenced from a list, a detail view, and a notifications panel) — without normalization, updating that user means finding and patching every denormalized copy, which is exactly the bug class normalization (via <code>createEntityAdapter</code>) eliminates by storing one canonical copy keyed by ID. It's overkill for state that's fetched, displayed once, and discarded (a single detail screen's data, a one-off form) — the selector/lookup overhead and boilerplate of normalizing aren't paying for anything since there's no duplication to deduplicate.</p>`,
  },
  {
    id: "a10-state-mgmt-19",
    category: "state",
    categoryLabel: "State",
    question: `What's the difference between React Query's refetchOnWindowFocus/refetchOnReconnect defaults on web versus how they need to be wired up in React Native, and why does it matter for a mobile app?`,
    answerHtml: `<p>On web these listen to the browser's <code>visibilitychange</code> and <code>online</code>/<code>offline</code> events automatically. React Native has no DOM, so React Query ships <code>focusManager.setEventListener</code> and <code>onlineManager.setEventListener</code> hooks that you must wire manually using <code>AppState</code> (foreground/background transitions) and <code>@react-native-community/netinfo</code> (connectivity changes) — without this wiring, <code>refetchOnWindowFocus</code> silently does nothing on RN, which matters because 'returning to a backgrounded app' is the mobile equivalent of a tab refocus and is exactly when cached data is most likely to be stale.</p>`,
  },
  {
    id: "a10-state-mgmt-20",
    category: "state",
    categoryLabel: "State",
    question: `What's the difference between Redux Toolkit's createAsyncThunk lifecycle and React Query's mutation lifecycle in terms of what state management responsibility falls on you?`,
    answerHtml: `<p><code>createAsyncThunk</code> generates pending/fulfilled/rejected action types, but <i>you</i> still write the extraReducers (or builder callbacks) that set loading flags, store the result, and store the error — including remembering to reset stale error/loading state on the next dispatch. React Query's <code>useMutation</code>/<code>useQuery</code> derive <code>isPending</code>, <code>isError</code>, <code>data</code>, and <code>error</code> automatically from the promise lifecycle with no reducer code, and additionally give you caching, retries, and dedup for free — the tradeoff is less control over exactly how/where that derived state is stored and a second mental model for what is 'client state' (Redux) vs 'server state' (React Query) in a hybrid codebase.</p>`,
  },
  {
    id: "a10-state-mgmt-21",
    category: "state",
    categoryLabel: "State",
    question: `Why is 'server state' (data from an API) considered fundamentally different from 'client state' (UI toggles, form inputs, theme) in modern state management thinking, and what mistake does treating them the same lead to?`,
    answerHtml: `<p>Server state is owned by a remote system you don't control, can go stale the instant you fetch it, may be shared/edited by other clients, and needs caching, background refetching, and reconciliation — none of which apply to client state, which is synchronously and exclusively owned by the current session. Treating server state like client state (storing API responses directly in Redux/Zustand and manually dispatching fetch/loading/error actions) means hand-rolling cache invalidation, dedup, and staleness logic that React Query/SWR/RTK Query already solve — the common modern split is: client state in Zustand/Jotai/Redux, server state in React Query/SWR/RTK Query, used together rather than picking one for everything.</p>`,
  },
  {
    id: "a10-state-mgmt-22",
    category: "state",
    categoryLabel: "State",
    question: `In a list screen using FlashList/FlatList backed by a Zustand store, what's the risk of selecting the entire array (useStore(s => s.items)) versus selecting per-row data in each renderItem?`,
    answerHtml: `<p>Selecting the whole array in the parent list component means any mutation to <i>any</i> item (even via immutable replace-in-place patterns) changes the array reference, re-rendering the parent and potentially causing FlashList/FlatList to re-evaluate more than necessary. The more scalable pattern is to pass only the item's ID into each row and have each row component call <code>useStore(s => s.itemsById[id])</code> itself — that way updating one item only re-renders that one row's component, not the whole list, mirroring the per-item subscription model Jotai's atomFamily gives you natively.</p>`,
  },
  {
    id: "a10-state-mgmt-23",
    category: "state",
    categoryLabel: "State",
    question: `What does React Query's retry option do by default, and why can the default be actively harmful for a mutation (not a query) against a non-idempotent endpoint?`,
    answerHtml: `<p>By default, queries retry failed requests 3 times with exponential backoff; mutations, by contrast, default to <code>retry: false</code> precisely because retrying a non-idempotent POST (e.g. 'charge card', 'create order') on a transient network blip can duplicate the side effect — the failure might mean 'the server never got it' or 'the server got it and the response was lost,' and blindly retrying assumes the former. If you do enable mutation retries, the endpoint needs an idempotency key or idempotent semantics so a duplicate request is safely a no-op rather than a duplicate charge or duplicate row.</p>`,
  },
  {
    id: "a10-state-mgmt-24",
    category: "state",
    categoryLabel: "State",
    question: `How does Jotai handle async data (e.g. an atom whose initial value is a Promise) differently from how you'd model the same thing in Zustand?`,
    answerHtml: `<p>Jotai atoms can hold a Promise directly, and reading that atom with <code>useAtom</code> inside a component automatically suspends via React Suspense until it resolves — no manual <code>isLoading</code> flag needed, because the atom's pending state <i>is</i> the Promise's pending state, surfaced through Suspense boundaries. Zustand has no built-in async/Suspense integration: an async fetch in a Zustand store is just a regular action that manually sets <code>loading: true</code>, awaits, then sets <code>data</code>/<code>error</code>/<code>loading: false</code> — you get explicit control but have to write the boilerplate Jotai's Promise-as-atom-value pattern gives you implicitly.</p>`,
  },
];

export const ADVANCED10_QUIZ: QuizQuestion[] = [
  {
    id: "a10q-state-mgmt-1",
    category: "state",
    categoryLabel: "State",
    question: `A component reads \`const { user } = useContext(AppContext)\` where AppContext's provider value is \`{ user, theme, setTheme }\` recreated as a new object on every render. The component only cares about \`user\`, which rarely changes, but \`theme\` changes on every screen the user visits. What happens?`,
    options: [`The component never re-renders because it destructures only \`user\``, `The component re-renders every time \`theme\` changes, because it subscribes to the whole context value reference, not the destructured field`, `React automatically splits the subscription based on destructuring`, `Only components that call \`setTheme\` re-render`],
    answer: 1,
    explanationHtml: `<p>Context subscriptions are reference-based on the entire value passed to the Provider, not field-based. Destructuring happens after the re-render is already triggered — it doesn't opt you out of the subscription. This is why a stable-reference split (separate contexts, or \`useMemo\` on the value, or external stores with selectors) is needed to avoid the re-render.</p>`,
  },
  {
    id: "a10q-state-mgmt-2",
    category: "state",
    categoryLabel: "State",
    question: `In React Query, you call \`queryClient.invalidateQueries({ queryKey: ['todos'] })\`. Which queries does this affect?`,
    options: [`Only the exact query with key \`['todos']\` and nothing else`, `Every query in the cache regardless of key`, `All queries whose key starts with \`['todos']\` as a prefix match, e.g. \`['todos']\` and \`['todos', { status: 'active' }]\``, `Nothing, because object keys require \`exact: true\` to match`],
    answer: 2,
    explanationHtml: `<p>React Query uses partial/prefix matching by default for \`invalidateQueries\` — any query key that starts with the given key array is matched and marked stale. \`exact: true\` would narrow it to only that precise key; the default behavior is the broader prefix match, which is what makes structuring keys hierarchically (\`['todos']\`, \`['todos', {filters}]\`) useful for bulk invalidation.</p>`,
  },
  {
    id: "a10q-state-mgmt-3",
    category: "state",
    categoryLabel: "State",
    question: `Why does Zustand's \`useStore(selector)\` typically avoid needing \`React.memo\` on consumers to get fine-grained re-renders, in a way plain Context cannot?`,
    options: [`Zustand re-renders the entire app on every change but batches it efficiently`, `Zustand uses useSyncExternalStore and re-renders a component only if the selector's return value differs by reference (or custom equality) from the previous render, independent of unrelated store changes`, `Zustand stores are immutable so React skips re-renders automatically`, `Zustand requires React.memo on every consumer just like Context does`],
    answer: 1,
    explanationHtml: `<p>The selector pattern means each component declares exactly which derived slice it cares about; the store notifies subscribers on every change, but each subscriber's hook only forces a re-render if its own selector's output changed. Context has no equivalent built-in selector layer — every consumer of a context re-renders whenever the provided value reference changes, full stop.</p>`,
  },
  {
    id: "a10q-state-mgmt-4",
    category: "state",
    categoryLabel: "State",
    question: `A \`useMutation\` call implements optimistic updates: \`onMutate\` cancels in-flight queries, snapshots the previous cache value, and writes the optimistic value. The mutation then fails. What is the correct role of \`onError\` in this pattern?`,
    options: [`onError should call invalidateQueries only, ignoring the snapshot`, `onError should roll back the cache to the snapshot returned by onMutate's context, undoing the optimistic write`, `onError is not needed if onSettled calls invalidateQueries`, `onError should retry the mutation automatically up to 3 times`],
    answer: 1,
    explanationHtml: `<p>\`onMutate\` returns a context object (typically \`{ previousData }\`) that React Query passes to \`onError\` as its third argument. \`onError\` uses that context to restore the cache to its pre-optimistic state via \`setQueryData\`, so the UI doesn't keep showing a change that never actually happened on the server. Relying on \`onSettled\`'s \`invalidateQueries\` alone leaves a window where the UI shows incorrect optimistic data until the refetch completes — explicit rollback in \`onError\` is the more correct, immediate fix.</p>`,
  },
  {
    id: "a10q-state-mgmt-5",
    category: "state",
    categoryLabel: "State",
    question: `Which scenario is the best fit for Jotai's atom-per-item (atomFamily) pattern over a single Zustand store holding an array of items?`,
    options: [`A small static config object that never changes after app launch`, `A large, frequently-updated list (e.g. a live order book or chat thread) where individual rows update independently and only the changed row should re-render`, `Global auth state shared across the whole app`, `A boolean flag toggled by a single settings screen`],
    answer: 1,
    explanationHtml: `<p>atomFamily gives each list item its own independent atom/subscription, so updating one row's data only re-renders the component reading that specific atom — ideal for high-churn, per-row-independent lists. A single Zustand array selected wholesale (or even with a naive selector) tends to re-render the whole list on any item change unless you carefully scope selectors per row, which is exactly the per-item granularity atomFamily provides by default. Static config or a single global boolean has no per-item granularity problem to solve, so the added complexity isn't justified there.</p>`,
  },
  {
    id: "a10q-state-mgmt-6",
    category: "state",
    categoryLabel: "State",
    question: `Why does React Query default \`retry: false\` for mutations while defaulting queries to retry 3 times with backoff?`,
    options: [`Mutations are slower than queries so retries would time out`, `Queries are idempotent reads, safe to retry; mutations are often non-idempotent writes (e.g. POST/charge), where blind retries risk duplicating the side effect`, `It's an arbitrary default with no technical reasoning`, `Mutations always fail permanently on first error so retrying is pointless`],
    answer: 1,
    explanationHtml: `<p>A failed GET can be safely retried because reading data twice has no side effect. A failed POST/PUT for something like 'place order' or 'charge card' might have actually succeeded server-side even though the client saw a network error — retrying blindly risks a duplicate charge or duplicate resource. That's why mutations require the caller to opt in to retries, ideally paired with idempotency keys on the backend.</p>`,
  },
  {
    id: "a10q-state-mgmt-7",
    category: "state",
    categoryLabel: "State",
    question: `On React Native, why doesn't React Query's \`refetchOnWindowFocus\` work out of the box the way it does on web?`,
    options: [`React Query doesn't support React Native at all`, `RN has no DOM visibilitychange/online events; you must wire focusManager and onlineManager to AppState and NetInfo manually for focus/reconnect refetching to work`, `refetchOnWindowFocus is deprecated and replaced by refetchOnAppForeground automatically on RN`, `RN apps never lose focus so the feature is irrelevant`],
    answer: 1,
    explanationHtml: `<p>React Query's focus/online detection is built on browser APIs (\`visibilitychange\`, \`online\`/\`offline\`) that don't exist in React Native. The library exposes \`focusManager.setEventListener\` and \`onlineManager.setEventListener\` precisely so RN apps can plug in \`AppState\` (foreground/background) and \`@react-native-community/netinfo\` (connectivity) — without doing this manually, focus-triggered and reconnect-triggered refetches silently never fire on a mobile app, which is significant because backgrounding/foregrounding is the dominant 'staleness' trigger on mobile.</p>`,
  },
];
