// Batch 9 — React Navigation stack/tab/drawer nesting, deep linking & universal links, Expo Router file-based routing (groups, dynamic segments, layouts), and navigation state persistence.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED9_FLASHCARDS: Flashcard[] = [
  {
    id: "a9-navigation-1",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `In Expo Router, a route group like (tabs) doesn't show up in the URL. What actually happens when you nest two navigator-defining groups, e.g. app/(drawer)/(tabs)/_layout.tsx?`,
    answerHtml: `<p>Route groups exist so you can compose independent navigator trees — drawer, tabs, stack — without that structure leaking into the URL; nesting <code>(drawer)/(tabs)</code> gives you a real Drawer-wrapping-Tabs navigator tree, not just folder organization. Mechanically: parens mark a <b>route group</b> — the segment name is stripped from the URL, but its <code>_layout.tsx</code> still mounts and wraps every screen below it.</p><p><b>Red flag:</b> forgetting that each ancestor layout can render its own header — nest two navigator-defining groups without setting <code>headerShown: false</code> on the outer one and you ship two headers stacked on screen. Route params also don't pass through groups automatically; re-declare them in each nested layout's <code>Stack.Screen</code>/<code>Tabs.Screen</code> options.</p><p><b>I treat each group boundary as its own navigator with its own header responsibility, and I explicitly suppress every ancestor's header except the one I want visible.</b></p>`,
  },
  {
    id: "a9-navigation-2",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `Expo Router supports [...slug] and [[...slug]]. What's the functional difference, and when does each fail to match?`,
    answerHtml: `<p>The choice determines whether the bare parent path needs its own route or falls through to the catch-all — get it wrong and you either 404 on the parent path or ship a route conflict. <code>[...slug]</code> is a <b>required</b> catch-all — it needs at least one remaining segment, so the bare parent path doesn't match it (you'd need a sibling <code>index.tsx</code> for that case).</p><p><code>[[...slug]]</code> is <b>optional</b> — it matches zero or more segments, so it also matches the parent path. Declaring both an <code>index.tsx</code> and a <code>[[...slug]]</code> in the same folder is a conflict, since the optional catch-all already covers the empty case.</p><p><b>I reach for the optional form when the catch-all should also own the bare parent path, and the required form plus a separate index route when they're semantically different screens.</b></p>`,
  },
  {
    id: "a9-navigation-3",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `Why might pushing a new screen onto a React Navigation native-stack inside a tab navigator NOT update the URL the way you'd expect on Expo Router web builds?`,
    answerHtml: `<p>This matters because a desynced URL/back-history breaks shareable links and the browser back button on web — the exact thing deep linking is supposed to guarantee. Expo Router derives the URL from the full navigation state tree, but only the <i>focused</i> path within each parallel navigator (tabs, drawer) contributes to the URL at a given level. If you imperatively push via a stack ref that isn't the currently focused tab's stack, the state updates but the visible tab doesn't change — so the URL bar (web) or back button history can desync from what's on screen unless you also switch focus to that tab.</p><p>This is also why deep-linking into a specific tab's nested stack screen requires the URL to encode the full group/segment path, not just the screen name — Expo Router needs every ancestor navigator's active route to reconstruct state.</p><p><b>I always navigate through the router API rather than a raw stack ref, so focus and URL stay in sync across parallel navigators.</b></p>`,
  },
  {
    id: "a9-navigation-4",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `What's the difference between a 'universal link' (iOS) / 'app link' (Android) and a custom URL scheme, and why do interviewers care which one you used for a feature?`,
    answerHtml: `<p>A custom scheme (<code>myapp://</code>) requires no domain verification — any app can register it, it works without the app installed only by failing silently or falling through, and it doesn't work from a plain web link tapped in Safari/Chrome (shows a UI prompt or fails). <b>Universal links / app links</b> use <code>https://</code> URLs tied to a real domain, verified via <code>apple-app-site-association</code> (iOS) or <code>assetlinks.json</code> (Android) hosted at <code>/.well-known/</code>. They degrade gracefully to the website if the app isn't installed, work from any context (Messages, email, Safari), and are the only option Apple's App Review and most marketing/attribution tooling expect for real deep-linking features.</p><p><b>I default to universal links for anything that needs to work reliably from Messages, email, or a marketing site — custom schemes only for internal or dev-only redirects like OAuth callbacks.</b></p>`,
  },
  {
    id: "a9-navigation-5",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `Where does Expo Router read the universal-link domain/path configuration from, and what's the most common reason AASA verification fails in production despite a correct app.json?`,
    answerHtml: `<p>Expo Router itself doesn't generate the AASA file — you host <code>apple-app-site-association</code> (no extension, served as <code>application/json</code>, no redirects) at your domain's <code>/.well-known/</code> path, and declare the association in <code>app.json</code> under <code>ios.associatedDomains</code> (e.g. <code>applinks:gerardocordero.dev</code>).</p><p>The most common production failure: the file is served with a redirect (e.g. HTTP→HTTPS or a trailing-slash redirect) or wrong content-type. Apple's CDN caches AASA (roughly a 1–6 hour refresh window) and each device only re-checks for an update on its own cadence — about once a week, plus on fresh install/update — so a server-side fix doesn't reach already-installed devices immediately, making the bug look intermittent during testing.</p><p><b>Red flag:</b> assuming a broken AASA is always a config bug — if it verifies on fresh installs but fails on existing devices, it's very likely propagation delay, not your <code>app.json</code>.</p><p><b>When universal links look intermittently broken, I check the AASA content-type and redirect chain first, then give Apple's CDN and per-device refresh cadence time before touching code.</b></p>`,
  },
  {
    id: "a9-navigation-6",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `When you have both an Expo Router app and a separate marketing website on the same apex domain, what's the deep-linking pitfall when both need .well-known files?`,
    answerHtml: `<p>Only one <code>apple-app-site-association</code> and one <code>assetlinks.json</code> can exist per domain (per <code>/.well-known/</code> path) — you can't have two unrelated apps' AASA files merged unless you intentionally combine them into one JSON with multiple app ID entries and path patterns. If the marketing site (e.g. a separate Next.js Cloudflare Pages deploy) and the Expo app's universal links share a domain, deploying the marketing site's own AASA can silently overwrite the app's, breaking deep links with no build-time error — it only surfaces at runtime when links stop opening the app.</p><p><b>On a shared apex domain, I treat the AASA/assetlinks.json as one owned artifact with multiple app entries, not something each deploy can write independently.</b></p>`,
  },
  {
    id: "a9-navigation-7",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `Explain initialRouteName in an Expo Router _layout.tsx Stack/Tabs and why omitting it can break deep links into a nested stack.`,
    answerHtml: `<p><code>initialRouteName</code> tells the navigator which screen to mount as the back-stack root <i>when that navigator is entered without an explicit route</i> (e.g. tapping a tab icon, or deep-linking directly to a nested screen). Without it, Expo Router defaults to <code>index</code> if present.</p><p>If you deep-link straight to <code>/(tabs)/profile/edit</code> and the profile stack has no <code>initialRouteName</code> (or no <code>index.tsx</code>), the back button on <code>edit</code> may have no synthesized parent screen to return to, landing the user on an empty stack or causing an immediate app-exit on back-press — React Navigation calls this 'rehydrating' the stack and it needs a known root to synthesize the missing intermediate screens.</p><p><b>I always set an explicit initialRouteName (or ship an index.tsx) on any navigator that can be entered directly via deep link, so React Navigation has a root to rehydrate the stack against.</b></p>`,
  },
  {
    id: "a9-navigation-8",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `What is 'state restoration' / navigation persistence in React Navigation, and what's the concrete risk of enabling it in production without versioning?`,
    answerHtml: `<p>React Navigation can serialize the full navigation state tree to storage (<code>onStateChange</code> + <code>AsyncStorage</code>, wired via <code>NavigationContainer</code>'s <code>initialState</code>/restoration props, or automatically in Expo Router dev mode) and rehydrate it on next launch, so the user reopens the app on the same screen/stack depth they left.</p><p>The risk: persisted state references route <i>names</i> and param shapes from the build that saved it. If you rename a route, remove a screen, or change a param's required shape in a later release, rehydrating old persisted state can throw or navigate to a non-existent route. Production usage needs either a state-shape version stamp (discard if mismatched) or to disable persistence outside development.</p><p><b>Red flag:</b> shipping navigation-state persistence to production without a version stamp — it works fine until the next release renames a route, then it crashes on launch for exactly the users you were trying to help.</p><p><b>I version any persisted navigation state and discard-to-initial-route on mismatch, rather than trusting old state to always rehydrate cleanly.</b></p>`,
  },
  {
    id: "a9-navigation-9",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `Why is persisting navigation state generally discouraged in production apps (versus just using it for Fast Refresh / dev convenience)?`,
    answerHtml: `<p>The deeper issue isn't just shape-versioning risk — a resumed screen can point at data that's since gone stale, been deleted, or now requires re-auth, so the destination screen needs defensive guards it wouldn't otherwise carry. Restored navigation state can reopen a user on a screen whose underlying data is now stale, deleted, or requires re-authentication (e.g. a deep-linked order-detail screen for an order that's since been cancelled).</p><p>Most production apps instead persist only specific <i>app state</i> (last tab, last filter) explicitly and rebuild navigation from a known-good initial route, rather than serializing the whole tree — it's primarily an Expo Router/RN dev-mode convenience (survives Fast Refresh and Metro reloads) rather than a recommended production UX pattern.</p><p><b>I persist the specific bits of state I actually want to restore — last tab, last filter — and let navigation rebuild from a known-good root, instead of serializing the whole tree.</b></p>`,
  },
  {
    id: "a9-navigation-10",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `What's the difference between Expo Router's <Link> with href as an object vs. a string, and when does the object form matter for type safety?`,
    answerHtml: `<p>A string href (<code>/profile/123</code>) is parsed at runtime and isn't checked against your actual route params. The object form (<code>{ pathname: '/profile/[id]', params: { id: '123' } }</code>) lets <b>Typed Routes</b> (enabled via <code>experiments.typedRoutes</code> in app.json, generating <code>.expo/types</code>) validate at compile time that the pathname exists and the params object matches the dynamic segments that route declares — catching a typo'd route name or missing param as a TS error instead of a silent 404/blank screen at runtime.</p><p><b>I reach for the object form with Typed Routes on any app past prototype stage — a typo'd route name becomes a compile error instead of a runtime 404.</b></p>`,
  },
  {
    id: "a9-navigation-11",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `How does Expo Router decide which screen owns the native header vs. which uses a custom header, when a Stack screen sits inside a Tabs screen inside a Drawer?`,
    answerHtml: `<p>Getting this wrong ships either duplicate headers or a missing back/hamburger affordance — both read as an amateur navigation implementation. Each navigator level (Stack, Tabs, Drawer) can independently render its own header/chrome; by default you'd get one from each ancestor that has header chrome enabled, which usually isn't what you want. The standard pattern: pick exactly one level to own the visible header (commonly the innermost Stack) and set <code>headerShown: false</code> in every ancestor layout's screen options.</p><p><b>Red flag:</b> suppressing an ancestor's header without re-wiring its navigation affordance — a suppressed Drawer header means its hamburger icon disappears too, unless you manually add it back via <code>headerLeft: () => &lt;DrawerToggleButton /&gt;</code> from <code>@react-navigation/drawer</code> on the surviving header.</p><p><b>I pick exactly one navigator level to own the header, and I explicitly re-wire any lost navigation affordance (hamburger, back chevron) onto that surviving header.</b></p>`,
  },
  {
    id: "a9-navigation-12",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `What problem does React Navigation's linking config 'getStateFromPath' / 'getPathFromState' solve, and why would you ever need to override Expo Router's auto-generated versions?`,
    answerHtml: `<p>These two functions are the bidirectional bridge between a URL string and the navigator's internal state tree — <code>getStateFromPath</code> parses an incoming URL (cold start, deep link, universal link) into nested route state; <code>getPathFromState</code> does the reverse for generating shareable URLs and web history entries. Expo Router generates both from your file structure automatically.</p><p>You'd override them when the URL shape you need (for SEO, marketing, or backward compatibility with old links) doesn't map cleanly to your file structure — e.g. legacy <code>/p/:id</code> URLs that must keep working after you renamed the folder to <code>/products/[id]</code>, requiring a custom alias layer rather than a file rename.</p><p><b>I only override Expo Router's generated linking functions when I have a URL contract I can't change — usually SEO or backward compatibility with links already in the wild.</b></p>`,
  },
  {
    id: "a9-navigation-13",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `In a modal presented via Expo Router (a route with presentation: 'modal' in its Stack.Screen options), why can dismissing it via swipe-down desync from your in-app navigation state, and what's the correct hook to guard it?`,
    answerHtml: `<p>Getting this wrong means confirmation dialogs ("discard changes?") silently don't fire for the most common exit gesture on iOS, quietly losing user data. iOS's native swipe-to-dismiss gesture for a modal stack screen happens at the native UIKit level, separate from any explicit <code>router.back()</code> button handler — so code that only manages related state (a form draft, an 'unsaved changes' flag) inside a back-button's onPress never runs when the user swipes instead.</p><p><b>Red flag:</b> reaching for <code>beforeRemove</code> + <code>e.preventDefault()</code> to guard a swipe-dismissed modal — it's documented as not working reliably with <code>@react-navigation/native-stack</code> (Expo Router's default stack). Use the <code>usePreventRemove</code> hook instead, and consider <code>gestureEnabled: false</code> while a confirmation is pending.</p><p><b>For any dismissible modal holding unsaved state, I guard with usePreventRemove rather than beforeRemove, because the swipe gesture bypasses the button handler entirely.</b></p>`,
  },
  {
    id: "a9-navigation-14",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `What's the difference between resetting navigation state (navigation.reset()) and simply pushing/replacing, and when is reset mandatory rather than a style choice?`,
    answerHtml: `<p><code>push</code>/<code>replace</code> mutate the existing stack (add a frame, or swap the top frame) and preserve everything underneath. <code>navigation.reset()</code> discards the entire back-stack of that navigator and installs a fresh one. Reset is mandatory — not stylistic — after state-invalidating transitions like logout or completing a multi-step flow (checkout, onboarding): leaving the old stack intact means the back button/swipe can return the user to a screen built on now-invalid auth/session state (e.g. back-swiping from the login screen into a still-mounted 'my orders' screen that immediately 401s or shows stale data).</p><p><b>Red flag:</b> reaching for <code>router.dismissAll()</code> as your logout reset — it only pops stacked/modal screens back to the closest stack's root, it doesn't clear sibling navigators (tabs/drawer) or reset auth-gated state. The idiomatic Expo Router pattern for logout is a layout-level auth guard (<code>&lt;Redirect /&gt;</code> or <code>router.replace()</code> in a protected group's <code>_layout.tsx</code>) rather than manually resetting a stack ref.</p><p><b>After any auth/session-invalidating transition, I treat a stack reset (or an Expo Router auth-guard redirect) as mandatory, not a style preference — an intact old stack is a bug waiting for a back-swipe.</b></p>`,
  },
  {
    id: "a9-navigation-15",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `How does Expo Router structure auth-gated routing differently from the classic React Navigation pattern of conditionally rendering an AuthStack vs. AppStack at the NavigationContainer root?`,
    answerHtml: `<p>The single-tree layout-guard pattern costs a bit more upfront complexity — auth state has to be checked at the right layout boundary instead of one top-level switch — but the classic swap-the-whole-navigator-tree pattern pays for that simplicity by discarding all navigation state on every auth transition, which breaks the "redirect to login, then resume where you were" flow most auth-gated products need. Expo Router favors a single tree with a layout-level guard: a root <code>_layout.tsx</code> (or a group's layout) checks auth state and calls <code>router.replace('/login')</code> or renders a <code>Redirect</code> component for protected groups, while unprotected routes stay mounted.</p><p>Treat the extra guard-placement complexity as an investment: it's what lets a deep link into a protected route redirect-to-login and then replay the original target after auth, rather than dumping the user on a generic home screen post-login.</p><p><b>I use a layout-level auth guard rather than swapping the whole navigator tree, specifically so a protected deep link can survive a login redirect and resume.</b></p>`,
  },
  {
    id: "a9-navigation-16",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `What's a 'parallel route' / 'array of Slot' pattern conceptually missing from React Navigation's mental model that Expo Router's groups partially address, and what's still NOT possible compared to Next.js parallel routes?`,
    answerHtml: `<p>This gap matters when a screen needs two independently-loading, independently-erroring regions (e.g. a sidebar feed and a main detail pane) without one blocking the other's back-navigation. Next.js's <code>@slot</code> parallel routes let multiple independent route trees render simultaneously in named slots within one layout, each with its own loading/error boundary and independent back-navigation. Expo Router has no equivalent primitive — its groups (<code>(tabs)</code>, <code>(drawer)</code>) map onto React Navigation's existing parallel <i>navigators</i> (Tabs, Drawer), which do render multiple screens 'simultaneously' in the sense of staying mounted, but they share one global back/history stack semantics per navigator type and don't support arbitrary named slots inside an arbitrary layout the way Next.js does. You can approximate independent regions with nested navigators, but not truly independent, declaratively-named concurrent route trees in one screen.</p><p><b>I tell interviewers Expo Router's groups map to React Navigation's parallel navigators, not to Next.js-style named slots — the closest approximation is nested navigators, with shared back-stack semantics per navigator type.</b></p>`,
  },
  {
    id: "a9-navigation-17",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `Why does adding a Stack.Screen with options={{ animation: 'none' }} sometimes still show a flash/flicker on Android even though iOS looks instant?`,
    answerHtml: `<p>On the new architecture with native-stack (<code>@react-navigation/native-stack</code>, which Expo Router uses by default), <code>animation: 'none'</code> disables the transition animation, but Android's activity/fragment-based native stack can still show a brief flash from the underlying surface composition (a frame of the previous screen's background or a flicker during the fragment swap) that iOS's UIKit-based transition doesn't exhibit, because the two platforms' native navigation primitives have different compositing models. This is a longstanding, well-documented platform asymmetry in <code>react-navigation</code>/<code>react-native-screens</code> issue trackers, not a bug in your config.</p><p><b>Red flag:</b> burning time tuning animation config or timing to chase this away — the fix is a consistent screen background color so the flash is imperceptible, not further animation tweaking.</p><p><b>When a fragment-swap flash shows up only on Android, I match background colors across screens rather than fighting the platform's compositing model.</b></p>`,
  },
  {
    id: "a9-navigation-18",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `What's the practical difference between @react-navigation/native-stack and @react-navigation/stack (the JS-based one), and why does Expo Router default to native-stack?`,
    answerHtml: `<p><code>native-stack</code> renders transitions using the platform's actual native navigation controller (UINavigationController on iOS, Fragment-based on Android), so animations, gestures, and header behavior are pixel-identical to a native app and run on the native thread rather than the JS thread. The older JS-based <code>stack</code> re-implements the transition in React Native/Animated, giving full cross-platform customizability (e.g. fully custom shared-element-style transitions) at the cost of JS-thread-driven animation, which is more prone to jank under load. Expo Router defaults to native-stack because most apps want the free, correct platform-native feel, and you opt into the JS stack only when you need transitions native-stack can't express.</p><p><b>I default to native-stack for the native feel and thread-safety, and reach for the JS stack only when I need a transition native-stack genuinely can't express.</b></p>`,
  },
  {
    id: "a9-navigation-19",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `How do you pass a non-serializable value (e.g. a callback, a Date, a class instance) to a screen via React Navigation/Expo Router params, and why is this restricted?`,
    answerHtml: `<p>This restriction protects two things: state-restoration correctness and re-render performance, since navigation params live in the serializable state tree that gets diffed and can be persisted to storage. You generally can't pass a function or class instance directly — passing one triggers a 'Non-serializable values were found in the navigation state' warning and breaks state persistence/restoration.</p><p><b>Red flag:</b> passing a callback prop through navigation params to "just get data back" from a screen — it's a common shortcut that breaks the moment persistence or a state diff touches that param.</p><p>The workaround is to pass an ID/key as the param and look up the actual object from a store (Zustand/Context/React Query cache) inside the destination screen, keeping the navigation layer itself dumb and serializable.</p><p><b>I pass IDs through navigation params and resolve the real object from a store on the destination screen, keeping params serializable end to end.</b></p>`,
  },
  {
    id: "a9-navigation-20",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `When using Expo Router with \`output: 'static'\` web export, what navigation-related capability is lost compared to the SSR/server output mode?`,
    answerHtml: `<p>This determines whether you can ship a fully dynamic site (user-generated slugs, per-request auth redirects) as a static export at all — the answer is no, and picking static export anyway is a design mistake that surfaces late. Static export pre-renders each route to its own HTML file at build time with no server runtime, so anything requiring per-request logic — server-side redirects based on auth/cookies, dynamically generated routes not known at build time, or API routes that need to run on every request — isn't available. Dynamic segments (e.g. <code>app/post/[id].tsx</code>) need their possible values enumerated at build time via a <code>generateStaticParams</code> function, or they fall back to client-side resolution after hydration. Server output (Node.js/Bun server, or EAS Hosting) keeps a runtime that can resolve routes and API handlers per request, which is why a fully dynamic, infinitely-parameterized site (e.g. user-generated slugs) needs server output rather than static export.</p><p><b>Before picking static export, I check whether every route's params are enumerable at build time — if they're not, that's a server-output requirement, not a static-export limitation to work around.</b></p>`,
  },
  {
    id: "a9-navigation-21",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `Why might two sibling apps that both define \`myapp://\` as a custom scheme cause a non-deterministic deep-link failure on Android but a clearer failure on iOS?`,
    answerHtml: `<p>This is exactly why production deep-linking shouldn't rely on a custom scheme alone — the same scheme string can be claimed by more than one installed app, so resolution isn't guaranteed to hit your app at all. Custom URL schemes aren't required to be globally unique — nothing prevents two installed apps from registering the same scheme. On Android, when multiple installed apps register the same scheme, the OS shows a disambiguation chooser dialog letting the user pick (and optionally set a default) — so behavior varies until the user makes a choice, and once they do, the system silently routes to whichever app they picked. iOS historically also allowed multiple apps to claim the same scheme with similarly nondeterministic resolution, which is precisely why Apple pushes universal links (domain-verified, so only the domain-owner's app can claim it) as the supported mechanism for any deep-linking feature you depend on in production.</p><p><b>Custom scheme collisions are the concrete reason I never depend on a bare custom scheme for a production deep-link feature — only a domain-verified universal/app link guarantees the OS routes to my app.</b></p>`,
  },
  {
    id: "a9-navigation-22",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `What's the role of \`scheme\` in app.json for an Expo Router app, and what's a common mistake when an app needs to support both a custom scheme and universal links simultaneously?`,
    answerHtml: `<p><code>expo.scheme</code> registers the custom URL scheme (<code>myapp://</code>) used for things like OAuth redirect callbacks, Expo Go/dev-client deep links during development, and any internal-tooling links. Universal links are configured separately via <code>ios.associatedDomains</code> / Android <code>intentFilters</code> and don't depend on <code>scheme</code> at all.</p><p><b>Red flag:</b> assuming setting <code>scheme</code> alone enables "deep linking" broadly — it doesn't give you the graceful-degradation, no-prompt, tappable-from-anywhere behavior of universal links. Teams ship only the custom scheme, test it via \`npx uri-scheme open\`, and then discover in production that real-world links (Messages, email) don't open the app because universal link domain verification was never set up.</p><p><b>Scheme and universal-link config are two separate systems that happen to live in the same app.json — I set up both explicitly rather than assuming one implies the other.</b></p>`,
  },
];

export const ADVANCED9_QUIZ: QuizQuestion[] = [
  {
    id: "a9q-navigation-1",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `An Expo Router app has app/(tabs)/(stack)/profile/[id].tsx. A universal link arrives for https://example.com/profile/42 while the app is cold-started. What must Expo Router resolve, beyond just matching [id] to 42, for the screen to render in the correct place?`,
    options: [`Nothing else; dynamic segment matching alone is sufficient`, `It must reconstruct the full ancestor navigator state (which tab, which stack position) so the screen mounts inside the correct nested navigators, not just match the leaf route`, `It must re-run the Metro bundler to resolve the new route`, `It must convert the universal link into a custom scheme link first`],
    answer: 1,
    explanationHtml: `<p>Matching <code>[id]</code> to <code>42</code> is necessary but not sufficient — that's the misconception behind "dynamic segment matching alone is sufficient." Deep linking into a nested file structure requires <code>getStateFromPath</code> to build state for every ancestor navigator (the tab group, the inner stack) on the path, not just the leaf screen — otherwise React Navigation has no tab/stack context to mount the screen into.</p>`,
  },
  {
    id: "a9q-navigation-2",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `Why does Expo Router favor @react-navigation/native-stack as its default stack implementation rather than the JS-based @react-navigation/stack?`,
    options: [`native-stack is the only one that supports TypeScript`, `native-stack delegates transitions and gestures to the platform's actual native navigation controller, giving pixel-correct platform feel without JS-thread animation work`, `JS-based stack doesn't support dynamic route params`, `native-stack is required for static web export`],
    answer: 1,
    explanationHtml: `<p>TypeScript support is unrelated to the transition implementation — both stacks work fine with TypeScript, so that option is a distractor with no basis. native-stack wraps the real UINavigationController (iOS) / Fragment-based stack (Android), so transitions run natively rather than being reimplemented in JS/Animated — better default fidelity and performance, at the cost of less transition customizability than the JS stack offers.</p>`,
  },
  {
    id: "a9q-navigation-3",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `A team persists React Navigation state to AsyncStorage for crash-recovery UX. After a release that renames a route and changes a required param shape, some users' apps crash on next launch during state restoration. What's the correct production-grade mitigation?`,
    options: [`Disable AsyncStorage entirely so navigation never persists`, `Stamp persisted state with a schema/app version and discard (fall back to initial route) on mismatch before rehydrating`, `Wrap every screen component in a try/catch`, `Force all users to reinstall the app on every release`],
    answer: 1,
    explanationHtml: `<p>Disabling persistence entirely throws away the crash-recovery UX the feature exists for — it fixes the crash by deleting the feature, not by fixing it. Persisted navigation state is only safe to rehydrate if its shape matches the current route table. Versioning the persisted blob and discarding on mismatch (falling back to a known-good initial route) avoids crashing on stale, incompatible state — this is exactly why most production apps avoid blanket state persistence outside development.</p>`,
  },
  {
    id: "a9q-navigation-4",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `What's the key functional difference between the required catch-all [...slug].tsx and the optional catch-all [[...slug]].tsx in Expo Router?`,
    options: [`There is no functional difference, only a naming convention`, `[[...slug]] also matches the parent path with zero segments; [...slug] requires at least one segment and needs a separate index route for the bare path`, `[...slug] only works on web builds; [[...slug]] only works on native`, `[[...slug]] disables TypeScript route typing`],
    answer: 1,
    explanationHtml: `<p>They're not interchangeable naming conventions — the double-bracket form makes the catch-all optional, matching zero or more segments (including none), so it also covers what would otherwise be a separate <code>index.tsx</code>. The single-bracket form requires at least one segment, so a bare parent path needs its own index route.</p>`,
  },
  {
    id: "a9q-navigation-5",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `An app's apple-app-site-association file is correct in app.json's associatedDomains, but universal links still fail to open the app for users who installed weeks ago, while fresh installs work fine. What's the most likely cause?`,
    options: [`The app's bundle ID changed and is now invalid`, `Apple's CDN caches AASA (hours-scale refresh) and each already-installed device only re-checks for an updated copy on its own periodic cadence (roughly weekly), so a server-side fix doesn't reach existing devices immediately`, `Universal links require the app to be open in the background at all times`, `AASA files expire after 24 hours and must be regenerated daily`],
    answer: 1,
    explanationHtml: `<p>A bundle ID change would break the app consistently for everyone, including fresh installs — it wouldn't explain a fresh-install/existing-install split. Apple's CDN caches AASA content for hours at a time, and devices that already have the app installed only periodically re-check for an updated association file (roughly once a week) rather than on every link tap — so a server-side AASA fix doesn't retroactively help devices running on a cached, broken version until their next scheduled re-check or a fresh install/update.</p>`,
  },
  {
    id: "a9q-navigation-6",
    category: "navigation",
    categoryLabel: "Navigation",
    question: `After logging a user out, why is navigation.reset() (or an equivalent full-tree reset / auth-guard redirect) preferred over a normal push/replace?`,
    options: [`Reset is purely a stylistic/animation choice with no functional difference`, `Push/replace leaves the old authenticated screens in the back-stack, so back-navigation or swipe gestures could return the user to screens built on now-invalid session state`, `React Navigation requires a reset call before any navigation.replace() can be used`, `Reset is needed only to satisfy TypeScript's navigation typing`],
    answer: 1,
    explanationHtml: `<p>Calling it a style choice misses the real risk: push/replace only touches the top of the stack; everything beneath (e.g. authenticated screens) stays mounted and reachable via back-navigation. After logout, that's a real bug risk — a back-swipe could land on a screen expecting a valid session. A full reset (or, in Expo Router, an auth-guard redirect in a protected layout) discards the stale stack and installs a clean one rooted at the login screen. Note: Expo Router's <code>router.dismissAll()</code> is not equivalent here — it only pops modal/stacked screens to the closest stack's root, it doesn't reset sibling navigators or auth-gated state.</p>`,
  },
];
