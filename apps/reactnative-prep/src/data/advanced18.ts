// Batch 18 — NativeWind/StyleSheet performance, responsive design (useWindowDimensions, Platform.select), dark mode theming, and platform-specific UI divergence.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED18_FLASHCARDS: Flashcard[] = [
  {
    id: "a18-styling-1",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `NativeWind compiles className strings to StyleSheet objects at build time. What happens at runtime when a className is built dynamically, e.g. with a template literal driven by a JS variable?`,
    answerHtml: `<p>Dynamic classNames quietly downgrade NativeWind from its compiled, zero-runtime-cost path to a slower runtime resolution path &mdash; that's the practical impact worth leading with. NativeWind's compiler extracts class names statically from the source AST during build, so it can only resolve literal strings it can actually see. A template literal built from a runtime variable isn't visible at compile time, so NativeWind falls back to its <b>runtime CSS interop</b> resolution path instead of a precompiled <code>StyleSheet</code> entry &mdash; slower, and it can miss platform-specific resolution that the compiler would otherwise bake in. Prefer static arbitrary-value strings (<code>className="w-[200px]"</code>) or drive dynamic values through the <code>style</code> prop instead.</p><p><b>Red flag:</b> assuming any string you can build in JS is fair game for className &mdash; if NativeWind can't see the literal at build time, it can't compile it.</p><p><b>I keep dynamic values out of className strings and push them through the style prop, so NativeWind never has to fall back to runtime resolution.</b></p>`,
  },
  {
    id: "a18-styling-2",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `With the New Architecture, styles are no longer serialized across a bridge. Why does <code>StyleSheet.create</code> still matter for performance?`,
    answerHtml: `<p>The old justification (avoiding bridge serialization via numeric IDs) is gone under Fabric/JSI, since host objects can be passed directly. <code>StyleSheet.create</code> still helps because it returns a <b>referentially stable</b> object across renders &mdash; an inline <code>style={{...}}</code> literal is a new object every render, which defeats shallow-equality checks in <code>React.memo</code>'d children and forces Fabric to re-diff style props it didn't actually need to. It also validates the style shape once instead of on every render.</p><p><b>Red flag:</b> saying StyleSheet.create "doesn't matter anymore" under the New Architecture because bridge serialization is gone &mdash; that conflates the old justification with the current one.</p><p><b>I still use StyleSheet.create today, not for the bridge, but because a stable object reference is what lets React.memo actually skip a re-render.</b></p>`,
  },
  {
    id: "a18-styling-3",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `A FlashList row changes background color and border <b>width</b> on press. Why can this cause jank even though no JS work is happening, and how do you fix it?`,
    answerHtml: `<p>The root cause is the box model, not the JS thread &mdash; that's the framing worth leading with. Background-color changes are pure paint (no relayout), but a border-<b>width</b> change alters the box model and forces Fabric's UI-thread layout pass to remeasure &mdash; on lower-end Android devices, repeated relayouts across many recycled rows can still drop frames. (Border-<b>radius</b>, by contrast, is paint-only and does not trigger relayout, so it's not the culprit here.) The fix is to keep press-feedback properties paint-only &mdash; use opacity or background color instead of border width/thickness &mdash; and, for true 60fps feedback, drive the change with Reanimated on the UI thread (<code>useAnimatedStyle</code>) instead of setState-driven React re-render, which skips the JS thread round-trip entirely.</p><p><b>Red flag:</b> treating all border properties as equivalent for perf purposes &mdash; radius is paint-only, width/thickness forces relayout.</p><p><b>I keep interactive feedback paint-only and move anything perf-sensitive onto the UI thread with Reanimated.</b></p>`,
  },
  {
    id: "a18-styling-4",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What's the practical difference between NativeWind's \`dark:\` variant and manually branching with \`useColorScheme()\`?`,
    answerHtml: `<p><code>dark:</code>'s role is a declarative styling variant that owns its own OS-appearance subscription; <code>useColorScheme()</code>'s role is an imperative hook you reach for when the theme needs to drive non-style logic. \`dark:\` classes are resolved by NativeWind's style-resolution layer reacting to the OS appearance/\`Appearance\` change through its own internal subscription &mdash; you don't write any branching logic, and every styled node using \`dark:\` updates consistently without you having to thread a color-scheme value through props yourself. Branching manually with <code>useColorScheme()</code> still triggers a component re-render on the appearance-change event (NativeWind's \`dark:\` resolution isn't free of re-renders either &mdash; it's driven by the same underlying change event), but now <i>you</i> own recomputing the conditional and re-passing it down, with more JS call-site code and more chances to miss a child that doesn't re-render.</p><p><b>Red flag:</b> assuming useColorScheme() is somehow more "real-time" than dark: &mdash; both ride the same OS event, only who owns the subscription differs.</p><p><b>I reach for dark: for purely visual changes since it centralizes the subscription, and only drop to useColorScheme() when the theme choice affects non-style logic like an icon asset or an analytics event.</b></p>`,
  },
  {
    id: "a18-styling-5",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why is \`useWindowDimensions()\` generally preferred over \`Dimensions.get('window')\` for responsive layouts, and what's the one case where \`Dimensions.get\` is still the right call?`,
    answerHtml: `<p><code>useWindowDimensions()</code>'s role is a subscription hook for anything rendered on screen; <code>Dimensions.get('window')</code>'s role is a one-shot synchronous read for use outside the render tree. <code>useWindowDimensions()</code> subscribes to dimension-change events and triggers a re-render automatically on rotation, foldable hinge changes, or Android multi-window resize &mdash; <code>Dimensions.get('window')</code> is a one-time synchronous read with no subscription, so a layout computed from it goes stale after rotation unless you manually wire an <code>Dimensions.addEventListener</code> listener (legacy pattern, easy to leak). <code>Dimensions.get</code> is still correct for a value read once outside the render tree &mdash; e.g. inside a module-level constant, a non-component utility function, or a one-shot calculation before mount where a hook can't be called.</p><p><b>Red flag:</b> calling Dimensions.get('window') inside a component body "because it's simpler" &mdash; that's exactly the case that goes stale on rotation.</p><p><b>Inside a component I default to useWindowDimensions(); Dimensions.get is only for a one-shot read outside the render tree.</b></p>`,
  },
  {
    id: "a18-styling-6",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `On a foldable Android device, what's the gap between \`useWindowDimensions()\` and the actual usable layout area, and how do you account for it?`,
    answerHtml: `<p>The gap matters because a naive full-bleed layout renders content directly into a physically occluded seam. <code>useWindowDimensions()</code> reports the window's pixel dimensions but says nothing about the <b>hinge/fold seam</b> &mdash; a foldable in tabletop or book posture has a physical occlusion zone that a naive full-bleed layout will render content into. The underlying data (fold posture, hinge bounds) comes from Android's Jetpack WindowManager \`FoldingFeature\` API, which isn't exposed by React Native core; you need a custom native module (or a community package that wraps \`WindowManager\`) bridging that API to JS, then offset content around the seam &mdash; \`useWindowDimensions\` alone treats a folded device as a single rectangle.</p><p><b>Red flag:</b> assuming useWindowDimensions() is foldable-aware because it reports live dimensions &mdash; hinge posture is a separate native API it doesn't expose.</p><p><b>Hinge-awareness has to come from a native bridge to WindowManager, not from useWindowDimensions alone.</b></p>`,
  },
  {
    id: "a18-styling-7",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `When should \`Platform.select()\` be preferred over \`Platform.OS === 'ios' ? a : b\`, beyond style preference?`,
    answerHtml: `<p><code>Platform.select()</code>'s role is readability at scale &mdash; an object keyed by \`ios\`/\`android\`/\`web\`/\`default\`/\`macos\`/\`windows\` that lets you co-locate all platform branches in one readable expression rather than nested ternaries. The real advantage shows up with <b>more than two platforms</b> (e.g. you also support web or out-of-tree platforms like macOS/Windows via React Native for Windows+macOS) &mdash; ternary chains become unreadable past two branches, while \`Platform.select\` scales linearly and falls back to \`default\` cleanly.</p><p><b>Red flag:</b> reaching for Platform.select for file-extension-style bundle elimination &mdash; it does no such thing. That's the separate \`.ios.tsx\`/\`.android.tsx\` file convention, which Metro resolves at bundle time and is the better choice when the divergence is large (a whole component, not a style value).</p><p><b>I use Platform.select once I'm branching on more than two platforms or the ternary nesting hurts readability &mdash; otherwise a plain ternary is fine.</b></p>`,
  },
  {
    id: "a18-styling-8",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What's the actual mechanism Metro uses to resolve \`Component.ios.tsx\` vs \`Component.android.tsx\`, and why is this more efficient than a runtime \`Platform.OS\` branch for large divergent components?`,
    answerHtml: `<p>The bundle-size win comes from exclusion at resolution time, not from runtime dead-code elimination &mdash; that's the framing worth leading with. Metro's resolver appends platform-specific extensions (\`.ios.js\`, \`.android.js\`, \`.native.js\`, \`.web.js\`) to the module path during bundling and picks the matching file <b>before</b> the JS bundle is built &mdash; the non-matching platform's file is never included in that platform's bundle at all. A runtime \`Platform.OS\` branch, by contrast, ships <i>both</i> implementations in the same bundle and switches between them at runtime, increasing bundle size and JS parse time for code that will never execute on that device. For a large divergent component (different libraries, different native dependencies per platform), the file-extension convention is strictly better for bundle size; for a one-line style or prop tweak, \`Platform.select\` inline is less ceremony.</p><p><b>Red flag:</b> assuming Hermes or Metro tree-shakes the unreached runtime branch out of the bundle &mdash; it doesn't; only the file-extension convention excludes code before bundling.</p><p><b>For a large divergent component I use the file-extension convention for bundle size; for a one-line tweak, an inline Platform.select is less ceremony.</b></p>`,
  },
  {
    id: "a18-styling-9",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Your app needs an iOS-only blur/vibrancy effect with no real Android equivalent. What's the tradeoff between conditionally rendering a fallback component vs. using \`expo-blur\`'s built-in Android emulation?`,
    answerHtml: `<p><code>expo-blur</code>'s \`BlurView\` renders true native vibrancy via \`UIVisualEffectView\` on iOS; on Android it can render genuine real-time blur via \`RenderEffect\` on API 31+ devices, but falls back to a semi-transparent tinted overlay approximation on older Android versions (or when the real blur path isn't available).</p><p>As a pitch: yes, accepting the built-in fallback risks a visually "cheap-looking" Android UI on older devices &mdash; that's the real cost, and I'd say so plainly. But the alternative, explicitly branching to a platform-appropriate Android treatment (Material elevation/scrim), costs more code paths to maintain and more surface area for platform-specific bugs. I'd default to the built-in fallback since it keeps one component tree and prop API across platforms (less code, consistent behavior contract), and only invest in a bespoke Android treatment once the older-Android cohort and the visual quality bar justify the extra maintenance surface.</p><p><b>I ship the built-in fallback first for consistency and lower maintenance cost, and only fork to a Material-native treatment once user data shows it's worth the extra code path.</b></p>`,
  },
  {
    id: "a18-styling-10",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why can excessive \`className\` strings with many NativeWind utility classes on a deeply nested tree cause measurable startup cost, and how do you diagnose it?`,
    answerHtml: `<p>The cost is real even though resolution is compiled: NativeWind still has to walk the prop and merge resolved style objects (specificity/cascade resolution for conflicting utilities, responsive/dark variants) for every styled node at render time &mdash; a tree with hundreds of densely-classed nodes adds measurable reconciliation and style-merge cost on first render, which shows up as added Time-to-Interactive.</p><p>Diagnose it as a framework: 1. Isolate &mdash; is the added time inside your component logic or inside the styling library's own code? 2. Prove it &mdash; use the React DevTools Profiler (commit duration per component) or toggle Hermes's sampling profiler around first mount to see where the time actually goes. 3. Fix the specific bottleneck &mdash; consolidate repeated utility combinations into a single custom class via \`tailwind.config\`'s \`extend\` or a shared component, rather than repeating 15 utility classes per list row.</p><p><b>I profile before I optimize className usage &mdash; if the flame graph time is in the styling library, not my component, the fix is consolidating classes, not touching my component logic.</b></p>`,
  },
  {
    id: "a18-styling-11",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What's the difference between NativeWind's \`dark\` mode strategy 'media' (OS-driven) and 'class' (manually toggled), and which should a senior engineer pick for an app with an in-app theme switcher (not just system theme)?`,
    answerHtml: `<p>'media' strategy's role is following the OS &mdash; it ties dark styles purely to the OS \`Appearance\` setting via \`useColorScheme()\` under the hood, and there is no way to force light mode while the OS is in dark mode. 'class' strategy's role is giving the app control &mdash; it applies dark styles based on a class/context value you own explicitly (e.g. a root-level provider toggling a 'dark' flag), independent of the OS setting. An app offering an in-app Light/Dark/System picker needs the 'class' strategy so 'Light' can override an OS-dark device, with 'System' falling through to read \`useColorScheme()\` itself.</p><p><b>The moment a product wants an in-app theme picker instead of pure OS-following, 'class' is the only strategy that can express "override the OS."</b></p>`,
  },
  {
    id: "a18-styling-12",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why does StyleSheet flattening (\`StyleSheet.compose\` / passing an array of styles) matter for render performance, and what's the failure mode of passing a freshly-created array of styles on every render?`,
    answerHtml: `<p>The failure mode matters because it silently defeats memoization &mdash; that's the practical stake. RN/Fabric flattens an array of style objects into one resolved style object before sending it to the native layer; if the array itself is a new array literal each render (e.g. \`style={[styles.base, isActive && styles.active]}\` is fine since \`styles.base\`/\`styles.active\` are stable, but \`style={[{margin: x}, styles.base]}\` creates a new object inside the array every render), the flattening still has to re-run and any \`React.memo\` comparison on the \`style\` prop sees a new reference and re-renders regardless of whether the resolved values changed. The fix is keeping every member of the array a stable reference (from \`StyleSheet.create\` or a memoized value) so only the array wrapper differs, which Fabric handles cheaply, while avoiding inline object literals as array members.</p><p><b>Red flag:</b> assuming any array of styles is "cheap" because Fabric flattens it &mdash; flattening doesn't fix an unstable reference inside the array.</p><p><b>I keep every member of a style array a stable reference and avoid inline object literals as array members, so only the array wrapper differs.</b></p>`,
  },
  {
    id: "a18-styling-13",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `When using \`useWindowDimensions()\` to drive a breakpoint-based responsive layout (e.g. switching from a 1-column to 2-column grid), what's the re-render cost on every pixel of resize (e.g. during a drag-resize on iPad Split View or desktop web), and how do you mitigate it?`,
    answerHtml: `<p>The cost compounds during a continuous gesture, not a discrete event. <code>useWindowDimensions()</code> fires a re-render on every dimension-change event, which during a continuous drag-resize (iPad Split View divider, foldable unfolding, browser window resize on web) can fire dozens of times per second &mdash; if your component does expensive layout math or re-renders a large subtree on every width pixel, this causes visible jank during the resize gesture. Mitigate by deriving only the breakpoint <i>bucket</i> (e.g. \`width >= 768 ? 'tablet' : 'phone'\`) and memoizing on that bucket value rather than raw width, or by debouncing/throttling the dimension value with a small \`useEffect\` + timeout before it drives a layout recalculation.</p><p><b>I never let raw resize pixels drive layout directly &mdash; I bucket into breakpoints or debounce first, so a drag gesture triggers one recalculation, not sixty.</b></p>`,
  },
  {
    id: "a18-styling-14",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What's the difference between \`Platform.OS\` and \`Platform.Version\`, and when would a senior engineer need the latter for UI divergence?`,
    answerHtml: `<p><code>Platform.OS</code>'s role is telling you the OS <i>family</i> &mdash; \`'ios' | 'android' | 'web' | ...\` &mdash; it can't distinguish OS versions. \`Platform.Version\`'s role is telling you the OS <i>release</i> within that family (iOS: a numeric/string OS version like \`'17.4'\`; Android: the API level integer), needed when a UI behavior genuinely differs by <i>OS version</i>, not just OS family &mdash; e.g. gating a Material You dynamic-color treatment behind Android API 31+ (\`Platform.OS === 'android' \\&\\& Platform.Version >= 31\`), or working around an iOS-version-specific \`SafeAreaView\` quirk that was fixed in a later iOS release. (Android also exposes a separate \`Platform.constants\` object with \`Brand\`, \`Model\`, \`Release\`, \`uiMode\`, etc. for device-level branching, but the version check itself is \`Platform.Version\`.)</p><p><b>Platform.OS answers "which OS," Platform.Version answers "which release of that OS" &mdash; I reach for Version only when the bug or feature gate is version-specific, not family-specific.</b></p>`,
  },
  {
    id: "a18-styling-15",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why is \`SafeAreaView\` from \`react-native\` itself considered legacy/insufficient for production apps, and what replaces it?`,
    answerHtml: `<p>RN core's \`SafeAreaView\` is a production risk because it's incomplete on both platform coverage and reactivity &mdash; that's the framing worth leading with. It only works on iOS (it's a no-op on Android) and only accounts for the device's safe-area insets at mount time, not dynamically (e.g. it doesn't react to a modal pushing content, keyboard insets, or orientation change cleanly in all cases). The standard replacement is \`react-native-safe-area-context\`'s \`useSafeAreaInsets()\`/\`SafeAreaProvider\`, which works cross-platform (Android included), updates reactively, and lets you apply insets surgically (e.g. only \`paddingBottom\` for a tab bar) instead of wrapping an entire screen and double-padding when combined with a navigator that already applies insets.</p><p><b>Red flag:</b> assuming SafeAreaView "just works" cross-platform because it ships in react-native core &mdash; it's iOS-only and static.</p><p><b>I don't use core's SafeAreaView in production &mdash; react-native-safe-area-context is the only one that's actually cross-platform and reactive.</b></p>`,
  },
  {
    id: "a18-styling-16",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `In a dark-mode-aware design system, why is hardcoding semantic color tokens (e.g. \`text-gray-900 dark:text-gray-100\`) on every component a maintenance risk compared to defining theme-aware design tokens once?`,
    answerHtml: `<p>Repeating \`dark:\` variants inline at every call site is a maintenance risk because a single design change becomes a repo-wide find-and-replace with real odds of missing a screen &mdash; inconsistency, not extra typing, is the actual cost.</p><p>As a pitch: yes, defining tokens once up front costs a bit more setup than just writing inline \`dark:\` variants. But the alternative's cost is concrete and compounding &mdash; every future rebrand or dark-surface adjustment means auditing every call site, and any screen you miss ships an inconsistent theme silently. Defining semantic tokens once (e.g. \`bg-surface\`, \`text-primary\` mapped to different hex values per theme in \`tailwind.config\`'s \`theme.extend.colors\`, or via NativeWind's \`vars()\`/CSS custom properties) centralizes the mapping so changing a brand color is a one-line config edit, and it also documents <i>intent</i> (surface vs. primary text) rather than a raw gray scale value, which is more robust when the palette itself changes.</p><p><b>I invest in semantic design tokens up front because the alternative is a repo-wide find-and-replace every time the palette changes.</b></p>`,
  },
  {
    id: "a18-styling-17",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why might switching themes (light/dark) cause a visible flash of incorrectly-themed content on initial app load, and how do you prevent it?`,
    answerHtml: `<p>The flash is a race between the first paint and an async preference read &mdash; that's the framing worth leading with. If the theme/color-scheme preference is read asynchronously (e.g. from \`AsyncStorage\` for a user-overridden preference) while the initial render already committed using a default value, the UI paints once with the wrong theme, then re-paints when the async read resolves &mdash; a visible 'flash of unstyled/wrong theme'. The fix is to either delay the first paint behind a splash screen (\`expo-splash-screen\`'s \`preventAutoHideAsync\` + hide only after the preference resolves) or persist the preference synchronously where possible (e.g. a fast synchronous storage layer, or reading \`Appearance.getColorScheme()\` synchronously as the default before the async override loads) so the first paint already matches the eventual theme.</p><p><b>I never let the first paint race an async preference read &mdash; either hold the splash screen or seed the initial theme synchronously.</b></p>`,
  },
  {
    id: "a18-styling-18",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What's the risk of using NativeWind's arbitrary value syntax extensively (e.g. \`top-[37px]\`, \`w-[83%]\`) versus defining values in the Tailwind config scale?`,
    answerHtml: `<p>The risk isn't that arbitrary values don't work &mdash; NativeWind's compiler does resolve them statically since they're literal strings &mdash; it's that they erode the constraint that makes a utility-class system valuable in the first place: consistent spacing across the app. Overuse leads to a UI with dozens of one-off magic numbers that don't compose, are hard to audit, and make responsive/dark-mode variants harder to reason about since each arbitrary value needs its own variant override rather than inheriting from a scale.</p><p><b>Red flag:</b> treating "it compiles fine" as proof an arbitrary value is a good idea &mdash; compiling isn't the same as maintainable.</p><p><b>I reserve arbitrary values for true one-offs matching a designer's exact spec, and promote anything reused twice or more into the theme.extend scale.</b></p>`,
  },
  {
    id: "a18-styling-19",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How does NativeWind's planned move (v5, built on \`react-native-css\`) toward a more standards-aligned CSS engine change the platform-divergence story compared to NativeWind v2/v3/v4's StyleSheet-compilation approach?`,
    answerHtml: `<p>This move matters because it changes what class of bugs exist, not just implementation detail &mdash; that's the framing worth leading with. NativeWind v2 through the current v4 translate Tailwind classes into RN \`StyleSheet\` objects, which means any CSS feature without a direct RN style equivalent (true cascading, complex selectors, container queries) simply isn't supported. The \`react-native-css\`-based engine NativeWind is moving toward implements more of the actual CSS cascade and box model semantics directly, narrowing the gap between what works on web (\`className\` via react-native-web or Expo web) and native &mdash; reducing the cases where a className behaves correctly on web but silently no-ops or differs on native. It won't eliminate platform divergence (native still lacks full CSS layout primitives like grid in all cases) but it should shrink the surprise surface for engineers used to web CSS semantics.</p><p><b>Red flag:</b> citing v5's CSS-cascade engine as current behavior without checking which version a given codebase runs &mdash; most production apps today are still on the StyleSheet-compilation model (v4).</p><p><b>I'd confirm which NativeWind major version a codebase is on before reasoning about its CSS-compatibility surface &mdash; v4 and v5 behave differently enough that the answer changes.</b></p>`,
  },
  {
    id: "a18-styling-20",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why does an \`Animated.View\` or Reanimated-driven style change generally bypass \`StyleSheet\`/NativeWind diffing entirely, and what does that mean for combining NativeWind classes with Reanimated \`useAnimatedStyle\`?`,
    answerHtml: `<p>The bypass matters because the animated channel and the compiled-utility channel never negotiate with each other &mdash; you can't put an animated value inside a \`className\` string, full stop. Reanimated's \`useAnimatedStyle\` produces a style object that's written directly to the native shadow node on the UI thread via the worklet runtime, independent of React's render/commit cycle &mdash; it never goes through NativeWind's className resolution or \`StyleSheet.create\` registry at all.</p><p><b>Red flag:</b> trying to interpolate a Reanimated shared value into a className string &mdash; it doesn't work because the animated channel bypasses className resolution entirely.</p><p>Practically, you apply static NativeWind classes for the non-animated style and pass the Reanimated \`useAnimatedStyle\` result through the \`style\` prop alongside it (RN merges the \`className\`-resolved style with the explicit \`style\` prop). <b>I keep the animated channel and the compiled-className channel fully separate &mdash; static classes via className, animated values via style.</b></p>`,
  },
  {
    id: "a18-styling-21",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `A senior candidate claims 'NativeWind has zero runtime cost since it's all compiled to StyleSheet.' What's the more precise, accurate claim, and why does the distinction matter in an interview?`,
    answerHtml: `<p><b>Red flag:</b> repeating "NativeWind has zero runtime cost since it's all compiled to StyleSheet" verbatim &mdash; that's the marketing claim, not the precise one, and an interviewer probing this is testing whether you understand compiled-vs-dynamic resolution paths, not just repeating it.</p><p>The precise claim: NativeWind <i>statically resolvable</i> classNames are compiled ahead-of-time into stable style objects with no runtime class-name parsing &mdash; but dynamic class composition, responsive variants tied to \`useWindowDimensions\`, \`dark:\` variants tied to the color scheme, and any arbitrary value built from a runtime variable still require runtime evaluation (CSS interop / context subscriptions) to pick the right resolved style.</p><p><b>I'd say "statically resolvable classNames are compile-time, but dark: variants, responsive breakpoints, and dynamic classes still resolve at runtime" &mdash; that's the version that survives a follow-up question.</b></p>`,
  },
  {
    id: "a18-styling-22",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why is testing dark mode purely by toggling the OS appearance setting in the simulator insufficient for catching real bugs, and what additional case should you test?`,
    answerHtml: `<p>Toggling OS appearance only exercises two paths: cold start in the other theme, and a live OS toggle while the app is foregrounded. It misses the case where the app is <b>backgrounded</b> in one theme and the OS theme changes while backgrounded (common with scheduled dark mode / sunset-triggered switching) &mdash; some screens cache a theme-derived value (e.g. a status bar style or native module state) at mount and don't re-evaluate on \`AppState\` foreground, producing a stale-themed screen until a manual re-render is triggered.</p><p>Test it as a sequence: 1. Background the app. 2. Switch system appearance while backgrounded. 3. Foreground the app and verify <i>every</i> screen &mdash; not just the currently-focused one in a stack/tab navigator &mdash; picks up the new theme.</p><p><b>Red flag:</b> only testing the live-toggle-while-foregrounded case &mdash; the backgrounded-theme-change path is the one that actually ships stale-themed screens.</p><p><b>I always add the background-switch-foreground sequence to dark mode testing, because that's the path that catches screens caching a stale theme value.</b></p>`,
  },
  {
    id: "a18-styling-23",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why can a \`StatusBar\` color/style mismatch on Android specifically reveal a platform-divergence bug that wouldn't show on iOS?`,
    answerHtml: `<p>This is a canonical 'looks fine on the platform you tested, breaks on the other' bug, worth naming specifically in an interview. iOS status bar style is largely managed by the OS chrome and \`expo-status-bar\`'s \`style\` prop (\`light\`/\`dark\`/\`auto\`) maps cleanly. Android additionally exposes a translucent/colored status bar background that must be explicitly synced with \`backgroundColor\` and edge-to-edge settings (especially post-Android 15/API 35 edge-to-edge enforcement for apps targeting that SDK) &mdash; a dark-mode implementation that only flips text/icon style (\`barStyle\`) but forgets to flip the Android status bar background color produces a readable-on-iOS, illegible-on-Android bug (e.g. dark icons on a now-dark background).</p><p><b>Red flag:</b> testing dark mode only on iOS and assuming Android "just follows" &mdash; Android's status bar background is a separate value you own explicitly.</p><p><b>Any dark-mode status bar fix I ship, I verify on Android specifically &mdash; barStyle and background color are two separate knobs there.</b></p>`,
  },
  {
    id: "a18-styling-24",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `When profiling a NativeWind-heavy screen with the Hermes sampling profiler, what symptom in the flame graph specifically points to className resolution overhead vs. general re-render overhead?`,
    answerHtml: `<p>The distinction matters because it tells you what to optimize: your component logic, or the styling library's resolution path. Diagnose it as a framework: 1. Isolate &mdash; where does the flame graph time actually sit? 2. Read the symptom &mdash; general re-render overhead shows up as time inside your component functions and React reconciliation (\`render\`, \`commitWork\`); className resolution overhead shows up as time inside NativeWind's interop/style-resolution functions that sit between the component render and the native \`setNativeProps\`/style commit. 3. Fix accordingly &mdash; if a large share of frame time is inside the styling library's own resolution code, consolidate utility classes or hoist them to \`StyleSheet.create\`-equivalents; don't spend time optimizing component render logic that isn't the bottleneck.</p><p><b>Before I touch component render logic on a NativeWind-heavy screen, I check whether the flame-graph time is actually inside the styling library's resolution code &mdash; that's a different fix entirely.</b></p>`,
  },
];

export const ADVANCED18_QUIZ: QuizQuestion[] = [
  {
    id: "a18q-styling-1",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `A component uses \`className="p-4 bg-white dark:bg-black"\` and is wrapped in \`React.memo\`. The screen re-renders frequently due to unrelated state changes elsewhere in the tree. What determines whether this memoized component actually re-renders?`,
    options: [`It always re-renders because NativeWind classes can't be memoized`, `Whether the resolved style object reference changes, which for a static className is stable across renders unless the color scheme or other variant-driving context changes`, `It never re-renders once mounted, regardless of props`, `Only the OS color scheme controls re-renders, not React.memo`],
    answer: 1,
    explanationHtml: `<p>For a fully static className, NativeWind resolves to a stable compiled style reference, so a properly memoized component (stable props otherwise) won't re-render on unrelated parent re-renders. It will re-render when a variant it depends on actually changes &mdash; e.g. the color scheme flips for the \`dark:\` variant &mdash; because that changes the resolved style value the component receives.</p><p>The tempting distractor is "only the OS color scheme controls re-renders, not React.memo" &mdash; that's a half-truth: the color-scheme change is what makes the resolved style value change, but it's still React.memo's shallow prop comparison that decides whether the re-render actually happens. A new style reference is what triggers it, not the OS event directly.</p>`,
  },
  {
    id: "a18q-styling-2",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why does Metro's \`.ios.tsx\` / \`.android.tsx\` file-extension convention produce a smaller production bundle than an equivalent \`Platform.OS === 'ios' ? <A/> : <B/>\` runtime branch inside a single file?`,
    options: [`Metro compresses platform-specific files better than runtime branches`, `The non-matching platform file is excluded from resolution before bundling, so its code never enters that platform's bundle; a runtime branch ships both implementations in one bundle`, `Runtime branches are always tree-shaken by Hermes at build time`, `There is no actual bundle size difference between the two approaches`],
    answer: 1,
    explanationHtml: `<p>Metro's resolver picks the platform-specific file at module-resolution time, before the bundle is assembled, so the unused platform's file is never included. A runtime \`Platform.OS\` branch keeps both code paths in the same module, so both ship in every platform's bundle and only the unused branch is dead at runtime (not eliminated from the bundle).</p><p>Neither compression nor Hermes tree-shaking is the mechanism here &mdash; Metro doesn't compress platform files differently, and Hermes has no build step that strips an unreached runtime branch out of the bundle. The size difference comes entirely from module resolution happening before bundling, not after.</p>`,
  },
  {
    id: "a18q-styling-3",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `An app reads the user's saved theme preference from AsyncStorage and applies it after the first render, causing a visible flash of the wrong theme on cold start. What is the most direct fix?`,
    options: [`Switch from NativeWind to inline styles`, `Delay first paint (e.g. keep the splash screen visible) until the stored preference resolves, or seed the initial render with a synchronously available default that matches the eventual value`, `Disable dark mode support entirely`, `Use Platform.select instead of useColorScheme`],
    answer: 1,
    explanationHtml: `<p>The flash happens because the UI commits once with a default/system theme before the async AsyncStorage read resolves, then repaints. Holding the splash screen until the preference loads (or synchronously seeding the initial theme from a fast-available source) avoids ever painting the wrong theme in the first place.</p>`,
  },
  {
    id: "a18q-styling-4",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why is \`useWindowDimensions()\` preferred over a one-time \`Dimensions.get('window')\` call inside a component body for responsive layout?`,
    options: [`useWindowDimensions is faster to execute on every call`, `Dimensions.get is deprecated and throws a warning`, `useWindowDimensions subscribes to dimension-change events and re-renders the component on rotation/resize, while Dimensions.get is a one-time synchronous snapshot that goes stale`, `Dimensions.get only works on Android`],
    answer: 2,
    explanationHtml: `<p>useWindowDimensions is a hook that subscribes to the underlying dimension-change event and triggers a re-render whenever the window size changes (rotation, split view resize, fold/unfold). Dimensions.get('window') returns a snapshot at call time with no subscription, so a layout computed from it inside a component body won't update when the window later changes size unless you wire your own listener.</p>`,
  },
  {
    id: "a18q-styling-5",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `A row's background color flips instantly on press via setState + inline style inside a FlashList, causing dropped frames on a mid-range Android device during fast scrolling with active presses. What's the most targeted fix?`,
    options: [`Switch the whole list from FlashList back to FlatList`, `Drive the press feedback with a Reanimated useAnimatedStyle running on the UI thread instead of a JS-thread setState re-render`, `Remove StyleSheet.create and use only inline styles`, `Disable dark mode for list rows`],
    answer: 1,
    explanationHtml: `<p>setState-driven style changes require a JS-thread re-render, prop diff, and a commit to the native side for every press, competing with scroll-driven work on the same thread. Reanimated's useAnimatedStyle writes directly to the native shadow tree from a worklet on the UI thread, skipping the JS thread round-trip and avoiding contention with JS-thread scroll/list work.</p>`,
  },
  {
    id: "a18q-styling-6",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Which scenario most directly justifies choosing NativeWind's 'class' dark-mode strategy over the 'media' strategy?`,
    options: [`The app only needs to follow the OS appearance setting and nothing else`, `The app offers an in-app theme picker (Light / Dark / System) that must be able to override the OS-level appearance setting`, `The app doesn't support dark mode at all`, `The app is web-only and never runs natively`],
    answer: 1,
    explanationHtml: `<p>'media' strategy ties dark styling purely to the OS Appearance setting with no override mechanism. 'class' strategy lets the app control which theme classes apply via an explicit value (e.g. a context/provider flag), which is required to let a user pick 'Light' while their OS is set to dark mode, or vice versa, with 'System' falling back to read the OS value itself.</p>`,
  },
];
