// Batch 21 — Metro bundler configuration (resolver, transformer, monorepo symlinks), EAS Build environment variables & secrets, and monorepo tooling (Expo + Turborepo/Nx) gotchas.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED21_FLASHCARDS: Flashcard[] = [
  {
    id: "a21-monorepo-tooling-1",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `In a pnpm/yarn monorepo, why does Metro need watchFolders pointed beyond the app directory, and what's the cost of setting it too broadly?`,
    answerHtml: `<p>Metro's module resolver walks up looking for <code>node_modules</code>, but its file watcher (Watchman) only watches <code>watchFolders</code> plus <code>projectRoot</code> by default. Shared workspace packages live outside <code>apps/your-app</code>, so without adding the repo root (or the specific package dirs) to <code>watchFolders</code>, edits to a shared package won't trigger Fast Refresh and symlinked packages can fail to resolve.</p><p>Cost: pointing it at the whole monorepo root makes Metro crawl every workspace's <code>node_modules</code>, including unrelated apps, which slows Watchman startup and can hit OS file-descriptor limits on large repos. Scope it to just the packages actually consumed.</p>`,
  },
  {
    id: "a21-monorepo-tooling-2",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `What does enabling symlink resolution in Metro (default in @expo/metro-config) actually change about how a module's relative imports are resolved?`,
    answerHtml: `<p>Older Metro resolved a symlink to its real on-disk path and then resolved relative imports from that <i>real</i> path, which breaks pnpm's content-addressable store (packages live deeply nested under <code>node_modules/.pnpm</code> and are symlinked into place). With symlink-aware resolution, Metro resolves relative imports from the <b>symlink's apparent location</b> instead, matching Node's <code>--preserve-symlinks</code> semantics.</p><p>This is why pnpm monorepos became viable with Expo without manual workarounds — symlink support shipped as beta in React Native 0.72 and became the default in 0.73, and <code>@expo/metro-config</code> turns it on by default in current SDKs.</p>`,
  },
  {
    id: "a21-monorepo-tooling-3",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `What is resolver.disableHierarchicalLookup for, and when would you actually want it on versus off in a monorepo?`,
    answerHtml: `<p>By default Metro does Node-style hierarchical lookup: for an import in a deeply nested package, it walks up through every ancestor <code>node_modules</code> directory. In a large monorepo with many workspace packages, that walk is expensive and can also let a package accidentally resolve a phantom dependency it never declared (something hoisted by a sibling package).</p><p>Setting <code>disableHierarchicalLookup: true</code> disables that default ascending search — it does <i>not</i> affect <code>extraNodeModules</code> or <code>nodeModulesPaths</code>, which still apply. So resolution becomes faster and stricter, but you must hand-list every shared package path via those two options. Use it once you've audited dependencies and want to catch phantom-dependency bugs; leave it off while still discovering what's actually needed.</p>`,
  },
  {
    id: "a21-monorepo-tooling-4",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `Two workspace packages both depend on react-native but at slightly different resolved versions. How does this manifest as a runtime bug, and what's the Metro-level fix?`,
    answerHtml: `<p>It manifests as "Invalid hook call" or duplicate-React-instance style errors, but for RN specifically it's commonly two copies of <code>react-native</code> (or <code>react</code>) ending up in the bundle — singletons like Context providers, the Fabric/UIManager registry, or Reanimated's worklet runtime silently split into two instances that don't see each other's state.</p><p>Fix: force a single resolved copy via <code>resolver.extraNodeModules</code> (alias <code>react-native</code>/<code>react</code> to the root copy) or, more robustly, hoist them as peerDependencies in shared packages so the package manager never installs a second copy. pnpm's strict isolation makes this bug more common than with hoisted npm/yarn installs.</p>`,
  },
  {
    id: "a21-monorepo-tooling-5",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `What's the difference between resolver.extraNodeModules and resolver.nodeModulesPaths in Metro config, and when do you reach for each in a monorepo?`,
    answerHtml: `<p><code>extraNodeModules</code> is a map from module name to a forced path, consulted after the standard <code>node_modules</code> lookup and after <code>nodeModulesPaths</code> — use it to alias a specific package (e.g. force every <code>react-native</code> import to resolve to one root copy).</p><p><code>nodeModulesPaths</code> is a list of additional directories Metro checks for a module <i>after</i> it has searched through all the normal <code>node_modules</code> directories in the hierarchy — use it to add a workspace's hoisted root <code>node_modules</code> (or any location outside the direct source-file path) as a further fallback. They compose in order: hierarchical node_modules lookup, then nodeModulesPaths, then extraNodeModules.</p>`,
  },
  {
    id: "a21-monorepo-tooling-6",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `Why can adding a new workspace package to a monorepo silently break Metro's bundle with "Unable to resolve module" even though the package builds fine with tsc?`,
    answerHtml: `<p>TypeScript resolves via <code>tsconfig.json</code> path aliases or project references, which are compile-time only and don't affect runtime module resolution. Metro resolves purely from the filesystem/package.json exports at bundle time — it has no concept of TS path mapping unless you also configure <code>resolver.extraNodeModules</code> or symlinks to match.</p><p>The fix is to keep TS paths and Metro resolution in sync: either symlink the package into <code>node_modules</code> (what pnpm/yarn workspaces do automatically) so both tools agree, or mirror the alias in both <code>tsconfig.json</code> paths and Metro's resolver config.</p>`,
  },
  {
    id: "a21-monorepo-tooling-7",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `A shared UI package in the monorepo exports both .ts source and compiled dist output. Which should Metro consume, and why does the wrong choice break Fast Refresh?`,
    answerHtml: `<p>Metro should consume the package's <b>source</b> (via <code>main</code>/<code>exports</code> pointing at <code>src/index.ts</code>, or a workspace-aware <code>main</code> field), not a pre-built <code>dist</code>. If Metro resolves to compiled <code>dist</code> output, editing the shared package's source won't trigger Metro's watcher (it's not in the resolved file's dependency graph) and Fast Refresh won't fire — you'd need a separate build-watch step to regenerate dist first.</p><p>This is why Expo monorepo templates set shared packages' <code>main</code> to source and rely on Metro's own transformer (Babel) to compile TS/JSX on the fly, rather than pre-building with tsc.</p>`,
  },
  {
    id: "a21-monorepo-tooling-8",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `What does Metro's transformer.getTransformOptions / Babel config caching get wrong by default in a monorepo with multiple babel.config.js files, and how do you debug it?`,
    answerHtml: `<p>Babel resolves config by walking up from the file being transformed looking for the nearest <code>babel.config.js</code>/<code>.babelrc</code>. If a shared package in the monorepo has its own (or no) Babel config that differs from the app's (e.g. missing <code>babel-preset-expo</code> or a different JSX transform), Metro transforms that file with the wrong preset — producing cryptic syntax errors or features like NativeWind class transforms silently not applying.</p><p>Debug with <code>BABEL_SHOW_CONFIG_FOR=&lt;path-to-file&gt;</code> when running Metro, which prints exactly which config file and which merged options Babel resolved for that file. The general fix is one root-level <code>babel.config.js</code> with <code>babelrcRoots</code> covering all workspace packages, so they all inherit the same preset.</p>`,
  },
  {
    id: "a21-monorepo-tooling-9",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `What's the practical difference between an EAS "secret" and an EAS "environment variable" with visibility set to Secret, and which one shows up in eas.json or the dashboard plaintext view?`,
    answerHtml: `<p>Legacy EAS <code>secret:create</code> (account/project-level secrets) are write-only — once created you can never view the value again, only delete and recreate. The current Environment Variables system (<code>eas env:create</code>) unifies config and secrets into one store scoped per environment (development/preview/production) with a <code>visibility</code> field: <code>plaintext</code>, <code>sensitive</code> (masked in logs/CLI, may be revealed locally), or <code>secret</code> (write-only, never leaves EAS servers, not even readable during local build-config resolution).</p><p>Neither a plaintext nor a secret-visibility variable is ever written into <code>eas.json</code> itself — eas.json only references an environment via the profile's <code>"environment"</code> field (or build-profile-level inline <code>env</code> for plaintext values); actual EAS-managed values live server-side and are injected at build time.</p>`,
  },
  {
    id: "a21-monorepo-tooling-10",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `Why can a public env var (EXPO_PUBLIC_API_URL) work fine in eas update but break in a fresh production build, and what's the root cause?`,
    answerHtml: `<p><code>EXPO_PUBLIC_*</code> variables are inlined into the JS bundle at <b>build time</b> (a Babel transform in <code>babel-preset-expo</code> swaps <code>process.env.EXPO_PUBLIC_API_URL</code> for a literal string when Metro bundles). An OTA update via <code>eas update</code> only ships a new JS bundle — if you change the env var's value in the EAS dashboard but only run <code>eas update</code> (not a new native build), the update still contains whatever value was baked in at the <i>last native build</i>, because the update's JS bundle was itself built with that build-time value, not re-read at runtime.</p><p>Root cause: public env vars are not dynamic config — they're compile-time constants. Changing them requires either a new build or, for true runtime config, fetching from a remote config endpoint instead.</p>`,
  },
  {
    id: "a21-monorepo-tooling-11",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `What's the security risk of using EXPO_PUBLIC_ prefixed variables for anything beyond non-secret config, and how does Expo enforce the boundary?`,
    answerHtml: `<p>Any variable prefixed <code>EXPO_PUBLIC_</code> is intentionally inlined into the client JS bundle and is therefore extractable by anyone who downloads the app (unzip the IPA/APK, grep the bundle, or just inspect network/JS at runtime). Using it for an API secret, a signing key, or anything access-controlled ships that secret to every device.</p><p>Expo enforces the boundary by convention, not by a hard technical block: only env vars with that exact prefix get inlined client-side; everything else is available only to build-time scripts (app.config.js, EAS Build hooks) and never reaches the bundle. The discipline required is naming — a typo'd prefix on a sensitive var is a real, easy-to-miss leak vector.</p>`,
  },
  {
    id: "a21-monorepo-tooling-12",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `In app.config.js (dynamic config), env vars referenced via process.env are read where, and why does this differ from EXPO_PUBLIC_ vars used directly in app code?`,
    answerHtml: `<p><code>app.config.js</code> executes as a <b>Node script</b> at config-evaluation time (<code>expo prebuild</code>, <code>expo start</code>, or EAS Build's config-resolution step) — it can read any <code>process.env</code> var, public-prefixed or not, because it runs on the build machine, not in the shipped bundle. Values it reads can be baked into <code>expo.extra</code> (e.g. <code>extra.apiUrl</code>) and retrieved at runtime via <code>expo-constants</code>.</p><p>This differs from <code>EXPO_PUBLIC_*</code> used directly in app source: those go through babel-preset-expo's inline-replacement transform and only work in code Metro bundles (not in app.config.js, which Metro never touches). Conflating the two is a common bug — a non-public var works in app.config.js but silently becomes <code>undefined</code> if referenced directly in a component.</p>`,
  },
  {
    id: "a21-monorepo-tooling-13",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `Why does an EAS Build sometimes use a stale environment variable value even after you update it in the dashboard, and what build-profile setting controls this?`,
    answerHtml: `<p>EAS env vars are resolved per build profile against an <b>environment</b> (development/preview/production), set via the profile's <code>"environment"</code> field in <code>eas.json</code> (e.g. <code>{"build":{"production":{"environment":"production"}}}</code>). If a build is triggered against a profile with no <code>environment</code> field set, EAS Build doesn't pull dashboard-managed variables for that profile at all, so it won't see the updated value.</p><p>Also relevant: testing via a local prebuild with a stale <code>.env</code> file checked out is a separate, more common culprit — always confirm whether the build actually pulled fresh vars (e.g. via <code>eas env:exec</code> or by checking the printed values in build logs) rather than assuming the dashboard change is live.</p>`,
  },
  {
    id: "a21-monorepo-tooling-14",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `What does eas env:exec do and why is it the recommended way to test EAS-managed env vars locally before trusting a cloud build?`,
    answerHtml: `<p><code>eas env:exec &lt;environment&gt; -- &lt;command&gt;</code> fetches the resolved environment variables for the given environment from EAS's servers and injects them into the local shell for the duration of the command, without writing them to disk. This lets you run e.g. <code>eas env:exec production -- npx expo start</code> and verify the app behaves correctly with production values before burning a cloud build cycle.</p><p>It's preferred over manually copying values into a local <code>.env</code> because it eliminates drift — a hand-maintained local <code>.env</code> can silently diverge from what's actually configured on EAS, and you'd debug a build failure that only reproduces with the real values.</p>`,
  },
  {
    id: "a21-monorepo-tooling-15",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `How does eas.json's build profile inheritance (extends) interact with environment variables, and what's a common footgun?`,
    answerHtml: `<p>Build profiles in <code>eas.json</code> can <code>"extends"</code> another profile, and EAS CLI's profile resolver explicitly merges the inline <code>env</code> object key-by-key (<code>{...base.env, ...update.env}</code>) — a child profile's own <code>env</code> block does <b>not</b> wipe out the parent's; it only overrides keys it redeclares, and unrelated parent keys still apply.</p><p>The actual footgun: a key set in the child with the <i>same name</i> as a parent key silently shadows it with no warning (easy to do by accident across a <code>base</code> → <code>preview</code> → <code>production</code> chain), and this inline-<code>env</code> merge is a completely separate mechanism from the EAS dashboard's Environment Variables store (selected via the profile's <code>"environment"</code> field) — mixing the two systems and assuming one cascades into the other is the real source of confusion.</p>`,
  },
  {
    id: "a21-monorepo-tooling-16",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `Why does Turborepo's local/remote cache produce a stale Metro bundle if a shared package's dist output changed but its package.json version didn't?`,
    answerHtml: `<p>Turborepo's cache key (the task "hash") is derived from the input files it's told to watch for a task — by default the package's own source tree plus its declared <code>dependsOn</code> graph. If a shared package is consumed by Metro through a built <code>dist</code> folder but Turbo's pipeline doesn't list that package's build task as a real <code>dependsOn</code> for the app's build/dev task (e.g. <code>"dependsOn": ["^build"]</code> missing), Turbo can replay a cached "success" for the app without re-running the dependency's build — so Metro bundles last cached dist output even though source changed.</p><p>Fix: declare the proper task graph (<code>^build</code> as a dependency of the consuming app's relevant tasks) so Turbo invalidates correctly, and make sure the shared package's <code>outputs</code> in <code>turbo.json</code> actually point at the dist directory Metro reads.</p>`,
  },
  {
    id: "a21-monorepo-tooling-17",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `What's the difference between Turborepo pruning and Metro's own tree-shaking, and why do monorepo CI/EAS setups sometimes need both?`,
    answerHtml: `<p><code>turbo prune</code> operates at the <b>workspace/package level</b> before any bundler runs — it generates a minimal subset of the monorepo (only the packages a given app actually depends on, with a pruned lockfile) so a Docker build or EAS Build worker doesn't need to install/clone unrelated apps. Metro's tree-shaking (newer Metro versions, or Hermes + dead-code elimination) operates at the <b>module/export level</b> inside a single package's bundle, removing unused exports/branches from the final JS bundle.</p><p>They're complementary and address different costs: pruning shrinks install time and build context size; bundler tree-shaking shrinks the shipped JS payload. A monorepo without pruning still produces a small final bundle but wastes CI minutes installing every workspace's dependencies.</p>`,
  },
  {
    id: "a21-monorepo-tooling-18",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `Why does an Nx-managed Expo app sometimes fail Metro resolution for a library with no obvious error, and what's the Nx-specific cause?`,
    answerHtml: `<p>Nx libraries are commonly referenced via TypeScript path mappings in the root <code>tsconfig.base.json</code> (e.g. <code>@myorg/ui</code> → <code>libs/ui/src/index.ts</code>) rather than as real workspace packages with their own <code>package.json</code> resolved through <code>node_modules</code> symlinks. Metro doesn't read <code>tsconfig.json</code> paths at all — it only does filesystem/node_modules resolution — so an import that typechecks perfectly under Nx's TS project references throws "Unable to resolve module" in Metro.</p><p>Fix is either to mirror the path mapping into Metro's <code>resolver.extraNodeModules</code>/<code>resolver.resolveRequest</code>, or use <code>@nx/expo</code>'s generated Metro config (via <code>withNxMetro</code>) which resolves workspace libs from <code>tsconfig</code> paths automatically, wired from the Nx project graph instead of hand-maintaining two parallel alias systems.</p>`,
  },
  {
    id: "a21-monorepo-tooling-19",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `A monorepo app works fine in expo start (Metro dev server) but the EAS Build fails to resolve a workspace package. What's the most likely root cause given the two environments resolve modules differently?`,
    answerHtml: `<p>Most likely: the local dev server runs against a fully installed monorepo (all workspaces symlinked via pnpm/yarn at the root), but the EAS Build worker does a clean install from a different working directory or lockfile state. If the workspace package isn't declared as a real dependency in the app's <code>package.json</code> (just relied on via hoisting/proximity locally), the worker's clean install won't symlink it in.</p><p>The fix is ensuring every workspace package actually consumed gets a real entry in the app's <code>package.json</code> dependencies (so pnpm/yarn workspace resolution creates the symlink deterministically) rather than relying on accidental hoisting that happens to work in a long-lived local node_modules.</p>`,
  },
  {
    id: "a21-monorepo-tooling-20",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `What's the difference between Metro's resolver.sourceExts and resolver.assetExts, and why does adding a new file type (e.g. .svg via react-native-svg-transformer) require touching both?`,
    answerHtml: `<p><code>sourceExts</code> lists extensions Metro treats as <i>code</i> to run through the transformer pipeline (Babel) and include in the JS dependency graph; <code>assetExts</code> lists extensions Metro treats as <i>opaque binary assets</i> to copy/hash and reference by URI, never transformed as code.</p><p>SVG-as-component (react-native-svg-transformer) needs <code>.svg</code> <b>removed from assetExts and added to sourceExts</b>, plus a custom <code>transformer.babelTransformerPath</code> that converts SVG markup into a React component — because by default Metro treats <code>.svg</code> as a binary asset, which is wrong if you want <code>import Logo from './logo.svg'</code> to yield a component rather than an image URI.</p>`,
  },
  {
    id: "a21-monorepo-tooling-21",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `Why might two apps in the same monorepo need different metro.config.js resolver.blockList settings, and what's the symptom of getting this wrong?`,
    answerHtml: `<p><code>resolver.blockList</code> (a regex or array of regexes) excludes matching paths from Metro's module resolution and watching. In a monorepo, if app A and app B live as siblings and both have their own <code>node_modules</code> plus a hoisted root <code>node_modules</code>, Metro can find <i>duplicate</i> copies of the same package (one in the app's local node_modules, one hoisted) and either throw "duplicate module name" errors or silently pick the wrong one depending on resolution order.</p><p>The fix is blocking the sibling app's directory (and its nested node_modules) from app A's Metro config so it never even scans into app B's tree — commonly composing a regex with Metro's own <code>metro-config</code> exclusionList helper so it merges with the defaults instead of overwriting them.</p>`,
  },
  {
    id: "a21-monorepo-tooling-22",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `Why does running pnpm install with default settings sometimes break Expo/Metro resolution that worked under yarn classic, even after enabling Metro's symlink support?`,
    answerHtml: `<p>pnpm's strict-by-default <code>node-linker</code> creates a non-flat <code>node_modules</code> where only directly-declared dependencies are visible at the top level (everything else is nested under <code>.pnpm</code> and symlinked) — this is correct-but-strict and surfaces phantom dependencies (packages your code imports but never declared) that yarn classic's flat hoisting silently tolerated. Even with Metro's symlink support on, a package that was never added to the consuming app's <code>package.json</code> simply won't be reachable.</p><p>This is exactly why this repo's own <code>.npmrc</code> sets <code>node-linker=hoisted</code> — it trades pnpm's strictness for yarn-classic-style flat resolution, sidestepping phantom-dependency breakage in exchange for losing pnpm's dependency-isolation guarantees.</p>`,
  },
];

export const ADVANCED21_QUIZ: QuizQuestion[] = [
  {
    id: "a21q-monorepo-tooling-1",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `A pnpm monorepo's Expo app fails to Fast Refresh when you edit a shared UI package, even though the import resolves fine and the app runs. What's the most likely root cause?`,
    options: [`The shared package's directory isn't included in Metro's watchFolders, so Watchman never reports its file changes`, `EXPO_PUBLIC_ env vars need to be re-inlined on every edit`, `Hermes bytecode caching prevents any hot reload in monorepos`, `Turborepo's remote cache is serving a stale build artifact`],
    answer: 0,
    explanationHtml: `<p>Resolution and watching are separate concerns in Metro. A package can resolve correctly (it's on disk, symlinked properly) while still being invisible to the file watcher if its directory isn't covered by <code>watchFolders</code> — the watcher only reports changes for paths it's told to watch, independent of whether resolution can find the file.</p>`,
  },
  {
    id: "a21q-monorepo-tooling-2",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `Why does forcing resolver.disableHierarchicalLookup = true in a Metro monorepo config sometimes break previously-working imports rather than just speeding things up?`,
    options: [`It disables Babel transforms for all workspace packages`, `It removes Metro's default ascending node_modules walk, so any package relying on hierarchical/hoisted resolution (instead of an explicit extraNodeModules/nodeModulesPaths entry) stops resolving`, `It forces all imports to use absolute paths only`, `It only affects production builds, never dev server resolution`],
    answer: 1,
    explanationHtml: `<p>disableHierarchicalLookup trades convenience for strictness: Metro stops walking up through ancestor node_modules directories, so any dependency that was resolving purely because it happened to be hoisted somewhere up the tree now fails unless you've explicitly added it to <code>nodeModulesPaths</code> or <code>extraNodeModules</code> (those two options keep working regardless of this setting).</p>`,
  },
  {
    id: "a21q-monorepo-tooling-3",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `An app reads an EXPO_PUBLIC_API_URL value that was changed in the EAS dashboard, but after running eas update (not a new build), users still see the old API URL. Why?`,
    options: [`eas update doesn't support environment variables at all`, `EXPO_PUBLIC_ variables are inlined into the JS bundle at build time, and eas update only ships a new JS bundle built from whatever value was current at the last native build`, `The variable's visibility must be set to plaintext for OTA updates to pick it up`, `eas update requires a manual cache purge command before new env values apply`],
    answer: 1,
    explanationHtml: `<p>EXPO_PUBLIC_ variables are compile-time constants baked into the bundle by a Babel transform in babel-preset-expo. An OTA update is just a new JS bundle build — if you don't rebuild against the new dashboard value at update time, the update still reflects whatever was baked in previously. Truly dynamic values need a runtime fetch, not a build-time env var.</p>`,
  },
  {
    id: "a21q-monorepo-tooling-4",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `In eas.json, a 'production' build profile extends a 'base' profile. The base profile sets env: {API_URL: 'https://api.example.com'}, and production sets its own env: {NODE_ENV: 'production'} without repeating API_URL. What happens to API_URL for production builds?`,
    options: [`It's automatically merged in from base, so production still has API_URL set`, `EAS throws a validation error because the child profile's env object doesn't repeat every parent key`, `The production profile's env object replaces the base profile's wholesale, so API_URL is lost unless redeclared`, `Only platform-specific (ios/android) env blocks are merged; top-level env blocks always replace`],
    answer: 0,
    explanationHtml: `<p>EAS CLI's profile resolver merges env key-by-key when a profile extends another (<code>{...base.env, ...update.env}</code> in eas-cli's mergeProfiles), so unrelated keys from the parent survive — only a key with the same name in the child overrides the parent's value. This is a common point of confusion in base → preview → production profile chains: people assume (incorrectly) that defining any env block in a child wipes out the parent's, when in fact it merges additively and only same-named keys get shadowed.</p>`,
  },
  {
    id: "a21q-monorepo-tooling-5",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `An Nx-managed Expo app imports a library via the TS path mapping @myorg/ui in tsconfig.base.json. tsc and the IDE resolve it fine, but Metro throws 'Unable to resolve module @myorg/ui'. What's the underlying reason?`,
    options: [`Nx libraries can never be consumed by Metro under any configuration`, `Metro doesn't read tsconfig.json path mappings at all — it only does filesystem/node_modules-based resolution, so a TS-only alias is invisible to it`, `The library is missing a package.json main field`, `tsc and Metro use incompatible module formats (CJS vs ESM) by default`],
    answer: 1,
    explanationHtml: `<p>TypeScript path mappings are purely a compile-time/type-checking concern. Metro never reads tsconfig.json; it resolves modules from the filesystem and node_modules. Nx's @nx/expo package addresses this with withNxMetro, which wires Metro's resolver to resolve workspace libs from the Nx project graph / tsconfig paths instead of relying on TS path aliases alone.</p>`,
  },
  {
    id: "a21q-monorepo-tooling-6",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `A Turborepo pipeline runs the Expo app's dev/build task without listing ^build as a dependsOn entry, while a shared UI package is consumed via its compiled dist output. What's the observable failure?`,
    options: [`Turbo refuses to run any task without an explicit dependsOn entry`, `Metro bundles stale dist output from the shared package because Turbo can replay a cached 'success' for the app without re-running the dependency's build task`, `The app fails typecheck but bundles correctly`, `Metro automatically rebuilds any out-of-date dependency regardless of Turbo's task graph`],
    answer: 1,
    explanationHtml: `<p>Turbo's cache validity is driven by its declared task graph. If the consuming task doesn't depend on the shared package's build task (^build), Turbo has no signal that the dependency changed and can serve a cached result — meanwhile Metro happily bundles whatever dist files are currently on disk, which may be stale.</p>`,
  },
  {
    id: "a21q-monorepo-tooling-7",
    category: "integrations",
    categoryLabel: "Integrations",
    question: `Why does setting EAS env var visibility to 'secret' (rather than 'sensitive' or 'plaintext') mean you can never view the value again in the EAS dashboard after creation?`,
    options: [`It's a UI limitation that will be fixed in a future EAS Studio release`, `Secret-visibility values are stored write-only by design — they can be injected into builds but never re-displayed, even to project admins, matching the legacy eas secret:create behavior`, `Secret values are stored client-side only and never reach EAS servers`, `It only applies to variables created via the CLI, not the dashboard`],
    answer: 1,
    explanationHtml: `<p>The secret visibility tier is intentionally write-only as a security property, not a UI gap — once set, EAS will inject it into builds but will not surface the plaintext value again to anyone, mirroring how the legacy account/project secrets system worked before the unified env var store.</p>`,
  },
];
