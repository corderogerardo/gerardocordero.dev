// Batch 15 — Networking with fetch/Axios interceptors, offline queues & retry strategies, WebSocket reconnection patterns, and background sync.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED15_FLASHCARDS: Flashcard[] = [
  {
    id: "a15-networking-1",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Why does an Axios response interceptor that refreshes an expired access token need a mutex/single-flight guard, and what breaks without one?`,
    answerHtml: `<p>Without a guard, every in-flight request that hits a 401 around the same time triggers its own <code>/refresh</code> call. Refresh tokens are often <b>single-use/rotating</b>, so a second concurrent refresh either fails (token already consumed) or invalidates the first refreshed token, logging the user out — a race that silently ejects the user mid-session. Fix: a module-level <code>isRefreshing</code> flag plus a queue of pending resolvers — the first 401 starts the refresh; later 401s subscribe to that same in-flight promise and retry once it resolves, instead of firing their own refresh.</p><p><b>I guard token refresh with a single-flight promise so N concurrent 401s only ever trigger one refresh call.</b></p>`,
  },
  {
    id: "a15-networking-2",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `In an Axios interceptor, why is retry usually implemented by replaying \`error.config\` rather than letting the caller catch and retry?`,
    answerHtml: `<p>Centralizing replay in the interceptor keeps retry/backoff/auth policy consistent everywhere a request is made, instead of every call site inventing its own — that's the architectural win. Mechanically: <code>error.config</code> is the original request config, so you can mutate it (e.g. set a <code>_retry</code> flag, refresh the Authorization header) and call <code>axios(error.config)</code> to replay the exact same request transparently — the caller just awaits one promise.</p><p><b>I replay the original error.config from the interceptor so retry policy lives in one place, not scattered across call sites.</b></p>`,
  },
  {
    id: "a15-networking-3",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Why is retrying a POST (e.g. "create order") after a timeout dangerous, and how do you make it safe at the client?`,
    answerHtml: `<p>A timeout means the response was lost, not necessarily that the server never processed the request — blind retry can create a duplicate order, which is a business-impact bug (a double-charged customer), not just a technical glitch. Fix: generate a client-side <b>idempotency key</b> (UUID) per logical operation and send it as a header, reusing the same key across retries of that operation. The server dedupes by key and returns the original result instead of re-executing the mutation.</p><p><b>Red flag:</b> treating all retries as equally safe because GET is idempotent — non-idempotent mutations need an explicit idempotency key before you retry them.</p><p><b>I never retry a POST without an idempotency key — a lost response isn't proof the server didn't process it.</b></p>`,
  },
  {
    id: "a15-networking-4",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `What's wrong with retrying every failed request with the same fixed delay, and what should replace it?`,
    answerHtml: `<p>Fixed-delay retry across many clients causes a <b>thundering herd</b>: all clients back off the same amount and hammer the server again simultaneously after an outage. Replace with <b>exponential backoff + jitter</b> (e.g. <code>base * 2^attempt + random(0, base)</code>), capped at a max delay. Jitter decorrelates clients so retries spread out instead of synchronizing.</p><p><b>I always pair exponential backoff with jitter — backoff alone still lets every client retry in lockstep.</b></p>`,
  },
  {
    id: "a15-networking-5",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Which HTTP failures should an Axios retry interceptor retry automatically, and which should it never retry?`,
    answerHtml: `<p>Safe to auto-retry: network errors, timeouts, <code>502/503/504</code>, and <code>429</code> (respecting <code>Retry-After</code> if present) — these are typically transient and the request is idempotent (GET) or idempotency-keyed. Never blind-retry <code>400/401/403/404/422</code> — these are deterministic client errors that will fail identically again; retrying just wastes a round trip (401 is the one exception, but only via the refresh-and-retry-once flow, not generic retry).</p><p><b>Red flag:</b> retrying a 401 the same way you'd retry a 500 — a 401 gets exactly one retry, gated behind the refresh flow, never a generic backoff loop.</p><p><b>I only auto-retry transient, idempotent failures — deterministic 4xx errors get surfaced, not retried.</b></p>`,
  },
  {
    id: "a15-networking-6",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Why does AbortController matter for fetch/Axios calls tied to React Native screen lifecycles, beyond just "canceling slow requests"?`,
    answerHtml: `<p>If a component unmounts (user navigates away) before a fetch resolves, calling <code>setState</code> in the <code>.then()</code> on an unmounted component is a leak/warning source, and in a list screen a stale response can overwrite newer data (race condition) if the user navigated to screen B then back to a re-fetched screen A. Wiring <code>AbortController</code> into a <code>useEffect</code> cleanup (<code>return () => controller.abort()</code>) cancels the underlying request so the promise rejects with <code>AbortError</code> before it can touch state, and frees the OS-level socket/radio on cellular.</p><p><b>I always wire AbortController into the effect cleanup so navigating away cancels the request instead of leaving it to race a still-mounted screen.</b></p>`,
  },
  {
    id: "a15-networking-7",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `In Axios, how do you cancel an in-flight request with AbortController, and how does this differ from the deprecated CancelToken API?`,
    answerHtml: `<p>Pass <code>{ signal: controller.signal }</code> in the request config; calling <code>controller.abort()</code> rejects the promise with an <code>AbortError</code> (Axios normalizes via <code>axios.isCancel(err)</code> / <code>err.code === 'ERR_CANCELED'</code>). This replaced the old <code>CancelToken.source()</code> API (deprecated since Axios 0.22) and is now also the native <code>fetch</code> cancellation mechanism — one signal can cancel multiple requests, and AbortController composes with timeouts via <code>AbortSignal.timeout()</code> on web/Node.</p><p><b>AbortController is the one cancellation primitive I use everywhere now — Axios normalizes it, fetch defines it natively, and CancelToken is dead.</b></p>`,
  },
  {
    id: "a15-networking-8",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Why can't \`fetch\` time out a request the way \`axios({ timeout })\` can, and what's the fix in a React Native app?`,
    answerHtml: `<p>Native <code>fetch</code> has no built-in <code>timeout</code> option — left alone it waits indefinitely on a stalled connection. The standard fix is to wire an <code>AbortController</code> manually: start a <code>setTimeout(() => controller.abort(), ms)</code> alongside the request and pass <code>{ signal: controller.signal }</code> to <code>fetch</code>. Note <code>AbortSignal.timeout()</code> (the one-liner shortcut) is <b>not reliably available in React Native's Hermes engine</b> as of 2025-2026 (throws <code>AbortSignal.timeout is not a function</code> on some RN/Hermes versions per open RN issues) — the manual <code>setTimeout</code>+<code>abort()</code> pattern is the safe cross-version choice. Axios's <code>timeout</code> config does this internally for you, which is one of the practical reasons teams still reach for Axios over bare fetch in RN apps with flaky cellular networks.</p><p><b>I always wire timeout manually via AbortController + setTimeout in React Native — I don't assume AbortSignal.timeout() is available on Hermes.</b></p>`,
  },
  {
    id: "a15-networking-9",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `What's a key difference between how \`fetch\` and \`axios\` treat HTTP error status codes (4xx/5xx)?`,
    answerHtml: `<p><code>fetch</code>'s role is a low-level network primitive — it resolves as long as an HTTP response came back at all, so a <code>404</code> or <code>500</code> resolves successfully and you must check <code>response.ok</code>/<code>response.status</code> yourself. <code>axios</code>'s role is an opinionated HTTP client — it rejects the promise for any non-2xx status by default (configurable via <code>validateStatus</code>), which is why Axios's interceptor-based retry/refresh patterns key off <code>error.response.status</code> in a single <code>catch</code>/interceptor instead of branching after every successful fetch.</p><p><b>Red flag:</b> treating a bare <code>fetch</code> call like Axios and skipping the <code>response.ok</code> check — a 500 page silently "succeeds" and gets rendered as data.</p><p><b>I never trust a resolved fetch promise as success — I check response.ok before touching the body.</b></p>`,
  },
  {
    id: "a15-networking-10",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Why should an offline mutation queue be persisted to disk rather than kept in memory, and what does that imply for serialization?`,
    answerHtml: `<p>An app can be killed by the OS (backgrounded, low memory) or the device can lose power between queuing a mutation and flushing it; an in-memory queue loses that mutation silently, producing data loss the user believes was saved. Persisting to AsyncStorage/MMKV/SQLite means every queued mutation must be <b>plain serializable data</b> (URL, method, body, headers, a client-generated id) rather than a closure or live request object, since functions can't survive JSON serialization across app restarts.</p><p><b>Red flag:</b> queuing closures or live request objects instead of plain data — they can't survive JSON serialization across an app restart, so the mutation silently vanishes.</p><p><b>I persist the offline queue as plain serializable records, never closures, because the app can be killed before it flushes.</b></p>`,
  },
  {
    id: "a15-networking-11",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `When replaying a queued offline mutation, why is queue order alone not sufficient — what extra conflict-handling is usually needed?`,
    answerHtml: `<p>Replaying strictly in FIFO order assumes the server state hasn't diverged, but other devices/users may have mutated the same resource while this device was offline. A naive replay can silently overwrite newer server data (lost update) or fail outright on a version mismatch. Production queues typically attach an optimistic-concurrency token (e.g. <code>updatedAt</code> or a version number) to each mutation and resolve <code>409 Conflict</code> responses with a merge strategy or last-write-wins policy, rather than assuming queue order is authority order.</p><p><b>I attach a version/updatedAt token to every queued mutation so replay can detect a conflict instead of blindly overwriting newer server state.</b></p>`,
  },
  {
    id: "a15-networking-12",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `What's the practical difference between an offline mutation queue and a normal retry-with-backoff interceptor?`,
    answerHtml: `<p>A retry interceptor handles <i>transient</i> failures on a request that's still being awaited by the caller — it lives and dies with that call. An offline queue handles the case where there's <b>no connectivity at all</b>: the mutation is durably stored, the caller's promise typically resolves immediately (optimistic UI), and a separate process (often triggered by a connectivity-change listener, e.g. <code>@react-native-community/netinfo</code>'s <code>addEventListener</code>) drains the queue later, independent of any single screen being mounted.</p><p><b>A retry interceptor's job is surviving a blip on a call that's still in flight; an offline queue's job is surviving no connectivity at all.</b></p>`,
  },
  {
    id: "a15-networking-13",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Why is \`NetInfo.isConnected\` (or \`navigator.onLine\` on web) an unreliable signal for "safe to retry now", and what's a more robust check?`,
    answerHtml: `<p>"Connected" often just means associated with a Wi-Fi AP or cell tower, not that the network has real internet egress (captive portals, VPN misconfiguration, carrier issues) — this is the classic false-positive. <code>NetInfo</code> exposes <code>isInternetReachable</code> (a separate, slower probe) for that reason. The robust pattern: treat connectivity events as a trigger to <i>attempt</i> a flush, but let the actual HTTP request's success/failure be the real source of truth, re-queuing on failure rather than trusting the connectivity flag alone.</p><p><b>Red flag:</b> gating a retry purely on <code>isConnected</code>/<code>navigator.onLine</code> — both report link-layer association, not real internet egress, so a captive portal or VPN misconfig reads as "connected."</p><p><b>I treat connectivity events as a trigger to attempt a flush, not proof it will succeed — the request's own result is the source of truth.</b></p>`,
  },
  {
    id: "a15-networking-14",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `What causes a WebSocket reconnect loop to make an outage worse, and how does exponential backoff with jitter prevent it?`,
    answerHtml: `<p>If every client reconnects on a fixed interval (e.g. immediately, or every 1s), thousands of clients reconnecting in lockstep after a server restart create a synchronized spike that can re-crash the server before it stabilizes — a thundering herd. Exponential backoff (1s, 2s, 4s, 8s...) spreads attempts over time, and adding random jitter to each delay decorrelates clients that started backing off at the same moment, smoothing the reconnect traffic instead of producing recurring spikes.</p><p><b>I never let WebSocket clients reconnect on a fixed interval — exponential backoff with jitter is what keeps a server restart from turning into a second outage.</b></p>`,
  },
  {
    id: "a15-networking-15",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `After a WebSocket reconnects, why can't the client just resume normally — what state needs reconciling?`,
    answerHtml: `<p>Any messages broadcast while the socket was disconnected are simply gone — WebSockets don't buffer for offline clients by default. A robust client tracks the last received message's sequence number or timestamp and, on reconnect, either (a) sends it to the server to request a backfill/replay of missed events, or (b) triggers a full state refetch via REST as a fallback. Without this, the client silently has a stale view until the next message happens to arrive.</p><p><b>On reconnect I always reconcile — either replay missed events by sequence number or refetch state via REST — a resumed socket doesn't mean the client saw everything.</b></p>`,
  },
  {
    id: "a15-networking-16",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Why is \`onclose\` alone not enough to decide whether a WebSocket should reconnect?`,
    answerHtml: `<p>The WebSocket close event carries a <code>code</code> (and optional <code>reason</code>) that distinguishes recoverable from terminal closes. Codes like <code>1006</code> (abnormal closure, e.g. network drop) or <code>1001</code> (going away) should trigger reconnect-with-backoff. But codes like <code>1008</code> (policy violation — e.g. bad auth) or an application-level "banned/invalid token" frame mean reconnecting with the same credentials will just fail again in a loop; the client should stop and force re-authentication instead of retrying blindly.</p><p><b>Red flag:</b> reconnecting unconditionally on any <code>onclose</code> — a terminal code like 1008 (policy/auth violation) means the same credentials will fail again, so blind retry just loops; branch on the close code and force re-auth for terminal ones.</p><p><b>I branch reconnect logic on the close code — transient codes get backoff-retry, terminal/policy codes get re-authentication, never both treated the same.</b></p>`,
  },
  {
    id: "a15-networking-17",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Why do production WebSocket clients implement application-level heartbeats (ping/pong) in addition to the protocol's built-in ping/pong frames?`,
    answerHtml: `<p>Many proxies, NATs, and especially mobile carrier networks silently drop idle TCP connections or buffer/swallow low-level WebSocket control frames without telling either endpoint — the socket can look "open" while actually being dead. An app-level heartbeat (a JSON <code>{type: 'ping'}</code> sent every N seconds, expecting a <code>pong</code> within a timeout) gives the client a reliable signal to detect a half-open connection and force-close + reconnect, rather than waiting on a TCP timeout that can take minutes.</p><p><b>I add an application-level heartbeat on top of protocol ping/pong because carrier NATs and proxies can silently kill an idle socket while it still looks open.</b></p>`,
  },
  {
    id: "a15-networking-18",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `On React Native specifically, why does app backgrounding need explicit handling in WebSocket reconnect logic?`,
    answerHtml: `<p>iOS suspends a backgrounded app's JS thread (network sockets get torn down by the OS after a short grace period, and there's no persistent background socket without special entitlements like VoIP push), and Android can also kill the connection under memory pressure or Doze mode. A WebSocket left "connected" while backgrounded is usually already dead by the time the app resumes. The correct pattern: listen to <code>AppState</code> changes, proactively close the socket on background (or let it die) and reconnect-from-scratch on the <code>active</code> transition, rather than assuming the old socket is still good.</p><p><b>Red flag:</b> assuming a WebSocket is still alive after the app returns from background — treat it as dead, close proactively on background, and reconnect from scratch on the active transition.</p><p><b>I never trust a backgrounded socket to still be open — I close it on background and reconnect fresh on foreground.</b></p>`,
  },
  {
    id: "a15-networking-19",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `What's the difference between client-driven polling-based "background sync" and the native Background Sync / Background Fetch APIs, and why does it matter on iOS?`,
    answerHtml: `<p>A naive approach keeps a timer running in JS to sync periodically — but JS timers don't run once the app is backgrounded/suspended. True background sync on iOS uses <code>BGTaskScheduler</code> (exposed in Expo via <code>expo-background-task</code>, which replaced the now-deprecated <code>expo-background-fetch</code> as of SDK 53), where the OS — not the app — decides when to wake the app briefly based on usage patterns, battery, and network conditions, with no guaranteed interval and a strict execution time budget (seconds, not minutes). Code must do a small, fast sync (flush a queue, fetch a delta) and call the completion handler promptly, or iOS kills it and penalizes future scheduling.</p><p><b>Red flag:</b> relying on a running JS <code>setInterval</code> for background sync — JS timers stop the moment the app suspends, so nothing fires until the user reopens it. Use <code>BGTaskScheduler</code>/<code>expo-background-task</code>, where the OS wakes the app on its own schedule within a tight time budget.</p><p><b>I never assume a JS timer keeps ticking in the background — real background sync has to go through the OS scheduler.</b></p>`,
  },
  {
    id: "a15-networking-20",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Why is a "sync on app foreground" listener (via \`AppState\`) still necessary even if you've set up \`expo-background-task\`?`,
    answerHtml: `<p>Background task execution on both iOS and Android is opportunistic and rate-limited by the OS — there's no guarantee it ran recently, or at all, especially if the app was force-quit (iOS cancels scheduled background tasks on force-quit) or the device was in low-power mode. An <code>AppState</code> listener that triggers a sync/refetch on the <code>background → active</code> transition is the reliable fallback that guarantees freshness the moment a user actually opens the app, regardless of what the OS scheduler did in the meantime.</p><p><b>I always pair scheduled background sync with a foreground AppState listener — the OS scheduler is opportunistic, so foreground refresh is the guaranteed fallback.</b></p>`,
  },
  {
    id: "a15-networking-21",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `How does Axios actually order interceptor execution, and why does that matter for a chain that does (1) attach auth header, (2) log requests, (3) retry on 401?`,
    answerHtml: `<p>Verified against Axios's source (<code>Axios.prototype.request</code>): request interceptors are <code>unshift</code>'d onto the chain, so they run in <b>reverse</b> registration order — the most-recently-<code>.use()</code>'d request interceptor runs first, closest to the moment the request actually dispatches. Response interceptors are <code>push</code>'d, so they run in <b>registration order</b> — the first-registered response interceptor sees the response (or error) first. Concretely: if auth-attach is registered after the logger as a request interceptor, auth-attach runs <i>before</i> the logger (good — logger sees the header). But if the 401-retry interceptor is registered after a generic error-logging response interceptor, the logger sees the raw error first and the retry interceptor sees it second — ordering must be deliberate, especially when interceptors are registered from different modules.</p><p><b>I register interceptors deliberately, not by habit — request interceptors run last-in-first-out, response interceptors run in registration order, and mixing that up silently reorders auth, logging, and retry.</b></p>`,
  },
  {
    id: "a15-networking-22",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `What race condition can occur between an offline mutation queue and a normal (online) optimistic update to the same record, and how is it usually avoided?`,
    answerHtml: `<p>If a user edits a record while offline (queued), then regains connectivity and the queue is mid-flush, a second edit issued "live" before the queued one has actually synced can race it — the live request might land first, then the stale queued mutation overwrites it on replay. The usual fix: serialize all mutations to a given resource through the same queue (even when online), so there's one consistent, ordered pipeline per resource rather than two independent paths (queue vs. direct call) that can interleave unpredictably.</p><p><b>I route every mutation to a resource through one queue, online or offline, so there's a single ordered pipeline instead of two paths that can race.</b></p>`,
  },
  {
    id: "a15-networking-23",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Why might a senior engineer choose \`fetch\` + a thin wrapper over Axios for a new React Native project in 2025-2026, given React Query/TanStack Query is also in the stack?`,
    answerHtml: `<p>Because React Query already owns caching, retry-with-backoff, request dedup, and cancellation (via its own AbortController integration) at the query-client level, most of what Axios interceptors are traditionally reached for becomes redundant — that's the architectural reason to reconsider Axios once React Query is in the stack.</p><p>The real cost of dropping Axios: you give up its built-in <code>timeout</code> option, automatic non-2xx rejection, and mature interceptor ergonomics, so a thin fetch wrapper has to reimplement base URL handling, auth header injection, and JSON parsing by hand. But keeping Axios anyway has a concrete cost too — two overlapping retry/cache/cancellation systems (Axios interceptors underneath, React Query's policies on top) create ambiguity about which layer actually retried a given request, plus the extra bundle weight of an abstraction layer that's mostly translating fetch semantics into Axios semantics for React Query to then wrap again. Reframed as an investment: collapsing to one data layer (fetch + thin wrapper, or Axios alone without duplicating React Query's retry logic) means one policy surface to reason about and debug, which matters more as the number of endpoints and engineers grows — divergent retry behavior between two libraries is the kind of bug that only surfaces in production under load.</p><p><b>Red flag:</b> keeping Axios out of habit once React Query already covers retry/cache/cancel — that's an unnecessary abstraction layer, not a safety net.</p><p><b>Once React Query owns caching and retry, I keep the HTTP layer as thin as possible — usually plain fetch — so there's exactly one retry/cache policy to reason about, not two disagreeing ones.</b></p>`,
  },
  {
    id: "a15-networking-24",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Why does queuing offline mutations per-resource (not just globally FIFO) help avoid wasted network calls?`,
    answerHtml: `<p>If a user offline toggles a "favorite" on/off three times before reconnecting, a naive global FIFO queue replays all three mutations in order — three network calls for a net-zero change. A queue that coalesces/collapses mutations targeting the same resource+field (keeping only the latest intent, or squashing toggle pairs) before flush reduces both network usage and the chance of an intermediate state being briefly visible to other clients/devices during replay.</p><p><b>I coalesce same-resource mutations in the offline queue before flush — replaying every recorded toggle wastes calls and can flash stale state to other clients.</b></p>`,
  },
];

export const ADVANCED15_QUIZ: QuizQuestion[] = [
  {
    id: "a15q-networking-1",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `A mobile app's Axios response interceptor refreshes the access token on a 401 and retries the original request. Under poor connectivity, the user briefly has 6 requests in flight, and 3 hit 401 around the same time. Without a single-flight guard, what's the most likely failure?`,
    options: [`The app crashes with an unhandled promise rejection`, `Multiple concurrent refresh calls race; the rotating refresh token gets consumed twice and the user is logged out`, `Axios automatically deduplicates the refresh calls, so nothing breaks`, `The original 3 requests succeed but return stale cached data`],
    answer: 1,
    explanationHtml: `<p>Rotating refresh tokens are single-use. Two near-simultaneous refresh calls mean the second one presents an already-consumed token and gets rejected, often triggering a forced logout. This is why refresh logic needs an <code>isRefreshing</code> flag and a pending-request queue so only one refresh call is ever in flight.</p><p>The tempting wrong answer is assuming Axios "just handles" concurrent refreshes for you (option 2) — it doesn't; Axios has no built-in single-flight behavior, so without an explicit guard every concurrent 401 fires its own refresh call.</p>`,
  },
  {
    id: "a15q-networking-2",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Which retry policy is most appropriate for a GET request that fails with a 503 Service Unavailable response carrying a \`Retry-After: 30\` header?`,
    options: [`Retry immediately in a tight loop until it succeeds`, `Ignore Retry-After and use a fixed 1-second retry interval`, `Honor Retry-After as a minimum delay, then apply exponential backoff with jitter for subsequent attempts if it fails again`, `Never retry 5xx errors automatically since they indicate a server bug`],
    answer: 2,
    explanationHtml: `<p>503 with <code>Retry-After</code> is the server explicitly telling clients when it expects to recover (often during a deploy or overload) — ignoring it adds to the very load causing the problem. Honor it as the floor for the first retry, then fall back to backoff+jitter if it keeps failing, since the header is only a hint for one retry, not a guarantee.</p><p>The tempting wrong answers both oversimplify: "never retry 5xx" ignores that many 5xx codes (502/503/504) are transient infrastructure blips, not code bugs; and a fixed 1-second retry that ignores <code>Retry-After</code> can hit the server again before it's actually recovered.</p>`,
  },
  {
    id: "a15q-networking-3",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `A React Native app's WebSocket reconnect logic uses \`setTimeout(reconnect, 1000)\` on every \`onclose\`, with no variation between clients. After a server restart serving 50,000 connected clients, what's the likely outcome?`,
    options: [`Clients reconnect smoothly since 1 second is a reasonable delay`, `All clients attempt to reconnect at nearly the same moment, creating a load spike that can prevent the server from stabilizing`, `The fixed delay has no effect on server load since WebSocket handshakes are stateless`, `Only clients on Wi-Fi will reconnect; cellular clients will fail silently`],
    answer: 1,
    explanationHtml: `<p>This is the classic thundering-herd problem: a synchronized fixed delay means every client retries in lockstep, so the server faces a recurring spike of simultaneous reconnect attempts instead of a smoothed-out trickle. Exponential backoff with random jitter decorrelates clients so reconnects spread out over time.</p><p>The tempting wrong answer is judging "1 second is reasonable" in isolation (option 0) — the problem isn't the delay's length, it's that every client uses the exact same delay with no jitter, so they all retry in lockstep regardless of how long or short that delay is.</p>`,
  },
  {
    id: "a15q-networking-4",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `An offline mutation queue persists pending API calls to AsyncStorage and replays them on reconnect. A user toggles a boolean setting on, then off, then on again while offline. What's the main risk of replaying the queue exactly as recorded, in order?`,
    options: [`AsyncStorage has a hard limit of 2 mutations, so the third write is silently dropped`, `Three redundant network calls are made for what is, net, a single state change — wasting bandwidth and risking a visible flicker to other clients`, `The mutations will execute in reverse order due to how Promise.all schedules them`, `AsyncStorage cannot store more than one pending mutation per key, so only the last toggle survives`],
    answer: 1,
    explanationHtml: `<p>Naive FIFO replay sends every recorded mutation even when the net effect is a no-op or could be collapsed to one call. A queue that coalesces same-resource mutations before flush avoids the redundant calls and the brief incorrect state other clients might observe during replay. (AsyncStorage itself has no such 2-item limit — that's a distractor.)</p><p>Options 0 and 2 invent mechanics AsyncStorage/Promise scheduling don't actually have — neither a hard 2-item cap nor reversed execution order is real; they're distractors testing whether you reach for the real explanation instead of a specific-sounding but fabricated one.</p>`,
  },
  {
    id: "a15q-networking-5",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `Why does a WebSocket client need to track the close event's status code (e.g. distinguishing 1006 from 1008) rather than always reconnecting on any \`onclose\`?`,
    options: [`Close codes determine which compression algorithm to use on reconnect`, `Some close reasons (like auth/policy violations) are terminal and will fail again identically on retry, so blind reconnect creates an infinite failing loop instead of surfacing a re-auth flow`, `Close codes are only informational and have no bearing on reconnect strategy`, `1006 always means the server is permanently down and the client should stop retrying`],
    answer: 1,
    explanationHtml: `<p>Codes like 1008 (policy violation, often bad/expired auth at the protocol level) mean the same credentials will fail again — looping retries just burns battery and spams the server. The client should branch: transient codes (e.g. 1006 abnormal closure) get backoff-retry, terminal/policy codes trigger re-authentication instead.</p><p>The tempting wrong answer is treating 1006 as always fatal (option 3) — 1006 (abnormal closure) is usually a transient network drop and should get backoff-retry; it's codes like 1008 that are terminal and warrant giving up on retry.</p>`,
  },
  {
    id: "a15q-networking-6",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `An Expo app schedules background sync via \`expo-background-task\` (wrapping iOS \`BGTaskScheduler\`) to flush a queue of pending uploads periodically. What's the most accurate description of how this executes on iOS?`,
    options: [`The JS timer keeps running at the exact configured interval even while the app is fully backgrounded`, `iOS guarantees the task fires within +/- 5 seconds of the requested interval, every time`, `iOS decides opportunistically when to briefly wake the app based on usage patterns and battery/network state, with no guaranteed interval and a tight execution budget`, `Background tasks only run while the device is plugged in and on Wi-Fi, never otherwise`],
    answer: 2,
    explanationHtml: `<p>BGTaskScheduler-based execution is opportunistic and OS-managed — there's no fixed interval guarantee, and the wake window is short, so the sync logic must be fast and idempotent. This is why apps still need an \`AppState\`-driven foreground sync as a reliable fallback rather than depending solely on background scheduling.</p><p>The tempting wrong answers all assume a hard guarantee (a fixed interval, a +/-5s window, or a Wi-Fi/charging requirement) — BGTaskScheduler makes no such promises; the OS decides opportunistically, which is exactly why an AppState-driven foreground sync remains necessary as a fallback.</p>`,
  },
  {
    id: "a15q-networking-7",
    category: "data",
    categoryLabel: "DATA & STATE",
    question: `A team replaces \`CancelToken.source()\` with \`AbortController\` in their Axios layer and wires \`controller.abort()\` into a screen's \`useEffect\` cleanup. What problem does this primarily solve?`,
    options: [`It permanently fixes 401 errors by automatically refreshing tokens on abort`, `It prevents state updates on unmounted components and frees the in-flight request when the user navigates away before the response arrives`, `It converts all GET requests into POST requests for caching purposes`, `It guarantees the request will succeed on retry without backoff logic`],
    answer: 1,
    explanationHtml: `<p>Tying AbortController to unmount cleanup ensures a request that's no longer relevant (because the user left the screen) is actually canceled at the network layer, avoiding both wasted bandwidth and the classic "setState on unmounted component" / stale-response race condition. CancelToken is the deprecated predecessor API that AbortController replaced across both fetch and Axios.</p><p>The tempting wrong answer conflates cancellation with reliability (option 3) — aborting a stale request has nothing to do with whether a retried request will succeed; AbortController only controls whether an unwanted request keeps running, not its outcome.</p>`,
  },
];
