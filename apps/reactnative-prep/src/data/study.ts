// Content migrated from the original single-file study guide. These typed modules are now the source of truth — edit directly.
export type StudySection = { id: string; num: string; title: string; html: string };

export const STUDY_INTRO_HTML = "<span class=\"lbl\">Be honest, then bridge</span> Three areas are likely lighter on your CV than\n      the JD wants: <b>MobX</b>, deep <b>GraphQL caching</b>, and heavy <b>native</b> code. Each card below flags a “close the\n      gap” note so you can answer truthfully <i>and</i> show you can ramp — exactly the senior move.";

export const STUDY_SECTIONS: StudySection[] = [
  {
    "id": "st-1",
    "num": "01",
    "title": "01 · React Native, JavaScript & TypeScript",
    "html": "<p><b>What they want:</b> strong <i>commercial</i> RN/JS/TS — not tutorials, production apps. TypeScript fluency: typed\n        props and hooks, discriminated unions for state, generics for reusable utilities, strict null-safety, typing API\n        responses so the compiler catches shape mismatches.</p>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> 10 yrs JS, 8 yrs React/RN, 5 yrs TypeScript, 8+ shipped apps. TS is\n        your daily driver on Valt. Talk about how typing DTOs and store slices prevents whole classes of runtime bugs.</div>"
  },
  {
    "id": "st-2",
    "num": "02",
    "title": "02 · New Architecture, lifecycle, rendering & performance",
    "html": "<p><b>What they want:</b> deep understanding of the New Architecture (Fabric, TurboModules, JSI, Codegen, Hermes),\n        component lifecycle (mount → render → commit → effects), why re-renders happen, and how to optimize. See the\n        <a href=\"#arch-9\" onclick=\"goTab('arch')\">Architecture · New Architecture</a> section for the full breakdown.</p>\n      <ul>\n        <li>Render behavior: reconciliation, keys, <code>React.memo</code>, referential stability, when <code>useMemo</code>/<code>useCallback</code> actually help.</li>\n        <li>Concurrent React (18/19) — transitions, <code>useDeferredValue</code> — and how Fabric enables it.</li>\n        <li>Lists: <code>FlatList</code> windowing, <code>getItemLayout</code>, FlashList for heavy feeds.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> You profiled and killed re-render flicker and frozen screens on Valt,\n        and drove a Hermes rollout at Bits Kingdom. You can speak to rendering from the trenches.</div>"
  },
  {
    "id": "st-3",
    "num": "03",
    "title": "03 · State management — Zustand & MobX",
    "html": "<p><b>What they want:</b> senior RN JDs increasingly name <i>Zustand and MobX</i> specifically. You're strong on Zustand;\n        MobX is the one to brush up.</p>\n      <h4>Zustand (your strength)</h4>\n      <p>A tiny store created with <code>create()</code>; components subscribe with a <b>selector</b> so they re-render only when\n        their slice changes. No providers, no boilerplate, works outside React. Talk about selector discipline and\n        <code>shallow</code> equality to avoid extra renders.</p>\n      <h4>MobX (close the gap)</h4>\n      <p>MobX is <b>transparent reactive state</b>: you mark state <code>observable</code>, wrap components in\n        <code>observer()</code>, and they automatically re-render when the exact observables they read change. You mutate state\n        directly inside <code>action</code>s, and <code>computed</code> values derive cached state. Mental model: a spreadsheet —\n        change a cell, every formula that depends on it recalculates.</p>\n      <table>\n        <tr><th></th><th>Zustand</th><th>MobX</th></tr>\n        <tr><td>Style</td><td>Immutable-ish, selector subscriptions</td><td>Mutable, observable + reactions</td></tr>\n        <tr><td>Re-render trigger</td><td>Selector result changes</td><td>Observed field changes</td></tr>\n        <tr><td>Updates</td><td><code>set(state =&gt; …)</code></td><td>mutate inside an <code>action</code></td></tr>\n        <tr><td>Derived state</td><td>compute in selector</td><td><code>computed</code> (cached)</td></tr>\n      </table>\n      <div class=\"callout tip\"><span class=\"lbl\">How to say it honestly</span> “My production depth is in Zustand and Redux, and\n        I've used Context and React Query for server state. I've worked with MobX's model — observables, observer components,\n        actions, and computed values — and since I understand reactive state deeply, I'd be productive in your MobX code quickly.”\n        Then pivot to a Zustand performance story you own.</div>\n      <div class=\"callout\"><span class=\"lbl\">Key distinction to land</span> <b>Server state vs client state.</b> React Query/SWR\n        own server cache (fetching, caching, revalidation); Zustand/MobX own client/UI state. Mixing them up is a common\n        mistake — keep them separate.</div>"
  },
  {
    "id": "st-4",
    "num": "04",
    "title": "04 · GraphQL & efficient data-fetching",
    "html": "<p><b>What they want:</b> integrate with GraphQL services and understand efficient fetching + caching. Senior talking\n        points: query only the fields you need (vs REST over-fetching), a <b>normalized cache</b> (Apollo/urql store entities by\n        id so one update reflects everywhere), pagination with cursors + <code>fetchMore</code>, optimistic responses, and\n        cache invalidation after mutations.</p>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> You built and consumed <b>AppSync GraphQL</b> at Novacomp (Cognito,\n        Lambda resolvers, DynamoDB, OpenSearch) and you've used React Query/SWR caching with optimistic updates for years —\n        the same caching instincts transfer directly to Apollo/urql.</div>\n      <div class=\"callout tip\"><span class=\"lbl\">Close the gap</span> Skim Apollo Client's normalized cache &amp; <code>cache.modify</code>,\n        and be ready to contrast a <b>normalized cache</b> (Apollo) with a <b>query-key cache</b> (React Query). One sentence on\n        each shows real understanding.</div>"
  },
  {
    "id": "st-5",
    "num": "05",
    "title": "05 · Native iOS / Android development",
    "html": "<p><b>What they want:</b> hands-on native (Swift, Objective-C, Kotlin, Java), native SDK integration, and build-config\n        changes when RN isn't enough. You don't need to be a full native engineer — you need to be the RN engineer who isn't\n        afraid of the native side.</p>\n      <ul>\n        <li>Writing/patching a <b>native module</b> or TurboModule; bridging a native SDK to JS.</li>\n        <li>Build config: <b>CocoaPods</b>/Xcode schemes, Gradle/build variants, signing &amp; provisioning, permissions in Info.plist / AndroidManifest.</li>\n        <li>Reading native crash logs and SDK docs.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> PSPDFKit native integration with <b>native module patches</b>, custom\n        iOS/Android camera-permission hooks, native universal/deep-link handling, and owning app IDs, certificates, and\n        notification setup on Apple Developer + Google Cloud.</div>\n      <div class=\"callout tip\"><span class=\"lbl\">How to say it honestly</span> “I'm comfortable dropping into native to integrate\n        an SDK, patch a module, or fix build config — I've done PSPDFKit patches and native permission hooks. I'm not writing\n        large Swift/Kotlin features daily, but I read it, debug it, and ship the native glue RN needs.”</div>"
  },
  {
    "id": "st-6",
    "num": "06",
    "title": "06 · Complex, large-scale & legacy codebases",
    "html": "<p><b>What they want:</b> ramp fast on a big, evolving codebase and contribute without breaking things. Method: read the\n        data flow before the files, find the smallest safe change, lean on types and tests, follow existing patterns rather than\n        importing your own.</p>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> Three years inside one growing app (Valt), Turborepo monorepo tooling,\n        and a documented habit of “quickly understand and contribute to a large, evolving codebase.” You've also <i>built</i>\n        shared packages others depend on.</div>"
  },
  {
    "id": "st-7",
    "num": "07",
    "title": "07 · Testing & quality",
    "html": "<p><b>What they want:</b> low post-release bug rate via real testing — unit, integration, and CI-gated E2E. Know the\n        pyramid: many fast unit tests (Jest), fewer integration tests (React Native Testing Library — test behavior, not\n        implementation), few E2E (Maestro / Detox) on critical flows. Talk about testable architecture: pure functions, injected\n        dependencies, thin components.</p>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> Jest unit &amp; integration, <b>CI-gated E2E</b>, and Maestro on your CV;\n        you set up CI test pipelines from scratch (Bitbucket Pipelines) early in your career. “Maintain a testable and reusable\n        codebase” is literally a line you live.</div>"
  },
  {
    "id": "st-8",
    "num": "08",
    "title": "08 · CI/CD, releases & third-party SDKs",
    "html": "<p><b>What they want:</b> own the path to production — CI/CD pipelines, App Store + Google Play releases, third-party SDK\n        integration. Know: build automation (EAS Build / Fastlane / GH Actions), OTA updates (EAS Update / CodePush) and their\n        limits (JS-only, not native), staged rollouts, and release/review processes.</p>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> You <b>owned production releases and App Store review responses</b> on\n        Valt, used Expo EAS for CD, GitHub Actions, App Center, and integrated SDKs like Twilio, PSPDFKit, OneSignal, Stripe, and\n        In-App Purchases. This is a clear strength — lead with it.</div>"
  },
  {
    "id": "st-9",
    "num": "09",
    "title": "09 · Refactoring, tech debt & stability",
    "html": "<p><b>What they want:</b> reduce crash rates and tech debt with direction. Approach: measure first (Sentry/Crashlytics for\n        crash-free rate), refactor behind tests in small safe steps, strangle legacy patterns gradually rather than big-bang\n        rewrites, and tie every refactor to a metric (startup time, crash rate, re-renders).</p>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> Performance refactors on Valt (re-render &amp; frozen-screen fixes),\n        version-based cache invalidation for safe rollouts, and driving RN 0.59→0.62 + Hermes upgrades — all stability work.</div>"
  },
  {
    "id": "st-10",
    "num": "10",
    "title": "10 · Experiment-driven development",
    "html": "<p><b>What they want:</b> this is core to experiment-driven product teams — A/B tests, feature flags, analytics-driven iteration. Know:\n        feature-flag patterns (gradual rollout, kill switches, flag hygiene/cleanup), how an experiment is instrumented (assign\n        variant → fire events → read metrics), guardrail metrics, and writing code that can run two variants cleanly.</p>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> You've worked analytics + tracking (Google Analytics, App Center,\n        OneSignal) and shipped iteratively with product as a PM-adjacent engineer. Frame your delivery style as “ship, measure,\n        iterate.”</div>\n      <div class=\"callout tip\"><span class=\"lbl\">Close the gap</span> Be ready to name tools by category — flags (LaunchDarkly,\n        Statsig, or a homegrown system), analytics (Amplitude/Mixpanel/Segment) — and say you adapt to whatever they use.</div>"
  },
  {
    "id": "st-11",
    "num": "11",
    "title": "11 · Debugging, profiling & troubleshooting",
    "html": "<p><b>What they want:</b> strong, systematic debugging. Toolbelt: React DevTools Profiler &amp; the “why did this render”\n        tooling, Flipper / the new RN DevTools, Hermes profiler, native instruments (Xcode Instruments, Android Profiler),\n        Sentry breadcrumbs, and network inspection. Method: reproduce → isolate → measure → fix → add a regression test.</p>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> Flipper profiling, render-thread debugging, and the Valt performance\n        investigation are your headline debugging stories. “I profile instead of guessing” is your signature line.</div>"
  },
  {
    "id": "st-12",
    "num": "12",
    "title": "12 · Soft skills & collaboration",
    "html": "<p><b>What they want:</b> adaptability, attention to detail (pixel-perfect, clean code), initiative, team play, and clear\n        communication with non-technical stakeholders. In a remote, PST-overlapping role, <b>written clarity and proactive\n        updates</b> matter as much as code.</p>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> You acted as PM with clients, ran recruiting interviews, mentored a\n        junior dev, and coordinated dev/design teams. You explain technical things to non-technical people for a living — say so.</div>"
  },
  {
    "id": "st-13",
    "num": "13",
    "title": "13 · Navigation & deep linking",
    "html": "<p><b>Core:</b> navigation is a tree of navigators — <b>stack</b> (push/pop), <b>tabs</b>, and <b>drawer</b> — that you nest\n        to model real apps. Screens receive typed <code>params</code>; <code>useFocusEffect</code> and <code>useIsFocused</code> run\n        work only while a screen is active. <b>Deep links</b> map an incoming URL (<code>myapp://chat/42</code> or an https\n        universal link) to a screen + params via a linking config, so a push notification or email lands the user on the exact\n        screen.</p>\n      <ul>\n        <li>Auth-gated flows: conditionally render the auth stack vs the app stack — never just hide screens.</li>\n        <li>Persisting navigation state across restarts; resetting stacks cleanly after login/logout.</li>\n        <li>Universal / App Links need native setup: associated domains (iOS) + intent filters / <code>assetlinks.json</code> (Android).</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">New concept</span> <b>Expo Router</b> — file-based routing (a file in\n        <code>app/</code> <i>is</i> a route), nested <code>_layout</code> files, <b>typed routes</b>, and <code>&lt;Link prefetch&gt;</code>.\n        It's becoming the default; learn how its file tree compiles down to the same React Navigation tree underneath.</div>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> You shipped <b>universal/deep-link handling</b> on Valt and owned app\n        IDs, associated domains, and notification setup — so URL→screen routing and the native link config are things you've\n        actually wired, not just read about.</div>"
  },
  {
    "id": "st-14",
    "num": "14",
    "title": "14 · Animations & gestures — Reanimated & Gesture Handler",
    "html": "<p><b>Core:</b> the old <code>Animated</code> API drives values from the <b>JS thread</b>, so motion stutters whenever JS is\n        busy. <b>Reanimated</b> fixes this by running animation code in <b>worklets on the UI thread</b> — <code>useSharedValue</code>\n        holds the value, <code>useAnimatedStyle</code> maps it to styles, and <code>withTiming</code>/<code>withSpring</code> animate\n        it at 60fps even while the JS thread is blocked. <b>Gesture Handler</b> processes pan/tap/pinch natively and composes\n        gestures (simultaneous, race, sequence).</p>\n      <ul>\n        <li>Worklets: tiny functions marked to run on the UI thread; hop back with <code>runOnJS</code>, into it with <code>runOnUI</code>.</li>\n        <li><b>Layout Animations</b> — declarative <code>entering</code>/<code>exiting</code>/<code>layout</code> props for enter/exit.</li>\n        <li><code>useDerivedValue</code> for values computed from other shared values <i>without</i> re-rendering React.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">New concept</span> The <b>UI-thread mental model</b>: an animation that never\n        touches React's render is the secret to smoothness. Internalize <i>what runs where</i> — worklet (UI) vs JS — and you can\n        explain jank precisely instead of hand-waving.</div>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> You eliminated <b>re-render flicker and frozen screens</b> on Valt —\n        the same instinct (keep work off the thread that's busy) is exactly why Reanimated's UI-thread model wins. Say it as\n        “I already think in terms of which thread is doing the work.”</div>"
  },
  {
    "id": "st-15",
    "num": "15",
    "title": "15 · Accessibility (a11y)",
    "html": "<p><b>Core:</b> make the UI work with <b>VoiceOver</b> (iOS) and <b>TalkBack</b> (Android). Mark elements\n        <code>accessible</code>, give them an <code>accessibilityRole</code> (“button”, “header”, “image”), a clear\n        <code>accessibilityLabel</code>, and <code>accessibilityState</code> (selected / disabled / checked). Respect <b>Dynamic\n        Type</b> (text scales with the OS font setting), keep touch targets ≥ 44pt, and meet <b>WCAG AA</b> contrast.</p>\n      <ul>\n        <li><code>AccessibilityInfo</code> — detect a running screen reader; <code>announceForAccessibility</code> for live updates.</li>\n        <li>Manage focus after navigation; group related elements; hide purely decorative ones from the reader.</li>\n        <li>Honor <b>reduce-motion</b> for users who disable animations.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">New concept</span> Accessibility is a <b>senior signal</b>, not a nice-to-have —\n        it's tested (accessibility inspector, automated a11y lint) and often a compliance requirement. Being the engineer who bakes\n        it in early reads as craftsmanship.</div>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> Your <b>pixel-perfect, detail-oriented</b> reputation is the bridge:\n        position a11y as that same care extended to users on assistive tech. Even one shipped VoiceOver pass is a strong story.</div>"
  },
  {
    "id": "st-16",
    "num": "16",
    "title": "16 · Mobile security deep-dive",
    "html": "<p><b>Core:</b> the JS bundle ships to the device and can be read — <b>never put secrets in it</b>. Store tokens in the\n        <b>iOS Keychain / Android Keystore</b> (via <code>expo-secure-store</code> or <code>react-native-keychain</code>), <i>not</i>\n        AsyncStorage (plaintext). Handle <b>access/refresh tokens</b> carefully, gate sensitive actions behind biometrics, and use\n        HTTPS everywhere (ATS on iOS). Know <b>certificate pinning</b> (trust only your cert/CA) and its rotation tradeoff.</p>\n      <ul>\n        <li><b>OWASP MASVS / Mobile Top 10</b> — the vocabulary interviewers expect: insecure storage, weak crypto, and so on.</li>\n        <li>Jailbreak/root detection and code obfuscation as defense-in-depth (not silver bullets).</li>\n        <li>Attestation: <b>App Attest</b> (iOS) / <b>Play Integrity</b> (Android) to verify a genuine app and device.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">New concept</span> “Parse the threat model out loud”: secrets-in-bundle, data at\n        rest, data in transit, and device integrity are four distinct buckets. Naming them shows senior security maturity.</div>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> You shipped <b>FaceID/TouchID biometric auth, Google Sign-In, and\n        Salesforce auth</b> on Valt — real auth and sensitive-data flows. You've already made the secure-storage and token\n        decisions; now you can name the framework (Keychain/Keystore, MASVS) around them.</div>"
  },
  {
    "id": "st-17",
    "num": "17",
    "title": "17 · Memory management & leak hunting",
    "html": "<p><b>Core:</b> two memory worlds — the <b>JS heap</b> (managed by Hermes' GC) and <b>native memory</b> (views, images,\n        native modules). The classic RN leaks: event listeners, subscriptions, and timers you never clean up; stale closures that\n        capture large objects; and screens retained by navigation. The fix is disciplined <code>useEffect</code> cleanup,\n        <code>AbortController</code> to cancel in-flight fetches, and removing listeners on unmount.</p>\n      <ul>\n        <li><b>Images</b> are the biggest native memory cost — downsample, cache sensibly, avoid huge bitmaps in lists.</li>\n        <li>Tools: Xcode <b>Instruments</b> (Allocations / Leaks), Android Profiler, and Hermes heap snapshots.</li>\n        <li>Inline objects/functions in render feed churn and can keep references alive longer than expected.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">New concept</span> Leaks show up as <b>slow, cumulative</b> jank and crashes after\n        long sessions — not an immediate bug. You hunt them with a profiler <i>over time</i>, not a single stack trace.</div>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> Your <b>frozen-screen and re-render investigation</b> on Valt is\n        lifecycle/memory discipline, and the <b>Hermes rollout</b> you drove improved GC behavior directly. “I profile memory across\n        a session instead of guessing” extends your signature debugging line.</div>"
  },
  {
    "id": "st-18",
    "num": "18",
    "title": "18 · App startup, bundling & Metro",
    "html": "<p><b>Core:</b> <b>TTI (time-to-interactive)</b> is the metric. At launch the OS inits the native app, then the <b>JS bundle</b>\n        loads and the first screen renders. <b>Metro</b> is RN's bundler; <b>Hermes</b> precompiles JS to <b>bytecode</b> at build\n        time so there's no parse step on-device — faster cold start, lower memory. Cut startup further with <b>inline requires</b> /\n        lazy loading so modules load on first use, not all at boot.</p>\n      <ul>\n        <li>Shrink the bundle: tree-shaking, <code>React.lazy</code> + Suspense for heavy screens, trim/curate dependencies.</li>\n        <li>Optimize assets (image sizes, font subsetting); measure <b>cold</b> vs <b>warm</b> start separately.</li>\n        <li>Splash → first paint: don't block the first frame on network or heavy synchronous work.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">New concept</span> Treat startup as a <b>budget</b>: every eagerly-imported module\n        and synchronous boot task spends milliseconds. Senior teams track TTI like a metric and defend it in code review.</div>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> You <b>drove a Hermes rollout</b> (Bits Kingdom) and led <b>RN 0.59→0.62\n        upgrades</b> — startup, bytecode, and bundle work is literally on your CV. You can speak to before/after launch time from\n        real rollouts.</div>"
  },
  {
    "id": "st-19",
    "num": "19",
    "title": "19 · Advanced TypeScript & runtime validation",
    "html": "<p><b>Core:</b> past the basics, seniors lean on <b>discriminated unions</b> to model state machines (impossible states won't\n        compile), <b>generics</b> for reusable hooks/utilities, utility types (<code>Partial</code>, <code>Pick</code>,\n        <code>Omit</code>, <code>Record</code>), <code>as const</code> for literal inference, and <b>branded types</b> so a\n        <code>UserId</code> can't be passed where an <code>OrgId</code> is expected.</p>\n      <ul>\n        <li>Template-literal &amp; mapped/conditional types for typed routes, event names, and API shapes.</li>\n        <li>Type navigation params and API DTOs end-to-end so the compiler catches shape drift.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">New concept</span> <b>Types are compile-time only</b> — they vanish at runtime, so a\n        bad API response still crashes you. Validate untrusted JSON at the boundary with <b>zod</b> (or io-ts): write the schema\n        once, <i>infer</i> the type from it, and “parse, don't validate.” One schema = a single source of truth for type + check.</div>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> 5 yrs TypeScript, typing <b>DTOs and store slices</b> on Valt — you\n        already prevent whole classes of shape bugs at compile time. Runtime validation (zod at the network edge) is the next layer,\n        and an easy, current thing to add to your vocabulary.</div>"
  },
  {
    "id": "st-20",
    "num": "20",
    "title": "20 · Observability & production health",
    "html": "<p><b>Core:</b> once it ships you run on <b>signals</b>. <b>Sentry / Crashlytics</b> capture crashes; <b>crash-free users %</b>\n        is the headline metric. Upload <b>source maps</b> (Hermes needs its bytecode source maps) so traces symbolicate to real\n        files/lines. <b>Breadcrumbs</b> reconstruct the steps before a crash, and performance monitoring surfaces slow renders, slow\n        screens, and ANRs.</p>\n      <ul>\n        <li>Release health: watch a new version's crash rate and <b>halt or roll back</b> on a spike.</li>\n        <li><b>OTA updates</b> (EAS Update / CodePush) — track adoption, keep a rollback path, and remember OTA is JS-only.</li>\n        <li>Tie dashboards/alerts to guardrail metrics so regressions page you, not your users.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">New concept</span> <b>Release-health gating</b>: a deploy isn't “done” at submit —\n        it's done when the crash-free rate holds across the rollout. Treating monitoring as part of shipping is a senior tell.</div>\n      <div class=\"map\"><span class=\"lbl\">Your proof</span> You <b>owned production releases and App Store review responses</b> on\n        Valt and used <b>App Center, analytics (GA), and OneSignal</b> — plus EAS for CD. Your “ship, measure, iterate” style with\n        crash-free rate as the guardrail is exactly this card.</div>"
  }
];
