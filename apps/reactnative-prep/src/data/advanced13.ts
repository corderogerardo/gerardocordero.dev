// Batch 13 — Reanimated worklets & shared values, Gesture Handler composition, layout animations, and the UI thread vs JS thread execution model.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED13_FLASHCARDS: Flashcard[] = [
  {
    id: "a13-animations-1",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What exactly is a Reanimated "worklet," and what does the Babel plugin do to make \`runOnUI\`/gesture callbacks work?`,
    answerHtml: `<p>A worklet is a small, self-contained JS function that can be serialized, shipped to a separate JS runtime running on the <b>UI thread</b>, and executed there independently of the React Native (JS-thread) runtime. The Reanimated/Worklets Babel plugin walks the AST of any function marked (implicitly, e.g. inside <code>useAnimatedStyle</code>, or explicitly via a <code>'worklet'</code> directive) and stringifies its source, statically analyzes which outer-scope variables it references, and attaches that captured closure plus a hash as metadata (<code>.__closure</code>, <code>.__workletHash</code>) on the function object. At runtime that serialized form is compiled into a real function inside the UI-thread JS runtime, so the same logic exists as two separate function instances in two separate JS VMs.</p>`,
  },
  {
    id: "a13-animations-2",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why can't a worklet just import and call an arbitrary function from a third-party JS library?`,
    answerHtml: `<p>Because the Babel worklet transform only rewrites functions it can statically see and convert to a worklet at compile time (the worklet itself, plus any function explicitly marked with <code>'worklet'</code> or wrapped to be called from one). An imported library function wasn't compiled with the worklet directive, so it only exists in the JS-thread runtime — calling it from the UI thread throws because it's <code>undefined</code> there. You either need the library to ship pre-marked worklets, manually copy the logic as a worklet, or hop back to the JS thread with <code>runOnJS</code> to call it.</p>`,
  },
  {
    id: "a13-animations-3",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How does a SharedValue (\`useSharedValue\`) actually share state between the JS and UI threads — is it really "shared memory"?`,
    answerHtml: `<p>Not literally shared memory in the C++ sense for the JS-thread side, but conceptually close: a SharedValue is backed by a native object accessible via JSI from both runtimes. Reading/writing <code>.value</code> from a worklet on the UI thread touches the native value directly and synchronously, with no serialization. Reading/writing it from the JS thread goes through a JSI host object too, but cross-thread mutation triggers a microtask-scheduled synchronization so the UI-thread copy stays consistent — this is why a SharedValue mutation made on the JS thread isn't guaranteed to be visible on the UI thread within the same synchronous tick.</p>`,
  },
  {
    id: "a13-animations-4",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why does mutating \`sharedValue.value\` inside a React component's render body (not an event handler or effect) cause problems?`,
    answerHtml: `<p>Reading <code>.value</code> during render does <i>not</i> subscribe the component to re-render on future changes (SharedValues intentionally bypass React's reactivity to avoid re-renders), so the rendered output silently goes stale. Writing <code>.value</code> during render is worse: it mutates external state as a side effect of a function that's supposed to be pure, which can run multiple times under React's concurrent rendering / StrictMode double-invoke, corrupting the animation's state. Value reads/writes belong in worklets, event handlers, or <code>useAnimatedReaction</code>/<code>useDerivedValue</code>, never directly in JSX-producing code.</p>`,
  },
  {
    id: "a13-animations-5",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What's the difference between \`useDerivedValue\` and \`useAnimatedReaction\`, and when would you reach for the latter?`,
    answerHtml: `<p><code>useDerivedValue</code> computes and returns a new SharedValue purely as a function of other SharedValues — it's for producing a value, and Reanimated tracks the dependency automatically by which <code>.value</code>s you read inside it. <code>useAnimatedReaction</code> takes a "prepare" worklet (what to watch) and a "react" worklet (a side effect to run when the watched value changes, receiving previous and current values) — it doesn't return anything; it's for imperative side effects on the UI thread (e.g. triggering haptics, calling <code>runOnJS</code> to sync to JS state, conditionally starting another animation) rather than producing a derived value.</p>`,
  },
  {
    id: "a13-animations-6",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why is \`runOnJS\` necessary, and what are its actual delivery-timing guarantees?`,
    answerHtml: `<p>A worklet running in the UI-thread JS runtime can't directly call back into the React/JS-thread runtime — they're separate JSI runtimes with no shared call stack. <code>runOnJS(fn)(args)</code> serializes the call and schedules it onto the JS thread's event loop as a discrete async task. It is <i>not</i> synchronous and not guaranteed to run before the next frame — under JS-thread congestion (e.g. a long synchronous render) calls can queue up and arrive bursty or delayed, which is why driving high-frequency state (like a scroll-linked counter) through <code>runOnJS</code> on every frame is an anti-pattern; throttling or keeping the value on the UI thread is preferred.</p>`,
  },
  {
    id: "a13-animations-7",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Contrast \`runOnUI\` and \`runOnJS\` — what direction does each cross, and what's the practical use case for \`runOnUI\`?`,
    answerHtml: `<p><code>runOnJS</code> goes UI thread → JS thread (call from inside a worklet to reach React state, native modules, or any non-worklet JS). <code>runOnUI</code> goes JS thread → UI thread: it schedules a worklet to run on the UI thread from regular JS-thread code, e.g. kicking off an imperative animation from a button's <code>onPress</code> handler without going through a gesture or a hook. It's the escape hatch for "I'm on the JS thread right now but I want this specific worklet logic to execute on the UI thread immediately."</p>`,
  },
  {
    id: "a13-animations-8",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Describe the gesture activation state machine in Gesture Handler — what are the key states a \`Gesture.Pan()\` cycles through?`,
    answerHtml: `<p>Each gesture instance is a native recognizer with states: <code>UNDETERMINED</code> → <code>BEGAN</code> (touch down, gesture is being evaluated) → either <code>FAILED</code>/<code>CANCELLED</code> (didn't meet activation criteria, e.g. didn't move far enough) or <code>ACTIVE</code> (criteria met, e.g. <code>minDistance</code> exceeded) → <code>END</code> (finger lifted while active) or <code>CANCELLED</code> mid-flight. Callbacks like <code>.onBegin</code>, <code>.onStart</code>, <code>.onUpdate</code>, <code>.onEnd</code>, <code>.onFinalize</code> map to these transitions, and understanding this machine is essential for composing gestures correctly, since composition operators arbitrate based on which state each underlying recognizer reaches first.</p>`,
  },
  {
    id: "a13-animations-9",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What's the difference between \`Gesture.Simultaneous\`, \`Gesture.Race\`, and \`Gesture.Exclusive\`?`,
    answerHtml: `<ul><li><b>Simultaneous</b>: all listed gestures are allowed to recognize and activate concurrently (e.g. pinch + rotate together for a photo viewer).</li><li><b>Race</b>: all gestures begin evaluating together, but the first one to reach <code>ACTIVE</code> wins and the others are forced to <code>FAIL</code>/<code>CANCELLED</code> — used when gestures are mutually exclusive but you don't want to declare priority up front (e.g. a horizontal pan vs. a long press).</li><li><b>Exclusive</b>: explicit priority order — the first gesture in the list gets first refusal; only if it fails does the next one get a chance to activate. Used when you want a deterministic winner regardless of timing (e.g. a double-tap should win over a single-tap).</li></ul>`,
  },
  {
    id: "a13-animations-10",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How does \`simultaneousWithExternalGesture\` differ from composing two gestures with \`Gesture.Simultaneous()\`, and when do you need it instead?`,
    answerHtml: `<p><code>Gesture.Simultaneous(a, b)</code> creates a new composed gesture object that must be attached as a single unit to one <code>GestureDetector</code>. <code>a.simultaneousWithExternalGesture(b)</code> lets two gestures that live on <i>separate</i> <code>GestureDetector</code>s (e.g. a pan on a parent ScrollView and a pan on a nested draggable child, or a gesture defined in a different component tree) cooperate instead of one cancelling the other — necessary because, by default, two independent unrelated detectors compete and the first to claim the touch blocks the other; you can't pass cross-tree gestures into the composition functions directly.</p>`,
  },
  {
    id: "a13-animations-11",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why does nesting a \`Gesture.Pan()\` inside a horizontal \`ScrollView\` (or \`FlatList\`) often fail to work as expected, and how do you fix it?`,
    answerHtml: `<p>RN's built-in ScrollView uses its own native gesture responder system (not Gesture Handler's), and Gesture Handler's recognizers compete with it via the platform's native gesture-disambiguation rules (UIKit's gesture recognizer delegate on iOS, ViewGroup's touch interception on Android), not React Native's JS-level responder system. The pan gesture typically needs <code>.blocksExternalGesture()</code>/<code>.requireExternalGestureToFail()</code>, or the ScrollView must be swapped for Gesture Handler's own <code>ScrollView</code> wrapper (which exposes a real, composable native gesture) so directional ownership (horizontal pan vs. vertical scroll) can be arbitrated explicitly via <code>activeOffsetX</code>/<code>failOffsetY</code>.</p>`,
  },
  {
    id: "a13-animations-12",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What do \`activeOffsetX\` and \`failOffsetY\` (on \`Gesture.Pan()\`) actually control, and why are both often set together?`,
    answerHtml: `<p><code>activeOffsetX([-N, N])</code> says "only transition to ACTIVE once horizontal movement exceeds N px in either direction"; <code>failOffsetY([-N, N])</code> says "immediately fail this gesture if vertical movement exceeds N px before it activates." Setting both together is how you build directional locking for a swipeable row inside a vertical list: small vertical movement during the initial touch doesn't accidentally trigger the horizontal swipe, and once the list's vertical scroll starts moving, the pan gesture bails out instead of fighting the scroll view for the touch.</p>`,
  },
  {
    id: "a13-animations-13",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How do \`withTiming\`, \`withSpring\`, and \`withDecay\` differ in what input they take and what they're best suited for?`,
    answerHtml: `<ul><li><b>withTiming</b>: duration-based, Bezier-easing curve to an explicit target value — predictable, fixed-length animations (modal fade, fixed-distance slide).</li><li><b>withSpring</b>: physics-based (mass/damping/stiffness or duration+bounce in newer APIs), velocity-aware — natural for gesture-driven release animations because it can take the gesture's release velocity as its initial velocity, producing a continuous, non-jarring handoff from finger-tracking to settle.</li><li><b>withDecay</b>: pure physics-based deceleration with no fixed target, governed by initial velocity and a deceleration/clamp range — used for momentum scrolling/flinging where the end position is a consequence of the physics, not chosen up front.</li></ul>`,
  },
  {
    id: "a13-animations-14",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What does \`withSequence\` guarantee about interruption that chaining \`.then()\`-style imperative calls wouldn't?`,
    answerHtml: `<p><code>withSequence(anim1, anim2, ...)</code> compiles the whole chain into a single animation descriptor that's assigned to the shared value atomically — the UI thread owns and steps through the entire sequence per-frame without round-tripping to JS between steps, so it can't be desynced by JS-thread jank between stages. If you instead used \`setTimeout\`/callback chaining from JS to kick off each successive \`withTiming\`, each restart is a separate cross-thread call subject to JS-thread scheduling delay, and a single dropped/delayed JS tick visibly staggers the sequence.</p>`,
  },
  {
    id: "a13-animations-15",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `When using \`useAnimatedStyle\`, why must you avoid conditionally reading a SharedValue (e.g. only reading \`.value\` inside an \`if\` branch that's sometimes skipped)?`,
    answerHtml: `<p>Reanimated determines which SharedValues a derived computation (\`useAnimatedStyle\`, \`useDerivedValue\`, \`useAnimatedReaction\`'s prepare function) depends on by recording every <code>.value</code> access during execution — it's dependency tracking by execution trace, not static analysis of your code. If a branch that reads a particular SharedValue doesn't execute on a given run, that value isn't registered as a dependency for that run, so a later change to it might not trigger a re-evaluation, producing stale styles. The safe pattern is to always read every SharedValue the worklet might need unconditionally before branching on it.</p>`,
  },
  {
    id: "a13-animations-16",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What problem does \`useAnimatedScrollHandler\` solve that a regular \`onScroll\` prop on \`ScrollView\` can't?`,
    answerHtml: `<p>RN's standard <code>onScroll</code> callback is a JS-thread event, throttled by <code>scrollEventThrottle</code> and delivered asynchronously across the bridge/JSI boundary with JS-thread scheduling latency — unusable for driving 60-120fps parallax or sticky-header effects without visible lag. <code>useAnimatedScrollHandler</code> attaches a native event handler that runs as a worklet directly on the UI thread for every native scroll event, writing straight into a SharedValue with no JS-thread round trip, so dependent <code>useAnimatedStyle</code> transforms update in the same frame as the scroll.</p>`,
  },
  {
    id: "a13-animations-17",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What is \`entering\`/\`exiting\` layout animation in Reanimated, and what's the key constraint on how it composes with the component's actual layout?`,
    answerHtml: `<p><code>entering</code>/<code>exiting</code> props (e.g. <code>FadeIn</code>, <code>SlideInRight</code>, or a custom builder) run a UI-thread animation when a component mounts/unmounts inside a layout-animation-aware tree, independent of React's commit timing — the unmounting component is actually kept alive natively until the exit animation finishes, then removed. The constraint: these are layout-level transforms applied around the component's already-rendered subtree, so they animate position/opacity/scale of the whole view as a unit; they can't independently animate two unrelated style properties on the same node with different curves the way \`useAnimatedStyle\` can, and exit animations require the parent not to skip the native removal hook (e.g. some list virtualization paths).</p>`,
  },
  {
    id: "a13-animations-18",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How does \`Layout\` animation (e.g. \`LinearTransition\`) differ in mechanism from \`entering\`/\`exiting\`?`,
    answerHtml: `<p><code>entering</code>/<code>exiting</code> animate a component's mount/unmount transition (its appearance/disappearance). The layout-transition prop (e.g. <code>layout={LinearTransition}</code>) instead detects when a still-mounted component's position or size changes between renders (because a sibling was added/removed, a flex property changed, content reflowed) and automatically interpolates from the old layout frame to the new one, rather than the component silently snapping to its new spot. Internally Reanimated's native layout-animation manager diffs the before/after layout metrics and hands the UI thread an animation between those two rects.</p>`,
  },
  {
    id: "a13-animations-19",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Are Reanimated's layout animations (entering/exiting/layout transitions) a New Architecture (Fabric)-only feature? What's the real history?`,
    answerHtml: `<p>No — it's the opposite of "Fabric-only by design." \`entering\`/\`exiting\` layout animations shipped in <b>Reanimated 2</b> (2021) as Reanimated's own native implementation (distinct from, and an alternative to, RN core's older built-in \`LayoutAnimation\` module) and originally worked on the <b>legacy/old architecture</b>. When Fabric/New Architecture rolled out, layout animations actually had compatibility gaps for a while (tracked in multiple Reanimated GitHub issues through 2024) before reaching parity. The only place "New Architecture only" is true is at the library-version level: <b>Reanimated 4</b> (2025+) dropped old-architecture support entirely for everything it does, not specifically because layout animations have an architectural dependency on Fabric.</p>`,
  },
  {
    id: "a13-animations-20",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Explain the practical difference between the UI thread and the "JS thread" in a New Architecture RN app — are they still literally two OS threads, and how does that change with Reanimated's involvement?`,
    answerHtml: `<p>Yes — the JS thread runs your React component tree's render/effects/event-handling logic in one Hermes runtime instance; the UI thread (the platform's main thread) handles native view layout/drawing and, with Reanimated, also hosts a <i>second</i> JS runtime instance dedicated to executing worklets. Fabric's synchronous-layout-effects capability lets some work that used to require a JS-thread round trip happen directly on the UI thread, but React's actual JS execution still happens off the main thread — Reanimated's trick is giving the UI thread its own independent, lightweight JS runtime so animation logic specifically can run there without contending with React's JS thread or blocking rendering.</p>`,
  },
  {
    id: "a13-animations-21",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why is computing an expensive derived value (e.g. parsing a large JSON-driven layout) inside a \`useAnimatedStyle\` worklet dangerous, even though it "runs on the UI thread"?`,
    answerHtml: `<p>The UI thread is still a single thread shared with native rendering/compositing — running heavy synchronous work inside a worklet blocks the same thread that's supposed to draw the next frame, causing dropped frames just as surely as blocking the JS thread would stall React. "Runs on the UI thread" buys isolation from JS-thread congestion, not a free lane; worklets still need to be cheap, per-frame-budget operations (interpolation, simple math), with heavy one-off computation done on the JS thread and only the result handed to the UI thread via a SharedValue.</p>`,
  },
  {
    id: "a13-animations-22",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What does \`measure()\` inside a worklet do, and why is it riskier than reading a SharedValue?`,
    answerHtml: `<p><code>measure(animatedRef)</code> synchronously queries the current native layout frame (x, y, width, height, page coordinates) of a view tagged with <code>useAnimatedRef</code>, directly on the UI thread — useful for gesture math that needs a target's real on-screen bounds (e.g. drag-and-drop drop-zone detection). It's riskier than a SharedValue read because it can return <code>null</code> (e.g. before the first layout commit, or if the view was unmounted) and forces a synchronous layout query rather than reading a cached value, so it must be null-checked every call and used sparingly since repeated synchronous layout queries during a high-frequency gesture have a real perf cost.</p>`,
  },
  {
    id: "a13-animations-23",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How does \`Gesture.Manual()\` differ from the higher-level gesture types like \`Gesture.Pan()\`, and when would a senior engineer reach for it?`,
    answerHtml: `<p>The built-in gesture types (Pan, Tap, Pinch, LongPress, etc.) each encode a fixed, platform-tuned activation policy (distance thresholds, timing windows) you only get to tweak via exposed config props. <code>Gesture.Manual()</code> gives you the raw touch stream (\`onTouchesDown\`, \`onTouchesMove\`, \`onTouchesUp\`, \`onTouchesCancelled\`) with full control over when you call \`state.activate()\`/\`state.fail()\`/\`state.end()\` yourself — used for bespoke recognizers that don't fit any built-in shape, e.g. a custom multi-finger gesture with non-standard activation rules, or reimplementing a recognizer that needs to inspect touch identity/order that the standard gestures don't expose.</p>`,
  },
  {
    id: "a13-animations-24",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `When composing a pinch-to-zoom + pan gesture, why is it usually wrong to track absolute scale/translation directly in \`onUpdate\`, and what pattern avoids the jump?`,
    answerHtml: `<p>\`onUpdate\`'s event payload (e.g. \`event.scale\`, \`event.translationX\`) is relative to the gesture's <i>own</i> start, reset to a baseline (1 for scale, 0 for translation) every time the gesture restarts (e.g. lifting and re-placing a second finger). Writing that raw value directly into the shared scale/translation SharedValue overwrites whatever value was accumulated before, causing a visible jump back to the baseline. The standard pattern is to keep a separate "saved" SharedValue snapshotting the value at gesture start (\`onStart: savedScale.value = scale.value\`) and compute the live value as \`savedScale.value * event.scale\` (or \`+\` for translation) inside \`onUpdate\`, so each new gesture continues from where the last one left off.</p>`,
  },
];

export const ADVANCED13_QUIZ: QuizQuestion[] = [
  {
    id: "a13q-animations-1",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `A worklet running on the UI thread needs to update a piece of React component state (via a \`useState\` setter) when a gesture ends. What is the correct mechanism, and what is its delivery guarantee?`,
    options: [`Call the setState function directly from the worklet; Reanimated proxies all function calls automatically`, `Wrap the call in runOnJS(setStateFn)(value); it schedules an async call onto the JS thread's queue, not a synchronous same-frame update`, `Assign to a SharedValue and React will automatically re-render because SharedValues are reactive like useState`, `Use useAnimatedReaction's prepare function to call setState directly since it already runs on the UI thread`],
    answer: 1,
    explanationHtml: `<p><code>runOnJS</code> is required because the UI-thread worklet runtime has no direct call path into the JS-thread React runtime. It schedules the call asynchronously onto the JS thread's queue — it is not synchronous and not guaranteed to land within the same frame, especially under JS-thread congestion. SharedValues are explicitly non-reactive (no re-render on \`.value\` change) by design, which is what makes them cheap to mutate at 60-120fps.</p>`,
  },
  {
    id: "a13q-animations-2",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `You nest a Gesture Handler \`Gesture.Pan()\` for a swipeable list row inside a vertical FlatList. Vertical scrolling fights with the swipe gesture. Which configuration correctly establishes directional ownership?`,
    options: [`Set simultaneousWithExternalGesture on the pan so both can run at once`, `Wrap the pan in Gesture.Race() with no further config and let the faster one win`, `Set activeOffsetX on the pan to require horizontal movement to activate, and failOffsetY so vertical movement before activation fails the pan, letting the list's scroll take over`, `Increase the pan's minPointers to 2 so a single-finger scroll never triggers it`],
    answer: 2,
    explanationHtml: `<p><code>activeOffsetX([-N, N])</code> requires meaningful horizontal movement before the pan activates, and <code>failOffsetY([-N, N])</code> immediately bails the pan out if the touch moves vertically first — together this gives the standard directional-lock pattern so a vertical drag is ceded to the list's native scroll while a horizontal drag is claimed by the row's swipe gesture. Making both gestures simultaneous would make them fight rather than resolve ownership, and bumping minPointers changes finger count, not direction.</p>`,
  },
  {
    id: "a13q-animations-3",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why does writing to \`sharedValue.value\` inside a React component's render function (outside any worklet, hook, or event handler) cause subtle bugs rather than an immediate crash?`,
    options: [`It crashes the UI thread runtime immediately because render is not worklet-compiled`, `It silently mutates external state as a side effect of a function React may invoke multiple times (e.g. under StrictMode or concurrent rendering), and reading .value during render doesn't subscribe to future updates, so the UI can go stale or get corrupted on repeated renders`, `SharedValues are immutable once created, so the assignment is a no-op`, `It throws a TypeError because .value is read-only outside useAnimatedStyle`],
    answer: 1,
    explanationHtml: `<p>Render functions in React can run more than once for the same commit (StrictMode double-invoke, concurrent rendering interruptions), so mutating external state there isn't a clean single side effect — it can run extra times and corrupt accumulated state. Separately, reading \`.value\` during render does not establish a subscription (SharedValues deliberately bypass React's re-render mechanism), so even a correct read goes stale without the component knowing to re-render.</p>`,
  },
  {
    id: "a13q-animations-4",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What is the actual history of Reanimated's \`entering\`/\`exiting\` layout animations (FadeOut, SlideOutLeft, etc.) with respect to React Native's New Architecture (Fabric)?`,
    options: [`They are a Fabric-only feature that has never worked on the legacy architecture`, `They shipped in Reanimated 2 using Reanimated's own native implementation and originally worked on the legacy/old architecture; Fabric support came later and had compatibility gaps for a while before reaching parity`, `They are implemented entirely on top of React Native core's built-in LayoutAnimation module, just with a worklet wrapper`, `Layout animations require TurboModules specifically, independent of Fabric or the legacy renderer`],
    answer: 1,
    explanationHtml: `<p>\`entering\`/\`exiting\` layout animations were introduced in Reanimated 2 (2021) as Reanimated's own native implementation — distinct from RN core's older \`LayoutAnimation\` API — and originally targeted the legacy/old architecture, where they worked. When Fabric arrived, layout animations actually had known compatibility issues for a period (tracked in Reanimated's GitHub issues into 2024) before full support landed. Reanimated 4 (2025+) later dropped old-architecture support for the library as a whole, which is a version-level decision, not evidence that layout animations were architecturally tied to Fabric from the start.</p>`,
  },
  {
    id: "a13q-animations-5",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `During a pinch-to-zoom gesture built with Gesture.Pinch(), you write \`scale.value = event.scale\` directly inside \`onUpdate\`. What visible bug does this produce when the user lifts and re-places fingers mid-zoom (restarting the gesture), and why?`,
    options: [`No bug — event.scale is always cumulative across gesture restarts`, `The zoom level visibly jumps back toward 1, because event.scale resets to a baseline of 1 at the start of each new gesture instance, overwriting the previously accumulated scale instead of continuing from it`, `The app crashes because scale.value can't be reassigned inside onUpdate`, `The gesture stops firing onUpdate entirely after the first restart`],
    answer: 1,
    explanationHtml: `<p><code>event.scale</code> (like \`event.translationX/Y\`) is relative to that particular gesture instance's start, not cumulative across restarts. Assigning it directly overwrites any previously accumulated value, snapping back to baseline. The fix is tracking a separate "saved" SharedValue captured in \`onStart\` and multiplying/adding the live event delta onto it in \`onUpdate\`.</p>`,
  },
  {
    id: "a13q-animations-6",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why is \`useAnimatedScrollHandler\` able to drive a parallax header transform with no visible lag, while a plain \`ScrollView onScroll\` prop driving the same transform via \`setState\` typically cannot keep up at 60-120fps?`,
    options: [`useAnimatedScrollHandler polls scroll position from a native timer instead of listening to events`, `onScroll events are throttled and delivered to the JS thread asynchronously, then a setState triggers a full React re-render each time; useAnimatedScrollHandler instead runs as a worklet directly on the UI thread per native scroll event, writing straight into a SharedValue with no JS round trip or re-render`, `ScrollView's onScroll is deprecated and no longer fires on the New Architecture`, `useAnimatedScrollHandler disables the bounce/overscroll effect, which is what causes the lag in the plain version`],
    answer: 1,
    explanationHtml: `<p>The plain \`onScroll\` path is JS-thread-bound: throttled delivery, async dispatch, and (if used to drive a transform via setState) a full re-render per update — all subject to JS-thread congestion. \`useAnimatedScrollHandler\` registers a worklet that the native scroll event invokes directly on the UI thread, writing into a SharedValue that \`useAnimatedStyle\` reads in the same frame, with no cross-thread hop or React re-render in the loop.</p>`,
  },
  {
    id: "a13q-animations-7",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `A worklet inside useAnimatedStyle does heavy synchronous computation (e.g. parsing a large array) on every frame. What is the correct mental model for why this still causes dropped frames, despite running on the "UI thread"?`,
    options: [`Worklets run on a dedicated background thread separate from rendering, so heavy computation there should never affect frame rate`, `The UI thread is the same single OS thread that handles native layout and compositing/drawing; blocking it with heavy synchronous worklet work delays the next frame's draw just as blocking the JS thread would stall React — "runs on the UI thread" isolates it from JS congestion, it doesn't grant unlimited per-frame budget`, `Heavy computation in a worklet is automatically moved to a Web Worker-equivalent thread by Reanimated, so the slowdown must come from a different cause like GC pauses`, `useAnimatedStyle worklets are re-compiled by Babel on every frame, which is the real source of the slowdown`],
    answer: 1,
    explanationHtml: `<p>The UI thread hosts both the worklet runtime and the platform's native rendering/compositing pipeline. A worklet that does expensive synchronous work eats into the same per-frame budget that drawing needs, so it drops frames just like blocking the JS thread stalls React — the isolation Reanimated provides is from JS-thread contention, not from rendering itself. Heavy one-off work should be done off the per-frame path (JS thread or precomputed) and only the result fed in via a SharedValue.</p>`,
  },
];
