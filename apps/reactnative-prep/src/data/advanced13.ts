// Batch 13 — Reanimated worklets & shared values, Gesture Handler composition, layout animations, and the UI thread vs JS thread execution model.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED13_FLASHCARDS: Flashcard[] = [
  {
    id: "a13-animations-1",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What exactly is a Reanimated "worklet," and what does the Babel plugin do to make \`runOnUI\`/gesture callbacks work?`,
    answerHtml: `<p>Worklets exist so gesture and animation logic can run on the UI thread at 60-120fps without a JS-thread round trip on every frame — that's the entire performance case for Reanimated over driving animations from React state. Mechanically, a worklet is a small, self-contained JS function that can be serialized, shipped to a separate JS runtime running on the <b>UI thread</b>, and executed there independently of the React Native (JS-thread) runtime. The Reanimated/Worklets Babel plugin walks the AST of any function marked (implicitly, e.g. inside <code>useAnimatedStyle</code>, or explicitly via a <code>'worklet'</code> directive) and stringifies its source, statically analyzes which outer-scope variables it references, and attaches that captured closure plus a hash as metadata (<code>.__closure</code>, <code>.__workletHash</code>) on the function object. At runtime that serialized form is compiled into a real function inside the UI-thread JS runtime, so the same logic exists as two separate function instances in two separate JS VMs.</p><p><b>A worklet is a plain JS function statically compiled and shipped to a second JS runtime on the UI thread, so animation logic runs independently of React's JS thread.</b></p>`,
  },
  {
    id: "a13-animations-2",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why can't a worklet just import and call an arbitrary function from a third-party JS library?`,
    answerHtml: `<p>Because the Babel worklet transform only rewrites functions it can statically see and convert to a worklet at compile time (the worklet itself, plus any function explicitly marked with <code>'worklet'</code> or wrapped to be called from one). An imported library function wasn't compiled with the worklet directive, so it only exists in the JS-thread runtime — calling it from the UI thread throws because it's <code>undefined</code> there. You either need the library to ship pre-marked worklets, manually copy the logic as a worklet, or hop back to the JS thread with <code>runOnJS</code> to call it.</p><p><b>Worklet compilation is static, not runtime magic — a function only runs on the UI thread if the Babel plugin saw and transformed it, so uncompiled third-party code is undefined there.</b></p>`,
  },
  {
    id: "a13-animations-3",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How does a SharedValue (\`useSharedValue\`) actually share state between the JS and UI threads — is it really "shared memory"?`,
    answerHtml: `<p>SharedValues exist so state that changes at 60-120fps — drag position, scroll offset, gesture scale — can be read and written without going through React's re-render cycle or the JS↔native bridge on every frame. Not literally shared memory in the C++ sense for the JS-thread side, but conceptually close: a SharedValue is backed by a native object accessible via JSI from both runtimes. Reading/writing <code>.value</code> from a worklet on the UI thread touches the native value directly and synchronously, with no serialization. Reading/writing it from the JS thread goes through a JSI host object too, but cross-thread mutation triggers a microtask-scheduled synchronization so the UI-thread copy stays consistent — this is why a SharedValue mutation made on the JS thread isn't guaranteed to be visible on the UI thread within the same synchronous tick.</p><p><b>A SharedValue isn't shared memory, it's a JSI-backed object both runtimes can reach — UI-thread access is synchronous, JS-thread access is synchronized via a microtask, so cross-thread visibility isn't instant.</b></p>`,
  },
  {
    id: "a13-animations-4",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why does mutating \`sharedValue.value\` inside a React component's render body (not an event handler or effect) cause problems?`,
    answerHtml: `<p>Treating a SharedValue like component state and touching <code>.value</code> in the render body breaks React's core contract that render is a pure, repeatable function of props/state — and it breaks silently rather than loudly. Reading <code>.value</code> during render does <i>not</i> subscribe the component to re-render on future changes (SharedValues intentionally bypass React's reactivity to avoid re-renders), so the rendered output silently goes stale. Writing <code>.value</code> during render is worse: it mutates external state as a side effect of a function that's supposed to be pure, which can run multiple times under React's concurrent rendering / StrictMode double-invoke, corrupting the animation's state. Value reads/writes belong in worklets, event handlers, or <code>useAnimatedReaction</code>/<code>useDerivedValue</code>, never directly in JSX-producing code.</p><p><b>Red flag:</b> reaching for a SharedValue read/write inside the render body because "it's just a value" — SharedValues deliberately opt out of React's reactivity, so render is the one place they don't belong.</p><p><b>SharedValue reads and writes live in worklets, event handlers, or derived-value hooks — never in the render body, because render must stay pure and SharedValues don't trigger re-renders.</b></p>`,
  },
  {
    id: "a13-animations-5",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What's the difference between \`useDerivedValue\` and \`useAnimatedReaction\`, and when would you reach for the latter?`,
    answerHtml: `<p><code>useDerivedValue</code>'s role is producing a value: it computes and returns a new SharedValue purely as a function of other SharedValues, and Reanimated tracks the dependency automatically by which <code>.value</code>s you read inside it. <code>useAnimatedReaction</code>'s role is running a side effect: it takes a "prepare" worklet (what to watch) and a "react" worklet (what to do when the watched value changes, receiving previous and current values) and returns nothing — it's for imperative UI-thread work (triggering haptics, calling <code>runOnJS</code> to sync to JS state, conditionally starting another animation) rather than producing a derived value. Reach for <code>useAnimatedReaction</code> the moment the thing you need isn't "a new value to render" but "do something when this value crosses a threshold."</p><p><b>useDerivedValue produces a value; useAnimatedReaction runs a side effect — if you're not returning something to feed into a style, you want the reaction hook.</b></p>`,
  },
  {
    id: "a13-animations-6",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why is \`runOnJS\` necessary, and what are its actual delivery-timing guarantees?`,
    answerHtml: `<p><code>runOnJS</code> exists because a worklet running in the UI-thread JS runtime can't directly call back into the React/JS-thread runtime — they're separate JSI runtimes with no shared call stack. <code>runOnJS(fn)(args)</code> serializes the call and schedules it onto the JS thread's event loop as a discrete async task. It is <i>not</i> synchronous and not guaranteed to run before the next frame — under JS-thread congestion (e.g. a long synchronous render) calls can queue up and arrive bursty or delayed.</p><p><b>Red flag:</b> driving high-frequency state (a scroll-linked counter, a per-frame position readout) through <code>runOnJS</code> on every frame — say instead that you'd throttle the call or keep the value on the UI thread entirely.</p><p><b>runOnJS schedules an async task on the JS thread's queue, not a synchronous same-frame call — treat every runOnJS call as "eventually, maybe bursty," never as guaranteed-next-frame.</b></p>`,
  },
  {
    id: "a13-animations-7",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Contrast \`runOnUI\` and \`runOnJS\` — what direction does each cross, and what's the practical use case for \`runOnUI\`?`,
    answerHtml: `<p><code>runOnJS</code>'s role is UI thread → JS thread: call it from inside a worklet to reach React state, native modules, or any non-worklet JS. <code>runOnUI</code>'s role is the opposite direction, JS thread → UI thread: it schedules a worklet to run on the UI thread from regular JS-thread code, e.g. kicking off an imperative animation from a button's <code>onPress</code> handler without going through a gesture or a hook. It's the escape hatch for "I'm on the JS thread right now but I want this specific worklet logic to execute on the UI thread immediately."</p><p><b>runOnJS crosses UI-to-JS, runOnUI crosses JS-to-UI — name the direction first and the right function follows.</b></p>`,
  },
  {
    id: "a13-animations-8",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Describe the gesture activation state machine in Gesture Handler — what are the key states a \`Gesture.Pan()\` cycles through?`,
    answerHtml: `<p>Composition operators like <code>Gesture.Race</code>/<code>Gesture.Exclusive</code> arbitrate purely by which recognizer reaches which state first, so you can't reason about gesture composition, conflict resolution, or which callback fires when without this state machine. Each gesture instance is a native recognizer with states: <code>UNDETERMINED</code> → <code>BEGAN</code> (touch down, gesture is being evaluated) → either <code>FAILED</code>/<code>CANCELLED</code> (didn't meet activation criteria, e.g. didn't move far enough) or <code>ACTIVE</code> (criteria met, e.g. <code>minDistance</code> exceeded) → <code>END</code> (finger lifted while active) or <code>CANCELLED</code> mid-flight. Callbacks like <code>.onBegin</code>, <code>.onStart</code>, <code>.onUpdate</code>, <code>.onEnd</code>, <code>.onFinalize</code> map to these transitions.</p><p><b>Every gesture is a recognizer walking UNDETERMINED → BEGAN → ACTIVE/FAILED → END/CANCELLED, and composition operators just race or order recognizers against that same state machine.</b></p>`,
  },
  {
    id: "a13-animations-9",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What's the difference between \`Gesture.Simultaneous\`, \`Gesture.Race\`, and \`Gesture.Exclusive\`?`,
    answerHtml: `<p>Getting gesture composition wrong produces exactly the UX bugs users report as "the app feels broken" — a swipe that also triggers a tap, or a pinch that cancels the pan underneath it — so knowing which operator to reach for is a correctness question, not a style choice.</p><ul><li><b>Simultaneous</b>: all listed gestures are allowed to recognize and activate concurrently (e.g. pinch + rotate together for a photo viewer).</li><li><b>Race</b>: all gestures begin evaluating together, but the first one to reach <code>ACTIVE</code> wins and the others are forced to <code>FAIL</code>/<code>CANCELLED</code> — used when gestures are mutually exclusive but you don't want to declare priority up front (e.g. a horizontal pan vs. a long press).</li><li><b>Exclusive</b>: explicit priority order — the first gesture in the list gets first refusal; only if it fails does the next one get a chance to activate. Used when you want a deterministic winner regardless of timing (e.g. a double-tap should win over a single-tap).</li></ul><p><b>Simultaneous lets gestures coexist, Race lets timing decide the winner, Exclusive lets you decide the winner up front.</b></p>`,
  },
  {
    id: "a13-animations-10",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How does \`simultaneousWithExternalGesture\` differ from composing two gestures with \`Gesture.Simultaneous()\`, and when do you need it instead?`,
    answerHtml: `<p><code>Gesture.Simultaneous(a, b)</code>'s role is composing gestures that live under one <code>GestureDetector</code> into a single attached unit. <code>a.simultaneousWithExternalGesture(b)</code>'s role is coordinating gestures that live on <i>separate</i> detectors entirely (e.g. a pan on a parent ScrollView and a pan on a nested draggable child, or a gesture defined in a different component tree) — necessary because, by default, two independent unrelated detectors compete and the first to claim the touch blocks the other; you can't pass cross-tree gestures into the composition functions directly.</p><p><b>Same detector, same tree: use Gesture.Simultaneous. Different detectors, different trees: use simultaneousWithExternalGesture.</b></p>`,
  },
  {
    id: "a13-animations-11",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why does nesting a \`Gesture.Pan()\` inside a horizontal \`ScrollView\` (or \`FlatList\`) often fail to work as expected, and how do you fix it?`,
    answerHtml: `<p>The root cause is that RN's built-in ScrollView uses its own native gesture responder system, not Gesture Handler's — the two compete via the platform's native gesture-disambiguation rules (UIKit's gesture recognizer delegate on iOS, ViewGroup's touch interception on Android), not React Native's JS-level responder system, so neither side inherently "wins" cleanly. Fixing it is a sequence: 1. Swap the ScrollView for Gesture Handler's own <code>ScrollView</code> wrapper, which exposes a real, composable native gesture instead of the JS-responder one. 2. Give the pan explicit directional criteria via <code>activeOffsetX</code>/<code>failOffsetY</code> so it only claims horizontal movement. 3. If two detectors still fight, arbitrate explicitly with <code>.blocksExternalGesture()</code>/<code>.requireExternalGestureToFail()</code> rather than leaving resolution to timing.</p><p><b>The fix isn't a prop tweak, it's swapping to Gesture Handler's ScrollView so both gestures speak the same native disambiguation protocol.</b></p>`,
  },
  {
    id: "a13-animations-12",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What do \`activeOffsetX\` and \`failOffsetY\` (on \`Gesture.Pan()\`) actually control, and why are both often set together?`,
    answerHtml: `<p><code>activeOffsetX([-N, N])</code> says "only transition to ACTIVE once horizontal movement exceeds N px in either direction"; <code>failOffsetY([-N, N])</code> says "immediately fail this gesture if vertical movement exceeds N px before it activates." Setting both together is how you build directional locking for a swipeable row inside a vertical list: small vertical movement during the initial touch doesn't accidentally trigger the horizontal swipe, and once the list's vertical scroll starts moving, the pan gesture bails out instead of fighting the scroll view for the touch.</p><p><b>activeOffsetX claims the gesture, failOffsetY cedes it — together they're how you build directional locking without a single line of manual arbitration code.</b></p>`,
  },
  {
    id: "a13-animations-13",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How do \`withTiming\`, \`withSpring\`, and \`withDecay\` differ in what input they take and what they're best suited for?`,
    answerHtml: `<p>Picking the wrong animation primitive is why gesture-driven UI feels "off" even when the numbers are right — a timing curve can't take velocity as input, so a released drag animated with withTiming always looks like it stops dead instead of coasting.</p><ul><li><b>withTiming</b>: duration-based, Bezier-easing curve to an explicit target value — predictable, fixed-length animations (modal fade, fixed-distance slide).</li><li><b>withSpring</b>: physics-based (mass/damping/stiffness or duration+bounce in newer APIs), velocity-aware — natural for gesture-driven release animations because it can take the gesture's release velocity as its initial velocity, producing a continuous, non-jarring handoff from finger-tracking to settle.</li><li><b>withDecay</b>: pure physics-based deceleration with no fixed target, governed by initial velocity and a deceleration/clamp range — used for momentum scrolling/flinging where the end position is a consequence of the physics, not chosen up front.</li></ul><p><b>If the target value is fixed, use withTiming; if velocity should carry into the animation, use withSpring; if there's no target at all, use withDecay.</b></p>`,
  },
  {
    id: "a13-animations-14",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What does \`withSequence\` guarantee about interruption that chaining \`.then()\`-style imperative calls wouldn't?`,
    answerHtml: `<p>The guarantee that matters is reliability under load: <code>withSequence(anim1, anim2, ...)</code> compiles the whole chain into a single animation descriptor assigned to the shared value atomically, so the UI thread owns and steps through the entire sequence per-frame without round-tripping to JS between steps — it can't be desynced by JS-thread jank between stages. If you instead used \`setTimeout\`/callback chaining from JS to kick off each successive \`withTiming\`, each restart is a separate cross-thread call subject to JS-thread scheduling delay, and a single dropped/delayed JS tick visibly staggers the sequence.</p><p><b>withSequence is one atomic UI-thread descriptor; JS-side chaining is N separate cross-thread calls, each a chance for jank to show through.</b></p>`,
  },
  {
    id: "a13-animations-15",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `When using \`useAnimatedStyle\`, why must you avoid conditionally reading a SharedValue (e.g. only reading \`.value\` inside an \`if\` branch that's sometimes skipped)?`,
    answerHtml: `<p>Reanimated determines which SharedValues a derived computation (\`useAnimatedStyle\`, \`useDerivedValue\`, \`useAnimatedReaction\`'s prepare function) depends on by recording every <code>.value</code> access during execution — it's dependency tracking by execution trace, not static analysis of your code. If a branch that reads a particular SharedValue doesn't execute on a given run, that value isn't registered as a dependency for that run, so a later change to it might not trigger a re-evaluation, producing stale styles.</p><p><b>Red flag:</b> gating a <code>.value</code> read behind an <code>if</code> the way you would with a normal variable — Reanimated's dependency tracking is trace-based, so a skipped branch means a silently missing dependency. Always read every SharedValue the worklet might need unconditionally, then branch on the read value.</p><p><b>Dependency tracking here is by execution trace, not static analysis — if a branch doesn't run, that value was never registered as a dependency.</b></p>`,
  },
  {
    id: "a13-animations-16",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What problem does \`useAnimatedScrollHandler\` solve that a regular \`onScroll\` prop on \`ScrollView\` can't?`,
    answerHtml: `<p>RN's standard <code>onScroll</code> callback is a JS-thread event, throttled by <code>scrollEventThrottle</code> and delivered asynchronously across the bridge/JSI boundary with JS-thread scheduling latency — unusable for driving 60-120fps parallax or sticky-header effects without visible lag. <code>useAnimatedScrollHandler</code> attaches a native event handler that runs as a worklet directly on the UI thread for every native scroll event, writing straight into a SharedValue with no JS-thread round trip, so dependent <code>useAnimatedStyle</code> transforms update in the same frame as the scroll.</p><p><b>useAnimatedScrollHandler removes the JS-thread hop entirely — the scroll event, the SharedValue write, and the style update all happen on the UI thread in the same frame.</b></p>`,
  },
  {
    id: "a13-animations-17",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What is \`entering\`/\`exiting\` layout animation in Reanimated, and what's the key constraint on how it composes with the component's actual layout?`,
    answerHtml: `<p>These props exist to solve the specific gap of animating a component's disappearance after React has already decided to unmount it — without them, an unmounted component is just gone, no fade, no slide. <code>entering</code>/<code>exiting</code> props (e.g. <code>FadeIn</code>, <code>SlideInRight</code>, or a custom builder) run a UI-thread animation when a component mounts/unmounts inside a layout-animation-aware tree, independent of React's commit timing — the unmounting component is actually kept alive natively until the exit animation finishes, then removed. The constraint: these are layout-level transforms applied around the component's already-rendered subtree, so they animate position/opacity/scale of the whole view as a unit; they can't independently animate two unrelated style properties on the same node with different curves the way \`useAnimatedStyle\` can, and exit animations require the parent not to skip the native removal hook (e.g. some list virtualization paths).</p><p><b>entering/exiting keep an unmounting view alive natively long enough to animate it out — but they animate the whole view as one unit, not individual style properties independently.</b></p>`,
  },
  {
    id: "a13-animations-18",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How does \`Layout\` animation (e.g. \`LinearTransition\`) differ in mechanism from \`entering\`/\`exiting\`?`,
    answerHtml: `<p><code>entering</code>/<code>exiting</code>'s role is the mount/unmount transition — appearance and disappearance. The <code>layout</code> prop's (e.g. <code>layout={LinearTransition}</code>) role is different: it detects when a still-mounted component's position or size changes between renders (because a sibling was added/removed, a flex property changed, content reflowed) and automatically interpolates from the old layout frame to the new one, rather than the component silently snapping to its new spot. Internally Reanimated's native layout-animation manager diffs the before/after layout metrics and hands the UI thread an animation between those two rects.</p><p><b>entering/exiting animate whether a view exists on screen; layout animates where an already-mounted view sits.</b></p>`,
  },
  {
    id: "a13-animations-19",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Are Reanimated's layout animations (entering/exiting/layout transitions) a New Architecture (Fabric)-only feature? What's the real history?`,
    answerHtml: `<p>No — it's the opposite of "Fabric-only by design," and getting this backwards in an interview signals you've absorbed marketing framing rather than the library's actual history.</p><p><b>Red flag:</b> asserting that entering/exiting layout animations are a New-Architecture-only feature — say instead that they predate Fabric and had compatibility gaps <i>during</i> the Fabric transition, not the reverse.</p><p>\`entering\`/\`exiting\` layout animations shipped in <b>Reanimated 2</b> (2021) as Reanimated's own native implementation (distinct from, and an alternative to, RN core's older built-in \`LayoutAnimation\` module) and originally worked on the <b>legacy/old architecture</b>. When Fabric/New Architecture rolled out, layout animations actually had compatibility gaps for a while (tracked in multiple Reanimated GitHub issues through 2024) before reaching parity. The only place "New Architecture only" is true is at the library-version level: <b>Reanimated 4</b> (2025+) dropped old-architecture support entirely for everything it does, not specifically because layout animations have an architectural dependency on Fabric.</p><p><b>Layout animations predate Fabric — Reanimated 4 dropping old-architecture support is a library-version decision, not proof the feature was ever architecturally tied to it.</b></p>`,
  },
  {
    id: "a13-animations-20",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Explain the practical difference between the UI thread and the "JS thread" in a New Architecture RN app — are they still literally two OS threads, and how does that change with Reanimated's involvement?`,
    answerHtml: `<p>Yes — the JS thread runs your React component tree's render/effects/event-handling logic in one Hermes runtime instance; the UI thread (the platform's main thread) handles native view layout/drawing and, with Reanimated, also hosts a <i>second</i> JS runtime instance dedicated to executing worklets. Fabric's synchronous-layout-effects capability lets some work that used to require a JS-thread round trip happen directly on the UI thread, but React's actual JS execution still happens off the main thread — Reanimated's trick is giving the UI thread its own independent, lightweight JS runtime so animation logic specifically can run there without contending with React's JS thread or blocking rendering.</p><p><b>They're still two real OS threads — Reanimated's contribution is a second, lightweight JS runtime living on the UI thread's side of that split, purely for worklets.</b></p>`,
  },
  {
    id: "a13-animations-21",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why is computing an expensive derived value (e.g. parsing a large JSON-driven layout) inside a \`useAnimatedStyle\` worklet dangerous, even though it "runs on the UI thread"?`,
    answerHtml: `<p>The UI thread is still a single thread shared with native rendering/compositing — running heavy synchronous work inside a worklet blocks the same thread that's supposed to draw the next frame, causing dropped frames just as surely as blocking the JS thread would stall React.</p><p><b>Red flag:</b> treating "runs on the UI thread" as a free performance lane — it buys isolation from JS-thread congestion, not unlimited per-frame budget. Worklets still need to be cheap, per-frame-budget operations (interpolation, simple math), with heavy one-off computation done on the JS thread and only the result handed over via a SharedValue.</p><p><b>The UI thread has the same per-frame budget as the JS thread does for rendering — moving work there avoids JS congestion, it doesn't grant unlimited compute.</b></p>`,
  },
  {
    id: "a13-animations-22",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What does \`measure()\` inside a worklet do, and why is it riskier than reading a SharedValue?`,
    answerHtml: `<p><code>measure(animatedRef)</code> synchronously queries the current native layout frame (x, y, width, height, page coordinates) of a view tagged with <code>useAnimatedRef</code>, directly on the UI thread — useful for gesture math that needs a target's real on-screen bounds (e.g. drag-and-drop drop-zone detection). It's riskier than a SharedValue read for two concrete reasons: it can return <code>null</code> (e.g. before the first layout commit, or if the view was unmounted), so it must be null-checked every call; and it forces a synchronous native layout query rather than reading a cached value, so calling it on every frame of a high-frequency gesture has a real perf cost.</p><p><b>measure() is a synchronous native layout query that can return null — treat every call site as needing a null check, and every frame it runs on as a real cost.</b></p>`,
  },
  {
    id: "a13-animations-23",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How does \`Gesture.Manual()\` differ from the higher-level gesture types like \`Gesture.Pan()\`, and when would a senior engineer reach for it?`,
    answerHtml: `<p>The built-in gesture types' role (Pan, Tap, Pinch, LongPress, etc.) is a fixed, platform-tuned activation policy (distance thresholds, timing windows) you only get to tweak via exposed config props. <code>Gesture.Manual()</code>'s role is the opposite end of the spectrum: it gives you the raw touch stream (\`onTouchesDown\`, \`onTouchesMove\`, \`onTouchesUp\`, \`onTouchesCancelled\`) with full control over when you call \`state.activate()\`/\`state.fail()\`/\`state.end()\` yourself — used for bespoke recognizers that don't fit any built-in shape, e.g. a custom multi-finger gesture with non-standard activation rules, or reimplementing a recognizer that needs to inspect touch identity/order that the standard gestures don't expose.</p><p><b>Reach for a built-in gesture type until its config props run out, then drop to Gesture.Manual for full control over the raw touch stream.</b></p>`,
  },
  {
    id: "a13-animations-24",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `When composing a pinch-to-zoom + pan gesture, why is it usually wrong to track absolute scale/translation directly in \`onUpdate\`, and what pattern avoids the jump?`,
    answerHtml: `<p>The root cause: \`onUpdate\`'s event payload (e.g. \`event.scale\`, \`event.translationX\`) is relative to the gesture's <i>own</i> start, reset to a baseline (1 for scale, 0 for translation) every time the gesture restarts (e.g. lifting and re-placing a second finger).</p><p><b>Red flag:</b> writing the raw event value directly into the shared scale/translation SharedValue — it overwrites whatever value was accumulated before, causing a visible jump back to baseline the moment a finger lifts and re-lands.</p><p>The standard pattern: 1. Keep a separate "saved" SharedValue that snapshots the accumulated value. 2. In \`onStart\`, copy the current value in: \`savedScale.value = scale.value\`. 3. In \`onUpdate\`, compute the live value as \`savedScale.value * event.scale\` (or \`+\` for translation), so each new gesture instance continues from where the last one left off instead of resetting.</p><p><b>Never assign a raw gesture-event delta directly to the accumulated SharedValue — multiply or add it onto a "saved" snapshot taken at onStart.</b></p>`,
  },
];

export const ADVANCED13_QUIZ: QuizQuestion[] = [
  {
    id: "a13q-animations-1",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `A worklet running on the UI thread needs to update a piece of React component state (via a \`useState\` setter) when a gesture ends. What is the correct mechanism, and what is its delivery guarantee?`,
    options: [`Call the setState function directly from the worklet; Reanimated proxies all function calls automatically`, `Wrap the call in runOnJS(setStateFn)(value); it schedules an async call onto the JS thread's queue, not a synchronous same-frame update`, `Assign to a SharedValue and React will automatically re-render because SharedValues are reactive like useState`, `Use useAnimatedReaction's prepare function to call setState directly since it already runs on the UI thread`],
    answer: 1,
    explanationHtml: `<p><code>runOnJS</code> is required because the UI-thread worklet runtime has no direct call path into the JS-thread React runtime. It schedules the call asynchronously onto the JS thread's queue — it is not synchronous and not guaranteed to land within the same frame, especially under JS-thread congestion. SharedValues are explicitly non-reactive (no re-render on \`.value\` change) by design, which is what makes them cheap to mutate at 60-120fps. And <code>useAnimatedReaction</code>'s prepare function still runs on the UI thread — it has the identical no-direct-call-path problem as any other worklet, so it needs <code>runOnJS</code> too, not a shortcut.</p>`,
  },
  {
    id: "a13q-animations-2",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `You nest a Gesture Handler \`Gesture.Pan()\` for a swipeable list row inside a vertical FlatList. Vertical scrolling fights with the swipe gesture. Which configuration correctly establishes directional ownership?`,
    options: [`Set simultaneousWithExternalGesture on the pan so both can run at once`, `Wrap the pan in Gesture.Race() with no further config and let the faster one win`, `Set activeOffsetX on the pan to require horizontal movement to activate, and failOffsetY so vertical movement before activation fails the pan, letting the list's scroll take over`, `Increase the pan's minPointers to 2 so a single-finger scroll never triggers it`],
    answer: 2,
    explanationHtml: `<p><code>activeOffsetX([-N, N])</code> requires meaningful horizontal movement before the pan activates, and <code>failOffsetY([-N, N])</code> immediately bails the pan out if the touch moves vertically first — together this gives the standard directional-lock pattern so a vertical drag is ceded to the list's native scroll while a horizontal drag is claimed by the row's swipe gesture. Making both gestures simultaneous would make them fight rather than resolve ownership, and bumping minPointers changes finger count, not direction.</p>`,
  },
  {
    id: "a13q-animations-3",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why does writing to \`sharedValue.value\` inside a React component's render function (outside any worklet, hook, or event handler) cause subtle bugs rather than an immediate crash?`,
    options: [`It crashes the UI thread runtime immediately because render is not worklet-compiled`, `It silently mutates external state as a side effect of a function React may invoke multiple times (e.g. under StrictMode or concurrent rendering), and reading .value during render doesn't subscribe to future updates, so the UI can go stale or get corrupted on repeated renders`, `SharedValues are immutable once created, so the assignment is a no-op`, `It throws a TypeError because .value is read-only outside useAnimatedStyle`],
    answer: 1,
    explanationHtml: `<p>Render functions in React can run more than once for the same commit (StrictMode double-invoke, concurrent rendering interruptions), so mutating external state there isn't a clean single side effect — it can run extra times and corrupt accumulated state. Separately, reading \`.value\` during render does not establish a subscription (SharedValues deliberately bypass React's re-render mechanism), so even a correct read goes stale without the component knowing to re-render. Nothing here throws or no-ops: SharedValues are mutable by design and \`.value\` is writable from any thread, not gated to worklets — so there's no runtime guard to catch the mistake, which is exactly why it's a subtle bug and not a crash.</p>`,
  },
  {
    id: "a13q-animations-4",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What is the actual history of Reanimated's \`entering\`/\`exiting\` layout animations (FadeOut, SlideOutLeft, etc.) with respect to React Native's New Architecture (Fabric)?`,
    options: [`They are a Fabric-only feature that has never worked on the legacy architecture`, `They shipped in Reanimated 2 using Reanimated's own native implementation and originally worked on the legacy/old architecture; Fabric support came later and had compatibility gaps for a while before reaching parity`, `They are implemented entirely on top of React Native core's built-in LayoutAnimation module, just with a worklet wrapper`, `Layout animations require TurboModules specifically, independent of Fabric or the legacy renderer`],
    answer: 1,
    explanationHtml: `<p>\`entering\`/\`exiting\` layout animations were introduced in Reanimated 2 (2021) as Reanimated's own native implementation — distinct from RN core's older \`LayoutAnimation\` API — and originally targeted the legacy/old architecture, where they worked. When Fabric arrived, layout animations actually had known compatibility issues for a period (tracked in Reanimated's GitHub issues into 2024) before full support landed. Reanimated 4 (2025+) later dropped old-architecture support for the library as a whole, which is a version-level decision, not evidence that layout animations were architecturally tied to Fabric from the start. Nor do they have a specific dependency on TurboModules — that's a separate, unrelated axis of the New Architecture.</p>`,
  },
  {
    id: "a13q-animations-5",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `During a pinch-to-zoom gesture built with Gesture.Pinch(), you write \`scale.value = event.scale\` directly inside \`onUpdate\`. What visible bug does this produce when the user lifts and re-places fingers mid-zoom (restarting the gesture), and why?`,
    options: [`No bug — event.scale is always cumulative across gesture restarts`, `The zoom level visibly jumps back toward 1, because event.scale resets to a baseline of 1 at the start of each new gesture instance, overwriting the previously accumulated scale instead of continuing from it`, `The app crashes because scale.value can't be reassigned inside onUpdate`, `The gesture stops firing onUpdate entirely after the first restart`],
    answer: 1,
    explanationHtml: `<p><code>event.scale</code> (like \`event.translationX/Y\`) is relative to that particular gesture instance's start, not cumulative across restarts. Assigning it directly overwrites any previously accumulated value, snapping back to baseline. Nothing here crashes or stops the gesture from firing — the value is simply wrong, which is what makes it a subtle visual bug rather than a hard error. The fix is tracking a separate "saved" SharedValue captured in \`onStart\` and multiplying/adding the live event delta onto it in \`onUpdate\`.</p>`,
  },
  {
    id: "a13q-animations-6",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why is \`useAnimatedScrollHandler\` able to drive a parallax header transform with no visible lag, while a plain \`ScrollView onScroll\` prop driving the same transform via \`setState\` typically cannot keep up at 60-120fps?`,
    options: [`useAnimatedScrollHandler polls scroll position from a native timer instead of listening to events`, `onScroll events are throttled and delivered to the JS thread asynchronously, then a setState triggers a full React re-render each time; useAnimatedScrollHandler instead runs as a worklet directly on the UI thread per native scroll event, writing straight into a SharedValue with no JS round trip or re-render`, `ScrollView's onScroll is deprecated and no longer fires on the New Architecture`, `useAnimatedScrollHandler disables the bounce/overscroll effect, which is what causes the lag in the plain version`],
    answer: 1,
    explanationHtml: `<p>The plain \`onScroll\` path is JS-thread-bound: throttled delivery, async dispatch, and (if used to drive a transform via setState) a full re-render per update — all subject to JS-thread congestion. \`onScroll\` isn't deprecated and fires the same way under the New Architecture; the difference is entirely about which thread processes the event, not event availability. \`useAnimatedScrollHandler\` registers a worklet that the native scroll event invokes directly on the UI thread, writing into a SharedValue that \`useAnimatedStyle\` reads in the same frame, with no cross-thread hop or React re-render in the loop.</p>`,
  },
  {
    id: "a13q-animations-7",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `A worklet inside useAnimatedStyle does heavy synchronous computation (e.g. parsing a large array) on every frame. What is the correct mental model for why this still causes dropped frames, despite running on the "UI thread"?`,
    options: [`Worklets run on a dedicated background thread separate from rendering, so heavy computation there should never affect frame rate`, `The UI thread is the same single OS thread that handles native layout and compositing/drawing; blocking it with heavy synchronous worklet work delays the next frame's draw just as blocking the JS thread would stall React — "runs on the UI thread" isolates it from JS congestion, it doesn't grant unlimited per-frame budget`, `Heavy computation in a worklet is automatically moved to a Web Worker-equivalent thread by Reanimated, so the slowdown must come from a different cause like GC pauses`, `useAnimatedStyle worklets are re-compiled by Babel on every frame, which is the real source of the slowdown`],
    answer: 1,
    explanationHtml: `<p>The UI thread hosts both the worklet runtime and the platform's native rendering/compositing pipeline. A worklet that does expensive synchronous work eats into the same per-frame budget that drawing needs, so it drops frames just like blocking the JS thread stalls React — the isolation Reanimated provides is from JS-thread contention, not from rendering itself. Worklets aren't recompiled on every frame either (the Babel transform runs once at build time), and there's no separate Web-Worker-style background thread involved — the worklet executes synchronously on the very same UI thread that draws frames. Heavy one-off work should be done off the per-frame path (JS thread or precomputed) and only the result fed in via a SharedValue.</p>`,
  },
];
