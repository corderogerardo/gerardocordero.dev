// Batch 20 — Accessibility: screen reader support (accessibilityLabel, accessibilityRole), focus management, dynamic type/font scaling, and accessibility inspector tooling.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED20_FLASHCARDS: Flashcard[] = [
  {
    id: "a20-accessibility-1",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `On iOS, what's the difference between accessibilityLabel and accessibilityHint, and what happens if you stuff both into one accessibilityLabel?`,
    answerHtml: `<p><code>accessibilityLabel</code> answers 'what is this' (read first, every focus). <code>accessibilityHint</code> answers 'what happens if I activate it' (read after the label/role/state, and only on some platforms by default — VoiceOver reads it only if 'Speak Hints' is enabled in VoiceOver's Verbosity settings, which users can turn off; TalkBack on Android always announces hints, with no equivalent toggle). If you concatenate the hint into the label, VoiceOver re-reads the whole blob on <i>every</i> re-focus, including the action description, which gets tedious on repeated navigation and breaks the convention sighted-but-low-vision users rely on (label first, then a pause, then hint).</p><p><b>Red flag:</b> concatenating the hint into the label to 'be safe' — it gets re-read in full on every re-focus and destroys the label-then-pause-then-hint convention users expect.</p><p><b>I keep accessibilityLabel to what the element is and accessibilityHint to what activating it does, and never merge them into one string.</b></p>`,
  },
  {
    id: "a20-accessibility-2",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why does setting accessibilityRole="button" on a custom Pressable sometimes fail to announce 'button' on Android, even though it works on iOS?`,
    answerHtml: `<p>On Android, <code>accessibilityRole</code> maps through <code>AccessibilityNodeInfo.setClassName()</code> under the hood — TalkBack derives the spoken role from the underlying Android widget class, not a direct string. If the host View's accessibility delegate isn't wired correctly (e.g. a custom native view, or a wrapper that doesn't propagate the prop down to the actual rendered node), the className never gets set and TalkBack falls back to announcing nothing or 'View'. iOS's UIAccessibilityTraits, by contrast, is a flag set applied directly to the element, so it's more forgiving of wrapper nesting.</p><p><b>I treat accessibilityRole as a hint to the platform's semantics tree, not a guaranteed announcement — I verify it landed with the Layout Inspector or Accessibility Inspector, not just by listening.</b></p>`,
  },
  {
    id: "a20-accessibility-3",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What does accessibilityState actually control, and why can't you fake a 'selected' tab purely with styling?`,
    answerHtml: `<p><code>accessibilityState</code> (e.g. <code>{ selected: true, disabled: false, checked: 'mixed' }</code> — <code>checked</code> accepts a boolean or the literal <code>'mixed'</code> for indeterminate checkboxes) is read by the OS accessibility tree separately from visual styling — a screen reader user never sees your highlighted border or bold font. Without setting <code>selected</code>/<code>checked</code>/<code>expanded</code> explicitly, VoiceOver/TalkBack announce the element with no state at all, so a visually-obvious 'active tab' is functionally invisible to a screen reader user. This is the most common a11y bug in custom tab bars and segmented controls.</p><p><b>Red flag:</b> assuming visual styling communicates state — a screen reader never sees the border or bold font, only <code>accessibilityState</code>.</p><p><b>I always set accessibilityState explicitly for selection/expansion/checked state — visual styling never reaches the accessibility tree.</b></p>`,
  },
  {
    id: "a20-accessibility-4",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `When should you use accessibilityElementsHidden / importantForAccessibility="no-hide-descendants" versus accessible={false} on a container?`,
    answerHtml: `<p><code>accessible={false}</code> (Android: <code>importantForAccessibility="no"</code>) removes the container itself from being a focusable unit but its children remain individually focusable. <code>accessibilityElementsHidden</code> (iOS) / <code>importantForAccessibility="no-hide-descendants"</code> (Android) hides the entire subtree — the container <i>and</i> every descendant — from the accessibility tree. Use the subtree-hide version for decorative overlays, background decals, or a modal's underlying screen content while a modal is open; using plain <code>accessible={false}</code> there would still let screen reader focus 'leak' into background elements.</p><p><b>I reach for the subtree-hide variant any time background content sits behind an open modal, so focus can't leak into it.</b></p>`,
  },
  {
    id: "a20-accessibility-5",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What's the difference between accessible={true} grouping a View versus leaving children individually accessible, and what's the failure mode of over-grouping?`,
    answerHtml: `<p><code>accessible={true}</code> on a View collapses all descendant text/labels into a single focus stop, concatenating their text content (or using the explicit <code>accessibilityLabel</code> if set) and making children un-individually-focusable. This is correct for a card that should read as one swipe stop (e.g. 'John Doe, Online, tap to message'). The failure mode is over-grouping interactive rows — if a list row has a button inside a grouped container, the button becomes unreachable by itself; VoiceOver swipes over the whole row as one stop and double-tap triggers only the row's own <code>onPress</code>, not the inner button.</p><p><b>Red flag:</b> grouping a list row that has its own separately-actionable child — the inner control becomes unreachable, not just harder to find.</p><p><b>I group static content into one accessible stop, but I never group a container that has its own actionable child.</b></p>`,
  },
  {
    id: "a20-accessibility-6",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How does setAccessibilityFocus (AccessibilityInfo.setAccessibilityFocus) differ from JS focus() on a TextInput, and when do you need it?`,
    answerHtml: `<p><code>ref.focus()</code> moves the software keyboard / input focus (where typed characters go) — it does not necessarily move the screen reader's reading cursor. <code>AccessibilityInfo.setAccessibilityFocus(reactTag)</code> explicitly moves VoiceOver/TalkBack's focus ring to a given node. You need the latter after programmatic navigation (route change, modal open, async error banner appearing) because the OS does not automatically re-target the screen reader to new content — without it, a sighted-blind user stays focused on whatever element they last touched, even though the screen changed underneath them.</p><p><b>After any programmatic navigation, I move accessibility focus explicitly with setAccessibilityFocus — the OS won't do it for me.</b></p>`,
  },
  {
    id: "a20-accessibility-7",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `On a route change with Expo Router, why might VoiceOver announce nothing or read stale content from the previous screen?`,
    answerHtml: `<p>Expo Router (built on React Navigation) doesn't automatically move accessibility focus to the new screen on push — the underlying native navigator swap can leave the OS pointing at whatever element was focused before. The fix is to either give the new screen's heading <code>accessibilityViewIsModal</code>/explicit <code>setAccessibilityFocus</code> on mount, or rely on a header title with <code>accessibilityAutoFocus</code>-equivalent patterns. On iOS specifically, posting a <code>UIAccessibilityScreenChangedNotification</code> (RN does this for some navigators, not custom screen transitions) is what makes VoiceOver re-scan; a fully custom transition (e.g. shared-element or modal libraries) can suppress this and needs manual <code>AccessibilityInfo.setAccessibilityFocus</code> in a post-transition effect.</p><p><b>I treat accessibility focus as something I own on every route change, not something the router hands me for free.</b></p>`,
  },
  {
    id: "a20-accessibility-8",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What's the practical effect of accessibilityLiveRegion="polite" vs "assertive" on Android, and is there an iOS equivalent?`,
    answerHtml: `<p><code>accessibilityLiveRegion="polite"</code> queues the announcement after TalkBack finishes its current utterance (used for non-urgent updates like a 'saved' toast). <code>"assertive"</code> interrupts whatever TalkBack is currently saying (reserved for errors/validation failures). Caveat for current RN: <code>accessibilityLiveRegion</code> is Android-only and historically only worked reliably under the old Paper renderer — on Fabric (the New Architecture, mandatory since RN 0.82) it's known to misbehave, so don't lean on it as the sole mechanism. On iOS there's no declarative live-region prop — instead you fire <code>AccessibilityInfo.announceForAccessibility(message)</code> imperatively (interrupting by default), or <code>announceForAccessibilityWithOptions(message, { queue: true })</code> (added in RN ~0.77) to get 'polite' queued-behind-existing-speech behavior, which is the actual cross-platform-equivalent primitive for non-interrupting announcements — you don't have to hand-roll your own debounce/sequencing for that case anymore.</p><p><b>I use accessibilityLiveRegion only as an Android-specific enhancement, never as the sole mechanism — announceForAccessibility (queued for 'polite') is the one API that actually behaves the same on both platforms.</b></p>`,
  },
  {
    id: "a20-accessibility-9",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why does wrapping every touchable in accessibilityRole="button" sometimes hurt usability for switch-like custom controls?`,
    answerHtml: `<p>VoiceOver/TalkBack expose role-specific gesture hints and rotor behavior — a 'button' role tells the user 'double-tap to activate', but a toggle should use <code>accessibilityRole="switch"</code> paired with <code>accessibilityState={{ checked }}</code>, which announces 'on'/'off' state changes and lets the rotor's 'Form Controls' category find it. Mislabeling a switch as a button means the user gets 'button, double tap to activate' with no indication of current state, forcing them to listen to surrounding context or guess — a regression versus the native <code>Switch</code> component, which gets this right for free.</p><p><b>Red flag:</b> labeling a toggle as a button — VoiceOver/TalkBack then announce it as a plain button with no on/off state at all.</p><p><b>A control that carries state gets the role that carries state — switch, not button.</b></p>`,
  },
  {
    id: "a20-accessibility-10",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What does the iOS 'Larger Accessibility Sizes' range add beyond standard Dynamic Type, and why can naive RN font scaling break layouts at that range?`,
    answerHtml: `<p>Standard Dynamic Type has 7 steps (xSmall to xxxLarge); enabling 'Larger Accessibility Sizes' in Settings unlocks 5 more steps (AX1–AX5) that scale far more aggressively — body text can reach roughly 310% of its base size at AX5. Code that does naive <code>fontSize * PixelRatio.getFontScale()</code> without clamping can produce fonts so large that fixed-height containers clip text, fixed-width buttons overflow off-screen, or icon+text rows collapse. The fix is testing layouts specifically at AX5 (not just '+1 step'), using flexible/wrapping layouts, and clamping scale factors (<code>maxFontSizeMultiplier</code>, not a blanket <code>allowFontScaling={false}</code>) only where truncation would otherwise be worse than capped growth.</p><p><b>Red flag:</b> testing layouts only one step above default and calling it 'supports Dynamic Type' — AX1-AX5 scales far more aggressively, and that's where overflow actually surfaces.</p><p><b>I test my layouts at AX5, not just +1 step, because that's where fixed-height containers and single-line truncation actually break.</b></p>`,
  },
  {
    id: "a20-accessibility-11",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What's the difference between maxFontSizeMultiplier and allowFontScaling={false} on RN's Text component, and when is each the right call?`,
    answerHtml: `<p><code>allowFontScaling={false}</code> fully opts a text node out of Dynamic Type/system font scale — appropriate only for things that are genuinely not content (a fixed-size icon glyph rendered as text, a logo wordmark). <code>maxFontSizeMultiplier</code> lets text scale but caps how far (e.g. <code>1.3</code> caps a 16pt label at ~21pt even if the user has AX5 set), which preserves layout integrity while still respecting the user's preference partially. Apple's HIG and most accessibility audits flag <code>allowFontScaling={false}</code> on body/label text as a violation; <code>maxFontSizeMultiplier</code> is the defensible middle ground for tight layouts like tab bar labels or stat badges.</p><p><b>Red flag:</b> reaching for allowFontScaling={false} on body text to fix a layout-overflow bug — it permanently opts the user out of their font-size preference to fix a one-time layout issue.</p><p><b>I cap growth with maxFontSizeMultiplier instead of disabling scaling outright — the layout bug is temporary, the accessibility regression isn't.</b></p>`,
  },
  {
    id: "a20-accessibility-12",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How do PixelRatio.getFontScale() and useWindowDimensions() relate to building a layout that adapts to Dynamic Type, versus relying on RN's automatic Text scaling?`,
    answerHtml: `<p>RN's <code>Text</code> auto-scales <code>fontSize</code> by the system font scale without you doing anything (unless disabled). But layout containers — fixed-height rows, icon-to-text alignment, line-clamped headers — don't auto-adjust. <code>PixelRatio.getFontScale()</code> gives you the raw multiplier so you can conditionally switch a row from horizontal to vertical layout, increase a container's minHeight, or switch from <code>numberOfLines={1}</code> to unlimited when scale crosses a threshold (e.g. <code>&gt; 1.5</code>). This is the standard pattern for 'reflow' at large accessibility sizes rather than truncating critical content.</p><p><b>Text auto-scales for free; the container around it doesn't, so I read getFontScale() to reflow layout, not just to resize fonts.</b></p>`,
  },
  {
    id: "a20-accessibility-13",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `On Android, what's the relationship between the system 'Font size' setting and the separate 'Display size' setting, and why does only one of them affect RN's fontScale?`,
    answerHtml: `<p>Android exposes two independent sliders: Font size (scales text only, RN-reported range is roughly 0.85–1.3 depending on device, default 1.0) and Display size (scales the whole UI density — icons, touch targets, spacing, text together). RN's <code>PixelRatio.getFontScale()</code> reflects only the Font size slider. Display size changes effectively alter the device's reported density/DP values instead, which RN picks up via standard dp-based layout (since RN already lays out in density-independent points) — so you don't get a separate signal for it, but your existing flex/dp-based layouts mostly handle it automatically, whereas large Font size specifically needs the text-reflow handling described above.</p><p><b>Only the Font size slider changes RN's reported fontScale — Display size is a density change my dp-based layouts already absorb.</b></p>`,
  },
  {
    id: "a20-accessibility-14",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What is the iOS Accessibility Inspector's 'Audit' feature actually checking, and what categories of bugs does it catch that manual VoiceOver swiping won't?`,
    answerHtml: `<p>Xcode's Accessibility Inspector Audit (the wand icon, or run programmatically via XCTest's <code>performAccessibilityAudit()</code> since Xcode 15) runs automated checks across roughly seven categories: missing labels on interactive elements (elementDetection/sufficientElementDescription), insufficient contrast ratios, hit-target sizes below 44x44pt, clipped text, conflicting/duplicate traits, and a few others. Manual VoiceOver swiping verifies the <i>reading order and flow</i> subjectively, but won't quantitatively catch a 38x38pt button or a 3.8:1 contrast ratio — the Audit is the systematic pass for objective, measurable a11y criteria that's easy to miss by ear.</p><p><b>I run the Audit for the objective, measurable stuff — contrast ratios and hit-target sizes — and manual VoiceOver swiping for reading order and flow.</b></p>`,
  },
  {
    id: "a20-accessibility-15",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How do you inspect the accessibility tree of a React Native app on Android without TalkBack running, and what does it tell you that the RN component tree doesn't?`,
    answerHtml: `<p>Android Studio's Layout Inspector (or standalone <code>uiautomatorviewer</code> from the Android SDK platform-tools) shows the actual <code>AccessibilityNodeInfo</code> tree the OS sees — content description, className-derived role, clickable/focusable flags, and bounds — which is what TalkBack actually reads, not what your JSX declares. This catches cases where a prop you set in RN (like <code>accessibilityLabel</code>) never made it to the native node due to a misconfigured custom native view or a third-party component that doesn't forward accessibility props, a class of bug invisible from the JS side alone.</p><p><b>When a label doesn't reach TalkBack, I go straight to the native accessibility tree with Layout Inspector or uiautomatorviewer instead of guessing from the JSX.</b></p>`,
  },
  {
    id: "a20-accessibility-16",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What does AccessibilityInfo.isScreenReaderEnabled() let you do that you shouldn't overuse, and what's the anti-pattern interviewers probe for here?`,
    answerHtml: `<p>It lets you branch behavior when a screen reader is active — e.g. skip an autoplaying carousel's auto-advance, or simplify a complex gesture-based interaction to a button-based alternative. The anti-pattern is using it to build a parallel, screen-reader-only UI tree that diverges significantly from the visual one: it doubles maintenance, drifts out of sync over time, and screen reader users end up testing a different app than everyone else. The better default is one UI with correct semantics (roles, states, labels) that works for both, reserving the API for genuine interaction-model differences (e.g. swipe-to-delete needs an actions-menu alternative).</p><p><b>Red flag:</b> building a second, screen-reader-only UI tree that diverges from the visual one — it doubles maintenance and drifts out of sync over time.</p><p><b>One UI with correct semantics beats two UIs that slowly stop matching each other.</b></p>`,
  },
  {
    id: "a20-accessibility-17",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why can a perfectly labeled custom slider still be unusable with VoiceOver, and what prop fixes it?`,
    answerHtml: `<p>A custom slider built from PanResponder/gesture-handler typically only handles drag gestures — but VoiceOver disables direct touch-passthrough on focused elements and instead expects swipe-up/swipe-down to generate increment/decrement actions when <code>accessibilityRole="adjustable"</code> is set (on Android, TalkBack maps the volume-up/volume-down buttons to the same increment/decrement actions instead of a swipe). Without that role plus an <code>onAccessibilityAction</code> handler for the <code>increment</code>/<code>decrement</code> actions (and ideally <code>accessibilityValue={{ min, max, now }}</code> for the spoken value), a screen reader user focuses the slider but has no gesture that maps to 'drag the thumb' — the control is focusable but functionally dead.</p><p><b>A focusable element isn't the same as a usable one — accessibilityRole='adjustable' plus onAccessibilityAction is what actually lets VoiceOver move the thumb.</b></p>`,
  },
  {
    id: "a20-accessibility-18",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What's the difference between accessibilityActions and the platform-native double-tap/activate gesture, and when do you need custom actions?`,
    answerHtml: `<p>Double-tap (the default activate gesture) maps to a single <code>onPress</code>. <code>accessibilityActions</code> (paired with <code>onAccessibilityAction</code>) exposes additional named actions — like 'delete' or 'reply' — that appear in VoiceOver's rotor or as a long-press menu in TalkBack, without requiring extra screen-reader-only buttons. This matters for swipe-to-reveal-actions list rows (e.g. swipeable delete/archive): the swipe gesture itself is inaccessible to a screen reader user (their swipe means 'next element'), so the equivalent actions must be exposed via <code>accessibilityActions</code> to remain reachable.</p><p><b>When a gesture is the only way to reach an action, I expose that same action through accessibilityActions so it survives a screen reader's own gesture vocabulary.</b></p>`,
  },
  {
    id: "a20-accessibility-19",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `How does reduceMotion (AccessibilityInfo.isReduceMotionEnabled) interact with Reanimated-driven animations, and whose responsibility is it to respect it?`,
    answerHtml: `<p>Reanimated does not automatically suppress or shorten animations when the OS 'Reduce Motion' setting is on — that's entirely on the app. The pattern is to read <code>AccessibilityInfo.isReduceMotionEnabled()</code> (plus subscribe to the <code>reduceMotionChanged</code> event) once at a hook/context level, then branch animation configs: skip parallax/zoom transitions, replace spring/timing entrance animations with instant opacity swaps or near-zero-duration crossfades. This matters for vestibular-disorder users for whom large-scale motion can trigger nausea or dizziness — it's a distinct concern from screen-reader support but is checked under the same accessibility umbrella.</p><p><b>Red flag:</b> assuming an animation library respects Reduce Motion automatically — it doesn't; the app has to read the setting and branch its own animation config.</p><p><b>Reduce Motion isn't something my animation library gives me for free — I read isReduceMotionEnabled() and branch the config myself.</b></p>`,
  },
  {
    id: "a20-accessibility-20",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `What does accessibilityLabelledBy / accessibilityLabeledBy do, and what relationship does it model that a single accessibilityLabel can't?`,
    answerHtml: `<p>It lets one element's accessible label be sourced from (or associated with) another element by matching the target's <code>nativeID</code>, modeling a label-for relationship — e.g. a custom form field where a separate <code>&lt;Text&gt;</code> 'Email Address' visually labels an input that's a distinct sibling node, not a parent-child grouping. Without it, you'd have to duplicate the label string into the input's own <code>accessibilityLabel</code> manually, which drifts if the visible label text changes independently (e.g. localization updates one but not the other).</p><p><b>When a label lives in a sibling Text rather than a parent, accessibilityLabelledBy is what keeps them wired together instead of duplicating the string.</b></p>`,
  },
  {
    id: "a20-accessibility-21",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why is testing only with VoiceOver/TalkBack's default settings insufficient for a senior-level accessibility review, and what additional axes should be tested?`,
    answerHtml: `<p>Default screen reader testing misses: (1) Larger Accessibility Sizes (AX1-AX5) layout breakage, (2) Reduce Motion / Reduce Transparency interactions with custom animations and blur effects, (3) Switch Control / Voice Control as alternate input modalities that rely on the same accessibility tree but navigate differently (item-scanning vs. linear swipe), (4) color-contrast and Increase Contrast mode, and (5) Bold Text setting interacting with custom font weights. A thorough review exercises the OS-level accessibility settings matrix, not just 'turn on VoiceOver and swipe through once.'</p><p><b>Turning on VoiceOver once and swiping through isn't an accessibility review — the real bugs live in Larger Accessibility Sizes, Reduce Motion, and Switch/Voice Control.</b></p>`,
  },
  {
    id: "a20-accessibility-22",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `When grouping a complex card (avatar + name + status + timestamp) into one accessible element, how do you control the spoken reading order if it differs from the visual DOM order?`,
    answerHtml: `<p>By default, grouped children are read in the order they appear in the render tree (depth-first), which usually matches visual order for simple flex layouts — but absolute positioning or z-index tricks can make visual and tree order diverge. The fix is either reordering the actual JSX/component tree to match intended reading order (preferred — keeps a single source of truth), or supplying an explicit <code>accessibilityLabel</code> on the group that overrides the auto-concatenation entirely, sidestepping order issues at the cost of having to keep that string manually in sync with the visual content.</p><p><b>Grouped reading order follows the render tree, not the visual layout, so I fix divergence by reordering the tree — not by hand-writing an override label.</b></p>`,
  },
];

export const ADVANCED20_QUIZ: QuizQuestion[] = [
  {
    id: "a20q-accessibility-1",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `A custom toggle component is built with Pressable and visually shows a colored pill that slides left/right. VoiceOver announces it as 'button, double tap to activate' with no on/off state. What's the most targeted fix?`,
    options: [`Add accessibilityLabel="Toggle" to the Pressable`, `Set accessibilityRole="switch" and accessibilityState={{ checked: value }}`, `Wrap the Pressable in accessible={true}`, `Increase the touch target to 44x44pt`],
    answer: 1,
    explanationHtml: `<p>The role tells VoiceOver this is a switch (changing the gesture hint and rotor category), and <code>accessibilityState.checked</code> is what gets the on/off value spoken and updated on change. Adding <code>accessibilityLabel="Toggle"</code> is the tempting first move because a label feels like 'fixing accessibility' — but a label only says what the element is, it never carries state, so VoiceOver would still announce 'button' with no on/off value. A bigger hit target doesn't address the missing role/state semantics either.</p>`,
  },
  {
    id: "a20q-accessibility-2",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Why might setting allowFontScaling={false} across an entire app's Text components to 'fix' layout overflow at large Dynamic Type sizes be flagged in an accessibility review?`,
    options: [`It causes a measurable performance regression on Hermes`, `It silently breaks RTL layouts on Android`, `It opts all text out of the user's font-size preference entirely, which is a more severe accessibility regression than the layout issue it solves`, `It's deprecated in React Native's New Architecture and has no effect`],
    answer: 2,
    explanationHtml: `<p>Disabling font scaling app-wide removes a core accessibility accommodation (the user's chosen text size) for low-vision users — trading a one-time layout-overflow bug for a permanent usability barrier. The 'performance regression' option is a plausible-sounding distractor — <code>allowFontScaling</code> has no measurable perf cost — but the real issue reviewers flag is the accessibility regression itself, not a fabricated Hermes concern. The correct fix is <code>maxFontSizeMultiplier</code> plus flexible layouts, not blanket opt-out.</p>`,
  },
  {
    id: "a20q-accessibility-3",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `On Android, a custom RN view wraps a third-party native UI kit component and sets accessibilityLabel, but TalkBack announces nothing when focusing it. What's the most likely root cause?`,
    options: [`accessibilityLabel is iOS-only and has no Android equivalent`, `The underlying native view doesn't forward the accessibility props to its AccessibilityNodeInfo, so the label never reaches the OS accessibility tree`, `TalkBack requires accessibilityHint to be set before it will read accessibilityLabel`, `The component needs importantForAccessibility="yes" explicitly even though that's the default`],
    answer: 1,
    explanationHtml: `<p><code>accessibilityLabel</code> works on both platforms (mapped to <code>contentDescription</code> on Android), but if a wrapped third-party native view doesn't propagate RN's accessibility props down to its own <code>AccessibilityNodeInfo</code>, the value is set on the JS/bridge side but never reaches what TalkBack actually reads. 'iOS-only' is the tempting wrong answer for anyone who hasn't hit this specific propagation bug before — but the API itself is fully cross-platform; what's failing here is one native view, not the prop. This is exactly the class of bug a Layout Inspector / uiautomatorviewer trace catches.</p>`,
  },
  {
    id: "a20q-accessibility-4",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `After a successful form submission inside a modal, you want VoiceOver to immediately announce 'Saved successfully' without requiring the user to swipe to find a status message. Which API is designed for this?`,
    options: [`accessibilityLabel on the modal root`, `AccessibilityInfo.announceForAccessibility("Saved successfully")`, `accessibilityLiveRegion="assertive" on the modal root view`, `AccessibilityInfo.setAccessibilityFocus on the close button`],
    answer: 1,
    explanationHtml: `<p><code>announceForAccessibility</code> is the cross-platform imperative API for one-off spoken announcements not tied to a persisting UI element — exactly the 'toast-style' confirmation case. <code>accessibilityLiveRegion="assertive"</code> is the tempting choice because it sounds like the 'urgent announcement' prop, but it's Android-only, tied to a node whose *content* changes in place rather than a one-shot announcement, and is unreliable under the Fabric renderer (the now-mandatory New Architecture) — it has no effect on iOS at all.</p>`,
  },
  {
    id: "a20q-accessibility-5",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `A swipeable list row reveals a 'Delete' button via a horizontal pan gesture. A screen reader user reports they can't find any way to delete the item. What's the correct accessibility fix?`,
    options: [`Increase the swipe-trigger threshold so it's easier to trigger by accident`, `Add accessibilityHint="Swipe left to delete" to the row`, `Expose a 'Delete' accessibilityAction on the row (via accessibilityActions + onAccessibilityAction) so it surfaces through the rotor or long-press menu, independent of the pan gesture`, `Set accessibilityViewIsModal={true} on the delete button`],
    answer: 2,
    explanationHtml: `<p>A screen reader user's swipe gesture is consumed by VoiceOver/TalkBack for element-to-element navigation, so a custom pan-to-reveal gesture is fundamentally unreachable that way. Adding an <code>accessibilityHint</code> is the tempting one-line fix, but a hint only describes what the swipe would do — it doesn't give the user a gesture that actually triggers it. The standard accessible pattern is exposing equivalent actions via <code>accessibilityActions</code>, which surface through the rotor (iOS) or a long-press actions menu (TalkBack).</p>`,
  },
  {
    id: "a20q-accessibility-6",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `Which statement about iOS Dynamic Type's 'Larger Accessibility Sizes' range (AX1-AX5) is accurate?`,
    options: [`It's a separate opt-in API that apps must explicitly enable per Text component before it applies`, `It only affects system apps, not third-party apps including React Native ones`, `It extends scaling well beyond the standard 7-step range, up to roughly 310% of base size at AX5, and is enabled via a separate Settings toggle from the standard Dynamic Type slider`, `It is identical in scaling factor to the standard xxxLarge step, just renamed`],
    answer: 2,
    explanationHtml: `<p>'Larger Accessibility Sizes' is a distinct, more aggressive scaling tier (AX1-AX5, on top of the 7 standard steps xSmall-xxxLarge) unlocked by its own Settings toggle, applying automatically to any RN Text that allows font scaling — there's no per-component opt-in needed. The 'separate opt-in API' option is the tempting misconception, because AX sizes feel like a special mode, so engineers assume they need to explicitly support it — in reality it's automatic, and the real work is testing layouts at that range, since standard xxxLarge alone misses where overflow bugs actually surface.</p>`,
  },
  {
    id: "a20q-accessibility-7",
    category: "nativeui",
    categoryLabel: "Native UI",
    question: `A screen has a heading, then a card grouped with accessible={true} containing an avatar Image, a Text name, and a Text status — in that JSX order. VoiceOver focuses the card and reads "status, name" instead of "name, status." What's happening?`,
    options: [`accessible={true} always reverses child reading order on iOS`, `The Image component is being read first because it lacks accessibilityLabel, displacing the text order`, `The visual layout uses absolute positioning or similar that makes the render-tree order differ from JSX source order, since reading order follows the actual tree/paint order, not necessarily authoring intent`, `VoiceOver requires an explicit accessibilityLabel on the group whenever there are 3+ descendants, otherwise order is undefined`],
    answer: 2,
    explanationHtml: `<p>Grouped reading order follows the actual component tree order (which for absolutely-positioned or z-index-layered children can diverge from the visual/intended order). Blaming the missing <code>accessibilityLabel</code> on the Image is the tempting distractor — it sounds like a classic 'unlabeled image' bug — but the Image lacking its own label doesn't reorder its siblings; the divergence is purely a tree-vs-visual-order issue. The fix is reordering the underlying tree to match intended reading order, or supplying an explicit overriding <code>accessibilityLabel</code> on the group.</p>`,
  },
];
