// Batch 17 — expo-secure-store/Keychain usage, certificate pinning in RN, secure token storage & refresh flows, and jailbreak/root detection tradeoffs.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED17_FLASHCARDS: Flashcard[] = [
  {
    id: "a17-security-1",
    category: "security",
    categoryLabel: "Security",
    question: `Why is expo-secure-store unsuitable for storing large session blobs or arrays of data, and what's the practical workaround?`,
    answerHtml: `<p>On iOS, <code>SecureStore</code> writes to the <b>Keychain</b>, which Apple does not document as a general-purpose blob store — in practice, large items are slow to read/write and some OS versions impose undocumented size ceilings, so the Keychain is meant for small secrets (tokens, keys), not bulk data. On Android it backs onto <b>SharedPreferences encrypted via the Android Keystore</b>, which is also meant for small key-value pairs, not bulk data. The fix is to store only the small secret (a token, a symmetric key) in SecureStore, and use it to encrypt/decrypt a larger payload kept in AsyncStorage, MMKV, or a SQLite/expo-sqlite database — SecureStore becomes the root-of-trust key, not the data store.</p>`,
  },
  {
    id: "a17-security-2",
    category: "security",
    categoryLabel: "Security",
    question: `What does setting \`keychainAccessible: SecureStore.AFTER_FIRST_UNLOCK\` actually trade away compared to the default, and when would you deliberately choose it?`,
    answerHtml: `<p>The default Keychain accessibility on iOS for SecureStore is <code>WHEN_UNLOCKED</code>, meaning the OS can decrypt the item only while the device is currently unlocked. <code>AFTER_FIRST_UNLOCK</code> tells the Keychain to keep the item readable <b>even after the device has been locked again</b>, as long as it was unlocked at least once since boot — so a background fetch task or a silent push handler can read a refresh token without the user re-unlocking. The trade-off: it widens the window during which a jailbroken device or a backup-extraction attack can recover the secret while the phone is locked. Use it only for tokens needed by background work (e.g., silent token refresh before a push-triggered sync), never for the most sensitive secrets like a biometric-gated signing key.</p>`,
  },
  {
    id: "a17-security-3",
    category: "security",
    categoryLabel: "Security",
    question: `What does \`requireAuthentication: true\` on SecureStore actually protect against, and what does it NOT protect against?`,
    answerHtml: `<p>It wraps the Keychain item in <code>kSecAccessControlBiometryCurrentSet</code> (or device passcode fallback), forcing Face ID/Touch ID/passcode at <b>read time</b>. It protects against another person physically picking up an <i>unlocked</i> phone and opening your app to view the secret. It does <b>not</b> protect against: a jailbroken device hooking the Keychain APIs at runtime, a malicious app exploiting an OS Keychain vulnerability, or the secret being exfiltrated once the legitimate app has already decrypted it into JS memory. It's a UX/local-shoulder-surfing control, not a defense against device compromise.</p>`,
  },
  {
    id: "a17-security-4",
    category: "security",
    categoryLabel: "Security",
    question: `Why is \`expo-secure-store\` unavailable on Expo web, and what's the architecturally correct fallback — and why is \`localStorage\` the wrong answer for tokens?`,
    answerHtml: `<p>There's no Keychain/Keystore equivalent in a browser sandbox, so SecureStore throws/no-ops on web. The architecturally correct pattern for web is to keep auth tokens out of JS-accessible storage entirely: have your backend set an <b>httpOnly, Secure, SameSite cookie</b> so the token never touches <code>localStorage</code> or <code>document.cookie</code>-readable space. <code>localStorage</code> (and non-httpOnly cookies) are readable by any script on the page, so a single XSS vulnerability anywhere in your bundle or a dependency exfiltrates every stored token — there's no sandboxing between scripts on the same origin like there is between apps on mobile.</p>`,
  },
  {
    id: "a17-security-5",
    category: "security",
    categoryLabel: "Security",
    question: `In a cross-platform Expo app, why can't you treat SecureStore as offering uniform security guarantees across iOS, Android, and web?`,
    answerHtml: `<p>Each platform backs SecureStore with a different primitive with different threat models: iOS Keychain (hardware-backed Secure Enclave key wrapping on most devices), Android Keystore-encrypted SharedPreferences (hardware-backed on devices with StrongBox/TEE, software-only on older/cheap devices), and on web it's unavailable. So 'SecureStore' is really an abstraction over three different security postures — a security review must ask 'which OS/keystore backend' rather than assume one bar. On older or budget Android devices without a TEE, the keystore can fall back to software encryption, which is meaningfully weaker than what a flagship iPhone offers.</p>`,
  },
  {
    id: "a17-security-6",
    category: "security",
    categoryLabel: "Security",
    question: `How would you implement certificate pinning in a React Native app, and why doesn't the New Architecture's JSI change the fundamental approach?`,
    answerHtml: `<p>Pinning has to happen below the JS layer, at the native networking stack, because by the time a request reaches JS-level \`fetch\`, TLS has already been negotiated by the OS. The standard approach is a native module/config wrapping <b>TrustKit</b> (iOS) or <b>OkHttp's CertificatePinner</b> (Android) — or a cross-platform wrapper like <code>react-native-ssl-pinning</code> — that validates the server's certificate (or public key) hash against a pinned set before the TLS handshake completes. JSI/TurboModules change <i>how</i> JS calls into that native code (synchronous, no bridge serialization) but not <i>where</i> the pinning logic lives — it's still entirely native-side certificate validation, JS just triggers/configures it.</p>`,
  },
  {
    id: "a17-security-7",
    category: "security",
    categoryLabel: "Security",
    question: `What's the difference between pinning a leaf certificate vs. pinning the public key (SPKI hash), and why do most production setups prefer the latter?`,
    answerHtml: `<p>Pinning the <b>leaf certificate</b> (or its hash) breaks the moment you rotate certs — e.g., a routine Let's Encrypt renewal — requiring an app store release to update the pin before the old cert expires. Pinning the <b>SPKI (Subject Public Key Info) hash of the public key</b> survives cert renewal as long as the same key pair is reused, and lets you pin an <i>intermediate</i> CA's key so any leaf cert issued under it validates. Most production setups pin 2+ SPKI hashes (current key + a backup key generated and stored offline) precisely to allow planned key rotation without bricking already-shipped app versions.</p>`,
  },
  {
    id: "a17-security-8",
    category: "security",
    categoryLabel: "Security",
    question: `What is the operational failure mode of certificate pinning that makes teams reluctant to adopt it, and what mitigates it?`,
    answerHtml: `<p>If the pinned certificate/key expires, gets revoked, or is rotated without a corresponding pin update shipped to <i>and adopted by</i> the entire user base first, every pinned client starts <b>hard-failing all network requests</b> — effectively a self-inflicted denial-of-service with no server-side fix, since old app binaries can't be patched remotely (only via OTA JS update if the pin set lives in JS-reachable config, which somewhat undermines the point). Mitigations: pin multiple keys (active + backup), set a pin expiration date with a soft-fail/report-only fallback before hard enforcement, and stage rollout via a remote kill-switch/feature flag that can disable pinning enforcement if something goes wrong.</p>`,
  },
  {
    id: "a17-security-9",
    category: "security",
    categoryLabel: "Security",
    question: `Does certificate pinning protect against a malicious app on the same device reading your stored tokens? What threat does it actually address?`,
    answerHtml: `<p>No — pinning is purely a network-layer control. It defends against a <b>person-in-the-middle</b> attacker who has compromised a CA, installed a rogue root cert (corporate MITM proxies, malicious Wi-Fi/VPN profiles), or otherwise gotten a 'trusted-looking' TLS cert in front of your traffic. It does nothing for on-device threats like Keychain extraction, memory dumping, or a malicious sibling app — those require Keychain accessibility flags, biometric gating, and root/jailbreak detection respectively. Conflating 'we pin certs' with 'our tokens are safe at rest' is a common interview trap.</p>`,
  },
  {
    id: "a17-security-10",
    category: "security",
    categoryLabel: "Security",
    question: `Why is storing a JWT access token in Redux/MobX/Zustand state (even if it originated from SecureStore) a security smell, and what's the safer pattern?`,
    answerHtml: `<p>Once a token is in JS-managed application state, it's exposed to anything that can execute JS in your app's context — a malicious or compromised third-party SDK, a Hermes-level memory dump on a jailbroken device, or a debugging bridge left enabled in a misconfigured build. The safer pattern is to keep the long-lived <b>refresh token only in SecureStore</b>, hold the short-lived <b>access token in memory only</b> (a module-level variable or a non-persisted store slice, never written to AsyncStorage/Redux-persist), and re-derive a fresh access token from the refresh token on cold start rather than persisting the access token at all.</p>`,
  },
  {
    id: "a17-security-11",
    category: "security",
    categoryLabel: "Security",
    question: `Walk through the failure mode of a naive (non-mutex'd) token refresh implementation when multiple API calls fail with 401 simultaneously.`,
    answerHtml: `<p>If each failed request independently triggers its own 'refresh the token' call in an Axios/fetch interceptor, N concurrent 401s fire N concurrent refresh requests. Most refresh-token rotation schemes (OAuth2 with rotating refresh tokens) <b>invalidate the old refresh token as soon as it's used once</b> — so the first refresh call succeeds and gets a new refresh token, but the other N-1 calls present the now-invalidated refresh token and get rejected, logging the user out even though they had a valid session. The fix is a single in-flight refresh promise that all 401 handlers await/share — the first caller starts the refresh, everyone else queues on that same promise instead of issuing their own.</p>`,
  },
  {
    id: "a17-security-12",
    category: "security",
    categoryLabel: "Security",
    question: `What is refresh token rotation, and what specific attack does it detect that a static (non-rotating) refresh token cannot?`,
    answerHtml: `<p>With rotation, every time a refresh token is exchanged for a new access token, the server also issues a <b>new refresh token and invalidates the old one</b> — refresh tokens are single-use. This lets the server detect token theft: if a stolen refresh token is replayed by an attacker <i>after</i> the legitimate client already rotated it, the server sees a reuse of a now-dead token and can treat it as a compromise signal — typically revoking the entire token family (all descendants of that refresh token) and forcing re-authentication. A static refresh token gives no such signal: a copied token works indefinitely for both the attacker and the legitimate user with no way to distinguish them.</p>`,
  },
  {
    id: "a17-security-13",
    category: "security",
    categoryLabel: "Security",
    question: `Where should the silent token-refresh logic live relative to your data-fetching layer (React Query/SWR/Apollo), and what race condition do you need to guard against on app resume?`,
    answerHtml: `<p>Refresh logic belongs in a single shared HTTP client interceptor layer (the Axios instance or fetch wrapper that React Query's queryFn ultimately calls), not duplicated per-query — otherwise every hook reinvents 401 handling. On <b>app foreground/resume</b> (e.g., \`AppState\` going active after backgrounding), a common race is: the access token expired while backgrounded, several queries refetch-on-focus simultaneously, and without the shared in-flight-refresh guard described above you get the same multi-refresh stampede. A robust setup also proactively checks token expiry (decoded \`exp\` claim or a stored expiry timestamp) on resume and refreshes <i>before</i> firing the batch of resumed queries, rather than reactively waiting for 401s.</p>`,
  },
  {
    id: "a17-security-14",
    category: "security",
    categoryLabel: "Security",
    question: `Why is storing a refresh token in SecureStore still not enough to call your auth flow 'secure' — what's the remaining gap most teams skip?`,
    answerHtml: `<p>SecureStore protects the token <b>at rest</b> on a non-compromised, non-jailbroken device with TLS intact. It does nothing about: (1) the token being logged accidentally (crash reporters like Sentry capturing request headers), (2) the refresh endpoint lacking rotation/replay detection (see above), (3) no token revocation path when a device is reported lost — if you never built a 'kill this refresh token server-side' endpoint, SecureStore-at-rest security is moot once the device itself is compromised, (4) long-lived refresh tokens with no absolute expiry, meaning a stolen token works forever even if never 'detected' via rotation. Secure storage is one link in a chain that also needs server-side revocation and rotation.</p>`,
  },
  {
    id: "a17-security-15",
    category: "security",
    categoryLabel: "Security",
    question: `Name two concrete techniques jailbreak detection libraries (e.g., \`jail-monkey\`) use, and explain why each is trivially defeatable.`,
    answerHtml: `<ul><li><b>File-existence checks</b> (looking for <code>/Applications/Cydia.app</code>, <code>/bin/bash</code>, \`su\` binaries, Sileo/Zebra paths) — defeated by the jailbreak tool simply not installing in default paths, or by hooking the filesystem APIs (e.g., via a tweak) to hide those paths from the calling process, which jailbreak tweaks like A-Bypass / Shadow do routinely.</li><li><b>Sandbox/write-permission checks</b> (attempting to write outside the app sandbox, e.g. to \`/private\`) — defeated by hooking \`fopen\`/\`open\` at the libc level to fake a permission-denied response, or simply because some modern jailbreaks (rootless) keep stricter sandboxing that fools naive checks into reporting 'not jailbroken'.</li></ul><p>The general theme: any check running in user-space JS/native code on a jailbroken device can itself be hooked by the same tooling (Frida, Substrate/libhooker) that achieved the jailbreak, so detection is fundamentally a best-effort signal, not a guarantee.</p>`,
  },
  {
    id: "a17-security-16",
    category: "security",
    categoryLabel: "Security",
    question: `What's the difference in detection difficulty between Android root detection and iOS jailbreak detection, and why?`,
    answerHtml: `<p>Android root detection is generally <b>harder to bypass reliably</b> from the attacker's side at the framework level because Google ships first-party attestation (<b>Play Integrity API</b>, the successor to the now-retired SafetyNet Attestation) that does hardware-backed attestation of boot state, verified boot, and device integrity, signed by Google's servers — much harder to spoof than a client-side file check since the verdict is computed/signed server-side. iOS has no equivalent first-party API exposed to third-party apps (Apple relies on the closed ecosystem + DeviceCheck/App Attest for app-integrity, not jailbreak-specific signals), so iOS jailbreak detection is almost entirely client-side heuristics (file paths, URL scheme checks for Cydia, fork() success test, dyld inspection for injected libraries) — all bypassable by sufficiently capable tooling like Magisk Hide equivalents (e.g., Shadow, A-Bypass) on the jailbreak side.</p>`,
  },
  {
    id: "a17-security-17",
    category: "security",
    categoryLabel: "Security",
    question: `From a risk-management standpoint, what should jailbreak/root detection actually gate in a banking or fintech-style RN app — and what's the wrong way to use it?`,
    answerHtml: `<p>The wrong way: silently blocking the entire app on a positive detection, which (a) creates false positives on legitimate power-user devices or custom ROMs, generating support burden, and (b) gives attackers a single boolean to bypass-and-be-done. The better pattern treats it as one signal feeding a <b>risk score</b>: on a jailbroken/rooted device, you might disable biometric-gated high-value actions (large transfers), reduce session timeout, force step-up authentication (OTP) for sensitive operations, or flag the session server-side for fraud review — while still allowing basic app use. This degrades gracefully and doesn't pretend client-side detection is a hard security boundary, since it never can be.</p>`,
  },
  {
    id: "a17-security-18",
    category: "security",
    categoryLabel: "Security",
    question: `Why is client-side jailbreak/root detection sometimes called 'security theater,' and what's the actual countermeasure for the underlying threat it's trying to address?`,
    answerHtml: `<p>The underlying threat is: on a jailbroken/rooted device, the attacker has ring-0-adjacent control and can bypass <i>any</i> client-side check, including the jailbreak-detection code itself (hook the detection function to always return \`false\`). So detection alone doesn't stop a determined attacker — it mainly deters unsophisticated tampering and satisfies compliance checkboxes (PCI-DSS, some fintech regulatory requirements explicitly ask for it). The actual countermeasure for high-value operations is moving trust off the device entirely: server-side anomaly detection, transaction-level step-up auth, hardware-backed attestation (Play Integrity / App Attest) verified server-side rather than trusting a client-reported boolean, and treating the device as untrusted by default for the operations that matter most.</p>`,
  },
  {
    id: "a17-security-19",
    category: "security",
    categoryLabel: "Security",
    question: `What is Apple's App Attest / Google's Play Integrity API, and how does it differ fundamentally from a \`jail-monkey\`-style detection library?`,
    answerHtml: `<p>Both are <b>hardware-backed attestation APIs</b>: the app asks the OS for a cryptographic attestation that the binary is genuine, untampered, and running on a genuine device/OS — the device's secure hardware (Secure Enclave / TEE) signs the attestation, and your <b>backend verifies the signature against Apple/Google's servers</b>, not the client trusting its own judgment. This differs fundamentally from libraries like \`jail-monkey\`: those run heuristic checks entirely in-process and return a boolean the app itself decides to trust, which is exactly what's hookable. App Attest/Play Integrity moves the trust decision off-device to a server verifying a signed assertion, closing the 'attacker just patches the boolean' hole — at the cost of needing backend integration and not being 100% available on every device/OS version.</p>`,
  },
  {
    id: "a17-security-20",
    category: "security",
    categoryLabel: "Security",
    question: `If you're using \`react-native-keychain\` instead of \`expo-secure-store\`, what additional capability does it expose that's relevant to a security-conscious refresh-token design?`,
    answerHtml: `<p><code>react-native-keychain</code> exposes finer-grained Keychain/Keystore controls than SecureStore's simplified API, notably: <b>biometry-gated access with custom prompt text per call</b>, <b>access control combinators</b> (e.g., \`BIOMETRY_CURRENT_SET\` invalidates the stored item if the enrolled biometrics change — meaning a new fingerprint/face added by an attacker after device theft can't unlock old secrets), and the ability to store structured username+password-style credential pairs natively rather than a single string. For a refresh-token design, the \`BIOMETRY_CURRENT_SET\` invalidation-on-re-enrollment behavior is the key differentiator — it's a defense against the 'attacker adds their own fingerprint to a stolen unlocked phone' scenario, which SecureStore's simpler API doesn't directly expose.</p>`,
  },
  {
    id: "a17-security-21",
    category: "security",
    categoryLabel: "Security",
    question: `Why can't certificate pinning alone stop an attacker who has gained jailbreak-level access to the device?`,
    answerHtml: `<p>Certificate pinning logic ultimately runs as code on the device (native module validating the chain). On a jailbroken device, tools like Frida/SSL Kill Switch hook directly into the TLS validation functions (e.g., \`SSL_CTX_set_verify\`, \`SecTrustEvaluate\`, or the pinning library's own comparison function) and force them to always return 'trusted,' completely bypassing the pin check before it ever inspects the real certificate. This is why pinning is positioned as a defense against <b>network-level</b> MITM (rogue Wi-Fi, compromised CA) for a non-compromised device — it is not a defense once the device itself is compromised, same theme as jailbreak detection being bypassable on a jailbroken device.</p>`,
  },
  {
    id: "a17-security-22",
    category: "security",
    categoryLabel: "Security",
    question: `In an Expo managed-workflow app (no bare native code), what's the practical path to add certificate pinning, and what does that imply about EAS Build?`,
    answerHtml: `<p>Since pinning requires native networking-layer code (TrustKit/OkHttp config), you can't do it in pure JS/Expo Go. The path is either a <b>config plugin</b> wrapping an existing native library (e.g., a community Expo config plugin for \`react-native-ssl-pinning\` or a custom plugin injecting TrustKit's Info.plist config / OkHttp's \`CertificatePinner\` into the native project at prebuild time), or ejecting/using the bare workflow. Either way, this means it can <b>only run in a custom dev client / EAS development or production build</b> — Expo Go cannot load custom native modules. The pin set, once baked into a build, also can't be updated via OTA JS updates (\`expo-updates\`) without also updating any JS-side reference, but the native TLS validation itself requires a new binary if the pinned config is compiled into native code rather than read from a remotely-fetched config.</p>`,
  },
  {
    id: "a17-security-23",
    category: "security",
    categoryLabel: "Security",
    question: `What's the security risk of storing a certificate pin set or API keys in \`app.json\`/\`app.config.js\` \`extra\` fields versus environment variables baked at build time, in the context of a public GitHub repo?`,
    answerHtml: `<p>Both end up embedded in the compiled JS bundle and are <b>trivially extractable</b> by unzipping the IPA/APK and reading the Hermes bytecode or JS bundle — neither \`app.json extra\` nor \`EXPO_PUBLIC_*\` env vars are 'secret' once shipped, they're just configuration that ships to the client. The actual risk being asked about here is different: if the *source* \`app.json\`/\`.env\` committed to a public repo contains a real backend API key (not just a public pin hash, which is fine to expose since pins are public-key hashes by design), that key is exposed to anyone browsing GitHub, not just app-extraction. Certificate pin hashes are safe to commit/ship (they're public key fingerprints, not secrets); actual API secrets, service-role keys, or signing credentials are not, and must never go in client-shipped config regardless of obfuscation.</p>`,
  },
  {
    id: "a17-security-24",
    category: "security",
    categoryLabel: "Security",
    question: `Why does using \`SecureStore.setItemAsync\` synchronously right after login, without checking \`SecureStore.isAvailableAsync()\`, risk a silent failure on some platforms?`,
    answerHtml: `<p>SecureStore is backed by the Keychain/Keystore, both OS features that can be <b>unavailable or restricted</b> in certain contexts — most notably on Expo web (no native API at all, calls reject), and in rare cases on Android if the device lacks the expected secure storage backend. \`isAvailableAsync()\` exists precisely so you can detect this and branch to a fallback (or refuse to proceed on a security-sensitive path) instead of letting a write silently throw inside a fire-and-forget \`.catch\`-less call, which in a token-storage flow means the user appears logged in (in-memory state set) while the persisted token never actually got written — breaking on next app launch with a confusing 'logged out unexpectedly' bug that's hard to reproduce because it's platform-dependent.</p>`,
  },
];

export const ADVANCED17_QUIZ: QuizQuestion[] = [
  {
    id: "a17q-security-1",
    category: "security",
    categoryLabel: "Security",
    question: `A senior engineer reviews a PR where the access token AND refresh token are both written to AsyncStorage (unencrypted) for 'simplicity, since SecureStore is slow.' What's the most accurate critique?`,
    options: [`This is fine because AsyncStorage is sandboxed per-app on both iOS and Android`, `Only the access token needs SecureStore; the refresh token is short-lived so AsyncStorage is acceptable`, `Both tokens belong in SecureStore (refresh token especially, given its long lifetime and high blast radius); AsyncStorage is unencrypted and readable via device backups or a compromised/rooted device`, `Neither needs SecureStore since TLS already protects tokens in transit`],
    answer: 2,
    explanationHtml: `<p>AsyncStorage on both platforms is unencrypted (plist on iOS, plain file/SQLite on Android by default) — it's app-sandboxed against <i>other apps</i> in the normal case, but not against device backups (iTunes/iCloud/ADB backups can extract it) or a rooted/jailbroken device. The refresh token is the higher-value target since it's long-lived and grants ongoing access — exactly the one that most needs Keychain/Keystore-backed encryption-at-rest, not less.</p>`,
  },
  {
    id: "a17q-security-2",
    category: "security",
    categoryLabel: "Security",
    question: `Your team pins only the leaf TLS certificate of \`api.example.com\`. Six months later, the cert is renewed (same key pair reused) ahead of schedule due to a CA policy change. What happens to already-shipped app versions with the old pin?`,
    options: [`Nothing — pinning the public key survives certificate renewal automatically`, `All requests from those clients fail TLS validation until they update to a build with the new pin, because the leaf cert hash changed even though the key didn't`, `The app falls back to unpinned TLS automatically as a safety mechanism`, `Only requests during the renewal window fail; afterward they self-heal via OCSP stapling`],
    answer: 1,
    explanationHtml: `<p>Pinning the leaf certificate hash is tied to that specific certificate, not the key — even though the underlying key pair was reused, a renewed cert is cryptographically a different certificate (different serial, validity dates, signature) with a different hash. This is exactly why SPKI/public-key pinning is preferred over leaf-cert pinning: it survives renewal as long as the key is reused. Leaf pinning has no automatic fallback and bricks connectivity until an app update ships.</p>`,
  },
  {
    id: "a17q-security-3",
    category: "security",
    categoryLabel: "Security",
    question: `Why is hardware-backed attestation (Apple App Attest / Google Play Integrity) considered a stronger jailbreak/root signal than a client-side library like jail-monkey?`,
    options: [`It checks more file paths and URL schemes than client-side libraries, covering more jailbreak tools`, `It runs faster, so it can be called on every API request without a performance hit`, `The attestation is cryptographically signed by secure hardware and verified by your backend against Apple/Google's servers, rather than trusting a boolean computed and self-reported by code running on the (possibly compromised) device itself`, `It is bundled into Expo Go by default, so no native build is required`],
    answer: 2,
    explanationHtml: `<p>The core weakness of client-side detection is that the verdict is computed by code running in the same compromised environment it's trying to detect — an attacker can hook/patch that code. Hardware-backed attestation moves the decision off-device: secure hardware signs a statement about device/app integrity, and a server you control verifies that signature against the platform vendor, which is far harder to forge than patching a local boolean.</p>`,
  },
  {
    id: "a17q-security-4",
    category: "security",
    categoryLabel: "Security",
    question: `A team disables certificate pinning enforcement entirely after a production incident where a routine cert rotation locked out all users for 6 hours. What's the best practice that would have prevented the incident without abandoning pinning altogether?`,
    options: [`Pin only on Android, since iOS Keychain already provides equivalent protection`, `Pin multiple SPKI hashes (current + backup key) and/or use a remote kill-switch to disable enforcement quickly if a rotation goes wrong, rather than pinning a single leaf certificate with no fallback`, `Switch from TLS to a custom encryption scheme that doesn't depend on certificates`, `Increase the certificate's validity period to 10 years to avoid rotations`],
    answer: 1,
    explanationHtml: `<p>The operational risk of pinning is exactly this kind of self-inflicted lockout. Production-grade pinning setups pin at least two keys (active + offline backup) so planned rotations are non-events, and/or ship a remote-config kill-switch so enforcement can be disabled quickly if something goes wrong — without waiting for an app store release. Abandoning pinning trades this manageable risk for full MITM exposure. (Note: public CAs cap certificate validity well under 10 years — current limits are around 398 days — so 'extend validity' isn't a real lever anyway.)`,
  },
  {
    id: "a17q-security-5",
    category: "security",
    categoryLabel: "Security",
    question: `On Expo web, \`expo-secure-store\` is unavailable. What is the architecturally correct way to handle auth tokens for the web build of a universal Expo app, and why is \`localStorage\` discouraged?`,
    options: [`Use \`localStorage\` since it persists across sessions just like SecureStore does on native`, `Have the backend set an httpOnly, Secure, SameSite cookie so the token is never exposed to JS at all; localStorage is readable by any script on the page, so a single XSS bug exfiltrates every stored token`, `Base64-encode the token before storing it in localStorage to add a layer of obfuscation`, `Store the token in a Service Worker cache, which is sandboxed from page JavaScript`],
    answer: 1,
    explanationHtml: `<p>localStorage (and JS-readable cookies) have no isolation between scripts on the same origin — any XSS, whether from your own code or a compromised third-party dependency, can read everything in it. httpOnly cookies are invisible to JavaScript entirely, so even a successful XSS can't directly exfiltrate the token (though it could still ride along using the cookie for same-origin requests, which is why SameSite and CSRF protections matter too). Base64 is encoding, not encryption, and provides zero real protection.</p>`,
  },
  {
    id: "a17q-security-6",
    category: "security",
    categoryLabel: "Security",
    question: `Your fintech app's risk team asks: 'should we hard-block app usage entirely on a positive jailbreak detection?' What's the strongest engineering counter-argument for a graduated response instead?`,
    options: [`Jailbreak detection libraries are deprecated and Apple no longer allows them in App Store builds`, `Hard-blocking has no security benefit since jailbreak detection cannot be implemented on iOS at all`, `Detection is a heuristic with both false positives (legitimate power-user devices, false flags) and a hookable false-negative ceiling (sophisticated jailbreaks bypass it entirely) — gating only high-risk actions (large transfers, step-up auth) on the signal, rather than blocking all app use, balances real fraud reduction against support cost and the false sense of security a binary block creates`, `Hard-blocking is required by PCI-DSS so there is no room for a graduated approach`],
    answer: 2,
    explanationHtml: `<p>Client-side jailbreak detection is a noisy, bypassable signal, not a guarantee. Treating it as a hard gate maximizes false-positive support burden while giving sophisticated attackers a single check to defeat for full access. The mature pattern uses it as one input to a risk score that gates specific sensitive operations and informs server-side fraud review, accepting that the device itself can never be fully trusted.</p>`,
  },
];
