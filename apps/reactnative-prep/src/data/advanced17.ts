// Batch 17 — expo-secure-store/Keychain usage, certificate pinning in RN, secure token storage & refresh flows, and jailbreak/root detection tradeoffs.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED17_FLASHCARDS: Flashcard[] = [
  {
    id: "a17-security-1",
    category: "security",
    categoryLabel: "Security",
    question: `Why is expo-secure-store unsuitable for storing large session blobs or arrays of data, and what's the practical workaround?`,
    answerHtml: `<p>SecureStore exists to protect small secrets, not to serve as a general-purpose datastore — treating it as one risks slow writes or undocumented size ceilings once the payload grows beyond a token or key. On iOS, <code>SecureStore</code> writes to the <b>Keychain</b>, which Apple does not document as a general-purpose blob store — in practice, large items are slow to read/write and some OS versions impose undocumented size ceilings. On Android it backs onto <b>SharedPreferences encrypted via the Android Keystore</b>, which is also meant for small key-value pairs, not bulk data. The fix is to store only the small secret (a token, a symmetric key) in SecureStore, and use it to encrypt/decrypt a larger payload kept in AsyncStorage, MMKV, or a SQLite/expo-sqlite database. <b>I use SecureStore as the root-of-trust key, not the data store — the payload lives in regular storage, encrypted by a key that never leaves the Keychain/Keystore.</b></p>`,
  },
  {
    id: "a17-security-2",
    category: "security",
    categoryLabel: "Security",
    question: `What does setting \`keychainAccessible: SecureStore.AFTER_FIRST_UNLOCK\` actually trade away compared to the default, and when would you deliberately choose it?`,
    answerHtml: `<p>Keychain accessibility is a trade-off between background access and at-rest exposure window, so the choice has to match what actually needs to run while the device is locked. The default for SecureStore is <code>WHEN_UNLOCKED</code>, meaning the OS can decrypt the item only while the device is currently unlocked. <code>AFTER_FIRST_UNLOCK</code> tells the Keychain to keep the item readable <b>even after the device has been locked again</b>, as long as it was unlocked at least once since boot — so a background fetch task or a silent push handler can read a refresh token without the user re-unlocking. The cost: it widens the window during which a jailbroken device or a backup-extraction attack can recover the secret while the phone is locked. <b>I only relax to AFTER_FIRST_UNLOCK for tokens that background work genuinely needs, like a silent refresh before a push-triggered sync — never for a biometric-gated signing key or anything more sensitive.</b></p>`,
  },
  {
    id: "a17-security-3",
    category: "security",
    categoryLabel: "Security",
    question: `What does \`requireAuthentication: true\` on SecureStore actually protect against, and what does it NOT protect against?`,
    answerHtml: `<p><code>requireAuthentication: true</code> is a UX/shoulder-surfing control, not a device-compromise defense — conflating the two turns a reasonable feature into a false sense of security. Mechanically, it wraps the Keychain item in <code>kSecAccessControlBiometryCurrentSet</code> (or device passcode fallback), forcing Face ID/Touch ID/passcode at <b>read time</b>, so it protects against another person physically picking up an <i>unlocked</i> phone and opening your app to view the secret.</p><p>It does <b>not</b> protect against a jailbroken device hooking the Keychain APIs at runtime, a malicious app exploiting an OS Keychain vulnerability, or the secret being exfiltrated once the legitimate app has already decrypted it into JS memory.</p><p><b>Red flag:</b> presenting biometric-gated SecureStore as protection against device compromise — it only stops a casual bystander holding the already-unlocked phone. <b>I frame it as a local-access control, not a substitute for server-side revocation or hardware attestation.</b></p>`,
  },
  {
    id: "a17-security-4",
    category: "security",
    categoryLabel: "Security",
    question: `Why is \`expo-secure-store\` unavailable on Expo web, and what's the architecturally correct fallback — and why is \`localStorage\` the wrong answer for tokens?`,
    answerHtml: `<p>Auth tokens need equivalent protection on every platform a universal app ships to, and the correct move on web isn't a weaker local-storage stand-in — it's removing the token from JS reach entirely. There's no Keychain/Keystore equivalent in a browser sandbox, so SecureStore throws/no-ops on web. The architecturally correct pattern for web is to have your backend set an <b>httpOnly, Secure, SameSite cookie</b> so the token never touches <code>localStorage</code> or <code>document.cookie</code>-readable space. <code>localStorage</code> (and non-httpOnly cookies) are readable by any script on the page, so a single XSS vulnerability anywhere in your bundle or a dependency exfiltrates every stored token — there's no sandboxing between scripts on the same origin like there is between apps on mobile. <b>On web I keep tokens out of JS entirely via httpOnly cookies; localStorage is for non-sensitive data only.</b></p>`,
  },
  {
    id: "a17-security-5",
    category: "security",
    categoryLabel: "Security",
    question: `In a cross-platform Expo app, why can't you treat SecureStore as offering uniform security guarantees across iOS, Android, and web?`,
    answerHtml: `<p>Calling something 'SecureStore' implies one security bar, but each platform backs it with a different primitive and threat model — a review that assumes uniformity will miss real gaps. iOS Keychain gets hardware-backed Secure Enclave key wrapping on most devices, Android Keystore-encrypted SharedPreferences is hardware-backed only on devices with StrongBox/TEE (software-only on older/cheap devices), and on web it's unavailable entirely. On older or budget Android devices without a TEE, the keystore can fall back to software encryption, meaningfully weaker than what a flagship iPhone offers. <b>A security review has to ask 'which OS and which keystore backend,' not assume SecureStore means one fixed guarantee.</b></p>`,
  },
  {
    id: "a17-security-6",
    category: "security",
    categoryLabel: "Security",
    question: `How would you implement certificate pinning in a React Native app, and why doesn't the New Architecture's JSI change the fundamental approach?`,
    answerHtml: `<p>Pinning only works if it happens below JS, because by the time a request reaches JS-level \`fetch\`, TLS has already been negotiated by the OS — validating in JS is validating after the trust decision was already made. The standard approach is a native module/config wrapping <b>TrustKit</b> (iOS) or <b>OkHttp's CertificatePinner</b> (Android) — or a cross-platform wrapper like <code>react-native-ssl-pinning</code> — that validates the server's certificate (or public key) hash against a pinned set before the TLS handshake completes. JSI/TurboModules change <i>how</i> JS calls into that native code (synchronous, no bridge serialization) but not <i>where</i> the pinning logic lives — it's still entirely native-side certificate validation, JS just triggers/configures it. <b>New Architecture changes the call mechanism, not the trust boundary — pinning still has to live in native networking code.</b></p>`,
  },
  {
    id: "a17-security-7",
    category: "security",
    categoryLabel: "Security",
    question: `What's the difference between pinning a leaf certificate vs. pinning the public key (SPKI hash), and why do most production setups prefer the latter?`,
    answerHtml: `<p>Leaf pinning locks the app to one certificate; SPKI pinning locks it to a key — that distinction is the whole reason production setups avoid the former. Pinning the <b>leaf certificate</b> (or its hash) breaks the moment you rotate certs — e.g., a routine Let's Encrypt renewal — requiring an app store release to update the pin before the old cert expires. Pinning the <b>SPKI (Subject Public Key Info) hash of the public key</b> survives cert renewal as long as the same key pair is reused, and lets you pin an <i>intermediate</i> CA's key so any leaf cert issued under it validates. Most production setups pin 2+ SPKI hashes (current key + a backup key generated and stored offline) precisely to allow planned key rotation without bricking already-shipped app versions. <b>I pin the SPKI hash, not the leaf cert, and always carry a backup key so rotation is a non-event.</b></p>`,
  },
  {
    id: "a17-security-8",
    category: "security",
    categoryLabel: "Security",
    question: `What is the operational failure mode of certificate pinning that makes teams reluctant to adopt it, and what mitigates it?`,
    answerHtml: `<p>Pinning turns a routine operational event — cert expiry or rotation — into a client-side outage if the pin isn't updated and adopted before the old cert dies, which is exactly why teams hesitate to adopt it. If the pinned certificate/key expires, gets revoked, or is rotated without a corresponding pin update shipped to <i>and adopted by</i> the entire user base first, every pinned client starts <b>hard-failing all network requests</b> — effectively a self-inflicted denial-of-service with no server-side fix, since old app binaries can't be patched remotely (only via OTA JS update if the pin set lives in JS-reachable config, which somewhat undermines the point). Mitigations: pin multiple keys (active + backup), set a pin expiration date with a soft-fail/report-only fallback before hard enforcement, and stage rollout via a remote kill-switch/feature flag that can disable pinning enforcement if something goes wrong. <b>I never ship pinning without a remote kill-switch — the failure mode of a bad pin is worse than no pinning at all.</b></p>`,
  },
  {
    id: "a17-security-9",
    category: "security",
    categoryLabel: "Security",
    question: `Does certificate pinning protect against a malicious app on the same device reading your stored tokens? What threat does it actually address?`,
    answerHtml: `<p>Pinning is a network-layer control only — it says nothing about what happens to data once it's on the device. It defends against a <b>person-in-the-middle</b> attacker who has compromised a CA, installed a rogue root cert (corporate MITM proxies, malicious Wi-Fi/VPN profiles), or otherwise gotten a 'trusted-looking' TLS cert in front of your traffic. It does nothing for on-device threats like Keychain extraction, memory dumping, or a malicious sibling app — those require Keychain accessibility flags, biometric gating, and root/jailbreak detection respectively.</p><p><b>Red flag:</b> conflating 'we pin certificates' with 'our tokens are safe at rest' — they're separate layers, and an interviewer will probe exactly this gap. <b>Pinning secures the transport; storage security is a completely separate control.</b></p>`,
  },
  {
    id: "a17-security-10",
    category: "security",
    categoryLabel: "Security",
    question: `Why is storing a JWT access token in Redux/MobX/Zustand state (even if it originated from SecureStore) a security smell, and what's the safer pattern?`,
    answerHtml: `<p>Once a token sits in JS-managed application state, its blast radius expands to anything that can execute JS in your app's context — not just your own code. A malicious or compromised third-party SDK, a Hermes-level memory dump on a jailbroken device, or a debugging bridge left enabled in a misconfigured build can all read it. The safer pattern is to keep the long-lived <b>refresh token only in SecureStore</b>, hold the short-lived <b>access token in memory only</b> (a module-level variable or a non-persisted store slice, never written to AsyncStorage/Redux-persist), and re-derive a fresh access token from the refresh token on cold start rather than persisting the access token at all. <b>Access tokens live in memory and die with the process; only the refresh token gets encrypted-at-rest storage.</b></p>`,
  },
  {
    id: "a17-security-11",
    category: "security",
    categoryLabel: "Security",
    question: `Walk through the failure mode of a naive (non-mutex'd) token refresh implementation when multiple API calls fail with 401 simultaneously.`,
    answerHtml: `<p>An un-mutex'd refresh handler turns a single expired token into a mass logout, because most rotation schemes only tolerate the refresh token being used once. If each failed request independently triggers its own 'refresh the token' call in an Axios/fetch interceptor, N concurrent 401s fire N concurrent refresh requests. Most refresh-token rotation schemes (OAuth2 with rotating refresh tokens) <b>invalidate the old refresh token as soon as it's used once</b> — so the first refresh call succeeds and gets a new refresh token, but the other N-1 calls present the now-invalidated refresh token and get rejected, logging the user out even though they had a valid session. The fix is a single in-flight refresh promise that all 401 handlers await/share — the first caller starts the refresh, everyone else queues on that same promise instead of issuing their own. <b>I guard token refresh with a single shared in-flight promise so concurrent 401s can't race each other into a false logout.</b></p>`,
  },
  {
    id: "a17-security-12",
    category: "security",
    categoryLabel: "Security",
    question: `What is refresh token rotation, and what specific attack does it detect that a static (non-rotating) refresh token cannot?`,
    answerHtml: `<p>Rotation turns refresh-token theft from an undetectable leak into a detectable event, which is the whole point of adopting it. Every time a refresh token is exchanged for a new access token, the server also issues a <b>new refresh token and invalidates the old one</b> — refresh tokens are single-use. If a stolen refresh token is replayed by an attacker <i>after</i> the legitimate client already rotated it, the server sees a reuse of a now-dead token and can treat it as a compromise signal — typically revoking the entire token family (all descendants of that refresh token) and forcing re-authentication. A static refresh token gives no such signal: a copied token works indefinitely for both the attacker and the legitimate user with no way to distinguish them. <b>Rotation doesn't prevent theft — it makes theft detectable via reuse of a dead token.</b></p>`,
  },
  {
    id: "a17-security-13",
    category: "security",
    categoryLabel: "Security",
    question: `Where should the silent token-refresh logic live relative to your data-fetching layer (React Query/SWR/Apollo), and what race condition do you need to guard against on app resume?`,
    answerHtml: `<p>Refresh logic has to live in one shared layer, or every data-fetching hook reinvents 401 handling with its own race conditions. It belongs in a single shared HTTP client interceptor (the Axios instance or fetch wrapper that React Query's queryFn ultimately calls), not duplicated per-query. On <b>app foreground/resume</b> (e.g., \`AppState\` going active after backgrounding), a common race is: the access token expired while backgrounded, several queries refetch-on-focus simultaneously, and without the shared in-flight-refresh guard you get the same multi-refresh stampede described above. A robust setup also proactively checks token expiry (decoded \`exp\` claim or a stored expiry timestamp) on resume and refreshes <i>before</i> firing the batch of resumed queries, rather than reactively waiting for 401s. <b>I refresh proactively on resume based on token expiry, not reactively on the first 401 from a stampede of refetches.</b></p>`,
  },
  {
    id: "a17-security-14",
    category: "security",
    categoryLabel: "Security",
    question: `Why is storing a refresh token in SecureStore still not enough to call your auth flow 'secure' — what's the remaining gap most teams skip?`,
    answerHtml: `<p>Secure-at-rest storage is one link in a chain, and teams that stop there leave the chain's weakest links unaddressed. SecureStore protects the token <b>at rest</b> on a non-compromised, non-jailbroken device with TLS intact. It does nothing about: (1) the token being logged accidentally (crash reporters like Sentry capturing request headers), (2) the refresh endpoint lacking rotation/replay detection (see above), (3) no token revocation path when a device is reported lost — if you never built a 'kill this refresh token server-side' endpoint, SecureStore-at-rest security is moot once the device itself is compromised, (4) long-lived refresh tokens with no absolute expiry, meaning a stolen token works forever even if never 'detected' via rotation. <b>Secure storage answers 'is it encrypted at rest' — it doesn't answer 'can we revoke it,' and both have to be yes.</b></p>`,
  },
  {
    id: "a17-security-15",
    category: "security",
    categoryLabel: "Security",
    question: `Name two concrete techniques jailbreak detection libraries (e.g., \`jail-monkey\`) use, and explain why each is trivially defeatable.`,
    answerHtml: `<p>Every client-side jailbreak check has the same structural weakness: it runs in the same compromised environment it's trying to detect, so it can be hooked. Two concrete examples:</p><ul><li><b>File-existence checks</b> (looking for <code>/Applications/Cydia.app</code>, <code>/bin/bash</code>, \`su\` binaries, Sileo/Zebra paths) — defeated by the jailbreak tool simply not installing in default paths, or by hooking the filesystem APIs (e.g., via a tweak) to hide those paths from the calling process, which jailbreak tweaks like A-Bypass / Shadow do routinely.</li><li><b>Sandbox/write-permission checks</b> (attempting to write outside the app sandbox, e.g. to \`/private\`) — defeated by hooking \`fopen\`/\`open\` at the libc level to fake a permission-denied response, or simply because some modern jailbreaks (rootless) keep stricter sandboxing that fools naive checks into reporting 'not jailbroken'.</li></ul><p>The general theme: any check running in user-space JS/native code on a jailbroken device can itself be hooked by the same tooling (Frida, Substrate/libhooker) that achieved the jailbreak, so detection is fundamentally a best-effort signal, not a guarantee. <b>Client-side jailbreak checks are a speed bump for casual tampering, not a security boundary — anything running on the device can be hooked by the same tooling that jailbroke it.</b></p>`,
  },
  {
    id: "a17-security-16",
    category: "security",
    categoryLabel: "Security",
    question: `What's the difference in detection difficulty between Android root detection and iOS jailbreak detection, and why?`,
    answerHtml: `<p>The two platforms differ because Google, not Apple, ships a first-party server-verified attestation API — that asymmetry is the entire reason Android detection is harder to bypass reliably. Android root detection benefits from <b>Play Integrity API</b> (the successor to the now-retired SafetyNet Attestation), which does hardware-backed attestation of boot state, verified boot, and device integrity, signed by Google's servers — much harder to spoof than a client-side file check since the verdict is computed/signed server-side. iOS has no equivalent first-party API exposed to third-party apps (Apple relies on the closed ecosystem + DeviceCheck/App Attest for app-integrity, not jailbreak-specific signals), so iOS jailbreak detection is almost entirely client-side heuristics (file paths, URL scheme checks for Cydia, fork() success test, dyld inspection for injected libraries) — all bypassable by sufficiently capable tooling like Magisk Hide equivalents (e.g., Shadow, A-Bypass) on the jailbreak side. <b>Android's edge comes from a server-verified attestation API; iOS jailbreak detection is stuck relying on client-side heuristics that a hook can defeat.</b></p>`,
  },
  {
    id: "a17-security-17",
    category: "security",
    categoryLabel: "Security",
    question: `From a risk-management standpoint, what should jailbreak/root detection actually gate in a banking or fintech-style RN app — and what's the wrong way to use it?`,
    answerHtml: `<p>The wrong way: silently blocking the entire app on a positive detection, which (a) creates false positives on legitimate power-user devices or custom ROMs, generating support burden, and (b) gives attackers a single boolean to bypass-and-be-done. A graduated response costs more engineering effort up front — a risk-scoring layer instead of one if-statement — but that cost buys back the support tickets and false confidence a hard block creates. The better pattern treats detection as one signal feeding a <b>risk score</b>: on a jailbroken/rooted device, you might disable biometric-gated high-value actions (large transfers), reduce session timeout, force step-up authentication (OTP) for sensitive operations, or flag the session server-side for fraud review — while still allowing basic app use. At scale, this also avoids re-fighting the same block logic every time a new jailbreak-hiding tool appears, since a score-based gate degrades independently of any single detection technique. <b>I treat jailbreak detection as one input to a risk score, never as a hard gate — it degrades gracefully and doesn't pretend client-side detection is a security boundary.</b></p>`,
  },
  {
    id: "a17-security-18",
    category: "security",
    categoryLabel: "Security",
    question: `Why is client-side jailbreak/root detection sometimes called 'security theater,' and what's the actual countermeasure for the underlying threat it's trying to address?`,
    answerHtml: `<p>It's called security theater because the check can be defeated by the exact same access level that makes it necessary — a jailbroken device gives the attacker ring-0-adjacent control, which includes the ability to hook the detection function itself and force it to always return \`false\`. So detection alone doesn't stop a determined attacker — it mainly deters unsophisticated tampering and satisfies compliance checkboxes (PCI-DSS, some fintech regulatory requirements explicitly ask for it). The actual countermeasure for high-value operations is moving trust off the device entirely: server-side anomaly detection, transaction-level step-up auth, hardware-backed attestation (Play Integrity / App Attest) verified server-side rather than trusting a client-reported boolean, and treating the device as untrusted by default for the operations that matter most. <b>For anything high-value, I move the trust decision server-side — client-side detection alone can't be the security boundary.</b></p>`,
  },
  {
    id: "a17-security-19",
    category: "security",
    categoryLabel: "Security",
    question: `What is Apple's App Attest / Google's Play Integrity API, and how does it differ fundamentally from a \`jail-monkey\`-style detection library?`,
    answerHtml: `<p>App Attest/Play Integrity are trust-verification APIs backed by hardware; jail-monkey-style libraries are self-reported heuristics — that role difference is what makes one hookable and the other not. Both are <b>hardware-backed attestation APIs</b>: the app asks the OS for a cryptographic attestation that the binary is genuine, untampered, and running on a genuine device/OS — the device's secure hardware (Secure Enclave / TEE) signs the attestation, and your <b>backend verifies the signature against Apple/Google's servers</b>, not the client trusting its own judgment. Libraries like \`jail-monkey\` run heuristic checks entirely in-process and return a boolean the app itself decides to trust, which is exactly what's hookable. App Attest/Play Integrity moves the trust decision off-device to a server verifying a signed assertion, closing the 'attacker just patches the boolean' hole — at the cost of needing backend integration and not being 100% available on every device/OS version. <b>jail-monkey asks the device to grade its own homework; App Attest/Play Integrity has a server check the signature.</b></p>`,
  },
  {
    id: "a17-security-20",
    category: "security",
    categoryLabel: "Security",
    question: `If you're using \`react-native-keychain\` instead of \`expo-secure-store\`, what additional capability does it expose that's relevant to a security-conscious refresh-token design?`,
    answerHtml: `<p>The capability that matters for refresh-token design is re-enrollment invalidation, because it closes a theft scenario SecureStore's simpler API can't address. \`react-native-keychain\` exposes finer-grained Keychain/Keystore controls than SecureStore's simplified API, notably: <b>biometry-gated access with custom prompt text per call</b>, <b>access control combinators</b> (e.g., \`BIOMETRY_CURRENT_SET\` invalidates the stored item if the enrolled biometrics change — meaning a new fingerprint/face added by an attacker after device theft can't unlock old secrets), and the ability to store structured username+password-style credential pairs natively rather than a single string. <b>For a refresh-token design, BIOMETRY_CURRENT_SET is the differentiator — it defends against an attacker enrolling their own biometric on a stolen unlocked phone, which SecureStore's API doesn't directly expose.</b></p>`,
  },
  {
    id: "a17-security-21",
    category: "security",
    categoryLabel: "Security",
    question: `Why can't certificate pinning alone stop an attacker who has gained jailbreak-level access to the device?`,
    answerHtml: `<p>Pinning is code running on the device, so it inherits the same weakness as jailbreak detection: it can be hooked by the exact tooling that jailbroke the device. Certificate pinning logic ultimately runs as native code validating the chain. On a jailbroken device, tools like Frida/SSL Kill Switch hook directly into the TLS validation functions (e.g., \`SSL_CTX_set_verify\`, \`SecTrustEvaluate\`, or the pinning library's own comparison function) and force them to always return 'trusted,' completely bypassing the pin check before it ever inspects the real certificate. This is why pinning is positioned as a defense against <b>network-level</b> MITM (rogue Wi-Fi, compromised CA) for a non-compromised device. <b>Pinning defends the network path on a clean device — once the device itself is compromised, no client-side check, pinning included, is a real boundary.</b></p>`,
  },
  {
    id: "a17-security-22",
    category: "security",
    categoryLabel: "Security",
    question: `In an Expo managed-workflow app (no bare native code), what's the practical path to add certificate pinning, and what does that imply about EAS Build?`,
    answerHtml: `<p>Pinning requires native networking-layer code, which draws a hard line around what Expo managed workflow can ship without a custom build. Since pinning needs TrustKit/OkHttp config, you can't do it in pure JS/Expo Go. The path is either a <b>config plugin</b> wrapping an existing native library (e.g., a community Expo config plugin for \`react-native-ssl-pinning\` or a custom plugin injecting TrustKit's Info.plist config / OkHttp's \`CertificatePinner\` into the native project at prebuild time), or ejecting/using the bare workflow. Either way, this can <b>only run in a custom dev client / EAS development or production build</b> — Expo Go cannot load custom native modules. The pin set, once baked into a build, also can't be updated via OTA JS updates (\`expo-updates\`) without also updating any JS-side reference, but the native TLS validation itself requires a new binary if the pinned config is compiled into native code rather than read from a remotely-fetched config.</p><p><b>Red flag:</b> assuming pinning can ship through Expo Go or a pure-JS OTA update — it can't; it needs a native build every time the pin set changes. <b>I reach for a config plugin over ejecting, since it keeps the rest of the app on managed workflow.</b></p>`,
  },
  {
    id: "a17-security-23",
    category: "security",
    categoryLabel: "Security",
    question: `What's the security risk of storing a certificate pin set or API keys in \`app.json\`/\`app.config.js\` \`extra\` fields versus environment variables baked at build time, in the context of a public GitHub repo?`,
    answerHtml: `<p>The real risk here isn't the pin hash being visible — it's a genuine secret sitting in a public source tree, and those are two very different problems. Both \`app.json extra\` fields and \`EXPO_PUBLIC_*\` env vars end up embedded in the compiled JS bundle and are <b>trivially extractable</b> by unzipping the IPA/APK and reading the Hermes bytecode or JS bundle — neither is 'secret' once shipped, they're just configuration that ships to the client. Certificate pin hashes are safe to commit/ship (they're public key fingerprints, not secrets); the actual risk is a real backend API key, service-role key, or signing credential committed to a public repo, exposed to anyone browsing GitHub regardless of how it's obfuscated in the bundle. <b>Public key hashes are fine to commit; real secrets never belong in client-shipped config, obfuscated or not.</b></p>`,
  },
  {
    id: "a17-security-24",
    category: "security",
    categoryLabel: "Security",
    question: `Why does using \`SecureStore.setItemAsync\` synchronously right after login, without checking \`SecureStore.isAvailableAsync()\`, risk a silent failure on some platforms?`,
    answerHtml: `<p>Skipping \`isAvailableAsync()\` turns a platform limitation into a silent, hard-to-reproduce bug instead of a handled branch. SecureStore is backed by the Keychain/Keystore, both OS features that can be <b>unavailable or restricted</b> in certain contexts — most notably on Expo web (no native API at all, calls reject), and in rare cases on Android if the device lacks the expected secure storage backend. \`isAvailableAsync()\` exists precisely so you can detect this and branch to a fallback (or refuse to proceed on a security-sensitive path) instead of letting a write silently throw inside a fire-and-forget \`.catch\`-less call.</p><p><b>Red flag:</b> writing to SecureStore right after login without checking availability or catching the rejection — the user appears logged in (in-memory state set) while the persisted token never actually got written, breaking on next app launch with a confusing 'logged out unexpectedly' bug that's hard to reproduce because it's platform-dependent. <b>I always check isAvailableAsync() (or catch the write) before trusting that a token actually persisted.</b></p>`,
  },
];

export const ADVANCED17_QUIZ: QuizQuestion[] = [
  {
    id: "a17q-security-1",
    category: "security",
    categoryLabel: "Security",
    question: `A senior engineer reviews a PR where the access token AND refresh token are both written to AsyncStorage (unencrypted) for 'simplicity, since SecureStore is slow.' What's the most accurate critique?`,
    options: [`This is fine because AsyncStorage is sandboxed per-app on both iOS and Android`, `Only the access token needs SecureStore; the refresh token is short-lived so AsyncStorage is acceptable`, `Both tokens belong in SecureStore (refresh token especially, given its long lifetime and high blast radius); AsyncStorage is unencrypted and readable via device backups or a compromised/rooted device`, `Neither needs SecureStore since TLS already protects tokens in transit`],
    answer: 2,
    explanationHtml: `<p>The tempting wrong answer is treating AsyncStorage's per-app sandboxing as sufficient — it protects against <i>other apps</i> reading it in the normal case, but not against device backups (iTunes/iCloud/ADB backups can extract it) or a rooted/jailbroken device, where sandboxing doesn't apply. The other trap is assuming the refresh token needs less protection than the access token because "it's just used to get new tokens" — it's actually the higher-value target since it's long-lived and grants ongoing access, exactly the one that most needs Keychain/Keystore-backed encryption-at-rest, not less. Both tokens belong in SecureStore.</p>`,
  },
  {
    id: "a17q-security-2",
    category: "security",
    categoryLabel: "Security",
    question: `Your team pins only the leaf TLS certificate of \`api.example.com\`. Six months later, the cert is renewed (same key pair reused) ahead of schedule due to a CA policy change. What happens to already-shipped app versions with the old pin?`,
    options: [`Nothing — pinning the public key survives certificate renewal automatically`, `All requests from those clients fail TLS validation until they update to a build with the new pin, because the leaf cert hash changed even though the key didn't`, `The app falls back to unpinned TLS automatically as a safety mechanism`, `Only requests during the renewal window fail; afterward they self-heal via OCSP stapling`],
    answer: 1,
    explanationHtml: `<p>The tempting wrong answer is "the public key survives renewal automatically" — true for SPKI pinning, but this scenario pins the <i>leaf certificate</i>, not the key, so that logic doesn't apply here; conflating the two is the exact mistake SPKI pinning is designed to avoid. The OCSP-stapling option is a red herring: OCSP is about revocation status, not certificate pinning validation, and pinning has no automatic fallback. A renewed cert is cryptographically a different certificate (different serial, validity dates, signature) even when the key pair is reused, so its hash differs and every pinned client fails TLS validation until an app update ships the new pin.</p>`,
  },
  {
    id: "a17q-security-3",
    category: "security",
    categoryLabel: "Security",
    question: `Why is hardware-backed attestation (Apple App Attest / Google Play Integrity) considered a stronger jailbreak/root signal than a client-side library like jail-monkey?`,
    options: [`It checks more file paths and URL schemes than client-side libraries, covering more jailbreak tools`, `It runs faster, so it can be called on every API request without a performance hit`, `The attestation is cryptographically signed by secure hardware and verified by your backend against Apple/Google's servers, rather than trusting a boolean computed and self-reported by code running on the (possibly compromised) device itself`, `It is bundled into Expo Go by default, so no native build is required`],
    answer: 2,
    explanationHtml: `<p>The tempting wrong answer is "it checks more file paths" — that describes a more thorough heuristic, but heuristics are still self-reported by code running in the same (possibly compromised) environment, so more checks doesn't close the hookability gap. Attestation is stronger for a structural reason, not a coverage reason: the verdict is cryptographically signed by secure hardware and verified by your backend against Apple/Google's servers, rather than trusting a boolean computed and self-reported by the device itself. It's also not about speed, and it isn't bundled into Expo Go — it requires backend integration and, on iOS, a real device/build.</p>`,
  },
  {
    id: "a17q-security-4",
    category: "security",
    categoryLabel: "Security",
    question: `A team disables certificate pinning enforcement entirely after a production incident where a routine cert rotation locked out all users for 6 hours. What's the best practice that would have prevented the incident without abandoning pinning altogether?`,
    options: [`Pin only on Android, since iOS Keychain already provides equivalent protection`, `Pin multiple SPKI hashes (current + backup key) and/or use a remote kill-switch to disable enforcement quickly if a rotation goes wrong, rather than pinning a single leaf certificate with no fallback`, `Switch from TLS to a custom encryption scheme that doesn't depend on certificates`, `Increase the certificate's validity period to 10 years to avoid rotations`],
    answer: 1,
    explanationHtml: `<p>The tempting distractor is extending certificate validity to avoid rotations — but public CAs cap validity well under 10 years (current limits are around 398 days), so that "fix" isn't actually available. The real fix is pinning multiple SPKI hashes (current + backup key) and/or shipping a remote kill-switch to disable enforcement quickly if a rotation goes wrong, rather than pinning a single leaf certificate with no fallback. Pinning only on one platform or dropping TLS for a custom scheme don't address the actual failure mode — a routine, planned rotation locking out every client because there was no second pin or fast disable path.</p>`,
  },
  {
    id: "a17q-security-5",
    category: "security",
    categoryLabel: "Security",
    question: `On Expo web, \`expo-secure-store\` is unavailable. What is the architecturally correct way to handle auth tokens for the web build of a universal Expo app, and why is \`localStorage\` discouraged?`,
    options: [`Use \`localStorage\` since it persists across sessions just like SecureStore does on native`, `Have the backend set an httpOnly, Secure, SameSite cookie so the token is never exposed to JS at all; localStorage is readable by any script on the page, so a single XSS bug exfiltrates every stored token`, `Base64-encode the token before storing it in localStorage to add a layer of obfuscation`, `Store the token in a Service Worker cache, which is sandboxed from page JavaScript`],
    answer: 1,
    explanationHtml: `<p>The tempting wrong answer is that localStorage "persists across sessions just like SecureStore" — true, but persistence isn't the property being tested; JS-readability is. localStorage (and JS-readable cookies) have no isolation between scripts on the same origin, so any XSS, whether from your own code or a compromised third-party dependency, can read everything in it. httpOnly cookies are invisible to JavaScript entirely, so even a successful XSS can't directly exfiltrate the token (though it could still ride along using the cookie for same-origin requests, which is why SameSite and CSRF protections matter too). Base64 is encoding, not encryption, and provides zero real protection despite looking obfuscated. A Service Worker cache is also script-accessible within the origin's scope, not a secure enclave — it doesn't solve the exposure problem either.</p>`,
  },
  {
    id: "a17q-security-6",
    category: "security",
    categoryLabel: "Security",
    question: `Your fintech app's risk team asks: 'should we hard-block app usage entirely on a positive jailbreak detection?' What's the strongest engineering counter-argument for a graduated response instead?`,
    options: [`Jailbreak detection libraries are deprecated and Apple no longer allows them in App Store builds`, `Hard-blocking has no security benefit since jailbreak detection cannot be implemented on iOS at all`, `Detection is a heuristic with both false positives (legitimate power-user devices, false flags) and a hookable false-negative ceiling (sophisticated jailbreaks bypass it entirely) — gating only high-risk actions (large transfers, step-up auth) on the signal, rather than blocking all app use, balances real fraud reduction against support cost and the false sense of security a binary block creates`, `Hard-blocking is required by PCI-DSS so there is no room for a graduated approach`],
    answer: 2,
    explanationHtml: `<p>The tempting wrong answers overstate absolutes: jailbreak detection libraries aren't deprecated or disallowed by Apple, iOS jailbreak detection is possible (just limited to client-side heuristics, as covered earlier), and PCI-DSS asks for controls against compromised devices — it doesn't mandate a specific hard-block implementation. The actual strongest argument is that detection is a heuristic with both false positives (legitimate power-user devices, false flags) and a hookable false-negative ceiling (sophisticated jailbreaks bypass it entirely) — gating only high-risk actions on the signal, rather than blocking all app use, balances real fraud reduction against support cost and the false sense of security a binary block creates.</p>`,
  },
];
