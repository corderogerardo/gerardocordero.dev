// Batch 12 — List virtualization (FlatList vs FlashList internals), Hermes bytecode precompilation, JS thread blocking diagnosis, bundle size reduction, and avoiding unnecessary re-renders.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED12_FLASHCARDS: Flashcard[] = [
  {
    id: "a12-list-perf-1",
    category: "perf",
    categoryLabel: "PERF",
    question: `FlashList v1 used native-assisted cell recycling. What did FlashList v2 actually change, and does v2 still recycle cells?`,
    answerHtml: `<p><b>Yes — v2 still recycles.</b> Off-screen items are not unmounted; per Shopify's own docs, when a row scrolls out of view its component instance is kept alive and re-rendered with a different item's props rather than destroyed (this is why <code>useRecyclingState</code> exists — to auto-reset local state that would otherwise leak from the previous item).</p><p>What v2 actually changed: it's a ground-up, <b>JS-only rewrite</b> with no native recycler dependency (v1 leaned on a native RecyclerListView-style implementation), built specifically for React Native's New Architecture so it can get synchronous layout measurements through Fabric. Because of that, v2 <b>auto-measures</b> cells and no longer needs <code>estimatedItemSize</code>, <code>estimatedListSize</code>, or the size-estimate role of <code>overrideItemLayout</code> (which in v2 only controls span, not size) — that's the real improvement, not the removal of recycling.</p><p><b>Red flag:</b> don't tell an interviewer v2 "dropped recycling" — cell instances are still reused; what changed is the layout engine underneath. <b>"FlashList v2 still recycles cells — it swapped the native measurement layer for a JS-only, Fabric-synchronous one, which is what let it drop estimatedItemSize."</b></p>`,
  },
  {
    id: "a12-list-perf-2",
    category: "perf",
    categoryLabel: "PERF",
    question: `FlatList's virtualization renders items in 'windows.' What three props control how aggressively it virtualizes, and what's the tradeoff of shrinking them?`,
    answerHtml: `<p><code>windowSize</code> (multiples of viewport height kept rendered, default 21 = 10 above + 10 below + 1 visible), <code>initialNumToRender</code>, and <code>maxToRenderPerBatch</code> (items rendered per fill-in batch during scroll).</p><p>Shrinking them cuts memory and off-screen render cost, but on a fast fling the renderer can't keep up with scroll velocity, so you see momentary blank cells. It is a direct memory-vs-blank-flash tradeoff, not free virtualization.</p><p><b>"windowSize and maxToRenderPerBatch trade memory for render headroom — shrink them and you trade blank cells on fast flings for a smaller resident view count."</b></p>`,
  },
  {
    id: "a12-list-perf-3",
    category: "perf",
    categoryLabel: "PERF",
    question: `Why does a stable, correct keyExtractor matter more for FlatList performance than just avoiding React's 'missing key' warning?`,
    answerHtml: `<p>VirtualizedList (FlatList's engine) uses the key to decide cell <b>identity</b> across data updates — it's how the diff between old and new data arrays maps to 'this is the same logical row, just re-rendered' versus 'this is a new row, mount fresh.'</p><p>Using array <code>index</code> as the key on a reorderable or filterable list causes wrong identity mapping: rows get re-rendered with stale internal state (text input values, animation state) attached to the wrong data, and the diff can't skip unchanged rows, so updates touch far more cells than necessary.</p><p><b>"A stable, content-based key isn't about silencing a warning — it's how VirtualizedList maps old state to the right row after a reorder or filter."</b></p>`,
  },
  {
    id: "a12-list-perf-4",
    category: "perf",
    categoryLabel: "PERF",
    question: `What does \`getItemLayout\` actually buy you on FlatList, and why doesn't FlashList need an equivalent prop?`,
    answerHtml: `<p><code>getItemLayout</code> lets FlatList <b>skip the measurement pass</b>: if you can compute each item's exact offset/length synchronously (fixed-height rows), it can jump straight to <code>scrollToIndex</code>/<code>scrollToOffset</code> targets and compute the visible window without waiting for <code>onLayout</code> callbacks from rendered cells.</p><p>FlashList's v2 layout engine measures cells as they render (synchronously, via Fabric) and maintains a running size cache automatically, so it doesn't need a hand-written layout function — the cost is a brief 'layout settles after first render' period instead of needing exact a-priori knowledge.</p><p><b>"getItemLayout buys FlatList a synchronous jump-to-offset by trading in exact layout math; FlashList doesn't need that trade because it measures cells itself through Fabric."</b></p>`,
  },
  {
    id: "a12-list-perf-5",
    category: "perf",
    categoryLabel: "PERF",
    question: `A junior engineer says 'I switched to FlashList and scroll FPS didn't improve.' What are the two most common root causes that have nothing to do with which list component you use?`,
    answerHtml: `<p>1. <b>Heavy row components</b> — if each row does expensive work (unmemoized inline computation, large image decode on the JS/UI thread, deeply nested unmemoized children), virtualization only limits <i>how many</i> rows render, not <i>how expensive</i> each one is. Swapping list libraries doesn't fix a slow row.</p><p>2. <b>List-level re-renders</b> — if the parent re-renders and recreates <code>renderItem</code>, <code>keyExtractor</code>, or the data array reference every time, every visible cell re-renders regardless of virtualization. The fix is memoizing those props (and the row component itself), not picking a different list.</p><p><b>Red flag:</b> swapping list libraries before profiling is a tell that you're guessing, not diagnosing — virtualization only bounds row count, not row cost or re-render churn.</p><p><b>"Before I touch the list component, I profile whether it's row cost or list-level re-renders — swapping libraries doesn't fix either."</b></p>`,
  },
  {
    id: "a12-list-perf-6",
    category: "perf",
    categoryLabel: "PERF",
    question: `Explain what 'Hermes bytecode precompilation' means at build time, and what specifically moves from runtime cost to build-time cost.`,
    answerHtml: `<p>Normally a JS engine parses source text, builds an AST, and compiles to bytecode every time it loads a script. Hermes instead ships with a standalone compiler (<code>hermesc</code>) that runs <b>at build time</b> (via the Metro/Gradle/Xcode build pipeline) to turn your JS bundle into Hermes Bytecode (HBC), a serialized, engine-specific binary format.</p><p>At app launch, the Hermes VM loads HBC directly — no parse, no AST construction, no bytecode-gen step on device. That parse+compile cost (which scales with bundle size and is worse on low-end CPUs) is paid once by your build machine instead of by every user on every cold start.</p><p><b>"Precompiling to Hermes bytecode moves parse-and-compile from every user's cold start to one build-machine run — the VM just loads and executes."</b></p>`,
  },
  {
    id: "a12-list-perf-7",
    category: "perf",
    categoryLabel: "PERF",
    question: `Hermes supports both bytecode loading from a file and from an in-memory buffer. Why does the Android build specifically benefit from mmap-able bytecode, and what's the practical implication for bundle placement?`,
    answerHtml: `<p>HBC's binary format is designed to be <b>mmap-able</b>: the OS can map the bytecode file directly into the process's address space and page it in lazily, rather than the runtime reading and copying the whole file into a JS-managed buffer.</p><p>Practically, this means the bytecode should ship as a real file in the APK/AAB (not inlined as a string asset or compressed inside something the runtime must fully decompress first) — React Native's default Android packaging already does this, which is part of why Hermes startup beats JSC even before considering parse time.</p><p><b>"Because HBC is mmap-able, shipping it as a real file the OS can page in lazily is part of Hermes's startup edge — independent of the parse-time savings."</b></p>`,
  },
  {
    id: "a12-list-perf-8",
    category: "perf",
    categoryLabel: "PERF",
    question: `What is Hermes' "lazy compilation" mode, and why is it normally disabled in release builds shipped to users?`,
    answerHtml: `<p>In dev/JS-engine mode, Hermes can defer compiling a function's body until it's first called (lazy parsing), which speeds up local dev reloads since you rarely execute every function in a file immediately.</p><p>For release builds you precompile to bytecode ahead of time (<code>hermesc</code> with full, eager compilation) specifically so there is zero on-device compilation step — lazy compilation would reintroduce a parse/compile cost on first call, defeating the purpose of bytecode precompilation. The dev-time laziness and the release-time eagerness are different tradeoffs for different goals (iteration speed vs. cold-start speed).</p><p><b>Red flag:</b> shipping a release build with lazy compilation still on reintroduces the exact on-device parse/compile cost bytecode precompilation exists to remove.</p><p><b>"Lazy compilation is a dev-loop optimization, not a release one — release builds want eager, ahead-of-time bytecode so there's zero compile step on the device."</b></p>`,
  },
  {
    id: "a12-list-perf-9",
    category: "perf",
    categoryLabel: "PERF",
    question: `Two apps ship the same bundle size, but one has a noticeably faster Hermes cold start. Besides bytecode precompilation itself, what bundle-shape factor commonly explains the gap?`,
    answerHtml: `<p><b>Module-level (top-of-file) side effects.</b> Hermes bytecode loading is fast, but every <code>require()</code>d module still runs its top-level code synchronously on the JS thread at startup — instantiating classes, building large constant objects/maps, running polyfills, registering things globally.</p><p>A bundle with the same byte size but heavier eager initialization (e.g. a UI library that builds large icon/theme lookup tables at import time, or barrel imports that pull in code never used on the first screen) pays more JS-thread time before TTI, independent of how fast the bytecode itself loaded.</p><p><b>"Bytecode loading speed and module-init cost are separate budgets — a bundle can load fast and still block TTI if its top-level code does real work."</b></p>`,
  },
  {
    id: "a12-list-perf-10",
    category: "perf",
    categoryLabel: "PERF",
    question: `You suspect the JS thread is blocked, not the UI/main thread. What's the most reliable way to distinguish 'JS thread blocked' from 'native UI thread blocked' when the symptom is just 'the app feels frozen'?`,
    answerHtml: `<p>Which thread owns the freeze changes the fix entirely, so isolate it before reaching for a profiler:</p><p>1. <b>Check touch registration.</b> RN's gesture responder and most native UI updates (animations driven by the native driver, scroll) happen on the native/UI thread independent of JS — so start by seeing if taps produce any native feedback at all.</p><p>2. <b>Check for a JS-driven effect.</b> If <b>taps register but produce no JS-driven effect</b> (no state update, no navigation) while existing native-driven animations keep running smoothly, the JS thread is blocked but native isn't.</p><p>3. <b>Confirm with separate FPS readouts.</b> If instead the whole screen is unresponsive — even native-driven scroll/animation stutters — that points to the native/UI thread or the shadow thread (layout) being the bottleneck instead. Flipper's/React DevTools' JS thread FPS view, or the in-app perf monitor's two separate FPS readouts (UI vs JS), make this concrete rather than inferred.</p><p><b>"Touch responsiveness is the tell: taps that register but do nothing mean JS is blocked; stuttering native animation means the UI thread is."</b></p>`,
  },
  {
    id: "a12-list-perf-11",
    category: "perf",
    categoryLabel: "PERF",
    question: `What in-app and Hermes-specific tools let you profile *why* the JS thread is blocked, beyond just observing low JS FPS?`,
    answerHtml: `<p>1. <b>Hermes' built-in sampling profiler</b> (<code>__hermes_profile</code> hooks reachable via dev menu "Start/Stop Sampling Profiler," or programmatically) generates a <code>.cpuprofile</code> you can load into Chrome DevTools' or Flipper's flame graph to see exactly which JS functions consumed CPU time.</p><p>2. <b>The in-app Perf Monitor</b> (dev menu) shows separate JS and UI FPS counters in real time, so you can correlate a janky interaction with a JS-thread dip versus a UI-thread dip.</p><p>3. <b>Systrace / Android Studio Profiler</b> and <b>Instruments Time Profiler</b> (iOS) for native-level thread timelines when the bottleneck might be crossing into native modules or bridge/JSI calls.</p><p><b>"For JS-thread stalls I reach for Hermes's sampling profiler and the flame graph first — it tells me which function is hot instead of just that FPS dropped."</b></p>`,
  },
  {
    id: "a12-list-perf-12",
    category: "perf",
    categoryLabel: "PERF",
    question: `A list of 500 rows renders fine, but typing in a search box at the top of the screen causes visible input lag on every keystroke. The list isn't even being filtered yet (filtering happens on submit). What's the likely cause and the fix?`,
    answerHtml: `<p>The search input's local state update is triggering a re-render of the <b>parent component that also owns the list</b>, and because <code>renderItem</code>/row components aren't memoized (or the data array reference changes identity each render), all 500 rows re-render and re-reconcile on every keystroke — even though their content didn't change.</p><p>Fix, in order: <b>1.</b> Isolate the input's state so it doesn't live in a component that's an ancestor of the list (lift it to a sibling, or wrap the list in <code>React.memo</code> with stable props). <b>2.</b> Ensure <code>renderItem</code> and <code>data</code> have stable references (<code>useCallback</code>/<code>useMemo</code> or module-level functions) so the list itself doesn't think its inputs changed.</p><p><b>"Input state one level too high is a classic re-render leak — isolate it from the list's ancestry before touching memoization."</b></p>`,
  },
  {
    id: "a12-list-perf-13",
    category: "perf",
    categoryLabel: "PERF",
    question: `Why can \`console.log\` calls themselves become a meaningful JS-thread bottleneck in React Native, and where does the cost actually go in the old vs. new architecture?`,
    answerHtml: `<p>On the legacy bridge, every <code>console.log</code> serializes its arguments and sends them across the async bridge to native for display in Metro/the device log — in a tight loop (e.g. logging inside a list's <code>renderItem</code> or a frequently firing effect) this adds real serialization and bridge-traffic overhead on the JS thread.</p><p>On the New Architecture (JSI), logging still costs object stringification/formatting in JS and a synchronous JSI call to the native logger, which is cheaper than bridge serialization but still not free — logs left in hot paths (render, scroll handlers, animation frames) are a classic and easily overlooked JS-thread tax in both architectures.</p><p><b>Red flag:</b> assuming JSI made logging free — it's cheaper than bridge serialization, not free; verbose logs in <code>renderItem</code> or scroll handlers still tax the JS thread on both architectures.</p><p><b>"console.log in a hot path is a JS-thread tax on the old bridge and the new architecture alike — cheaper under JSI, never free."</b></p>`,
  },
  {
    id: "a12-list-perf-14",
    category: "perf",
    categoryLabel: "PERF",
    question: `What does Metro's \`inlineRequires\` transform do, and why does it specifically help cold-start time rather than runtime performance?`,
    answerHtml: `<p>By default, every <code>import</code>/<code>require</code> at the top of a file executes <b>eagerly</b> when that file is first evaluated, even if the imported binding is only used inside a function that may never be called on this screen. <code>inlineRequires</code> rewrites those imports to be called lazily, right where they're used, deferring the module's evaluation until first actual use.</p><p>This mainly helps cold start because RN evaluates your entire dependency graph from the entry point at launch; deferring rarely-hit modules (a settings screen's code, a rarely opened modal) means less synchronous JS-thread work before the first screen is interactive. It does little for steady-state runtime performance since the deferred modules still run in full once they're first touched.</p><p><b>"inlineRequires trades eager module init for lazy, first-use init — it buys cold start, not steady-state performance."</b></p>`,
  },
  {
    id: "a12-list-perf-15",
    category: "perf",
    categoryLabel: "PERF",
    question: `Hermes ships a precompiled "bytecode" bundle, yet Metro still produces a single JS bundle by default for many RN apps. What's the relationship between Metro bundling and Hermes compilation, and where does code-splitting fit in?`,
    answerHtml: `<p>Metro's job is <b>bundling</b>: resolving the module graph and concatenating everything into one (or more) JS file(s). Hermes compilation is a separate, later step that takes Metro's output JS and compiles it to bytecode — it doesn't change what code is included, only how it's represented at load time.</p><p>So a single giant Metro bundle compiled to Hermes bytecode still pays the cost of loading/initializing all of that code path even if bytecode parsing itself is fast. Real bundle-size reduction needs Metro-level work: code-splitting via <code>React.lazy</code>/dynamic <code>import()</code> with Metro's async require support, or RAM bundles/multi-bundle setups, so unused screens' code isn't even in the bytecode that loads at launch.</p><p><b>"Hermes compiles what Metro bundles — it doesn't decide what's included. Real size reduction is a Metro-level code-splitting problem, not a Hermes one."</b></p>`,
  },
  {
    id: "a12-list-perf-16",
    category: "perf",
    categoryLabel: "PERF",
    question: `Name three concrete bundle-size reduction techniques specific to React Native (not generic JS minification) and what each one actually removes.`,
    answerHtml: `<ul><li><b>Hermes bytecode + dead code elimination via the Hermes compiler's <code>-O</code> optimizations</b> — strips unreachable code paths at compile time, beyond what Metro's Terser pass catches.</li><li><b>Platform-specific extensions (\`.ios.js\`/\`.android.js\`) and Proguard/R8 (Android) or dead-stripping (iOS)</b> — removes the other platform's code and unused native symbols from the final native binary, separate from the JS bundle.</li><li><b>Selective/targeted imports instead of barrel imports</b> (e.g. importing a single icon component instead of an entire icon library's index) — prevents Metro from pulling the whole library's module graph into the bundle just because one export was used.</li></ul><p><b>"Minification shrinks what's already there; RN-specific wins come from removing whole code paths — dead compiled code, the other platform's files, and barrel imports you never meant to pull in."</b></p>`,
  },
  {
    id: "a12-list-perf-17",
    category: "perf",
    categoryLabel: "PERF",
    question: `Why does enabling Hermes' bytecode compilation not shrink the *JS bundle size* metric that bundle analyzers (like \`react-native-bundle-visualizer\`) report, even though it reduces real on-device cost?`,
    answerHtml: `<p>Bundle analyzers typically visualize the <b>Metro JS bundle</b> (the source-level JS, pre-Hermes), because that's what's available for static module-graph analysis — module names, sizes, and import relationships only exist at that stage; HBC is a flat binary blob without that structure.</p><p>Hermes bytecode is often comparable in size or even slightly larger than the minified JS source for the same code (bytecode trades text compactness for execution speed), so 'bundle size' as a download-size number and 'cold-start cost' as a CPU-time number are two different axes — reducing one doesn't automatically reduce the other.</p><p><b>"Bundle size and cold-start CPU time are different axes — a bundle analyzer measures the Metro JS stage, so Hermes gains show up in launch profiling, not in the size report."</b></p>`,
  },
  {
    id: "a12-list-perf-18",
    category: "perf",
    categoryLabel: "PERF",
    question: `What's the practical difference between using \`React.memo\` on a list row component versus memoizing the \`renderItem\` callback passed to FlatList, and do you need both?`,
    answerHtml: `<p><code>React.memo</code> on the row component prevents that component from re-rendering when its <b>props</b> are referentially/shallowly equal to the previous render — it protects against the row re-rendering due to parent re-renders that don't actually change this row's data.</p><p>Memoizing <code>renderItem</code> (with <code>useCallback</code>) prevents FlatList/VirtualizedList itself from treating the prop as 'changed' across parent re-renders, which can trigger broader internal effects. You generally need <b>both</b>: an unmemoized row component will still re-render every time it's called regardless of how stable <code>renderItem</code> is, and an unmemoized <code>renderItem</code> can cause VirtualizedList-level churn even if each row internally bails out fast.</p><p><b>"React.memo protects the row from the parent; a memoized renderItem protects the list from the parent — you need both, or one gap lets re-renders through."</b></p>`,
  },
  {
    id: "a12-list-perf-19",
    category: "perf",
    categoryLabel: "PERF",
    question: `A component re-renders every time its parent re-renders even though none of its primitive props changed. You find it receives a \`style={{ marginTop: 8 }}\` prop. Why does this cause unnecessary re-renders, and what's the fix?`,
    answerHtml: `<p>An inline object literal creates a <b>new reference</b> on every render, so even a child wrapped in <code>React.memo</code> fails its shallow-equality prop check (<code>{} !== {}</code> in JS) and re-renders regardless of the actual values inside it being identical.</p><p>Fix: hoist the style to a module-level <code>StyleSheet.create({...})</code> object (stable reference forever) or wrap an inline style in <code>useMemo</code> if it's genuinely dynamic. The same applies to inline arrow functions passed as props (<code>onPress={() => doThing(id)}</code>) — wrap in <code>useCallback</code> or move identity-stable handlers up.</p><p><b>Red flag:</b> reaching for React.memo without also auditing inline object/array/function props — memo can't help if the props are new references every render.</p><p><b>"An inline style object defeats React.memo the same way an inline arrow function does — both are new references every render, so hoist or memoize them, not just the component."</b></p>`,
  },
  {
    id: "a12-list-perf-20",
    category: "perf",
    categoryLabel: "PERF",
    question: `Why is \`React.memo\` alone often insufficient to stop re-renders in a component that consumes a Context value, and what's the standard mitigation?`,
    answerHtml: `<p><code>React.memo</code> only guards against re-renders triggered by a parent re-rendering and passing new props — it has no effect on re-renders triggered by a <b>subscribed Context</b> changing, because <code>useContext</code> subscribes the component directly to the provider's value regardless of memoization. Any context value change re-renders every consumer, even ones that only read an unrelated field of a large context object.</p><p>Mitigations: split one large context into multiple smaller ones scoped by what changes together (e.g. separate ThemeContext from UserContext), or move frequently-changing state out of Context into a selector-based store (Zustand, Jotai, Redux with useSelector) that only re-renders subscribers whose selected slice actually changed.</p><p><b>Red flag:</b> treating Context as a general-purpose state manager for frequently-changing values — every consumer re-renders on any provider change, and React.memo has no defense because useContext subscribes directly.</p><p><b>"Context is dependency injection, not a state manager — for high-frequency state I reach for a selector-based store so only the components reading that slice re-render."</b></p>`,
  },
  {
    id: "a12-list-perf-21",
    category: "perf",
    categoryLabel: "PERF",
    question: `What does React Native's New Architecture (Fabric) change about how state updates propagate to the native side, and how does that interact with re-render cost?`,
    answerHtml: `<p>Fabric introduces a C++ shadow tree shared between JS and native, with synchronous layout/measurement possible via JSI. Paired with React 18's concurrent root — which the New Architecture enables, and which is now the default path since New Architecture became mandatory in current RN releases — it supports <b>automatic batching</b> of state updates far more consistently than the legacy renderer (which only batched inside React event handlers, not inside promises/timeouts/native callbacks).</p><p>Practically, this means fewer redundant commit passes for updates that originate outside a React event handler (e.g. multiple state updates inside an async native-module callback now batch into one re-render instead of one per setState call), reducing wasted re-renders that previously slipped past automatic-batching boundaries on the legacy bridge.</p><p><b>"Fabric plus the concurrent root batches updates far more consistently than the legacy bridge — fewer commit passes for state changes that originate outside a React event handler."</b></p>`,
  },
  {
    id: "a12-list-perf-22",
    category: "perf",
    categoryLabel: "PERF",
    question: `When profiling re-renders with React DevTools Profiler, you see a component re-render with 'props changed' as the reason, but the prop values look identical when you log them. What's actually being compared, and what causes this mismatch?`,
    answerHtml: `<p>React DevTools' 'why did this render' reasoning is based on <b>reference (===) equality</b>, not deep value equality — it's reporting that the prop's identity changed, regardless of whether the JSON-stringified contents are the same.</p><p>This commonly happens with derived data: a \`.filter()\`/\`.map()\`/\`.sort()\` call (or a spread \`{...obj}\`) run fresh inside the parent's render body produces a new array/object every time even when the input data hasn't changed. The fix is wrapping the derivation in <code>useMemo</code> keyed on its real dependencies, so the reference is stable across renders where the underlying data is unchanged.</p><p><b>"'Props changed' in DevTools means reference inequality, not value inequality — derived arrays/objects need useMemo or they'll look 'changed' every render even with identical contents."</b></p>`,
  },
  {
    id: "a12-list-perf-23",
    category: "perf",
    categoryLabel: "PERF",
    question: `Reanimated's \`useAnimatedStyle\` and similar worklet-driven style updates don't cause React re-renders even though the UI visibly updates every frame. How is that possible, and why does it matter for performance?`,
    answerHtml: `<p>Worklets compiled by Reanimated run on the <b>UI thread</b> and mutate shared values that feed directly into the native view's props via the Fabric/native bridge, bypassing React's JS-thread render-and-reconcile cycle entirely for that style update. There is no corresponding React Fiber commit for each frame of the animation.</p><p>This matters because a 60fps animation driven this way costs zero JS-thread renders and zero reconciliation diffing — versus driving the same animation through useState + setInterval, which would force a full React re-render (and child re-renders, unless memoized) on every single frame, both jank-prone and JS-thread-expensive.</p><p><b>"Worklet-driven styles write to the native view directly from the UI thread — there's no Fiber commit per frame, which is why they don't cost a React re-render at all."</b></p>`,
  },
  {
    id: "a12-list-perf-24",
    category: "perf",
    categoryLabel: "PERF",
    question: `FlatList exposes \`removeClippedSubviews\`. What does it actually do on each platform, and why is it not a universal performance win?`,
    answerHtml: `<p>On Android it detaches the native views of off-screen cells from the view hierarchy (they remain in JS/React's tree, just not mounted natively), reducing the native view count and GPU memory pressure. On iOS its support has historically been inconsistent/buggy and is generally not recommended.</p><p>It's not a universal win because clipping/unclipping native views has its own CPU cost on each scroll frame, and it can cause measurement glitches with certain layouts (absolutely positioned children, custom native components that don't expect to be reattached). It's a targeted fix for 'too many native views' memory pressure, not a default-on optimization.</p><p><b>Red flag:</b> flipping removeClippedSubviews on by default — it has its own per-frame clip/unclip cost and can break absolutely-positioned or custom native children; reach for it only when profiling shows view count is the actual memory pressure.</p><p><b>"removeClippedSubviews trades native view count for per-frame clip/unclip cost — I turn it on when profiling shows view count is the bottleneck, not as a default."</b></p>`,
  },
];

export const ADVANCED12_QUIZ: QuizQuestion[] = [
  {
    id: "a12q-list-perf-1",
    category: "perf",
    categoryLabel: "PERF",
    question: `An app shows smooth native-driven scroll animation, but tapping a button produces no visible state change for several seconds. What does this combination most strongly indicate?`,
    options: [`The GPU is the bottleneck`, `The JS thread is blocked while the native/UI thread is free`, `The native/UI thread is blocked while the JS thread is free`, `The Metro bundler is still building`],
    answer: 1,
    explanationHtml: `<p>Native-driven scroll/animation runs independent of JS, so it staying smooth rules out a native/UI thread stall. A tap that registers (native handles the touch) but produces no JS-driven effect points to the JS thread being busy with synchronous work and unable to process the event queue.</p>`,
  },
  {
    id: "a12q-list-perf-2",
    category: "perf",
    categoryLabel: "PERF",
    question: `What is the primary cold-start benefit Hermes bytecode precompilation provides over shipping plain JS to the device?`,
    options: [`It reduces the number of native modules linked into the app`, `It eliminates the on-device parse and bytecode-generation step by doing it at build time`, `It automatically removes unused JS code from the bundle`, `It compresses the JS bundle to a smaller download size`],
    answer: 1,
    explanationHtml: `<p>Hermes' compiler (hermesc) runs at build time to produce HBC, so the Hermes VM on-device just loads and executes bytecode instead of parsing source and generating bytecode itself — that parse/compile step is what's eliminated from the critical path, not bundle download size or dead-code elimination (those are separate concerns).</p>`,
  },
  {
    id: "a12q-list-perf-3",
    category: "perf",
    categoryLabel: "PERF",
    question: `A list row component is wrapped in \`React.memo\`, but it still re-renders on every parent re-render. The row receives \`onPress={() => onSelect(item.id)}\` as a prop. What's the most likely cause?`,
    options: [`React.memo does not work on functional components that use hooks`, `The inline arrow function creates a new reference every render, failing memo's shallow prop comparison`, `FlatList disables memoization for all renderItem children`, `The item.id value is changing on every render`],
    answer: 1,
    explanationHtml: `<p>React.memo does a shallow comparison of props; an inline arrow function literal is a new object reference on every render regardless of whether item.id or onSelect actually changed, so the equality check fails and the row re-renders anyway. Wrapping the handler in useCallback (or hoisting it) fixes this. React.memo works the same on components using hooks as on any other functional component — that option is a distractor unrelated to the actual cause, which is purely prop reference identity.</p>`,
  },
  {
    id: "a12q-list-perf-4",
    category: "perf",
    categoryLabel: "PERF",
    question: `Why might enabling Hermes bytecode compilation show little to no improvement in a bundle-size analyzer report, even though cold-start CPU time improves?`,
    options: [`Bundle analyzers are incompatible with Hermes and report inaccurate numbers`, `Hermes bytecode is typically similar in size to minified JS and analyzers usually visualize the pre-Hermes Metro bundle anyway`, `Hermes bytecode compilation is a no-op unless inlineRequires is also enabled`, `Bundle analyzers only measure native binary size, not JS`],
    answer: 1,
    explanationHtml: `<p>Bundle size (download/storage bytes) and cold-start CPU cost (parse+compile time) are different axes. Analyzers work on the structured Metro JS output because HBC lacks module-level structure for visualization, and HBC isn't reliably smaller than minified JS — it trades textual compactness for faster execution, so size metrics don't move much even though real device startup time does.</p>`,
  },
  {
    id: "a12q-list-perf-5",
    category: "perf",
    categoryLabel: "PERF",
    question: `What does Metro's \`inlineRequires\` transform optimize for, and what's its main limitation?`,
    options: [`It reduces runtime list re-render cost by deferring renderItem evaluation`, `It defers module evaluation to first use, helping cold start, but does nothing for steady-state runtime performance`, `It compiles JS directly to native ARM code, replacing Hermes bytecode`, `It automatically splits the bundle into per-route chunks for code-splitting`],
    answer: 1,
    explanationHtml: `<p>inlineRequires rewrites eager top-level imports into lazy, call-site require()s, so modules that aren't used on the first screen don't run their top-level side effects at launch — directly helping cold start. It has no effect once a module is actually used, since at that point it runs in full just as it always would have, and it doesn't perform route-based code-splitting (that needs React.lazy / dynamic import infrastructure).</p>`,
  },
  {
    id: "a12q-list-perf-6",
    category: "perf",
    categoryLabel: "PERF",
    question: `In React Native's New Architecture, why does Reanimated's worklet-driven \`useAnimatedStyle\` avoid the re-render cost that an equivalent \`useState\`-driven animation would incur?`,
    options: [`Worklets run on the UI thread and mutate shared values that update native view props directly, without going through React's JS-thread render/reconcile cycle`, `Worklets are precompiled by Hermes into native code that bypasses JavaScript entirely`, `useState-driven animations are batched automatically so there's no actual difference`, `Reanimated disables React DevTools profiling during animations`],
    answer: 0,
    explanationHtml: `<p>Worklets execute on the UI thread and write directly to the native view's props through Fabric, so no React Fiber commit happens per animation frame. A useState+setInterval animation, by contrast, forces a full JS-thread render and reconciliation pass on every frame, which is both costlier and more jank-prone at 60fps. Worklets aren't compiled to native machine code either — they still run as JS, just on the UI thread's own runtime, decoupled from React's commit cycle rather than replaced by it.</p>`,
  },
  {
    id: "a12q-list-perf-7",
    category: "perf",
    categoryLabel: "PERF",
    question: `FlatList's \`windowSize\` prop is reduced from the default to save memory on a long list. What's the direct tradeoff?`,
    options: [`Initial render time increases substantially`, `Memory usage actually increases due to more frequent re-mounting`, `On fast scroll flings, users may briefly see blank/unrendered cells because rendering can't keep up with scroll velocity`, `It has no visible effect since FlatList always renders the full list internally`],
    answer: 2,
    explanationHtml: `<p>windowSize controls how many viewport-heights of content stay rendered around the visible area. Shrinking it lowers memory and off-screen render cost, but during a fast fling the renderer may not fill in new cells before they're scrolled into view, producing visible blank gaps — a direct memory-vs-blank-cell tradeoff rather than a free optimization. A smaller windowSize doesn't cause extra re-mounting or increase memory — it reduces resident cell count; the cost shows up as scroll-fling gaps, not memory growth.</p>`,
  },
];
