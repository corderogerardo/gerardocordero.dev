// Batch 7 - in-browser & on-device AI (Chrome Prompt API, Transformers.js, WebGPU).
// Authored + adversarially verified via workflow.
import type { Flashcard } from "./flashcards";
import type { StudySection } from "./study";
import type { Prompt } from "./prompts";

export const ADVANCED7_FLASHCARDS: Flashcard[] = [
  {
    "id": "web-ai-1",
    "category": "ai",
    "categoryLabel": "AI",
    "question": "What is Chrome's Built-in AI Prompt API, and how do you feature-detect availability and create a session with a download monitor?",
    "answerHtml": "<p>The <b>Prompt API</b> is a Chrome built-in AI surface that runs <b>Gemini Nano on-device</b>, exposed via the global <code>window.LanguageModel</code>. No network call, no API key, no per-token cost.</p><p>The flow is always: <b>feature-detect &rarr; check availability &rarr; create &rarr; prompt</b>. Pass the <i>same</i> options object to <code>availability()</code> and <code>create()</code> so the readiness check matches the session you build.</p><div class=\"code\">if (!(&quot;LanguageModel&quot; in self)) {\n  // fall back to a cloud model\n} else {\n  const opts = { /* same for both calls */ };\n  const status = await LanguageModel.availability(opts);\n  // &quot;unavailable&quot; | &quot;downloadable&quot; | &quot;downloading&quot; | &quot;available&quot;\n  if (status !== &quot;unavailable&quot;) {\n    const session = await LanguageModel.create({\n      ...opts,\n      monitor(m) {\n        m.addEventListener(&quot;downloadprogress&quot;, e =&gt;\n          console.log(`model ${Math.round(e.loaded * 100)}%`));\n      },\n    });\n    const answer = await session.prompt(&quot;Summarize: ...&quot;);\n  }\n}</div><p>The <code>monitor</code> hook matters because the first <code>create()</code> may trigger a multi-hundred-MB model download; surface progress to the user instead of appearing frozen. For token-by-token output use <code>session.promptStreaming(text)</code>, which returns a <code>ReadableStream</code>.</p>",
    "level": "senior"
  },
  {
    "id": "web-ai-2",
    "category": "ai",
    "categoryLabel": "AI",
    "question": "What is Gemini Nano, where does it run, and what are its practical constraints versus a cloud model?",
    "answerHtml": "<p><b>Gemini Nano</b> is Google's smallest Gemini model, designed to run <b>fully on-device</b>. In the browser it powers Chrome's Built-in AI APIs (Prompt, Summarizer, etc.). The weights ship to the device once and inference happens locally on the GPU/NPU.</p><ul><li><b>Chrome-only</b> (desktop primarily); Chrome 149+ added several languages. Not a cross-browser web standard yet, so always feature-detect.</li><li><b>Hardware needs:</b> sufficient disk for the model download, enough VRAM/unified memory, and a capable GPU. On unsupported hardware <code>availability()</code> returns <code>&quot;unavailable&quot;</code>.</li><li><b>Capability ceiling:</b> a small model. Great for summarization, rewriting, classification, short Q&amp;A; weaker than frontier cloud models on long-context reasoning and world knowledge.</li></ul><div class=\"code\">// Embedded iframes must opt in:\n&lt;iframe src=&quot;...&quot; allow=&quot;language-model&quot;&gt;&lt;/iframe&gt;</div><p>The win is privacy, offline use, and zero per-call cost; the cost is a download, device compute, and a lower quality ceiling than the cloud.</p>",
    "level": "senior"
  },
  {
    "id": "web-ai-3",
    "category": "ai",
    "categoryLabel": "AI",
    "question": "Beyond the Prompt API, what task-specific Built-in AI APIs does Chrome expose, and why prefer them over raw prompting?",
    "answerHtml": "<p>Chrome ships several <b>task-specific Built-in AI APIs</b>, each on-device and each with the same <code>availability()</code> / <code>create()</code> lifecycle:</p><ul><li><b>Summarizer</b> &mdash; condense text (TL;DR, key points, headlines).</li><li><b>Writer</b> &mdash; generate new prose from a prompt.</li><li><b>Rewriter</b> &mdash; rephrase, change tone, expand or shorten.</li><li><b>Translator</b> &mdash; translate between languages on-device.</li><li><b>LanguageDetector</b> &mdash; detect the language of input text.</li></ul><div class=\"code\">if (&quot;Summarizer&quot; in self) {\n  const s = await Summarizer.create({ type: &quot;tldr&quot;, length: &quot;short&quot; });\n  const tldr = await s.summarize(longArticle);\n}\n\nif (&quot;Translator&quot; in self) {\n  const t = await Translator.create({\n    sourceLanguage: &quot;en&quot;, targetLanguage: &quot;es&quot;,\n  });\n  const out = await t.translate(&quot;Hello world&quot;);\n}</div><p>Prefer these over hand-rolled prompts: they are tuned for the task, give structured options instead of prompt-engineering, and degrade predictably. You still feature-detect and fall back, because support and downloaded models vary per device.</p>",
    "level": "senior"
  },
  {
    "id": "web-ai-4",
    "category": "ai",
    "categoryLabel": "AI",
    "question": "How does Transformers.js v3 let you run models in the browser, and how do you produce text embeddings with it?",
    "answerHtml": "<p><b>Transformers.js v3</b> (npm <code>@huggingface/transformers</code>) runs Hugging Face models directly in the browser on <b>ONNX Runtime Web</b>, with a WebGPU backend (falling back to WASM). Unlike Chrome Built-in AI, it is <b>cross-browser</b> and you choose the model.</p><p>The high-level entry point is <code>pipeline(task, model, options)</code>. For semantic search you want <code>feature-extraction</code> with a small embedding model like MiniLM (384-dim):</p><div class=\"code\">import { pipeline } from &quot;@huggingface/transformers&quot;;\n\nconst extractor = await pipeline(\n  &quot;feature-extraction&quot;,\n  &quot;Xenova/all-MiniLM-L6-v2&quot;,\n  { device: &quot;webgpu&quot;, dtype: &quot;q8&quot; }   // GPU + 8-bit quantized weights\n);\n\nconst out = await extractor(&quot;a sentence to embed&quot;, {\n  pooling: &quot;mean&quot;,   // pool token vectors into one\n  normalize: true,  // L2-normalize -&gt; cosine == dot product\n});\nconst embedding = out.tolist()[0];  // number[] of length 384</div><p>Key knobs: <code>device: &quot;webgpu&quot;</code> for GPU acceleration, <code>dtype: &quot;q8&quot;</code> to shrink the model, and <code>normalize: true</code> so similarity is a plain dot product.</p>",
    "level": "senior"
  },
  {
    "id": "web-ai-5",
    "category": "ai",
    "categoryLabel": "AI",
    "question": "What is WebGPU and why is it the enabling technology for serious in-browser AI?",
    "answerHtml": "<p><b>WebGPU</b> is the modern browser GPU API (successor to WebGL) that exposes general-purpose <b>compute shaders</b> to JavaScript. It is what makes heavy ML inference in a tab practical.</p><ul><li><b>Compute, not just graphics:</b> the matrix multiplies behind transformers run on the GPU instead of the CPU, often an order of magnitude faster than WASM/CPU.</li><li><b>Foundation layer:</b> Transformers.js (<code>device: &quot;webgpu&quot;</code>), ONNX Runtime Web's WebGPU EP, and WebLLM all rely on it.</li><li><b>Availability is the catch:</b> needs a recent browser and capable hardware. Always detect and fall back to WASM/CPU.</li></ul><div class=\"code\">if (&quot;gpu&quot; in navigator) {\n  const adapter = await navigator.gpu.requestAdapter();\n  if (adapter) { /* use device: &quot;webgpu&quot; */ }\n  else { /* fall back to wasm */ }\n} else {\n  // older browser: device: &quot;wasm&quot;\n}</div><p>Rule of thumb: WebGPU present &rarr; you can run real models locally; absent &rarr; you can still run tiny ones on WASM, just slower.</p>",
    "level": "senior"
  },
  {
    "id": "web-ai-6",
    "category": "ai",
    "categoryLabel": "AI",
    "question": "What is ONNX Runtime Web and what role does it play under Transformers.js?",
    "answerHtml": "<p><b>ONNX Runtime Web</b> is the in-browser build of Microsoft's ONNX Runtime inference engine. Models are exported to the open <b>ONNX</b> graph format, and ORT Web executes that graph in the browser.</p><p>It is the <b>execution layer beneath Transformers.js</b>: when you call a <code>pipeline</code>, Transformers.js loads ONNX weights and hands the graph to ORT Web to run. ORT Web picks an <b>execution provider</b>:</p><ul><li><b>WebGPU EP</b> &mdash; GPU compute, fastest, needs WebGPU support.</li><li><b>WASM EP</b> &mdash; CPU via WebAssembly (often multi-threaded with SIMD), the universal fallback.</li></ul><div class=\"code\">// Transformers.js exposes ORT settings via env:\nimport { env } from &quot;@huggingface/transformers&quot;;\nenv.backends.onnx.wasm.numThreads = 4;  // tune WASM threads</div><p>So the stack is: <b>your model (ONNX) &rarr; ONNX Runtime Web &rarr; WebGPU or WASM</b>. Transformers.js is the friendly API; ORT Web does the actual tensor math.</p>",
    "level": "senior"
  },
  {
    "id": "web-ai-7",
    "category": "ai",
    "categoryLabel": "AI",
    "question": "What is WebLLM / MLC, what class of models does it run, and when is that a reasonable choice?",
    "answerHtml": "<p><b>WebLLM</b> (<code>@mlc-ai/web-llm</code>) runs <b>full chat LLMs</b> &mdash; Llama, Phi, Qwen, Mistral &mdash; entirely in the browser on <b>WebGPU</b>, compiled via the <b>MLC</b> (Machine Learning Compilation) toolchain. This goes far beyond tiny embedding models: it is a real generative LLM in a tab.</p><div class=\"code\">import { CreateMLCEngine } from &quot;@mlc-ai/web-llm&quot;;\n\nconst engine = await CreateMLCEngine(\n  &quot;Llama-3.1-8B-Instruct-q4f32_1-MLC&quot;,\n  { initProgressCallback: (p) =&gt; console.log(p.text) }\n);\nconst reply = await engine.chat.completions.create({\n  messages: [{ role: &quot;user&quot;, content: &quot;Explain WebGPU&quot; }],\n});</div><p><b>The cost:</b> a <b>multi-GB model download</b> (cache it aggressively), high VRAM needs, and slower first token than a cloud API.</p><p><b>Choose it when</b> privacy is paramount (data must never leave the device), you need offline capability, or you want zero marginal cost at scale &mdash; and your users have capable GPUs. Otherwise the cloud is usually faster and lighter for the end user.</p>",
    "level": "beyond"
  },
  {
    "id": "web-ai-8",
    "category": "ai",
    "categoryLabel": "AI",
    "question": "How do you build in-browser semantic search with embeddings and cosine similarity?",
    "answerHtml": "<p><b>Semantic search</b> ranks documents by <i>meaning</i>, not keyword overlap. The recipe: embed every document once, embed the query, and rank by <b>cosine similarity</b>.</p><p>If you L2-<b>normalize</b> the vectors at embedding time (<code>normalize: true</code> in Transformers.js), cosine similarity reduces to a plain <b>dot product</b> &mdash; a cheap loop.</p><div class=\"code\">// 1. Embed docs once (cache the vectors)\nconst docVecs = await Promise.all(\n  docs.map(d =&gt; embed(d))   // each a normalized 384-dim array\n);\n\n// 2. Embed the query\nconst q = await embed(query);\n\n// 3. Rank by dot product (== cosine for normalized vectors)\nconst dot = (a, b) =&gt; a.reduce((s, x, i) =&gt; s + x * b[i], 0);\nconst ranked = docVecs\n  .map((v, i) =&gt; ({ i, score: dot(q, v) }))\n  .sort((a, b) =&gt; b.score - a.score)\n  .slice(0, 5);   // top-k</div><p>For a few thousand docs a linear scan is fine in JS. Beyond that, precompute and store vectors (IndexedDB) and consider an ANN index. The whole thing runs offline with no server.</p>",
    "level": "senior"
  },
  {
    "id": "web-ai-9",
    "category": "ai",
    "categoryLabel": "AI",
    "question": "How would you implement RAG entirely in the browser, and what are the moving parts?",
    "answerHtml": "<p><b>Browser RAG</b> = retrieval-augmented generation with both the retriever and the generator running on-device. No server, no data leaving the machine.</p><p>Pipeline:</p><ol><li><b>Index:</b> chunk your docs, embed each chunk with Transformers.js (<code>feature-extraction</code>, MiniLM), store vectors in IndexedDB.</li><li><b>Retrieve:</b> embed the user query, rank chunks by cosine similarity, take top-k.</li><li><b>Augment + generate:</b> stuff the top-k chunks into a prompt and run a local LLM &mdash; either Chrome's Prompt API (Gemini Nano) or WebLLM.</li></ol><div class=\"code\">const top = retrieve(query, 4);              // semantic search, top-k = 4\nconst context = top.map(t =&gt; t.text).join(&quot;\\n---\\n&quot;);\n\nconst session = await LanguageModel.create();\nconst answer = await session.prompt(\n  `Answer using ONLY this context:\\n${context}\\n\\nQ: ${query}`\n);</div><p><b>Tradeoffs:</b> grounded, private, offline &mdash; but the local model's small context window limits how many chunks you can pass, and Gemini Nano's reasoning is weaker than a cloud model. Tight chunking and good retrieval matter more here than in cloud RAG.</p>",
    "level": "beyond"
  },
  {
    "id": "web-ai-10",
    "category": "ai",
    "categoryLabel": "AI",
    "question": "What is model quantization (q8 vs q4) and how does it trade size against quality for in-browser models?",
    "answerHtml": "<p><b>Quantization</b> stores model weights in fewer bits than the original fp32/fp16. This is the single biggest lever for shipping models to the browser, where download size and memory are the real constraints.</p><ul><li><b>q8</b> (8-bit) &mdash; ~4x smaller than fp32, with minimal quality loss. A safe default for embeddings (<code>dtype: &quot;q8&quot;</code>).</li><li><b>q4</b> (4-bit) &mdash; ~8x smaller, faster, lower memory, but a more noticeable quality drop. Common for big generative LLMs in WebLLM where the fp16 model simply wouldn't fit.</li></ul><div class=\"code\">// Transformers.js: pick precision per model\nconst extractor = await pipeline(\n  &quot;feature-extraction&quot;,\n  &quot;Xenova/all-MiniLM-L6-v2&quot;,\n  { device: &quot;webgpu&quot;, dtype: &quot;q8&quot; }   // try &quot;q4&quot; if size-bound\n);</div><p><b>Rule of thumb:</b> embeddings &rarr; q8 (cheap accuracy). Large chat LLMs &rarr; q4 to make the download/VRAM feasible, accepting some quality loss. Always measure on <i>your</i> task &mdash; quantization sensitivity varies by model.</p>",
    "level": "senior"
  },
  {
    "id": "web-ai-11",
    "category": "ai",
    "categoryLabel": "AI",
    "question": "How do you keep in-browser inference from freezing the UI, and how do you avoid re-downloading models on every visit?",
    "answerHtml": "<p>Two production concerns: <b>don't block the main thread</b> and <b>don't re-download weights</b>.</p><p><b>Run inference in a Web Worker.</b> Model loading and the inference loop are CPU/GPU-heavy and synchronous enough to jank the UI. Move them off the main thread and post results back.</p><div class=\"code\">// worker.js\nimport { pipeline } from &quot;@huggingface/transformers&quot;;\nlet extractor;\nself.onmessage = async (e) =&gt; {\n  extractor ??= await pipeline(\n    &quot;feature-extraction&quot;, &quot;Xenova/all-MiniLM-L6-v2&quot;,\n    { device: &quot;webgpu&quot;, dtype: &quot;q8&quot; });\n  const out = await extractor(e.data, { pooling: &quot;mean&quot;, normalize: true });\n  self.postMessage(out.tolist()[0]);\n};\n\n// main thread\nconst w = new Worker(&quot;worker.js&quot;, { type: &quot;module&quot; });\nw.postMessage(&quot;embed this&quot;);\nw.onmessage = (e) =&gt; useEmbedding(e.data);</div><p><b>Cache the model.</b> Transformers.js caches weights in the browser <b>Cache API</b> by default so the second visit is instant; you can also persist computed embeddings in <b>IndexedDB</b>. For Chrome Built-in AI, Gemini Nano is downloaded and managed by the browser itself, so subsequent <code>create()</code> calls reuse it. Surface the first-load <code>downloadprogress</code> so users understand the one-time wait.</p>",
    "level": "senior"
  },
  {
    "id": "web-ai-12",
    "category": "ai",
    "categoryLabel": "AI",
    "question": "Make the honest case for in-browser/on-device AI versus the cloud. When should you pick each?",
    "answerHtml": "<p><b>The case FOR on-device/in-browser AI:</b></p><ul><li><b>Privacy:</b> data never leaves the device &mdash; huge for health, legal, personal notes, and regulated domains.</li><li><b>Offline:</b> works with no network once the model is cached.</li><li><b>Zero marginal cost:</b> no per-token API bill; cost scales with your users' hardware, not your server budget.</li><li><b>Low latency</b> for small models &mdash; no round trip.</li></ul><p><b>The honest tradeoffs:</b></p><ul><li><b>Download size:</b> hundreds of MB (Gemini Nano, MiniLM) to multiple GB (WebLLM). A real first-load tax.</li><li><b>WebGPU availability:</b> not universal; older browsers/devices fall back to slow WASM or can't run larger models at all.</li><li><b>Device compute &amp; battery:</b> you're spending the user's GPU and power.</li><li><b>Quality ceiling:</b> small/quantized local models trail frontier cloud models on hard reasoning and long context.</li></ul><p><b>Choose cloud when</b> you need top-tier reasoning, long context, consistent behavior across all devices, or large models you can't ask users to download. <b>Choose on-device when</b> privacy/offline/cost dominate and the task fits a small model. Best practice: <b>feature-detect and degrade gracefully</b> &mdash; local when capable, cloud otherwise.</p>",
    "level": "beyond"
  }
];

export const ADVANCED7_STUDY: StudySection[] = [
  {
    "id": "st-38",
    "num": "38",
    "title": "38 · AI in the browser & on-device",
    "html": "<p><b>The shift:</b> models that used to live behind an API now run <i>locally</i> — in the browser tab, on the user's GPU, with nothing leaving the device. As a React Native / web engineer you should be able to place any feature on the on-device-vs-cloud spectrum and name the right runtime. There are two distinct families:</p>\n\n<ul>\n  <li><b>Small task models — Transformers.js v3</b> (<code>@huggingface/transformers</code>). Runs Hugging Face models in JS on <b>ONNX Runtime Web</b>, accelerated by <b>WebGPU</b> (fallback to WASM). Great for <i>feature-extraction</i> (embeddings, e.g. <code>Xenova/all-MiniLM-L6-v2</code> &rarr; 384-dim vectors), text classification / sentiment, NER, zero-shot, and <i>ASR</i> (Whisper). Models are tens-to-hundreds of MB; <b>quantize</b> with <code>dtype: &quot;q8&quot;</code> / <code>&quot;q4&quot;</code> to shrink them at a small quality cost.</li>\n  <li><b>Full LLMs in the browser.</b> Two routes: (1) <b>Chrome Built-in AI / Prompt API</b> exposes <b>Gemini Nano</b> on-device via the global <code>window.LanguageModel</code> — no weights you ship, the browser manages the download. Sibling task APIs (each with its own <code>availability()</code> / <code>create()</code>): <code>Summarizer</code>, <code>Writer</code>, <code>Rewriter</code>, <code>Translator</code>, <code>LanguageDetector</code>. (2) <b>WebLLM / @mlc-ai/web-llm</b> runs full open models (Llama, Phi, Qwen) compiled by MLC onto WebGPU — multi-GB download, but any model you want, in any Chromium/WebGPU browser.</li>\n</ul>\n\n<p><b>The killer pattern — in-browser semantic search &amp; RAG.</b> Embed your content once (each doc &rarr; a normalized vector), embed the user's query the same way, then rank docs by <b>cosine similarity</b> — which, for L2-normalized vectors, is just a dot product. That's a privacy-preserving search engine with zero backend. Add a local LLM and you have <b>RAG in the browser</b>: retrieve top-k similar chunks, stuff them into a prompt, generate the answer on-device.</p>\n\n<div class=\"code\">import { pipeline } from &quot;@huggingface/transformers&quot;;\n\n// 1) Build an embedder once (WebGPU, quantized). Do this in a Web Worker.\nconst embed = await pipeline(\n  &quot;feature-extraction&quot;,\n  &quot;Xenova/all-MiniLM-L6-v2&quot;,\n  { device: &quot;webgpu&quot;, dtype: &quot;q8&quot; }\n);\n\nasync function vector(text: string): Promise&lt;number[]&gt; {\n  const out = await embed(text, { pooling: &quot;mean&quot;, normalize: true });\n  return out.tolist()[0] as number[]; // 384-dim, L2-normalized\n}\n\n// 2) cosine sim === dot product when both are L2-normalized\nconst cos = (a: number[], b: number[]) =&gt;\n  a.reduce((s, x, i) =&gt; s + x * b[i], 0);\n\n// 3) Rank a corpus against a query\nconst docVecs = await Promise.all(docs.map(vector));\nconst q = await vector(&quot;how do I cache models offline?&quot;);\nconst ranked = docs\n  .map((d, i) =&gt; ({ d, score: cos(q, docVecs[i]) }))\n  .sort((a, b) =&gt; b.score - a.score);</div>\n\n<p><b>Feature-detect and degrade gracefully — always.</b> WebGPU and the Prompt API are not everywhere, and the model may need to download first. Never assume; probe, then fall back to WASM, to a cloud call, or to a plain-text experience.</p>\n\n<div class=\"code\">// WebGPU present?\nconst hasGPU =\n  &quot;gpu&quot; in navigator &amp;&amp; !!(await navigator.gpu?.requestAdapter());\n\n// Chrome Built-in AI (Prompt API). Pass the SAME options to availability() + create().\nif (&quot;LanguageModel&quot; in self) {\n  // &quot;unavailable&quot; | &quot;downloadable&quot; | &quot;downloading&quot; | &quot;available&quot;\n  const status = await LanguageModel.availability();\n  if (status !== &quot;unavailable&quot;) {\n    const session = await LanguageModel.create({\n      monitor(m) {\n        m.addEventListener(&quot;downloadprogress&quot;, (e) =&gt;\n          console.log(e.loaded)\n        );\n      },\n    });\n    const stream = session.promptStreaming(&quot;Summarize this changelog…&quot;);\n    for await (const chunk of stream) render(chunk);\n  }\n} else {\n  // graceful fallback: cloud API or non-AI UI\n}</div>\n\n<ul>\n  <li><b>Run inference off the main thread.</b> Load the pipeline and run <code>prompt()</code> / <code>embed()</code> inside a <b>Web Worker</b> so model download and GPU work never block the UI thread (no jank — the same instinct as keeping work off the JS thread in RN).</li>\n  <li><b>Cache the weights.</b> Transformers.js caches downloaded models via the browser <b>Cache API / IndexedDB</b>, so the multi-MB download is a one-time cost — subsequent loads are offline-instant.</li>\n  <li><b>Embedded contexts:</b> an <code>&lt;iframe&gt;</code> needs <code>allow=&quot;language-model&quot;</code> to use the Prompt API.</li>\n</ul>\n\n<div class=\"callout tip\"><span class=\"lbl\">New concept</span> <b>Cosine similarity is the whole trick.</b> An embedding model maps text into a vector where <i>direction encodes meaning</i>. Two pieces of text are &quot;similar&quot; when their vectors point the same way — measured by cosine of the angle between them. If you <b>L2-normalize</b> every vector at creation time (<code>normalize: true</code>), the cosine collapses to a plain <b>dot product</b>: one multiply-add loop, no square roots at query time. That's why in-browser semantic search over a few hundred docs is effectively free — embed once, then it's just arithmetic.</div>\n\n<p><b>The decision — on-device vs cloud.</b> Reach for <b>on-device</b> when you want: <i>privacy</i> (data never leaves the device), <i>offline</i> capability, <i>zero per-call cost</i>, and <i>low latency</i> for small models. Reach for the <b>cloud</b> when you need <i>frontier quality</i> (the largest models), <i>huge context windows</i>, or to avoid pushing a multi-GB download and heavy GPU/battery load onto the user's device. A common shape is hybrid: embeddings + retrieval + light tasks on-device, escalate only the hard generation to a cloud LLM.</p>\n\n<div class=\"map\"><span class=\"lbl\">Your proof</span> Your CV already lists <b>on-device AI</b>, and your monorepo ships <code>@huggingface/transformers</code> — so you can speak to this from real code, not theory. Frame it in interviews as: &quot;I added in-browser feature-extraction with Transformers.js on WebGPU, ran it in a Web Worker with cached weights, and ranked results by cosine similarity — private, offline, zero per-call cost.&quot; Tie it to <b>Hermes</b>: the same discipline you used keeping work off the JS thread for smooth RN frames is exactly why on-device inference belongs in a Worker. That story — <i>place the model on the privacy/latency/cost spectrum, then pick the runtime</i> — is what separates a senior answer from a buzzword answer.</div>"
  }
];

export const ADVANCED7_PROMPTS: Prompt[] = [
  {
    "id": "code-11",
    "kind": "coding",
    "level": "senior",
    "title": "In-browser semantic search ranking",
    "tags": [
      "in-browser-ai",
      "semantic-search",
      "embeddings",
      "cosine-similarity",
      "typescript"
    ],
    "promptHtml": "<p>You are building an <b>offline, privacy-first semantic search</b> over a small set of help articles, entirely in the browser. At build/index time you already produced an embedding for each document using a MiniLM model (Transformers.js, <code>Xenova/all-MiniLM-L6-v2</code>, 384 dimensions). At query time you embed the user's query with the <i>same</i> model and the <i>same</i> pooling/normalization settings.</p><p>Given an array of documents, each with a precomputed embedding, and a single query embedding, write the ranking layer in <b>TypeScript</b>:</p><ul><li>Implement <code>cosineSimilarity(a, b)</code> over two numeric vectors.</li><li>Implement a <code>topK</code> ranker that returns the <code>k</code> most relevant documents, each with its similarity score, sorted descending.</li></ul><p>Assume embeddings are produced with <code>{ pooling: &quot;mean&quot;, normalize: true }</code>. Explain how that assumption affects your similarity math, and call out the edge cases you would guard against.</p>",
    "reveal": [
      {
        "label": "Approach",
        "html": "<ul><li><b>Similarity metric:</b> semantic relevance is measured by the angle between vectors, so <b>cosine similarity</b> is the right choice: <code>dot(a,b) / (||a|| * ||b||)</code>. It ignores magnitude and compares direction, which is what embedding spaces encode.</li><li><b>Exploit normalization:</b> Transformers.js with <code>normalize: true</code> returns <b>L2-normalized</b> vectors (unit length). For unit vectors the denominator is 1, so cosine similarity collapses to a plain <b>dot product</b>. That is faster and avoids per-call norm computation. I still write a general <code>cosineSimilarity</code> for safety/correctness, but the hot path can be a dot product when I trust the normalization.</li><li><b>Same model + same options:</b> the query and documents MUST be embedded with the same model, pooling, and dtype, or the vectors live in different spaces and scores are meaningless. This is the most common correctness bug.</li><li><b>topK, not full sort of huge data:</b> for a small corpus a full sort is fine (clear, O(n log n)). For larger corpora I would keep a bounded min-heap of size k for O(n log k). I note the tradeoff but keep the small-corpus version readable.</li><li><b>Edge cases:</b> mismatched vector lengths (throw early), zero vectors (avoid divide-by-zero, return 0), <code>k</code> larger than the corpus (clamp), and NaN guards. Cosine ranges from -1 to 1; for normalized text embeddings scores are typically positive but I do not assume it.</li><li><b>Performance note:</b> run the embedding model in a <b>Web Worker</b> so the UI thread stays free; the ranking math here is cheap and can stay on the main thread.</li></ul>"
      },
      {
        "label": "Solution",
        "html": "<div class=\"code\">export type Doc = {\n  id: string;\n  text: string;\n  embedding: number[]; // 384-dim, L2-normalized at index time\n};\n\nexport type Ranked = { doc: Doc; score: number };\n\n/**\n * Cosine similarity = dot(a, b) / (||a|| * ||b||).\n * For L2-normalized vectors this equals the dot product,\n * but we compute it generally so callers can&#39;t be bitten\n * by un-normalized inputs.\n */\nexport function cosineSimilarity(a: number[], b: number[]): number {\n  if (a.length !== b.length) {\n    throw new Error(\n      `Vector length mismatch: ${a.length} vs ${b.length}`,\n    );\n  }\n  let dot = 0;\n  let normA = 0;\n  let normB = 0;\n  for (let i = 0; i &lt; a.length; i++) {\n    const x = a[i];\n    const y = b[i];\n    dot += x * y;\n    normA += x * x;\n    normB += y * y;\n  }\n  const denom = Math.sqrt(normA) * Math.sqrt(normB);\n  if (denom === 0) return 0; // guard zero vectors\n  return dot / denom;\n}\n\n/**\n * Rank documents against a query embedding and return the\n * top-k by descending similarity. `queryEmbedding` must come\n * from the SAME model + pooling/normalize options used to\n * build the doc embeddings.\n */\nexport function topK(\n  queryEmbedding: number[],\n  docs: Doc[],\n  k: number,\n): Ranked[] {\n  const limit = Math.max(0, Math.min(k, docs.length));\n  if (limit === 0) return [];\n\n  const scored: Ranked[] = docs.map((doc) =&gt; ({\n    doc,\n    score: cosineSimilarity(queryEmbedding, doc.embedding),\n  }));\n\n  scored.sort((p, q) =&gt; q.score - p.score);\n  return scored.slice(0, limit);\n}\n\n// --- usage (query side) ---\n// import { pipeline } from &quot;@huggingface/transformers&quot;;\n// const extractor = await pipeline(\n//   &quot;feature-extraction&quot;,\n//   &quot;Xenova/all-MiniLM-L6-v2&quot;,\n//   { device: &quot;webgpu&quot;, dtype: &quot;q8&quot; },\n// );\n// const out = await extractor(query, {\n//   pooling: &quot;mean&quot;,\n//   normalize: true,\n// });\n// const queryEmbedding = out.tolist()[0] as number[];\n// const results = topK(queryEmbedding, docs, 5);</div>"
      }
    ]
  },
  {
    "id": "code-12",
    "kind": "coding",
    "level": "senior",
    "title": "Use Chrome's Prompt API with graceful fallback",
    "tags": [
      "in-browser-ai",
      "prompt-api",
      "gemini-nano",
      "feature-detection",
      "typescript"
    ],
    "promptHtml": "<p>Chrome ships an <b>on-device Prompt API</b> backed by Gemini Nano, exposed as <code>window.LanguageModel</code>. The model may be unavailable, downloadable (needs a one-time download), downloading, or available, and the global may not exist at all in other browsers.</p><p>Write a <b>TypeScript</b> async helper, <code>askOnDevice(prompt)</code>, that:</p><ul><li>Feature-detects <code>window.LanguageModel</code>.</li><li>Calls <code>availability()</code> and handles each state.</li><li>Creates a session, wiring a <code>downloadprogress</code> monitor so a multi-megabyte model download can report progress.</li><li>Prompts the model and returns the text.</li><li>Throws a clear, typed error (or signals a fallback) when on-device inference is not possible — so the caller can fall back to a cloud API.</li></ul><p>Be explicit about passing the <i>same options</i> to <code>availability()</code> and <code>create()</code>, and about why feature detection matters here.</p>",
    "reveal": [
      {
        "label": "Approach",
        "html": "<ul><li><b>Feature-detect first, never assume:</b> <code>window.LanguageModel</code> only exists on recent Chrome desktop. On every other browser the global is <code>undefined</code>, so I guard with <code>typeof</code> / <code>&quot;LanguageModel&quot; in self</code> before touching it. No try/catch as a substitute for detection.</li><li><b>availability() is a gate, not a boolean:</b> it returns <code>&quot;unavailable&quot;</code> | <code>&quot;downloadable&quot;</code> | <code>&quot;downloading&quot;</code> | <code>&quot;available&quot;</code>. I branch on all four. <code>unavailable</code> means fall back; <code>downloadable</code>/<code>downloading</code> mean a session creation will trigger or join a download; <code>available</code> means ready now.</li><li><b>Same options to availability() and create():</b> the API keys capability on the options you pass &mdash; <code>expectedInputs</code> and <code>expectedOutputs</code>, each an array of <code>{ type, languages }</code>. Recent Chrome <i>requires</i> an output language: omit it and it warns, and can refuse to emit text in an &quot;untested&quot; language (<code>NotSupportedError</code>) &mdash; so the prompt silently fails. If you check availability with one set of options and create with another, the answer you got no longer applies. I build one <code>options</code> object and pass it to both.</li><li><b>Download monitor:</b> the Nano weights are large and download once. I attach a <code>monitor(m)</code> callback in <code>create()</code> and listen for <code>downloadprogress</code> so the UI can show a progress bar instead of an unexplained hang.</li><li><b>Clean failure path:</b> when the global is missing or availability is <code>unavailable</code>, I throw a typed sentinel error (<code>OnDeviceUnavailableError</code>) so the caller can deterministically fall back to a cloud LLM rather than guessing from a generic exception.</li><li><b>Resource hygiene &amp; embedding:</b> sessions hold model state; in real code I would reuse/destroy them. In an iframe, the host page must grant <code>allow=&quot;language-model&quot;</code> or the API is blocked — worth flagging in review.</li><li><b>Tradeoff framing:</b> on-device gives privacy (data never leaves the device), offline use, and zero per-call cost; the cost is download size and dependence on device compute. The fallback preserves UX when those aren&#39;t met.</li></ul>"
      },
      {
        "label": "Solution",
        "html": "<div class=\"code\">// Minimal typing for the Chrome Prompt API surface we use.\ntype Availability =\n  | &quot;unavailable&quot;\n  | &quot;downloadable&quot;\n  | &quot;downloading&quot;\n  | &quot;available&quot;;\n\n// The `downloadprogress` event exposes `loaded` in [0, 1].\ninterface DownloadProgressEvent extends Event {\n  readonly loaded: number;\n}\n\ninterface CreateMonitor {\n  addEventListener(\n    type: &quot;downloadprogress&quot;,\n    cb: (e: DownloadProgressEvent) =&gt; void,\n  ): void;\n}\n\ninterface LanguageModelSession {\n  prompt(input: string): Promise&lt;string&gt;;\n  promptStreaming(input: string): ReadableStream&lt;string&gt;;\n  destroy(): void;\n}\n\ninterface LanguageModelOptions {\n  // Recent Chrome attests I/O languages; omit the output\n  // language and it warns + can throw NotSupportedError.\n  expectedInputs?: { type: &quot;text&quot;; languages: string[] }[];\n  expectedOutputs?: { type: &quot;text&quot;; languages: string[] }[];\n  monitor?: (m: CreateMonitor) =&gt; void;\n}\n\ninterface LanguageModelStatic {\n  availability(\n    opts?: LanguageModelOptions,\n  ): Promise&lt;Availability&gt;;\n  create(\n    opts?: LanguageModelOptions,\n  ): Promise&lt;LanguageModelSession&gt;;\n}\n\ndeclare global {\n  interface Window {\n    LanguageModel?: LanguageModelStatic;\n  }\n}\n\nexport class OnDeviceUnavailableError extends Error {\n  constructor(\n    public readonly state: Availability | &quot;missing&quot;,\n    options?: ErrorOptions,\n  ) {\n    super(`On-device model unavailable: ${state}`, options);\n    this.name = &quot;OnDeviceUnavailableError&quot;;\n  }\n}\n\nexport async function askOnDevice(\n  prompt: string,\n  onProgress?: (fraction: number) =&gt; void,\n): Promise&lt;string&gt; {\n  // 1) Feature-detect the global before anything else.\n  const LM =\n    typeof window !== &quot;undefined&quot; ? window.LanguageModel : undefined;\n  if (!LM) {\n    throw new OnDeviceUnavailableError(&quot;missing&quot;);\n  }\n\n  // 2) Build ONE options object, reused for both calls.\n  //    Declare input AND output languages — recent Chrome\n  //    refuses to emit text in an unattested language.\n  const options: LanguageModelOptions = {\n    expectedInputs: [{ type: &quot;text&quot;, languages: [&quot;en&quot;] }],\n    expectedOutputs: [{ type: &quot;text&quot;, languages: [&quot;en&quot;] }],\n  };\n\n  // 3) Gate on availability (same options as create()).\n  const state = await LM.availability(options);\n  if (state === &quot;unavailable&quot;) {\n    throw new OnDeviceUnavailableError(state);\n  }\n\n  // 4) Create a session; wire a download monitor for the\n  //    downloadable/downloading states (multi-MB weights).\n  let session: LanguageModelSession;\n  try {\n    session = await LM.create({\n      ...options,\n      monitor(m) {\n        m.addEventListener(&quot;downloadprogress&quot;, (e) =&gt; {\n          onProgress?.(e.loaded);\n        });\n      },\n    });\n  } catch (err) {\n    // create() can reject if the download fails or is blocked\n    // (e.g. missing iframe allow=&quot;language-model&quot;).\n    throw new OnDeviceUnavailableError(state, { cause: err });\n  }\n\n  // 5) Prompt, then release model resources.\n  try {\n    return await session.prompt(prompt);\n  } finally {\n    session.destroy();\n  }\n}\n\n// --- caller with graceful cloud fallback ---\nexport async function ask(prompt: string): Promise&lt;string&gt; {\n  try {\n    return await askOnDevice(prompt);\n  } catch (err) {\n    if (err instanceof OnDeviceUnavailableError) {\n      return askCloudLLM(prompt); // your hosted fallback\n    }\n    throw err;\n  }\n}\n\ndeclare function askCloudLLM(p: string): Promise&lt;string&gt;;</div>"
      }
    ]
  },
  {
    "id": "design-9",
    "kind": "design",
    "level": "architect",
    "title": "Design a privacy-first on-device AI study tutor",
    "tags": [
      "in-browser-ai",
      "system-design",
      "privacy",
      "webgpu",
      "web-worker",
      "rag"
    ],
    "promptHtml": "<p>Design a <b>privacy-first study tutor that runs AI entirely in the browser</b>. Students upload their own notes (PDFs, pasted text) and then: (1) <b>semantic search</b> over their notes, (2) <b>ask questions</b> and get grounded answers (RAG over their own material), and (3) get <b>summaries / flashcards</b>. A hard requirement: <i>student notes must never leave the device</i> — no server-side inference, works offline after first load, no per-query cost.</p><p>Walk through how you'd architect this with today's browser AI stack (Chrome Built-in AI / Prompt API, Transformers.js, WebLLM, WebGPU, Web Workers, the Cache API). Cover model selection, feature detection and fallback, threading, caching, the privacy model, and the offline/cost tradeoffs. Be explicit about what degrades gracefully and what you tell the user when their device can't run a capability.</p>",
    "reveal": [
      {
        "label": "Clarify & requirements",
        "html": "<ul><li><b>Privacy is the top constraint:</b> notes never leave the device. That rules out any cloud inference for the core flows and pushes <i>all</i> embedding + generation on-device. Confirm whether a cloud fallback is acceptable for users on weak devices, or whether the product is strictly offline-only (it changes the fallback story).</li><li><b>Capabilities &amp; their model needs:</b> (a) <b>semantic search</b> needs only a small <b>embedding model</b> (MiniLM, 384-dim) — cheap, runs almost anywhere. (b) <b>Q&amp;A / RAG</b> and (c) <b>summaries/flashcards</b> need a <b>generative LLM</b> — either Chrome's on-device <b>Prompt API (Gemini Nano)</b> or a <b>WebLLM</b> model (Llama/Phi/Qwen) on WebGPU.</li><li><b>Device variance:</b> WebGPU and the Prompt API are not universal. I need explicit feature detection and a tiered experience, not a single hard requirement.</li><li><b>Scale:</b> a single student's notes — small corpus (tens to low thousands of chunks). In-memory cosine ranking is plenty; no vector DB infrastructure needed.</li><li><b>Non-goals to confirm:</b> multi-device sync (would imply a server and break the privacy promise unless E2E-encrypted), and accuracy guarantees (small on-device models are weaker than frontier cloud models).</li><li><b>Success criteria:</b> first-load model download time, time-to-first-token, search latency, and graceful behavior on unsupported devices.</li></ul>"
      },
      {
        "label": "Key decisions",
        "html": "<ul><li><b>Tiered model strategy.</b> Always-on: <b>Transformers.js + MiniLM</b> (<code>feature-extraction</code>, <code>device: &quot;webgpu&quot;</code> with <b>wasm fallback</b>, <code>dtype: &quot;q8&quot;</code>) for embeddings — small, broadly runnable, powers search even on modest devices. For generation, prefer Chrome's <b>Prompt API</b> when <code>window.LanguageModel.availability()</code> is <code>available</code>/<code>downloadable</code> (smallest footprint, OS-managed weights); otherwise use <b>WebLLM</b> on WebGPU for a larger local LLM; if neither is available, degrade to search-and-summarize-via-extraction only, and clearly tell the user generation isn't supported on their device.</li><li><b>Feature detection &amp; fallback ladder.</b> Detect in order: <code>&quot;LanguageModel&quot; in self</code> + <code>availability()</code> &rarr; use Prompt API (pass the <i>same options</i> to <code>availability()</code> and <code>create()</code>, attach a <code>downloadprogress</code> monitor). Else detect <code>navigator.gpu</code> (WebGPU) &rarr; load WebLLM. Else embeddings still run on wasm, so <b>search keeps working</b>; generation is disabled with an explanatory message. Each rung degrades a feature, never the whole app.</li><li><b>RAG pipeline, all local.</b> Ingest &rarr; chunk notes &rarr; embed each chunk with MiniLM (normalized) &rarr; store vectors in <b>IndexedDB</b>. At query time: embed the question, rank chunks by <b>cosine similarity</b> (dot product of normalized vectors), take top-k, build a grounded prompt, and feed it to the local LLM (Prompt API or WebLLM). Citations point back to the student's own chunks.</li><li><b>Threading.</b> Run embedding and LLM inference in <b>Web Workers</b> so model loading and token generation never block the UI thread; the main thread does ranking (cheap) and rendering. Stream tokens (<code>promptStreaming</code> / WebLLM streaming) for responsiveness.</li><li><b>Caching &amp; offline.</b> Cache model weights via the <b>Cache API / IndexedDB</b> (Transformers.js does this natively; WebLLM caches in browser storage) so the multi-GB/MB download happens once. A service worker caches the app shell. After first successful load, the whole tutor works <b>offline</b>.</li><li><b>Quantization tradeoff.</b> Use <code>q8</code> (or <code>q4</code> for the LLM) to shrink download and memory at a small quality cost — explicitly chosen to make first-load and low-end devices viable.</li></ul>"
      },
      {
        "label": "Senior signals",
        "html": "<ul><li><b>Privacy as architecture, not a feature.</b> The candidate makes &quot;notes never leave the device&quot; a structural property (no inference endpoint exists) rather than a policy promise — and recognizes that any sync/fallback to cloud must be E2E-encrypted or off by default, or it breaks the guarantee.</li><li><b>Right-sized models per task.</b> Doesn't reach for a multi-GB local LLM to do search; pairs a tiny embedding model with a generative model only where generation is actually needed. Understands 384-dim MiniLM + cosine is sufficient for a single-student corpus.</li><li><b>Real feature detection and graceful degradation.</b> Knows the Prompt API and WebGPU aren't universal, builds an explicit fallback ladder, and keeps the core (search) alive even when generation isn't supported — plus honest user messaging about what their device can/can't do.</li><li><b>API correctness details.</b> Mentions passing identical options to <code>availability()</code> and <code>create()</code>, wiring a <code>downloadprogress</code> monitor for the one-time weight download, and <code>allow=&quot;language-model&quot;</code> for embedded iframes — the things that bite people in practice.</li><li><b>Performance discipline.</b> Inference in Web Workers, streaming output, caching weights so first-load cost is paid once, and quantization as a deliberate size/quality lever.</li><li><b>Clear-eyed on tradeoffs.</b> Articulates the wins (privacy, offline, zero per-call cost, low latency for small models) against the costs (large first download, dependence on device compute/battery/WebGPU, weaker quality than frontier cloud models) — and ties each decision back to those tradeoffs rather than asserting a single &quot;best&quot; stack.</li></ul>"
      }
    ]
  }
];
