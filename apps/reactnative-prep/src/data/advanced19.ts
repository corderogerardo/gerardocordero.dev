// Batch 19 — App Store/Play Store submission requirements, code signing (provisioning profiles, Android keystores), staged rollouts, and OTA update vs native build update tradeoffs.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED19_FLASHCARDS: Flashcard[] = [
  {
    id: "a19-release-submit-1",
    category: "release",
    categoryLabel: "Releases",
    question: `Why does an EAS production iOS build fail with a provisioning profile mismatch even though the certificate is valid, and what's the actual cause most of the time?`,
    answerHtml: `<p>Provisioning profiles bind three things together: the <b>App ID</b> (with its entitlements), the <b>distribution certificate</b>, and (for ad hoc) registered device UDIDs. A mismatch almost always means the entitlements on the App ID changed (e.g. you added Push Notifications or App Groups capability) but the profile wasn't regenerated to include it. EAS normally auto-manages this via <code>credentials.json</code>/remote credentials, but if you hand-edit native entitlements outside of <code>app.json</code>'s <code>ios.entitlements</code>, EAS's auto-provisioning won't know to refresh the profile, and Apple rejects the stale profile at build or upload time.</p>`,
  },
  {
    id: "a19-release-submit-2",
    category: "release",
    categoryLabel: "Releases",
    question: `What's the practical difference between an Apple Distribution certificate and an Apple Development certificate when both are valid in your account, and why does this matter for EAS build profiles?`,
    answerHtml: `<p>A <b>Development</b> certificate signs builds that can only run on devices explicitly listed in a provisioning profile (debug builds, internal dev clients). A <b>Distribution</b> certificate signs builds destined for App Store review or ad hoc/enterprise distribution and carries no device list restriction for App Store profiles. In <code>eas.json</code>, the <code>development</code> build profile pulls a development cert + ad hoc-style profile, while <code>production</code> pulls the distribution cert + App Store profile — mixing them up produces a build that installs fine via simulator/internal but gets rejected on App Store Connect upload with an invalid signature.</p>`,
  },
  {
    id: "a19-release-submit-3",
    category: "release",
    categoryLabel: "Releases",
    question: `What does it mean for an Android keystore to be 'lost' in the Play App Signing era, and why is the blast radius smaller than it used to be?`,
    answerHtml: `<p>Before Play App Signing, your <b>upload keystore</b> was also the <b>signing key</b> baked into every installed APK — lose it and you can never publish an update to the same package name again (have to ship a new app). With <b>Play App Signing</b> (default since 2021, mandatory for App Bundles), Google holds the real signing key; your local keystore is only an <b>upload key</b> used to authenticate you to Google's servers. If you lose it, you request a reset of the upload key through Play Console identity verification — the app's actual signing key (and existing installs) are unaffected.</p>`,
  },
  {
    id: "a19-release-submit-4",
    category: "release",
    categoryLabel: "Releases",
    question: `Why does EAS Build default to having EAS generate and hold your Android keystore, and when would you instead supply your own?`,
    answerHtml: `<p>EAS auto-generates and stores the keystore server-side so a team never has a 'who has the .jks file' problem and credentials survive laptop loss. You'd supply your own keystore when <b>migrating an existing app already published with that exact key</b> — Play Store enforces the upload key matches what it has on file (or the Play App Signing upload-key chain), so a fresh EAS-generated key would be rejected as a different signer. Use <code>eas credentials</code> to upload an existing <code>.jks</code>/<code>.keystore</code> instead of letting EAS mint a new one.</p>`,
  },
  {
    id: "a19-release-submit-5",
    category: "release",
    categoryLabel: "Releases",
    question: `A reviewer asks: 'Why can iOS staged rollouts pause/resume but you can't target a specific cohort, while Play's staged rollout lets you halt and even roll back?' What's the underlying mechanism difference?`,
    answerHtml: `<p>Apple's <b>phased release</b> (App Store Connect) is a fixed 7-day curve (1%/2%/5%/10%/20%/50%/100%) applied only to <i>automatic update</i> opt-in users — you can pause it (freezes at current %) or resume, but there's no manual percentage control and no true rollback short of expediting a new version. Google Play's <b>staged rollout</b> lets you set an arbitrary percentage manually and halt it to stop further users from receiving the release. A true 'rollback' on Play isn't a special signing-model feature — it's just publishing the prior APK/AAB as a new, higher-versionCode release, which staged-rollout (and already-updated) users will then receive like any normal update.</p>`,
  },
  {
    id: "a19-release-submit-6",
    category: "release",
    categoryLabel: "Releases",
    question: `What is the practical risk of using \`eas update\` (OTA) to ship a fix for a crash that's tied to a native module bug?`,
    answerHtml: `<p>OTA updates via <code>expo-updates</code> only replace the <b>JS bundle and assets</b> — they cannot touch compiled native code (Swift/Kotlin/native modules) or anything requiring an Info.plist/AndroidManifest change. If the crash originates in a native module or native permission declaration, an OTA 'fix' will download successfully but the underlying native crash persists, giving a false sense of resolution while users on the affected build keep crashing. You need a new native build (and store/TestFlight submission) for true native fixes.</p>`,
  },
  {
    id: "a19-release-submit-7",
    category: "release",
    categoryLabel: "Releases",
    question: `Which App Store Review Guideline restricts what kinds of OTA/JS updates are allowed, and how does this affect your \`expo-updates\` strategy?`,
    answerHtml: `<p>Guideline <b>2.5.2 (Software Requirements)</b> is the one that matters: apps generally may not download or execute code that changes the app's primary purpose or feature set outside of what was reviewed, but Apple carves out an explicit exception for interpreted code (JavaScript) that doesn't materially change the app's intended use — the same logic Expo/CodePush-style OTA updates rely on. In practice: scope <code>eas update</code> to bug fixes, content, and minor UI tweaks; gate substantial feature flags or new functionality behind a native build so review actually sees what ships, and document this reasoning in App Review notes if asked.</p>`,
  },
  {
    id: "a19-release-submit-8",
    category: "release",
    categoryLabel: "Releases",
    question: `What's the difference between \`runtimeVersion\` policy \`appVersion\` vs \`nativeVersion\` in \`expo-updates\`, and what breaks if you pick the wrong one?`,
    answerHtml: `<p><code>runtimeVersion</code> is the compatibility key the updates server uses to match an OTA bundle to compatible native binaries — it must change whenever native code changes. The <code>appVersion</code> policy ties runtimeVersion to your marketing version string (<code>expo.version</code>) alone, which is simple but dangerous: forgetting to bump the marketing version after an actual native change lets an incompatible OTA update reach an old native binary and crash. The <code>nativeVersion</code> policy instead derives it from <code>version(buildNumber)</code>/<code>version(versionCode)</code>, tying compatibility to the actual native build rather than just the marketing string — but neither is a content hash. The <code>fingerprint</code> policy is the one that's genuinely safer for this, since it derives the runtime version from an actual hash of native config/dependencies, changing only when something native-relevant actually changed.</p>`,
  },
  {
    id: "a19-release-submit-9",
    category: "release",
    categoryLabel: "Releases",
    question: `Why might \`eas update\`'s rollback feature (\`eas update:rollback-to-embedded\` or republishing a prior update) not actually 'roll back' for all users immediately?`,
    answerHtml: `<p><code>expo-updates</code> checks for updates on launch (by default) and applies the new bundle on the <i>next</i> cold start after download, not instantly mid-session. Users who are offline, who don't relaunch, or who have <code>checkAutomatically: ON_ERROR_RECOVERY</code>/manual fetch configured won't pull the rollback until their next qualifying launch. So a 'rollback' is really 'publish a new update pointing back to known-good code,' and full propagation depends on your update-check policy and how often users actually relaunch the app.</p>`,
  },
  {
    id: "a19-release-submit-10",
    category: "release",
    categoryLabel: "Releases",
    question: `What does TestFlight's 90-day build expiration interact with badly if your release cadence is OTA-heavy?`,
    answerHtml: `<p>TestFlight builds (and their associated native binary) expire 90 days after upload regardless of how recently you've shipped OTA updates on top of them — testers get locked out of the app entirely even if your JS has been updated weekly via <code>eas update</code>. Teams that rely heavily on OTA and rarely cut native builds can be surprised when external testers suddenly can't launch the app; you need a recurring native build cadence (even trivial rebuilds) independent of your OTA velocity to keep TestFlight distribution alive.</p>`,
  },
  {
    id: "a19-release-submit-11",
    category: "release",
    categoryLabel: "Releases",
    question: `Google Play's pre-launch report and Android App Bundle (AAB) — why does Play require AAB over universal APK for new apps, and what does this mean for keystore/signing flow?`,
    answerHtml: `<p>AAB defers APK generation to Google's servers, which use <b>device-specific APK splitting</b> (per-ABI, per-density, per-language) to shrink download size — this requires Play App Signing because Google needs the actual signing key to generate and sign these split APKs on the fly, which is why AAB effectively mandates Play App Signing (your local key becomes upload-only). This is also why you can't manually side-load-test the exact binary a user receives; you use <code>bundletool</code> to generate a device-specific APK set locally for QA.</p>`,
  },
  {
    id: "a19-release-submit-12",
    category: "release",
    categoryLabel: "Releases",
    question: `What's the difference between Apple's 'Expedited Review' and a standard review queue, and what's the actual cost of overusing it?`,
    answerHtml: `<p>Expedited review (requested via App Store Connect or the Apple Developer contact form) jumps your build to the front of the queue, typically resolving in hours instead of 1-3 days, intended for critical bug fixes or time-sensitive launches. Apple explicitly limits how often a developer account can use it — repeated/non-critical requests get silently deprioritized or denied in the future, so it should be reserved for genuine production-breaking issues, not as a default workaround for slow planning.</p>`,
  },
  {
    id: "a19-release-submit-13",
    category: "release",
    categoryLabel: "Releases",
    question: `Why does \`eas.json\`'s \`autoIncrement\` for \`buildNumber\`/\`versionCode\` matter for App Store/Play submission, and what happens if you forget it?`,
    answerHtml: `<p>Both stores reject a binary upload whose build number (iOS) or versionCode (Android) is not strictly greater than the last one submitted for that bundle ID/package, even for an internal TestFlight/internal-track build. With <code>appVersionSource: remote</code> and <code>autoIncrement: true</code> in the production profile, EAS tracks the last used number server-side and bumps automatically; without it, a forgotten manual bump causes the build to succeed locally but fail at the submit step with a duplicate-version error, wasting a full build cycle.</p>`,
  },
  {
    id: "a19-release-submit-14",
    category: "release",
    categoryLabel: "Releases",
    question: `What is the security purpose of Apple's entitlements + provisioning profile combo versus Android's signature-based permission model, at a conceptual level?`,
    answerHtml: `<p>iOS entitlements are <b>centrally authorized by Apple</b> — your provisioning profile is the proof Apple issued you the right to use a capability (Push, HealthKit, etc.), checked at install/launch time by the OS. Android has no centralized capability authorization; instead, the <b>APK signature</b> proves update authenticity (same signer can replace the app) and dangerous permissions are runtime-granted by the user, not pre-approved by Google. This is why iOS capability mistakes fail at build/submit time (signing-level enforcement) while Android permission mistakes typically fail at runtime (user denies, or Play policy review flags overly broad permissions post-submission).</p>`,
  },
  {
    id: "a19-release-submit-15",
    category: "release",
    categoryLabel: "Releases",
    question: `Your app uses \`expo-updates\` and you ship an OTA update with a typo'd asset URL that 500s. What's the actual failure mode for users, and how does \`fallbackToCacheTimeout\` factor in?`,
    answerHtml: `<p><code>expo-updates</code> downloads and verifies an update as an atomic manifest+asset bundle before ever applying it — if any asset fails to fetch, the update is discarded and the app continues running the last known-good bundle (embedded or previously cached), not a half-broken state. <code>fallbackToCacheTimeout</code> only controls how long a cold launch will block waiting for a fresh manifest check before falling back to the cached bundle and checking in the background; it doesn't affect this corruption case. So a broken asset URL fails closed — no users see the bad bundle, but the update silently never progresses past download, which means you need EAS Update dashboard/\`eas update:view\` monitoring to catch it rather than relying on crash reports.</p>`,
  },
  {
    id: "a19-release-submit-16",
    category: "release",
    categoryLabel: "Releases",
    question: `Why would a senior interviewer ask 'what's your rollback story for a native crash discovered after 100% rollout' and what's the honest answer for an Expo/EAS app?`,
    answerHtml: `<p>There is no instant native rollback — Apple and Google don't let you 'unpublish to a prior binary' for users who already updated; both stores only let you halt forward rollout of the <i>current</i> bad release (Play: halt staged rollout; Apple: remove from sale or pause phased release, which doesn't un-install). The real mitigation is layered: (1) staged/phased rollout to limit blast radius before promoting to 100%, (2) \`expo-updates\` OTA rollback for any portion of the crash that's JS-reachable (e.g. disabling a feature flag in JS to avoid hitting the native code path), and (3) expedited review to get a truly fixed native build out fast. The honest answer acknowledges native rollback is fundamentally asymmetric — forward-fix is the only real lever once installed.</p>`,
  },
  {
    id: "a19-release-submit-17",
    category: "release",
    categoryLabel: "Releases",
    question: `What does 'sign-in required for review' (App Review Guideline 2.1) commonly trip up in submission, and how do demo accounts interact with staged rollout testing?`,
    answerHtml: `<p>If your app requires authentication, Apple reviewers need persistent demo credentials in the App Review notes (or a sign-in-free guest/demo mode) — reviewers won't create real accounts or use email OTP flows reliably, and rejection here is one of the most common non-technical holdups under Guideline 2.1 (App Completeness). This is orthogonal to staged rollout, but the gotcha is: if your demo account's data is environment-pinned (e.g. only seeded in production but you're testing a staged rollout build pointed at a staging API), the reviewer hits a broken/empty state — demo accounts must work against whatever backend the submitted binary actually points to.</p>`,
  },
  {
    id: "a19-release-submit-18",
    category: "release",
    categoryLabel: "Releases",
    question: `What's the difference between EAS's \`internal\` distribution and TestFlight/Play internal testing tracks, and when do you use EAS internal distribution instead?`,
    answerHtml: `<p>EAS <code>internal</code> distribution builds a installable binary (ad hoc signed for iOS with registered UDIDs, or a direct APK for Android) distributed via a link/QR code <i>outside</i> the App Store/Play infrastructure entirely — no review, no store metadata, instant install. TestFlight/Play internal testing tracks go through each store's lightweight review and require the tester to have the TestFlight/Play Store app. Use EAS internal distribution for fast same-day QA loops (designers, PMs, the dev team) where store review latency (even TestFlight's ~hours) is friction you don't want; use store tracks when you need to validate the actual store-distributed artifact (in-app purchase entitlements, Play Integrity, sign-in capability checks tied to the store identity).</p>`,
  },
  {
    id: "a19-release-submit-19",
    category: "release",
    categoryLabel: "Releases",
    question: `Why does adding a new permission (e.g. \`NSCameraUsageDescription\` or a new Android dangerous permission) force a native rebuild even though it's 'just config' in \`app.json\`?`,
    answerHtml: `<p>iOS usage-description strings live in <b>Info.plist</b> and Android permissions live in <b>AndroidManifest.xml</b> — both are compiled into the native binary at build time via Expo config plugins (\`expo prebuild\`), not read at runtime from JS. There's no mechanism for \`expo-updates\` to patch a binary's manifest/plist after install, so any permission/entitlement change is native-build-only, never OTA-eligible, regardless of how trivial the change looks in \`app.json\`.</p>`,
  },
  {
    id: "a19-release-submit-20",
    category: "release",
    categoryLabel: "Releases",
    question: `In a staged Play rollout, what's the difference between 'halt rollout' and 'rollback' in the Play Console, and why does Google explicitly warn against using rollback as a routine tool?`,
    answerHtml: `<p>'Halt rollout' freezes the percentage of users on the new release — no further users get it, but the existing rolled-out cohort keeps it. 'Rollback' (available within a limited window) reverts the staged users back to the previous version's APK, but Google explicitly cautions this can cause data/schema downgrade issues if the halted version already ran migrations (e.g. a local DB schema bump) — going back to old code with new-schema data is the failure mode interviewers probe for, since Play's rollback only swaps the binary, not any state it already mutated.</p>`,
  },
  {
    id: "a19-release-submit-21",
    category: "release",
    categoryLabel: "Releases",
    question: `What's the role of Apple's 'App Store Connect API key' vs an Apple ID + app-specific password in EAS submit authentication, and why does EAS recommend the API key?`,
    answerHtml: `<p>The legacy flow used an Apple ID + app-specific password, which is tied to a personal account, breaks on 2FA/password changes, and can't be scoped — anyone with it has broad account access. An <b>App Store Connect API key</b> (generated in Users and Access > Keys) is a service-account-style credential scoped to API actions, doesn't expire on password rotation, and can be uploaded once to EAS's credential store so CI never needs interactive 2FA. This is why the project stores submission credentials on EAS via \`eas credentials\` rather than in \`eas.json\` or repo secrets.</p>`,
  },
  {
    id: "a19-release-submit-22",
    category: "release",
    categoryLabel: "Releases",
    question: `Why can't you simply use an OTA update to change \`app.json\`'s \`expo.version\` or add a new native module dependency, and what's the failure if you try?`,
    answerHtml: `<p><code>app.json</code>/\`app.config.js\` is evaluated at <b>build time</b> by \`expo prebuild\`/EAS Build to generate native project files — it is not bundled as part of the JS payload \`expo-updates\` ships. An OTA update changing version strings has no effect on the actual installed binary's version (App Store/Play still report the old number), and adding a native dependency via OTA is simply impossible — the JS \`require\` would fail at runtime with a missing native module error since the compiled binary never linked it.</p>`,
  },
  {
    id: "a19-release-submit-23",
    category: "release",
    categoryLabel: "Releases",
    question: `What does 'codesigning' actually verify at iOS install/launch time, beyond just 'is this app from Apple'?`,
    answerHtml: `<p>Code signing verifies (1) the binary's contents haven't been tampered with since signing (hash check against the embedded signature), (2) the signer's certificate chains back to a trusted Apple CA, and (3) the entitlements requested in the binary are exactly what the provisioning profile authorized — so it's both an integrity check and a capability-authorization check in one mechanism. This is also why ad hoc TestFlight/internal builds fail silently on un-registered devices: the OS checks the device UDID against the profile's allowed list as part of the same verification at launch, before the app even runs.</p>`,
  },
  {
    id: "a19-release-submit-24",
    category: "release",
    categoryLabel: "Releases",
    question: `How does \`expo-updates\`' update check policy (\`checkAutomatically: ON_LOAD\` vs \`ON_ERROR_RECOVERY\` vs \`NEVER\`) change your effective rollback speed, and what's the tradeoff against startup latency?`,
    answerHtml: `<p><code>ON_LOAD</code> (default) checks for an update on every cold start, blocking up to \`fallbackToCacheTimeout\` ms before falling back to cached — fastest propagation of a rollback/fix but adds startup latency and network dependency on every launch. <code>ON_ERROR_RECOVERY</code> only checks when the app fails to load the cached bundle, minimizing startup cost but meaning a bad release can sit live far longer since healthy app instances never re-check. The tradeoff is propagation speed of emergency fixes/rollbacks versus cold-start performance — most production apps keep \`ON_LOAD\` specifically so a rollback actually reaches users quickly.</p>`,
  },
];

export const ADVANCED19_QUIZ: QuizQuestion[] = [
  {
    id: "a19q-release-submit-1",
    category: "release",
    categoryLabel: "Releases",
    question: `Your team migrates an existing, already-published Android app to EAS Build. What must you do with the keystore to avoid Play Store rejecting the next update?`,
    options: [`Let EAS generate a new keystore automatically — Play App Signing reconciles any signer`, `Upload the existing upload keystore to EAS via \`eas credentials\` so the new build's upload key matches what Play already trusts`, `Nothing — Play Store only checks the package name, not the signing key`, `Generate a new keystore and request a full app signing key reset via Play Console before the next build`],
    answer: 1,
    explanationHtml: `<p>Play Store enforces that an update's upload key matches the one on file (directly, or via the Play App Signing upload-key trust chain). A freshly EAS-generated keystore is a different signer and would be rejected as an invalid update. You must import the existing keystore into EAS's credential manager, not let EAS mint a new one.</p>`,
  },
  {
    id: "a19q-release-submit-2",
    category: "release",
    categoryLabel: "Releases",
    question: `A crash is traced to a native module's incorrect handling of a permission denial. The on-call engineer wants to ship a fix via \`eas update\` to avoid waiting for App Store review. Why is this the wrong call?`,
    options: [`\`eas update\` requires App Store review the same as a native build`, `OTA updates can only patch JavaScript/assets — they cannot change compiled native module code, so the crash will persist for affected users`, `\`eas update\` is disabled on iOS for crash fixes by Apple policy`, `OTA updates always increase the app's runtimeVersion, forcing a forced native reinstall anyway`],
    answer: 1,
    explanationHtml: `<p><code>expo-updates</code> only ships JS bundle and asset changes. A bug inside a compiled native module is unreachable by OTA — you need a new native build and a fresh store/TestFlight submission to actually fix it. Shipping an OTA 'fix' here would just give false confidence while the crash continues.</p>`,
  },
  {
    id: "a19q-release-submit-3",
    category: "release",
    categoryLabel: "Releases",
    question: `What is the key functional difference between Apple's phased release and Google Play's staged rollout?`,
    options: [`Phased release is automatic-update-only with a fixed 7-day curve and pause/resume only; staged rollout lets you set an arbitrary percentage and halt it manually`, `Staged rollout is iOS-only and phased release is Android-only`, `Both are functionally identical, differing only in terminology`, `Phased release allows targeting by user cohort while staged rollout is purely random sampling with no halt option`],
    answer: 0,
    explanationHtml: `<p>Apple's phased release follows a fixed schedule (1/2/5/10/20/50/100%) applied only to opted-in automatic-update users, with pause/resume but no manual percentage control. Google Play's staged rollout gives you direct percentage control and the ability to halt forward progress at will.</p>`,
  },
  {
    id: "a19q-release-submit-4",
    category: "release",
    categoryLabel: "Releases",
    question: `Why does Android App Bundle (AAB) submission to Play Store effectively require Play App Signing rather than self-managed signing?`,
    options: [`AAB files are always unsigned and Google signs them purely as a formality`, `Google's servers generate device-specific split APKs from the AAB at install time, which requires Google to hold the actual signing key to produce and sign those splits`, `Play Store no longer supports any form of self-managed signing for any artifact type`, `AAB is just a zip wrapper with no signing implications at all`],
    answer: 1,
    explanationHtml: `<p>AAB defers APK generation to Google's infrastructure, which splits by ABI/density/language per device. To sign these generated splits, Google needs the real signing key — hence Play App Signing, with your local keystore reduced to an upload-authentication key only.</p>`,
  },
  {
    id: "a19q-release-submit-5",
    category: "release",
    categoryLabel: "Releases",
    question: `A team adds \`NSCameraUsageDescription\` to \`app.json\` and wants to ship it via \`eas update\` to skip a native rebuild. What happens?`,
    options: [`It works fine since Info.plist values are read from \`app.json\` at runtime by \`expo-updates\``, `It fails to ship — usage description strings are compiled into Info.plist at native build time and have no OTA delivery path; a native rebuild is required`, `It works only on Android, not iOS`, `EAS automatically detects the change and silently triggers an EAS Build instead of an update`],
    answer: 1,
    explanationHtml: `<p>Info.plist (and AndroidManifest.xml) entries are generated by config plugins during \`expo prebuild\`/native build, then compiled into the binary. There is no runtime mechanism for \`expo-updates\` to patch them — any permission or entitlement change requires a new native build and store submission.</p>`,
  },
  {
    id: "a19q-release-submit-6",
    category: "release",
    categoryLabel: "Releases",
    question: `Which \`expo-updates\` \`checkAutomatically\` setting maximizes how quickly a published rollback reaches users, at the cost of added cold-start latency?`,
    options: [`NEVER, since it forces a hard reset on every launch`, `ON_ERROR_RECOVERY, since it checks only on failure, which is faster overall`, `ON_LOAD, since every cold start checks for a new update before (optionally) falling back to cache`, `Checking frequency has no effect on rollback propagation speed`],
    answer: 2,
    explanationHtml: `<p>\`ON_LOAD\` checks for updates on every cold launch (bounded by \`fallbackToCacheTimeout\`), so a published rollback propagates as fast as users relaunch. \`ON_ERROR_RECOVERY\` only checks when the cached bundle fails to load, so healthy instances of a bad release never re-check and can stay on broken JS far longer.</p>`,
  },
  {
    id: "a19q-release-submit-7",
    category: "release",
    categoryLabel: "Releases",
    question: `Why does EAS recommend an App Store Connect API key over an Apple ID + app-specific password for \`eas submit\` automation in CI?`,
    options: [`API keys are faster to generate but functionally identical in security scope`, `App-specific passwords are deprecated by Apple and no longer work at all`, `The API key is a scoped, non-personal credential that doesn't break on password rotation or 2FA prompts, making it suitable for unattended CI submission`, `API keys are required only for macOS apps, not iOS`],
    answer: 2,
    explanationHtml: `<p>An App Store Connect API key is a service-style credential scoped to API actions, stored once on EAS, and unaffected by personal account password changes or interactive 2FA — exactly what unattended CI submission needs. Apple ID + app-specific password ties credentials to a personal account and is brittle for automation.</p>`,
  },
];
