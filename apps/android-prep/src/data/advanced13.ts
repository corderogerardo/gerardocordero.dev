// Batch 13 — Retrofit + OkHttp interceptors, Room (relations, migrations, Flow-backed DAOs), offline-first patterns (single source of truth, repository pattern), and caching strategies.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED13_FLASHCARDS: Flashcard[] = [
  {
    id: "a13-data-layer-1",
    category: "data",
    categoryLabel: "Data",
    question: `An OkHttp Authenticator vs. an application interceptor that injects an Authorization header — when do you need Authenticator instead?`,
    answerHtml: `<p>Use an <b>Authenticator</b> (<code>authenticator()</code>) when credentials should be added <i>reactively</i>, after a <code>401</code> response: OkHttp calls it with the failed <code>Response</code>, and you return a new request with refreshed credentials (or <code>null</code> to give up and surface the failure).</p><p>An interceptor that injects a token proactively on every request is simpler but can't cleanly react to server-side rejection — you'd be hand-rolling retry-after-401 logic. In practice both are used together: an interceptor sets the happy-path header, an Authenticator handles refresh-on-401, and the refresh call itself is guarded by a lock so concurrent 401s trigger only one network refresh instead of a stampede.</p>`,
  },
  {
    id: "a13-data-layer-2",
    category: "data",
    categoryLabel: "Data",
    question: `Why does body-logging behave differently depending on whether you register it as an application interceptor or a network interceptor, especially with caching enabled?`,
    answerHtml: `<p><b>Application interceptors</b> run exactly once per logical call, see the original (decompressed) request/response, and still run on a fully cached response. <b>Network interceptors</b> run once per actual socket-level attempt — skipped entirely on a cache hit, and re-invoked on every redirect — and see the wire bytes (e.g. gzip-encoded body, hop-by-hop headers).</p><p>Put a logger on the network level and a cached response produces no log line at all, which reads as "the API isn't being called" during debugging. Put it at the application level if you want one consistent log per call regardless of cache/redirects; use the network level only when you specifically need to inspect what went over the wire.</p>`,
  },
  {
    id: "a13-data-layer-3",
    category: "data",
    categoryLabel: "Data",
    question: `An interceptor calls chain.proceed(request) twice to implement its own retry-on-5xx logic. What's the subtle bug this introduces with a request that has a body?`,
    answerHtml: `<p>Request bodies in OkHttp are typically one-shot streams (e.g. backed by a buffered source tied to the original <code>RequestBody.writeTo()</code>). Calling <code>proceed()</code> a second time replays the same <code>Request</code> object, and if the body isn't trivially re-writable (streaming from a file/network source already consumed), the retry sends an empty or truncated body instead of failing loudly.</p><p>The safer pattern is to let OkHttp's built-in <code>retryOnConnectionFailure</code> handle transport-level retries, and for application-level retry-on-5xx, build a fresh <code>Request</code> (cloned via <code>request.newBuilder()</code>) each attempt, or ensure the <code>RequestBody</code> is buffered/re-creatable up front.</p>`,
  },
  {
    id: "a13-data-layer-4",
    category: "data",
    categoryLabel: "Data",
    question: `Where in the interceptor chain do application interceptors vs. network interceptors sit relative to OkHttp's cache, and what does that mean for a cache-write interceptor?`,
    answerHtml: `<p>The order is: <b>application interceptors</b> → <code>RetryAndFollowUpInterceptor</code> → <code>BridgeInterceptor</code> → <code>CacheInterceptor</code> → <code>ConnectInterceptor</code> → <b>network interceptors</b> → <code>CallServerInterceptor</code> (the actual socket call).</p><p>So application interceptors see every call once, before the cache is even consulted — they can't tell if a response came from cache or network. Network interceptors sit right before the wire (after the connection is established) and are the only place you see the real outbound/inbound headers OkHttp itself manages (e.g. <code>Content-Encoding: gzip</code> stripped by <code>BridgeInterceptor</code>). If you want to mutate cache-control behavior per-response, you typically do it in an application interceptor by rewriting response headers, not a network one.</p>`,
  },
  {
    id: "a13-data-layer-5",
    category: "data",
    categoryLabel: "Data",
    question: `Retrofit's @Body suspend function throws HttpException on a 404. How do you distinguish 'this user genuinely doesn't exist' (a valid domain state) from 'the network/server failed' without losing type safety?`,
    answerHtml: `<p>Wrap the call's return type in Retrofit's built-in <code>Response&lt;T&gt;</code> (not the OkHttp one) instead of a bare suspend return type. <code>response.isSuccessful</code>, <code>response.code()</code>, and <code>response.errorBody()</code> let you branch on status without an exception for expected non-2xx outcomes like a domain-meaningful 404.</p><p>A common pattern is a small sealed <code>ApiResult&lt;T&gt;</code> (Success/HttpError/NetworkError) that a repository-layer adapter produces from <code>Response&lt;T&gt;</code>, so call sites pattern-match instead of catching exceptions for control flow — exceptions stay reserved for truly exceptional cases (timeouts, serialization bugs).</p>`,
  },
  {
    id: "a13-data-layer-6",
    category: "data",
    categoryLabel: "Data",
    question: `Why does adding a custom CallAdapter.Factory matter if Retrofit already supports suspend functions natively?`,
    answerHtml: `<p>Retrofit's native suspend support always returns either the deserialized body, Retrofit's <code>Response&lt;T&gt;</code>, or throws. A custom <code>CallAdapter.Factory</code> lets you change what the call site receives entirely — e.g. wrap every endpoint in your own <code>Result&lt;T&gt;</code>/<code>ApiResult&lt;T&gt;</code> sealed type automatically, so every repository gets uniform error handling for free instead of each one writing its own try/catch around <code>HttpException</code> and <code>IOException</code>.</p><p>This centralizes the network-exception-to-domain-error mapping in one place (the adapter) rather than duplicating it across dozens of repository methods.</p>`,
  },
  {
    id: "a13-data-layer-7",
    category: "data",
    categoryLabel: "Data",
    question: `You configure both an OkHttp cache (Cache-Control based) and a Room-backed offline cache for the same API data. What problem does this duplication create, and what's the usual resolution?`,
    answerHtml: `<p>You now have two caches with independent invalidation rules and no single source of truth: the OkHttp HTTP cache obeys server <code>Cache-Control</code>/<code>ETag</code> headers, while Room holds whatever you wrote last. UI built on Room can show stale data even though the HTTP cache already has a fresher response sitting unused, or vice versa — and debugging "why am I seeing old data" now means checking two layers.</p><p>The usual resolution in an offline-first architecture is to make Room (or another local DB) the single source of truth the UI observes, and treat the OkHttp cache purely as a transport-level optimization (e.g. for conditional GETs / bandwidth savings) that the app code never reads from directly — the repository always reads through Room.</p>`,
  },
  {
    id: "a13-data-layer-8",
    category: "data",
    categoryLabel: "Data",
    question: `What's the difference between configuring Retrofit's converter with a lenient vs. strict Moshi/kotlinx.serialization setup, and why does it matter for backward-compatible API evolution?`,
    answerHtml: `<p>A strict configuration fails parsing on unknown fields or missing non-nullable fields — safer for catching contract drift early, but brittle in production: if the backend adds a new field or the app is a version behind, every response for that endpoint becomes unparseable and the feature breaks entirely.</p><p>A lenient configuration (e.g. kotlinx.serialization's <code>ignoreUnknownKeys = true</code>, or Moshi models with sensible defaults/nullable fields) degrades gracefully — new unknown fields are ignored, missing fields fall back to defaults. For a shipped mobile client where you can't force every user onto the newest build, lenient parsing is almost always the right default; strict parsing belongs in tests against a contract/schema, not in the production converter.</p>`,
  },
  {
    id: "a13-data-layer-9",
    category: "data",
    categoryLabel: "Data",
    question: `In Room, you define a one-to-many relation with @Relation across UserEntity and PostEntity. What query strategy does Room use under the hood, and what's the N+1 risk?`,
    answerHtml: `<p>Room runs the parent query first, then issues a <i>second</i> query for the child entities using a <code>WHERE parentId IN (...)</code> batch (chunked if the parent list is large, since SQLite has a parameter limit). This is not classic N+1 (one query per parent) — Room batches it into effectively 2 queries total per <code>@Relation</code> field.</p><p>The real risk is nesting multiple <code>@Relation</code>s several levels deep (e.g. User → Posts → Comments → Likes): each level is its own batched query, so a deeply nested relation graph still multiplies query count linearly with depth, and Room can't push that down into a single SQL join — if you need one round-trip, write a manual <code>@Query</code> with explicit JOINs instead.</p>`,
  },
  {
    id: "a13-data-layer-10",
    category: "data",
    categoryLabel: "Data",
    question: `Why can't you observe a Flow<List<UserWithPosts>> (a @Relation query) as efficiently as a Flow<List<UserEntity>> in terms of recomposition/update frequency?`,
    answerHtml: `<p>Room's invalidation tracker watches the tables referenced by the query. A <code>@Relation</code>-based query touches both the parent and child tables, so the Flow re-runs (and emits) whenever <i>either</i> table changes — even if the change is to a Post belonging to a User who isn't currently visible in your list, or an unrelated column update that doesn't affect the relation shape.</p><p>This causes more frequent, sometimes unnecessary re-queries and downstream recomposition compared to a flat single-table Flow. Mitigations: scope relation queries narrowly (filter by the specific parent IDs the screen needs), or use <code>distinctUntilChanged()</code> downstream if the mapped UI model often turns out equal despite a re-emission.</p>`,
  },
  {
    id: "a13-data-layer-11",
    category: "data",
    categoryLabel: "Data",
    question: `What does Room's auto-migration (@AutoMigration) handle, and what schema changes still force you to write a manual Migration?`,
    answerHtml: `<p><code>@AutoMigration</code> (paired with exported schema JSON files via <code>exportSchema = true</code>) can handle additive/structural changes it can infer unambiguously: adding a table, adding a nullable column or one with a default value, deleting a table/column when explicitly annotated with <code>@DeleteTable</code>/<code>@DeleteColumn</code>, and renaming via <code>@RenameTable</code>/<code>@RenameColumn</code> annotations on the new version's <code>AutoMigration</code> spec.</p><p>You still need a manual <code>Migration</code> for anything requiring data transformation logic: splitting one column into two, merging tables, changing a column's type with conversion (e.g. String date → Long epoch millis), backfilling a new non-null column from existing data, or any migration where the SQL isn't a straight structural diff.</p>`,
  },
  {
    id: "a13-data-layer-12",
    category: "data",
    categoryLabel: "Data",
    question: `Your Room database ships with a destructive fallback (fallbackToDestructiveMigration()) in production. What's the actual failure mode users hit, and why is this almost always wrong for an offline-first app?`,
    answerHtml: `<p>On any version bump where you forgot (or chose not) to write a migration, Room drops and recreates every table rather than crashing — silently wiping all local data on upgrade. For a simple cache that's repopulated from network, that might be tolerable; for an offline-first app where Room is the source of truth (drafts, queued writes, user-generated content not yet synced), it's <i>data loss</i> presented to the user as the app just "forgetting" their work, with no error and no crash report to flag it.</p><p>The safer default is to let migrations fail loudly in development (Room throws <code>IllegalStateException</code> if no migration path exists) and only use destructive fallback narrowly — e.g. scoped to debug builds, or to specific known-disposable tables via <code>fallbackToDestructiveMigrationOnDowngrade</code>/per-table strategies — never as a blanket production safety net.</p>`,
  },
  {
    id: "a13-data-layer-13",
    category: "data",
    categoryLabel: "Data",
    question: `How should a Room DAO function be typed to support paging through a large offline-cached dataset without loading it all into memory, and how does this interact with a Flow-based observation?`,
    answerHtml: `<p>Return <code>PagingSource&lt;Int, Entity&gt;</code> from the DAO (via the <code>androidx.room:room-paging</code> artifact) and feed it to a <code>Pager</code> from Paging 3, rather than a <code>Flow&lt;List&lt;Entity&gt;&gt;</code> that materializes the entire table. Room generates a <code>PagingSource</code> implementation that issues windowed <code>LIMIT</code>/<code>OFFSET</code> queries and integrates with the invalidation tracker, so the <code>Pager</code>'s <code>Flow&lt;PagingData&lt;Entity&gt;&gt;</code> automatically invalidates and reloads the current pages when underlying tables change.</p><p>The tradeoff vs. a plain Flow: you give up easy access to the full list (e.g. for a "select all" bulk action) and take on Paging 3's complexity (RemoteMediator for network+DB paging, separators, loading states) in exchange for bounded memory use on large tables.</p>`,
  },
  {
    id: "a13-data-layer-14",
    category: "data",
    categoryLabel: "Data",
    question: `Describe the single-source-of-truth (SSOT) repository pattern for offline-first: where does a UI-triggered 'refresh' actually write data, and what does the UI ever read from?`,
    answerHtml: `<p>The UI never reads network responses directly. A repository function triggered by refresh (pull-to-refresh, app foreground, periodic sync) fetches from the network and writes the result into the local database (Room) as a side effect — it does not return the network response to the caller. The UI's data stream is a <code>Flow</code> querying the local DB, which Room re-emits automatically once the write lands, propagating the update to any active observers.</p><p>This means "refresh" and "read" are decoupled: a refresh can fail (no network) and the UI keeps showing the last known-good local data instead of an error screen, while a separate signal (e.g. a <code>refreshState</code> StateFlow) communicates loading/error status for the action itself, independent of the data stream.</p>`,
  },
  {
    id: "a13-data-layer-15",
    category: "data",
    categoryLabel: "Data",
    question: `In an SSOT repository, how do you expose 'is this data stale / currently syncing' to the UI without polluting the entity model that Room persists?`,
    answerHtml: `<p>Keep sync metadata (last-fetched timestamp, in-flight flag, error state) out of the persisted entity and out of business data entirely — model it as separate, often in-memory, state: a <code>MutableStateFlow&lt;SyncStatus&gt;</code> in the repository, or a small separate Room table (e.g. <code>RemoteKeys</code>/<code>SyncMetadata</code>) keyed by resource, updated alongside the data write inside the same transaction.</p><p>The UI layer then combines two flows — the data Flow from Room and the sync-status Flow — typically via <code>combine()</code>, rather than embedding a <code>isStale</code> boolean on every row, which would force a write (and a Flow re-emission, and recomposition) just to update staleness bookkeeping unrelated to the actual content.</p>`,
  },
  {
    id: "a13-data-layer-16",
    category: "data",
    categoryLabel: "Data",
    question: `Network-bound resource pattern: a repository function does 'emit cached data → fetch from network → save to DB → emit updated data.' What Kotlin construct cleanly expresses this, and what's the pitfall if you get the emission order wrong?`,
    answerHtml: `<p><code>flow { }</code> builder combined with Room's Flow-returning DAO query expresses this well: emit the first value from <code>dao.observe().first()</code> (or just let a <code>flatMapLatest</code> over the DB Flow handle re-emission), launch the network fetch, write the result via <code>dao.upsert()</code>, and let Room's own invalidation re-trigger the downstream Flow rather than manually emitting the post-fetch value yourself.</p><p>The pitfall: if you fetch-then-write inside the same flow builder and also manually <code>emit()</code> the new value, you can race with Room's own invalidation-triggered emission, producing either a duplicate emission or, worse, briefly emitting stale data after fresh data if the manual emit fires before the DB write is durably visible to the query. Letting the DB write be the single trigger for UI-facing emissions avoids this class of bug entirely.</p>`,
  },
  {
    id: "a13-data-layer-17",
    category: "data",
    categoryLabel: "Data",
    question: `Why is a Room upsert (INSERT ... ON CONFLICT) preferable to insert-or-update-by-checking-existence for syncing network data into the offline cache, beyond just being less code?`,
    answerHtml: `<p>Checking existence then branching (query → if exists, update; else insert) is two round-trips and a race: between the existence check and the write, another writer (e.g. a background sync worker and a user edit) could change the row, causing a lost update or a constraint violation on the insert. Room's <code>@Upsert</code> (or <code>@Insert(onConflict = OnConflictStrategy.REPLACE)</code> on a unique/primary key) is a single atomic statement handled by SQLite itself.</p><p>One real caveat: REPLACE-based conflict resolution deletes and re-inserts the conflicting row, which changes its SQLite <code>rowid</code> and fires delete+insert triggers/foreign-key cascades rather than an update — <code>@Upsert</code> (added in Room 2.5) compiles to a real <code>ON CONFLICT DO UPDATE</code> and avoids that side effect, making it the better default when rowid identity or cascade behavior matters.</p>`,
  },
  {
    id: "a13-data-layer-18",
    category: "data",
    categoryLabel: "Data",
    question: `How does Room's Flow support detect 'something changed' under the hood, and what's the performance implication for a DAO query with a complex WHERE clause or JOIN?`,
    answerHtml: `<p>Room wraps the query in an <code>InvalidationTracker</code> that registers a per-table trigger (via SQLite's own trigger mechanism, writing into an internal modification-log table) for every table referenced in the query. Any <code>INSERT</code>/<code>UPDATE</code>/<code>DELETE</code> on those tables marks them dirty; Room then <i>re-runs the entire query</i> to produce the new Flow emission — it does not diff rows or know whether the specific change actually affects this query's result set.</p><p>For a heavy JOIN/aggregate query, this means any write to any referenced table — even one wholly unrelated to the current result (e.g. a different user's row) — forces a full query re-execution. For hot tables, this can mean unnecessary CPU/IO. Mitigations: narrow queries to fewer tables, add appropriate indices so the re-run is cheap, or move very hot, narrowly-scoped reads off the relation-based Flow entirely.</p>`,
  },
  {
    id: "a13-data-layer-19",
    category: "data",
    categoryLabel: "Data",
    question: `What's the tradeoff between caching by Cache-Control max-age vs. caching by ETag/If-None-Match in OkHttp's response cache, particularly for an offline-first mobile client on flaky networks?`,
    answerHtml: `<p><code>max-age</code> caching is fully offline-capable within the freshness window — OkHttp serves from disk with zero network round-trip, which matters on flaky/metered connections. Its downside is staleness: the client can't know if the server's data actually changed until <code>max-age</code> expires, even if it changed seconds after the cache was written.</p><p>ETag/<code>If-None-Match</code> conditional requests always hit the network (a 304 still requires a round-trip) but guarantee freshness when reachable and save bandwidth (no body on 304) — at the cost of failing to reduce latency or working at all when offline. A common combination is short <code>max-age</code> for instant repeat loads plus ETag validation on revalidation, letting OkHttp's cache pick the cheaper path automatically per the <code>Cache-Control</code> semantics in the response.</p>`,
  },
  {
    id: "a13-data-layer-20",
    category: "data",
    categoryLabel: "Data",
    question: `You need to force OkHttp to serve a stale cached response when the device is offline, even though the server's max-age has expired. How do you do this without a custom interceptor rewriting every response header?`,
    answerHtml: `<p>Add a request-side <code>Cache-Control</code> override on the specific call: <code>CacheControl.Builder().maxStale(Int.MAX_VALUE, TimeUnit.SECONDS).build()</code> (or <code>only-if-cached</code> combined with <code>max-stale</code>) set via <code>request.cacheControl()</code>. This tells OkHttp's cache it's acceptable to return an expired entry rather than going to network, which it otherwise wouldn't do by default since the server's response headers govern freshness.</p><p>This is request-scoped, so you typically gate it behind connectivity detection (e.g. only attach <code>max-stale</code> when <code>ConnectivityManager</code> reports no network) rather than applying it globally, which would mask genuinely stale data even when fresh data is reachable.</p>`,
  },
  {
    id: "a13-data-layer-21",
    category: "data",
    categoryLabel: "Data",
    question: `In a repository combining a local Room Flow with a remote fetch, how do you avoid the UI briefly showing an empty/loading state every time the screen is reopened, if the data is already cached locally?`,
    answerHtml: `<p>Structure the repository so the Flow exposed to the UI is sourced directly from Room (<code>dao.observeX()</code>), and trigger the network refresh as a fire-and-forget side effect (e.g. launched in a coroutine scope tied to the repository or ViewModel, not awaited before emitting). Because Room's Flow immediately emits whatever's already in the table on collection, the UI gets the cached data instantly on screen-open, with the network refresh updating it later (if needed) via the same Flow once Room's write lands.</p><p>The bug pattern to avoid is gating the Flow behind the network call (e.g. <code>flow { val fresh = api.get(); dao.insert(fresh); emitAll(dao.observe()) }</code> with no early emission) — that forces every screen-open to wait on a network round-trip before showing anything, defeating the offline-first benefit even when perfectly good cached data already exists.</p>`,
  },
  {
    id: "a13-data-layer-22",
    category: "data",
    categoryLabel: "Data",
    question: `What's the difference between OkHttp's connection pool reuse and HTTP/2 multiplexing in terms of how many concurrent Retrofit calls actually hit the network simultaneously over one host?`,
    answerHtml: `<p>OkHttp's <code>ConnectionPool</code> reuses idle TCP/TLS connections across requests (avoiding repeated handshake cost), but under HTTP/1.1 each connection serves one request at a time — concurrency to one host is bounded by the pool's max idle connections per host (5 by default), with extra requests queued.</p><p>HTTP/2 (negotiated via ALPN, which OkHttp does automatically when the server supports it) multiplexes many logical streams over a <i>single</i> TCP connection, so concurrent Retrofit calls to the same host don't each need their own connection — they share one, dramatically reducing connection overhead and avoiding the per-host queueing limit. This is largely transparent to app code, but it matters when reasoning about why a burst of parallel API calls on app start isn't as expensive as it looks.</p>`,
  },
  {
    id: "a13-data-layer-23",
    category: "data",
    categoryLabel: "Data",
    question: `You add an application interceptor that strips Cache-Control: no-store from responses to force caching of endpoints the backend marked non-cacheable. What's the real risk?`,
    answerHtml: `<p><code>no-store</code> is usually set deliberately for responses containing sensitive or rapidly-changing data (auth tokens, payment status, one-time codes). Overriding it client-side to force caching can persist that data to the OkHttp disk cache unencrypted, where it survives even after the in-memory session ends — a security/privacy regression the backend explicitly tried to prevent.</p><p>If the goal is offline availability for data the backend marks non-cacheable, the correct layer is your own application-level cache (Room) under your own invalidation rules, not silently defeating the HTTP cache-control contract the server set for a reason you may not have full visibility into.</p>`,
  },
  {
    id: "a13-data-layer-24",
    category: "data",
    categoryLabel: "Data",
    question: `How would you implement a 'write succeeds locally first, syncs to server in the background' flow (optimistic offline write) using Room + a repository, and what has to be tracked to make it safe?`,
    answerHtml: `<p>The write goes to Room immediately (marking the row e.g. <code>syncStatus = PENDING</code>), the UI's Flow re-emits instantly showing the optimistic state, and a background mechanism (WorkManager, ideally with network constraints) picks up rows in <code>PENDING</code> state and pushes them to the server, updating <code>syncStatus = SYNCED</code> (or <code>FAILED</code>) on completion — which again flows back to the UI automatically via Room's Flow.</p><p>What must be tracked beyond the data itself: a local-only identifier strategy if the server assigns the real ID (so you can reconcile the optimistic row with the server's response, e.g. a temporary negative/UUID local ID swapped for the server ID post-sync), retry/backoff state for failed syncs, and conflict handling if the same entity was also modified server-side in the meantime (last-write-wins vs. a merge strategy is a product decision, not just an engineering one).</p>`,
  },
];

export const ADVANCED13_QUIZ: QuizQuestion[] = [
  {
    id: "a13q-data-layer-1",
    category: "data",
    categoryLabel: "Data",
    question: `An app needs to retry a failed request with a refreshed auth token after a 401, ensuring concurrent requests don't each trigger their own token refresh. Which OkHttp mechanism is purpose-built for this?`,
    options: [`A network interceptor that inspects every response code`, `An Authenticator, synchronized so only one refresh call runs at a time`, `Setting retryOnConnectionFailure(true) on the OkHttpClient`, `An application interceptor that calls chain.proceed() twice`],
    answer: 1,
    explanationHtml: `<p><b>Authenticator</b> is invoked by OkHttp specifically on authentication failures (401/407) and is designed to return a new request with updated credentials; synchronizing the refresh logic inside it (e.g. with a lock) prevents a thundering herd of simultaneous refresh calls from concurrent requests. <code>retryOnConnectionFailure</code> only covers transport-level failures, not 401s, and manually double-calling <code>proceed()</code> in an interceptor risks resending unrepeatable request bodies.</p>`,
  },
  {
    id: "a13q-data-layer-2",
    category: "data",
    categoryLabel: "Data",
    question: `A Room entity has a @Relation defining UserWithPosts (one User to many Posts). What does Room actually generate to populate this relation?`,
    options: [`A single SQL query with a JOIN between the user and post tables`, `One query for the user, then a second batched query for posts filtered by parent IDs`, `One query per post, issued in a loop for each user returned`, `A SQLite view that materializes the relation on write`],
    answer: 1,
    explanationHtml: `<p>Room runs the parent query first, then a separate query for child rows using a batched <code>WHERE parentId IN (...)</code> (chunked for large ID sets) — not a JOIN, and not classic per-row N+1. This keeps it to roughly two queries per <code>@Relation</code> field regardless of row count, but nested relations multiply this per level of nesting.</p>`,
  },
  {
    id: "a13q-data-layer-3",
    category: "data",
    categoryLabel: "Data",
    question: `Which Room schema change CANNOT be handled by @AutoMigration and requires a manually written Migration?`,
    options: [`Adding a new table`, `Adding a nullable column to an existing table`, `Splitting one column's data into two new columns with transformed values`, `Deleting a column, annotated with @DeleteColumn`],
    answer: 2,
    explanationHtml: `<p>Auto-migrations handle structural diffs Room can infer unambiguously: new tables, new nullable/defaulted columns, and explicitly annotated deletes/renames. Anything requiring actual data transformation logic — like splitting a column's existing values into two new columns — has no unambiguous structural inference, so it needs a hand-written <code>Migration</code> with explicit SQL/data-copy logic.</p>`,
  },
  {
    id: "a13q-data-layer-4",
    category: "data",
    categoryLabel: "Data",
    question: `In the single-source-of-truth offline-first pattern, what should a 'pull to refresh' action actually do?`,
    options: [`Fetch from the network and return the response directly to the UI to render`, `Fetch from the network, write the result into the local database, and let the UI's existing DB-backed Flow pick up the change`, `Clear the local database, then fetch from the network and repopulate it before unblocking the UI`, `Bypass the repository and call the Retrofit service directly from the ViewModel`],
    answer: 1,
    explanationHtml: `<p>SSOT means the UI only ever reads from the local database (e.g. via a Room <code>Flow</code>). A refresh is a write-side operation: fetch, then persist to the DB; the UI updates because its existing Flow re-emits, not because the refresh call hands data back directly. This keeps the UI resilient — a failed refresh just means no DB update, and the UI keeps showing the last good cached state instead of erroring out.</p>`,
  },
  {
    id: "a13q-data-layer-5",
    category: "data",
    categoryLabel: "Data",
    question: `An OkHttp response has Cache-Control: max-age=300 but the device just went offline and the cached entry is 600 seconds old. By default, what does OkHttp's cache do for a new request to the same URL?`,
    options: [`Serves the stale cached response anyway since the device is offline`, `Returns the cached response because OkHttp ignores max-age when offline`, `Treats the cache entry as stale and attempts a network request, which fails (no fallback to stale data without explicit configuration)`, `Automatically retries with exponential backoff until network returns`],
    answer: 2,
    explanationHtml: `<p>By default OkHttp respects the freshness window strictly: once <code>max-age</code> is exceeded, the cache entry is stale and a network attempt is required, which fails offline with no automatic fallback. To allow stale data when offline, you must explicitly opt in per-request with a <code>CacheControl</code> that sets <code>maxStale</code>, typically gated behind a connectivity check.</p>`,
  },
  {
    id: "a13q-data-layer-6",
    category: "data",
    categoryLabel: "Data",
    question: `Why is @Upsert generally preferred over @Insert(onConflict = OnConflictStrategy.REPLACE) when syncing server data into a Room table that has foreign-key relationships or triggers?`,
    options: [`Upsert is faster because it skips conflict checking entirely`, `REPLACE deletes and re-inserts the conflicting row (changing its rowid and firing delete/insert side effects), while Upsert performs a true UPDATE on conflict`, `Upsert is the only strategy that works inside a Flow-returning DAO`, `REPLACE silently ignores conflicting rows instead of writing them`],
    answer: 1,
    explanationHtml: `<p><code>OnConflictStrategy.REPLACE</code> is implemented as SQLite's <code>INSERT OR REPLACE</code>, which under the hood deletes the existing conflicting row and inserts a new one — changing its <code>rowid</code> and triggering any delete/insert triggers or cascading foreign-key actions tied to row identity. <code>@Upsert</code> (Room 2.5+) compiles to a real <code>INSERT ... ON CONFLICT DO UPDATE</code>, preserving row identity and only firing update-relevant behavior, which matters whenever cascades or rowid stability are relied on.</p>`,
  },
  {
    id: "a13q-data-layer-7",
    category: "data",
    categoryLabel: "Data",
    question: `A Room DAO query joins three tables and is exposed as a Flow. A write happens to one of the three tables, completely unrelated in content to the rows currently shown by this query. What happens?`,
    options: [`Nothing — Room diffs the actual rows affected and only re-emits if the visible result set changed`, `The Flow re-runs the full query and re-emits, because Room's invalidation tracker watches at the table level, not the row level`, `The query silently goes stale until the app restarts`, `Room throws because triggers can't span three tables in a join`],
    answer: 1,
    explanationHtml: `<p>Room's <code>InvalidationTracker</code> registers per-table triggers and marks a table dirty on any write to it, regardless of which rows changed or whether they're relevant to a given query's result. Any query referencing that table re-runs in full on the next invalidation cycle. This is a known cost of Flow-backed multi-table queries — it trades some unnecessary re-execution for not having to hand-write fine-grained change tracking.</p>`,
  },
];
