// Batch 14 — Unit testing ViewModels with coroutines, instrumented tests, Compose UI testing (semantics, test tags), Espresso vs Compose testing, and Robolectric tradeoffs.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED14_FLASHCARDS: Flashcard[] = [
  {
    id: "a14-testing-1",
    category: "test",
    categoryLabel: "Testing",
    question: `When unit testing a ViewModel that launches coroutines on \`viewModelScope\`, why does swapping \`Dispatchers.Main\` for a \`TestDispatcher\` matter, and what's the modern way to do it?`,
    answerHtml: `<p><code>viewModelScope</code> is hardcoded to launch on <code>Dispatchers.Main</code>, which doesn't exist on the JVM unit-test classpath — without a substitute you get <code>Module with the Main dispatcher had failed to initialize</code>. The fix is <code>Dispatchers.setMain(testDispatcher)</code> in setup and <code>Dispatchers.resetMain()</code> in teardown, using a <code>StandardTestDispatcher</code> or <code>UnconfinedTestDispatcher</code> from <code>kotlinx-coroutines-test</code>.</p><p>In practice this is wrapped in a JUnit <code>TestWatcher</code>/rule (e.g. a custom <code>MainDispatcherRule</code>) so every test class doesn't repeat the boilerplate, and so the dispatcher is reset even if the test throws.</p>`,
  },
  {
    id: "a14-testing-2",
    category: "test",
    categoryLabel: "Testing",
    question: `What's the practical difference between \`StandardTestDispatcher\` and \`UnconfinedTestDispatcher\`, and when would you deliberately choose \`StandardTestDispatcher\`?`,
    answerHtml: `<p><code>UnconfinedTestDispatcher</code> runs coroutines eagerly and immediately on the calling thread until the first suspension point, so code often 'just works' without manual advancement — but that masks real scheduling and can hide bugs where ordering matters.</p><p><code>StandardTestDispatcher</code> queues coroutines and only runs them when you call <code>advanceUntilIdle()</code>, <code>runCurrent()</code>, or <code>advanceTimeBy()</code>. Choose it when you need to assert on intermediate states — e.g. that a loading flag is <code>true</code> <i>before</i> a network call resolves — since the queued dispatcher lets you pause mid-flight and inspect state deterministically.</p>`,
  },
  {
    id: "a14-testing-3",
    category: "test",
    categoryLabel: "Testing",
    question: `A ViewModel exposes a \`StateFlow<UiState>\`. Why is \`Turbine\`'s \`awaitItem()\` generally preferred over manually collecting into a \`MutableList\` for assertions?`,
    answerHtml: `<p>Manual collection requires launching a background job (e.g. in a <code>backgroundScope</code> or a separate coroutine) and remembering to cancel it, plus races where you assert before the collector has processed an emission. <code>Turbine</code>'s <code>flow.test { }</code> creates a structured scope that suspends until each emission actually arrives, and <code>awaitItem()</code> fails loudly with a clear timeout error if nothing emits — rather than a flaky empty-list assertion.</p><p>It also enforces that you account for every emission: <code>cancelAndIgnoreRemainingEvents()</code> or <code>awaitComplete()</code> makes leftover unconsumed events an explicit decision, not a silent gap.</p>`,
  },
  {
    id: "a14-testing-4",
    category: "test",
    categoryLabel: "Testing",
    question: `Why does a \`StateFlow\` test sometimes fail to see the *initial* value when collected with \`Turbine\`, and how do you fix it?`,
    answerHtml: `<p><code>StateFlow</code> is a hot, conflated flow — if the ViewModel already emitted its initial value before the test starts collecting, and another value follows before your first <code>awaitItem()</code>, you can miss the initial state because conflation only guarantees the latest value, not every intermediate one.</p><p>Fix: start the <code>flow.test {}</code> block (or your collector) <i>before</i> triggering the action that causes emissions, so the test subscribes while the flow is still at its initial value, and assert on <code>awaitItem()</code> immediately rather than after invoking side effects.</p>`,
  },
  {
    id: "a14-testing-5",
    category: "test",
    categoryLabel: "Testing",
    question: `What does \`runTest\` from \`kotlinx-coroutines-test\` do differently from a plain \`runBlocking\`, and why does that matter for testing \`delay()\`-based logic?`,
    answerHtml: `<p><code>runTest</code> runs the test body in a <code>TestScope</code> backed by virtual time: calls to <code>delay()</code> are skipped instantly instead of actually pausing the thread, and the test fails if there are uncompleted/leaked child coroutines when it finishes.</p><p>This means a debounce or retry-with-backoff implementation using real <code>delay(2000)</code> calls can be tested in milliseconds of wall-clock time, while still verifying the *relative* timing logic is correct.</p>`,
  },
  {
    id: "a14-testing-6",
    category: "test",
    categoryLabel: "Testing",
    question: `You have a ViewModel that calls a repository returning \`Flow<T>\`. What's the risk of testing it with a fake repository that uses \`flowOf(...)\` versus one backed by a \`MutableSharedFlow\` or \`Channel\`?`,
    answerHtml: `<p><code>flowOf(value)</code> emits and completes immediately — fine for a single happy-path snapshot, but it can't model a slow/never-completing upstream, multiple emissions over time, or cancellation behavior. Tests built only on <code>flowOf</code> won't catch bugs like a ViewModel that doesn't cancel a previous collection when a new request starts (missing <code>collectLatest</code> vs <code>collect</code>).</p><p>A fake backed by <code>MutableSharedFlow</code>/<code>Channel</code> lets the test control emission timing explicitly, which is necessary to verify loading-state transitions, cancellation-on-restart, and race conditions.</p>`,
  },
  {
    id: "a14-testing-7",
    category: "test",
    categoryLabel: "Testing",
    question: `Why can't you reliably test \`combine\`d or \`stateIn\`-derived flows from a ViewModel using a single \`awaitItem()\` call right after triggering an update?`,
    answerHtml: `<p><code>stateIn</code> with <code>SharingStarted.WhileSubscribed()</code> only starts the upstream when there's an active collector, and \`combine\` re-emits whenever *any* upstream emits — so a single state change can produce several rapid intermediate combined values before settling, especially if multiple source flows update in the same dispatch.</p><p>The reliable pattern is to drain expected intermediate emissions explicitly (or use <code>expectMostRecentItem()</code> in Turbine when only the final value matters) rather than assuming the very next emission is the final settled state.</p>`,
  },
  {
    id: "a14-testing-8",
    category: "test",
    categoryLabel: "Testing",
    question: `What is \`SharingStarted.WhileSubscribed(5000)\` doing on a \`stateIn\` call, and what testing gotcha does the 5-second timeout create?`,
    answerHtml: `<p>It keeps the upstream flow active for 5 seconds after the last collector disappears, so a quick screen-rotation-style unsubscribe/resubscribe doesn't restart an expensive upstream (like a network-backed flow). It's a production optimization, not a testing concern by itself.</p><p>The gotcha: in a <code>StandardTestDispatcher</code> test using virtual time, if you stop collecting and don't <code>advanceTimeBy</code> far enough, the upstream coroutine looks like it's still 'running' and can leak into <code>runTest</code>'s end-of-test idle/leak check, producing a confusing failure unrelated to your actual assertion.</p>`,
  },
  {
    id: "a14-testing-9",
    category: "test",
    categoryLabel: "Testing",
    question: `Why might a senior engineer deliberately avoid mocking a \`Flow\`-returning repository method with Mockito/MockK's \`every { } returns flowOf(...)\` for anything beyond the simplest case?`,
    answerHtml: `<p>Mocking frameworks return the same flow instance/builder regardless of call count or arguments by default, which makes it awkward to express 'first call returns loading then error, second call (retry) returns success' — you end up chaining <code>andThen</code> or argument matchers that read worse than a small hand-written fake.</p><p>A lightweight fake class with mutable internal state (e.g. a backing <code>MutableStateFlow</code> the test pushes into) is usually clearer, avoids mocking-framework coroutine quirks (like relaxed mocks silently returning empty flows), and documents the contract the ViewModel actually depends on.</p>`,
  },
  {
    id: "a14-testing-10",
    category: "test",
    categoryLabel: "Testing",
    question: `What's the core architectural difference between a Robolectric test and a true instrumented (\`androidTest\`) test, and what category of bug does Robolectric structurally miss?`,
    answerHtml: `<p>Robolectric runs on the local JVM (like a unit test) but provides 'shadow' implementations of Android framework classes, simulating the framework instead of running it on a real or emulated OS. Instrumented tests run the actual compiled APK on a real device/emulator with the genuine Android framework.</p><p>Robolectric structurally misses bugs that live in the real OS/ART behavior it can't fully simulate: real rendering/layout pixel output, real IPC and Binder behavior, actual hardware sensors/camera, exact OEM-specific framework deviations, and subtle threading/timing differences from real device scheduling.</p>`,
  },
  {
    id: "a14-testing-11",
    category: "test",
    categoryLabel: "Testing",
    question: `Why is Robolectric often faster in CI than Espresso instrumented tests, and what's the tradeoff senior teams accept when they push more coverage onto Robolectric?`,
    answerHtml: `<p>Robolectric tests run on the local JVM in parallel with regular unit tests — no APK install, no emulator boot, no ADB round-trips — so a suite that would take minutes on an emulator can run in seconds. This makes it attractive for fast feedback loops and pre-merge CI gates.</p><p>The tradeoff: shadow classes can drift from real framework behavior (especially on newer API levels before Robolectric ships matching shadows), so teams accept a residual risk that some Robolectric-green tests fail or behave differently on a real device, and keep a smaller, slower instrumented suite as the final truth check.</p>`,
  },
  {
    id: "a14-testing-12",
    category: "test",
    categoryLabel: "Testing",
    question: `In Compose UI testing, what does \`composeTestRule.onNodeWithTag("submit_button").performClick()\` actually depend on under the hood, and why is relying on text content instead of a \`testTag\` often a maintenance trap?`,
    answerHtml: `<p>Compose UI tests query the <b>semantics tree</b>, a parallel tree Compose builds for accessibility and tooling — <code>testTag</code> is a semantics property set via <code>Modifier.testTag("submit_button")</code> that has no visual effect but gives the test a stable, semantics-tree handle independent of what's rendered on screen.</p><p>Matching on visible text (<code>onNodeWithText("Submit")</code>) couples the test to copy, localization, and case changes — a translator or copywriter can break the test suite without touching logic. <code>testTag</code> decouples test identity from presentation, the same reasoning behind RN's <code>testID</code> convention.</p>`,
  },
  {
    id: "a14-testing-13",
    category: "test",
    categoryLabel: "Testing",
    question: `What does Compose's \`composeTestRule.waitForIdle()\` actually wait for, and why isn't it sufficient when testing a screen that does an async network call on launch?`,
    answerHtml: `<p><code>waitForIdle()</code> blocks until Compose's own work queues are idle: pending recompositions, measure/layout/draw passes, and animations driven by the Compose clock. It does <i>not</i> know about arbitrary coroutines or callbacks outside Compose's control, like a ViewModel's network call running on <code>viewModelScope</code> with real dispatchers.</p><p>For async work, the correct approach is either to inject a deterministic test dispatcher/fake repository so the data is already resolved before assertions run, or to use <code>composeTestRule.waitUntil { }</code> polling for a semantics condition (e.g. the loading spinner's tag disappearing) rather than assuming idle implies 'data loaded.'</p>`,
  },
  {
    id: "a14-testing-14",
    category: "test",
    categoryLabel: "Testing",
    question: `When would you reach for \`createComposeRule()\` versus \`createAndroidComposeRule<MyActivity>()\` in a Compose UI test, and what capability do you lose with the lighter-weight option?`,
    answerHtml: `<p><code>createComposeRule()</code> hosts your composable in a minimal, generic test Activity Compose provides — fastest to run, ideal for testing an isolated composable in unit-style fashion (\`setContent { MyComposable() }\`). <code>createAndroidComposeRule&lt;MyActivity&gt;()</code> launches your *actual* Activity, giving you a real intent/lifecycle/\`ViewModelStoreOwner\` context.</p><p>With the lighter rule you lose anything that depends on the real hosting Activity: \`Activity\`-scoped \`ViewModel\`s wired through your app's actual DI graph, deep-link intent handling, window-level configuration (e.g. real \`Activity\` \`onConfigurationChanged\`), and behavior that depends on your app's actual theme/manifest setup.</p>`,
  },
  {
    id: "a14-testing-15",
    category: "test",
    categoryLabel: "Testing",
    question: `How does Compose's semantics-based testing handle synchronization differently from Espresso's \`IdlingResource\` mechanism, and what's the practical payoff?`,
    answerHtml: `<p>Espresso requires you to register custom <code>IdlingResource</code>s (or use <code>IdlingRegistry</code>) so it knows to wait for async work — e.g. wrapping an OkHttp call counter — and forgetting one is a classic source of flaky, manually-added <code>Thread.sleep</code> hacks. Compose testing synchronizes automatically with the Compose runtime: it knows about pending recomposition/animation/measure work without any registration because it's built into the same framework.</p><p>The payoff is fewer flaky waits for pure-Compose UI changes, but it's also why Compose sync says nothing about non-Compose async (network, coroutines) — that part still needs explicit handling, same root problem as Espresso's IdlingResource gap, just narrower in scope.</p>`,
  },
  {
    id: "a14-testing-16",
    category: "test",
    categoryLabel: "Testing",
    question: `What's the key reason Espresso's \`ViewActions\` and matchers don't directly work on a screen built with Compose, and what bridges the gap when an app mixes both UI toolkits?`,
    answerHtml: `<p>Espresso operates on the Android View hierarchy (<code>View</code>/<code>ViewGroup</code>), matching via <code>ViewMatchers</code> like <code>withId</code>. Compose doesn't build a \`View\` per element — a whole composable subtree is typically hosted inside a single <code>ComposeView</code>, so from Espresso's perspective there's often just one opaque view with no internal IDs to match against.</p><p>The bridge is <code>androidx.compose.ui.test.junit4</code>'s interop APIs: you can obtain a <code>ComposeTestRule</code> alongside an Espresso \`ActivityScenarioRule\` and use <code>onView(...)</code> for the View-based parts and <code>composeTestRule.onNodeWithTag(...)</code> for the Compose parts of the same screen, since Compose semantics nodes can also be exposed as accessibility nodes Espresso partially understands.</p>`,
  },
  {
    id: "a14-testing-17",
    category: "test",
    categoryLabel: "Testing",
    question: `In a Compose semantics tree, what does 'merged' vs 'unmerged' semantics mean, and why does it matter when a \`onNodeWithText\` match unexpectedly fails inside a custom composable made of several child composables?`,
    answerHtml: `<p>Compose merges semantics from descendant nodes into a parent when the parent is marked as a single accessibility-meaningful unit (e.g. a clickable \`Row\` containing an \`Icon\` and \`Text\` — common for list items/buttons). By default, test finders query the <i>merged</i> tree, matching what a screen reader would announce as one unit.</p><p>If you query with \`useUnmergedTree = true\` you instead see each child's raw semantics individually — necessary when you need to assert on a specific child's property (like an icon's content description) that got combined away in the merged view, or you'll get a 'node not found' failure on the merged tree even though the text is technically present in a child.</p>`,
  },
  {
    id: "a14-testing-18",
    category: "test",
    categoryLabel: "Testing",
    question: `When you \`mock()\` an Android \`Context\`-dependent collaborator in a JVM unit test instead of using Robolectric, what's the typical limitation, and what's a concrete symptom?`,
    answerHtml: `<p>Plain Mockito mocks of \`Context\`-related classes only stub the methods you explicitly program — any call you didn't anticipate (like \`getResources().getString(id)\` returning the real localized string, or \`getSystemService()\` wiring to a real manager) throws \`NullPointerException\` or returns a default/null rather than behaving like a real Android object.</p><p>Symptom: a ViewModel test that calls a thin wrapper around \`Resources.getString(R.string.error_generic)\` passes when you stub that one call, but breaks the moment the production code adds a second resource lookup the test didn't anticipate — Robolectric's shadowed \`Resources\` would have resolved it for free from the real resource table.</p>`,
  },
  {
    id: "a14-testing-19",
    category: "test",
    categoryLabel: "Testing",
    question: `What does it mean that Robolectric tests are 'unit tests' in Gradle's source-set sense (\`src/test\`) even though they exercise framework code, and what build-speed consequence follows?`,
    answerHtml: `<p>Robolectric lives in \`src/test\` (the JVM unit-test source set) rather than \`src/androidTest\`, so it compiles and runs as part of \`testDebugUnitTest\`/\`./gradlew test\` — no APK packaging, no device/emulator, no Gradle Managed Device or \`connectedAndroidTest\` task involved.</p><p>Consequence: Robolectric tests benefit from the same incremental compilation, parallel JVM forking, and local caching as ordinary JUnit tests, and can run on any CI runner without an Android SDK emulator image — which is why teams push Robolectric hard for cases instrumented tests are 'too slow to justify' for routine PR gating.</p>`,
  },
  {
    id: "a14-testing-20",
    category: "test",
    categoryLabel: "Testing",
    question: `Why is \`runTest\`'s built-in unfinished-coroutine detection a double-edged sword when testing a ViewModel that launches a 'fire and forget' coroutine (e.g. analytics logging) inside \`init {}\`?`,
    answerHtml: `<p><code>runTest</code> fails the test if there are coroutines still running on its \`TestScope\` dispatcher when the test body completes, by design — it's meant to catch real leaks (uncancelled jobs, unconsumed flows). A \`viewModelScope.launch { delay(Long.MAX_VALUE); ... }\`-style fire-and-forget task, or one waiting on a never-completing flow, can trip this even though it's intentional production behavior.</p><p>The documented fix is <code>TestScope.backgroundScope</code>: launching the long-running/never-completing piece of work there (instead of plain \`runTest\`'s own scope) tells the framework it's expected to outlive the test body, and it's auto-cancelled when the test ends — no manual job tracking or suppressing the leak check needed. This only applies to coroutines you've confirmed are intentionally indefinite; an unexpected leak is still a real bug to fix, not paper over.</p>`,
  },
  {
    id: "a14-testing-21",
    category: "test",
    categoryLabel: "Testing",
    question: `How should a senior engineer answer 'why not just use Robolectric for everything and skip the device matrix'?`,
    answerHtml: `<p>Robolectric's shadows are reverse-engineered approximations of framework behavior maintained by the open-source project, not the actual OS — they lag new API levels, don't always replicate vendor (Samsung/OEM) framework patches, and can't validate real rendering, real touch dispatch timing, real camera/sensor/Bluetooth hardware, or genuine multi-process/Binder behavior.</p><p>A mature strategy treats Robolectric as the fast middle tier (faster than instrumented, more realistic than pure mocks) and keeps a thinner instrumented + manual/automated device-matrix layer for the things that are fundamentally about *real device behavior* — exactly the classic testing-pyramid argument applied to Android's specific JVM-vs-device split.</p>`,
  },
  {
    id: "a14-testing-22",
    category: "test",
    categoryLabel: "Testing",
    question: `In a \`MainDispatcherRule\` implemented as a JUnit \`TestWatcher\`, why does overriding \`finished()\` matter more than just setting the dispatcher once in a \`@Before\` method?`,
    answerHtml: `<p>A \`TestWatcher\`'s \`finished()\` callback runs even if the test throws an assertion error or exception, guaranteeing \`Dispatchers.resetMain()\` always executes. The \`TestRule\` form is preferred mainly because it's reusable as a single \`@get:Rule\` line shared across many test classes, instead of every class repeating its own \`@Before\`/\`@After\` boilerplate.</p><p>Skipping the symmetric reset risks one test's \`TestDispatcher\` leaking as the \`Main\` dispatcher into the next test executed in the same JVM fork, causing order-dependent flakiness.</p>`,
  },
];

export const ADVANCED14_QUIZ: QuizQuestion[] = [
  {
    id: "a14q-testing-1",
    category: "test",
    categoryLabel: "Testing",
    question: `A ViewModel test using \`StandardTestDispatcher\` calls a suspend function on \`viewModelScope.launch\` but the resulting \`StateFlow\` never updates within the test body. What is the most likely missing step?`,
    options: [`The test needs \`Dispatchers.resetMain()\` called before the assertion`, `The test needs to call \`advanceUntilIdle()\` (or \`runCurrent()\`) to actually run the queued coroutine`, `\`StateFlow\` cannot be tested outside instrumented tests`, `The ViewModel must use \`GlobalScope\` instead of \`viewModelScope\``],
    answer: 1,
    explanationHtml: `<code>StandardTestDispatcher</code> queues coroutines instead of running them eagerly; without <code>advanceUntilIdle()</code> or <code>runCurrent()</code>, the launched coroutine sits unexecuted and the state never updates. <code>resetMain()</code> is a teardown step, not relevant mid-test. <code>StateFlow</code> is fully testable on the JVM, and <code>viewModelScope</code> is correct as-is.`,
  },
  {
    id: "a14q-testing-2",
    category: "test",
    categoryLabel: "Testing",
    question: `Why does \`composeTestRule.onNodeWithTag("loading_spinner").assertDoesNotExist()\` sometimes pass immediately even though the spinner is still genuinely showing on a real device after the same code change?`,
    options: [`\`assertDoesNotExist()\` is broken in current Compose testing versions`, `Compose test idling only synchronizes with Compose's own recomposition/animation work, not external async work driving the spinner's visibility off a non-test dispatcher`, `Test tags are stripped in release builds, so the assertion is meaningless`, `The semantics tree never includes loading indicators by design`],
    answer: 1,
    explanationHtml: `Compose's automatic test synchronization covers Compose-internal work (recomposition, layout, the Compose animation clock). If the spinner's visibility is driven by a coroutine on a real (non-test) dispatcher, synchronization can race ahead of that external work, making the assertion pass prematurely while the real app still shows the spinner a moment longer.`,
  },
  {
    id: "a14q-testing-3",
    category: "test",
    categoryLabel: "Testing",
    question: `A team wants both speed and realism, so they keep Robolectric tests for most ViewModel+UI logic and a smaller Espresso/Compose-instrumented suite for a handful of screens. What is the most defensible technical reason for NOT eliminating the instrumented layer entirely?`,
    options: [`Espresso is required by the Play Store for app approval`, `Robolectric cannot compile Kotlin coroutines code`, `Robolectric's shadow classes approximate framework behavior and can diverge from real OS/ART/OEM behavior, real rendering, and real IPC, which only an actual device or emulator can validate`, `Instrumented tests run faster than Robolectric so they should replace it entirely`],
    answer: 2,
    explanationHtml: `Robolectric simulates the Android framework via shadow classes on the JVM; it's not the genuine OS, so its results are an approximation. Real-device-specific behavior (actual rendering output, Binder/IPC, hardware, OEM deviations) can only be validated with a real device or emulator. Instrumented tests are slower, not faster, and Play Store approval has no such testing requirement.`,
  },
  {
    id: "a14q-testing-4",
    category: "test",
    categoryLabel: "Testing",
    question: `In a Compose UI test, \`onNodeWithText("Submit")\` fails to find a node even though a \`Row { Icon(...); Text("Submit") }\` is visibly rendered with a click modifier on the \`Row\`. What is the most likely cause?`,
    options: [`Text nodes are never included in the semantics tree`, `The default merged-semantics tree combined the \`Icon\` and \`Text\` into the parent \`Row\`'s semantics, and the query needs \`useUnmergedTree = true\` or should target the merged node by its own properties`, `\`onNodeWithText\` only works with \`createAndroidComposeRule\`, not \`createComposeRule\``, `Compose does not support clickable \`Row\`s in tests`],
    answer: 1,
    explanationHtml: `When a parent like a clickable \`Row\` is marked as a single semantics-meaningful node, Compose merges its descendants' semantics into it by default. Querying the child \`Text\`'s raw text on the unmerged tree (\`useUnmergedTree = true\`), or matching the merged node directly, resolves the common 'node not found' surprise for composed UI elements.`,
  },
  {
    id: "a14q-testing-5",
    category: "test",
    categoryLabel: "Testing",
    question: `A ViewModel test fakes a repository's \`Flow<Resource<Data>>\` with \`flowOf(Resource.Success(data))\`. The test passes, but a production bug exists where rapid re-invocation of the loading function doesn't cancel the previous in-flight request. Why does this test fail to catch that bug?`,
    options: [`\`flowOf\` cannot be collected inside \`runTest\``, `The fake completes immediately on a single emission, so there's no way to observe overlapping in-flight emissions or verify cancellation/\`collectLatest\` behavior`, `The bug only exists in instrumented tests, never in unit tests`, `\`Resource.Success\` wrapper types break Turbine assertions`],
    answer: 1,
    explanationHtml: `\`flowOf(value)\` emits once and completes synchronously, leaving no window to simulate two overlapping requests or assert that the first was cancelled when the second started. A fake backed by a \`MutableSharedFlow\`/\`Channel\` is needed to model timing-sensitive bugs like missing \`collectLatest\` cancellation.`,
  },
  {
    id: "a14q-testing-6",
    category: "test",
    categoryLabel: "Testing",
    question: `Which statement best describes how Espresso and Compose's testing framework differ in how they decide a UI is ready to be asserted on?`,
    options: [`They are identical; both use \`IdlingResource\` registration exclusively`, `Espresso requires explicit \`IdlingResource\` registration for async work it doesn't natively understand, while Compose's test APIs automatically synchronize with Compose's own recomposition/layout/animation work without registration`, `Compose requires \`IdlingResource\` registration just like Espresso, with no automatic synchronization`, `Espresso automatically detects all async work including coroutines, making \`IdlingResource\` obsolete`],
    answer: 1,
    explanationHtml: `Espresso's synchronization model is built around \`IdlingResource\`s that you must register for anything outside its default View/main-looper awareness. Compose testing has built-in awareness of pending Compose work (recomposition, layout, the test clock for animations) with no manual registration — though, like Espresso, it still has no visibility into arbitrary non-Compose async work such as raw coroutines on production dispatchers.`,
  },
  {
    id: "a14q-testing-7",
    category: "test",
    categoryLabel: "Testing",
    question: `A test class has multiple \`@Test\` methods, each using \`Dispatchers.setMain(testDispatcher)\` in a \`@Before\` method, but no corresponding reset. Tests pass individually but fail intermittently when the full suite runs. What is the most likely root cause?`,
    options: [`\`Dispatchers.setMain\` is not thread-safe and must never be used in tests`, `JUnit randomizes test method order, which is unrelated to dispatcher state`, `Without \`Dispatchers.resetMain()\` in \`@After\` (or a TestWatcher's \`finished()\`), the test \`Main\` dispatcher can leak across test classes sharing a JVM fork, causing order-dependent state bleed`, `\`StandardTestDispatcher\` instances must be declared as companion-object singletons`],
    answer: 2,
    explanationHtml: `\`Dispatchers.setMain\` installs a global override for the \`Main\` dispatcher for the whole JVM process/fork. Without a guaranteed \`resetMain()\` (typically via \`@After\` or a \`TestWatcher.finished()\`), a leftover test dispatcher from one test class can still be active when another test class runs in the same fork, producing flaky, order-dependent failures — exactly the kind of bug a \`MainDispatcherRule\` is designed to prevent.`,
  },
];
