// Batch 20 — Scaling multi-module apps: dependency graphs, build performance from modularization, feature module communication (event bus vs shared ViewModel vs navigation results).
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED20_FLASHCARDS: Flashcard[] = [
  {
    id: "a20-modularization-1",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why does a naive setup where every feature module depends on every other feature module kill Gradle build parallelism, even on a multi-core CI machine?`,
    answerHtml: `<p>Gradle parallelizes <b>independent</b> module compile tasks across workers, but a module's task can only start once every module it depends on has finished. Direct feature-to-feature dependencies collapse the graph toward a long chain instead of a wide tree, so most modules wait in series instead of compiling concurrently. The fix is routing cross-feature dependencies through thin <code>:core</code>/<code>:api</code> modules so feature modules sit at the same depth with no edges between each other, maximizing the width Gradle can schedule in parallel.</p>`,
  },
  {
    id: "a20-modularization-2",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What's the practical difference between Gradle's configuration phase and execution phase, and why does module count hurt the configuration phase specifically?`,
    answerHtml: `<p>The <b>configuration phase</b> evaluates every <code>build.gradle.kts</code>, resolves the project graph, and creates task objects — it runs on every invocation regardless of caching. The <b>execution phase</b> runs the tasks and is what build caching speeds up. As module count grows, configuration time grows roughly linearly (worse with heavy convention-plugin logic) because every module's script re-evaluates even for a one-line change. This is why <b>configuration cache</b> (serializing the task graph itself, skipping re-evaluation entirely on a cache hit) gives a bigger win than execution caching alone on large graphs.</p>`,
  },
  {
    id: "a20-modularization-3",
    category: "arch",
    categoryLabel: "Architecture",
    question: `A shared :core:ui module that every feature depends on keeps growing, and clean-build times creep up even though feature count stayed flat. What's the likely culprit and the fix?`,
    answerHtml: `<p><code>:core:ui</code> has become a <b>god module</b>: unrelated utilities (networking helpers, analytics, design-system components, formatting) keep landing in one place because it's the path of least resistance. Since every feature depends on it, any change anywhere inside it invalidates the build/compile cache for the entire app downstream. The fix is splitting it along change-frequency and concern boundaries (e.g. <code>:core:design-system</code>, <code>:core:network</code>, <code>:core:analytics</code>) so a change to one doesn't poison the ABI of unrelated consumers.</p>`,
  },
  {
    id: "a20-modularization-4",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What does Gradle's \`api\` vs \`implementation\` configuration control, and what's the build-performance consequence of overusing \`api\`?`,
    answerHtml: `<p><code>implementation</code> dependencies are hidden from a module's consumers at compile time (they're only on the runtime classpath transitively); <code>api</code> dependencies leak onto consumers' compile classpaths. Overusing <code>api</code> widens the effective dependency graph — a change to a leaf module's public API forces recompilation of every module that transitively sees it via <code>api</code> edges, not just direct dependents. Defaulting to <code>implementation</code> and only promoting to <code>api</code> when a type genuinely appears in a module's own public signatures keeps the graph's recompilation blast radius small.</p>`,
  },
  {
    id: "a20-modularization-5",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why does converting an app from monolithic to multi-module sometimes make incremental builds *slower* for a single-file change in a deeply nested feature module?`,
    answerHtml: `<p>Each module boundary adds fixed overhead: Gradle still has to configure every project, check up-to-date status for each task, and (without configuration cache) re-resolve dependency graphs. If a feature module sits deep in the graph with many upstream dependencies, Gradle must validate the whole chain of inputs even though only one file changed. Modularization wins for <i>parallelism</i> and <i>cache hit rate across a team/CI</i>, not necessarily for a single dev's single-file edit — that's why pairing it with configuration cache, the build cache, and a flat-as-possible graph matters more than module count alone.</p>`,
  },
  {
    id: "a20-modularization-6",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What is dependency inversion's role in breaking a cycle between :feature:checkout and :feature:cart that both need to navigate to each other?`,
    answerHtml: `<p>Neither feature module should depend on the other directly (that's a cycle Gradle will refuse to build). Instead, define a navigation contract — an interface or a route key — in a low-level module (e.g. <code>:core:navigation</code>) that both feature modules depend on. <code>:feature:cart</code> declares "I need something that can navigate to checkout" via the abstraction; the app module (or a DI graph) wires the concrete implementation at runtime. This inverts the dependency so both features only point downward to a shared core, never sideways at each other.</p>`,
  },
  {
    id: "a20-modularization-7",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Compare an event bus, a shared ViewModel, and navigation results as mechanisms for cross-feature-module communication. What's the key structural tradeoff?`,
    answerHtml: `<p>An <b>event bus</b> (e.g. a shared <code>SharedFlow</code> in a core module) decouples sender and receiver completely but makes data flow implicit and hard to trace/test — anyone can emit, anyone can listen, and lifecycle/replay semantics need careful handling. A <b>shared ViewModel</b> scoped to a navigation graph or Activity gives strongly typed, lifecycle-aware state sharing, but it requires both features to agree on that scope and couples them to a shared parent, which can reintroduce a build dependency. <b>Navigation result APIs</b> (passing a typed result back through the back stack, e.g. Navigation Compose's saved-state handle results) keep communication request/response-shaped and scoped to one interaction, with no lingering shared state — but they don't fit continuous/streaming communication.</p>`,
  },
  {
    id: "a20-modularization-8",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why is a global event bus considered an anti-pattern for feature-to-feature communication in a large modular app, even though it solves the immediate coupling problem?`,
    answerHtml: `<p>It trades compile-time coupling for runtime coupling that's invisible to the type system and to build tooling. Nothing in the dependency graph shows that <code>:feature:profile</code> reacts to an event published by <code>:feature:settings</code> — you can delete or rename the event's producer and the consumer silently stops working with no compile error. It also tends to accumulate a kitchen-sink event type over time, and testing requires standing up the whole bus rather than just injecting a fake collaborator. It's most defensible for genuinely cross-cutting, fire-and-forget signals (e.g. "user logged out") where many unrelated modules need to react without knowing about each other.</p>`,
  },
  {
    id: "a20-modularization-9",
    category: "arch",
    categoryLabel: "Architecture",
    question: `When using a shared ViewModel for feature-to-feature communication, what scoping decision determines whether it actually decouples build dependencies, and what's the common mistake?`,
    answerHtml: `<p>The shared ViewModel itself must live in a module both features can depend on without creating a cycle (typically a navigation-graph-scoped module or a thin shared <code>:core</code> module), and it should expose state/events via an interface or sealed type rather than a concrete class with feature-specific dependencies baked in. The common mistake is putting the shared ViewModel inside one of the feature modules "because that's where it's used most" — that forces the other feature to depend on the first feature's module, recreating the coupling modularization was meant to remove.</p>`,
  },
  {
    id: "a20-modularization-10",
    category: "arch",
    categoryLabel: "Architecture",
    question: `How do navigation result APIs (e.g. Navigation Compose's \`savedStateHandle\` result pattern) avoid requiring :feature:a and :feature:b to know about each other's types?`,
    answerHtml: `<p>The result is passed as a primitive or <code>Parcelable</code>/serializable value keyed by a string route/key, set on the back stack entry's saved-state handle by the destination popping back, and read by the previous destination's saved-state handle observer. Neither module needs a compile-time reference to the other's classes — the contract is the key name and the serialized shape, agreed by convention (often centralized as constants in a shared navigation module). This keeps the dependency graph clean at the cost of losing compile-time type safety on the payload unless that contract is itself versioned in a shared module.</p>`,
  },
  {
    id: "a20-modularization-11",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What is an \`:api\` (or \`:public\`) vs \`:impl\` module split, and what build-performance problem does it specifically target?`,
    answerHtml: `<p>A feature is split into a tiny <code>:feature:x:api</code> module containing only interfaces, navigation contracts, and DTOs, and a <code>:feature:x:impl</code> module with the actual UI/ViewModel/business logic. Other features depend only on <code>:api</code> (never <code>:impl</code>), and only the app module depends on <code>:impl</code> to wire it up via DI. This means changing internal implementation details of a feature never triggers recompilation of any other feature module — only changes to the small, stable <code>:api</code> surface do, which dramatically shrinks the typical incremental-build blast radius.</p>`,
  },
  {
    id: "a20-modularization-12",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why can Gradle's build cache (local or remote) fail to produce a cache hit for a module even when its source files haven't changed?`,
    answerHtml: `<p>Task output caching keys on a hash of all declared task <i>inputs</i> — source files, yes, but also classpath contents (including transitive dependency versions), compiler flags, annotation processor arguments, and in some setups absolute file paths or environment-specific values. If an upstream module's version bumps, or two CI machines have different JDK paths leaking into inputs, the cache key changes even though the actual source is identical. Getting consistent cache hits across a team requires relative/normalized paths, pinned toolchains, and ensuring annotation processors (Hilt, Room, etc.) don't embed non-deterministic data like timestamps into generated code.</p>`,
  },
  {
    id: "a20-modularization-13",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What does \`--scan\` / the Gradle Build Scan reveal that helps diagnose a multi-module build performance regression, beyond just total build time?`,
    answerHtml: `<p>A build scan breaks down time per task with a timeline view showing parallel worker utilization, flags tasks that were <code>UP-TO-DATE</code> vs cache-hit vs executed-from-scratch, surfaces configuration-phase time per project, and shows the dependency resolution time including any network calls to a remote cache or repository. It's how you distinguish "this module is slow because it actually has more code to compile" from "this module is slow because something is invalidating its cache key every build" (e.g. a non-deterministic annotation processor or a version catalog entry that changes per-build).</p>`,
  },
  {
    id: "a20-modularization-14",
    category: "arch",
    categoryLabel: "Architecture",
    question: `How does a Gradle version catalog (\`libs.versions.toml\`) help large multi-module graphs beyond just deduplicating dependency strings?`,
    answerHtml: `<p>It centralizes version resolution so every module references the same generated type-safe accessor (<code>libs.androidx.compose.ui</code>) rather than a hardcoded version string, which prevents version drift between modules that would otherwise force Gradle to resolve multiple versions of the same library and pick a winner via conflict resolution — a common source of subtle ABI/runtime mismatches and wasted resolution time. It also makes a single-line version bump apply consistently across dozens of modules instead of requiring a search-and-replace.</p>`,
  },
  {
    id: "a20-modularization-15",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What's the risk of a convention plugin (a \`build-logic\` included build applying shared Gradle config to every module) becoming a bottleneck itself?`,
    answerHtml: `<p>Convention plugins run during the <b>configuration phase</b> of every module that applies them, so any slow logic inside one (file-system scans, eager property evaluation, network calls, or eagerly resolving a configuration to inspect it) gets paid N times, once per module, on every single build invocation — even an up-to-date one. The fix is using the Provider API for lazy configuration (avoiding <code>.get()</code> until execution time), avoiding eager <code>afterEvaluate</code> chains across modules, and keeping convention-plugin logic to declarative <code>plugins{}</code>/<code>extension{}</code> blocks rather than imperative scanning.</p>`,
  },
  {
    id: "a20-modularization-16",
    category: "arch",
    categoryLabel: "Architecture",
    question: `How do Android Dynamic Feature Modules differ from regular Gradle feature modules in terms of the dependency graph, and what constraint does that put on inter-feature communication?`,
    answerHtml: `<p>Regular feature modules are all compiled into one APK/AAB and linked at build time — the app module has a compile-time dependency on all of them. Dynamic Feature Modules are separate artifacts downloadable on demand via Play Feature Delivery; the base app module does <i>not</i> have a compile-time dependency on them (it can't, since they may not be installed). This means a dynamic feature can depend on the base module, but the base (or sibling dynamic features) cannot directly reference a dynamic feature's classes — communication has to go through reflection, an installed-state check plus a runtime-resolved interface, or \`SplitInstallManager\` callbacks, not normal constructor injection.</p>`,
  },
  {
    id: "a20-modularization-17",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What's the build-performance argument for keeping :app as a thin "assembly" module rather than putting any real logic in it?`,
    answerHtml: `<p><code>:app</code> sits at the root of the dependency graph — nothing depends on it, so it's the one module whose own internal changes never trigger recompilation elsewhere, but it also means anything that *should* be reusable but lives in <code>:app</code> can't be depended on by anything else without an inversion. Practically, keeping <code>:app</code> to just DI wiring, navigation graph assembly, and the manifest means most day-to-day changes happen in leaf/feature modules where Gradle's incremental and cache-hit behavior is most effective, and <code>:app</code>'s own (often slow, full-graph-touching) configuration and dexing work is only re-paid when assembly-level things actually change.</p>`,
  },
  {
    id: "a20-modularization-18",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What does "build graph width vs depth" mean for multi-module Android apps, and why do staff-level reviews of a modularization plan focus on it?`,
    answerHtml: `<p><b>Depth</b> is the longest chain of dependency edges from a leaf module to <code>:app</code>; <b>width</b> is how many independent (non-dependent) modules exist at a similar depth. Build parallelism scales with width — Gradle can only run as many module-tasks concurrently as are simultaneously unblocked, which is bounded by width, not total module count. A plan that creates 80 modules but funnels them all through 3 sequential core layers has poor width and won't actually speed up CI; reviewers look at the graph shape, not the module count, when judging whether modularization will pay off.</p>`,
  },
  {
    id: "a20-modularization-19",
    category: "arch",
    categoryLabel: "Architecture",
    question: `How does Compose's strong-skipping mode / stability inference interact with multi-module boundaries, and why can a poorly modularized data class still hurt recomposition performance app-wide?`,
    answerHtml: `<p>The Compose compiler infers a type as <i>stable</i> only if it can see (at compile time, within the compilation unit/module) that all its properties are stable. With strong skipping mode (the default since Kotlin 2.0.20 / current Compose compiler), a composable with an unstable parameter doesn't lose skippability outright — it's still skippable, but the runtime compares that parameter by referential equality (<code>===</code>) instead of <code>.equals()</code>. The cost shows up when a shared model lives in a module missing stability annotations (<code>@Immutable</code>/<code>@Stable</code>) or holds a mutable collection: callers that rebuild a new instance each recomposition (e.g. a freshly mapped <code>List</code>) will never pass the same reference twice, so the composable recomposes every time regardless of strong skipping. This makes the <code>:core:model</code> module's type design (and whether call sites reuse instances) a multiplier on UI performance across the whole app, not just a data-layer concern.</p>`,
  },
  {
    id: "a20-modularization-20",
    category: "arch",
    categoryLabel: "Architecture",
    question: `When two feature modules need to share mutable, continuously-updated state (not just a one-shot navigation result), why is a shared ViewModel usually preferred over a repository-level shared Flow accessed independently by each feature's own ViewModel?`,
    answerHtml: `<p>Both approaches can technically share data, but a shared ViewModel scoped to a common navigation graph gives a single source of truth for UI-layer state (selection, in-progress form data, scroll position) with one lifecycle to reason about, whereas two independent ViewModels each collecting the same repository Flow can drift if either applies its own local transformation, debounce, or caching — leading to inconsistent UI state between features even though the underlying data source is shared. The repository-Flow approach is the better fit for actual persisted/domain data; the shared ViewModel is better for transient, screen-flow-scoped UI state.</p>`,
  },
  {
    id: "a20-modularization-21",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What's the difference between a "feature module" and a "library module" in a typical scaled Android dependency graph, and why does conflating them cause graph problems?`,
    answerHtml: `<p>A <b>feature module</b> is vertical — it owns a user-facing flow (UI, ViewModel, feature-specific use cases) and is meant to depend on lower layers, never be depended on by siblings. A <b>library module</b> is horizontal — it provides reusable capability (networking, design system, persistence) and is meant to be depended on by many features with no dependency of its own going back up. Conflating them — e.g. one feature reaching into another feature's internals because "it's basically a library" — is exactly what reintroduces feature-to-feature edges and turns a layered graph back into a tangled one.</p>`,
  },
  {
    id: "a20-modularization-22",
    category: "arch",
    categoryLabel: "Architecture",
    question: `How does a remote build cache (e.g. a Develocity/Gradle Enterprise build cache node) change the calculus for module granularity compared to relying only on the local cache?`,
    answerHtml: `<p>With only a local cache, a developer only benefits from incremental builds on their own machine across their own previous builds — a fresh checkout or a teammate's change still rebuilds everything untouched locally. A remote cache means any developer or CI agent can reuse task outputs produced by *anyone* on the team for identical inputs, so finer-grained modules pay off much more: a module nobody on your team changed today can come back as a cache hit from CI's last run instead of a local rebuild. This is why teams investing in many small modules almost always pair it with a remote build cache — without one, the configuration overhead of many modules can outweigh the incremental-build win.</p>`,
  },
];

export const ADVANCED20_QUIZ: QuizQuestion[] = [
  {
    id: "a20q-modularization-1",
    category: "arch",
    categoryLabel: "Architecture",
    question: `A modularized app has :feature:a and :feature:b both depending only on :core:network and :core:ui, with no edges between them. :app depends on both features. What does this graph shape primarily enable?`,
    options: [`Gradle can compile :feature:a and :feature:b in parallel since neither blocks the other`, `It eliminates the need for a remote build cache`, `It guarantees R8 will produce a smaller APK`, `It removes the need for a DI framework`],
    answer: 0,
    explanationHtml: `<p>Since neither feature module depends on the other, once <code>:core:network</code> and <code>:core:ui</code> finish, Gradle's scheduler can run both feature modules' tasks concurrently on separate workers. This width-over-depth shape is exactly what makes modularization pay off in build parallelism; it has no direct bearing on APK size, build caching infrastructure, or DI choice.</p>`,
  },
  {
    id: "a20q-modularization-2",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why is overusing Gradle's \`api\` configuration (instead of \`implementation\`) for inter-module dependencies considered harmful to incremental build performance at scale?`,
    options: [`api dependencies are always resolved from a remote repository, implementation never is`, `api leaks a module's dependencies onto its consumers' compile classpath, widening the recompilation blast radius when that dependency's ABI changes`, `api dependencies cannot be cached by the Gradle build cache`, `api forces every module to use the same Kotlin compiler version`],
    answer: 1,
    explanationHtml: `<p><code>api</code> exposes a dependency transitively to anything that depends on the module, so a binary-interface change in that dependency forces recompilation across every transitive consumer, not just direct ones. <code>implementation</code> keeps that dependency off consumers' compile classpaths, limiting recompilation to the module itself. The other options are not accurate distinctions between the two configurations.</p>`,
  },
  {
    id: "a20q-modularization-3",
    category: "arch",
    categoryLabel: "Architecture",
    question: `:feature:cart needs to navigate to :feature:checkout, and :feature:checkout occasionally needs to navigate back to :feature:cart. Both are regular (non-dynamic) Gradle feature modules. What's the standard way to avoid a circular module dependency here?`,
    options: [`Merge both features into a single module`, `Have :feature:checkout depend on :feature:cart only, and use reflection for the reverse call`, `Define a shared navigation contract (route keys/interfaces) in a lower-level module that both feature modules depend on, and resolve the actual navigation at the app/navigation-graph level`, `Use an event bus exclusively and remove all module dependencies between core and feature modules`],
    answer: 2,
    explanationHtml: `<p>Extracting the shared contract into a module both features can depend on (without depending on each other) is the standard dependency-inversion fix for this exact pattern — it keeps the graph acyclic while letting both features navigate to each other via an abstraction. Merging modules defeats the purpose of modularization; reflection-based one-way coupling is fragile and still asymmetric; an event bus is one possible transport but isn't required or sufficient on its own to fix the dependency graph problem.</p>`,
  },
  {
    id: "a20q-modularization-4",
    category: "arch",
    categoryLabel: "Architecture",
    question: `A shared ViewModel is used to pass in-progress checkout state between :feature:cart and :feature:checkout. Where should that ViewModel's module live to actually preserve the modularization benefit?`,
    options: [`Inside :feature:cart, since cart is upstream of checkout in the user flow`, `Inside :feature:checkout, since it owns the final state`, `In a module both features can depend on without creating a cycle (e.g. a nav-graph-scoped shared module), exposing state via an interface or sealed type`, `In :app, instantiated directly inside the Activity`],
    answer: 2,
    explanationHtml: `<p>Putting the shared ViewModel inside either feature module forces the other feature to depend on that module, recreating feature-to-feature coupling. Placing it in app-level code works but couples it to a single Activity/host and loses reusability across navigation hosts. A shared module both features depend on (with the ViewModel's surface exposed abstractly) keeps the dependency graph acyclic and the contract testable in isolation.</p>`,
  },
  {
    id: "a20q-modularization-5",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Compose's strong-skipping mode (the default since Kotlin 2.0.20) lets composables with unstable parameters remain skippable by comparing them with referential equality (===) instead of .equals(). Given that, why does a shared model type defined without stability annotations or with a mutable collection field still hurt recomposition performance app-wide?`,
    options: [`Strong skipping is disabled automatically whenever a module-boundary type is involved, so skipping never happens for it`, `Stability inference only applies to ViewModels, never to types passed into Composables`, `Call sites that rebuild a new instance of that unstable type on every recomposition (e.g. a freshly mapped List) never pass the same reference twice, so referential equality always fails and the composable recomposes every time, across every consuming module`, `Modularization disables strong-skipping mode entirely for the whole app`],
    answer: 2,
    explanationHtml: `<p>Strong skipping mode means an unstable parameter doesn't make a composable unskippable outright — it's compared by reference instead. But if the value passed in is a new object each time (common for unstable types lacking immutability guarantees, like a mutable List rebuilt on each call), referential equality always fails, so the composable still recomposes every time it's called from any consuming module. The other options misstate how stability inference and strong-skipping actually work.</p>`,
  },
  {
    id: "a20q-modularization-6",
    category: "arch",
    categoryLabel: "Architecture",
    question: `A team splits :feature:profile into :feature:profile:api (interfaces, navigation contracts) and :feature:profile:impl (UI, ViewModel, business logic), with other features depending only on :api. What's the main incremental-build benefit?`,
    options: [`It removes the need to ever rebuild :feature:profile:impl`, `Changes to :feature:profile:impl's internals no longer trigger recompilation of other feature modules, since they only see the stable :api surface`, `It allows :feature:profile to skip the configuration phase entirely`, `It automatically parallelizes :impl's own internal compilation across more workers`],
    answer: 1,
    explanationHtml: `<p>Since other modules depend only on the small, rarely-changing <code>:api</code> module, changes confined to <code>:impl</code> (the vast majority of day-to-day work: UI tweaks, ViewModel logic, business rules) don't invalidate the compile/cache state of any other feature module. <code>:impl</code> itself still rebuilds when its own code changes; the win is in shrinking the blast radius for everyone else, not eliminating its own rebuild or affecting configuration-phase mechanics directly.</p>`,
  },
];
