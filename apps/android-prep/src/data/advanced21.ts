// Batch 21 — Accessibility in Compose (semantics tree, contentDescription, mergeDescendants, TalkBack testing), and adaptive layouts for large screens/foldables (WindowSizeClass).
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED21_FLASHCARDS: Flashcard[] = [
  {
    id: "a21-accessibility-1",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why can two adjacent Text composables inside a clickable Row sometimes be read by TalkBack as one announcement instead of two separate focus stops?`,
    answerHtml: `<p>Compose builds a <b>semantics tree</b> alongside the UI tree. A clickable container (or any node with <code>Modifier.semantics(mergeDescendants = true)</code>) folds all of its descendants' semantics into a single merged node. Accessibility services like TalkBack then treat that merged node as one focusable unit and read its combined text in one announcement, rather than stopping separately at each child Text.</p>`,
  },
  {
    id: "a21-accessibility-2",
    category: "compose",
    categoryLabel: "Compose",
    question: `What is the practical difference between Modifier.semantics { } and Modifier.clearAndSetSemantics { }?`,
    answerHtml: `<p><code>Modifier.semantics { }</code> adds or overrides specific properties on a node while still merging in the semantics of its children. <code>Modifier.clearAndSetSemantics { }</code> first wipes the entire subtree's semantics, then applies only what you set in the block. Use <code>clearAndSetSemantics</code> on a composite custom widget (e.g. five star icons forming a rating control) so TalkBack hears one synthetic description like "4 out of 5 stars" instead of every child icon's own semantics leaking through.</p>`,
  },
  {
    id: "a21-accessibility-3",
    category: "compose",
    categoryLabel: "Compose",
    question: `A decorative Icon next to a labeled Text is announced redundantly by TalkBack. Setting contentDescription = null on the Icon alone doesn't always silence it — why, and what's the fix?`,
    answerHtml: `<p><code>contentDescription = null</code> excludes that node from the semantics tree, but if a parent container already merges descendants (e.g. it's clickable or has its own <code>mergeDescendants = true</code>), the icon's node can still factor into focus traversal order even with no text. The reliable fix is to confirm the icon is a pure child of an already-merged container so it never becomes an independent focus stop — the bug usually traces to an extra mergeDescendants boundary you didn't intend between the icon and its label.</p>`,
  },
  {
    id: "a21-accessibility-4",
    category: "compose",
    categoryLabel: "Compose",
    question: `When should you use Modifier.semantics(mergeDescendants = true) directly instead of relying on a clickable modifier's default merging behavior?`,
    answerHtml: `<p>Apply it explicitly when you build a custom composite component from several non-interactive parts (icon + title + subtitle in a Card, for instance) and want TalkBack to treat them as one node <i>without</i> the component itself being clickable. The clickable modifier merges descendants as a side effect of making the node actionable; <code>mergeDescendants = true</code> lets you opt into the same grouping behavior for a purely informational composite, independent of interactivity.</p>`,
  },
  {
    id: "a21-accessibility-5",
    category: "compose",
    categoryLabel: "Compose",
    question: `How does the testTag modifier interact with the accessibility semantics tree, and why shouldn't you use it as a substitute for contentDescription?`,
    answerHtml: `<p><code>Modifier.testTag("...")</code> attaches a tag used by Compose UI testing (<code>onNodeWithTag</code>) and is exposed in the semantics tree as a <code>TestTag</code> property, but it is <b>not surfaced to TalkBack or any accessibility service</b> — it exists purely for automated test lookup. Using it in place of <code>contentDescription</code> leaves the node mute to real assistive technology even though your Compose UI tests would still pass, which is a common false-confidence trap: green tests, broken accessibility.</p>`,
  },
  {
    id: "a21-accessibility-6",
    category: "compose",
    categoryLabel: "Compose",
    question: `What does the Role semantics property do, and what's a concrete bug that happens when you build a custom toggle without setting it?`,
    answerHtml: `<p><code>Role</code> (set via <code>Modifier.semantics { role = Role.Switch }</code> etc.) tells TalkBack what kind of control a node is, which changes both the spoken description ("switch", "button", "checkbox") and the expected interaction hints announced. If you build a custom toggle from a <code>Box</code> with a click listener and skip <code>Role</code>, TalkBack announces it as a generic clickable item with no "double tap to toggle" semantics and no on/off state framing, so blind users can't tell it's a binary switch versus a button that performs an action.</p>`,
  },
  {
    id: "a21-accessibility-7",
    category: "compose",
    categoryLabel: "Compose",
    question: `How do stateDescription and the toggleable modifier work together to announce a switch's on/off state to TalkBack?`,
    answerHtml: `<p><code>Modifier.toggleable(value = checked, role = Role.Switch) { ... }</code> automatically sets the <code>ToggleableState</code> semantics property, which TalkBack maps to "on"/"off" in its announcement. If you need custom wording (e.g. "enabled"/"disabled" or a localized phrase), you override it with <code>Modifier.semantics { stateDescription = if (checked) "Enabled" else "Disabled" }</code> layered on top, since <code>stateDescription</code> takes precedence over the default boolean-to-on/off mapping.</p>`,
  },
  {
    id: "a21-accessibility-8",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why does liveRegion matter for a composable that shows asynchronous status text (e.g. "Saving..." → "Saved"), and what are the two modes?`,
    answerHtml: `<p>Without a live region, TalkBack only announces a node's content when it gains focus — text that updates silently in the background (like a save-status label) won't be re-announced. <code>Modifier.semantics { liveRegion = LiveRegionMode.Polite }</code> tells the accessibility service to announce changes opportunistically, waiting for the current speech to finish. <code>LiveRegionMode.Assertive</code> interrupts ongoing speech immediately for urgent updates (e.g. an error). Polite is correct for most status text; Assertive is reserved for critical, time-sensitive changes since it's jarring.</p>`,
  },
  {
    id: "a21-accessibility-9",
    category: "compose",
    categoryLabel: "Compose",
    question: `What's the difference between traversalIndex and the natural composition order for determining TalkBack's focus order, and when do you need it?`,
    answerHtml: `<p>By default, TalkBack traverses nodes in the order they appear in the semantics tree, which generally follows composition/layout order. <code>Modifier.semantics { traversalIndex = -1f }</code> (or any float) lets you override that order without restructuring your composable hierarchy — lower values are visited earlier. This is needed when a visually-later element (e.g. a floating "back" button drawn last via a Box overlay) should logically be read first, since visual z-order and composition order don't always match desired reading order.</p>`,
  },
  {
    id: "a21-accessibility-10",
    category: "compose",
    categoryLabel: "Compose",
    question: `How do you make a custom-drawn composable (e.g. a Canvas-based chart) accessible, given it has no semantic content by default?`,
    answerHtml: `<p>A <code>Canvas</code> draws pixels with zero semantic nodes, so TalkBack skips it entirely. You attach <code>Modifier.semantics { contentDescription = "..." }</code> directly on the Canvas's modifier chain. For genuinely interactive custom-drawn UI — like a draggable slider drawn by hand — you also need <code>Modifier.semantics { progressBarRangeInfo = ProgressBarRangeInfo(...) }</code> or similar typed semantics so TalkBack announces it with the correct control semantics, not just a static label.</p>`,
  },
  {
    id: "a21-accessibility-11",
    category: "compose",
    categoryLabel: "Compose",
    question: `What does Modifier.semantics { customActions = listOf(...) } solve, and give an example where it's the right tool over a second visible button.`,
    answerHtml: `<p><code>customActions</code> exposes additional actions on a node that TalkBack surfaces via its local context menu rather than requiring a second visible, focusable control. A classic case: a list item (e.g. an email row) that supports "Archive" and "Delete" via swipe gestures for sighted/touch users — instead of cluttering the row with two extra always-visible icon buttons just for accessibility parity, you expose the same actions as <code>CustomAccessibilityAction</code>s so TalkBack users reach them through the actions menu.</p>`,
  },
  {
    id: "a21-accessibility-12",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why is minimum touch target size (48dp) treated as an accessibility concern, and how do you audit it in Compose?`,
    answerHtml: `<p>WCAG and Android's accessibility guidelines require a minimum 48x48dp touch target so users with motor impairments or imprecise input (including TalkBack's explore-by-touch) can reliably activate controls; small icon buttons (e.g. a 24dp icon with no padding) fail this even if visually fine. Compose Material3 components (Checkbox, RadioButton, Switch, IconButton, etc.) set this minimum internally — but only while the component is interactive (non-null callback) — by expanding the invisible touch target beyond the visible bounds; custom clickable composables need it explicitly via <code>Modifier.sizeIn(minWidth = 48.dp, minHeight = 48.dp)</code> or <code>Modifier.minimumInteractiveComponentSize()</code>. Audit it with <code>onNodeWithTag(...).assertWidthIsAtLeast(48.dp)</code> / <code>assertHeightIsAtLeast(48.dp)</code> in Compose UI tests, or the built-in Accessibility Test Framework integration (<code>composeTestRule.enableAccessibilityChecks(...)</code> + <code>onRoot().performAccessibilityChecks()</code>, available since Compose 1.8.0) for the same checks the standalone Accessibility Scanner app performs.</p>`,
  },
  {
    id: "a21-accessibility-13",
    category: "compose",
    categoryLabel: "Compose",
    question: `How does Compose's semantics-based testing (e.g. assertIsDisplayed, onNodeWithContentDescription) relate to actually testing TalkBack behavior — and what gap remains?`,
    answerHtml: `<p>Compose UI tests query the same semantics tree TalkBack reads from, so <code>onNodeWithContentDescription("Submit")</code> verifies the node exists with the right description — a good proxy for "TalkBack could find this." But these tests don't exercise TalkBack itself: focus order quirks, actual spoken phrasing (locale, pluralization), gesture navigation, and merged-node announcement text are only verified by running the real TalkBack service (manually or via a tool like Espresso's AccessibilityChecks) on a device or emulator with TalkBack enabled.</p>`,
  },
  {
    id: "a21-accessibility-14",
    category: "compose",
    categoryLabel: "Compose",
    question: `What's the accessibility risk of using a Modifier.clickable on a plain Box/Row without specifying onClickLabel, and how do you fix it?`,
    answerHtml: `<p>A bare <code>Modifier.clickable { }</code> exposes the node as actionable but with a generic "double tap to activate" hint and no description of <i>what</i> the action does beyond whatever contentDescription is present. Passing <code>Modifier.clickable(onClickLabel = "Delete item") { ... }</code> sets the action's label, which TalkBack incorporates into its hint ("double tap to delete item") instead of a vague generic phrase — meaningfully better for discoverability when the click target's visual icon alone doesn't convey the action.</p>`,
  },
  {
    id: "a21-accessibility-15",
    category: "compose",
    categoryLabel: "Compose",
    question: `Explain WindowSizeClass and why it uses three coarse buckets (Compact/Medium/Expanded) rather than raw pixel or dp breakpoints.`,
    answerHtml: `<p><code>WindowSizeClass</code> (from <code>androidx.compose.material3.adaptive</code> / <code>androidx.window</code>) buckets the available window width and height into <b>Compact, Medium, and Expanded</b> categories at fixed dp breakpoints (width: &lt;600dp Compact, 600–839dp Medium, ≥840dp Expanded; height: &lt;480dp Compact, 480–899dp Medium, ≥900dp Expanded). The coarse buckets exist because chasing exact device dimensions is a losing battle across phones, foldables, tablets, and desktop windows — instead you design layout decisions ("show nav rail instead of bottom bar") around a handful of meaningful breakpoints that map to real layout needs, not every possible width.</p>`,
  },
  {
    id: "a21-accessibility-16",
    category: "compose",
    categoryLabel: "Compose",
    question: `How do you observe WindowSizeClass changes live as a user resizes a freeform/desktop window or unfolds a foldable, rather than just reading it once at launch?`,
    answerHtml: `<p>You compute it reactively from <code>currentWindowAdaptiveInfo()</code> (or <code>calculateWindowSizeClass(activity)</code> in older APIs) inside composition, not in <code>onCreate</code> — Compose re-executes the composable when the underlying <code>WindowMetrics</code> changes (rotation, fold/unfold, freeform resize), so the size class recomposes automatically as long as you read it via the Compose-aware API rather than caching it in a one-shot variable.</p>`,
  },
  {
    id: "a21-accessibility-17",
    category: "compose",
    categoryLabel: "Compose",
    question: `What's the distinction between a foldable's hinge "posture" (book/tabletop) and its WindowSizeClass, and which API exposes posture?`,
    answerHtml: `<p><code>WindowSizeClass</code> only describes available width/height buckets — it says nothing about a physical hinge. Posture (flat, half-opened "book" or "tabletop", or fully folded) comes from <code>androidx.window.layout.FoldingFeature</code> via <code>WindowInfoTracker</code>, which reports the hinge's bounds, orientation (vertical/horizontal), and occlusion type. A real adaptive layout often needs both: WindowSizeClass to decide overall layout structure (single vs. multi-pane) and FoldingFeature to avoid placing critical content under the hinge or to split content naturally along it (e.g. list on top, detail on bottom in tabletop mode).</p>`,
  },
  {
    id: "a21-accessibility-18",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why is reading LocalConfiguration.current.screenWidthDp directly considered the wrong tool for adaptive layout decisions compared to WindowSizeClass?`,
    answerHtml: `<p><code>LocalConfiguration.screenWidthDp</code> reports the full device/display width, which is wrong in multi-window or split-screen scenarios — your app may only occupy a fraction of that width while the configuration value still reflects the whole screen. <code>WindowSizeClass</code> is computed from the actual <b>window bounds your activity occupies</b>, so it correctly reflects a narrow split-screen pane as Compact even on a large tablet. Using configuration width for layout breakpoints causes real bugs in split-screen and freeform multi-window where the app thinks it has more space than it does.</p>`,
  },
  {
    id: "a21-accessibility-19",
    category: "compose",
    categoryLabel: "Compose",
    question: `What is NavigationSuiteScaffold's actual default rule for choosing a navigation component, and what's a common misconception about it?`,
    answerHtml: `<p>The default logic (<code>NavigationSuiteScaffoldDefaults.calculateFromAdaptiveInfo</code>) only switches between two components: a <b>NavigationBar</b> if the device is in tabletop posture or the window <i>height</i> size class is Compact, and a <b>NavigationRail</b> for everything else — including Medium <i>and</i> Expanded width. A common misconception is that it automatically upgrades to a permanent <b>NavigationDrawer</b> at Expanded width; it does not — Material's design guidance recommends a drawer for very wide/expanded layouts, but you have to opt into it yourself by overriding the <code>layoutType</code> parameter (e.g. with your own WindowSizeClass check), since the scaffold's built-in default never selects a drawer.</p>`,
  },
  {
    id: "a21-accessibility-20",
    category: "compose",
    categoryLabel: "Compose",
    question: `What problem does ListDetailPaneScaffold (Material 3 adaptive) solve, and what happens to the detail pane on a Compact-width device?`,
    answerHtml: `<p><code>ListDetailPaneScaffold</code> implements the canonical list-detail adaptive pattern: on Expanded/Medium width it shows the list and detail panes side by side; on Compact width (a phone) it collapses to single-pane navigation, pushing the detail pane on top of the list as a full-screen destination when an item is selected, and popping back on back-press. It's built on <code>ThreePaneScaffold</code> with built-in state (<code>rememberListDetailPaneScaffoldNavigator</code>) so back-stack handling between the collapsed/expanded representations is managed for you instead of you wiring two separate navigation graphs.</p>`,
  },
  {
    id: "a21-accessibility-21",
    category: "compose",
    categoryLabel: "Compose",
    question: `When migrating a phone-only screen to support Expanded width, what's a common mistake with state hoisting that breaks the multi-pane version?`,
    answerHtml: `<p>Phone-only screens often hoist "selected item" state no higher than the single screen that both shows the list and navigates to a detail screen via the nav-controller back stack. In a multi-pane layout both list and detail render <i>simultaneously</i> as siblings, so that selection state must be hoisted to a shared parent (or a <code>ThreePaneScaffoldNavigator</code>) visible to both panes — otherwise the detail pane has no way to know what was tapped in the list pane, since they're no longer sequential screens but concurrent composables.</p>`,
  },
  {
    id: "a21-accessibility-22",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why does Compose Multiplatform / Material 3 adaptive recommend designing against WindowSizeClass rather than against "is this a tablet" device checks?`,
    answerHtml: `<p>"Is this a tablet" conflates form factor with available space, which breaks for split-screen multitasking (a tablet app pinned to one-third width is effectively Compact), foldables (a folded device is a phone, unfolded is tablet-sized, same hardware), and desktop/ChromeOS freeform windows (any width is possible, resizable live). WindowSizeClass decouples the layout decision from the device identity entirely — the same composable logic produces correct behavior whether the Compact width comes from a phone, a split-screen pane, or a resized desktop window.</p>`,
  },
  {
    id: "a21-accessibility-23",
    category: "compose",
    categoryLabel: "Compose",
    question: `What accessibility regression commonly appears when a Compact single-pane screen is adapted into a multi-pane Expanded layout, specific to TalkBack focus order?`,
    answerHtml: `<p>When list and detail panes render side by side, the semantics tree now contains both subtrees simultaneously, and TalkBack's linear traversal will read through the entire list pane before ever reaching the detail pane (or vice versa, depending on composition order) — even though visually a user's eye jumps straight to the detail pane after selection. Without explicit <code>traversalIndex</code> tuning or moving focus programmatically (<code>FocusRequester</code> + <code>requestFocus()</code>) to the detail pane on selection, TalkBack users get a confusing, much longer linear path through content sighted users perceive as two independent regions.</p>`,
  },
];

export const ADVANCED21_QUIZ: QuizQuestion[] = [
  {
    id: "a21q-accessibility-1",
    category: "compose",
    categoryLabel: "Compose",
    question: `A Card composable wraps an Icon, a title Text, and a subtitle Text, with Modifier.clickable applied to the Card itself. What does TalkBack announce when the user swipes to focus it?`,
    options: [`Three separate announcements: icon description, title, then subtitle`, `A single merged announcement combining the icon's contentDescription (if any) plus the title and subtitle text, with a "double tap to activate" hint`, `Only the title text, ignoring the icon and subtitle`, `Nothing, because Card does not expose semantics by default`],
    answer: 1,
    explanationHtml: `<p>Adding <code>Modifier.clickable</code> makes the Card's semantics node actionable and sets <code>mergeDescendants = true</code> by default, folding all descendant semantics (icon, title, subtitle) into one merged node. TalkBack reads the combined content as a single announcement plus the click action hint, rather than stopping at each child individually.</p>`,
  },
  {
    id: "a21q-accessibility-2",
    category: "compose",
    categoryLabel: "Compose",
    question: `Which API correctly observes live changes to the available window size as a user resizes a freeform desktop window or unfolds a foldable device, inside a composable?`,
    options: [`LocalConfiguration.current.screenWidthDp read once in onCreate`, `currentWindowAdaptiveInfo() (or calculateWindowSizeClass) read reactively during composition`, `Build.MODEL string-matched against a list of known foldable device names`, `Resources.getSystem().displayMetrics cached in a companion object`],
    answer: 1,
    explanationHtml: `<p><code>currentWindowAdaptiveInfo()</code> / <code>calculateWindowSizeClass()</code> are Compose-aware and recompose automatically as the activity's window bounds change (resize, fold/unfold, rotation). <code>LocalConfiguration</code> reflects the whole display, not just the app's window in multi-window/split-screen, and device-model matching is brittle and doesn't react to live resizing at all.</p>`,
  },
  {
    id: "a21q-accessibility-3",
    category: "compose",
    categoryLabel: "Compose",
    question: `What does Modifier.clearAndSetSemantics { } do that plain Modifier.semantics { } does not?`,
    options: [`It disables the composable entirely for accessibility services`, `It merges child semantics with extra logging for debugging`, `It strips all existing semantics from the subtree (including descendants) before applying only what's specified in the block`, `It is purely a performance optimization with no effect on TalkBack output`],
    answer: 2,
    explanationHtml: `<p><code>clearAndSetSemantics</code> wipes the entire subtree's semantics first, then sets exactly what you specify — useful for composite custom widgets that should expose one synthetic description instead of leaking every child's semantics. Plain <code>semantics { }</code> adds/overrides properties while still merging children's semantics in.</p>`,
  },
  {
    id: "a21q-accessibility-4",
    category: "compose",
    categoryLabel: "Compose",
    question: `A team builds a custom toggle from a Box with Modifier.clickable and a manually drawn on/off indicator, but skips setting a Role. What does TalkBack announce, and why is that a problem?`,
    options: [`It announces "switch, on" or "switch, off" automatically regardless of Role`, `It throws a runtime exception because Role is mandatory for clickable nodes`, `It announces the node as a generic clickable item without switch-specific state framing or interaction hints, so users can't tell it toggles a binary state`, `It is automatically excluded from the semantics tree until a Role is set`],
    answer: 2,
    explanationHtml: `<p>Without <code>role = Role.Switch</code> (typically via <code>Modifier.toggleable</code>), TalkBack has no way to know the control represents a binary on/off state — it just announces a generic actionable item, omitting the on/off state framing and switch-specific interaction hint that sighted users infer visually from the indicator.</p>`,
  },
  {
    id: "a21q-accessibility-5",
    category: "compose",
    categoryLabel: "Compose",
    question: `In a ListDetailPaneScaffold-based screen, what's the correct way to handle 'selected item' state so both the list and detail panes work correctly at every WindowSizeClass?`,
    options: [`Keep selection state local to the list pane's composable only, since the detail pane is a separate navigation destination`, `Hoist selection state to a parent shared by both panes (or use the pane scaffold's navigator), since both panes may render concurrently in multi-pane layouts`, `Store selection in a static/global singleton to avoid recomposition`, `Selection state is unnecessary — ThreePaneScaffold infers selection from scroll position`],
    answer: 1,
    explanationHtml: `<p>In multi-pane (Medium/Expanded) layouts, list and detail panes are siblings rendered simultaneously, not sequential screens — so selection state needs to live above both, typically managed via <code>rememberListDetailPaneScaffoldNavigator</code>, so the detail pane can react to what's selected in the list pane regardless of single- or multi-pane mode.</p>`,
  },
  {
    id: "a21q-accessibility-6",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why is WindowSizeClass generally preferred over checking the device's physical screen size or model to decide adaptive layout behavior?`,
    options: [`WindowSizeClass is faster to compute than reading screen dimensions`, `Physical screen size doesn't account for split-screen, freeform multi-window, or foldable fold/unfold state, all of which change the actual space available to the app independent of the device`, `Device model checks are deprecated and will be removed in a future Android version`, `WindowSizeClass is the only API that works on Wear OS`],
    answer: 1,
    explanationHtml: `<p>A tablet in split-screen view, a foldable that's folded vs. unfolded, or a resizable desktop window all decouple "device hardware" from "space actually available to this app's window." WindowSizeClass is computed from the app's actual window bounds, so the same logic correctly adapts regardless of why the available space is small or large.</p>`,
  },
  {
    id: "a21q-accessibility-7",
    category: "compose",
    categoryLabel: "Compose",
    question: `A status Text composable updates from "Saving..." to "Saved" without regaining focus. What semantics property ensures TalkBack announces the change automatically?`,
    options: [`Modifier.semantics { contentDescription = "Saved" } alone`, `Modifier.semantics { liveRegion = LiveRegionMode.Polite }`, `Modifier.semantics { heading() }`, `Modifier.testTag("status")`],
    answer: 1,
    explanationHtml: `<p>TalkBack only announces a node's content when it gains accessibility focus by default; a <code>liveRegion</code> (Polite or Assertive) tells the service to proactively announce content changes on that node even without a focus change. <code>contentDescription</code> alone just sets the text that would be read on focus — it doesn't trigger re-announcement on update, and <code>testTag</code> is invisible to TalkBack entirely.</p>`,
  },
];
