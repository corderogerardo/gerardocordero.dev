// Batch 11 — MVVM vs MVI vs Clean Architecture layering, unidirectional data flow, single-activity navigation, and multi-module project structure (feature modules, :core, dependency direction, api vs implementation).
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED11_FLASHCARDS: Flashcard[] = [
  {
    id: "a11-arch-patterns-1",
    category: "arch",
    categoryLabel: "Architecture",
    question: `In MVI, why does a single immutable State object tend to scale better than MVVM's multiple independent LiveData/StateFlow streams as a screen grows in complexity?`,
    answerHtml: `<p>With multiple independent streams, the View must combine N states itself, and there's no guarantee about which combination of values it renders at any instant — two streams can emit at slightly different times, producing a transient inconsistent UI (e.g. a loading flag and an error flag both true). A single <b>State</b> data class is updated atomically via a reducer, so the View always renders one coherent snapshot. The tradeoff is more boilerplate per state change and a \`copy()\` call on every reducer transition.</p>`,
  },
  {
    id: "a11-arch-patterns-2",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What specific problem does a one-shot Event/SingleLiveEvent wrapper solve in MVVM, and why doesn't MVI usually need it?`,
    answerHtml: `<p>Plain <code>StateFlow</code>/<code>LiveData</code> replays the last value to new collectors (e.g. after a config change or re-subscription), so a 'show toast' or 'navigate' signal stored as state would re-fire. The Event wrapper (or a \`Channel\`/\`SharedFlow\` with no replay) makes it consumable-once. MVI architectures typically separate <b>State</b> (replayable, idempotent) from <b>Effect/SideEffect</b> (a \`Channel\`-backed one-shot stream) specifically so navigation/snackbar events don't need a wrapper hack — the channel itself is the one-shot mechanism.</p>`,
  },
  {
    id: "a11-arch-patterns-3",
    category: "arch",
    categoryLabel: "Architecture",
    question: `In Clean Architecture's dependency rule, why does the domain layer define repository interfaces rather than the data layer?`,
    answerHtml: `<p>This is the Dependency Inversion Principle applied at the architecture level: source-code dependencies must point inward, toward policy (domain), never outward toward detail (data/frameworks). If the domain depended on a concrete <code>UserRepositoryImpl</code> in the data layer, business rules would be coupled to Retrofit/Room specifics and couldn't be tested or reused without them. By having data implement an interface domain owns, the data layer depends on domain (allowed), and domain stays framework-agnostic and unit-testable in pure Kotlin/JVM with no Android dependency.</p>`,
  },
  {
    id: "a11-arch-patterns-4",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What does it mean for a UseCase/Interactor to be 'just a function' in Clean Architecture, and what's the actual payoff versus calling the repository directly from the ViewModel?`,
    answerHtml: `<p>A UseCase is typically a class with a single <code>operator fun invoke()</code> so call sites read like <code>getUserUseCase(id)</code>. The payoff isn't indirection for its own sake — it's a seam for cross-cutting business logic that doesn't belong in the ViewModel or the repository: combining multiple repositories, applying business rules (e.g. 'only show active subscriptions'), or sequencing calls. If a screen just forwards one repository call with no extra logic, a UseCase adds a file with zero behavior — that's the case to skip it and call the repository from the ViewModel directly.</p>`,
  },
  {
    id: "a11-arch-patterns-5",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why is unidirectional data flow (UDF) specifically valuable for debugging state bugs, compared to two-way data binding?`,
    answerHtml: `<p>In two-way binding, the View can mutate state directly (e.g. an EditText writing back into a bound property), so a bug can originate from either the View or the business logic, and there's no single place to set a breakpoint. In UDF, state flows View ← ViewModel only, and intents/events flow View → ViewModel only; the View never mutates state itself. Every state change has exactly one origin (the reducer/state holder), so reproducing a bug means logging every dispatched intent and every emitted state — a linear, replayable trail instead of a graph of mutation sites.</p>`,
  },
  {
    id: "a11-arch-patterns-6",
    category: "arch",
    categoryLabel: "Architecture",
    question: `In a UDF ViewModel using StateFlow, why is <code>update { }</code> (atomic compare-and-set) preferred over reading \`.value\` and assigning a new value, especially under MVI?`,
    answerHtml: `<p><code>MutableStateFlow.update { currentState -> currentState.copy(...) }</code> performs a lock-free atomic CAS loop, so if two coroutines reduce state concurrently (e.g. a network callback and a user click landing in the same tick), neither update is lost. Reading <code>.value</code>, computing a new state, then assigning <code>.value = newState</code> is a read-modify-write race: a concurrent writer between the read and the write gets silently overwritten — exactly the kind of bug UDF is supposed to make impossible.</p>`,
  },
  {
    id: "a11-arch-patterns-7",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Single-Activity architecture relies on Navigation Component's back stack instead of the Activity back stack. What concretely breaks if a feature module pushes a second Activity instead of a Fragment/composable destination into that graph?`,
    answerHtml: `<p>It fragments the back stack into two independent systems: the OS Activity back stack and the Nav Component's internal back stack inside the host Activity. Deep links, the Up button, and \`popBackStack()\` calls that assume a single graph won't traverse across the Activity boundary correctly, and shared-element transitions / the single Activity's shared ViewModel scope (\`activityViewModels()\`) no longer reach the new Activity. It also duplicates process-level overhead (a second Activity has its own window, decor view, and lifecycle) that single-Activity was adopted specifically to avoid.</p>`,
  },
  {
    id: "a11-arch-patterns-8",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What's the actual architectural argument for single-Activity beyond 'it's simpler' — i.e. what does it enable that multi-Activity structurally can't?`,
    answerHtml: `<p>It gives you one shared <code>ViewModelStoreOwner</code> scope (the Activity) that fragments/composables can share state through (<code>activityViewModels()</code>) without Bundles, and one consistent navigation graph where transitions, shared-element animations, and deep links are all handled by a single \`NavController\` with a uniform API. Multi-Activity forces inter-screen communication through Intent extras (serialization overhead, size limits) and \`startActivityForResult\`-style contracts, and animations between Activities are far more limited than between composables/fragments in one window.</p>`,
  },
  {
    id: "a11-arch-patterns-9",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why does Navigation Compose's <code>hiltViewModel()</code> (or \`navGraphViewModels\`) scoped to a nested \`NavGraph\` matter for multi-step flows like checkout or onboarding?`,
    answerHtml: `<p>A nested graph (e.g. a 3-step checkout flow) gets its own back-stack-entry-scoped lifecycle distinct from the host Activity. Calling <code>hiltViewModel(navController.getBackStackEntry("checkout_graph"))</code> from any screen inside that graph returns the same ViewModel instance, so step 2 and step 3 share in-progress form state without it living in the Activity (where it'd outlive the flow and leak into unrelated screens) or being re-fetched/re-validated at every step. When the user exits the nested graph, that ViewModel is cleared automatically.</p>`,
  },
  {
    id: "a11-arch-patterns-10",
    category: "arch",
    categoryLabel: "Architecture",
    question: `In a multi-module Gradle project, what's the functional difference between declaring a dependency as <code>api</code> versus <code>implementation</code>, and why does it matter for build performance?`,
    answerHtml: `<p><code>implementation</code> keeps the dependency on that module's own compile/runtime classpath but hides it from consumers' compile classpath — a module depending on yours can't see its transitive types. <code>api</code> re-exposes it transitively. The performance impact is real: with \`implementation\`, changing module C (used internally by B) doesn't force recompilation of A which depends on B, because C never leaked into B's public API surface. Defaulting everything to \`api\` creates a flat transitive graph where any leaf change ripples through Gradle's incremental compilation and invalidates far more downstream modules.</p>`,
  },
  {
    id: "a11-arch-patterns-11",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why should feature modules in a multi-module app generally not depend on each other directly, and what pattern replaces that dependency?`,
    answerHtml: `<p>Direct feature-to-feature dependencies (\`:feature-checkout\` depending on \`:feature-profile\`) create a tangled graph that kills parallel Gradle compilation, makes any single feature undeployable/untestable in isolation, and risks cycles. The standard fix is to route cross-feature communication through \`:core\` (shared contracts/navigation interfaces) or a thin \`:feature-api\` module that exposes only an interface/route, while the concrete \`:feature-profile\` implementation is wired in at the \`:app\` module via DI. Navigation between features goes through a shared route/contract type, not a direct class reference.</p>`,
  },
  {
    id: "a11-arch-patterns-12",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What's the difference between <code>:core:data</code>/<code>:core:network</code> style 'horizontal' core modules and a 'vertical' feature module, and why mixing the two granularities inside one module is a common mistake?`,
    answerHtml: `<p>Core modules are sliced by technical layer (networking, database, design-system) and are reused horizontally across every feature. Feature modules are sliced by business capability (checkout, profile) and are vertical slices containing their own UI/domain/data for that capability. Putting feature-specific UI code in \`:core:ui\`, or putting a shared \`ApiClient\` inside \`:feature-checkout\`, breaks the reuse model: other features either can't access genuinely shared code, or are forced to depend on an unrelated feature just to reach a utility that should have been horizontal.</p>`,
  },
  {
    id: "a11-arch-patterns-13",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why does enabling Gradle's configuration cache and parallel execution actually pay off for a multi-module app but barely matter for a single-module one?`,
    answerHtml: `<p>Configuration cache skips re-evaluating build scripts (the configuration phase) across builds, and parallel execution lets independent modules' tasks run on separate workers. A single-module project has one configuration graph and no inter-module parallelism to exploit. A 30+ module project has hundreds of inter-module task dependencies; Gradle can schedule unrelated modules' \`compileKotlin\`/\`lint\` tasks concurrently, and configuration cache avoids re-walking that whole dependency graph on every invocation — the win compounds with module count, which is precisely why modularization is sold as a build-speed strategy, not just an org strategy.</p>`,
  },
  {
    id: "a11-arch-patterns-14",
    category: "arch",
    categoryLabel: "Architecture",
    question: `In MVVM, what's the architectural risk of exposing a \`MutableLiveData\`/\`MutableStateFlow\` directly from a ViewModel instead of backing it with a private mutable field and a public read-only exposed type?`,
    answerHtml: `<p>It breaks the unidirectional contract: any caller holding the ViewModel reference (the View, or in a multi-module setup another module if the ViewModel leaks) can call <code>.value =</code> or <code>.postValue()</code> directly, bypassing whatever validation/business logic the ViewModel's own update methods enforce. The convention <code>private val _state: MutableStateFlow; val state: StateFlow = _state.asStateFlow()</code> isn't boilerplate for its own sake — it's the encapsulation boundary that makes 'state only changes via ViewModel methods' actually enforceable by the compiler rather than by convention.</p>`,
  },
  {
    id: "a11-arch-patterns-15",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why is \`SavedStateHandle\` part of the architectural contract for ViewModels in single-Activity apps, separate from the in-memory state the ViewModel already survives configuration changes with?`,
    answerHtml: `<p><code>ViewModel</code> already survives rotation because it's retained across the Activity recreation tied to a config change, but it does <i>not</i> survive process death (the OS reclaiming a backgrounded app's process under memory pressure) — on process death a brand-new ViewModel instance is created when the user returns. <code>SavedStateHandle</code> is backed by the Bundle the system does persist across process death, so critical UI state (form input, scroll position, current screen args) should be written there, not just held in a plain \`MutableStateFlow\` field, or it silently resets after the OS kills the backgrounded process.</p>`,
  },
  {
    id: "a11-arch-patterns-16",
    category: "arch",
    categoryLabel: "Architecture",
    question: `How does MVI's reducer pattern make state transitions easier to unit test than typical MVVM update logic scattered across multiple ViewModel functions?`,
    answerHtml: `<p>A pure reducer has the signature <code>(State, Intent) -> State</code> with no side effects (no coroutine launch, no repository call inline) — those are pushed out to a separate effect handler. That means testing every transition is just feeding inputs and asserting outputs with no mocking, no \`runTest\`/\`TestScope\`, no coroutine dispatcher injection. MVVM update functions often call a suspend repository function and update state inline in the same function, so testing the state transition also requires mocking the repository and controlling coroutine execution — coupling two concerns that the reducer pattern keeps separate.</p>`,
  },
  {
    id: "a11-arch-patterns-17",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What's the practical reason Clean Architecture maps data-layer DTOs to separate domain models instead of reusing the same data class across both layers?`,
    answerHtml: `<p>A DTO's shape is dictated by the API contract (nullable fields the backend might omit, JSON key names, pagination wrappers) — if the domain layer and UI consume that DTO directly, any backend schema change ripples straight into business logic and the UI. A domain model is owned by the app and shaped for business rules (non-null where business rules guarantee a value, computed fields, no Moshi/Gson annotations). The mapping function at the repository boundary is the one place a backend contract change has to be reconciled, instead of every downstream consumer.</p>`,
  },
  {
    id: "a11-arch-patterns-18",
    category: "arch",
    categoryLabel: "Architecture",
    question: `In a multi-module project, why is it usually wrong to put your DI bindings (e.g. \`@Binds\` for a repository interface to its implementation) in the \`:core\` module rather than colocated with the feature or data module that owns the implementation?`,
    answerHtml: `<p>If \`:core\` binds \`UserRepository\` to \`UserRepositoryImpl\`, then \`:core\` must depend on the concrete data module — inverting the intended dependency direction (core should be a low-level, widely depended-upon module, not one that depends upward toward implementations). It also means \`:core\` can't be reused by a configuration that wants a different implementation (e.g. a test or demo flavor) without modifying the shared module. Hilt modules providing a concrete binding belong in the module that owns the concrete type, installed in whatever component scope is appropriate; \`:core\` should hold only the interface/contract.</p>`,
  },
  {
    id: "a11-arch-patterns-19",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why does adding Compose Navigation's type-safe routes (\`@Serializable\` route objects, Navigation 2.8+) matter more in a multi-module app than a single-module one?`,
    answerHtml: `<p>String-based routes (<code>"profile/{userId}"</code>) require every module that navigates to a destination to know and hand-construct that string correctly, with no compile-time check across module boundaries — a typo or argument-type mismatch only fails at runtime, often deep in another team's feature module. Type-safe routes (a \`@Serializable\` data class as the destination) move that contract into a shared \`:core:navigation\` (or feature-api) module's actual Kotlin type, so a signature change becomes a compile error in every dependent module immediately, which is exactly the failure mode you want to catch early when features are owned by different people/modules.</p>`,
  },
  {
    id: "a11-arch-patterns-20",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What does it mean for MVI's State to be the 'single source of truth,' and what specific class of bug does that eliminate that plagues naive MVVM screens with several Boolean flags (\`isLoading\`, \`isError\`, \`isEmpty\`)?`,
    answerHtml: `<p>It means every renderable property of the screen lives in one State object, and the View is a pure function of that one value — there is no other place UI-relevant truth can live (no separate \`isLoading\` LiveData the View also checks). The bug it eliminates is impossible-state combinations: with separate flags, nothing stops \`isLoading = true\` and \`isError = true\` being true simultaneously, and the View has to defensively decide which one 'wins.' A sealed State (e.g. \`Loading\`, \`Success(data)\`, \`Error(message)\` as a sealed interface) makes that combination unrepresentable — it won't compile.</p>`,
  },
  {
    id: "a11-arch-patterns-21",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why might a team deliberately choose MVVM over MVI for a screen, despite MVI's stronger guarantees — i.e. what's the real cost of MVI?`,
    answerHtml: `<p>MVI's reducer pattern requires defining an explicit Intent sealed class, a State data class, and a reduce function even for screens with trivial state (one toggle, one list) — for a simple screen this is more files and more ceremony than a couple of \`StateFlow\` fields in a ViewModel. It also means every state change is a full \`copy()\` of a (potentially large) immutable object, which is fine for typical UI screens but is overhead some teams don't want to pay everywhere. A pragmatic codebase often uses MVVM for simple/leaf screens and reserves full MVI for screens with genuinely complex, interdependent state.</p>`,
  },
  {
    id: "a11-arch-patterns-22",
    category: "arch",
    categoryLabel: "Architecture",
    question: `In Clean Architecture, why is it considered a violation for a domain UseCase to import \`android.content.Context\` or any \`android.*\` package directly?`,
    answerHtml: `<p>The domain layer is meant to be pure Kotlin/JVM, with zero Android SDK dependency, so it can run in a plain JVM unit test (fast, no Robolectric/instrumented emulator) and so business rules are reusable outside Android (e.g. shared with a KMP target). Importing \`Context\` forces that UseCase to depend on the Android framework, which means tests now need Robolectric or a mocked Context, and the layer can no longer claim to be platform-independent — Context access (resources, system services) belongs behind a repository/data-source abstraction the domain layer calls through an interface.</p>`,
  },
  {
    id: "a11-arch-patterns-23",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why does testability differ sharply between a ViewModel under MVVM that calls a repository directly inside a \`viewModelScope.launch\` block versus one that delegates to an injected UseCase?`,
    answerHtml: `<p>Both are mockable in principle, but a UseCase as a single-method functional interface (<code>operator fun invoke()</code>) is trivial to fake with a lambda or a hand-written test double with no mocking framework, and it isolates exactly one behavior per test. A ViewModel calling the repository directly often calls multiple repository methods across multiple functions, so a repository mock needs to model more surface area, and a bug in business-rule composition (e.g. combining two repository calls) is being tested inside the ViewModel test rather than in a focused, reusable UseCase test that every consumer of that UseCase benefits from.</p>`,
  },
  {
    id: "a11-arch-patterns-24",
    category: "arch",
    categoryLabel: "Architecture",
    question: `How should a \`:core:designsystem\` module's dependency direction differ from a \`:core:data\` module's, and why would it be a mistake for \`:core:designsystem\` to depend on \`:core:data\`?`,
    answerHtml: `<p><code>:core:designsystem</code> (themes, reusable Compose components) should have effectively zero dependencies beyond Compose/Material itself — it's meant to be the most widely and safely depended-upon module in the graph. If it depended on \`:core:data\`, every feature module pulling in a button or theme would transitively pull in Retrofit/Room/networking dependencies it doesn't need, bloating compile units and creating an unnecessary coupling where a data-layer change could force recompilation of pure-UI consumers. Design-system modules should sit at the bottom of the dependency graph, depended upon, never depending on data/domain.</p>`,
  },
];

export const ADVANCED11_QUIZ: QuizQuestion[] = [
  {
    id: "a11q-arch-patterns-1",
    category: "arch",
    categoryLabel: "Architecture",
    question: `A screen has \`isLoading: Boolean\`, \`errorMessage: String?\`, and \`items: List<Item>\` as three separate StateFlow fields in an MVVM ViewModel. What is the main architectural risk this design has that a single sealed MVI State avoids?`,
    options: [`It uses more memory because three flows are allocated instead of one`, `The combination of flags can represent impossible UI states (e.g. loading and error both true) since nothing enforces mutual exclusivity`, `StateFlow cannot hold a List type without a custom serializer`, `Compose cannot collect more than one StateFlow per composable`],
    answer: 1,
    explanationHtml: `<p>Separate independent flags have no compiler-enforced relationship, so invalid combinations (loading + error, error + non-empty items) are representable and the View must defensively decide precedence. A sealed <code>State</code> (e.g. <code>Loading | Success | Error</code>) makes such combinations a compile error instead of a runtime defensive check.</p>`,
  },
  {
    id: "a11q-arch-patterns-2",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why does Clean Architecture require the domain layer to define repository interfaces rather than letting the data layer define them and the domain layer import that interface from data?`,
    options: [`Kotlin interfaces can only be declared in the lowest-level module of a project`, `It is purely a naming convention with no compile-time consequence`, `Dependency Inversion requires source-code dependencies to point inward (toward domain/policy); if data defined the interface, domain would depend outward on data`, `Repository interfaces must be defined in domain because Hilt cannot inject interfaces declared in a data module`],
    answer: 2,
    explanationHtml: `<p>The Dependency Inversion Principle, applied at the architecture level, means policy (domain/business rules) must not depend on detail (data/frameworks). Domain owns the interface; data implements it and depends on domain — keeping domain's dependency direction inward and framework-free.</p>`,
  },
  {
    id: "a11q-arch-patterns-3",
    category: "arch",
    categoryLabel: "Architecture",
    question: `In a multi-module Gradle build, module \`:feature-checkout\` depends on \`:core-network\` using \`implementation\`. Module \`:app\` depends on \`:feature-checkout\`. What can \`:app\` directly reference from \`:core-network\`'s public API as a result?`,
    options: [`Everything in :core-network's public API, since dependencies are always transitive`, `Nothing from :core-network directly — implementation hides it from :feature-checkout's consumers' compile classpath`, `Only classes annotated @Public in :core-network`, `Only suspend functions, since implementation only hides synchronous APIs`],
    answer: 1,
    explanationHtml: `<p><code>implementation</code> keeps a dependency on the declaring module's own classpath but does not re-expose it to consumers of that module. <code>:app</code> would need <code>:feature-checkout</code> to declare the dependency as <code>api</code> for <code>:core-network</code>'s types to be visible transitively to <code>:app</code>.</p>`,
  },
  {
    id: "a11q-arch-patterns-4",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What is the specific functional difference between a one-shot Effect/SideEffect channel and the State stream in an MVI ViewModel?`,
    options: [`Effect is used only for network errors, State is used for everything else`, `State is observed with collectAsState and Effect cannot be observed in Compose at all`, `State represents the current replayable UI snapshot; Effect represents events meant to be consumed exactly once (e.g. navigation, a one-time snackbar) and is typically backed by a Channel/SharedFlow with no replay`, `They are functionally identical; Effect is just a naming convention for State`],
    answer: 2,
    explanationHtml: `<p>State (StateFlow) replays its latest value to new collectors, which is correct for UI snapshots but wrong for one-shot signals like navigation — replaying those would re-trigger them. Effects use a no-replay stream (commonly a Channel) precisely so each event is delivered and consumed once.</p>`,
  },
  {
    id: "a11q-arch-patterns-5",
    category: "arch",
    categoryLabel: "Architecture",
    question: `A team puts a shared \`ApiClient\` class used by five different feature modules inside \`:feature-checkout\` because that's where it was first needed. What's the direct consequence for the other four feature modules?`,
    options: [`No consequence — Gradle automatically promotes frequently-used classes to a shared module`, `The other four features must each depend on :feature-checkout to reach ApiClient, coupling unrelated business features and undermining feature module isolation`, `ApiClient becomes a compile error since feature modules cannot contain shared utilities`, `The other four features must duplicate ApiClient's code since cross-feature imports are disallowed by Gradle by default`],
    answer: 1,
    explanationHtml: `<p>A genuinely shared, horizontally-reused class belongs in a \`:core\` module. Leaving it in a feature module forces unrelated features to take on a dependency for business logic they don't need, which breaks parallel build isolation and creates an arbitrary coupling that should instead route through \`:core\`.</p>`,
  },
  {
    id: "a11q-arch-patterns-6",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why does \`ViewModel\` surviving a configuration change (e.g. rotation) NOT make \`SavedStateHandle\` redundant for persisting screen state like form input?`,
    options: [`SavedStateHandle is faster than ViewModel's in-memory retention, so it's purely a performance optimization`, `ViewModel does not survive process death (the OS reclaiming a backgrounded process), while the Bundle backing SavedStateHandle does, so critical state should live there to survive that case`, `SavedStateHandle is required because ViewModel cannot hold mutable state at all`, `ViewModel instances are destroyed on every navigation event, unlike SavedStateHandle`],
    answer: 1,
    explanationHtml: `<p>ViewModel retention covers configuration changes within the same process, but the OS can kill a backgrounded app's process entirely under memory pressure — on return, a fresh ViewModel is created. SavedStateHandle is backed by the Bundle the system persists across that process death, so it's the mechanism for surviving that specific case.</p>`,
  },
  {
    id: "a11q-arch-patterns-7",
    category: "arch",
    categoryLabel: "Architecture",
    question: `In single-Activity architecture using Navigation Compose, why is a feature pushing a second Activity onto the task stack (instead of a destination in the shared NavController's graph) considered an architectural break?`,
    options: [`It splits navigation across two independent back-stack systems (OS Activity stack vs. NavController's internal stack), breaking the unified deep-link/back-navigation/shared-ViewModel-scope model single-Activity is built on`, `Android does not allow more than one Activity per app process`, `It causes a guaranteed ANR due to two Activities sharing one Looper`, `Compose cannot render inside a second Activity under any circumstances`],
    answer: 0,
    explanationHtml: `<p>Single-Activity architecture's value comes from one shared NavController graph and one shared ViewModelStoreOwner. A second Activity introduces a separate back stack and window, so deep links, Up navigation, and activityViewModels()-scoped shared state no longer reach across that boundary consistently.</p>`,
  },
];
