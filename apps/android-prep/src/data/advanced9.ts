// Batch 9 — Recomposition internals (skipping, stability inference, @Stable/@Immutable), CompositionLocal, side-effect APIs (LaunchedEffect, DisposableEffect, SideEffect, derivedStateOf, snapshotFlow), and remember/rememberSaveable semantics.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED9_FLASHCARDS: Flashcard[] = [
  {
    id: "a9-compose-internals-1",
    category: "compose",
    categoryLabel: "Compose",
    question: `A composable receives a List<String> as a parameter. Why does the Compose compiler treat it as unstable, and what's the practical consequence for skipping?`,
    answerHtml: `<p><code>List</code> is an interface, not a concrete type the compiler can prove is immutable &mdash; the runtime instance could be a <code>MutableList</code> that mutates without recomposition knowing about it. So the compose compiler marks any parameter typed as <code>List</code> (or any other plain Kotlin collection interface) as <b>unstable</b>. A composable with an unstable parameter loses skipping for that parameter entirely: it recomposes whenever its caller recomposes, even if the actual list reference and contents never changed. Fix: use <code>kotlinx.collections.immutable</code>'s <code>ImmutableList</code>, or wrap the data in a <code>@Immutable</code>-annotated holder class.</p>`,
  },
  {
    id: "a9-compose-internals-2",
    category: "compose",
    categoryLabel: "Compose",
    question: `Two sibling composables both call .value on the same MutableState<Int>, but a recomposition only re-runs one of them. Why?`,
    answerHtml: `<p>Compose's snapshot system tracks reads at the granularity of <b>which recompose scope read which state object during the last composition</b>, not at the granularity of the whole tree. When the state is written, the runtime looks up exactly which scopes subscribed to a read of that object and invalidates only those. If the other composable never called <code>.value</code> in its own body (e.g., it only received the value via an already-resolved parameter, or only holds a lambda that writes to it), it never subscribed, so it's left alone. This is the mechanism behind the advice to read state as deep/narrow as possible.</p>`,
  },
  {
    id: "a9-compose-internals-3",
    category: "compose",
    categoryLabel: "Compose",
    question: `What's the actual difference between @Immutable and @Stable, and when would you reach for @Stable instead of @Immutable?`,
    answerHtml: `<p><code>@Immutable</code> promises every public property's value never changes after construction. <code>@Stable</code> is a weaker promise: equal instances (by <code>equals()</code>) are guaranteed to produce the same composition output, properties <i>may</i> change but any change is observable through Compose's snapshot system (i.e., it's backed by <code>State</code>/<code>MutableState</code> internally), and <code>equals()</code>/<code>hashCode()</code> are well-behaved. Use <code>@Stable</code> for a class that wraps mutable fields as <code>MutableState</code> internally &mdash; for example a presenter/view-model-ish holder exposing <code>var count by mutableStateOf(0)</code> &mdash; because its fields do change over time, just always through the snapshot system Compose already tracks.</p>`,
  },
  {
    id: "a9-compose-internals-4",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why does adding a trailing lambda parameter (like onClick: () -> Unit) sometimes silently disable skipping for a composable, and how do you avoid it?`,
    answerHtml: `<p>A lambda captures variables from its enclosing scope by closure. If the lambda is created fresh on every parent recomposition (e.g., it captures a <code>var</code> or non-stable value, or isn't wrapped with <code>remember</code>), the lambda instance itself is a new object each time even though its logic is identical &mdash; and a fresh object fails the equality check the compose compiler inserts to decide whether to skip. The composable accepting that lambda then can't skip on that parameter. Fixes: hoist/<code>remember</code> the lambda so the same instance is reused, or rely on the fact that since Compose compiler 1.5.4 with strong skipping mode, lambdas with stable captures are auto-remembered &mdash; but lambdas capturing unstable values still defeat this.</p>`,
  },
  {
    id: "a9-compose-internals-5",
    category: "compose",
    categoryLabel: "Compose",
    question: `What does 'strong skipping mode' change about how Compose treats composables with unstable parameters?`,
    answerHtml: `<p>Before strong skipping mode, a composable with <i>any</i> unstable parameter became entirely non-skippable &mdash; the compiler couldn't reason about equality so it gave up and always recomposed it. Strong skipping mode instead compares unstable parameters <b>by reference identity (<code>===</code>)</b> instead of skipping the check altogether, and it auto-<code>remember</code>s lambdas. It shipped as an <b>opt-in</b> flag starting with Compose compiler 1.5.4, then became the <b>default</b> with the Compose Compiler 2.0.0 release (the K2-based compiler bundled into the Kotlin compiler from Kotlin 2.0 onward). So a composable that previously recomposed unconditionally now skips when the same unstable object reference is passed again &mdash; significantly reducing accidental over-recomposition without requiring every type to be marked stable.</p>`,
  },
  {
    id: "a9-compose-internals-6",
    category: "compose",
    categoryLabel: "Compose",
    question: `You annotate a data class @Immutable but one of its fields is a var. What happens, and why is this dangerous?`,
    answerHtml: `<p><code>@Immutable</code> is a developer promise, not a compiler-enforced contract &mdash; the compose compiler trusts the annotation and treats the type as stable without verifying it. If you mutate that <code>var</code> after construction, Compose may not recompose the UI that depends on it, because the runtime believes the object can never produce a different read result and therefore doesn't track it for invalidation. The result is stale UI with no crash and no warning &mdash; one of the nastier classes of Compose bugs because it's silent. The rule: only annotate <code>@Immutable</code>/<code>@Stable</code> types you've actually verified are immutable or that wrap their mutability in <code>State</code>.</p>`,
  },
  {
    id: "a9-compose-internals-7",
    category: "compose",
    categoryLabel: "Compose",
    question: `How does the Compose compiler decide stability for a class defined in a separate Gradle module versus the same module?`,
    answerHtml: `<p>Within a single module, the compiler can inspect every field of a class transitively and infer stability automatically (all fields are <code>val</code> and themselves stable types &rarr; stable). Across module boundaries, by default the compiler treats classes from other modules as <b>unstable</b> unless that module also had the Compose compiler plugin applied (so it emitted the <code>@StabilityInferred</code> metadata the consuming module reads), or the class is explicitly annotated <code>@Stable</code>/<code>@Immutable</code>. This is why a plain data class in a <code>:core:model</code> module that crosses into a feature module can unexpectedly disable skipping &mdash; the fix is to apply the Compose compiler plugin to that module too, or annotate the class explicitly.</p>`,
  },
  {
    id: "a9-compose-internals-8",
    category: "compose",
    categoryLabel: "Compose",
    question: `What's the difference in invalidation behavior between reading a CompositionLocal with .current directly in a composable body versus passing it as an explicit parameter?`,
    answerHtml: `<p>A direct <code>.current</code> read inside a composable subscribes that composable's own recompose scope to the <code>CompositionLocal</code>'s provided value, just like reading any other snapshot state &mdash; when the value providers up the tree changes, every composable that read <code>.current</code> recomposes, regardless of how deep it is or whether its direct parent changed. Passing the resolved value down as an explicit parameter instead routes it through normal parameter-equality skipping, so a deeply nested composable several levels below the actual change won't even get visited if intermediate composables correctly skip. CompositionLocal reads bypass the skip optimization at every read site.</p>`,
  },
  {
    id: "a9-compose-internals-9",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why is CompositionLocal a poor mechanism for passing frequently-changing values like animation progress or scroll offset down a deep tree?`,
    answerHtml: `<p>Every composable that reads <code>.current</code> for that local is subscribed directly to changes in the provided value. If the value changes every frame (animation progress, scroll position), every reading composable recomposes every frame too, with no opportunity to skip &mdash; you've effectively wired a high-frequency invalidation source directly into potentially dozens of scopes. The idiomatic fix for high-frequency values is to keep them as <code>State</code> read only at the leaf that actually needs pixel-level positioning (often inside a <code>Modifier</code> lambda like <code>graphicsLayer { }</code>, which reads state during the layout/draw phase and skips recomposition entirely), not via CompositionLocal.</p>`,
  },
  {
    id: "a9-compose-internals-10",
    category: "compose",
    categoryLabel: "Compose",
    question: `What problem does compositionLocalWithComputedDefaultOf solve that a plain compositionLocalOf { ... } default doesn't?`,
    answerHtml: `<p>A regular <code>compositionLocalOf { defaultValue }</code> factory's default lambda runs once, lazily, the first time it's needed, and that single computed default is then reused/cached for that composition &mdash; it's not recomputed if other CompositionLocals it might depend on change. <code>compositionLocalWithComputedDefaultOf</code> lets the default be derived dynamically from <i>other</i> CompositionLocals' current values at the point of read, recalculating per read context. It's for cases like a <code>TextStyle</code> default that should inherit and merge from an ambient <code>LocalContentColor</code>-style local rather than a single fixed fallback.</p>`,
  },
  {
    id: "a9-compose-internals-11",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why does staticCompositionLocalOf cause a full subtree recomposition on change, while compositionLocalOf only invalidates actual readers?`,
    answerHtml: `<p><code>compositionLocalOf</code> stores its value in a <code>State</code>, so reads are tracked individually by the snapshot system &mdash; only composables that called <code>.current</code> are invalidated on change, same as any other state read. <code>staticCompositionLocalOf</code> skips that tracking entirely: Compose has no record of who read it, so when the provided value changes, it can't selectively invalidate &mdash; it must recompose the <i>entire content under the <code>CompositionLocalProvider</code></i> from scratch. The tradeoff is that <code>static</code> avoids the overhead of state-read tracking for locals that almost never change (theme colors, layout direction), making normal reads cheaper at the cost of an expensive full-subtree recompose on the rare change.</p>`,
  },
  {
    id: "a9-compose-internals-12",
    category: "compose",
    categoryLabel: "Compose",
    question: `A LaunchedEffect(Unit) inside a composable that's part of a list item (e.g., in LazyColumn) seems to restart unexpectedly when the list reorders. Why, and what's the fix?`,
    answerHtml: `<p>If the list item composable doesn't have a stable, content-derived <code>key</code> in the <code>LazyColumn</code>'s <code>items(...)</code> call, Compose may reuse composition slots by position rather than identity when the underlying data reorders &mdash; from the effect's perspective, its enclosing composition is being torn down and a new one created (or its identity changes enough that the coroutine scope's lifecycle resets), restarting <code>LaunchedEffect</code>. The fix is to supply <code>items(list, key = { it.id })</code> so Compose tracks each item by stable identity across reorders, letting compositions (and their effects) survive a reorder instead of being recreated.</p>`,
  },
  {
    id: "a9-compose-internals-13",
    category: "compose",
    categoryLabel: "Compose",
    question: `What's the difference between the cancellation behavior of LaunchedEffect keyed on a changing value versus rememberCoroutineScope-launched work triggered from a callback?`,
    answerHtml: `<p><code>LaunchedEffect(key)</code> automatically cancels its running coroutine and relaunches a fresh one whenever <code>key</code> changes value (or the composable leaves composition), because the effect's lifecycle is tied to composition and the key comparison. A coroutine launched via <code>rememberCoroutineScope().launch { }</code> from, say, an <code>onClick</code> handler has no such automatic cancellation tied to recomposition &mdash; it only gets cancelled when the composable itself leaves the composition (the scope follows the composable's lifecycle), not on every recomposition or value change. Use <code>LaunchedEffect</code> when the work should restart in response to a value changing; use the remembered scope for one-shot, user-triggered work that shouldn't be torn down just because the screen recomposed.</p>`,
  },
  {
    id: "a9-compose-internals-14",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why does DisposableEffect require its lambda to end with an explicit onDispose { } block, when LaunchedEffect doesn't require an equivalent?`,
    answerHtml: `<p><code>DisposableEffect</code> is designed for wiring up non-coroutine, non-suspending side effects &mdash; registering a listener, observer, or callback with an external system (lifecycle observers, broadcast receivers, view-based listeners) &mdash; where cleanup must be explicit because there's no coroutine to cancel. The compiler actually enforces this: a <code>DisposableEffect</code> block that doesn't end in <code>onDispose { }</code> is a compile error, precisely because forgetting cleanup there leaks the registration permanently (no structured concurrency to fall back on). <code>LaunchedEffect</code> instead runs suspend code, and cancelling the coroutine itself is the cleanup mechanism, so there's no separate dispose callback to require.</p>`,
  },
  {
    id: "a9-compose-internals-15",
    category: "compose",
    categoryLabel: "Compose",
    question: `When should you reach for SideEffect instead of LaunchedEffect(Unit), given both seem to 'run something after composition'?`,
    answerHtml: `<p><code>SideEffect</code> runs synchronously on <b>every successful recomposition</b>, on the main/UI thread, with no coroutine and no key-based skipping &mdash; it's for publishing Compose-internal state out to a non-Compose system that must always be in sync with the latest committed composition (e.g., updating an analytics library's current screen tag, or syncing a value into a non-Compose view your screen still hosts). <code>LaunchedEffect(Unit)</code> runs once on entering composition (not on every recompose) and launches a coroutine, suited to async work like a one-time data fetch. Reaching for <code>LaunchedEffect(Unit)</code> for analytics tagging is a common mistake: it won't fire again on a relevant recomposition the way <code>SideEffect</code> does.</p>`,
  },
  {
    id: "a9-compose-internals-16",
    category: "compose",
    categoryLabel: "Compose",
    question: `What specific problem does derivedStateOf solve that you don't get just by computing a value inline in the composable body from a State?`,
    answerHtml: `<p>An inline computation (e.g., <code>val showButton = listState.firstVisibleItemIndex > 0</code>) re-evaluates on every recomposition of that composable, but more importantly, every <i>read</i> of <code>listState.firstVisibleItemIndex</code> itself triggers recomposition on every scroll-pixel change since it's backed by frequently-updating state. <code>derivedStateOf</code> wraps the computation in its own <code>State</code> that only reports a change (and thus only invalidates its readers) when the <b>computed result</b> actually differs, not when an input changes. So <code>showButton</code> derived this way only triggers recomposition when the boolean flips, even though the underlying scroll index changes dozens of times per second &mdash; it collapses a high-frequency input into a low-frequency output.</p>`,
  },
  {
    id: "a9-compose-internals-17",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why is wrapping derivedStateOf itself in remember mandatory, and what breaks if you forget it?`,
    answerHtml: `<p><code>derivedStateOf { ... }</code> creates a brand-new <code>State</code> object holding its own cache of the last computed value and the snapshot read observers needed to know when to recompute. If you don't <code>remember</code> it, every recomposition creates a fresh derived-state object with no memory of the previous result, defeating the entire purpose &mdash; you'd recompute on every recomposition just like the inline version, with the added overhead of allocating a new derived-state wrapper each time. The idiom is always <code>val x by remember { derivedStateOf { ... } }</code>.</p>`,
  },
  {
    id: "a9-compose-internals-18",
    category: "compose",
    categoryLabel: "Compose",
    question: `What's the difference between using snapshotFlow to observe Compose state versus exposing a StateFlow directly from a ViewModel and collecting it with collectAsState()?`,
    answerHtml: `<p><code>snapshotFlow { }</code> goes the <i>other direction</i>: it converts reads of Compose <code>State</code>/<code>MutableState</code> objects into a cold <code>Flow</code>, emitting whenever the snapshot system detects the read values changed (with built-in distinct-by-equality, like <code>distinctUntilChanged</code>). It's for bridging Compose-owned UI state (like <code>LazyListState.firstVisibleItemIndex</code>, which lives inside Compose) out into the Flow/coroutines world &mdash; e.g., debouncing scroll position changes before firing an analytics event. <code>collectAsState()</code> is the reverse bridge: turning a <code>Flow</code> (often a ViewModel's <code>StateFlow</code>, which is the source of truth) into Compose <code>State</code> so the UI can read it. They compose together but solve opposite directions of the same problem.</p>`,
  },
  {
    id: "a9-compose-internals-19",
    category: "compose",
    categoryLabel: "Compose",
    question: `Inside a LaunchedEffect, you call snapshotFlow { scrollState.value }.collect { ... }. Why doesn't this need a manual distinctUntilChanged(), and when would you still add one?`,
    answerHtml: `<p><code>snapshotFlow</code> already applies equality-based deduplication internally before emitting &mdash; if the lambda's result is <code>equals()</code> to the previously emitted value, no new emission happens, so consecutive identical reads (e.g., a snapshot read that fires but the integer didn't actually change) are filtered for free. You'd still add an explicit <code>distinctUntilChanged()</code> (or <code>.filter</code>) downstream if your equality semantics differ from the default &mdash; for instance, if the lambda returns a data class and you only care about a subset of its fields changing, since <code>snapshotFlow</code>'s dedup is based on the full returned value's <code>equals()</code>.</p>`,
  },
  {
    id: "a9-compose-internals-20",
    category: "compose",
    categoryLabel: "Compose",
    question: `remember { mutableStateOf(...) } survives recomposition but not configuration change or process death. What exactly does rememberSaveable add, and what's required for it to work with a custom type?`,
    answerHtml: `<p><code>rememberSaveable</code> additionally persists the value across <b>activity recreation</b> (configuration changes like rotation) and <b>process death + restore</b>, by saving it into the same <code>Bundle</code>-backed <code>SavedStateRegistry</code> that <code>onSaveInstanceState</code> uses &mdash; not into permanent storage, so it's lost if the user fully navigates away and the process is killed without the OS planning a restore. For a custom type, the value must either be one of the types <code>Bundle</code> natively supports (primitives, <code>Parcelable</code>, <code>Serializable</code>), or you supply a custom <code>Saver</code> (commonly via <code>mapSaver</code> or <code>listSaver</code>) telling Compose how to decompose the object into saveable primitives and reconstruct it.</p>`,
  },
  {
    id: "a9-compose-internals-21",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why can two rememberSaveable calls in the same composable for the same type silently collide, and how does Compose normally prevent this?`,
    answerHtml: `<p><code>rememberSaveable</code> auto-generates a key for the saved value based on the call site's position/structure in the composition (similar to how <code>remember</code> slots work), but if two calls happen to produce the same generated key &mdash; most commonly because they're inside the same loop or conditionally-called sibling branches without differentiation &mdash; their saved values can overwrite each other on restore. Compose detects exact key collisions for sibling calls in many common cases and throws at runtime, but subtler cases (e.g., across recompositions where call order differs) can silently restore the wrong value. The explicit fix is to pass a unique <code>key</code> parameter, e.g., <code>rememberSaveable(key = "item_\${item.id}")</code>, especially inside loops over dynamic lists.</p>`,
  },
  {
    id: "a9-compose-internals-22",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why does remember(key1) re-initialize its stored value when key1 changes, and how is this different from LaunchedEffect(key1) restarting?`,
    answerHtml: `<p><code>remember(key1) { compute() }</code> compares <code>key1</code> to its previous value on each recomposition; if it differs, the cached value is discarded and <code>compute()</code> runs again synchronously, inline, during composition &mdash; there's no coroutine, no cancellation semantics, just a cache invalidation. <code>LaunchedEffect(key1)</code> does something structurally similar (compare key, and if changed, treat as a new effect) but the consequence is asynchronous: it cancels the in-flight coroutine launched under the old key and starts a new suspend block under the new key, off the composition's critical path. Conceptually both are 'restart when key changes,' but <code>remember</code> recomputes a value synchronously in-line while <code>LaunchedEffect</code> relaunches an out-of-line coroutine.</p>`,
  },
  {
    id: "a9-compose-internals-23",
    category: "compose",
    categoryLabel: "Compose",
    question: `What's the practical difference between marking a Compose UI-state holder class @Stable with mutableStateOf fields versus making it a plain data class recreated on every state change (a copy-based immutable model)?`,
    answerHtml: `<p>A <code>@Stable</code> holder with internal <code>mutableStateOf</code> fields lets Compose track each field independently &mdash; a composable that reads only <code>holder.scrollOffset</code> recomposes when that field changes but not when <code>holder.title</code> changes, because each field is its own snapshot-tracked state. A copy-based immutable <code>data class</code> recreated wholesale on every change (<code>state = state.copy(scrollOffset = x)</code>) only has one granularity: any field change produces a new top-level object, so every composable reading <i>any</i> field of that object recomposes on <i>any</i> field changing, since they're all reading from the same parameter reference. The holder pattern gives finer-grained recomposition at the cost of more mutable internal state to reason about; the immutable-copy pattern is simpler to reason about and test but coarser-grained.</p>`,
  },
  {
    id: "a9-compose-internals-24",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why might enabling strong skipping mode change the observed behavior of a screen that relied on a composable recomposing every time purely because one of its parameters was an unstable lambda?`,
    answerHtml: `<p>Before strong skipping, an unstable-captured lambda parameter being recreated each recomposition guaranteed the receiving composable never skipped &mdash; some code accidentally depended on this to 'always re-run' (e.g., side logic stuffed into the composable body instead of a proper effect). Under strong skipping mode, the compiler auto-<code>remember</code>s lambdas with stable captures, and compares unstable parameters by reference equality rather than disabling skipping outright &mdash; so a composable that used to recompose on every parent recomposition might now correctly skip, exposing a latent bug where someone relied on a side effect running inside the composable body rather than in <code>LaunchedEffect</code>/<code>SideEffect</code>. This is a common real-world migration gotcha when adopting strong skipping mode.</p>`,
  },
];

export const ADVANCED9_QUIZ: QuizQuestion[] = [
  {
    id: "a9q-compose-internals-1",
    category: "compose",
    categoryLabel: "Compose",
    question: `A composable takes a parameter of type List<Item> where Item is a @Immutable data class. Will the composable be skippable based on that parameter?`,
    options: [`Yes, because Item is @Immutable the whole List inherits stability`, `No, because List is a plain Kotlin collection interface, which the compose compiler treats as unstable regardless of its element type`, `Yes, but only if the list has fewer than a fixed compiler-defined size`, `No, because @Immutable only applies to classes, never to types used as generic parameters`],
    answer: 1,
    explanationHtml: `<p>The compose compiler's stability inference doesn't special-case generic type arguments for collection interfaces &mdash; <code>List</code>, <code>Map</code>, and <code>Set</code> are treated as unstable because the compiler can't rule out a mutable runtime implementation, independent of whether the element type is stable. Use <code>kotlinx.collections.immutable</code>'s <code>ImmutableList</code> (or <code>@Immutable</code>-wrap the container) to get a stable collection type.</p>`,
  },
  {
    id: "a9q-compose-internals-2",
    category: "compose",
    categoryLabel: "Compose",
    question: `What is the key behavioral difference between compositionLocalOf and staticCompositionLocalOf?`,
    options: [`staticCompositionLocalOf can only hold val properties, compositionLocalOf can hold var`, `compositionLocalOf tracks individual reads and invalidates only readers on change; staticCompositionLocalOf has no read tracking and recomposes the entire provided subtree on change`, `staticCompositionLocalOf is resolved at compile time and cannot change at runtime`, `compositionLocalOf is only available inside @Composable functions while staticCompositionLocalOf works anywhere`],
    answer: 1,
    explanationHtml: `<p><code>compositionLocalOf</code> stores its value as snapshot <code>State</code>, so Compose tracks exactly which composables read <code>.current</code> and invalidates only those on a change. <code>staticCompositionLocalOf</code> skips that tracking for cheaper reads, but as a consequence a value change forces the entire subtree under the provider to recompose, since Compose has no record of who actually depends on it.</p>`,
  },
  {
    id: "a9q-compose-internals-3",
    category: "compose",
    categoryLabel: "Compose",
    question: `Inside a composable, you write val isAtTop by remember { derivedStateOf { listState.firstVisibleItemIndex == 0 } } . What is the main benefit over writing val isAtTop = listState.firstVisibleItemIndex == 0 directly?`,
    options: [`It runs the computation on a background thread automatically`, `It persists isAtTop across process death like rememberSaveable`, `It only triggers recomposition of readers of isAtTop when the boolean result actually flips, not on every scroll-index change`, `It makes listState itself immutable so it can be passed to other modules safely`],
    answer: 2,
    explanationHtml: `<p><code>derivedStateOf</code> creates a <code>State</code> that only reports a change when its computed output differs from the last computed output. <code>firstVisibleItemIndex</code> changes on virtually every scroll pixel, but <code>isAtTop</code> only flips rarely &mdash; wrapping it in <code>derivedStateOf</code> collapses that high-frequency input into low-frequency invalidations for anything reading <code>isAtTop</code>.</p>`,
  },
  {
    id: "a9q-compose-internals-4",
    category: "compose",
    categoryLabel: "Compose",
    question: `Which side-effect API is the correct choice for registering a BroadcastReceiver (or similar external listener) when a composable enters composition, and unregistering it when the composable leaves?`,
    options: [`LaunchedEffect, because it supports cleanup via a return statement`, `SideEffect, because it runs on every recomposition so the listener stays in sync`, `DisposableEffect, because it provides an explicit onDispose block for the unregister call and is designed for non-coroutine external resource lifecycles`, `snapshotFlow, because it converts the broadcast callback into a Flow automatically`],
    answer: 2,
    explanationHtml: `<p><code>DisposableEffect</code> exists specifically for wiring up and tearing down listeners/observers/callbacks tied to non-Compose, non-coroutine systems. Its <code>onDispose { }</code> block is mandatory (a compile error if omitted) precisely so the registration is always paired with cleanup when the key changes or the composable leaves composition.</p>`,
  },
  {
    id: "a9q-compose-internals-5",
    category: "compose",
    categoryLabel: "Compose",
    question: `Strong skipping mode is enabled (the default with current Compose compiler versions). A composable has one unstable parameter and no stable parameters changed. What happens on the parent's recomposition?`,
    options: [`The composable always recomposes unconditionally, exactly as it did before strong skipping mode existed`, `The composable is skipped entirely regardless of the unstable parameter's value`, `The composable compares the unstable parameter by reference equality (===); if the same instance is passed again, it skips`, `Compose throws a compile-time error requiring the parameter to be made stable`],
    answer: 2,
    explanationHtml: `<p>Strong skipping mode doesn't make unstable types stable &mdash; it changes the comparison strategy: unstable parameters are compared by reference identity instead of disabling skipping outright. If the exact same object reference is passed again (e.g., because it was hoisted/remembered upstream), the composable can still skip; only an actually-new instance forces recomposition.</p>`,
  },
  {
    id: "a9q-compose-internals-6",
    category: "compose",
    categoryLabel: "Compose",
    question: `Why is rememberSaveable insufficient as a substitute for persisting user data to a database or DataStore?`,
    options: [`rememberSaveable only works inside LazyColumn items`, `rememberSaveable writes synchronously to disk on every recomposition, causing jank`, `rememberSaveable's storage (the SavedStateRegistry/Bundle) survives configuration change and OS-initiated process death for restore, but not the user force-stopping the app, clearing app data, or needing the data on a different device/session`, `rememberSaveable requires a network connection to persist values to the SavedStateRegistry`],
    answer: 2,
    explanationHtml: `<p><code>rememberSaveable</code> piggybacks on the <code>Bundle</code>-based instance-state mechanism Android uses for configuration changes and OS-reclaimed process death where a restore is expected &mdash; it's not durable storage. Force-stopping the app, clearing data, uninstalling, or just needing the value beyond that single restorable instance all require real persistence (Room, DataStore, files), which <code>rememberSaveable</code> does not provide.</p>`,
  },
];
