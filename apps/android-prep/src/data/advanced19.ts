// Batch 19 — Navigation Compose (type-safe routes, deep links), DataStore (Preferences vs Proto), Paging 3 (RemoteMediator, PagingSource), and CameraX basics.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED19_FLASHCARDS: Flashcard[] = [
  {
    id: "a19-jetpack-libs-1",
    category: "framework",
    categoryLabel: "Framework",
    question: `In Navigation Compose's type-safe routes (Nav 2.8+), how do you define a route with arguments, and what's the key advantage over the old string-route approach?`,
    answerHtml: `<p>You define a <code>@Serializable</code> class or object (via <code>kotlinx.serialization</code>) and pass an instance directly to <code>navigate()</code>, e.g. <code>navigate(ProfileRoute(userId = 42))</code> instead of building <code>"profile/42"</code> by hand.</p><p>The win is compile-time safety: argument names and types are checked by the compiler, so a typo'd argument key can no longer compile and silently crash at runtime the way string-based routes could.</p>`,
  },
  {
    id: "a19-jetpack-libs-2",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why does deep linking into a type-safe Navigation Compose route still require a URI pattern via navDeepLink, even though the route itself is a typed Kotlin object?`,
    answerHtml: `<p>A deep link arrives as text (a URL from an intent or browser), so it has to be parsed into the typed route before navigation can use it. <code>navDeepLink&lt;Route&gt; { uriPattern = "..." }</code> still maps placeholders in the URI string onto the route's typed fields.</p><p>The type safety applies to in-app navigation calls; the boundary with the outside world is still string-based, just validated against the route's declared shape instead of a loose pattern map.</p>`,
  },
  {
    id: "a19-jetpack-libs-3",
    category: "framework",
    categoryLabel: "Framework",
    question: `What's the practical difference between nested navigation graphs and a flat graph with many composable() destinations, beyond visual organization?`,
    answerHtml: `<p>A nested graph (<code>navigation&lt;Route&gt;(startDestination = ...) { ... }</code>) gets its own <code>NavBackStackEntry</code> scope at the graph level, which is what makes a shared <code>ViewModel</code> across a flow (e.g. a multi-step checkout) possible via <code>hiltViewModel(navController.getBackStackEntry(graphRoute))</code> or the route-scoped <code>viewModel()</code> overload.</p><p>It also lets you pop the whole flow as a unit with <code>popUpTo(graphRoute)</code>, rather than tracking and popping each screen individually.</p>`,
  },
  {
    id: "a19-jetpack-libs-4",
    category: "framework",
    categoryLabel: "Framework",
    question: `How do you scope a ViewModel to survive across several screens in a flow but be cleared when the user leaves that flow entirely?`,
    answerHtml: `<p>Put those screens in a nested navigation graph and request the ViewModel using a <code>NavBackStackEntry</code> scoped to the parent graph's route rather than the current screen's: <code>val parentEntry = remember(backStackEntry) { navController.getBackStackEntry(graphRoute) }</code>, then <code>viewModel(parentEntry)</code>.</p><p>Because the entry lives as long as the graph is on the back stack, the ViewModel persists across screen-to-screen navigation inside the flow and is cleared automatically when the whole graph is popped.</p>`,
  },
  {
    id: "a19-jetpack-libs-5",
    category: "framework",
    categoryLabel: "Framework",
    question: `What does popUpTo with inclusive = true actually remove from the back stack, and what's a concrete case where forgetting it breaks UX?`,
    answerHtml: `<p><code>popUpTo(route) { inclusive = true }</code> pops every entry up to and including the entry matching <code>route</code>. Without <code>inclusive</code>, that entry stays on the stack.</p><p>Classic bug: after a login screen, navigating to home with <code>popUpTo("login") { inclusive = true }</code> removes login from the back stack so the system back button doesn't return the user to it. Omitting <code>inclusive</code> leaves login reachable via back, which is wrong post-auth.</p>`,
  },
  {
    id: "a19-jetpack-libs-6",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why might a navigate() call with launchSingleTop = true still create a duplicate destination on the back stack?`,
    answerHtml: `<p><code>launchSingleTop</code> only dedupes against the entry currently at the <b>top</b> of the back stack. If the same destination already exists deeper in the stack (not at the top), a new instance is still pushed on top — you get two copies, just not adjacent.</p><p>To truly reuse an existing instance anywhere in the stack you combine it with <code>popUpTo(route) { saveState = true }</code> plus <code>restoreState = true</code>, the pattern used for bottom-nav tab switching.</p>`,
  },
  {
    id: "a19-jetpack-libs-7",
    category: "framework",
    categoryLabel: "Framework",
    question: `In bottom navigation built on Navigation Compose, what do saveState and restoreState on navigate() actually preserve, and why is that needed for a good tab-switching feel?`,
    answerHtml: `<p><code>saveState = true</code> (on the <code>popUpTo</code> of the start destination) saves each tab's back stack and UI state (scroll position, partially-filled state held in <code>rememberSaveable</code>) into the <code>NavController</code>'s saved-state registry when you leave that tab. <code>restoreState = true</code> on the subsequent <code>navigate()</code> pulls that state back instead of recreating the destination from scratch.</p><p>Without both, switching tabs back and forth resets each tab to its start destination and loses scroll/form state — the opposite of how native bottom-nav (e.g. Play Store, Instagram) behaves.</p>`,
  },
  {
    id: "a19-jetpack-libs-8",
    category: "framework",
    categoryLabel: "Framework",
    question: `What's the structural difference between Preferences DataStore and Proto DataStore, and when does that difference actually matter?`,
    answerHtml: `<p>Preferences DataStore stores an untyped key-value map (like SharedPreferences but async/transactional), so there's no schema — you can read a key that was never written, or write a key with the wrong type at any call site, and the compiler won't catch it.</p><p>Proto DataStore stores a single serialized Protobuf message with a schema you define in a <code>.proto</code> file, giving you compile-time-checked, strongly-typed fields, nested objects, and built-in default values. It matters once your settings model has structure (nested objects, enums, lists) or correctness guarantees you want enforced — Preferences DataStore doesn't scale well past a flat bag of primitives.</p>`,
  },
  {
    id: "a19-jetpack-libs-9",
    category: "framework",
    categoryLabel: "Framework",
    question: `How does DataStore guarantee transactional, atomic updates, and what's the API shape that enforces this?`,
    answerHtml: `<p>All writes go through <code>updateData { currentState -> newState }</code> (Proto) or <code>edit { prefs -> ... }</code> (Preferences), which run on a single-threaded dispatcher internally and serialize concurrent updates — there's no public synchronous "set" API that could race.</p><p>Each update is applied to a snapshot and written atomically to disk (write to a temp file, then rename), so a process death mid-write can't leave a half-written, corrupted file the way a naive <code>FileOutputStream</code> write could.</p>`,
  },
  {
    id: "a19-jetpack-libs-10",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why is DataStore's data exposed as a Flow<T> instead of a synchronous getter, and what migration problem does this solve relative to SharedPreferences?`,
    answerHtml: `<p>DataStore is built on Kotlin coroutines/Flow from the ground up: <code>dataStore.data</code> emits the current value and re-emits on every successful update, so observers stay in sync without manual listener registration/unregistration (unlike <code>SharedPreferences.OnSharedPreferenceChangeListener</code>, which is easy to leak).</p><p>It also solves SharedPreferences' biggest correctness problem: <code>SharedPreferences.apply()</code>/<code>commit()</code> can block the main thread on first load or do disk I/O synchronously via <code>getX()</code>; DataStore has no synchronous read API at all, which forces callers off the main thread by construction.</p>`,
  },
  {
    id: "a19-jetpack-libs-11",
    category: "framework",
    categoryLabel: "Framework",
    question: `How should you migrate existing SharedPreferences data to DataStore without losing user settings on upgrade?`,
    answerHtml: `<p>Use <code>SharedPreferencesMigration</code>, passed into <code>PreferenceDataStoreFactory.create(produceMigrations = { listOf(SharedPreferencesMigration(context, prefsName)) })</code>. It runs once, before the first read, copying matching keys from the old <code>SharedPreferences</code> file into the new DataStore.</p><p>You can scope it with a <code>keysToMigrate</code> set or a <code>shouldRunMigration</code> predicate to avoid re-running after the first successful migration, and it composes with custom <code>DataMigration</code> implementations for Proto DataStore if you need to map old flat keys onto new message fields.</p>`,
  },
  {
    id: "a19-jetpack-libs-12",
    category: "framework",
    categoryLabel: "Framework",
    question: `What exception type does DataStore throw on read errors (e.g. corrupted file), and what's the idiomatic way to handle it in a Flow pipeline?`,
    answerHtml: `<p><code>dataStore.data</code> throws <code>IOException</code> through the Flow when the underlying file can't be read or deserialized — for Proto/custom serializers this is specifically a <code>CorruptionException</code> (a subclass of <code>IOException</code>) thrown by your <code>Serializer</code>. It does not throw for a missing file — that case yields the default value instead.</p><p>The idiomatic handling is <code>.catch { exception -> if (exception is IOException) emit(defaultValue) else throw exception }</code> upstream of any <code>map</code> calls, so corruption degrades to defaults rather than crashing the collector.</p>`,
  },
  {
    id: "a19-jetpack-libs-13",
    category: "framework",
    categoryLabel: "Framework",
    question: `In Paging 3, what's the division of responsibility between PagingSource and RemoteMediator when a feature needs both a local Room cache and a network source of truth?`,
    answerHtml: `<p><code>PagingSource</code> is the single source the <code>Pager</code> actually pages from at the UI layer — in the network+cache pattern it's generated by Room directly off a <code>@Query</code>, so the UI always observes the local database.</p><p><code>RemoteMediator</code> sits behind it and is invoked by Paging when the local <code>PagingSource</code> runs low on data (or on refresh); its job is purely to fetch from the network and write into Room. The UI never talks to <code>RemoteMediator</code> directly — it reacts to Room's <code>PagingSource</code> invalidating once the mediator's write lands.</p>`,
  },
  {
    id: "a19-jetpack-libs-14",
    category: "framework",
    categoryLabel: "Framework",
    question: `What do the three LoadType values (REFRESH, PREPEND, APPEND) mean inside RemoteMediator.load(), and which one is responsible for the initial page fetch?`,
    answerHtml: `<p><code>REFRESH</code> fires on initial load or a manual <code>refresh()</code>/pull-to-refresh, and is responsible for fetching the first page — typically the mediator clears stale cached rows here (or relies on a <code>RemoteKeys</code> table reset) before inserting the fresh page.</p><p><code>PREPEND</code> fires when the user scrolls toward the start of the loaded data (rare for typical infinite feeds, common for chat history loaded backwards), and <code>APPEND</code> fires when scrolling toward the end — the normal "load more" case. Each branch typically reads a previous/next page token from a <code>RemoteKeys</code> table to know what to fetch next.</p>`,
  },
  {
    id: "a19-jetpack-libs-15",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why does a typical network+database RemoteMediator implementation need a separate RemoteKeys table in Room, instead of deriving the next page token from the items themselves?`,
    answerHtml: `<p>Paged items (e.g. a list of articles) usually don't carry their own "next page cursor" — that's a property of the query/response, not the entity. A <code>RemoteKeys</code> table stores, per item or per page, the <code>prevKey</code>/<code>nextKey</code> (or a page number) returned by the API, keyed by item ID, inside the same database transaction as the items.</p><p>Storing it transactionally alongside the data is what lets <code>REFRESH</code> correctly clear both tables atomically and lets <code>PREPEND</code>/<code>APPEND</code> look up "what page comes next" from data that survives process death, rather than re-deriving it from in-memory state that a <code>RemoteMediator</code> (which can be recreated) wouldn't have.</p>`,
  },
  {
    id: "a19-jetpack-libs-16",
    category: "framework",
    categoryLabel: "Framework",
    question: `What does RemoteMediator.load() return to tell Paging whether more data exists, and what's the consequence of getting MediatorResult.endOfPaginationReached wrong?`,
    answerHtml: `<p>It returns <code>MediatorResult.Success(endOfPaginationReached: Boolean)</code> on success or <code>MediatorResult.Error(throwable)</code> on failure. <code>endOfPaginationReached = true</code> tells Paging there's no more data in that direction, which stops further <code>APPEND</code>/<code>PREPEND</code> calls and lets the UI show an "end of list" footer via <code>LoadState.NotLoading(endOfPaginationReached = true)</code>.</p><p>Setting it incorrectly to <code>true</code> too early silently truncates the list (Paging stops asking for more even though the API has more); leaving it <code>false</code> forever causes Paging to keep re-requesting a page that returns nothing, wasting calls and potentially spinning a footer loader indefinitely.</p>`,
  },
  {
    id: "a19-jetpack-libs-17",
    category: "framework",
    categoryLabel: "Framework",
    question: `How does Paging 3 decide what counts as a 'duplicate' page request, and why is PagingConfig.prefetchDistance more than just a UX tuning knob?`,
    answerHtml: `<p>Paging tracks load state per <code>LoadType</code> and won't issue a new <code>APPEND</code>/<code>PREPEND</code> while one is already in flight or already reached <code>endOfPaginationReached</code>, so duplicate requests are prevented by the library, not by mediator-side debouncing.</p><p><code>prefetchDistance</code> controls how many items from the edge of loaded data trigger the next page load — set too low, the user can scroll faster than the network responds and see a visible loading gap; set too high, you're fetching pages the user may never scroll to, wasting data and battery. It directly trades perceived latency against unnecessary network/DB work.</p>`,
  },
  {
    id: "a19-jetpack-libs-18",
    category: "framework",
    categoryLabel: "Framework",
    question: `What's the role of PagingData.map / insertSeparators in a Paging 3 pipeline, and why must transformations be applied to the Flow<PagingData<T>> rather than to a single loaded list?`,
    answerHtml: `<p><code>PagingData</code> represents an unbounded, lazily-loaded stream, not a materialized list, so you can't <code>.map</code> it like a <code>List</code> after the fact — transformations like <code>.map { }</code>, <code>.filter { }</code>, or <code>.insertSeparators { before, after -> }</code> (e.g. inserting date headers between items) are applied to each <code>PagingData</code> emission inside the <code>Flow</code>, so they re-run consistently as new pages load and old ones get dropped from memory.</p><p>This also keeps the transformation cheap per page rather than re-scanning the whole logical list on every page boundary.</p>`,
  },
  {
    id: "a19-jetpack-libs-19",
    category: "framework",
    categoryLabel: "Framework",
    question: `In Compose, what does LazyPagingItems give you over collecting Flow<PagingData<T>> directly, and what's a common mistake when keying list items from it?`,
    answerHtml: `<p><code>val items = pager.collectAsLazyPagingItems()</code> adapts the stream into something <code>LazyColumn</code> can index synchronously (<code>items.itemCount</code>, <code>items[index]</code>), and exposes <code>items.loadState</code> (refresh/append/prepend states) for rendering loaders and error rows directly in the list.</p><p>A common mistake is keying <code>LazyColumn</code>'s <code>items(...)</code> block by index instead of the item's stable ID; when pages get dropped and reloaded (Paging's memory-bounded window), index-based keys cause unnecessary recomposition and can scramble item identity for animations — use <code>items(count = items.itemCount, key = items.itemKey { it.id })</code>.</p>`,
  },
  {
    id: "a19-jetpack-libs-20",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why does CameraX route image data through use cases (Preview, ImageCapture, ImageAnalysis, VideoCapture) instead of exposing the Camera2 session directly, and what's the binding lifecycle model?`,
    answerHtml: `<p>Use cases are CameraX's abstraction over the much lower-level, callback-heavy Camera2 API — each one declares what it needs (a <code>Surface</code> for preview, JPEG capture, or an analyzer frame stream) and CameraX configures a single shared Camera2 capture session that satisfies all bound use cases simultaneously, handling device-specific quirks internally.</p><p>Binding is lifecycle-aware: <code>cameraProvider.bindToLifecycle(lifecycleOwner, cameraSelector, preview, imageCapture, imageAnalysis)</code> ties the camera's open/close to a <code>LifecycleOwner</code> (e.g. the hosting Activity/Fragment), so the camera is automatically released on stop/destroy without manual <code>Camera2</code> session teardown.</p>`,
  },
  {
    id: "a19-jetpack-libs-21",
    category: "framework",
    categoryLabel: "Framework",
    question: `What's the practical limit on combining CameraX use cases, and why can't you bind Preview + ImageCapture + ImageAnalysis + VideoCapture all at once on most devices?`,
    answerHtml: `<p>CameraX enforces a documented use-case combination matrix tied to the device's hardware capability level (LEGACY, LIMITED, FULL, LEVEL_3) — most devices support at most 3 concurrent use cases bound at once (commonly Preview + ImageCapture + ImageAnalysis, or swap ImageAnalysis for VideoCapture), because each consumes a hardware-backed surface and lower-tier devices only expose so many concurrent output streams.</p><p>Calling <code>bindToLifecycle</code> with an unsupported combination throws <code>IllegalArgumentException</code> ("No supported surface combination is found") at bind time — you have to check the supported combination matrix, or query device support directly via <code>isSessionConfigSupported</code> (newer CameraX releases), rather than assuming all four just work together.</p>`,
  },
  {
    id: "a19-jetpack-libs-22",
    category: "framework",
    categoryLabel: "Framework",
    question: `How does ImageAnalysis's backpressure strategy affect a real-time ML pipeline (e.g. ML Kit barcode scanning), and why does STRATEGY_KEEP_ONLY_LATEST matter?`,
    answerHtml: `<p><code>ImageAnalysis.Builder().setBackpressureStrategy(STRATEGY_KEEP_ONLY_LATEST)</code> tells CameraX to drop frames that arrive while the analyzer is still processing the previous one, keeping only the newest — essential because ML inference (barcode/face detection) is often slower than the camera's frame rate, and without this the analyzer queue backs up, increasing latency between what's on screen and what's been analyzed.</p><p>The alternative, <code>STRATEGY_BLOCK_PRODUCER</code>, queues frames and can stall the camera pipeline; for a live AR/scanning overlay, dropping stale frames (keep-latest) keeps the feedback loop responsive even if it means some frames are never analyzed.</p>`,
  },
  {
    id: "a19-jetpack-libs-23",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why must you call ImageProxy.close() explicitly inside an ImageAnalysis.Analyzer, and what happens if you forget or do heavy work without it?`,
    answerHtml: `<p>Each frame delivered to <code>Analyzer.analyze(image: ImageProxy)</code> holds a reference to a limited internal image buffer; CameraX won't deliver the next frame until you call <code>image.close()</code>, because the buffer pool size is small and bounded.</p><p>Forgetting to close it (or doing slow synchronous work before closing) stalls the camera pipeline — frames stop arriving entirely, which looks like a frozen preview/analysis rather than an exception, making it a notoriously hard-to-diagnose bug if you don't know this contract.</p>`,
  },
  {
    id: "a19-jetpack-libs-24",
    category: "framework",
    categoryLabel: "Framework",
    question: `What does CameraX's CameraSelector actually select on devices with more than two cameras (e.g. multiple rear lenses), and how do you target a specific physical lens?`,
    answerHtml: `<p><code>CameraSelector.DEFAULT_BACK_CAMERA</code> / <code>DEFAULT_FRONT_CAMERA</code> just pick "a" back or front-facing camera by <code>LensFacing</code> — on phones with multiple rear lenses (wide, ultra-wide, telephoto), CameraX/Camera2 typically exposes the main logical multi-camera as the default, and the OS may already fuse lenses behind it.</p><p>To target a specific physical camera you build a custom <code>CameraSelector</code> with a <code>CameraFilter</code> that inspects <code>Camera2CameraInfo</code> extras (e.g. focal length, <code>CameraCharacteristics</code>) for each available camera ID, since CameraX doesn't expose a simple "ultra-wide" enum — you're filtering Camera2-level characteristics through CameraX's interop layer.</p>`,
  },
];

export const ADVANCED19_QUIZ: QuizQuestion[] = [
  {
    id: "a19q-jetpack-libs-1",
    category: "framework",
    categoryLabel: "Framework",
    question: `A nested Navigation Compose graph is used to scope a ViewModel across a 3-screen onboarding flow. Which back-stack entry should hiltViewModel() be called against to share that ViewModel across all three screens?`,
    options: [`The back stack entry of the currently composed screen`, `The back stack entry of the parent navigation graph's route`, `The back stack entry of the very first screen the user ever visited in the app`, `No back stack entry is needed; a singleton-scoped Hilt module is required instead`],
    answer: 1,
    explanationHtml: `<p>Retrieving the graph's entry via <code>navController.getBackStackEntry(graphRoute)</code> and passing it to <code>hiltViewModel(parentEntry)</code> scopes the ViewModel to the graph's lifetime, so it survives screen-to-screen navigation inside the flow and clears when the graph is popped. Scoping to the current screen's entry would create a new instance per screen instead.</p>`,
  },
  {
    id: "a19q-jetpack-libs-2",
    category: "framework",
    categoryLabel: "Framework",
    question: `A RemoteMediator's load() function for LoadType.APPEND returns MediatorResult.Success(endOfPaginationReached = true) after the very first network call, even though the API has more pages. What's the observable effect in the UI?`,
    options: [`The app crashes with an IllegalStateException`, `The list keeps loading pages indefinitely, ignoring the flag`, `The list stops loading further pages and shows as if it reached the end, even though more data exists`, `Only PREPEND loads are affected; APPEND continues normally`],
    answer: 2,
    explanationHtml: `<p><code>endOfPaginationReached = true</code> is Paging's authoritative signal that there's nothing more to load in that direction. Setting it prematurely silently truncates the list — Paging stops issuing further <code>APPEND</code> calls and the UI's <code>LoadState.NotLoading(endOfPaginationReached = true)</code> renders as if pagination is complete.</p>`,
  },
  {
    id: "a19q-jetpack-libs-3",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why is Preferences DataStore generally a poor fit once an app's settings model includes nested objects or enums with strict valid values?`,
    options: [`Preferences DataStore can't be observed as a Flow, only read synchronously`, `Preferences DataStore stores an untyped key-value map with no schema, so structure and valid values aren't enforced at compile time`, `Preferences DataStore is limited to a maximum of 10 keys per file`, `Preferences DataStore requires a Proto schema file regardless of which API you use`],
    answer: 1,
    explanationHtml: `<p>Preferences DataStore is a flat, untyped key-value store — there's no schema, so nested structure has to be hand-encoded (e.g. JSON in a string) and there's no compile-time guarantee a key holds the type or value range you expect. Proto DataStore solves this with a generated, strongly-typed schema from a <code>.proto</code> file.</p>`,
  },
  {
    id: "a19q-jetpack-libs-4",
    category: "framework",
    categoryLabel: "Framework",
    question: `An ImageAnalysis.Analyzer in a CameraX pipeline runs ML Kit barcode detection that occasionally takes 300ms per frame on a slow device, and the developer never calls image.close(). What is the most likely symptom?`,
    options: [`The app crashes immediately with OutOfMemoryError on the first frame`, `Frame delivery to the analyzer stalls because CameraX won't deliver new frames until the previous ImageProxy is closed`, `CameraX automatically closes the ImageProxy after a timeout, so there's no observable effect`, `Only the preview use case is affected; analysis continues unaffected since they use separate buffers`],
    answer: 1,
    explanationHtml: `<p>CameraX's analyzer buffer pool is bounded, and the contract requires the analyzer to call <code>ImageProxy.close()</code> when done. If you never close it, the pipeline stalls waiting for a buffer slot to free up — it looks like a frozen analysis stream rather than throwing an exception, which is what makes it a classic CameraX gotcha.</p>`,
  },
  {
    id: "a19q-jetpack-libs-5",
    category: "framework",
    categoryLabel: "Framework",
    question: `In the standard Paging 3 network+database pattern, what does the UI layer's Pager actually page from when a RemoteMediator is configured?`,
    options: [`The RemoteMediator directly, bypassing the database`, `A merged in-memory cache combining network and database results`, `The local Room-backed PagingSource; RemoteMediator only fetches from network and writes into Room behind the scenes`, `Whichever source responded fastest for that particular page`],
    answer: 2,
    explanationHtml: `<p>The <code>Pager</code> is configured with a Room-generated <code>PagingSource</code> as its primary data source. <code>RemoteMediator</code> is a separate hook Paging invokes to fetch from network and persist into Room; the UI never reads from the mediator directly — it reacts when Room's <code>PagingSource</code> invalidates after the mediator's write commits.</p>`,
  },
  {
    id: "a19q-jetpack-libs-6",
    category: "framework",
    categoryLabel: "Framework",
    question: `A bottom-navigation setup calls navigate(tabRoute) with launchSingleTop = true but without popUpTo/saveState. What problem can still occur?`,
    options: [`Nothing; launchSingleTop alone fully prevents any duplicate destinations anywhere on the stack`, `A duplicate of the destination can still be pushed if an existing instance exists deeper in the back stack rather than at the top, and each tab's scroll/UI state resets on revisit`, `The app will crash with a DuplicateDestinationException`, `launchSingleTop silently disables back navigation for that destination entirely`],
    answer: 1,
    explanationHtml: `<p><code>launchSingleTop</code> only dedupes against the top-of-stack entry. Without <code>popUpTo(startDestination) { saveState = true }</code> and <code>restoreState = true</code>, switching tabs back and forth can still create duplicate entries deeper in the stack and loses each tab's saved scroll/UI state on every revisit — the standard bottom-nav pattern needs all three together.</p>`,
  },
  {
    id: "a19q-jetpack-libs-7",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why does CameraX typically cap concurrent use-case bindings (e.g. Preview + ImageCapture + ImageAnalysis) rather than allowing Preview, ImageCapture, ImageAnalysis, and VideoCapture all bound simultaneously on most devices?`,
    options: [`CameraX artificially restricts use cases for battery-saving reasons regardless of hardware`, `Each use case requires a hardware-backed output surface, and the device's camera hardware level (LEGACY/LIMITED/FULL/LEVEL_3) caps how many concurrent streams the sensor/ISP can serve`, `VideoCapture is deprecated and can never be combined with other use cases`, `Only one use case may be bound at a time on any Android device starting with API 33`],
    answer: 1,
    explanationHtml: `<p>CameraX's use-case combination support is governed by the device's reported Camera2 hardware support level. Lower-tier devices (LEGACY/LIMITED) expose fewer concurrent output streams, so binding too many use cases at once isn't an arbitrary CameraX limit — it reflects what the underlying camera hardware/ISP can actually serve concurrently.</p>`,
  },
];
