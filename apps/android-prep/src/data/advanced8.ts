// Batch 8 — Structured concurrency (coroutineScope vs supervisorScope), Flow operators (combine, flatMapLatest, debounce, conflate), exception handling (SupervisorJob, CoroutineExceptionHandler vs try/catch), cancellation cooperation, and testing coroutines with runTest/TestDispatcher/Turbine.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED8_FLASHCARDS: Flashcard[] = [
  {
    id: "a8-coroutines-1",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `coroutineScope { } throws inside one child. What happens to the sibling children, and what propagates to the caller?`,
    answerHtml: `<p><code>coroutineScope</code> uses a regular (non-supervisor) <b>Job</b> internally, so it follows the all-or-nothing rule: if one child fails with an exception other than <code>CancellationException</code>, the scope cancels all other children, waits for them to finish cancelling, then <b>rethrows</b> that exception to the caller. The caller can catch it with a normal try/catch around the <code>coroutineScope</code> call.</p>`,
  },
  {
    id: "a8-coroutines-2",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `Why does a try/catch placed around individual async{} blocks inside a coroutineScope often fail to stop a crash from propagating?`,
    answerHtml: `<p>The exception is thrown the moment <b>any child fails</b>, not when you call <code>.await()</code>. <code>coroutineScope</code> immediately cancels its siblings and propagates upward as soon as one child throws — wrapping <code>.await()</code> in try/catch only catches it if that particular deferred already failed by the time you await it; it does not stop the scope-wide cancellation that already happened to the other children.</p>`,
  },
  {
    id: "a8-coroutines-3",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `What concrete behavioral difference does supervisorScope add over coroutineScope regarding sibling failure?`,
    answerHtml: `<p><code>supervisorScope</code> installs a <b>SupervisorJob</b> as the scope's job, so a failure in one direct child does <b>not</b> cancel sibling children or the scope itself. Each child's failure must be handled at that child (e.g. via <code>try/catch</code> around an <code>async</code> child's <code>.await()</code>, or a <code>CoroutineExceptionHandler</code> on a <code>launch</code> child) — the scope itself stays alive and keeps running the other children.</p>`,
  },
  {
    id: "a8-coroutines-4",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `If you wrap an async{} child in supervisorScope but never call .await() on it, and the child throws, what happens?`,
    answerHtml: `<p>The exception is <b>silently swallowed until you call <code>.await()</code></b> — for a plain <code>Deferred</code>, the failure is stored and only surfaces when something awaits it. If nothing ever awaits it, the exception never surfaces (though the coroutine's internal job still marks itself failed/cancelled). This is the classic 'silent async failure' trap — prefer a <code>launch</code> + <code>CoroutineExceptionHandler</code> when you don't intend to await the result.</p>`,
  },
  {
    id: "a8-coroutines-5",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `Why doesn't a CoroutineExceptionHandler installed on a supervisorScope's child catch exceptions thrown from async{} the same way it does for launch{}?`,
    answerHtml: `<p><code>CoroutineExceptionHandler</code> is only consulted for coroutines that are roots of a hierarchy and that <b>automatically propagate</b> uncaught exceptions — that's <code>launch</code>. <code>async</code> defers exception delivery to whoever calls <code>.await()</code>, so the handler is skipped entirely for <code>async</code> failures; you must catch them at the <code>await()</code> call site instead.</p>`,
  },
  {
    id: "a8-coroutines-6",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `Where in a coroutine hierarchy does a CoroutineExceptionHandler actually need to be installed to take effect?`,
    answerHtml: `<p>On a <b>root</b> coroutine — one whose Job has no parent to propagate failures to, e.g. <code>GlobalScope.launch(handler) { }</code>, or a scope built as <code>CoroutineScope(SupervisorJob() + handler)</code> with <code>scope.launch { }</code> launched directly from it. Installing the handler on a <code>launch</code> nested inside another <code>launch</code> has no effect: a non-root coroutine's failure is first handed to its parent's structured-concurrency cancellation/propagation logic, and only a handler present at the actual root of that propagation is invoked — a handler attached deeper in the tree is simply never reached.</p>`,
  },
  {
    id: "a8-coroutines-7",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `What's the structural reason ViewModel's viewModelScope uses SupervisorJob rather than a plain Job?`,
    answerHtml: `<p>A ViewModel typically launches multiple independent coroutines (e.g. separate flows for different UI sections). With a plain <code>Job</code>, one failing coroutine would cancel <code>viewModelScope</code> entirely, killing every other unrelated coroutine launched from the same ViewModel. <code>SupervisorJob</code> isolates failures per top-level child so one feature's crash doesn't take down the whole ViewModel's concurrency.</p>`,
  },
  {
    id: "a8-coroutines-8",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `Inside supervisorScope, you launch two children, A and B. Child A throws an uncaught exception with no handler installed. What ultimately happens to the app process?`,
    answerHtml: `<p>Without a <code>CoroutineExceptionHandler</code> anywhere in the hierarchy, the uncaught exception from <code>launch</code> propagates to the thread's default uncaught-exception handling — on Android this typically crashes the app (it's treated like any other uncaught exception). <code>SupervisorJob</code> only isolates the failure from sibling coroutine <b>B</b> (B keeps running); it does <b>not</b> swallow or suppress the exception itself.</p>`,
  },
  {
    id: "a8-coroutines-9",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `Why is mixing try/catch around a withContext(Dispatchers.IO) { } block usually sufficient, while wrapping a coroutineScope { launch { ... } } in the same try/catch is not?`,
    answerHtml: `<p><code>withContext</code> doesn't start new structured children that race independently — it suspends and runs sequentially in the same coroutine, so a thrown exception inside it propagates synchronously up through the normal call stack and is caught by an enclosing try/catch. A <code>launch</code> inside <code>coroutineScope</code> runs concurrently; its exception isn't delivered through the call stack at the launch site but through structured-concurrency propagation, so a try/catch wrapped only around the <code>launch{}</code> call itself never sees it — you must catch around the entire <code>coroutineScope</code> instead.</p>`,
  },
  {
    id: "a8-coroutines-10",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `What does combine(flowA, flowB) { a, b -> ... } emit, and what's the key gotcha about timing?`,
    answerHtml: `<p><code>combine</code> emits a new combined value whenever <b>either</b> upstream flow emits, using the <b>latest</b> cached value from the other flow(s). It will not emit until both flows have emitted at least once. Gotcha: if one flow is much slower (e.g. a cold network flow vs. a fast local DB flow), early combined emissions wait on it, and once it does, recombination can fire much more often than the user perceives as 'one update' — leading to redundant recomposition unless you debounce or use <code>distinctUntilChanged()</code> downstream.</p>`,
  },
  {
    id: "a8-coroutines-11",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `How does flatMapLatest differ from flatMapMerge / flatMapConcat in terms of what happens to a previously-launched inner flow?`,
    answerHtml: `<p><code>flatMapLatest</code> <b>cancels the previous inner flow's collection</b> as soon as a new value arrives from the upstream flow, then starts collecting the new inner flow — only the latest inner flow's emissions reach downstream. <code>flatMapConcat</code> fully drains each inner flow in order before starting the next (no cancellation, sequential). <code>flatMapMerge</code> collects all inner flows concurrently (bounded by <code>concurrency</code>) and interleaves their emissions — also no cancellation of earlier inner flows.</p>`,
  },
  {
    id: "a8-coroutines-12",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `In a search-as-you-type feature, why is flatMapLatest typically paired with debounce rather than used alone?`,
    answerHtml: `<p><code>debounce(300)</code> on the query <code>Flow&lt;String&gt;</code> suppresses rapid intermediate emissions, only letting a value through once the upstream is quiet for the timeout — this avoids firing a network request on every keystroke. <code>flatMapLatest</code> then ensures that if a new (debounced) query does arrive while a previous search request is still in flight, that in-flight request's collection is cancelled. Used alone, <code>flatMapLatest</code> without <code>debounce</code> would still cancel stale searches, but you'd still launch (and immediately cancel) a request per keystroke, wasting work and connections.</p>`,
  },
  {
    id: "a8-coroutines-13",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `What does conflate() actually do, and how is it different from debounce() in terms of which values it drops?`,
    answerHtml: `<p><code>conflate()</code> lets the collector skip intermediate values when it's slower than the producer: it keeps only the <b>most recent</b> value while the collector is busy processing a prior one, dropping the ones in between. It never delays emission — the producer never waits. <code>debounce</code> drops a value only if a <b>newer</b> value arrives within the timeout window, and it actively delays/waits before deciding to emit, even if the collector is fast. Conflation is collector-pressure-driven; debounce is producer-timing-driven.</p>`,
  },
  {
    id: "a8-coroutines-14",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `What's the practical difference between buffer() and conflate() when the collector is the bottleneck?`,
    answerHtml: `<p><code>buffer(capacity)</code> queues emitted values so the producer can run ahead of a slow collector <b>without dropping anything</b> — it suspends the producer once the buffer is full. Called with no arguments, its default capacity is <code>Channel.BUFFERED</code> (currently 64) with the default overflow policy <code>SUSPEND</code>, and both are tunable via the <code>capacity</code>/<code>onBufferOverflow</code> parameters. <code>conflate()</code> is effectively a buffer of capacity 1 with <code>onBufferOverflow = DROP_OLDEST</code> — it never queues a backlog; it simply replaces the pending value, guaranteeing no growth in memory/latency but losing intermediate values.</p>`,
  },
  {
    id: "a8-coroutines-15",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `Why does cooperative cancellation mean a tight CPU-bound loop like \`while (i < n) { i++ }\` inside a coroutine will NOT be cancelled even after job.cancel() is called?`,
    answerHtml: `<p>Kotlin coroutine cancellation is <b>cooperative</b> — cancelling a job only flips an internal cancelled flag and, for suspended coroutines, throws <code>CancellationException</code> at the next suspension point. A loop with no suspending calls (no <code>delay</code>, no <code>yield</code>, no I/O) never hits a cancellation checkpoint, so it runs to completion regardless of <code>cancel()</code> having been called. You must manually check <code>isActive</code> (or call <code>ensureActive()</code>/<code>yield()</code>) inside the loop to make it cancellable.</p>`,
  },
  {
    id: "a8-coroutines-16",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `What's the difference between checking isActive vs calling ensureActive() inside a busy loop for cancellation cooperation?`,
    answerHtml: `<p><code>isActive</code> is a boolean property you check yourself, requiring you to write the <code>if (!isActive) return@launch</code> / <code>break</code> logic explicitly. <code>ensureActive()</code> is a convenience function that does that check internally and <b>throws <code>CancellationException</code> immediately</b> if the job is no longer active — it's preferred in most loops because it both stops execution and ensures the cancellation propagates correctly through structured concurrency (just throwing manually with a hand-rolled exception wouldn't carry the cancellation semantics other code expects).</p>`,
  },
  {
    id: "a8-coroutines-17",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `If you catch a generic Exception (or Throwable) inside a suspend function body and accidentally swallow a CancellationException, what real-world symptom does that cause?`,
    answerHtml: `<p>Swallowing <code>CancellationException</code> breaks cooperative cancellation: the coroutine's parent thinks the job is finishing normally and may proceed as if cancellation didn't happen, while the coroutine itself keeps running past where it should have stopped (e.g. continuing a network call after the screen left, or never reporting completion to the parent's join). The fix is to either avoid catching broad exception types around suspending code, or explicitly re-throw <code>CancellationException</code> in a catch block (\`catch (e: CancellationException) { throw e }\` before a broader catch, or check \`if (e is CancellationException) throw e\`).</p>`,
  },
  {
    id: "a8-coroutines-18",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `In runTest { }, why does delay(10_000) inside the tested coroutine not actually make the test take 10 seconds?`,
    answerHtml: `<p><code>runTest</code> runs on a <code>TestScope</code> backed by a virtual-time <code>TestCoroutineScheduler</code>. Calls to <code>delay()</code> don't block real time — the scheduler auto-advances virtual time to the next pending event whenever all coroutines are suspended waiting on it, so a 10-second delay resolves essentially instantly in wall-clock test execution while the code under test still sees logically correct elapsed time via <code>currentTime</code>.</p>`,
  },
  {
    id: "a8-coroutines-19",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `What's the difference between StandardTestDispatcher and UnconfinedTestDispatcher inside runTest, and when would the choice matter for a test's assertions?`,
    answerHtml: `<p><code>StandardTestDispatcher</code> queues launched coroutines rather than running them eagerly — they don't execute until you advance the scheduler (<code>advanceUntilIdle()</code>, <code>runCurrent()</code>, or virtual-time delay resolution). <code>UnconfinedTestDispatcher</code> runs coroutines <b>eagerly on the current thread</b> up to their first suspension point, similar to <code>Dispatchers.Unconfined</code>. This matters when asserting state immediately after calling a function that launches a coroutine: with Standard you must explicitly advance before state reflects the coroutine's work; with Unconfined the initial portion may already have executed synchronously, which can hide bugs where you rely on real dispatch ordering.</p>`,
  },
  {
    id: "a8-coroutines-20",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `Why does collecting a StateFlow or SharedFlow inside a test sometimes hang runTest forever unless you launch the collector in backgroundScope?`,
    answerHtml: `<p><code>StateFlow</code>/<code>SharedFlow</code> collection never completes on its own — the hot flow stays alive indefinitely, so a <code>collect</code> call launched directly in the test body via <code>launch { flow.collect { } }</code> (using the test's own job) prevents <code>runTest</code> from ever reaching idle/completion, since that child coroutine never finishes. <code>backgroundScope</code> (provided by <code>TestScope</code>) is a scope whose children are automatically cancelled when the test body completes, so collectors launched there don't block test completion while still being observable during the test.</p>`,
  },
  {
    id: "a8-coroutines-21",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `What problem does Turbine's flow.test { } solve compared to manually collecting a Flow into a list inside a test?`,
    answerHtml: `<p>Manually collecting (e.g. <code>flow.toList()</code> or a manual <code>collect</code> with a mutable list) requires the flow to complete or you to manually launch/cancel a job, and gives no built-in assertions about <b>emission order</b>, missed events, or leftover unconsumed events. Turbine's <code>test { }</code> turns the flow into a sequential <code>awaitItem()</code>/<code>awaitComplete()</code>/<code>awaitError()</code> API with a turbine (channel) under the hood, and — critically — it <b>fails the test</b> if there are unconsumed emissions left when the block ends, catching the common bug of asserting fewer emissions than a flow actually produced.</p>`,
  },
  {
    id: "a8-coroutines-22",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `Why does runTest fail a test by default if a coroutine launched inside it throws an uncaught exception, even though the launching scope might be a SupervisorJob in production code?`,
    answerHtml: `<p><code>runTest</code> installs its own structured-concurrency root and treats any uncaught exception from a child coroutine (that isn't otherwise handled) as a <b>test failure</b> by rethrowing it after the test body completes — this is intentional so bugs in background coroutines aren't silently lost. If the production code under test uses a real <code>SupervisorJob</code> with its own <code>CoroutineExceptionHandler</code>, that handler still runs first; only exceptions that aren't actually handled there propagate to fail the test, so a properly handled exception in app code is consistent with both behaviors.</p>`,
  },
  {
    id: "a8-coroutines-23",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `What's the risk of calling Dispatchers.setMain(StandardTestDispatcher()) without a matching Dispatchers.resetMain() in a test's teardown?`,
    answerHtml: `<p><code>Dispatchers.setMain</code> replaces the global <code>Main</code> dispatcher for the whole test JVM process, not just the current test class instance. If you don't reset it in an <code>@After</code>/teardown, the swapped-in <code>TestDispatcher</code> leaks into <b>subsequent test classes</b> running in the same JVM/process, which can cause unrelated tests to unexpectedly use virtual time or fail with 'Dispatchers.Main is not initialized' style errors once the leaked dispatcher's scheduler is in an inconsistent state.</p>`,
  },
  {
    id: "a8-coroutines-24",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `How does supervisorScope interact with a child that itself launches a coroutineScope internally — does the supervisor behavior propagate down into that nested scope?`,
    answerHtml: `<p>No — supervision is not inherited transitively through nested scopes. A <code>coroutineScope</code> launched <i>inside</i> a child of a <code>supervisorScope</code> creates its own regular <code>Job</code>, so failures among <b>its</b> children still cancel each other and propagate up to that inner scope's caller normally. Only the direct children of the actual <code>SupervisorJob</code>-backed scope get failure isolation; once you cross into a nested <code>coroutineScope</code>, standard all-or-nothing propagation resumes for that subtree.</p>`,
  },
];

export const ADVANCED8_QUIZ: QuizQuestion[] = [
  {
    id: "a8q-coroutines-1",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `Inside a supervisorScope, you launch two children with launch{}: child A throws an exception after 100ms, child B is still running a long-lived collection. No CoroutineExceptionHandler is installed anywhere in the hierarchy. What happens?`,
    options: [`Child B is cancelled and the exception is rethrown from supervisorScope`, `Child A's exception is silently discarded; child B keeps running unaffected`, `Child B keeps running, but A's uncaught exception still propagates to the thread's default handler (likely crashing the app)`, `supervisorScope automatically retries child A once before giving up`],
    answer: 2,
    explanationHtml: `<p><code>SupervisorJob</code> only isolates sibling cancellation — B is unaffected and keeps running. It does not swallow the exception: without a <code>CoroutineExceptionHandler</code> anywhere in the chain, A's uncaught exception from <code>launch</code> still propagates to the default uncaught-exception handling, which crashes the app on Android. People often mistakenly believe supervisorScope makes failures silent — it only prevents cross-cancellation.</p>`,
  },
  {
    id: "a8q-coroutines-2",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `Why does wrapping only the async{} call (not the subsequent .await()) in a try/catch fail to catch an exception thrown inside that async block?`,
    options: [`async exceptions are thrown immediately when async{} is called, not when await() runs`, `async never throws exceptions at all, only launch does`, `The exception is captured by the Deferred and only re-thrown when .await() is called, so the try/catch around the async{} call site never sees it`, `try/catch cannot be used with coroutine builders under any circumstances`],
    answer: 2,
    explanationHtml: `<p><code>async</code> defers exception delivery: the failure is stored in the resulting <code>Deferred</code> and only re-thrown at the point you call <code>.await()</code>. A try/catch that wraps the <code>async { }</code> call but not the <code>.await()</code> call will not catch it — you must wrap the <code>.await()</code> call (or the whole block containing both).</p>`,
  },
  {
    id: "a8q-coroutines-3",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `Given \`flowOf(1,2,3).onEach { delay(100) }.conflate().collect { value -> delay(300); println(value) }\`, roughly which values print, assuming the producer emits faster than the collector consumes?`,
    options: [`1, 2, 3 — conflate never drops values, only delays them`, `Only 1 — conflate stops the flow after the first conflated value`, `1, then 3 (2 is likely dropped because a newer value replaced it while the collector was still processing 1)`, `3, 2, 1 — conflate reverses emission order to prioritize the latest value first`],
    answer: 2,
    explanationHtml: `<p><code>conflate()</code> lets the producer keep emitting without waiting for the slow collector, but only keeps the most recent pending value. While the collector is busy with <code>1</code> (300ms), the producer emits <code>2</code> then <code>3</code> in the background (100ms apart); since <code>3</code> overwrites <code>2</code> before the collector becomes free, the collector typically only sees <code>1</code> and <code>3</code> — <code>2</code> is conflated away.</p>`,
  },
  {
    id: "a8q-coroutines-4",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `What's the most accurate description of why flatMapLatest is preferred over flatMapMerge for a 'load details when selected item changes' use case?`,
    options: [`flatMapLatest is more performant in every scenario regardless of use case`, `flatMapLatest cancels the in-flight load for the previous selection when a new selection arrives, preventing a stale (out-of-order) response from overwriting fresher data`, `flatMapMerge cannot handle suspend functions inside its transform lambda`, `flatMapLatest guarantees results arrive in the same order they were requested, while flatMapMerge does not`],
    answer: 1,
    explanationHtml: `<p>The core risk in this use case is a stale, slow response for an old selection arriving after a newer selection's faster response, overwriting fresh UI state with outdated data. <code>flatMapLatest</code> actively cancels the previous inner flow's collection as soon as a new upstream value arrives, eliminating that race. <code>flatMapMerge</code> would let both loads run concurrently and interleave results, risking exactly that stale-overwrite bug.</p>`,
  },
  {
    id: "a8q-coroutines-5",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `In a runTest block using the default StandardTestDispatcher, why might \`val job = launch { repository.observe().collect { results.add(it) } }\` followed immediately by an assertion on \`results\` fail, even though the flow should have emitted by then?`,
    options: [`StandardTestDispatcher queues the launched coroutine rather than running it immediately; you need advanceUntilIdle() or runCurrent() before the assertion to let it execute`, `Flow.collect cannot be called inside runTest at all`, `results.add(it) is not thread-safe inside coroutine tests`, `launch{} requires an explicit Dispatchers.IO argument to run inside runTest`],
    answer: 0,
    explanationHtml: `<p><code>StandardTestDispatcher</code> does not eagerly execute newly launched coroutines — they're scheduled but won't run until the test scheduler is advanced (<code>runCurrent()</code> to run currently-ready tasks, or <code>advanceUntilIdle()</code> to drain everything including virtual-time delays). Asserting immediately after <code>launch</code> without advancing checks state before the collector has had a chance to run.</p>`,
  },
  {
    id: "a8q-coroutines-6",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `A function catches exceptions like this: \`try { doSuspendWork() } catch (e: Exception) { Log.e(...) }\`. What's the specific danger of this pattern inside a coroutine that can be cancelled (e.g. tied to a ViewModel's viewModelScope)?`,
    options: [`Exception is a sealed class in Kotlin so this code won't compile`, `Log.e() is itself a suspend function and will throw a compile error here`, `It accidentally catches and swallows CancellationException (a subtype of Exception), which breaks cooperative cancellation by letting the coroutine appear to continue normally instead of stopping`, `catch (e: Exception) only catches checked exceptions, so this code has no effect on coroutines`],
    answer: 2,
    explanationHtml: `<p><code>CancellationException</code> is a subtype of <code>Exception</code> in Kotlin, so a broad <code>catch (e: Exception)</code> intercepts it too. Logging it and continuing instead of rethrowing breaks the cooperative cancellation contract — the coroutine doesn't actually stop, and the parent's structured-concurrency bookkeeping is left inconsistent. The fix is to rethrow <code>CancellationException</code> explicitly or catch a narrower exception type.</p>`,
  },
  {
    id: "a8q-coroutines-7",
    category: "coroutines",
    categoryLabel: "Coroutines",
    question: `Which statement correctly distinguishes debounce(300) from conflate() on a Flow<String> representing a search text field's changes?`,
    options: [`Both behave identically; they're interchangeable operator aliases`, `conflate() delays each emission by a fixed 300ms window before deciding whether to forward it; debounce() does not`, `debounce() only drops a pending value if a newer one arrives within the timeout window (time-based, producer-driven wait); conflate() drops intermediate values purely based on whether the collector is still busy (collector-pressure-driven, no waiting)`, `conflate() requires specifying a timeout parameter just like debounce(), since both are time-windowed operators`],
    answer: 2,
    explanationHtml: `<p><code>debounce</code> is time-based: it waits out a quiet window before emitting the latest value, dropping anything superseded within that window. <code>conflate</code> has no timing concept at all — it simply lets a fast producer keep emitting while a slow collector is busy, holding onto only the most recent value until the collector is ready, with zero added delay on the producer side.</p>`,
  },
];
