// Batch 16 — Android Keystore, BiometricPrompt, EncryptedSharedPreferences/DataStore, network security config & certificate pinning, and ProGuard/R8 obfuscation vs security.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED16_FLASHCARDS: Flashcard[] = [
  {
    id: "a16-security-1",
    category: "security",
    categoryLabel: "Security",
    question: `Why is a key generated in the Android Keystore considered non-exfiltratable, and what's the practical limit of that guarantee?`,
    answerHtml: `<p>Keys generated with <code>AndroidKeystore</code> as the provider never enter app process memory in extractable form &mdash; private/symmetric key material is generated and held inside a secure boundary (TEE, or a discrete <b>StrongBox</b> chip if requested), and all crypto operations are performed by that hardware on the app's behalf via a handle/alias.</p><p>The limit: this protects against <i>extraction</i>, not <i>misuse</i>. If the device is unlocked and the app's process is compromised, an attacker can still ask the Keystore to sign or decrypt arbitrary data using the key &mdash; they just can't copy the key bytes off the device.</p>`,
  },
  {
    id: "a16-security-2",
    category: "security",
    categoryLabel: "Security",
    question: `What's the difference between TEE-backed and StrongBox-backed Keystore keys, and when would you explicitly request StrongBox?`,
    answerHtml: `<p>TEE-backed keys run in a trusted execution environment (e.g. ARM TrustZone) that shares the main SoC but is isolated from the Android OS. <b>StrongBox</b> (<code>setIsStrongBoxBacked(true)</code> on <code>KeyGenParameterSpec</code>) requires a physically separate, certified secure element with its own CPU, RAM, and tamper resistance.</p><p>Request StrongBox for high-value keys where you need protection against physical/hardware-level attacks (side-channel, fault injection), not just OS-level compromise. Tradeoffs: not all devices support it &mdash; check <code>PackageManager.FEATURE_STRONGBOX_KEYSTORE</code> and fall back &mdash; and it's measurably slower (symmetric ops have been measured tens of times slower than TEE) with a narrower supported algorithm set (e.g. RSA capped at 2048-bit, AES 128/256, EC limited to P-256).</p>`,
  },
  {
    id: "a16-security-3",
    category: "security",
    categoryLabel: "Security",
    question: `A KeyGenParameterSpec sets setUserAuthenticationRequired(true) with setUserAuthenticationValidityDurationSeconds(-1). What does -1 mean, and why is it the safer choice for biometric-gated crypto keys?`,
    answerHtml: `<p><code>-1</code> means the key requires authentication <b>per operation</b> and is bound to the specific <code>BiometricPrompt.CryptoObject</code> presented at auth time, rather than unlocking the key for a fixed time window (e.g. <code>30</code> seconds of free use after one successful auth).</p><p>With a positive duration, any code path in the app &mdash; including injected/compromised code &mdash; can use the key during that window without re-prompting the user. With <code>-1</code>, the key literally cannot be initialized for a <code>Cipher</code>/<code>Signature</code> operation until the matching biometric prompt succeeds immediately before it, closing that replay window.</p>`,
  },
  {
    id: "a16-security-4",
    category: "security",
    categoryLabel: "Security",
    question: `What's the security difference between setUserAuthenticationParameters(timeout, AUTH_BIOMETRIC_STRONG) and using AUTH_DEVICE_CREDENTIAL as the allowed authenticator type for a Keystore key?`,
    answerHtml: `<p><code>AUTH_BIOMETRIC_STRONG</code> restricts unlocking the key to Class 3 (strong) biometrics &mdash; fingerprint or face implementations that meet Android's spoof/imposter acceptance thresholds and run in an isolated environment. <code>AUTH_DEVICE_CREDENTIAL</code> allows the key to be unlocked with the PIN/pattern/password fallback instead.</p><p>Combining both as a bitwise OR widens the attack surface to whichever is weaker (a shoulder-surfed PIN). For a key protecting something like a payment credential, restricting to <code>AUTH_BIOMETRIC_STRONG</code> alone &mdash; and handling the "no biometric enrolled" case explicitly in app logic &mdash; is the more defensible choice than silently OR-ing in the device credential as a convenience fallback.</p>`,
  },
  {
    id: "a16-security-5",
    category: "security",
    categoryLabel: "Security",
    question: `Why can't setInvalidatedByBiometricEnrollment(false) be treated as a security no-op, and what's the legitimate use case for disabling it?`,
    answerHtml: `<p>By default, Keystore keys with biometric auth are invalidated the moment a new fingerprint or face is enrolled &mdash; this prevents an attacker who adds their own biometric to an unlocked device from gaining standing access to existing keys. Calling <code>setInvalidatedByBiometricEnrollment(false)</code> turns that protection off, so the key survives new enrollments.</p><p>The legitimate case is narrow: app-level data (like an offline cache key) where losing access on re-enrollment would be a worse UX than the marginal security loss, and where the data isn't itself sensitive enough to justify forcing re-auth. For anything like payment or auth tokens, leave the default (<code>true</code>) so enrollment changes invalidate the key.</p>`,
  },
  {
    id: "a16-security-6",
    category: "security",
    categoryLabel: "Security",
    question: `Why does BiometricPrompt's CryptoObject matter — what attack does it close that a plain "show biometric dialog, then check a boolean result" approach doesn't?`,
    answerHtml: `<p>A boolean-only flow ("did the user pass biometric auth: yes/no") is just a UI gate &mdash; the actual sensitive operation (decrypting a token, signing a request) happens afterward in app code, with no cryptographic tie to that specific auth event. A hooked/rooted device can patch the boolean check or call the protected code path directly, skipping the prompt entirely.</p><p>Passing a <code>CryptoObject</code> wrapping a Keystore-backed <code>Cipher</code>/<code>Signature</code>/<code>Mac</code> means the OS itself only authorizes that <i>specific cryptographic object</i> to operate after the biometric succeeds, enforced inside the TEE. There's no app-level boolean to bypass &mdash; the crypto operation fails at the hardware layer without a valid auth event.</p>`,
  },
  {
    id: "a16-security-7",
    category: "security",
    categoryLabel: "Security",
    question: `What's the practical difference between BIOMETRIC_STRONG and BIOMETRIC_WEAK in BiometricManager.Authenticators, and why can't BIOMETRIC_WEAK ever unlock a CryptoObject?`,
    answerHtml: `<p>Strong (Class 3) and Weak (Class 2) are defined by Android's spoof acceptance rate (SAR) and imposter acceptance rate (IAR) testing thresholds, plus whether the sensor pipeline runs in an isolated execution environment. Many lower-end face-unlock implementations only qualify as Class 2.</p><p>Class 2 biometrics are considered too easily spoofed to gate cryptographic material, so the platform restricts <code>CryptoObject</code>-based auth (key unlock) to <code>BIOMETRIC_STRONG</code> only &mdash; in fact, passing a <code>CryptoObject</code> into <code>BiometricPrompt.authenticate()</code> implicitly forces <code>BIOMETRIC_STRONG</code>, and combining it with <code>setAllowedAuthenticators(BIOMETRIC_WEAK)</code> will crash. <code>BIOMETRIC_WEAK</code> can still authenticate a user for app-level convenience (e.g. unlocking a UI screen), just not for unlocking a key.</p>`,
  },
  {
    id: "a16-security-8",
    category: "security",
    categoryLabel: "Security",
    question: `Your BiometricPrompt.authenticate() call throws ERROR_NEGATIVE_BUTTON vs the system reporting a lockout (ERROR_LOCKOUT / ERROR_LOCKOUT_PERMANENT). How should each be handled differently, and why does conflating them create a security gap?`,
    answerHtml: `<p><code>ERROR_NEGATIVE_BUTTON</code> is a user choice (tapped "Cancel" or "Use password") and should route to your designated fallback flow. <code>ERROR_LOCKOUT</code> (temporary, after repeated failures) and <code>ERROR_LOCKOUT_PERMANENT</code> (requires device credential to reset) are the platform's brute-force defense kicking in.</p><p>The gap: if an app treats all errors the same and silently falls back to a weaker in-app auth path (e.g. a 4-digit app PIN with no rate limiting), it defeats the OS's lockout protection &mdash; an attacker who triggers <code>ERROR_LOCKOUT_PERMANENT</code> by guessing fingerprints just routes around it to a softer target the app itself created.</p>`,
  },
  {
    id: "a16-security-9",
    category: "security",
    categoryLabel: "Security",
    question: `EncryptedSharedPreferences is now deprecated. What's the actual security/reliability weakness that motivated moving away from it, beyond "it's just old", and what's the current recommended replacement?`,
    answerHtml: `<p><code>EncryptedSharedPreferences</code> (the <code>androidx.security.crypto</code> Tink-based wrapper, deprecated since security-crypto 1.1.0-alpha07) inherits the legacy <code>SharedPreferences</code> XML-file storage model: every read/write rewrites the entire file synchronously on the main thread by default, encryption is applied per-key/value rather than as a structured, app-managed scheme, and the library has had known issues recovering from corrupted master keys (e.g. after a Keystore wipe on some OEM skins) with no graceful migration path &mdash; a corrupted master key can render all stored prefs permanently unreadable.</p><p>Google's current guidance is to move to <b>Jetpack DataStore</b> (Proto or Preferences, which is coroutine/Flow-based and does off-main-thread I/O) and layer encryption on top yourself using <code>Cipher</code>/Tink with an Android Keystore key directly &mdash; giving you control over corruption handling, async I/O, and migration that the old wrapper didn't.</p>`,
  },
  {
    id: "a16-security-10",
    category: "security",
    categoryLabel: "Security",
    question: `DataStore (Preferences or Proto) has no built-in encryption. Walk through how you'd correctly layer AES-GCM encryption on top of it using an Android Keystore key.`,
    answerHtml: `<p>Generate or retrieve an AES-256 key from <code>AndroidKeystore</code> via <code>KeyGenParameterSpec</code> with <code>BLOCK_MODE_GCM</code> and <code>ENCRYPTION_PADDING_NONE</code>. On write: serialize the value, run it through a GCM <code>Cipher</code> in encrypt mode, and persist both the ciphertext <i>and</i> the IV (GCM needs a unique IV per encryption; a 12-byte random IV is standard) &mdash; e.g. as a single byte array storing <code>IV || ciphertext || authTag</code>.</p><p>On read: split the stored bytes back into IV and ciphertext, initialize the <code>Cipher</code> in decrypt mode with that IV, and decrypt &mdash; GCM's built-in authentication tag will throw if the data was tampered with. Wrap this as a custom DataStore <code>Serializer&lt;T&gt;</code> so the encrypt/decrypt happens transparently at the serialization boundary, keeping call sites ignorant of the crypto.</p>`,
  },
  {
    id: "a16-security-11",
    category: "security",
    categoryLabel: "Security",
    question: `Why is reusing the same IV across multiple AES-GCM encryptions with the same key a critical vulnerability, not just a style issue?`,
    answerHtml: `<p>GCM is a stream-cipher-based mode (CTR internally) combined with a polynomial MAC (GHASH). Reusing a (key, IV) pair means the keystream XORed with two different plaintexts is identical, so XORing the two ciphertexts cancels the keystream and leaks the XOR of the two plaintexts &mdash; often enough to recover both with known-plaintext techniques. Worse, IV reuse with GCM specifically also leaks the authentication subkey, letting an attacker forge valid ciphertexts/tags for that key going forward.</p><p>This is why a fresh random 12-byte IV per encryption (and storing it alongside the ciphertext, since IVs aren't secret) is mandatory, never a fixed or counter-reset-on-reinstall IV.</p>`,
  },
  {
    id: "a16-security-12",
    category: "security",
    categoryLabel: "Security",
    question: `What does android:networkSecurityConfig actually control, and what's the single most important default it changed starting with apps targeting Android 9 (API 28)?`,
    answerHtml: `<p>It's a declarative XML resource (<code>res/xml/network_security_config.xml</code>, referenced from the manifest's <code>&lt;application&gt;</code> tag) that lets you configure TLS trust anchors, certificate pinning, and cleartext traffic policy per-domain or globally, without writing a custom <code>TrustManager</code>.</p><p>Starting with apps that target API 28+, <code>cleartextTrafficPermitted</code> defaults to <code>false</code> &mdash; plain HTTP is blocked by default at the platform networking layer (affecting <code>HttpURLConnection</code> and most HTTP clients including OkHttp when they respect the platform's <code>NetworkSecurityPolicy</code>), forcing apps to explicitly opt in per-domain if they truly need cleartext (e.g. for local dev servers).</p>`,
  },
  {
    id: "a16-security-13",
    category: "security",
    categoryLabel: "Security",
    question: `In network_security_config.xml, what's the security difference between a <trust-anchors> entry that includes system vs. user, and why is including "user" dangerous in a production release config?`,
    answerHtml: `<p><code>&lt;certificates src="system"/&gt;</code> trusts only CAs preloaded by the OS/OEM. <code>&lt;certificates src="user"/&gt;</code> additionally trusts CAs the user (or anything with device admin/profile-owner access) has manually installed via Settings.</p><p>Including <code>user</code> in a release build means any locally installed proxy CA &mdash; the kind a user installs for a MITM debugging proxy like Charles or Burp, or that malware/a malicious MDM profile could install &mdash; is silently trusted by your app's TLS stack. The recommended pattern is a debug-only override config (<code>&lt;debug-overrides&gt;</code>) that adds <code>user</code> trust, scoped to debug builds only (it only applies when <code>android:debuggable</code> is true), so QA can intercept traffic but the shipped release config stays system-only.</p>`,
  },
  {
    id: "a16-security-14",
    category: "security",
    categoryLabel: "Security",
    question: `Compare SPKI hash pinning via network_security_config.xml's <pin-set> against pinning the full certificate. Why is SPKI (Subject Public Key Info) hash pinning the standard recommendation?`,
    answerHtml: `<p>Pinning the full leaf certificate breaks the moment that certificate is renewed (new serial, new validity dates, same key) &mdash; a near-certain operational outage on every cert rotation, since renewals happen on a schedule independent of app releases. <b>SPKI pin</b> (<code>&lt;pin digest="SHA-256"&gt;</code> of the public key, base64-encoded) survives certificate renewal as long as the CA reissues using the <i>same key pair</i>, and survives even a full reissue if you generate the new cert from a key you pre-pinned as a backup pin.</p><p>Best practice: pin at least two SPKI hashes &mdash; the current key and a backup/rotation key held offline &mdash; and set a reasonable <code>expiration</code> date on the <code>&lt;pin-set&gt;</code> so a forgotten pin doesn't permanently brick the app if rotation is mishandled.</p>`,
  },
  {
    id: "a16-security-15",
    category: "security",
    categoryLabel: "Security",
    question: `Your team adds certificate pinning. Three weeks later, a CDN-fronted API rotates its TLS cert behind a managed cert provider (e.g. auto-renewing via the CDN), and the app breaks for all users. What went wrong in the pinning strategy, and what should have been pinned instead?`,
    answerHtml: `<p>Pinning to the leaf cert (or even its SPKI) is fragile when the cert is issued by infrastructure you don't directly control (CDN/managed TLS) and rotates on a schedule outside your release cadence &mdash; the pin set in a shipped APK can't be updated without a new release (or a remote-config-driven pin set, which adds its own trust problem).</p><p>The safer strategy here is pinning to a stable intermediate or the CDN provider's root CA's SPKI (one level up the chain) rather than the leaf, since intermediates rotate far less often, combined with a backup pin and a server-side circuit breaker (e.g. ship pin updates via remote config with a long pin <code>expiration</code> as a safety valve) so a rotation doesn't require an emergency app release.</p>`,
  },
  {
    id: "a16-security-16",
    category: "security",
    categoryLabel: "Security",
    question: `Why does certificate pinning not protect against on-device attacks the way it protects against network MITM, and what's a concrete bypass an interviewer might expect you to name?`,
    answerHtml: `<p>Pinning validates the chain returned during the TLS handshake against a known-good key, which defeats network-level interception (rogue Wi-Fi, malicious CA, network-level MITM proxy). It does nothing once the attacker controls the device itself: a rooted device with <b>Frida</b> or <b>Xposed</b> can hook the app's <code>TrustManager</code>/<code>OkHttpClient.CertificatePinner</code> at runtime (the well-known <code>frida-android-unpinning</code>-style scripts hook <code>TrustManagerImpl.verifyChain</code> or <code>CertificatePinner.Builder.add</code>) and force pin checks to always pass, then route traffic through a proxy.</p><p>This is why pinning is one layer, not a complete defense &mdash; pairing it with root/tamper detection (e.g. via Play Integrity API) and treating pinning bypass as an accepted risk on a compromised device, rather than promising it as unbreakable, is the technically honest framing.</p>`,
  },
  {
    id: "a16-security-17",
    category: "security",
    categoryLabel: "Security",
    question: `What's the actual mechanism by which R8 (in default/full mode) shrinks and obfuscates code, and why is "R8 obfuscation" insufficient as a standalone anti-reverse-engineering control?`,
    answerHtml: `<p>R8 (the default compiler for shrinking/obfuscating/optimizing since AGP 8.0, running in full mode by default) performs whole-program static analysis to (1) shrink &mdash; remove unreachable classes/methods/fields via reachability analysis from declared entry points (manifest components, keep rules), (2) optimize &mdash; inline, merge classes, prune dead branches, and (3) obfuscate &mdash; rename classes/methods/fields to short, meaningless identifiers (<code>a</code>, <code>b</code>, ...) while preserving a mapping file for stack-trace de-obfuscation.</p><p>It's insufficient alone because renaming symbols doesn't encrypt logic or strings &mdash; control flow, algorithms, and any string literal not explicitly handled remain fully readable in a decompiler (jadx, etc.); R8 is a deterrent against casual inspection and a size/perf optimization, not a defense against a motivated reverse engineer.</p>`,
  },
  {
    id: "a16-security-18",
    category: "security",
    categoryLabel: "Security",
    question: `A proguard-rules.pro file has a broad keep rule like -keep class com.app.models.** { *; }. What's the security cost of overly broad keep rules, beyond just APK size?`,
    answerHtml: `<p>Every <code>-keep</code> rule exempts matched classes/members from both shrinking <i>and</i> obfuscation (unless split via <code>-keep,allowobfuscation</code>, which preserves reachability/keeps the member from being removed but still allows renaming). A broad wildcard keep on a whole package leaves that package's class names, method names, and field names &mdash; often including business logic, API models with revealing field names, or internal endpoints &mdash; fully readable in the shipped APK, directly undermining the obfuscation you added R8 for in the first place.</p><p>The fix is scoping keep rules as narrowly as possible (e.g. only the specific classes/members reflection or a serialization library actually needs, like <code>@Keep</code>-annotated DTOs or types referenced via reflection) and preferring <code>-keep,allowobfuscation</code> + <code>-keepclassmembers</code> patterns that retain reachability without retaining readable names.</p>`,
  },
  {
    id: "a16-security-19",
    category: "security",
    categoryLabel: "Security",
    question: `Why can't ProGuard/R8 obfuscation be relied on to protect a hardcoded API key or secret embedded in app code, even with full obfuscation enabled?`,
    answerHtml: `<p>Obfuscation renames <i>identifiers</i> (classes, methods, fields) &mdash; it does not transform string literal <i>values</i>. A hardcoded API key, even sitting inside a renamed class with a renamed field, is still present as a plain string constant in the compiled DEX, trivially extractable with <code>strings</code>, a decompiler, or even just <code>apktool</code> baksmali output. R8 does not encrypt or obscure string constants by default.</p><p>The correct fix is architectural: don't ship long-lived secrets in the client at all &mdash; proxy the call through a backend that holds the secret, or for things that must live on-device, derive/fetch at runtime and store in Keystore-backed encrypted storage rather than as a source-level constant.</p>`,
  },
  {
    id: "a16-security-20",
    category: "security",
    categoryLabel: "Security",
    question: `What's the relationship between R8 minification settings and crash reporting (e.g. Crashlytics/Sentry) in production, and what operational risk does it create if mishandled?`,
    answerHtml: `<p>R8's obfuscation renames symbols deterministically per build and emits a <code>mapping.txt</code> file that maps obfuscated names back to original ones. Crash-reporting SDKs need that exact mapping file uploaded (matched by build ID/version) to de-obfuscate stack traces; without it, production crash reports show meaningless symbol soup (<code>a.b.c.onCreate</code>) instead of actionable traces.</p><p>The operational risk: the mapping file is also exactly what an attacker would want to reverse your obfuscation &mdash; so it must be treated as a build artifact that's archived securely (e.g. Play Console's automatic deobfuscation file upload, or a private artifact store) and never bundled into the APK or committed to a public repo, or it defeats the obfuscation entirely.</p>`,
  },
  {
    id: "a16-security-21",
    category: "security",
    categoryLabel: "Security",
    question: `Does enabling R8 full-mode obfuscation have any effect on runtime security against an attacker using Frida for dynamic instrumentation? Why or why not?`,
    answerHtml: `<p>Effectively none on its own. Frida hooks are typically attached by class/method <i>signature</i> discovered at runtime via introspection (e.g. enumerating loaded classes, or hooking by offset/pattern), not by reading your original source names ahead of time &mdash; an attacker just runs <code>Java.enumerateLoadedClasses()</code> or inspects the obfuscated DEX live on the rooted device, where obfuscated names are just as enumerable as original ones.</p><p>Obfuscation raises the cost of static analysis (reading the decompiled source cold) but does nothing against dynamic instrumentation on a rooted/emulated device. Defending against that requires root/emulator detection, integrity checks (signature verification, Play Integrity API), and anti-debugging/anti-Frida techniques layered separately &mdash; and even those are an arms race, not an absolute barrier.</p>`,
  },
  {
    id: "a16-security-22",
    category: "security",
    categoryLabel: "Security",
    question: `How does the Play Integrity API differ from older SafetyNet Attestation, and what specific verdict types does it give an app for deciding whether to trust the runtime environment?`,
    answerHtml: `<p>SafetyNet (deprecated since 2022, fully shut down January 2025) gave a coarse <code>basicIntegrity</code> / <code>ctsProfileMatch</code> pair. <b>Play Integrity API</b> replaces it with three structured verdict categories returned in a signed token: <code>appIntegrity</code> (is this the genuine, unmodified app package recognized by Play, vs. sideloaded/repackaged), <code>deviceIntegrity</code> (does the device pass Google's compatibility/CTS checks and show no signs of rooting/tampering, with optional tiered labels like <code>MEETS_BASIC_INTEGRITY</code> vs <code>MEETS_DEVICE_INTEGRITY</code> vs <code>MEETS_STRONG_INTEGRITY</code> for recency of security patches), and <code>accountDetails</code> (is the Play Store account in good standing, licensed).</p><p>The token is verified server-side (Google's servers sign it), which matters: client-side-only integrity checks can be patched out by the same attacker the check is meant to catch, so the verdict should gate server-side behavior (e.g. refuse to issue a session token) rather than just an in-app UI warning.</p>`,
  },
  {
    id: "a16-security-23",
    category: "security",
    categoryLabel: "Security",
    question: `When storing a refresh token or session credential on-device, what's wrong with just calling EncryptedSharedPreferences/an encrypted DataStore "secure enough" without also considering auth-binding on the underlying key?`,
    answerHtml: `<p>Encryption at rest (whether via <code>EncryptedSharedPreferences</code> or a hand-rolled Keystore+DataStore scheme) protects the data from <i>offline</i> extraction &mdash; e.g. pulling the file off a rooted device or from a backup. But if the underlying AES key has no <code>setUserAuthenticationRequired</code> binding, any code running in the app's process (including injected/malicious code on a compromised device, or another app exploiting an IPC vulnerability) can call the Keystore to decrypt the data at will, with the device merely unlocked.</p><p>For high-value tokens, binding the wrapping key to biometric/device-credential auth (even with a short validity window for UX) adds a second factor that pure storage-at-rest encryption doesn't provide &mdash; though it shifts UX cost onto every read, which is why it's typically reserved for the most sensitive operations rather than every stored value.</p>`,
  },
  {
    id: "a16-security-24",
    category: "security",
    categoryLabel: "Security",
    question: `What does "key attestation" via Android Keystore prove to a remote server, and why is it a stronger trust signal than just checking a device integrity verdict alone?`,
    answerHtml: `<p>Calling key generation with an attestation challenge (<code>setAttestationChallenge(serverNonce)</code> on the <code>KeyGenParameterSpec</code>/<code>KeyPairGenerator</code> builder) returns a certificate chain rooted at a Google-controlled key, where the leaf certificate cryptographically asserts properties of the key itself: that it was generated inside secure hardware (TEE/StrongBox), its exact auth-binding parameters (e.g. <code>userAuthenticationRequired</code>), and that it has never left that hardware. The server verifies the chain against Google's published root certificates and checks the nonce to confirm freshness (anti-replay).</p><p>This is stronger than a general device-integrity verdict because it ties trust to a <i>specific key</i> with cryptographic proof of its generation parameters, rather than a point-in-time environment snapshot &mdash; useful for scenarios like binding a server-side session to hardware-backed proof of possession, where you want assurance the private key genuinely can't be exported even if the OS is later compromised.</p>`,
  },
];

export const ADVANCED16_QUIZ: QuizQuestion[] = [
  {
    id: "a16q-security-1",
    category: "security",
    categoryLabel: "Security",
    question: `An AES key is generated via KeyGenParameterSpec with setUserAuthenticationRequired(true) and setUserAuthenticationValidityDurationSeconds(30). What's the main security tradeoff of this configuration versus using -1 (per-operation auth bound to a CryptoObject)?`,
    options: [`There is no difference; both require biometric auth for every Cipher operation`, `30 seconds creates a window where any code path in the app can use the key without re-prompting the user, since the key is unlocked for that duration rather than bound to one authorized operation`, `Using a positive duration is strictly more secure because it reduces how often the user is prompted`, `Positive durations are rejected by the Keystore unless StrongBox is used`],
    answer: 1,
    explanationHtml: `<p>A positive validity duration unlocks the key for that whole window after one successful auth &mdash; any code (including a compromised path) can use it freely until it expires. <code>-1</code> requires a fresh auth tied to the specific <code>CryptoObject</code> for every operation, closing that replay window at the cost of prompting more often.</p>`,
  },
  {
    id: "a16q-security-2",
    category: "security",
    categoryLabel: "Security",
    question: `Why is BiometricPrompt's CryptoObject considered a stronger security boundary than checking a boolean "authentication succeeded" callback before manually decrypting data?`,
    options: [`CryptoObject is faster because it skips the TEE entirely`, `The Keystore cryptographically refuses to perform the operation without a valid matching auth event from the TEE, so there's no app-level boolean check to bypass on a compromised device`, `CryptoObject automatically encrypts data with a hardcoded key regardless of biometric result`, `Boolean callbacks are deprecated and no longer function on API 30+`],
    answer: 1,
    explanationHtml: `<p>With a boolean check, the actual crypto operation is a separate app-level call that hooking/patching can reach directly. With <code>CryptoObject</code>, the OS/TEE enforces that the specific cipher instance cannot operate without the matching auth event &mdash; there's no boolean gate to skip.</p>`,
  },
  {
    id: "a16q-security-3",
    category: "security",
    categoryLabel: "Security",
    question: `A network_security_config.xml in a release build includes <certificates src="user"/> in its trust-anchors. What's the concrete risk this introduces?`,
    options: [`It has no effect since user certs are ignored on API 28+`, `It only affects debug builds automatically, so there's no release risk`, `It causes all HTTPS connections to silently downgrade to HTTP`, `Any CA certificate the user (or malware/a malicious profile) has installed locally is trusted by the app's TLS stack, enabling on-device MITM that survives into production`],
    answer: 3,
    explanationHtml: `<p>Including <code>user</code> trust anchors in a shipped release means a manually installed proxy/malicious CA is trusted by the app. The recommended pattern is to scope <code>user</code> trust to a debug-only <code>&lt;debug-overrides&gt;</code> block.</p>`,
  },
  {
    id: "a16q-security-4",
    category: "security",
    categoryLabel: "Security",
    question: `Why do most certificate pinning implementations pin the SHA-256 hash of the Subject Public Key Info (SPKI) rather than the full leaf certificate?`,
    options: [`SPKI hashes are required by the Play Store for app review`, `SPKI pinning is faster to compute at TLS handshake time`, `SPKI pinning survives certificate renewal as long as the same key pair is reused, avoiding breakage on every cert rotation that full-cert pinning would cause`, `SPKI pinning works without HTTPS, reducing latency`],
    answer: 2,
    explanationHtml: `<p>A full-certificate pin breaks on every renewal (new serial/dates). An SPKI hash pin only changes when the underlying key pair changes, which is far less frequent &mdash; making it the practical default, usually paired with a backup pin for planned key rotation.</p>`,
  },
  {
    id: "a16q-security-5",
    category: "security",
    categoryLabel: "Security",
    question: `What does R8's obfuscation step actually rename, and what does it leave completely untouched that still poses a risk for embedded secrets?`,
    options: [`It renames classes, methods, and fields; it does not transform string literal values, so hardcoded secrets remain plainly readable in the compiled output`, `It encrypts all string literals by default in release builds`, `It renames only public API surface classes, leaving private members untouched`, `It strips all string constants from the DEX file automatically`],
    answer: 0,
    explanationHtml: `<p>R8 obfuscation only renames identifiers. String literals, including hardcoded API keys, remain as plain constants in the DEX and are trivially recoverable with basic decompilation/string-extraction tools regardless of obfuscation settings.</p>`,
  },
  {
    id: "a16q-security-6",
    category: "security",
    categoryLabel: "Security",
    question: `A backend wants to verify that a request genuinely came from an unmodified, Play-distributed copy of the app running on a non-rooted device, with the verdict resistant to client-side tampering. Which approach best satisfies this?`,
    options: [`Check a client-side root-detection library result and trust it if it returns false`, `Rely solely on certificate pinning to assume the request is legitimate`, `Request a Play Integrity API verdict token and verify its signature and claims server-side, rather than trusting any in-app integrity flag`, `Use ProGuard/R8 obfuscation as the integrity signal, since obfuscated apps are harder to repackage`],
    answer: 2,
    explanationHtml: `<p>Client-side checks (root detection libraries, obfuscation) can be patched out by the same attacker they're meant to catch. Play Integrity API issues a Google-signed token asserting app/device/account integrity that must be verified server-side, which is resistant to on-device tampering in a way purely local checks are not.</p>`,
  },
];
