// Plan de estudio de entrevistas Android en español. Los módulos tipados son la fuente de verdad — editar directamente.
export type StudySection = { id: string; num: string; title: string; html: string };

export const STUDY_INTRO_HTML = "<span class=\"lbl\">Profundidad sobre trivialidades</span> Las entrevistas senior y staff de Android profundizan en cómo <i>razonas</i> — por qué <b>StateFlow</b> sobre LiveData, cuándo la recomposición realmente te cuesta, cómo sobrevives la <b>muerte del proceso</b>, y dónde las coroutines filtran. Cada sección a continuación empareja el concepto con una nota de <b>en la práctica</b> para que puedas responder desde una decisión de ingeniería real, no una definición.";

export const STUDY_SECTIONS: StudySection[] = [
  {
    "id": "st-1",
    "num": "01",
    "title": "01 · Fundamentos del lenguaje Kotlin",
    "html": "<p><b>Lo que buscan:</b> Kotlin idiomático, de producción — no Java escrito en sintaxis de Kotlin. Fluidez con <code>val</code> vs <code>var</code>, inmutabilidad por defecto, <b>seguridad de nulos</b> (<code>?.</code>, <code>?:</code>, <code>!!</code> como mala práctica), data classes, jerarquías <code>sealed</code> para modelar estado, y las <b>funciones de ámbito</b> (<code>let</code>, <code>run</code>, <code>with</code>, <code>apply</code>, <code>also</code>).</p>\n      <ul>\n        <li>Prefiere <code>val</code> y colecciones inmutables; expón <code>List</code>, almacena <code>MutableList</code> privadamente.</li>\n        <li>Modela estado exhaustivo con <code>sealed interface</code> + <code>when</code> (sin rama <code>else</code> → el compilador impone completitud).</li>\n        <li><b>Delegación</b>: <code>by lazy</code>, <code>by viewModels()</code>, delegados de propiedades, y delegación de clases con <code>by</code>.</li>\n        <li><code>inline</code> + <code>reified</code> para conservar info de tipo genérico en tiempo de ejecución; entiende el costo (tamaño de código) de inlining.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> Una respuesta senior vincula seguridad de nulos con tasa de crash: el sistema de tipos empuja la anulabilidad al límite (parsing, tipos de plataforma de Java) para que el resto de la app sea no nulo por construcción. <code>!!</code> en una revisión es una señal de alarma — convierte una garantía en tiempo de compilación en un crash en tiempo de ejecución.</div>"
  },
  {
    "id": "st-2",
    "num": "02",
    "title": "02 · Coroutines y concurrencia estructurada",
    "html": "<p><b>Lo que buscan:</b> coroutines profundas — no solo <code>launch { }</code>. La concurrencia estructurada significa que cada coroutine tiene un <code>CoroutineScope</code> padre; cancelar el scope cancela sus hijos, y un fallo hijo cancela hermanos (a menos que un <code>SupervisorJob</code> los aísle).</p>\n      <ul>\n        <li>Las funciones <b>suspend</b> deben ser <i>seguras para el principal</i>: pueden llamarse en el hilo principal e internamente cambiar con <code>withContext(Dispatchers.IO)</code>.</li>\n        <li>Dispatchers: <code>Main</code> (UI), <code>IO</code> (I/O bloqueante, pool grande), <code>Default</code> (trabajo de CPU), <code>Unconfined</code> (rara vez).</li>\n        <li>La cancelación es cooperativa: verifica <code>isActive</code> / llama <code>ensureActive()</code>, y nunca tragues <code>CancellationException</code>.</li>\n        <li><code>coroutineScope</code> vs <code>supervisorScope</code>; <code>async</code>/<code>await</code> para descomposición paralela.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">Fija esto</span> <code>viewModelScope</code> y <code>lifecycleScope</code> son scopes estructurados conectados a un ciclo de vida — se cancelan automáticamente, lo cual es <i>por qué</i> casi nunca necesitas <code>GlobalScope</code> (que filtra porque nada lo cancela).</div>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> El encuadre senior: &quot;Dejo que el scope sea dueño de la cancelación.&quot; Una llamada de red iniciada en <code>viewModelScope</code> se cancela cuando el ViewModel se limpia, así que un cambio de configuración o retroceso no filtra una solicitud ni actualiza una UI muerta.</div>"
  },
  {
    "id": "st-3",
    "num": "03",
    "title": "03 · Flow, StateFlow y SharedFlow",
    "html": "<p><b>Lo que buscan:</b> streams reactivos bien hechos. <code>Flow</code> es un stream asíncrono frío; los operadores (<code>map</code>, <code>filter</code>, <code>combine</code>, <code>flatMapLatest</code>) se ejecutan en el contexto del collector a menos que uses <code>flowOn</code>.</p>\n      <table>\n        <tr><th></th><th>StateFlow</th><th>SharedFlow</th></tr>\n        <tr><td>Mantiene valor</td><td>Siempre (requiere inicial)</td><td>Replay opcional</td></tr>\n        <tr><td>Conflación</td><td>Conflado, distinto-hasta-que-cambie</td><td>Buffer configurable</td></tr>\n        <tr><td>Usar para</td><td><b>Estado</b> observable de UI</td><td><b>Eventos</b> únicos (navegar, toast)</td></tr>\n      </table>\n      <ul>\n        <li>Recolecta con <code>repeatOnLifecycle(STARTED)</code> (o <code>collectAsStateWithLifecycle()</code> en Compose) para que la recolección se detenga en segundo plano — no <code>launchWhenStarted</code> (obsoleto, mantiene el upstream caliente).</li>\n        <li><code>stateIn(scope, SharingStarted.WhileSubscribed(5000), initial)</code> comparte un flow frío como estado y detiene el upstream 5s después del último suscriptor — sobrevive rotación sin re-obtener.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> El error clásico: recolectar un flow en <code>onCreate</code> con un <code>launch</code> simple mantiene el collector vivo en segundo plano, desperdiciando trabajo y arriesgando actualizaciones de UI obsoletas. <code>repeatOnLifecycle</code> es la solución que los entrevistadores quieren escuchar.</div>"
  },
  {
    "id": "st-4",
    "num": "04",
    "title": "04 · Jetpack Compose — composición y recomposición",
    "html": "<p><b>Lo que buscan:</b> el modelo mental. Un <code>@Composable</code> es una función que <i>describe</i> UI a partir del estado; Compose construye un árbol y <b>recomposa</b> (reinvoca) solo las funciones cuyas entradas cambiaron. No hay jerarquía de vistas que mutar — cambias el estado y el framework reconcilia.</p>\n      <ul>\n        <li><b>Ámbito de recomposición</b>: el composable contenedor más pequeño que lee un estado dado. Leer estado bajo en el árbol mantiene la recomposición local.</li>\n        <li><b>Estabilidad</b>: Compose omite un composable si sus parámetros son <code>@Stable</code>/<code>@Immutable</code> e inmutables. Tipos inestables (ej. <code>List</code> de una fuente no estable) impiden el skip → usa <code>ImmutableList</code> / <code>kotlinx.collections.immutable</code>.</li>\n        <li>Fases: <b>Composición → Layout → Dibujo</b>. Difiere lecturas a la fase más tardía (ej. lambda <code>Modifier.offset { }</code>) para omitir recomposición en scroll/animación.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">Concepto nuevo</span> <b>Skip de agujero de dona</b>: un padre puede recomponer sin recomponer hijos estables. Internaliza 'qué lee este estado' y puedes explicar jank con precisión en lugar de memoizar a ciegas.</div>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> Activa las <b>métricas del compilador de Compose</b> / contadores de recomposición del Layout Inspector. Un senior depura una lista que se recomposición de más encontrando el parámetro inestable, no esparciendo <code>remember</code>.</div>"
  },
  {
    "id": "st-5",
    "num": "05",
    "title": "05 · Estado y efectos secundarios en Compose",
    "html": "<p><b>Lo que buscan:</b> state hoisting y las APIs de efectos secundarios. <code>remember</code> cachea entre recomposiciones; <code>rememberSaveable</code> sobrevive cambio de configuración/muerte del proceso. <b>Eleva el estado</b> hacia arriba (composables sin estado toman <code>value</code> + <code>onValueChange</code>) para que la UI sea reutilizable y testeable.</p>\n      <ul>\n        <li><code>LaunchedEffect(key)</code> — ejecuta un bloque suspend vinculado a la composición; se reinicia cuando <code>key</code> cambia, se cancela al salir.</li>\n        <li><code>rememberCoroutineScope()</code> — lanza desde callbacks (ej. clic en botón), con scope de la composición.</li>\n        <li><code>DisposableEffect</code> — registra/desregistra (listeners, observers) con un <code>onDispose</code>.</li>\n        <li><code>derivedStateOf</code> — calcula desde otro estado y solo recomposa cuando el <i>resultado</i> cambia (ej. <code>showButton = scrollOffset &gt; 0</code>).</li>\n        <li><code>produceState</code> — conecta una fuente asíncrona no-Compose a un <code>State</code>; <code>snapshotFlow</code> — convierte estado de Compose en un Flow.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> La trampa de la entrevista: lanzar una coroutine directamente en el cuerpo del composable en lugar de <code>LaunchedEffect</code> — se dispara en cada recomposición. Decir &quot;los efectos pertenecen a manejadores de efectos con key correcta&quot; señala madurez real de Compose.</div>"
  },
  {
    "id": "st-6",
    "num": "06",
    "title": "06 · Ciclo de vida de Activity / Fragment y cambios de configuración",
    "html": "<p><b>Lo que buscan:</b> el ciclo de vida al dedillo. Activity: <code>onCreate → onStart → onResume</code> (primer plano) → <code>onPause → onStop → onDestroy</code>. Un <b>cambio de configuración</b> (rotación, modo oscuro, idioma, escala de fuente) destruye y recrea la Activity por defecto.</p>\n      <ul>\n        <li>El ciclo de vida de la vista del Fragment es separado del fragment: usa <code>viewLifecycleOwner</code> para observadores, o filtras cuando la vista se destruye pero el fragment no.</li>\n        <li>No pelees contra cambios de configuración con <code>android:configChanges</code> a menos que realmente controles el redibujado — prefiere sobrevivir estado en un ViewModel.</li>\n        <li><code>onSaveInstanceState(Bundle)</code> persiste estado pequeño de UI a través de recreación y muerte del proceso (parcelable, &lt; ~1MB).</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> Respuesta senior: &quot;La rotación no es especial — es la misma ruta de recreación que la muerte del proceso con más estado retenido. Si mi estado sobrevive muerte del proceso vía SavedStateHandle, la rotación es gratis.&quot;</div>"
  },
  {
    "id": "st-7",
    "num": "07",
    "title": "07 · ViewModel, SavedStateHandle y muerte del proceso",
    "html": "<p><b>Lo que buscan:</b> dónde vive el estado y cómo sobrevive. Un <code>ViewModel</code> sobrevive cambios de configuración (con scope al ViewModelStoreOwner) pero se limpia cuando el propietario realmente termina. <b>No</b> sobrevive <b>muerte del proceso</b> por sí solo — el OS puede matar una app en segundo plano para recuperar memoria.</p>\n      <ul>\n        <li><code>SavedStateHandle</code> es el puente: un mapa de valores respaldado por estado de instancia guardado, así que los valores sobreviven muerte del proceso. Inyectalo en el ViewModel y expón estado vía <code>getStateFlow(key, default)</code>.</li>\n        <li>Nunca mantengas una referencia <code>Context</code>/View/Activity en un ViewModel (filtra). Usa el contexto de aplicación de <code>AndroidViewModel</code> solo si es inevitable.</li>\n        <li>Prueba la ruta de restauración: mata el proceso desde herramientas de desarrollo (&quot;No conservar actividades&quot;) y confirma que la pantalla se reconstruye correctamente.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">Fija esto</span> El ViewModel sobrevive <b>rotación</b>; SavedStateHandle sobrevive <b>muerte del proceso</b>; una base de datos/DataStore sobrevive <b>todo</b>. Nombrar los tres niveles es la señal senior.</div>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> El fallo realista: una pantalla de búsqueda pierde la consulta después de que el OS mate la app en segundo plano. Pon la consulta en SavedStateHandle y se restaura — la solución que los entrevistadores están escuchando.</div>"
  },
  {
    "id": "st-8",
    "num": "08",
    "title": "08 · Arquitectura — MVVM, MVI y Clean Architecture",
    "html": "<p><b>Lo que buscan:</b> una arquitectura defendible con <b>flujo de datos unidireccional</b>. MVVM: el ViewModel expone estado observable; la Vista lo renderiza y envía eventos hacia arriba. MVI condensa esto en un único <code>UiState</code> inmutable + un stream de <code>Intent</code>/<code>Action</code> reducido a nuevo estado — genial para pantallas complejas y con muchos experimentos.</p>\n      <ul>\n        <li>Capas de <b>Clean Architecture</b>: <b>UI</b> → <b>Dominio</b> (casos de uso, puro Kotlin, sin Android) → <b>Datos</b> (repositorios, fuentes). Las dependencias apuntan hacia adentro; el dominio no sabe nada de Retrofit o Room.</li>\n        <li>Fuente única de verdad: el repository decide caché vs red; la UI nunca llama a la API directamente.</li>\n        <li>Modela el estado como un solo objeto: <code>data class UiState(val items: List, val loading: Boolean, val error: String?)</code> — renderiza todo, evita booleanos dispersos.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> El pragmatismo gana: &quot;Uso MVVM con un estado inmutable único por pantalla y casos de uso solo donde la lógica de dominio no es trivial — no añado un caso de uso que solo reenvía una llamada a un repo.&quot; Esa sutileza se lee como juicio de staff, no dogma.</div>"
  },
  {
    "id": "st-9",
    "num": "09",
    "title": "09 · Inyección de dependencias — Hilt y Dagger",
    "html": "<p><b>Lo que buscan:</b> DI para mantener el código testeable y desacoplado. <b>Hilt</b> se apoya en Dagger y genera componentes vinculados a ciclos de vida de Android: <code>@HiltAndroidApp</code>, <code>@AndroidEntryPoint</code>, <code>@HiltViewModel</code>, y scopes (<code>@Singleton</code>, <code>@ActivityRetainedScoped</code>, <code>@ViewModelScoped</code>).</p>\n      <ul>\n        <li><code>@Module</code> + <code>@Provides</code> para cosas que no posees (Retrofit, OkHttp); <code>@Binds</code> para interfaz→impl.</li>\n        <li>Inyección por constructor (<code>@Inject constructor</code>) donde puedas — es la más testeable.</li>\n        <li>Qualifiers (<code>@Named</code> / custom) desambigúan múltiples bindings del mismo tipo (ej. dos clientes OkHttp).</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">Fija esto</span> El punto de la DI no es &quot;los objetos se crean por mí&quot; — es la <b>inversión de dependencias</b>: el código de alto nivel depende de interfaces, así que los tests intercambian un repository falso en una línea. Enmarca la DI como una herramienta de testeabilidad y modularización.</div>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> El grafo en tiempo de compilación de Hilt significa que un binding faltante es un error de compilación, no un crash en tiempo de ejecución — una ventaja real que debes nombrar sobre patrones de service-locator.</div>"
  },
  {
    "id": "st-10",
    "num": "10",
    "title": "10 · Persistencia — Room y DataStore",
    "html": "<p><b>Lo que buscan:</b> almacenamiento local elegido correctamente. <b>Room</b> es el ORM de SQLite: <code>@Entity</code>, <code>@Dao</code>, <code>@Query</code>, con verificación SQL en tiempo de compilación y tipos de retorno <code>Flow</code> para lecturas reactivas. <b>DataStore</b> reemplaza SharedPreferences: Preferences DataStore (clave-valor) o Proto DataStore (tipado), ambos asíncronos y transaccionales.</p>\n      <ul>\n        <li>Los DAOs de Room que retornan <code>Flow&lt;List&lt;T&gt;&gt;</code> emiten en cada cambio — la columna vertebral de offline-first.</li>\n        <li>Migraciones: proporciona un <code>Migration</code> o arriesga un crash en cambio de esquema; exporta el esquema para tests.</li>\n        <li>Nunca hagas disco I/O en el hilo principal — los DAOs <code>suspend</code> de Room y DataStore son seguros para el principal por diseño.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> &quot;SharedPreferences es síncrono y silenciosamente hace I/O en el hilo principal (<code>apply()</code> difiere la escritura a disco pero el commit en memoria aún bloquea). DataStore corrige eso con un Flow + coroutines.&quot; Ese contraste es una pregunta senior frecuente.</div>"
  },
  {
    "id": "st-11",
    "num": "11",
    "title": "11 · Red — Retrofit, OkHttp y serialización",
    "html": "<p><b>Lo que buscan:</b> una capa de red robusta. <b>Retrofit</b> define una interfaz de API tipada; <b>OkHttp</b> es el motor con el pool de conexiones, caché e <b>interceptores</b>. Serializa con <b>Moshi</b> o <b>kotlinx.serialization</b>.</p>\n      <ul>\n        <li>Interceptores: uno de <i>aplicación</i> para headers de auth/logging; uno de <i>red</i> para control de caché. Añade un <code>Authenticator</code> de refresh de token para 401s.</li>\n        <li>Modela resultados con un tipo <code>sealed</code> <code>Result</code>/<code>Either</code> — nunca dejes que excepciones crudas lleguen a la UI.</li>\n        <li>Mapea DTOs → modelos de dominio en el repository; los DTOs nunca se filtran a Compose.</li>\n        <li>Caché HTTP de OkHttp + ETags para GETs condicionales; reintenta solo llamadas idempotentes con backoff.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> Offline-first: el repository lee de Room (fuente de verdad) y refresca desde la red, escribiendo de vuelta a la DB para que la UI actualice vía su Flow — el usuario nunca mira un spinner, con semántica stale-while-revalidate.</div>"
  },
  {
    "id": "st-12",
    "num": "12",
    "title": "12 · Navegación y deep linking",
    "html": "<p><b>Núcleo:</b> <b>Navigation-Compose</b> modela la app como un grafo de destinos con rutas tipadas (rutas type-safe vía objetos de ruta <code>@Serializable</code> en Navigation 2.8+). El <code>NavController</code> posee el back stack; pasas argumentos y haces pop con resultados.</p>\n      <ul>\n        <li>Eleva el <code>NavController</code> en la raíz del grafo; pasa lambdas (<code>onNavigateToDetail</code>) a pantallas para que los composables se mantengan desacoplados de navegación y testeables.</li>\n        <li><b>Deep links</b>: declara <code>&lt;intent-filter&gt;</code> + <code>android:autoVerify</code> para App Links (https verificado) y un <code>navDeepLink</code> que mapea la URI a un destino.</li>\n        <li>Gráficos anidados para módulos de features; <code>SavedStateHandle</code> recibe args de navegación dentro del ViewModel.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> El gating de auth es una decisión de grafo, no una pantalla oculta: un usuario no autenticado recibe el grafo de auth como destino de inicio. App Links necesitan el <code>assetlinks.json</code> en tu dominio — un paso que la gente olvida.</div>"
  },
  {
    "id": "st-13",
    "num": "13",
    "title": "13 · Trabajo en segundo plano — WorkManager, Services y broadcasts",
    "html": "<p><b>Lo que buscan:</b> la herramienta correcta para trabajo fuera de pantalla. <b>WorkManager</b> es el predeterminado para trabajo <i>diferido, garantizado</i> (sincronización, subida) — sobrevive muerte del proceso y reinicio, respeta restricciones (red, carga), y retrocede en fallo.</p>\n      <ul>\n        <li><code>OneTimeWorkRequest</code> vs <code>PeriodicWorkRequest</code> (intervalo mínimo de 15 min); encadena con <code>beginWith().then()</code>; trabajo único para deduplicar.</li>\n        <li><b>Services de primer plano</b> para trabajo visible continuo por el usuario (reproducción, navegación) — requieren una notificación y, en Android 14+, un <code>foregroundServiceType</code> declarado.</li>\n        <li>Límites modernos de segundo plano: <code>JobScheduler</code>/WorkManager sobre wake-locks implícitos; <code>BroadcastReceiver</code> para eventos del sistema, pero la mayoría de broadcasts implícitos están restringidos.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> &quot;No uses un Service para una sincronización de red puntual — eso es WorkManager. Usa un service de primer plano solo cuando el usuario pueda verlo.&quot; Conocer el límite (y Doze/App Standby) es la señal senior.</div>"
  },
  {
    "id": "st-14",
    "num": "14",
    "title": "14 · Rendimiento — jank, timing de frames y baseline profiles",
    "html": "<p><b>Núcleo:</b> la métrica es <b>frames renderizados dentro del presupuesto de 16.6ms</b> (60fps; 8.3ms a 120Hz). Jank = un frame perdido, surfaced por <b>JankStats</b>, la librería <b>Macrobenchmark</b>, y trazas de Perfetto.</p>\n      <ul>\n        <li><b>Baseline Profiles</b>: distribuye pistas de compilación AOT para rutas calientes para que las primeras ejecuciones no se interpreten — victorias mayores en arranque en frío y scroll, generados vía Macrobenchmark.</li>\n        <li>Compose: evita parámetros inestables, difiere lecturas de estado a layout/draw, usa keys de <code>LazyColumn</code>, y <code>remember</code> computaciones costosas.</li>\n        <li>Disciplina del hilo principal: sin I/O, sin parseo JSON grande, sin decodificación de bitmap en el hilo de UI.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">Concepto nuevo</span> Mide en un <b>build de producción</b> con R8 habilitado — los builds de debug son engañosamente lentos y Compose no está completamente optimizado. Macrobenchmark maneja una instalación real.</div>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> &quot;Perfilo con Perfetto/Macrobenchmark, arreglo el frame caliente demostrado, luego añado un baseline profile y un benchmark para protegerlo.&quot; Perfilar primero vence a adivinar — la línea que los entrevistadores quieren.</div>"
  },
  {
    "id": "st-15",
    "num": "15",
    "title": "15 · Memory leaks, ANRs y profiling",
    "html": "<p><b>Núcleo:</b> dos modos de fallo. Los <b>leaks</b> retienen objetos que el GC debería liberar — el leak clásico de Android es un <code>Context</code>/View retenido más allá de su ciclo de vida (refs estáticos, inner classes, callbacks, un Handler con runnables retrasados). Los <b>ANRs</b> se disparan cuando el hilo principal está bloqueado &gt; 5s (despacho de entrada) — casi siempre I/O en el hilo principal o contención de locks.</p>\n      <ul>\n        <li><b>LeakCanary</b> en debug detecta automáticamente instancias retenidas y vuelca la cadena de referencias — nómbralo como tu primera herramienta.</li>\n        <li><b>Memory Profiler</b> de Android Studio + heap dumps para crecimiento acumulativo; trazas de <b>ANR</b> en Play Console / <code>/data/anr/traces.txt</code>.</li>\n        <li>Leak de coroutine común: lanzar en <code>GlobalScope</code> o recolectar un flow sin conciencia del ciclo de vida.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> Los leaks se presentan como jank lento y acumulativo y crashes OOM después de sesiones largas — los cazas con el tiempo con LeakCanary, no desde un solo stack trace. Los ANR los arreglas sacando trabajo del hilo principal.</div>"
  },
  {
    "id": "st-16",
    "num": "16",
    "title": "16 · Arranque de app y R8 / ProGuard",
    "html": "<p><b>Núcleo:</b> arranque en frío = fork del proceso → <code>Application.onCreate</code> → primer frame. El presupuesto se gasta en inicialización ansiosa, grafos de dependencias grandes, y trabajo en el hilo principal.</p>\n      <ul>\n        <li>Usa la librería <b>App Startup</b> para consolidar y ordenar inicializadores (e inicializar lazy los que no se necesitan al lanzamiento).</li>\n        <li><b>R8</b> reduce, optimiza, y ofusca: eliminación de código muerto/recursos corta tamaño y mejora carga; las reglas de keep (<code>-keep</code>) protegen reflexión (Gson, serialización).</li>\n        <li>Difiere trabajo no crítico fuera de la ruta de lanzamiento; muestra contenido rápido y hidrata el resto.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> &quot;Mido el arranque en frío en un build de producción con Macrobenchmark, recorto <code>Application.onCreate</code>, añado un baseline profile, y mantengo R8 habilitado.&quot; Tratar el arranque como un presupuesto defendido es una señal senior.</div>"
  },
  {
    "id": "st-17",
    "num": "17",
    "title": "17 · Testing — JUnit, MockK, Turbine y tests de UI Compose",
    "html": "<p><b>Lo que buscan:</b> una pirámide de testing real. Muchos tests <b>unitarios</b> rápidos (JUnit + <b>MockK</b>) sobre ViewModels, casos de uso y mappers; menos tests de <b>integración</b>; algunos <b>UI/E2E</b> (Compose test o Espresso) en flujos críticos.</p>\n      <ul>\n        <li>Coroutines/Flow: <code>runTest</code> + un <code>TestDispatcher</code> (inyecta el dispatcher, nunca hardcode <code>Dispatchers.IO</code>); aserta emisiones con <b>Turbine</b>.</li>\n        <li><b>Fakes vs mocks</b>: prefiere un repository fake escrito a mano para tests basados en estado; usa mocks para verificación de interacciones. El exceso de mocking acopla tests a la implementación.</li>\n        <li><b>Robolectric</b> ejecuta el framework de Android en la JVM (rápido, sin dispositivo); el <b>Compose UI test</b> usa semántica (<code>onNodeWithText</code>, <code>assertIsDisplayed</code>).</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> El patrón de dispatcher inyectable es la señal senior: <code>class Repo(private val io: CoroutineDispatcher)</code> para que los tests pasen <code>StandardTestDispatcher()</code> y controlen el tiempo virtual. Los dispatchers hardcodeados hacen los tests inestables.</div>"
  },
  {
    "id": "st-18",
    "num": "18",
    "title": "18 · Gradle — variantes, catálogos de versiones y modularización",
    "html": "<p><b>Lo que buscan:</b> puedes razonar sobre la compilación. <b>Variantes de build</b> = tipos de build (debug/release) × product flavors (free/paid, staging/prod). Los <b>catálogos de versiones</b> (<code>libs.versions.toml</code>) centralizan versiones de dependencias.</p>\n      <ul>\n        <li><b>KSP vs kapt</b>: KSP es el procesador de anotaciones nativo de Kotlin — mucho más rápido que kapt (que genera stubs de Java). Room/Hilt/Moshi soportan KSP; migra de kapt para velocidad de build.</li>\n        <li><b>Modularización</b>: divide por feature (<code>:feature:home</code>) y capa (<code>:core:data</code>) para compilaciones paralelas, compilación incremental más rápida, y límites forzados.</li>\n        <li>Convention plugins (<code>build-logic</code>) eliminan duplicación en configuración de módulos; <code>api</code> vs <code>implementation</code> controla qué se filtra transitivamente.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> &quot;La modularización compra compilaciones paralelas + incrementales y imposición arquitectónica — un módulo <code>:feature</code> físicamente no puede importar los internos de otra feature.&quot; Nombrar velocidad de build y límites es la respuesta de staff.</div>"
  },
  {
    "id": "st-19",
    "num": "19",
    "title": "19 · Seguridad — Keystore, cifrado e integridad",
    "html": "<p><b>Núcleo:</b> el APK se distribuye al dispositivo y puede ser decompilado — <b>nunca hardcodees secretos</b>. Almacena llaves en el <b>Android Keystore</b> (respaldado por hardware donde esté disponible); cifra datos locales con Jetpack Security (<code>EncryptedSharedPreferences</code> / <code>EncryptedFile</code>) o SQLCipher para Room.</p>\n      <ul>\n        <li>Tokens: acceso de vida corta + refresh; puerta a acciones sensibles detrás de <b>BiometricPrompt</b>.</li>\n        <li>Red: <b>Network Security Config</b> + <b>certificate pinning</b> (cuidado con rotación), solo HTTPS, sin texto plano.</li>\n        <li><b>Play Integrity API</b> certifica una app/dispositivo genuina; ofuscación (R8) y detección de root son defensa en profundidad, no balas de plata.</li>\n        <li>Conoce el vocabulario <b>OWASP MASVS / Mobile Top 10</b>: almacenamiento inseguro, cripto débil, etc.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> &quot;Analiza el modelo de amenazas en voz alta&quot;: secretos-en-bundle, datos en reposo, datos en tránsito, e integridad del dispositivo son cuatro categorías distintas. Nombrarlas muestra madurez de seguridad.</div>"
  },
  {
    "id": "st-20",
    "num": "20",
    "title": "20 · Accesibilidad y la frontera de AI en el dispositivo",
    "html": "<p><b>Núcleo:</b> distribuye para <b>TalkBack</b>. En Compose, establece <code>Modifier.semantics { contentDescription = ... }</code>, marca encabezados, agrupa nodos relacionados, respeta tamaño de objetivo táctil (48dp), Dynamic Type (unidades sp), y contraste (WCAG AA).</p>\n      <ul>\n        <li><code>Modifier.clickable(onClickLabel=...)</code>, <code>stateDescription</code>, y merge de semántica para controles compuestos.</li>\n        <li>Prueba con Accessibility Scanner y aserciones de semántica de Compose.</li>\n      </ul>\n      <p><b>Frontera:</b> AI generativa en el dispositivo vía <b>Gemini Nano</b> (AICore / APIs ML Kit GenAI) y la API <b>MediaPipe LLM Inference</b>, más <b>TensorFlow Lite</b> (LiteRT) clásico para visión/audio. La compensación: privacidad (los datos se mantienen locales), offline, sin costo de servidor — contra tamaño del modelo, RAM y fragmentación de dispositivos.</p>\n      <div class=\"map\"><span class=\"lbl\">En la práctica</span> &quot;Gemini Nano está limitado a dispositivos capaces (Pixel 8+/banderas vía AICore); detecto capacidades y recurro a un modelo de servidor de lo contrario.&quot; Razonar sobre la compensación de privacidad/latencia/costo y soporte de dispositivos es la respuesta de staff.</div>"
  }
];
