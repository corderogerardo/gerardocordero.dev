// Batch 18 — Sealed classes/interfaces for exhaustive when, inline/reified generics, delegation (by lazy, by Delegates.observable, interface delegation), and scope function (let/run/apply/also/with) tradeoffs.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED18_FLASHCARDS: Flashcard[] = [
  {
    id: "a18-kotlin-advanced-1",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Why does a \`when\` over a sealed class become exhaustive without an \`else\` branch, and what breaks that guarantee?`,
    answerHtml: `<p>The compiler can enumerate every subtype of a sealed class/interface because they must all be declared in the same module (and, for nested hierarchies, the same package since Kotlin 1.5). With every branch covered, <code>when</code> used as an expression type-checks without <code>else</code>, and the compiler errors if a new subtype is added but not handled.</p><p>That guarantee breaks the moment you add an <code>else</code> branch &quot;just in case&quot; — it silently swallows new subtypes instead of forcing a compile error, defeating the whole point of sealing the hierarchy.</p>`,
  },
  {
    id: "a18-kotlin-advanced-2",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `A sealed interface and a sealed class can both model a closed hierarchy. What's the practical reason to pick \`sealed interface\` over \`sealed class\`?`,
    answerHtml: `<p>A class can only extend one superclass, so a subtype already extending some other base class can't also extend a <code>sealed class</code>. A <code>sealed interface</code> doesn't consume the single-inheritance slot, so a type can implement it alongside extending an unrelated class or implementing other interfaces. Sealed interfaces are also the natural fit when subtypes share no common state, only a common contract.</p>`,
  },
  {
    id: "a18-kotlin-advanced-3",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Since Kotlin 1.7, sealed class subtypes no longer need to live in the same file. What's the actual constraint, and what changed in 1.5 vs 1.7?`,
    answerHtml: `<p>Kotlin 1.5 relaxed the &quot;same file&quot; rule to &quot;same package and same compilation module/source set&quot;. Kotlin 1.7 (stable) further relaxed this: a sealed class's direct inheritors can live in a <i>different module</i>, as long as that module is compiled together with (depends on) the module declaring the sealed class — no special compiler flag is needed, it Just Works for normal multi-module Gradle builds. The hard constraint that never goes away: the compiler must be able to see every subtype at compile time, so subtypes can't be defined outside the modules the compiler analyzes for that hierarchy (no external/runtime extension, no subclassing from an unrelated downstream module).</p>`,
  },
  {
    id: "a18-kotlin-advanced-4",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `How does pattern matching with \`is\` inside a sealed \`when\` branch interact with smart casting, and where does smart casting fail?`,
    answerHtml: `<p>Inside a branch like <code>is Loading -> ...</code>, the compiler smart-casts the sealed type to <code>Loading</code> for that branch's scope because the variable is a stable <code>val</code> (or stable property) the compiler can prove isn't reassigned. Smart casting fails for <code>var</code> properties, properties with a custom <code>get()</code>, or properties from a different module/class the compiler can't verify aren't mutated concurrently — there you need an explicit local <code>val</code> capture or an explicit cast.</p>`,
  },
  {
    id: "a18-kotlin-advanced-5",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `What does \`inline\` actually do to a higher-order function at compile time, and what's the main cost it eliminates?`,
    answerHtml: `<p>The compiler copies the function's bytecode directly into every call site, substituting the lambda body in place of invoking it. This eliminates the <b>lambda object allocation</b> (a synthetic class implementing <code>FunctionN</code>) and the associated <b>virtual call</b> through <code>invoke()</code> for each lambda parameter, which matters for hot paths and JIT-unfriendly megamorphic call sites.</p>`,
  },
  {
    id: "a18-kotlin-advanced-6",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Why can't you store a \`noinline\` lambda parameter the same way you'd treat a regular inline lambda parameter, and when is \`noinline\` required?`,
    answerHtml: `<p>An inlined lambda parameter has no runtime representation as an object — its code is spliced in, so it can't be passed to another function, stored in a field, or returned, because there's no <code>FunctionN</code> instance to reference. <code>noinline</code> marks one parameter of an inline function to keep it as a real lambda object, which is required whenever that specific lambda needs to be passed onward to a non-inline function, stored, or returned — while the function's other lambda parameters still get inlined.</p>`,
  },
  {
    id: "a18-kotlin-advanced-7",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `What problem does \`reified\` solve, and why is it only legal on inline function type parameters?`,
    answerHtml: `<p>Normally a generic type parameter <code>T</code> is erased at runtime (JVM type erasure), so you can't write <code>T::class</code> or do <code>value is T</code> inside a generic function body. Marking the type parameter <code>reified</code> lets the compiler substitute the actual concrete type argument into the inlined bytecode at each call site — since the function body is copied per call site, the compiler knows the real type there and bakes it in directly. It only works on <code>inline</code> functions because non-inline functions are compiled once into shared bytecode with no per-call-site type info to substitute.</p>`,
  },
  {
    id: "a18-kotlin-advanced-8",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `\`crossinline\` shows up alongside \`noinline\` but solves a different problem. What does it actually restrict, and why?`,
    answerHtml: `<p><code>crossinline</code> still inlines the lambda's code (unlike <code>noinline</code>) but forbids non-local returns from it. It's required when the lambda is invoked from another execution context inside the function — e.g., from within another lambda, an object literal, or a different call — where a non-local <code>return</code> back to the inline function's caller wouldn't make sense (the call stack frame to return to may no longer be active, such as inside a separately scheduled callback).</p>`,
  },
  {
    id: "a18-kotlin-advanced-9",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `What's the actual runtime cost of large inline functions, and why does the Kotlin compiler not inline everything by default?`,
    answerHtml: `<p>Inlining duplicates bytecode at every call site. For a small lambda body, this is cheaper than allocating a closure object — but for a large function called from many places, it causes <b>code bloat</b>: larger method bodies, larger class files, worse instruction cache behavior, and it can push methods past the JVM's <b>8000-byte bytecode threshold</b> for JIT inlining/compilation, ironically making the JIT less willing to optimize them. Kotlin doesn't auto-inline because the tradeoff (allocation savings vs. code size) is call-site dependent and the compiler can't know which side wins for arbitrary code.</p>`,
  },
  {
    id: "a18-kotlin-advanced-10",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `\`by lazy\` defaults to \`LazyThreadSafetyMode.SYNCHRONIZED\`. What does that mode actually guarantee, and what are the alternatives?`,
    answerHtml: `<p><code>SYNCHRONIZED</code> uses a lock so the initializer runs exactly once even under concurrent first-access from multiple threads — all threads see the same computed value, and late threads block until it's ready. <code>PUBLICATION</code> allows the initializer to potentially run more than once if accessed concurrently before completion, but all threads converge on the same (first-returned) instance — useful when the initializer is cheap/side-effect-free and contention matters more than redundant computation. <code>NONE</code> skips all thread-safety machinery entirely — fastest, but only safe if the property is guaranteed to be accessed from a single thread, or initialization is otherwise externally synchronized.</p>`,
  },
  {
    id: "a18-kotlin-advanced-11",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Under \`lazy { ... }\`, what happens if the initializer block throws an exception?`,
    answerHtml: `<p>The default (<code>SYNCHRONIZED</code> and <code>PUBLICATION</code>) implementations do <b>not cache the failure</b> — the next access re-invokes the initializer block. This means a lazy property backed by a flaky initializer (e.g., a network call) will keep retrying on every access until it succeeds, which can be a footgun if you expected exceptions to be cached like the success value is.</p>`,
  },
  {
    id: "a18-kotlin-advanced-12",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `How does \`Delegates.observable\` differ from \`Delegates.vetoable\`, and what does the callback receive in each?`,
    answerHtml: `<p>Both take an initial value and a lambda with the signature <code>(property, oldValue, newValue) -> Unit/Boolean</code>, invoked on every assignment. <code>observable</code>'s callback runs <b>after</b> the assignment has already taken effect and returns <code>Unit</code> — it's purely a notification hook (e.g., logging, triggering a UI re-render). <code>vetoable</code>'s callback runs <b>before</b> the assignment commits and returns a <code>Boolean</code>: returning <code>false</code> rejects the new value and the backing field is left unchanged, returning <code>true</code> lets the assignment proceed — making it a guard/validation hook, not just an observer.</p>`,
  },
  {
    id: "a18-kotlin-advanced-13",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Class delegation via \`by\` (e.g., \`class Wrapper(impl: Repo) : Repo by impl\`) forwards interface calls to \`impl\`. What's the catch when \`Wrapper\` overrides one method of \`Repo\`?`,
    answerHtml: `<p>Overriding a method in <code>Wrapper</code> replaces the delegate's implementation for calls made directly on a <code>Wrapper</code> instance, which is the expected decorator behavior. The catch: if <code>impl</code>'s own internal code calls that method on itself (self-invocation inside the delegate), it calls its own original implementation, not <code>Wrapper</code>'s override — there's no dynamic dispatch back through the wrapper, because <code>impl</code> has no awareness it's been wrapped. This is the same self-invocation pitfall that affects Java's decorator pattern.</p>`,
  },
  {
    id: "a18-kotlin-advanced-14",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Property delegation requires the delegate object to provide what operator functions, and how does this differ for \`var\` vs \`val\` properties?`,
    answerHtml: `<p>A delegate for a <code>val</code> property only needs <code>operator fun getValue(thisRef, property): T</code>. A delegate for a <code>var</code> property needs both <code>getValue</code> and <code>operator fun setValue(thisRef, property, value: T)</code>. These can be regular methods, extension functions, or come from interfaces like <code>ReadOnlyProperty&lt;R, T&gt;</code> / <code>ReadWriteProperty&lt;R, T&gt;</code> in <code>kotlin.properties</code> — the compiler resolves them by signature/operator convention, not by a marker interface requirement.</p>`,
  },
  {
    id: "a18-kotlin-advanced-15",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `How does \`Map\`/\`MutableMap\` work as a property delegate (e.g., constructor parameters delegated from a config map), and what's the lookup key?`,
    answerHtml: `<p><code>Map&lt;String, Any?&gt;</code> has extension operator functions <code>getValue</code>/<code>setValue</code> defined in the stdlib that let any <code>Map</code> act as a property delegate. The lookup key is the <b>property's name</b> (a string), so <code>val name: String by map</code> reads <code>map["name"]</code> and casts it to the declared type — this is commonly used for parsing loosely-typed data (JSON-like maps, config dictionaries) into strongly-typed properties without writing per-field boilerplate.</p>`,
  },
  {
    id: "a18-kotlin-advanced-16",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `What does it cost to write a custom property delegate versus using \`lazy\`, and when would you write your own?`,
    answerHtml: `<p>Each delegated property access compiles to a call through the delegate's <code>getValue</code>/<code>setValue</code>, which is an indirect call (not free, though usually negligible next to the work being done). You write a custom delegate when you need reusable behavior beyond what stdlib delegates offer — e.g., a delegate that validates on write, persists to disk/SharedPreferences/DataStore on every set, debounces updates, or logs access — anything where the same get/set policy needs to apply uniformly across many properties without copy-pasting the logic into each property's custom getter/setter.</p>`,
  },
  {
    id: "a18-kotlin-advanced-17",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `\`let\`, \`run\`, \`apply\`, \`also\`, and \`with\` all exist mainly to make code read better, not to add capability. What's the one functional axis that actually differentiates them?`,
    answerHtml: `<p>Two independent choices: (1) does the lambda receive the object as <code>it</code> (an explicit argument) or as <code>this</code> (an implicit receiver), and (2) does the call return the lambda's result or the original object. <code>let</code>/<code>run</code> return the lambda result; <code>apply</code>/<code>also</code> return the original object; <code>with</code> is non-extension and takes the receiver as an explicit argument while still using <code>this</code> inside. Everything else (chaining style, nullability idioms) follows from those two axes.</p>`,
  },
  {
    id: "a18-kotlin-advanced-18",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Why is \`apply\` idiomatic for object configuration (e.g., building a \`View\` or a data class instance) but \`also\` is not, even though both return the receiver?`,
    answerHtml: `<p><code>apply</code>'s lambda has the object as <code>this</code> (implicit receiver), so member calls inside read as direct mutations — <code>apply { text = "hi"; visibility = VISIBLE }</code> — matching the mental model of &quot;configure this object.&quot; <code>also</code>'s lambda gets the object as <code>it</code> (explicit), which is idiomatic for <b>side effects that reference the object but aren't really &quot;configuring&quot; it</b> — e.g., <code>.also { logger.log(it) }</code> or <code>.also { cache.put(key, it) }</code> — where naming the object clarifies intent and avoids ambiguity with outer \`this\`.</p>`,
  },
  {
    id: "a18-kotlin-advanced-19",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `When chaining scope functions on a nullable receiver, why does \`?.let { ... }\` followed by \`?: run { ... }\` read cleanly as an if-else, and what's a sharp edge?`,
    answerHtml: `<p><code>nullable?.let { use(it) } ?: run { fallback() }</code> works because <code>?.let</code> short-circuits to <code>null</code> when the receiver is null (skipping the lambda), and the Elvis operator's right side then runs as the &quot;else&quot; branch. The sharp edge: if the <code>let</code> block itself can legitimately return <code>null</code> (not just when the receiver is null), the Elvis operator can't distinguish &quot;receiver was null&quot; from &quot;let block computed null&quot; — it'll run the <code>run</code> fallback in both cases, silently changing behavior from a real <code>if/else</code>.</p>`,
  },
  {
    id: "a18-kotlin-advanced-20",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Nesting scope functions of the same kind (e.g., \`apply\` inside \`apply\`, or \`let\` inside \`run\`) is a common readability complaint. What's the actual mechanism that causes bugs, not just style issues?`,
    answerHtml: `<p><code>this</code>-receiver functions (<code>run</code>, <code>with</code>, <code>apply</code>) nested inside each other create ambiguous implicit receiver scopes — an unqualified member call inside the inner block can silently resolve to the <b>outer</b> receiver's member instead of the inner one if the inner receiver doesn't declare that member, with no compiler warning because both resolve validly. This is purely a \`this\`-shadowing hazard; it doesn't occur with \`it\`-based functions (\`let\`, \`also\`) since \`it\` must be referenced explicitly and can't be silently shadowed the same way.</p>`,
  },
  {
    id: "a18-kotlin-advanced-21",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Scope functions are inline. Does that mean using \`let\`/\`apply\`/\`run\` everywhere has zero runtime cost compared to not using them?`,
    answerHtml: `<p>Inlining removes the lambda allocation and the call overhead, so a single scope function call is essentially free at runtime versus hand-written equivalent code — the bytecode is nearly identical. But it's not literally &quot;zero cost&quot; in all dimensions: heavy nesting increases method bytecode size (the same inline-bloat tradeoff as any inline function), and \`it\`/\`this\` ambiguity can produce a correctness cost (wrong receiver resolution) even though there's no performance cost.</p>`,
  },
  {
    id: "a18-kotlin-advanced-22",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Why does \`with(receiver) { ... }\` exist as a standalone function instead of an extension function like the other four?`,
    answerHtml: `<p><code>with</code> takes the receiver as an explicit first parameter (<code>fun &lt;T, R&gt; with(receiver: T, block: T.() -> R): R</code>) rather than being called as <code>receiver.with { ... }</code>. This matters when the receiver is the result of an expression you don't want to repeat or assign to a variable, or — more importantly — when the receiver is nullable-unfriendly to extension-call syntax (you can't safely-call \`?.\` onto a value that isn't itself the receiver of the extension); \`with\` is the right tool specifically when you're grouping calls on an already-available non-null reference purely for scoping/readability, not transforming or null-checking it.</p>`,
  },
  {
    id: "a18-kotlin-advanced-23",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `How does smart-casting a sealed class result type (e.g., a \`Result<T>\` sealed hierarchy with \`Success\`/\`Error\`) interact with generics — what can go wrong with variance?`,
    answerHtml: `<p>If the sealed class is declared invariant (<code>sealed class Result&lt;T&gt;</code>) the compiler smart-casts each branch's payload type correctly per-branch. But if you need <code>Result&lt;out T&gt;</code> covariance (e.g., to assign a <code>Result&lt;String&gt;</code> to a <code>Result&lt;Any&gt;</code>-typed variable), a subtype like <code>Error</code> that doesn't carry a <code>T</code> at all should extend <code>Result&lt;Nothing&gt;</code> — using <code>Nothing</code> as that supertype's type argument — so it remains a subtype of every <code>Result&lt;T&gt;</code> (since <code>Nothing</code> is Kotlin's bottom type); getting this wrong (e.g., extending <code>Result&lt;Any?&gt;</code> instead of <code>Result&lt;Nothing&gt;</code>) breaks the variance contract and forces unsafe casts at use sites.</p>`,
  },
  {
    id: "a18-kotlin-advanced-24",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Reified type parameters let you write \`inline fun <reified T> Gson.fromJson(json: String): T\`. Why is this specifically valuable over the non-reified Java-style API, and what's the limitation that remains?`,
    answerHtml: `<p>The non-reified Java API needs an explicit <code>Class&lt;T&gt;</code> or <code>Type</code> token passed manually (<code>gson.fromJson(json, MyType::class.java)</code>) because of erasure; the reified version infers <code>T</code> from the call-site usage and synthesizes the class token internally, so callers just write <code>gson.fromJson&lt;MyType&gt;(json)</code> or even rely on target-type inference. The limitation: this still only captures <code>T</code>'s erased runtime class via <code>T::class</code> — it does <b>not</b> recover generic type arguments of <code>T</code> itself (e.g., <code>List&lt;MyType&gt;</code> still loses the inner <code>MyType</code> at runtime unless you separately use a <code>TypeToken</code>-style construct), because reification only solves erasure for the immediately reified parameter, not nested generic structure.</p>`,
  },
];

export const ADVANCED18_QUIZ: QuizQuestion[] = [
  {
    id: "a18q-kotlin-advanced-1",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `A sealed class hierarchy \`Shape\` has subtypes \`Circle\`, \`Square\`, and \`Triangle\`, all in the same module and package. A \`when (shape) { is Circle -> ...; is Square -> ... }\` expression used as a return value, with no \`else\` branch and \`Triangle\` unhandled, will:`,
    options: [`Compile fine; Triangle falls through and returns null`, `Fail to compile because the when expression is not exhaustive`, `Compile with a warning only`, `Throw a NoWhenBranchMatchedException at runtime but compile successfully`],
    answer: 1,
    explanationHtml: `<p>Because <code>Shape</code> is sealed, the compiler knows the complete subtype set and requires every branch to be covered when <code>when</code> is used as an expression (its result is consumed). Missing <code>Triangle</code> with no <code>else</code> is a compile error, not a runtime exception — the runtime exception only occurs if you bypass exhaustiveness checking (e.g., using <code>when</code> as a statement, or casting around the sealed type).</p>`,
  },
  {
    id: "a18q-kotlin-advanced-2",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `What is the primary reason \`inline\` functions improve performance for higher-order functions like a custom \`measureBlock { ... }\` timing utility?`,
    options: [`They make the JVM JIT-compile the function eagerly instead of lazily`, `They avoid allocating a lambda object and the virtual call through invoke()`, `They convert the lambda to run on a separate coroutine dispatcher`, `They allow the function to be called without importing it`],
    answer: 1,
    explanationHtml: `<p><code>inline</code> splices the function body and the lambda's code directly into the call site at compile time, eliminating both the synthetic <code>FunctionN</code> object allocation for the lambda and the indirect <code>invoke()</code> call. It has nothing to do with JIT eagerness, coroutine dispatchers, or imports.</p>`,
  },
  {
    id: "a18q-kotlin-advanced-3",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Given \`inline fun <reified T> Any?.isInstanceOf(): Boolean = this is T\`, why is \`reified\` required here instead of a plain type parameter \`T\`?`,
    options: [`Reified parameters make the function run faster on background threads`, `Without reified, T is erased at runtime and \`this is T\` would not compile`, `Reified is only a documentation hint with no compiler effect`, `Reified is required any time a function takes a generic argument`],
    answer: 1,
    explanationHtml: `<p>Without <code>reified</code>, generic type parameters are erased on the JVM, so a runtime type check like <code>this is T</code> has nothing to check against and fails to compile ("cannot check for instance of erased type"). <code>reified</code> only works on <code>inline</code> functions, since the compiler substitutes the real type per call site during inlining — it's not required for every generic function, only ones that need runtime type info.</p>`,
  },
  {
    id: "a18q-kotlin-advanced-4",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `\`Delegates.vetoable(initial) { _, old, new -> new >= old }\` is used as a property delegate for a \`var score: Int\`. What happens when code executes \`score = score - 10\`?`,
    options: [`The assignment always succeeds and the callback only logs the change`, `If new < old, the assignment is rejected and the property keeps its old value`, `It throws an exception because vetoable delegates are read-only`, `The callback runs after the assignment, so it can't prevent the decrease`],
    answer: 1,
    explanationHtml: `<p><code>vetoable</code>'s lambda runs <i>before</i> the write commits and returns a <code>Boolean</code> deciding whether to accept it. Since <code>new &gt;= old</code> evaluates to <code>false</code> for a decrease, the assignment is vetoed and the backing field stays at its previous value — unlike <code>observable</code>, which always commits the write and only notifies afterward.</p>`,
  },
  {
    id: "a18q-kotlin-advanced-5",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `\`class CachingRepo(private val inner: Repo) : Repo by inner { override fun getUser(id: String) = cache.getOrPut(id) { inner.getUser(id) } }\`. If \`inner\`'s own implementation of some other \`Repo\` method internally calls \`this.getUser(id)\` on itself, which \`getUser\` runs?`,
    options: [`CachingRepo's overridden getUser, because Kotlin always dispatches dynamically through the delegate`, `inner's original getUser, because the delegate has no awareness it has been wrapped`, `A compile error, because this creates a circular delegation`, `It depends on whether inner.getUser is marked open`],
    answer: 1,
    explanationHtml: `<p>Class delegation forwards calls made <i>on the wrapper</i> to the delegate, and overriding a member replaces that forwarding for direct calls on <code>CachingRepo</code>. But self-invocation inside <code>inner</code> calls <code>inner</code>'s own method directly — <code>inner</code> has no reference back to <code>CachingRepo</code>, so it can't dynamically dispatch to the override. This is the same decorator self-invocation pitfall found in Java.</p>`,
  },
  {
    id: "a18q-kotlin-advanced-6",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Comparing \`data.let { transform(it) }\` and \`data.run { transform(this) }\` where \`transform\` only needs the value once and doesn't call other members of \`data\`, what's the most accurate statement?`,
    options: [`let and run produce different return types here`, `They're functionally interchangeable here; the choice is purely a style/readability preference`, `run is faster because it avoids an extra parameter`, `let cannot be used on non-null values, only run can`],
    answer: 1,
    explanationHtml: `<p>Both <code>let</code> and <code>run</code> return the lambda's result; the only difference is <code>it</code> (explicit argument) vs <code>this</code> (implicit receiver) inside the lambda. With a single simple call like this, both compile to nearly identical inlined bytecode — the choice is stylistic, not functional. (<code>let</code> is more common specifically for null-checking chains via <code>?.let</code>, but that's a convention, not a hard constraint.)</p>`,
  },
  {
    id: "a18q-kotlin-advanced-7",
    category: "kotlin",
    categoryLabel: "Kotlin",
    question: `Why might a senior engineer flag heavy nesting of \`run { ... run { ... } ... }\` in code review even though both calls are inline and therefore allocate nothing extra?`,
    options: [`Nested run blocks are deprecated since Kotlin 1.9`, `Inner unqualified member calls can silently resolve to the outer receiver's this, shadowing intent with no compiler warning`, `run cannot legally be nested inside another run block`, `Each nested run allocates a new coroutine scope`],
    answer: 1,
    explanationHtml: `<p><code>run</code> (like <code>with</code> and <code>apply</code>) supplies an implicit <code>this</code> receiver. When nested, an unqualified call inside the inner block can resolve to either receiver's member depending on what's in scope, and if both resolve validly the compiler picks one without warning — a correctness hazard distinct from any performance concern, since inlining already removes the allocation cost.</p>`,
  },
];
