// Android interview study curriculum. Typed modules are the source of truth — edit directly.
export type StudySection = { id: string; num: string; title: string; html: string };

export const STUDY_INTRO_HTML = "<span class=\"lbl\">Depth over trivia</span> Senior and staff Android interviews probe how you <i>reason</i> — why <b>StateFlow</b> over LiveData, when recomposition actually costs you, how you survive <b>process death</b>, and where coroutines leak. Each section below pairs the concept with an <b>in practice</b> note so you can answer from a real engineering decision, not a definition.";

export const STUDY_SECTIONS: StudySection[] = [
  {
    "id": "st-1",
    "num": "01",
    "title": "01 · Kotlin language essentials",
    "html": "<p><b>What they want:</b> idiomatic, production Kotlin — not Java written in Kotlin syntax. Fluency with <code>val</code> vs <code>var</code>, immutability by default, <b>null-safety</b> (<code>?.</code>, <code>?:</code>, <code>!!</code> as a code smell), data classes, <code>sealed</code> hierarchies for modelling state, and the <b>scope functions</b> (<code>let</code>, <code>run</code>, <code>with</code>, <code>apply</code>, <code>also</code>).</p>\n      <ul>\n        <li>Prefer <code>val</code> and immutable collections; expose <code>List</code>, store <code>MutableList</code> privately.</li>\n        <li>Model exhaustive state with <code>sealed interface</code> + <code>when</code> (no <code>else</code> branch → compiler enforces completeness).</li>\n        <li><b>Delegation</b>: <code>by lazy</code>, <code>by viewModels()</code>, property delegates, and class delegation via <code>by</code>.</li>\n        <li><code>inline</code> + <code>reified</code> to keep generic type info at runtime; understand the cost (code-size) of inlining.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> A senior answer ties null-safety to crash rate: the type system pushes nullability to the boundary (parsing, platform types from Java) so the rest of the app is non-null by construction. <code>!!</code> in review is a red flag — it converts a compile-time guarantee back into a runtime crash.</div>"
  },
  {
    "id": "st-2",
    "num": "02",
    "title": "02 · Coroutines & structured concurrency",
    "html": "<p><b>What they want:</b> deep coroutines — not just <code>launch { }</code>. Structured concurrency means every coroutine has a parent <code>CoroutineScope</code>; cancelling the scope cancels its children, and a child failure cancels siblings (unless a <code>SupervisorJob</code> isolates them).</p>\n      <ul>\n        <li><b>suspend</b> functions must be <i>main-safe</i>: they can be called on the main thread and internally switch with <code>withContext(Dispatchers.IO)</code>.</li>\n        <li>Dispatchers: <code>Main</code> (UI), <code>IO</code> (blocking I/O, large pool), <code>Default</code> (CPU work), <code>Unconfined</code> (rarely).</li>\n        <li>Cancellation is cooperative: check <code>isActive</code> / call <code>ensureActive()</code>, and never swallow <code>CancellationException</code>.</li>\n        <li><code>coroutineScope</code> vs <code>supervisorScope</code>; <code>async</code>/<code>await</code> for parallel decomposition.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">Land this</span> <code>viewModelScope</code> and <code>lifecycleScope</code> are structured scopes wired to a lifecycle — they cancel automatically, which is <i>why</i> you almost never need <code>GlobalScope</code> (which leaks because nothing cancels it).</div>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> The senior framing: \"I let the scope own cancellation.\" A network call started in <code>viewModelScope</code> is cancelled when the ViewModel clears, so a config change or back-press doesn't leak a request or update a dead UI.</div>"
  },
  {
    "id": "st-3",
    "num": "03",
    "title": "03 · Flow, StateFlow & SharedFlow",
    "html": "<p><b>What they want:</b> reactive streams done right. <code>Flow</code> is a cold asynchronous stream; operators (<code>map</code>, <code>filter</code>, <code>combine</code>, <code>flatMapLatest</code>) run in the collector's context unless you <code>flowOn</code>.</p>\n      <table>\n        <tr><th></th><th>StateFlow</th><th>SharedFlow</th></tr>\n        <tr><td>Holds value</td><td>Always (initial required)</td><td>Optional replay</td></tr>\n        <tr><td>Conflation</td><td>Conflated, distinct-until-changed</td><td>Configurable buffer</td></tr>\n        <tr><td>Use for</td><td>Observable UI <b>state</b></td><td>One-shot <b>events</b> (navigate, toast)</td></tr>\n      </table>\n      <ul>\n        <li>Collect with <code>repeatOnLifecycle(STARTED)</code> (or <code>collectAsStateWithLifecycle()</code> in Compose) so collection stops in the background — not <code>launchWhenStarted</code> (deprecated, keeps upstream hot).</li>\n        <li><code>stateIn(scope, SharingStarted.WhileSubscribed(5000), initial)</code> shares a cold flow as state and stops upstream 5s after the last subscriber — surviving rotation without re-fetching.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> The classic bug: collecting a flow in <code>onCreate</code> with a plain <code>launch</code> keeps the collector alive in the background, wasting work and risking stale UI updates. <code>repeatOnLifecycle</code> is the fix interviewers want to hear.</div>"
  },
  {
    "id": "st-4",
    "num": "04",
    "title": "04 · Jetpack Compose — composition & recomposition",
    "html": "<p><b>What they want:</b> the mental model. A <code>@Composable</code> is a function that <i>describes</i> UI from state; Compose builds a tree and <b>recomposes</b> (re-invokes) only the functions whose inputs changed. There is no view hierarchy you mutate — you change state and the framework reconciles.</p>\n      <ul>\n        <li><b>Recomposition scope</b>: the smallest enclosing composable that reads a given state. Reading state low in the tree keeps recomposition local.</li>\n        <li><b>Stability</b>: Compose skips a composable if its params are <code>@Stable</code>/<code>@Immutable</code> and unchanged. Unstable types (e.g. <code>List</code> from a non-stable source) defeat skipping → use <code>ImmutableList</code> / <code>kotlinx.collections.immutable</code>.</li>\n        <li>Phases: <b>Composition → Layout → Drawing</b>. Defer reads to the latest phase (e.g. <code>Modifier.offset { }</code> lambda) to skip recomposition on scroll/animation.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">New concept</span> <b>Donut-hole skipping</b>: a parent can recompose without recomposing stable children. Internalize \"what reads this state\" and you can explain jank precisely instead of memoizing blindly.</div>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> Turn on <b>Compose compiler metrics</b> / Layout Inspector recomposition counts. A senior debugs an over-recomposing list by finding the unstable parameter, not by sprinkling <code>remember</code>.</div>"
  },
  {
    "id": "st-5",
    "num": "05",
    "title": "05 · Compose state & side-effects",
    "html": "<p><b>What they want:</b> state hoisting and the side-effect APIs. <code>remember</code> caches across recomposition; <code>rememberSaveable</code> survives config change/process death. <b>Hoist state</b> up (stateless composables take <code>value</code> + <code>onValueChange</code>) so UI is reusable and testable.</p>\n      <ul>\n        <li><code>LaunchedEffect(key)</code> — run a suspend block tied to composition; restarts when <code>key</code> changes, cancels on leave.</li>\n        <li><code>rememberCoroutineScope()</code> — launch from callbacks (e.g. button click), scoped to composition.</li>\n        <li><code>DisposableEffect</code> — register/unregister (listeners, observers) with an <code>onDispose</code>.</li>\n        <li><code>derivedStateOf</code> — compute from other state and only recompose when the <i>result</i> changes (e.g. <code>showButton = scrollOffset &gt; 0</code>).</li>\n        <li><code>produceState</code> — bridge a non-Compose async source into <code>State</code>; <code>snapshotFlow</code> — turn Compose state into a Flow.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> The interview trap: launching a coroutine directly in composition body instead of <code>LaunchedEffect</code> — it fires on every recomposition. Saying \"effects belong in effect handlers keyed correctly\" signals real Compose maturity.</div>"
  },
  {
    "id": "st-6",
    "num": "06",
    "title": "06 · Activity / Fragment lifecycle & configuration changes",
    "html": "<p><b>What they want:</b> the lifecycle cold. Activity: <code>onCreate → onStart → onResume</code> (foreground) → <code>onPause → onStop → onDestroy</code>. A <b>configuration change</b> (rotation, dark mode, locale, font scale) destroys and recreates the Activity by default.</p>\n      <ul>\n        <li>Fragment view lifecycle is separate from the fragment: use <code>viewLifecycleOwner</code> for observers, or you leak when the view is destroyed but the fragment isn't.</li>\n        <li>Don't fight config changes with <code>android:configChanges</code> unless you truly own the redraw — prefer surviving state in a ViewModel.</li>\n        <li><code>onSaveInstanceState(Bundle)</code> persists small UI state across recreation and process death (parcelable, &lt; ~1MB).</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> Senior answer: \"Rotation isn't special — it's the same recreation path as process death with more state retained. If my state survives process death via SavedStateHandle, rotation is free.\"</div>"
  },
  {
    "id": "st-7",
    "num": "07",
    "title": "07 · ViewModel, SavedStateHandle & process death",
    "html": "<p><b>What they want:</b> where state lives and how it survives. A <code>ViewModel</code> outlives configuration changes (scoped to the ViewModelStoreOwner) but is cleared when the owner is truly finished. It does <b>not</b> survive <b>process death</b> on its own — the OS can kill a backgrounded app to reclaim memory.</p>\n      <ul>\n        <li><code>SavedStateHandle</code> is the bridge: a key-value map backed by saved instance state, so values survive process death. Inject it into the ViewModel and expose state via <code>getStateFlow(key, default)</code>.</li>\n        <li>Never hold a <code>Context</code>/View/Activity reference in a ViewModel (leak). Use <code>AndroidViewModel</code>'s application context only if unavoidable.</li>\n        <li>Test the restore path: kill the process from dev tools (\"Don't keep activities\") and confirm the screen rebuilds correctly.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">Land this</span> ViewModel survives <b>rotation</b>; SavedStateHandle survives <b>process death</b>; a database/DataStore survives <b>everything</b>. Naming the three tiers is the senior tell.</div>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> The realistic failure: a search screen loses the query after the OS kills the app in the background. Put the query in SavedStateHandle and it restores — the fix interviewers are listening for.</div>"
  },
  {
    "id": "st-8",
    "num": "08",
    "title": "08 · Architecture — MVVM, MVI & Clean Architecture",
    "html": "<p><b>What they want:</b> a defensible architecture with <b>unidirectional data flow</b>. MVVM: the ViewModel exposes observable state; the View renders it and sends events up. MVI tightens this into a single immutable <code>UiState</code> + an <code>Intent</code>/<code>Action</code> stream reduced into new state — great for complex, experiment-heavy screens.</p>\n      <ul>\n        <li><b>Clean Architecture</b> layers: <b>UI</b> → <b>Domain</b> (use-cases, pure Kotlin, no Android) → <b>Data</b> (repositories, sources). Dependencies point inward; the domain knows nothing about Retrofit or Room.</li>\n        <li>Single source of truth: the repository decides cache vs network; the UI never calls the API directly.</li>\n        <li>Model state as one object: <code>data class UiState(val items: List, val loading: Boolean, val error: String?)</code> — render the whole thing, avoid scattered booleans.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> Pragmatism wins: \"I use MVVM with a single immutable state per screen and use-cases only where domain logic is non-trivial — I don't add a use-case that just forwards a repo call.\" That nuance reads as staff-level judgment, not dogma.</div>"
  },
  {
    "id": "st-9",
    "num": "09",
    "title": "09 · Dependency injection — Hilt & Dagger",
    "html": "<p><b>What they want:</b> DI to keep code testable and decoupled. <b>Hilt</b> sits on Dagger and generates components tied to Android lifecycles: <code>@HiltAndroidApp</code>, <code>@AndroidEntryPoint</code>, <code>@HiltViewModel</code>, and scopes (<code>@Singleton</code>, <code>@ActivityRetainedScoped</code>, <code>@ViewModelScoped</code>).</p>\n      <ul>\n        <li><code>@Module</code> + <code>@Provides</code> for things you don't own (Retrofit, OkHttp); <code>@Binds</code> for interface→impl.</li>\n        <li>Constructor injection (<code>@Inject constructor</code>) everywhere you can — it's the most testable.</li>\n        <li>Qualifiers (<code>@Named</code> / custom) disambiguate multiple bindings of the same type (e.g. two OkHttp clients).</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">Land this</span> The point of DI isn't \"objects get created for me\" — it's <b>dependency inversion</b>: high-level code depends on interfaces, so tests swap a fake repository in one line. Frame DI as a testability and modularity tool.</div>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> Hilt's compile-time graph means a missing binding is a build error, not a runtime crash — a real advantage over service-locator patterns you should name.</div>"
  },
  {
    "id": "st-10",
    "num": "10",
    "title": "10 · Persistence — Room & DataStore",
    "html": "<p><b>What they want:</b> local storage chosen correctly. <b>Room</b> is the SQLite ORM: <code>@Entity</code>, <code>@Dao</code>, <code>@Query</code>, with compile-time SQL verification and <code>Flow</code> return types for reactive reads. <b>DataStore</b> replaces SharedPreferences: Preferences DataStore (key-value) or Proto DataStore (typed), both async and transactional.</p>\n      <ul>\n        <li>Room DAOs returning <code>Flow&lt;List&lt;T&gt;&gt;</code> emit on every change — the backbone of offline-first.</li>\n        <li>Migrations: provide a <code>Migration</code> or risk a crash on schema change; export the schema for tests.</li>\n        <li>Never do disk I/O on the main thread — Room <code>suspend</code> DAOs and DataStore are main-safe by design.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> \"SharedPreferences is synchronous and silently does I/O on the main thread (<code>apply()</code> defers the disk write but the in-memory commit still blocks). DataStore fixes that with a Flow + coroutines.\" That contrast is a frequent senior question.</div>"
  },
  {
    "id": "st-11",
    "num": "11",
    "title": "11 · Networking — Retrofit, OkHttp & serialization",
    "html": "<p><b>What they want:</b> a robust network layer. <b>Retrofit</b> defines a typed API interface; <b>OkHttp</b> is the engine with the connection pool, cache, and <b>interceptors</b>. Serialize with <b>Moshi</b> or <b>kotlinx.serialization</b>.</p>\n      <ul>\n        <li>Interceptors: an <i>application</i> interceptor for auth headers/logging; a <i>network</i> interceptor for cache control. Add a token-refresh <code>Authenticator</code> for 401s.</li>\n        <li>Model results with a <code>sealed</code> <code>Result</code>/<code>Either</code> type — never let raw exceptions reach the UI.</li>\n        <li>Map DTOs → domain models in the repository; DTOs never leak into Compose.</li>\n        <li>OkHttp HTTP cache + ETags for conditional GETs; retry only idempotent calls with backoff.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> Offline-first: the repository reads from Room (source of truth) and refreshes from the network, writing back to the DB so the UI updates via its Flow — the user never stares at a spinner, with stale-while-revalidate semantics.</div>"
  },
  {
    "id": "st-12",
    "num": "12",
    "title": "12 · Navigation & deep linking",
    "html": "<p><b>Core:</b> <b>Navigation-Compose</b> models the app as a graph of destinations with typed routes (type-safe routes via <code>@Serializable</code> route objects in Navigation 2.8+). The <code>NavController</code> owns the back stack; you pass arguments and pop with results.</p>\n      <ul>\n        <li>Hoist the <code>NavController</code> at the graph root; pass lambdas (<code>onNavigateToDetail</code>) into screens so composables stay navigation-agnostic and testable.</li>\n        <li><b>Deep links</b>: declare <code>&lt;intent-filter&gt;</code> + <code>android:autoVerify</code> for App Links (verified https) and a <code>navDeepLink</code> mapping the URI to a destination.</li>\n        <li>Nested graphs for feature modules; <code>SavedStateHandle</code> receives nav args inside the ViewModel.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> Auth-gating is a graph decision, not a hidden screen: a logged-out user gets the auth graph as the start destination. App Links need the <code>assetlinks.json</code> on your domain — a step people forget.</div>"
  },
  {
    "id": "st-13",
    "num": "13",
    "title": "13 · Background work — WorkManager, Services & broadcasts",
    "html": "<p><b>What they want:</b> the right tool for off-screen work. <b>WorkManager</b> is the default for <i>deferrable, guaranteed</i> work (sync, upload) — it survives process death and reboot, respects constraints (network, charging), and backs off on failure.</p>\n      <ul>\n        <li><code>OneTimeWorkRequest</code> vs <code>PeriodicWorkRequest</code> (min 15-min interval); chain with <code>beginWith().then()</code>; unique work to dedupe.</li>\n        <li><b>Foreground services</b> for user-visible ongoing work (playback, navigation) — require a notification and, on Android 14+, a declared <code>foregroundServiceType</code>.</li>\n        <li>Modern background limits: <code>JobScheduler</code>/WorkManager over implicit wake-locks; <code>BroadcastReceiver</code> for system events, but most implicit broadcasts are restricted.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> \"Don't use a Service for a one-off network sync — that's WorkManager. Use a foreground service only when the user can see it.\" Knowing the boundary (and Doze/App Standby) is the senior signal.</div>"
  },
  {
    "id": "st-14",
    "num": "14",
    "title": "14 · Performance — jank, frame timing & baseline profiles",
    "html": "<p><b>Core:</b> the metric is <b>frames rendered within the 16.6ms budget</b> (60fps; 8.3ms at 120Hz). Jank = a dropped frame, surfaced by <b>JankStats</b>, the <b>Macrobenchmark</b> library, and Perfetto traces.</p>\n      <ul>\n        <li><b>Baseline Profiles</b>: ship AOT-compilation hints for hot paths so the first runs aren't interpreted — major cold-start and scroll wins, generated via Macrobenchmark.</li>\n        <li>Compose: avoid unstable params, defer state reads to layout/draw, use <code>LazyColumn</code> keys, and <code>remember</code> expensive computations.</li>\n        <li>Main-thread discipline: no I/O, no large JSON parse, no bitmap decode on the UI thread.</li>\n      </ul>\n      <div class=\"callout tip\"><span class=\"lbl\">New concept</span> Measure on a <b>release build</b> with R8 on — debug builds are misleadingly slow and Compose isn't fully optimized. Macrobenchmark drives a real install.</div>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> \"I profile with Perfetto/Macrobenchmark, fix the proven hot frame, then add a baseline profile and a benchmark to guard it.\" Profiling-first beats guessing — the line interviewers want.</div>"
  },
  {
    "id": "st-15",
    "num": "15",
    "title": "15 · Memory leaks, ANRs & profiling",
    "html": "<p><b>Core:</b> two failure modes. <b>Leaks</b> retain objects the GC should free — the classic Android leak is a <code>Context</code>/View held past its lifecycle (static refs, inner classes, callbacks, a Handler posting delayed runnables). <b>ANRs</b> fire when the main thread is blocked &gt; 5s (input dispatch) — almost always main-thread I/O or lock contention.</p>\n      <ul>\n        <li><b>LeakCanary</b> in debug auto-detects retained instances and dumps the reference chain — name it as your first tool.</li>\n        <li>Android Studio <b>Memory Profiler</b> + heap dumps for cumulative growth; <b>ANR</b> traces in Play Console / <code>/data/anr/traces.txt</code>.</li>\n        <li>Common coroutine leak: launching in <code>GlobalScope</code> or collecting a flow without lifecycle awareness.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> Leaks present as slow, cumulative jank and OOM crashes after long sessions — you hunt them over time with LeakCanary, not from a single stack trace. ANRs you fix by getting work off the main thread.</div>"
  },
  {
    "id": "st-16",
    "num": "16",
    "title": "16 · App startup & R8 / ProGuard",
    "html": "<p><b>Core:</b> cold start = process fork → <code>Application.onCreate</code> → first frame. The budget is spent on eager initialization, large dependency graphs, and main-thread work.</p>\n      <ul>\n        <li>Use the <b>App Startup</b> library to consolidate and order initializers (and lazy-init the ones not needed at launch).</li>\n        <li><b>R8</b> shrinks, optimizes, and obfuscates: dead-code/resource removal cuts size and improves load; keep rules (<code>-keep</code>) protect reflection (Gson, serialization).</li>\n        <li>Defer non-critical work off the launch path; show content fast and hydrate the rest.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> \"I measure cold start on a release build with Macrobenchmark, trim <code>Application.onCreate</code>, add a baseline profile, and keep R8 on.\" Treating startup as a defended budget is a senior tell.</div>"
  },
  {
    "id": "st-17",
    "num": "17",
    "title": "17 · Testing — JUnit, MockK, Turbine & Compose UI tests",
    "html": "<p><b>What they want:</b> a real test pyramid. Many fast <b>unit</b> tests (JUnit + <b>MockK</b>) on ViewModels, use-cases, and mappers; fewer <b>integration</b> tests; a few <b>UI/E2E</b> (Compose test or Espresso) on critical flows.</p>\n      <ul>\n        <li>Coroutines/Flow: <code>runTest</code> + a <code>TestDispatcher</code> (inject the dispatcher, never hardcode <code>Dispatchers.IO</code>); assert emissions with <b>Turbine</b>.</li>\n        <li><b>Fakes vs mocks</b>: prefer a hand-written fake repository for state-based tests; use mocks for interaction verification. Over-mocking couples tests to implementation.</li>\n        <li><b>Robolectric</b> runs the Android framework on the JVM (fast, no device); <b>Compose UI test</b> uses semantics (<code>onNodeWithText</code>, <code>assertIsDisplayed</code>).</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> The injectable-dispatcher pattern is the senior tell: <code>class Repo(private val io: CoroutineDispatcher)</code> so tests pass <code>StandardTestDispatcher()</code> and control virtual time. Hardcoded dispatchers make tests flaky.</div>"
  },
  {
    "id": "st-18",
    "num": "18",
    "title": "18 · Gradle — variants, version catalogs & modularization",
    "html": "<p><b>What they want:</b> you can reason about the build. <b>Build variants</b> = build types (debug/release) × product flavors (free/paid, staging/prod). <b>Version catalogs</b> (<code>libs.versions.toml</code>) centralize dependency versions.</p>\n      <ul>\n        <li><b>KSP vs kapt</b>: KSP is the Kotlin-native annotation processor — much faster than kapt (which generates Java stubs). Room/Hilt/Moshi support KSP; migrate off kapt for build speed.</li>\n        <li><b>Modularization</b>: split by feature (<code>:feature:home</code>) and layer (<code>:core:data</code>) for parallel builds, faster incremental compilation, and enforced boundaries.</li>\n        <li>Convention plugins (<code>build-logic</code>) DRY up module config; <code>api</code> vs <code>implementation</code> controls what leaks transitively.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> \"Modularization buys parallel + incremental builds and architectural enforcement — a <code>:feature</code> module physically can't import another feature's internals.\" Naming build-speed and boundaries is the staff answer.</div>"
  },
  {
    "id": "st-19",
    "num": "19",
    "title": "19 · Security — Keystore, encryption & integrity",
    "html": "<p><b>Core:</b> the APK ships to the device and can be decompiled — <b>never hardcode secrets</b>. Store keys in the <b>Android Keystore</b> (hardware-backed where available); encrypt local data with Jetpack Security (<code>EncryptedSharedPreferences</code> / <code>EncryptedFile</code>) or SQLCipher for Room.</p>\n      <ul>\n        <li>Tokens: short-lived access + refresh; gate sensitive actions behind <b>BiometricPrompt</b>.</li>\n        <li>Network: <b>Network Security Config</b> + <b>certificate pinning</b> (mind rotation), HTTPS only, no cleartext.</li>\n        <li><b>Play Integrity API</b> attests a genuine app/device; obfuscation (R8) and root detection are defense-in-depth, not silver bullets.</li>\n        <li>Know the <b>OWASP MASVS / Mobile Top 10</b> vocabulary: insecure storage, weak crypto, etc.</li>\n      </ul>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> \"Parse the threat model out loud\": secrets-in-bundle, data at rest, data in transit, and device integrity are four distinct buckets. Naming them shows security maturity.</div>"
  },
  {
    "id": "st-20",
    "num": "20",
    "title": "20 · Accessibility & the on-device AI frontier",
    "html": "<p><b>Core:</b> ship for <b>TalkBack</b>. In Compose, set <code>Modifier.semantics { contentDescription = ... }</code>, mark headings, group related nodes, honor touch-target size (48dp), Dynamic Type (sp units), and contrast (WCAG AA).</p>\n      <ul>\n        <li><code>Modifier.clickable(onClickLabel=...)</code>, <code>stateDescription</code>, and merging semantics for composite controls.</li>\n        <li>Test with the Accessibility Scanner and Compose semantics assertions.</li>\n      </ul>\n      <p><b>Frontier:</b> on-device generative AI via <b>Gemini Nano</b> (AICore / ML Kit GenAI APIs) and the <b>MediaPipe LLM Inference</b> API, plus classic <b>TensorFlow Lite</b> (LiteRT) for vision/audio. The trade-off: privacy (data stays local), offline, no server cost — against model size, RAM, and device fragmentation.</p>\n      <div class=\"map\"><span class=\"lbl\">In practice</span> \"Gemini Nano is gated to capable devices (Pixel 8+/flagships via AICore); I feature-detect and fall back to a server model otherwise.\" Reasoning about the privacy/latency/cost trade-off and device support is the staff-level answer.</div>"
  }
];
