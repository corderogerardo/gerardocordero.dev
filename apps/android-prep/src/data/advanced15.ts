// Batch 15 — App startup (cold/warm/hot, Baseline Profiles, App Startup library), memory leaks (LeakCanary, common Compose leak patterns), Macrobenchmark, and R8/ProGuard shrinking effects on perf.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED15_FLASHCARDS: Flashcard[] = [
  {
    id: "a15-perf-profiling-1",
    category: "perf",
    categoryLabel: "Performance",
    question: `What specifically distinguishes a warm start from a hot start, beyond "the process is already alive"?`,
    answerHtml: `<p>In a <b>hot start</b>, the Activity is simply brought back to the foreground from the recent-tasks stack &mdash; no <code>onCreate</code>/<code>onStart</code> work happens, just <code>onRestart</code>/<code>onResume</code> and a redraw of cached views, so it's nearly instant.</p><p>In a <b>warm start</b> the process and <code>Application</code> object are still alive (no zygote fork, no Application init), but the Activity itself was destroyed (e.g. evicted from memory or configuration change). The system re-runs <code>onCreate</code>, re-inflates the view hierarchy, and re-runs state restoration &mdash; meaningfully slower than hot, but skips process creation, the most expensive part of a cold start.</p>`,
  },
  {
    id: "a15-perf-profiling-2",
    category: "perf",
    categoryLabel: "Performance",
    question: `Where does the system draw the boundary for measuring Time to Initial Display (TTID) vs Time to Full Display (TTFD), and what does calling reportFullyDrawn() actually change?`,
    answerHtml: `<p><b>TTID</b> is measured automatically by the system from process start until the first frame is fully drawn (logged as <code>Displayed</code> in logcat) &mdash; it only proves *something* rendered, not that the screen is actually usable.</p><p><b>TTFD</b> requires the app to explicitly call <code>Activity.reportFullyDrawn()</code> once async content (network-loaded images, RecyclerView data, etc.) has rendered. Without that call, TTFD silently defaults to the TTID timestamp, which can make a janky, content-less first frame look artificially fast in Play Console's vitals dashboard.</p>`,
  },
  {
    id: "a15-perf-profiling-3",
    category: "perf",
    categoryLabel: "Performance",
    question: `What problem does the App Startup library (androidx.startup) solve, and what mechanism does it use to avoid the cost it replaces?`,
    answerHtml: `<p>It replaces the common pattern of one <code>ContentProvider</code> per library (WorkManager, Firebase, etc.) used purely to run init code before <code>Application.onCreate()</code> finishes. Each extra ContentProvider adds a binder-backed object the system must instantiate sequentially during process start, and that cost is invisible to the app author since it's declared by the library, not the app.</p><p>App Startup consolidates all of these into a <b>single ContentProvider</b> (<code>InitializationProvider</code>) that reads a manifest-declared dependency graph of <code>Initializer&lt;T&gt;</code> components and runs them in topological order on the main thread at startup, paying the ContentProvider tax once instead of N times.</p>`,
  },
  {
    id: "a15-perf-profiling-4",
    category: "perf",
    categoryLabel: "Performance",
    question: `If a library's Initializer in App Startup does network I/O before returning its result, what happens to app startup time, and how do you avoid it?`,
    answerHtml: `<p>App Startup runs all initializers <b>synchronously on the main thread</b> by default, in dependency order, before the first frame can be produced &mdash; it does not parallelize or background them automatically. A blocking network or disk call inside an <code>Initializer.create()</code> directly extends cold-start latency and can trigger ANRs.</p><p>Fix: defer truly expensive work out of <code>create()</code> &mdash; either lazily initialize on first use, dispatch the heavy part onto a background coroutine/executor and only synchronously do the minimal handle setup, or remove the component from automatic discovery (mark it manually-initialized in the manifest) and call it explicitly after the UI is interactive.</p>`,
  },
  {
    id: "a15-perf-profiling-5",
    category: "perf",
    categoryLabel: "Performance",
    question: `Why can disabling a library's automatic ContentProvider/Initializer registration in the merged manifest actually break things, and what's the safe way to do it?`,
    answerHtml: `<p>Manifest merging means a &lt;provider&gt; or &lt;meta-data&gt; <code>tools:node="remove"</code> override is global and silent &mdash; if another dependency (or a future version of the same library) relies on that same auto-init path, you've removed it for everyone, and the failure shows up as a confusing NPE/IllegalStateException far from the manifest change.</p><p>Safe approach: use the library's documented manual-init API where one exists (most AndroidX libraries that ship an Initializer also expose a manual configure/init call), and verify via <code>./gradlew :app:dependencies</code> / merged-manifest viewer that no other module still expects the auto-init to have run.</p>`,
  },
  {
    id: "a15-perf-profiling-6",
    category: "perf",
    categoryLabel: "Performance",
    question: `What is a Baseline Profile, and at what point in the build/install pipeline does it actually get consumed by the runtime?`,
    answerHtml: `<p>A Baseline Profile is a human-readable (compiled to binary at build time) list of classes and methods that are hot at startup or during critical user journeys, expressed in a profile DSL format understood by ART. It ships inside the APK/AAB as <code>assets/dexopt/baseline.prof</code>.</p><p>It's consumed by <b>ART's dex2oat compiler at install time on-device</b> &mdash; the listed methods are AOT-compiled to native code ahead of time, so the runtime skips the slow interpreter-then-JIT warm-up path for exactly those methods on first launches. (This is distinct from Play's separate <b>Cloud Profiles</b> mechanism, which aggregates real anonymized on-device JIT profiles from the install base and can supplement compilation for apps that ship without their own Baseline Profile.)</p>`,
  },
  {
    id: "a15-perf-profiling-7",
    category: "perf",
    categoryLabel: "Performance",
    question: `Without a Baseline Profile, why is a cold start on a freshly installed app slower than the same cold start a week later, even with no code changes?`,
    answerHtml: `<p>ART defaults to interpreting bytecode and JIT-compiling "hot" methods incrementally as the app actually runs. On day one, almost nothing has been profiled yet, so startup-critical code runs interpreted (or JIT-compiles just-in-time, which itself costs CPU during that exact frame).</p><p>Over repeated runs, ART's on-device profile (cloud profile/JIT profile in <code>/data/misc/profiles</code>) accumulates real usage data and the system periodically AOT-recompiles based on it (background dexopt, e.g. during charging/idle). A Baseline Profile front-loads that benefit immediately at install instead of waiting for the device to learn it organically.</p>`,
  },
  {
    id: "a15-perf-profiling-8",
    category: "perf",
    categoryLabel: "Performance",
    question: `How do you actually generate a Baseline Profile for an app, and what's the role of androidx.benchmark.macro.junit4.BaselineProfileRule?`,
    answerHtml: `<p>You write a <b>Baseline Profile Generator</b> module/test using <code>BaselineProfileRule</code> (from <code>androidx.benchmark:benchmark-macro-junit4</code>), which drives the release build through critical user journeys (launch app, navigate to key screens) on a connected device/emulator while ART records which classes/methods get touched.</p><p>Running that instrumented test produces a .prof/human-readable profile file; the Gradle <b>Baseline Profile plugin</b> (<code>androidx.baselineprofile</code>) then merges it into <code>src/main/baselineProfiles</code> (or generates it as part of the release build) so it's packaged into the APK/AAB automatically.</p>`,
  },
  {
    id: "a15-perf-profiling-9",
    category: "perf",
    categoryLabel: "Performance",
    question: `What's the difference between a Baseline Profile and a Startup Profile, and why would you need both?`,
    answerHtml: `<p>A <b>Baseline Profile</b> tells dex2oat which methods/classes to AOT-compile (turns interpreted/JIT into precompiled native code) &mdash; it's about compilation speed of hot code.</p><p>A <b>Startup Profile</b> instead controls the <b>layout of classes inside the DEX files themselves</b>, ordering startup-critical classes so they land in the primary/first dex and are co-located for faster classloading and page-ins from disk. They're complementary: Startup Profile reduces I/O/page-fault cost of loading classes; Baseline Profile reduces execution cost once loaded. Apps with very large dex (multidex) benefit most from also supplying a Startup Profile.</p>`,
  },
  {
    id: "a15-perf-profiling-10",
    category: "perf",
    categoryLabel: "Performance",
    question: `Your app already uses Baseline Profiles, but Macrobenchmark shows startup didn't improve after adding one for a newly added feature flow. What's the most likely root cause?`,
    answerHtml: `<p>The profile most likely went <b>stale relative to R8/ProGuard output</b> or the new code path. Baseline Profiles are matched against fully-qualified class/method names; if R8 renamed/inlined/merged the relevant classes after the profile was generated (or the profile was generated against a debug/non-minified build), the names in <code>baseline.prof</code> no longer match anything in the shipped release dex, so dex2oat silently skips those rules.</p><p>Fix: always (re)generate the Baseline Profile against a <b>release-configured, minified, non-debuggable build variant</b> (the androidx Baseline Profile Gradle plugin sets up a dedicated build variant for exactly this purpose), and regenerate whenever R8 rules or the obfuscation mapping changes meaningfully.</p>`,
  },
  {
    id: "a15-perf-profiling-11",
    category: "perf",
    categoryLabel: "Performance",
    question: `LeakCanary detects a leak via "retained instance" + heap dump + shortest-path analysis. What is the actual trigger that tells LeakCanary an object might be leaking, before it even takes a heap dump?`,
    answerHtml: `<p>LeakCanary hooks into Android's lifecycle callbacks (Activity/Fragment/View destroy, ViewModel <code>onCleared</code>) to wrap the destroyed object in a <code>KeyedWeakReference</code> and register it with the <code>ReferenceQueue</code>. It then triggers a GC and waits a short delay. If the weak reference is <b>not</b> cleared and enqueued after that forced GC, the object is still strongly reachable from a GC root despite being expected to be garbage &mdash; that's the "retained instance" signal that triggers a heap dump for shortest-path-to-GC-root analysis.</p>`,
  },
  {
    id: "a15-perf-profiling-12",
    category: "perf",
    categoryLabel: "Performance",
    question: `Why does LeakCanary wait for the object to be "retained" across a watch period instead of just dumping the heap immediately on destroy?`,
    answerHtml: `<p>Two reasons: first, GC is not synchronous/immediate &mdash; an object can be eligible for collection but not yet collected at the exact moment of destroy, so dumping immediately would produce false positives. Second, taking a heap dump is expensive (it freezes the app momentarily and produces a large .hprof file), so LeakCanary debounces: it waits, forces a GC, and only pays the heap-dump cost once it has real evidence (the weak reference survived the GC) that something is actually wrong.</p>`,
  },
  {
    id: "a15-perf-profiling-13",
    category: "perf",
    categoryLabel: "Performance",
    question: `In Compose, why does capturing a lambda inside remember { } without keys often cause an unintentional leak of the enclosing Composable's state?`,
    answerHtml: `<p><code>remember { }</code> without keys caches its result for the lifetime of the composition, so any lambda created inside it captures its closure (e.g. a Context, a NavController, or a callback referencing an outer ViewModel) and that closure is retained as long as the composable stays in composition &mdash; even across recompositions where the captured values have logically changed, since the lambda itself is never recreated.</p><p>If that captured object is something tied to a shorter lifecycle (an Activity Context, a screen-scoped callback) and the composable's lifetime outlives it (e.g. cached at a higher scope, or the composable is conceptually "the same" across navigation due to saved state), you get a stale/leaked reference. The fix is to key the <code>remember</code> on the values the lambda actually depends on, or hoist state instead of capturing context directly.</p>`,
  },
  {
    id: "a15-perf-profiling-14",
    category: "perf",
    categoryLabel: "Performance",
    question: `Why is launching a coroutine with GlobalScope.launch inside a Composable a classic Compose memory-leak pattern, and what should be used instead?`,
    answerHtml: `<p><code>GlobalScope</code> is not tied to any lifecycle &mdash; a coroutine started there keeps running (and keeps any captured composable-scope objects, like a closure over a local MutableState or a Context, alive) even after the composable leaves composition. There's no cancellation hook, so it leaks both the coroutine's captured references and wastes CPU/battery on work nobody needs anymore.</p><p>Use <code>rememberCoroutineScope()</code> (for scope tied to the composition, cancelled on leaving composition) or launch inside a <code>LaunchedEffect(key) { }</code> (cancelled and relaunched based on key changes, tied to composition lifecycle) instead &mdash; both are automatically cancelled when the composable leaves the composition.</p>`,
  },
  {
    id: "a15-perf-profiling-15",
    category: "perf",
    categoryLabel: "Performance",
    question: `A DisposableEffect registers a listener (e.g. a BroadcastReceiver, a Lifecycle observer, or a sensor listener) but the screen still leaks the Activity after navigating away. What's the most common mistake in how DisposableEffect is used?`,
    answerHtml: `<p>The most common mistake is <b>forgetting the <code>onDispose { }</code> block entirely</b>, or registering the listener correctly but unregistering the wrong reference (e.g. creating a new listener instance inside <code>onDispose</code> instead of capturing and removing the same instance that was registered). <code>DisposableEffect</code> only protects you if the cleanup lambda actually undoes what the effect block did, symmetrically, using the same object reference.</p><p>Secondary cause: missing or wrong <b>keys</b> on the effect &mdash; if the key changes on every recomposition (e.g. a lambda or unstable object passed as the key), the effect tears down and re-registers constantly without ever truly leaving composition naturally, but if the key is missing change detection entirely (e.g. <code>Unit</code> when it should track a real dependency), stale closures can persist registered against an old, now-dead target.</p>`,
  },
  {
    id: "a15-perf-profiling-16",
    category: "perf",
    categoryLabel: "Performance",
    question: `Why can a Modifier.pointerInput(Unit) { } block be a sneaky leak source in a long-lived Composable that handles drag/gesture state?`,
    answerHtml: `<p><code>pointerInput</code> launches a coroutine scoped to the composition that runs an <code>awaitPointerEventScope</code> loop, and any state captured inside it (a mutable list of points, a callback into a ViewModel, a Context) lives as long as that coroutine runs. If the key passed to <code>pointerInput</code> never changes (e.g. <code>Unit</code>) across logically distinct "sessions" of the gesture (say, the same composable is reused for different items in a list via key-based reuse), the coroutine and its captured state can be retained and conflated across item identities, holding onto stale closures from a previous item that should have been garbage collected.</p>`,
  },
  {
    id: "a15-perf-profiling-17",
    category: "perf",
    categoryLabel: "Performance",
    question: `Static Drawable, Bitmap, or Context references are a classic pre-Compose leak. What's the Compose-era equivalent of this mistake, specific to ImageBitmap/Painter caching?`,
    answerHtml: `<p>Hand-rolling a top-level (object-scoped, process-lifetime) cache of ImageBitmap/Painter instances keyed by resource ID or URL, built as a plain singleton <code>mutableMapOf</code>, leaks every Bitmap ever loaded for the life of the process since nothing ever evicts entries &mdash; each Bitmap can be several MB, and a few dozen accumulate into an OOM on memory-constrained devices.</p><p>The fix is to let Coil/Glide (which already implement bounded LRU memory caches tied to <code>ComponentCallbacks2.onTrimMemory</code>) own image caching instead of building a custom unbounded one, or if a custom cache is truly required, bound it with an LRU eviction policy and hook <code>onTrimMemory</code>.</p>`,
  },
  {
    id: "a15-perf-profiling-18",
    category: "perf",
    categoryLabel: "Performance",
    question: `What's the core difference between Macrobenchmark and Microbenchmark (both from androidx.benchmark), and when is each the right tool?`,
    answerHtml: `<p><b>Microbenchmark</b> measures the execution time of a specific function/code path in-process, in a tight loop, with JIT warmup accounted for &mdash; good for algorithmic/CPU-bound code (parsing, sorting, a Compose layout pass in isolation).</p><p><b>Macrobenchmark</b> drives the app as a black box from <b>outside the process</b> via UI Automator, measuring end-to-end user-facing metrics (startup time, frame timing/jank during scroll, specific user journeys) against a real, installed APK with realistic compilation state (it can force different compilation modes &mdash; None, BaselineProfile, Full &mdash; to isolate the effect of AOT compilation itself). Use Macrobenchmark when you want to validate Baseline Profile impact or real startup/scroll-jank numbers; use Microbenchmark to optimize a hot function in isolation.</p>`,
  },
  {
    id: "a15-perf-profiling-19",
    category: "perf",
    categoryLabel: "Performance",
    question: `Macrobenchmark's StartupTimingMetric requires CompilationMode to be set explicitly in most realistic test setups. Why does the choice of CompilationMode matter so much for startup benchmark results, and what does CompilationMode.Partial represent?`,
    answerHtml: `<p>Startup time is dominated not just by app logic but by how much code ART has to interpret/JIT versus run as precompiled native code, and that state is exactly what Baseline Profiles influence &mdash; so benchmarking without pinning CompilationMode means you're partly measuring noisy, run-dependent JIT/profile state instead of your code change.</p><p><code>CompilationMode.Partial(baselineProfileMode = ...)</code> simulates a real-world install: it applies the app's actual Baseline Profile (if present) the same way the Play Store / dex2oat would at install time, letting you measure "what a real user's first launches look like" rather than a fully-AOT-compiled (CompilationMode.Full, unrealistically fast) or fully-interpreted (CompilationMode.None, unrealistically slow) extreme.</p>`,
  },
  {
    id: "a15-perf-profiling-20",
    category: "perf",
    categoryLabel: "Performance",
    question: `Why must Macrobenchmark tests run against a release-configured (or benchmark build type) variant rather than the debug build, beyond "debug is just slower"?`,
    answerHtml: `<p>Debug builds disable ahead-of-time optimizations, keep <code>isDebuggable=true</code> (which makes ART force full interpretation/JIT-only and ignore Baseline Profiles &mdash; the OS deliberately disables AOT for debuggable apps for faster iteration), and skip R8 shrinking/obfuscation entirely. Benchmarking a debug build doesn't just give slower numbers, it gives <b>qualitatively different code paths and disabled optimizations</b>, so the results don't predict production behavior at all (e.g. a dead-code-eliminated branch in release simply doesn't exist to benchmark).</p><p>The standard setup is a dedicated, non-debuggable <code>benchmark</code> build type that mirrors release minification/shrinking/signing but stays separately buildable from CI, commonly scaffolded by the androidx.benchmark Gradle plugin.</p>`,
  },
  {
    id: "a15-perf-profiling-21",
    category: "perf",
    categoryLabel: "Performance",
    question: `What does FrameTimingMetric in Macrobenchmark actually capture, and how does it relate to Jetpack's JankStats library?`,
    answerHtml: `<p><code>FrameTimingMetric</code> instruments a UI interaction (e.g. a scroll gesture defined in the benchmark) and reports frame duration percentiles, surfacing how many frames exceeded the deadline (16.6ms at 60Hz, ~11.1ms at 90Hz, etc.) &mdash; this is the offline/CI equivalent of jank detection.</p><p><code>JankStats</code> is the runtime/production counterpart: it hooks Choreographer/frame metrics APIs live in a running app (including in the field, not just in a benchmark harness) and reports jank with a state label (e.g. which UI state was active when the frame was dropped), letting you correlate dropped frames to real user sessions instead of only synthetic benchmark runs. Using both gives lab (Macrobenchmark, controlled/CI) and field (JankStats, production telemetry) visibility into the same jank problem.</p>`,
  },
  {
    id: "a15-perf-profiling-22",
    category: "perf",
    categoryLabel: "Performance",
    question: `How does R8 full-mode shrinking (as opposed to ProGuard-compatible mode) more aggressively affect runtime performance, beyond just smaller APK size?`,
    answerHtml: `<p>R8 full mode performs more aggressive whole-program optimization than ProGuard-compatible mode: it can remove or merge classes, inline single-implementation interfaces, devirtualize calls, prune unreachable code paths more completely, and optimize across what ProGuard treated as opaque boundaries (since ProGuard-compat mode preserves certain ProGuard quirks/limitations for rule compatibility). The performance effect isn't just "smaller APK" &mdash; fewer classes means less to verify and load at class-load time, inlining reduces call overhead and can let the JIT/AOT compiler optimize larger contiguous blocks, and dead-code elimination shrinks the dex, which (combined with a Startup Profile) reduces page-ins during cold start.</p>`,
  },
];

export const ADVANCED15_QUIZ: QuizQuestion[] = [
  {
    id: "a15q-perf-profiling-1",
    category: "perf",
    categoryLabel: "Performance",
    question: `An app's Activity process is still alive but the Activity instance was destroyed by the system to reclaim memory. The user reopens the app and the system calls onCreate() again. What kind of start is this?`,
    options: [`Hot start`, `Warm start`, `Cold start`, `Background start`],
    answer: 1,
    explanationHtml: `<p>This is a <b>warm start</b>: the process/Application object survived (skipping zygote fork and Application init, the costliest part of cold start), but the Activity was torn down, so <code>onCreate</code> and view re-inflation still have to run. Hot start would skip <code>onCreate</code> entirely; cold start would also re-fork the process.</p>`,
  },
  {
    id: "a15q-perf-profiling-2",
    category: "perf",
    categoryLabel: "Performance",
    question: `An app never calls reportFullyDrawn(), but its first frame renders quickly with an empty list that fills in 800ms later via a network call. What does Play Console's Time to Full Display (TTFD) metric report for this launch?`,
    options: [`The time until the network call completes and the list is populated`, `Effectively the same as Time to Initial Display (TTID), since reportFullyDrawn() was never called`, `An error indicating the metric could not be measured`, `The average of TTID and the network call duration`],
    answer: 1,
    explanationHtml: `<p>TTFD only reflects real content-loaded time if the app explicitly calls <code>reportFullyDrawn()</code>. Without that call, the system has no signal beyond the first frame, so TTFD collapses to TTID &mdash; making a content-less, instant-but-empty first frame look fast even though the screen isn't actually usable yet.</p>`,
  },
  {
    id: "a15q-perf-profiling-3",
    category: "perf",
    categoryLabel: "Performance",
    question: `A library's Initializer in App Startup performs a blocking disk read inside create(), and the dependency graph places it before the app's own UI-critical initializer. What's the most direct consequence?`,
    options: [`No effect, since App Startup initializers always run on a background thread pool`, `Cold start is directly extended, since App Startup runs initializers synchronously on the main thread in dependency order`, `The initializer is automatically skipped because it blocks`, `Only warm starts are affected, not cold starts`],
    answer: 1,
    explanationHtml: `<p>App Startup executes all discovered Initializer&lt;T&gt; components synchronously on the main thread, honoring declared dependency order, before the first frame can be produced. A blocking call inside any initializer directly adds to startup latency (and risks ANRs) &mdash; there's no automatic backgrounding.</p>`,
  },
  {
    id: "a15q-perf-profiling-4",
    category: "perf",
    categoryLabel: "Performance",
    question: `What is the primary effect of a Baseline Profile, mechanically, on a freshly installed release APK?`,
    options: [`It compresses resources to reduce APK download size`, `It tells ART's dex2oat compiler to ahead-of-time compile the listed hot classes/methods at install time instead of relying on interpretation/JIT warm-up`, `It disables R8 obfuscation for the listed classes so stack traces are readable`, `It preloads the listed classes into a shared memory region across all apps on the device`],
    answer: 1,
    explanationHtml: `<p>A Baseline Profile is a list of startup/critical-path classes and methods consumed by ART's dex2oat compiler, which AOT-compiles exactly those methods at install time. This avoids the slower interpreter-then-JIT path on early launches, before the device has organically learned the app's hot paths through normal usage.</p>`,
  },
  {
    id: "a15q-perf-profiling-5",
    category: "perf",
    categoryLabel: "Performance",
    question: `LeakCanary forces a garbage collection and then checks a KeyedWeakReference before deciding to take a heap dump. Why does it wait and force GC rather than dumping the heap immediately when a watched object (e.g. a destroyed Activity) is detected?`,
    options: [`Heap dumps can only be taken right after a configuration change`, `An object eligible for collection isn't necessarily collected the instant it's destroyed, so dumping immediately would produce false positives, and heap dumps are expensive to take unconditionally`, `LeakCanary requires the app to be backgrounded before any heap dump`, `Forcing GC is required to free enough memory to write the heap dump file`],
    answer: 1,
    explanationHtml: `<p>GC is not synchronous with object destruction, so checking immediately would flag objects that are about to be collected normally. LeakCanary forces a GC and gives it a short window; only if the weak reference still hasn't been enqueued after that does it conclude the object is genuinely retained, and only then pays the real cost of a full heap dump and shortest-path analysis.</p>`,
  },
  {
    id: "a15q-perf-profiling-6",
    category: "perf",
    categoryLabel: "Performance",
    question: `A Composable launches GlobalScope.launch { ... } inside its body to perform a periodic background update, capturing a local MutableState in its closure. What's the core problem with this pattern?`,
    options: [`GlobalScope coroutines run on the main thread and will cause ANRs`, `GlobalScope is not tied to any lifecycle, so the coroutine (and everything it captures) keeps running and stays referenced even after the composable leaves composition`, `Compose does not allow launching coroutines from within a Composable function at all`, `GlobalScope.launch silently fails inside Composable functions due to recomposition`],
    answer: 1,
    explanationHtml: `<p>GlobalScope has process lifetime and no automatic cancellation hook tied to composition. The fix is <code>rememberCoroutineScope()</code> or a <code>LaunchedEffect</code>, both of which are cancelled when the composable leaves composition, releasing captured references instead of leaking them indefinitely.</p>`,
  },
  {
    id: "a15q-perf-profiling-7",
    category: "perf",
    categoryLabel: "Performance",
    question: `You want to validate that adding a Baseline Profile actually improved cold-start time, with realistic numbers that predict production behavior. Which Macrobenchmark setup is correct?`,
    options: [`Run StartupTimingMetric against the debug build type with CompilationMode.Full`, `Run StartupTimingMetric against a release/benchmark build type with CompilationMode.Partial(baselineProfileMode = ...) to simulate real install-time compilation`, `Run a Microbenchmark on the Application.onCreate() function in isolation`, `Manually force-stop and relaunch the app once, timing it with a stopwatch`],
    answer: 1,
    explanationHtml: `<p>Debug builds disable AOT/Baseline Profile usage entirely (ART skips it for debuggable apps), so debug numbers are meaningless here. CompilationMode.Partial on a release-configured build applies the Baseline Profile the same way a real device install would, giving numbers that actually predict production cold-start behavior &mdash; Full would be unrealistically fast since it AOT-compiles everything, not just the profiled hot path.</p>`,
  },
];
