// Batch 17 — Build variants & product flavors, version catalogs (libs.versions.toml), convention plugins for multi-module builds, build cache & configuration cache, and CI build performance.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED17_FLASHCARDS: Flashcard[] = [
  {
    id: "a17-gradle-ci-1",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `When you define flavorDimensions for environment (dev/prod) and tier (free/paid), how many build variants result, and what's the source-set merge precedence?`,
    answerHtml: `<p>Variants are the cartesian product across dimensions, so 2x2 yields <b>4 flavor combos</b> (<code>devFree</code>, <code>devPaid</code>, <code>prodFree</code>, <code>prodPaid</code>), each crossed with build types (debug/release) for 8 total variants.</p><p>Source set precedence when merging resources/manifests, highest to lowest: full variant (<code>devFreeDebug</code>) &gt; flavor combo (<code>devFree</code>) &gt; single flavor (<code>dev</code>, <code>free</code>, ordered by dimension declaration) &gt; build type (<code>debug</code>) &gt; <code>main</code>.</p>`,
  },
  {
    id: "a17-gradle-ci-2",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `Why does adding a third flavor dimension to a large app often blow up CI time far more than developers expect, even when each individual variant builds fast?`,
    answerHtml: `<p>Variant count multiplies across dimensions, so a 3rd dimension with just 2 flavors doubles an existing matrix (e.g. 4 flavor combos -> 8, x2 build types = 16 total variants). Each variant gets its own <code>compile*Kotlin</code>, <code>process*Resources</code>, and <code>merge*Manifest</code> tasks, so configuration time and task-graph size scale combinatorially.</p><p>Teams typically cap CI to the variants actually shipped (filter unwanted ones in <code>androidComponents.beforeVariants</code>) and use a single low-config variant for local dev iteration instead of paying the full matrix on every commit.</p>`,
  },
  {
    id: "a17-gradle-ci-3",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `What replaced the deprecated \`variantFilter\` for excluding unwanted build variants, and why is it preferred?`,
    answerHtml: `<p>The AGP <b>Variant API</b>'s <code>androidComponents.beforeVariants { variantBuilder -> ... }</code> block. Unlike the old <code>variantFilter</code> (which ran eagerly during the legacy variant model's configuration), <code>beforeVariants</code> runs as part of AGP's lazy variant creation, integrates with configuration caching, and exposes a typed <code>VariantBuilder</code> instead of a loosely-typed filter object.</p><p>Example: <code>androidComponents { beforeVariants(selector().withFlavor("tier" to "paid")) { it.enable = buildType != "debug" } }</code> skips building paid-debug entirely.</p>`,
  },
  {
    id: "a17-gradle-ci-4",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `What problem does a Gradle version catalog (libs.versions.toml) solve that a plain \`ext\` block of version constants in the root build.gradle did not?`,
    answerHtml: `<p>An <code>ext</code> block only centralizes version <i>strings</i>; every module still hand-writes the group:artifact coordinate, so renames/typos in coordinates aren't caught and there's no IDE autocomplete or type-safety for dependency references.</p><p>A version catalog defines <code>[versions]</code>, <code>[libraries]</code> (group, name, version.ref), and <code>[bundles]</code>/<code>[plugins]</code> in TOML, and Gradle generates a typed accessor object (<code>libs.androidx.core.ktx</code>) consumable from every subproject via the auto-registered <code>libs</code> extension &mdash; giving compile-time-checked, IDE-navigable dependency references and a single source of truth shared by build-logic/convention plugins too.</p>`,
  },
  {
    id: "a17-gradle-ci-5",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `How do you reference a Gradle version catalog from inside a convention plugin (a precompiled script plugin in \`build-logic\`), and what's the gotcha?`,
    answerHtml: `<p>Catalog accessors (<code>libs.*</code>) are generated for scripts in the main build, but a convention plugin lives in an included build (<code>build-logic</code>) with its own settings. You must explicitly expose the catalog there via, in <code>build-logic/settings.gradle.kts</code>: <code>dependencyResolutionManagement { versionCatalogs { create("libs") { from(files("../gradle/libs.versions.toml")) } } }</code>.</p><p>Inside the plugin's <code>build.gradle.kts</code> you then access it through the <code>libs</code> extension on the plugin module itself (not the consuming module), e.g. <code>val libs = the&lt;LibrariesForLibs&gt;()</code> in a precompiled script, or via <code>VersionCatalogsExtension</code> in a binary plugin's <code>apply()</code>.</p>`,
  },
  {
    id: "a17-gradle-ci-6",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `In a multi-module app with 40+ feature modules, what's the convention-plugin pattern for sharing Android/Kotlin build config, and why does it beat applying the same \`build.gradle.kts\` boilerplate per module?`,
    answerHtml: `<p>Create an included build (commonly named <code>build-logic</code>) containing precompiled script plugins, e.g. <code>android-library-convention.gradle.kts</code>, that apply <code>com.android.library</code> + <code>org.jetbrains.kotlin.android</code> and configure <code>compileSdk</code>, <code>minSdk</code>, Compose options, lint, and Kotlin compiler args in one place. Feature modules then apply just <code>id("android-library-convention")</code>.</p><p>This beats copy-pasted boilerplate because: (1) a compileSdk bump is a one-line change instead of 40, (2) the convention plugin is itself a compiled, cacheable Gradle artifact (faster than re-evaluating repeated Groovy/KTS blocks), and (3) it enforces consistency &mdash; a module literally cannot diverge on lint rules or Java target without overriding the plugin.</p>`,
  },
  {
    id: "a17-gradle-ci-7",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `What's the structural difference between \`buildSrc\` and a composite build (\`includeBuild("build-logic")\`) for hosting convention plugins, and why do most modern setups prefer the latter?`,
    answerHtml: `<p><code>buildSrc</code> is a magic, implicitly-included build Gradle always rebuilds and re-checks for changes before configuring the rest of the build &mdash; <b>any</b> change inside it invalidates the configuration cache for the entire build and forces a full recompile of buildSrc itself.</p><p>A composite build via <code>includeBuild("build-logic")</code> (declared in <code>settings.gradle.kts</code>) is a normal, independently buildable Gradle project with its own incremental compilation and better configuration-cache interaction &mdash; only the parts of build-logic actually touched get rebuilt. It also supports splitting into multiple subprojects (e.g. <code>build-logic:convention</code>, <code>build-logic:tooling</code>) which buildSrc cannot do as cleanly.</p>`,
  },
  {
    id: "a17-gradle-ci-8",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `A convention plugin needs to read the app's Kotlin version from the version catalog at plugin-apply time, but \`libs.versions.kotlin.get()\` throws at configuration time intermittently. What's the likely cause and fix?`,
    answerHtml: `<p>This typically happens when the convention plugin's <code>build.gradle.kts</code> accesses the catalog via the generated extension function before Gradle has fully wired the catalog into that project (common when the catalog file path is wrong or the plugin module isn't part of the same settings' <code>dependencyResolutionManagement</code>), or when mixing a <i>version</i> reference with a <i>library</i> alias incorrectly.</p><p>Fix: confirm <code>build-logic/settings.gradle.kts</code> points <code>from(files("../gradle/libs.versions.toml"))</code> at the real root-relative path, and prefer injecting the catalog via a typed accessor (<code>val libs = extensions.getByType&lt;VersionCatalogsExtension&gt;().named("libs")</code>) inside <code>apply()</code> rather than relying on the precompiled-script <code>libs.versions.x.get()</code> sugar, which depends on the script being compiled as a precompiled script plugin with catalog support enabled.</p>`,
  },
  {
    id: "a17-gradle-ci-9",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `What exactly does Gradle's build cache (local/remote) cache, and why doesn't enabling it help a task that has no declared \`@Input\`/\`@OutputFile\` annotations?`,
    answerHtml: `<p>The build cache stores task <b>outputs</b> keyed by a hash of all declared inputs (source files, classpath, plugin/task implementation version, and every property annotated <code>@Input</code>/<code>@InputFiles</code>/etc.). On a cache hit, Gradle copies the cached outputs instead of executing the task.</p><p>A custom task with undeclared inputs/outputs is invisible to the cache mechanism &mdash; Gradle has no key to hash and no outputs to store/restore, so it always executes (and worse, can produce <i>incorrect</i> incremental-build behavior since Gradle also uses these annotations for up-to-date checks). Every custom task must explicitly annotate its inputs/outputs (or implement <code>@CacheableTask</code> with properly classified path-sensitivity) to participate.</p>`,
  },
  {
    id: "a17-gradle-ci-10",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `What's the difference between the build cache and the configuration cache, and can a task be eligible for one but not the other?`,
    answerHtml: `<p>The <b>build cache</b> skips task <i>execution</i> by reusing previously produced outputs (keyed by task inputs) &mdash; it operates per-task, after the task graph is built. The <b>configuration cache</b> skips the entire <i>configuration phase</i> (evaluating build scripts, applying plugins, building the task graph) by serializing that result to disk and replaying it on the next build, valid only if no build-script-affecting input changed.</p><p>Yes &mdash; a task can be build-cacheable but break configuration-cache compatibility (e.g. it captures a <code>Project</code> reference or does I/O at configuration time inside <code>doLast {}</code> incorrectly), and vice versa: a task can be configuration-cache-safe but not declared <code>@CacheableTask</code> so it always re-executes even with a cache hit on configuration.</p>`,
  },
  {
    id: "a17-gradle-ci-11",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `Why does a \`Task.doLast { project.someExtension.value }\` lambda break Gradle's configuration cache, and what's the fix?`,
    answerHtml: `<p>The configuration cache serializes the task graph and any captured lambda state to disk after configuration, then deserializes and re-runs only the execution phase on subsequent builds &mdash; it never re-runs project configuration. A <code>doLast</code> lambda that closes over <code>project</code> (or any live <code>Project</code>/<code>Task</code> object) can't be safely serialized, since <code>Project</code> isn't meant to be touched during execution &mdash; Gradle throws an "invocation of Project at execution time" violation.</p><p>Fix: capture only the needed primitive/serializable value into a local val at configuration time (e.g. <code>val value = project.someExtension.value; doLast { use(value) }</code>), or use the Provider API (<code>Property&lt;T&gt;</code>/<code>Provider&lt;T&gt;</code>) so the value is lazily resolved without holding a live project reference.</p>`,
  },
  {
    id: "a17-gradle-ci-12",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `Your team enabled \`org.gradle.configuration-cache=true\` and CI builds got slower on the first run of every new branch, but faster after. Why, and is this expected?`,
    answerHtml: `<p>Expected. The configuration cache has to be <i>populated</i> (a full configuration-phase run plus serialization to <code>.gradle/configuration-cache/</code>) before it can be <i>reused</i>. A fresh CI checkout/branch typically has different cache-key inputs (changed build scripts, different env vars, a fresh <code>.gradle</code> dir if the cache isn't persisted between runs), so the first build pays the normal configuration cost plus serialization overhead.</p><p>To actually benefit on CI you must persist <code>~/.gradle/caches</code> (or the project's <code>.gradle</code> dir) between runs &mdash; e.g. via <code>actions/cache</code> keyed on <code>*.gradle.kts</code>/<code>libs.versions.toml</code> hashes &mdash; otherwise every CI run is a cold cache and you pay the serialization tax with no reuse benefit.</p>`,
  },
  {
    id: "a17-gradle-ci-13",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `What is the Gradle Build Cache's "task implementation version" component of a cache key, and why can upgrading AGP silently invalidate your entire remote cache?`,
    answerHtml: `<p>Part of a task's cache key includes a hash of the task's <i>implementation class bytecode</i> (and its action classes), not just its declared inputs &mdash; this is how Gradle avoids serving stale outputs after the task's logic itself changes.</p><p>Bumping the Android Gradle Plugin version changes the bytecode of nearly every built-in Android task (<code>compile*Kotlin</code>, <code>merge*Resources</code>, etc.), so every cache key changes simultaneously. The remote cache isn't corrupted &mdash; it's just a guaranteed 100% miss rate for one build immediately after an AGP upgrade, until the new keys repopulate it.</p>`,
  },
  {
    id: "a17-gradle-ci-14",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `A module declares a dependency on \`project(":core:network")\` only to call one utility function. What Gradle/AGP mechanism punishes this for incremental builds, and how do convention-plugin-based modularization strategies mitigate it?`,
    answerHtml: `<p>Gradle's task graph invalidates and recompiles a module whenever any of its dependencies' <i>API</i> (not just implementation) surface changes &mdash; and more importantly, changing any file in <code>:core:network</code> triggers re-running affected tasks transitively up the dependency graph for every module that depends on it, even if the change is unrelated to the one function used.</p><p>Mitigation: (1) use <code>implementation</code> instead of <code>api</code> in module dependency declarations so a module's internal changes don't leak ABI changes to dependents, shrinking the invalidation graph; (2) split coarse modules into narrower ones (e.g. extract just the needed utility into its own tiny module) so unrelated changes in the rest of <code>:core:network</code> don't touch you; (3) convention plugins lower the friction of splitting modules finely enough to limit blast radius by making a new module cheap to create (one <code>id("android-library-convention")</code> line).</p>`,
  },
  {
    id: "a17-gradle-ci-15",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `What does \`org.gradle.parallel=true\` actually parallelize, and what's the common pitfall where enabling it shows no speedup on a project with 50 modules?`,
    answerHtml: `<p>It allows Gradle to execute tasks from <i>different projects</i> (modules) concurrently, using worker threads up to <code>--max-workers</code> (defaults to CPU core count) &mdash; tasks within a single project still run sequentially, and tasks are still constrained by their declared dependency order in the task graph.</p><p>The common pitfall: if most modules ultimately funnel through a single bottleneck module (e.g. every feature module depends on one giant <code>:core:ui</code> that must fully compile first), the task graph has a long critical path with little real parallel opportunity regardless of worker count &mdash; parallelism only helps where the dependency graph is actually wide, not deep. Fixing this is a modularization problem (flattening the dependency graph), not a Gradle-flag problem.</p>`,
  },
  {
    id: "a17-gradle-ci-16",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `Why is \`kapt\` considered a build-performance liability compared to KSP, and what specifically about kapt's design defeats Gradle's incremental compilation?`,
    answerHtml: `<p><code>kapt</code> works by first generating Java stub sources from your Kotlin code so existing Java-based annotation processors (Dagger, Room, etc.) can run against them, then running those processors, then compiling the real Kotlin. The stub-generation phase alone is a slow, mostly non-incremental pass, and many annotation processors used through kapt aren't isolating/incremental-aware, so kapt often falls back to reprocessing the whole module on any change.</p><p>KSP (Kotlin Symbol Processing) works directly against Kotlin's compiler symbols/AST &mdash; no Java stub generation step &mdash; and was designed with incremental processing as a first-class concern, so equivalent processors (Room KSP, Dagger/Hilt's KSP mode) skip the stub tax entirely and report finer-grained incremental change sets.</p>`,
  },
  {
    id: "a17-gradle-ci-17",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `What's a Gradle Build Scan, and what specific information does it surface that local \`--profile\` output doesn't?`,
    answerHtml: `<p>A Build Scan (<code>--scan</code>, via the Develocity/Gradle Enterprise integration) uploads a detailed, shareable report of a single build: per-task timing, cache hit/miss reasons (e.g. "input property changed"), the exact reason a task was <code>NOT</code> up-to-date, dependency resolution time per configuration, and configuration-cache problem reports with stack traces.</p><p>The local <code>--profile</code> HTML report gives coarse timing buckets (configuration vs execution vs total) but not <i>why</i> a specific task missed cache or was re-run &mdash; Build Scans expose the actual cache-key-mismatch diagnostics (which input changed, by hash) needed to debug "why is this always re-running" issues, which is invaluable when chasing CI build-time regressions across a team.</p>`,
  },
  {
    id: "a17-gradle-ci-18",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `In a \`libs.versions.toml\`, what's the difference between declaring a library as \`module = "group:artifact"\` versus the split \`group = "..."\` / \`name = "..."\` keys, and when would you use a \`bundle\`?`,
    answerHtml: `<p><code>module</code> is shorthand for the combined <code>group:name</code> coordinate in one string; the split form is equivalent but sometimes preferred for readability or when generating entries programmatically. Both ultimately resolve to the same <code>Library</code> alias.</p><p>A <code>[bundles]</code> entry groups several library aliases under one name (e.g. <code>compose = ["compose-ui", "compose-material3", "compose-tooling"]</code>) so consumers write <code>implementation(libs.bundles.compose)</code> instead of listing each artifact &mdash; useful for a cohesive set of libraries (like Compose's multi-artifact surface) that modules almost always need together.</p>`,
  },
  {
    id: "a17-gradle-ci-19",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `Why does declaring \`id("com.android.application") version "8.x" apply false\` in the root \`build.gradle.kts\` matter for convention plugins, and what breaks if you omit \`apply false\`?`,
    answerHtml: `<p>The root-level <code>plugins {}</code> block with versions (often itself sourced from the version catalog's <code>[plugins]</code> table) declares the plugin <i>version</i> once so every subproject (or convention plugin) that later does <code>id("com.android.application")</code> without a version resolves consistently &mdash; this is the modern replacement for the old <code>buildscript { classpath ... }</code> approach. <code>apply false</code> means "make this version available for resolution, but don't actually apply the plugin to the root project itself."</p><p>Omitting <code>apply false</code> applies the Android plugin directly to the root project, which usually doesn't have an Android source layout, causing it to fail configuration (or pollute the root project with Android tasks it shouldn't have) &mdash; in a convention-plugin setup, the version still has to resolve exactly once at the settings/root level for \`build-logic\`-declared convention plugins to stay consistent.</p>`,
  },
  {
    id: "a17-gradle-ci-20",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `Two build variants, \`prodPaidRelease\` and \`prodFreeRelease\`, both reference \`BuildConfig.API_BASE_URL\`. Where do you define a per-variant \`buildConfigField\`, and what determines which one wins if both a flavor and a build type set the same field?`,
    answerHtml: `<p><code>buildConfigField</code> is set per flavor or build type inside the <code>android { productFlavors { paid { buildConfigField("String", "API_BASE_URL", "\\"https://paid.api\\"") } } }</code> / <code>buildTypes { release { ... } }</code> blocks.</p><p>When the same field is set at multiple levels, the more specific source wins using the same precedence as source-set merging: build type overrides flavor, and within flavors, dimension order (as declared in <code>flavorDimensions</code>) determines priority &mdash; the variant-specific merge always resolves to exactly one value per key, since later declarations in the merge order win deterministically.</p>`,
  },
  {
    id: "a17-gradle-ci-21",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `What's the purpose of \`isMinifyEnabled\` + R8 interacting with product flavors, and why might a \`paid\` flavor need its own ProGuard/R8 keep rules file separate from the base \`proguard-rules.pro\`?`,
    answerHtml: `<p>R8 shrinking/obfuscation runs per-variant (it operates on the merged, variant-specific bytecode), and rule files are additive across the merged source sets &mdash; <code>android { buildTypes { release { proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro") } } }</code> applies the base file to every release variant.</p><p>A flavor-specific rules file (declared via <code>productFlavors { paid { proguardFile("proguard-paid-rules.pro") } } }</code>) is needed when that flavor pulls in extra reflection-based libraries (e.g. a paid-only SDK using reflection/serialization) that the free flavor doesn't have &mdash; keeping those keep-rules scoped to the flavor that needs them avoids bloating the free variant's keep set (which would work against minification's whole purpose) while still shrinking aggressively where it's safe to.</p>`,
  },
  {
    id: "a17-gradle-ci-22",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `What does \`org.gradle.configureondemand=true\` do, and why is it generally discouraged in favor of just enabling the configuration cache?`,
    answerHtml: `<p>Configure-on-demand attempts to configure only the projects relevant to the requested tasks rather than every project in the build, reducing configuration time for large multi-module builds &mdash; but it's explicitly documented as <i>incubating/risky</i> for builds with cross-project configuration dependencies (e.g. a convention plugin or root script that configures subprojects based on another subproject's state), where it can produce incorrect results.</p><p>The configuration cache achieves a similar (often better) win more safely: it doesn't selectively skip configuration logic, it <i>caches the entire result</i> of a full, correct configuration pass and replays it &mdash; so it doesn't have configure-on-demand's correctness footguns around cross-project ordering, and Google now recommends it over configure-on-demand for AGP/Gradle 8+ projects.</p>`,
  },
  {
    id: "a17-gradle-ci-23",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `When two feature modules both depend on \`:core:design-system\` with \`api(libs.coil)\`, what downstream problem does this \`api\` usage cause, and why would a convention plugin lean teams toward \`implementation\` by default?`,
    answerHtml: `<p>An <code>api</code> dependency leaks onto the <i>compile classpath</i> of every module that depends on <code>:core:design-system</code>, meaning every feature module transitively gets Coil on its compile classpath whether it uses it directly or not &mdash; this both (1) widens the recompilation blast radius (a Coil version bump now potentially affects every dependent module's compile task, not just design-system's), and (2) can mask accidental direct usage of Coil APIs in feature modules that should be going through a design-system abstraction.</p><p>A convention plugin can't force call sites to choose <code>implementation</code> vs <code>api</code> directly, but teams often add a lint/dependency-analysis step (e.g. Dependency Analysis Gradle Plugin) into the shared convention to flag unused/misdeclared <code>api</code> dependencies, nudging modules toward the narrower <code>implementation</code> scope by default.</p>`,
  },
  {
    id: "a17-gradle-ci-24",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `What's the practical difference between \`gradle.properties\`' \`org.gradle.jvmargs=-Xmx4g\` and the \`org.gradle.workers.max\` setting, and which one would you tune first when CI runners are OOM-killing Gradle daemons?`,
    answerHtml: `<p><code>org.gradle.jvmargs</code> sets heap/JVM flags for the Gradle <i>daemon process itself</i> (where configuration and most build logic runs). <code>org.gradle.workers.max</code> caps how many parallel worker processes/threads Gradle spawns for parallel task execution (interacting with <code>org.gradle.parallel</code>) &mdash; each worker may itself be a separate JVM (e.g. for <code>javac</code>/<code>kotlinc</code> workers) with its own memory footprint.</p><p>When CI runners OOM, the fix is usually two-pronged: lower <code>org.gradle.workers.max</code> to match the runner's actual core/memory budget (too many parallel JVM workers on a memory-constrained runner is the classic OOM cause), and right-size <code>-Xmx</code> for the daemon itself &mdash; raising daemon heap alone doesn't help if the real pressure comes from N parallel compiler worker JVMs each requesting their own heap.</p>`,
  },
];

export const ADVANCED17_QUIZ: QuizQuestion[] = [
  {
    id: "a17q-gradle-ci-1",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `A project has flavor dimensions \`environment\` (dev, staging, prod) and \`tier\` (free, paid), plus the standard debug/release build types. How many total build variants does Gradle generate?`,
    options: [`6`, `9`, `12`, `18`],
    answer: 2,
    explanationHtml: `<p>3 environment flavors x 2 tier flavors = 6 flavor combinations, x 2 build types = <b>12 total variants</b>. Variant count is always the cartesian product across every dimension plus build types.</p>`,
  },
  {
    id: "a17q-gradle-ci-2",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `Why does \`buildSrc\` tend to be more disruptive to build performance on large projects than a composite build (\`includeBuild\`) hosting the same convention plugins?`,
    options: [`buildSrc cannot define precompiled script plugins, only binary plugins`, `Any change inside buildSrc forces Gradle to fully rebuild it and invalidates the configuration cache for the whole build, whereas a composite build only rebuilds the parts actually touched`, `buildSrc plugins cannot access the version catalog at all`, `Composite builds run in a separate JVM with more allocated heap by default`],
    answer: 1,
    explanationHtml: `<p><code>buildSrc</code> is implicitly included and Gradle always checks/rebuilds it before configuring the rest of the build, so any edit there invalidates the configuration cache globally. A composite build (<code>includeBuild("build-logic")</code>) is incrementally compiled like any other project, so only the touched parts get rebuilt — this is the main reason most modern multi-module setups moved off buildSrc.</p>`,
  },
  {
    id: "a17q-gradle-ci-3",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `What does the Gradle build cache use as part of a task's cache key, beyond the task's declared \`@Input\`/\`@InputFiles\` values?`,
    options: [`Only the Gradle version, never the task's own code`, `A hash of the task implementation's bytecode (and its actions), so upgrading the plugin that defines the task changes the key`, `The wall-clock time the task last ran`, `The developer's machine hostname, to scope caching per-developer`],
    answer: 1,
    explanationHtml: `<p>The cache key includes a hash of the task implementation class (and associated action classes), not just declared inputs. This is why bumping AGP — which changes the bytecode of built-in tasks like <code>compile*Kotlin</code> or <code>merge*Resources</code> — causes a one-time, build-wide cache miss across the whole project until new keys repopulate the cache.</p>`,
  },
  {
    id: "a17q-gradle-ci-4",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `A custom Gradle task has no \`@Input\`/\`@OutputFile\` annotations on its properties. What is the direct consequence for build/configuration caching?`,
    options: [`The task is cached using its class name alone as the key`, `Gradle infers inputs automatically from the task's constructor parameters`, `The task is invisible to the cache mechanism — there's no key to hash and no outputs to store, so it always executes and incremental up-to-date checks are unreliable`, `The task runs once and is cached forever regardless of changes`],
    answer: 2,
    explanationHtml: `<p>Gradle's incremental build and caching machinery depends entirely on annotated inputs/outputs. Without them, the task has no cache key and nothing registered to restore, so Gradle must execute it on every run, and its up-to-date checks can't be trusted either.</p>`,
  },
  {
    id: "a17q-gradle-ci-5",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `Why is KSP generally faster than kapt for processors like Room or Dagger/Hilt when available?`,
    options: [`KSP runs entirely on a remote build cache server by default`, `kapt must first generate Java stub sources from Kotlin code before running annotation processors, an extra largely non-incremental pass that KSP skips by working directly against Kotlin compiler symbols`, `KSP disables incremental compilation, trading correctness for raw speed`, `kapt and KSP perform identically; the difference is only in IDE support`],
    answer: 1,
    explanationHtml: `<p>kapt's stub-generation step exists so Java-based annotation processors can run against Kotlin code, but it's slow and many wrapped processors aren't incremental-aware. KSP processes Kotlin's own symbols/AST directly, with incremental processing designed in from the start, so equivalent KSP-based processors avoid the stub tax.</p>`,
  },
  {
    id: "a17q-gradle-ci-6",
    category: "gradle",
    categoryLabel: "Gradle",
    question: `What specifically does enabling Gradle's configuration cache skip on a cache hit?`,
    options: [`Task execution, by reusing previously produced task outputs`, `The entire configuration phase — evaluating build scripts, applying plugins, and building the task graph — by replaying a serialized result instead`, `Dependency resolution only, while still re-running all build scripts`, `Only the version catalog parsing step`],
    answer: 1,
    explanationHtml: `<p>The configuration cache serializes the outcome of the configuration phase (script evaluation, plugin application, task graph construction) and replays it directly on a hit, skipping that phase entirely. This is distinct from the build cache, which skips task execution by reusing outputs keyed on task inputs.</p>`,
  },
];
