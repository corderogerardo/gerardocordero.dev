// Batch 12 — WorkManager (constraints, chaining, ExistingWorkPolicy, expedited work), Services & foreground services, background execution limits, and Android 13+/14 behavior changes.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED12_FLASHCARDS: Flashcard[] = [
  {
    id: "a12-workmanager-1",
    category: "framework",
    categoryLabel: "Framework",
    question: `An app calls a OneTimeWorkRequest with setExpedited() but the system can't grant immediate quota. What happens?`,
    answerHtml: `<p>WorkManager does not fail the enqueue. On API 31+, expedited work maps to native JobScheduler expedited jobs, which run subject to a rolling per-app quota (similar to <code>JobScheduler</code>'s <b>EJ quota</b>); if quota is exhausted the job is deferred and runs as soon as quota replenishes, no notification required. On API 23-30, WorkManager emulates expedited work by briefly starting a synthetic foreground service when the app is in a state allowed to start one, falling back to a regular background job otherwise.</p>`,
  },
  {
    id: "a12-workmanager-2",
    category: "framework",
    categoryLabel: "Framework",
    question: `What is the practical difference between ExistingWorkPolicy.APPEND and APPEND_OR_REPLACE for a uniquely-named work chain?`,
    answerHtml: `<p><b>APPEND</b> queues the new request behind the existing chain, but if the chain's last unit already finished as <code>FAILED</code> or <code>CANCELLED</code>, the newly appended work inherits that terminal state and never runs. <b>APPEND_OR_REPLACE</b> behaves identically when the chain is healthy or still running, but discards a chain whose tail failed/was cancelled and starts a brand-new chain instead of propagating the failure.</p>`,
  },
  {
    id: "a12-workmanager-3",
    category: "framework",
    categoryLabel: "Framework",
    question: `With ExistingPeriodicWorkPolicy.UPDATE, what happens to the next scheduled run when you only change a periodic work's constraints, not its interval?`,
    answerHtml: `<p>The next run time is preserved — <code>UPDATE</code> (added in WorkManager 2.8) swaps in the new constraints/input data without resetting the periodic anchor, so the cadence the user already experienced keeps going. This matters because re-enqueuing with <code>REPLACE</code> would cancel and restart the periodic timer, silently delaying the next execution by a full interval.</p>`,
  },
  {
    id: "a12-workmanager-4",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why can't a PeriodicWorkRequest be chained with .then() the way OneTimeWorkRequests can?`,
    answerHtml: `<p>Periodic work is modeled as a recurring, standalone unit of work with no defined completion point, so WorkManager's <code>WorkContinuation</code> API (which depends on a request finishing to trigger the next) doesn't apply to it. To chain follow-up work off a periodic run, you have the periodic Worker itself enqueue a separate OneTimeWorkRequest from within <code>doWork()</code>.</p>`,
  },
  {
    id: "a12-workmanager-5",
    category: "framework",
    categoryLabel: "Framework",
    question: `A Worker's doWork() throws an uncaught exception. How does this differ from returning Result.failure()?`,
    answerHtml: `<p>WorkManager catches the exception internally and treats it the same as <code>Result.failure()</code> for propagation purposes — downstream chained work does not run, and a periodic request simply waits for its next scheduled run. The exception does <b>not</b> crash the host app or process; instead it's logged as an error, which is the catch: an unhandled exception fails silently from the user's perspective and won't show up in crash reporting unless you explicitly catch expected failure cases and return <code>Result.failure()</code>/<code>Result.retry()</code> deliberately.</p>`,
  },
  {
    id: "a12-workmanager-6",
    category: "framework",
    categoryLabel: "Framework",
    question: `What constraint-evaluation gotcha exists with NetworkType.CONNECTED versus NetworkType.UNMETERED on a device that's on a metered hotspot but reports a Wi-Fi connection?`,
    answerHtml: `<p>Android determines "unmetered" via <code>NetworkCapabilities.NET_CAPABILITY_NOT_METERED</code>, which reflects what the network actively advertises (e.g. a tethered hotspot can flag itself metered even over what looks like Wi-Fi) or what the user manually marked. WorkManager just observes that capability bit — it has no independent signal — so a request gated on <code>UNMETERED</code> can correctly stay deferred on a Wi-Fi SSID the user marked as a metered network in system settings.</p>`,
  },
  {
    id: "a12-workmanager-7",
    category: "framework",
    categoryLabel: "Framework",
    question: `How does WorkManager decide which constraints-tracking implementation to use across API levels, and why does this matter for battery?`,
    answerHtml: `<p>On API 23+, WorkManager constraint tracking (network, charging, battery, storage) is backed by <code>JobScheduler</code>, which the OS already runs centrally and batches across all apps. Below API 23, WorkManager falls back to a custom implementation (broadcast receivers, content observers, and <code>AlarmManager</code>) to poll constraint state, which is less battery-efficient since it isn't coalesced with the system's own scheduling.</p>`,
  },
  {
    id: "a12-workmanager-8",
    category: "framework",
    categoryLabel: "Framework",
    question: `Your foreground service in onStartCommand() calls startForeground() but the notification channel's importance is IMPORTANCE_MIN. What's the practical effect on Android 12+?`,
    answerHtml: `<p>The service still legally satisfies the foreground requirement — <code>startForeground()</code> succeeds and the process gets foreground priority — but on Android 12+, a low-importance channel lets the system delay showing the persistent notification UI for a few seconds and may collapse it, while the underlying foreground-service guarantees (process priority, exemption from background limits) remain unaffected by channel importance. Importance affects notification visibility/sound, not whether the service is legitimately "foreground" for OS scheduling purposes.</p>`,
  },
  {
    id: "a12-workmanager-9",
    category: "framework",
    categoryLabel: "Framework",
    question: `Starting in Android 14 (API 34), what changed about declaring a foreground service type in the manifest?`,
    answerHtml: `<p>Declaring <code>android:foregroundServiceType</code> in the manifest <code>&lt;service&gt;</code> entry became <b>mandatory</b> for apps targeting API 34, not just recommended. Each type (e.g. <code>dataSync</code>, <code>mediaPlayback</code>, <code>location</code>, <code>camera</code>, <code>microphone</code>, <code>connectedDevice</code>, <code>health</code>, <code>remoteMessaging</code>, <code>systemExempted</code>, <code>shortService</code>) also gained its own usage restrictions and, for several types, a required permission (e.g. <code>FOREGROUND_SERVICE_DATA_SYNC</code>); omitting the type or using a mismatched one throws at runtime on API 34+.</p>`,
  },
  {
    id: "a12-workmanager-10",
    category: "framework",
    categoryLabel: "Framework",
    question: `What is a 'short service' foreground service type introduced in Android 14, and what's its hard limit?`,
    answerHtml: `<p><code>FOREGROUND_SERVICE_TYPE_SHORT_SERVICE</code> is a new type for foreground work that's expected to finish quickly and doesn't fit the other typed categories. It's capped at a hard <b>3-minute</b> wall-clock limit per start (the clock starts from <code>startForeground()</code>); the OS calls <code>onTimeout()</code> (API 34+) to let the service wrap up, and if it doesn't stop itself within a few seconds the system stops it and raises an ANR. It exists so trivial short-lived foreground needs don't have to misuse a long-running type like <code>dataSync</code>.</p>`,
  },
  {
    id: "a12-workmanager-11",
    category: "framework",
    categoryLabel: "Framework",
    question: `On Android 12+ (API 31), under what circumstances does calling Context.startForegroundService() from the background throw ForegroundServiceStartNotAllowedException?`,
    answerHtml: `<p>Android 12 introduced broad background-start restrictions: if the app is not in one of the allowed states — itself in the foreground, holding a recent foreground/visible activity, executing a broadcast/bound-service callback with an exemption, or covered by a system-allowlist exemption (e.g. alarm, notification action, high-priority FCM message, Companion Device Manager) — calling <code>startForegroundService()</code> throws immediately rather than starting and then being killed. This pushed apps toward <code>WorkManager</code>'s expedited work or user-initiated job APIs for background-triggered urgent work instead of foreground services.</p>`,
  },
  {
    id: "a12-workmanager-12",
    category: "framework",
    categoryLabel: "Framework",
    question: `How do User-Initiated Data Transfer Jobs (UIDT, API 34) interact with foreground services for long downloads?`,
    answerHtml: `<p>Android 14 added <code>JobScheduler.UserInitiatedDataTransferJobBuilder</code>/<code>UserInitiatedJob</code> as a dedicated path for user-initiated network transfers (large downloads/uploads) that need to keep running reliably without the app holding a classic foreground service. These jobs run with elevated priority similar to a foreground service, automatically show user-facing progress in system UI on supported devices, and are meant to replace the older pattern of starting a <code>dataSync</code> foreground service purely to survive Doze/App Standby during a long transfer.</p>`,
  },
  {
    id: "a12-workmanager-13",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why does WorkManager prefer JobScheduler over a plain AlarmManager + BroadcastReceiver approach for deferrable background work on modern Android?`,
    answerHtml: `<p><code>JobScheduler</code> lets the OS batch and coalesce jobs from many apps against shared wake-ups, respects Doze/App Standby buckets natively, and gives constraint evaluation (network, charging, idle) for free at the platform level. An <code>AlarmManager</code>-driven approach wakes the device on the app's own schedule regardless of what else is pending, fighting Doze rather than cooperating with it — which is precisely the battery cost JobScheduler (and by extension WorkManager) was built to eliminate.</p>`,
  },
  {
    id: "a12-workmanager-14",
    category: "framework",
    categoryLabel: "Framework",
    question: `What is App Standby Buckets' effect on a WorkManager job enqueued with no constraints in an app the user hasn't opened in days?`,
    answerHtml: `<p>The app gets placed into a less-frequently-serviced standby bucket (<code>frequent</code>, <code>rare</code>, or <code>restricted</code>) based on usage patterns, and the OS throttles how often <i>any</i> of its jobs — including unconstrained WorkManager work — are allowed to run, down to roughly once a day for the <code>rare</code> bucket and even less for <code>restricted</code>. WorkManager doesn't bypass this; it just queues the request and waits for the OS scheduler to grant it a run window consistent with the app's current bucket.</p>`,
  },
  {
    id: "a12-workmanager-15",
    category: "framework",
    categoryLabel: "Framework",
    question: `What's the difference between WorkManager's setBackoffCriteria() and simply returning Result.retry() without configuring it?`,
    answerHtml: `<p>Every <code>WorkRequest</code> has a default backoff policy (<code>EXPONENTIAL</code>, starting at <code>WorkRequest.MIN_BACKOFF_MILLIS</code> = 10 seconds) even if you never call <code>setBackoffCriteria()</code> explicitly, so a bare <code>Result.retry()</code> still delays and exponentially backs off rather than retrying instantly. Calling <code>setBackoffCriteria()</code> lets you choose <code>LINEAR</code> backoff instead, or raise the initial delay — it does not change whether retry happens, only its timing curve.</p>`,
  },
  {
    id: "a12-workmanager-16",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why might a foreground service with type dataSync still get stopped by the system on Android 15 even though doWork() hasn't finished?`,
    answerHtml: `<p>Android 15 caps <code>dataSync</code>- and <code>mediaProcessing</code>-type foreground services at roughly <b>6 hours of cumulative foreground-service time within a rolling 24-hour window</b> per app (the timer resets if the user brings the app to the foreground). Once that budget is exhausted, the system calls <code>Service.onTimeout(int, int)</code> — a callback introduced in API 35 and not available on Android 14 or lower — and the service stops being treated as foreground regardless of whether work is mid-flight, forcing the app to fall back to a regular (background-restricted) service or reschedule via WorkManager/JobScheduler.</p>`,
  },
  {
    id: "a12-workmanager-17",
    category: "framework",
    categoryLabel: "Framework",
    question: `How does WorkManager guarantee at-least-once execution across a process death mid-doWork(), and what does that imply for Worker idempotency?`,
    answerHtml: `<p>WorkManager persists work state to a Room-backed SQLite database before execution and only marks a request complete after <code>doWork()</code> returns; if the process dies mid-execution the request is left in an enqueued/running state and gets retried by the scheduler on next opportunity. This means a Worker can be invoked more than once for logically "the same" unit of work (partial side effects from the killed run plus a full second run), so Workers must be written to be idempotent — safe to re-run, e.g. via upserts rather than blind inserts.</p>`,
  },
  {
    id: "a12-workmanager-18",
    category: "framework",
    categoryLabel: "Framework",
    question: `What's the distinction between a 'started' foreground service notification and the deferred notification behavior Android 12 introduced for short-lived foreground services?`,
    answerHtml: `<p>Since Android 12, the system can delay showing a foreground service's notification by up to ~10 seconds, giving short-lived foreground bursts a chance to finish before any notification ever appears — effectively avoiding flicker if the service stops in time. Services with action buttons, a <code>mediaPlayback</code>/<code>mediaProjection</code>/<code>phoneCall</code> type, or that opt out via <code>setForegroundServiceBehavior(FOREGROUND_SERVICE_IMMEDIATE)</code> show their notification immediately instead. This is a UX smoothing change, not a way to dodge the foreground-service notification requirement long-term.</p>`,
  },
  {
    id: "a12-workmanager-19",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why does chaining work with WorkContinuation.combine() require all input WorkRequests to be part of the same un-enqueued continuation graph?`,
    answerHtml: `<p><code>combine()</code> builds a dependency DAG by wiring the internal tags/IDs of the continuations passed to it; it needs the requests to still be un-enqueued so it can insert the prerequisite edges before anything runs. If you enqueue requests independently first, WorkManager has already committed them to the database without the combine relationship, so there's no API to retroactively splice a dependency between two already-enqueued work items.</p>`,
  },
  {
    id: "a12-workmanager-20",
    category: "framework",
    categoryLabel: "Framework",
    question: `On Android 13 (API 33), what changed about the POST_NOTIFICATIONS permission and how does it interact with foreground services that must show a notification?`,
    answerHtml: `<p>API 33 made notifications (including a foreground service's mandatory notification) a runtime-requested permission (<code>POST_NOTIFICATIONS</code>) rather than granted automatically at install. If the user denies it, the foreground service still starts and runs with foreground priority — the OS does not block <code>startForeground()</code>, and no permission is required just to launch a foreground service — but the notification itself is suppressed from the notification drawer (it remains visible in the system's Task Manager / active-services list), so the app keeps its elevated priority without the notification-shade disclosure the system normally guarantees.</p>`,
  },
  {
    id: "a12-workmanager-21",
    category: "framework",
    categoryLabel: "Framework",
    question: `What's the functional difference between WorkManager's ArrayCreatingInputMerger and the default OverwritingInputMerger when multiple prerequisite Workers feed one dependent Worker?`,
    answerHtml: `<p><code>OverwritingInputMerger</code> (the default) merges all prerequisite outputs into one <code>Data</code> object where, on key collisions, the last-processed prerequisite's value silently wins. <code>ArrayCreatingInputMerger</code> instead detects collisions and converts colliding keys into arrays of all the values supplied for that key across prerequisites, so no prerequisite's output is dropped — useful when several parallel Workers can legitimately produce values for the same key.</p>`,
  },
  {
    id: "a12-workmanager-22",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why is WorkManager not a suitable replacement for a foreground service that plays continuous audio in the background?`,
    answerHtml: `<p>WorkManager schedules <i>deferrable, guaranteed-eventually</i> background work — even expedited work targets near-term execution, not indefinite, continuous, time-critical operation, and plain Workers are subject to execution-window limits (roughly 10 minutes per run, unless explicitly promoted to a long-running foreground-backed Worker). Continuous media playback needs a true <code>mediaPlayback</code>-type foreground service so the OS keeps the process alive and unthrottled for as long as audio is actively playing, which is outside WorkManager's contract entirely.</p>`,
  },
  {
    id: "a12-workmanager-23",
    category: "framework",
    categoryLabel: "Framework",
    question: `How does Android 14's restriction on starting foreground services from a BOOT_COMPLETED broadcast receiver affect apps that auto-start sync work on boot?`,
    answerHtml: `<p>Android 14 specifically disallows launching a <b>microphone</b>-type foreground service directly out of a <code>BOOT_COMPLETED</code> receiver (calling <code>startForegroundService()</code> there for that type throws <code>ForegroundServiceStartNotAllowedException</code>); the same restriction was extended to <code>dataSync</code>, <code>camera</code>, <code>mediaPlayback</code>, <code>phoneCall</code>, and <code>mediaProjection</code> starting with <b>Android 15</b>. Apps needing post-boot sync should enqueue WorkManager work (or a permitted job) from the boot receiver instead, letting the OS scheduler decide when conditions allow it to actually run — this is the safer pattern regardless of target SDK.</p>`,
  },
  {
    id: "a12-workmanager-24",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why can a periodic WorkRequest's flex interval cause two consecutive runs to land closer together than the nominal repeat interval suggests?`,
    answerHtml: `<p>The flex interval defines a window at the <i>end</i> of each repeat interval during which the work is eligible to run (e.g. a 15-minute interval with a 5-minute flex can fire anytime in the last 5 minutes of that window). If the system happens to execute right at the start of one window's flex range and again near the end of the next, the actual gap between runs can be noticeably shorter than the full repeat interval, even though each run still respects its own window.</p>`,
  },
];

export const ADVANCED12_QUIZ: QuizQuestion[] = [
  {
    id: "a12q-workmanager-1",
    category: "framework",
    categoryLabel: "Framework",
    question: `An app targeting API 34 starts a foreground service for a large file upload but declares android:foregroundServiceType="dataSync" without adding the FOREGROUND_SERVICE_DATA_SYNC permission. What happens at runtime on a device running Android 14?`,
    options: [`The service starts normally; the permission is only checked at compile time`, `startForeground() throws a SecurityException-style failure because the required type permission is missing`, `The OS silently downgrades the service to type "shortService" with a 3-minute cap`, `WorkManager intercepts the call and reschedules it as expedited work instead`],
    answer: 1,
    explanationHtml: `<p>Starting with API 34, several foreground service types require a matching manifest permission (e.g. <code>dataSync</code> needs <code>FOREGROUND_SERVICE_DATA_SYNC</code>). Declaring the type without the permission causes the foreground-service start to fail at runtime rather than silently substituting a different type.</p>`,
  },
  {
    id: "a12q-workmanager-2",
    category: "framework",
    categoryLabel: "Framework",
    question: `A unique periodic WorkRequest named "daily-sync" is already enqueued and running fine. The app re-enqueues it with the same name using ExistingPeriodicWorkPolicy.KEEP, but with a different network constraint. What happens?`,
    options: [`The existing request is cancelled and replaced with the new constraint`, `WorkManager merges the constraints, requiring both the old and new conditions`, `The new enqueue call is a no-op; the original request and its constraints keep running unchanged`, `The app crashes with an IllegalStateException for duplicate unique names`],
    answer: 2,
    explanationHtml: `<p><code>KEEP</code> means: if work with this unique name already exists, ignore the new request entirely and leave the existing one (and its original constraints/schedule) untouched. This is the safe default for idempotent "ensure this periodic job exists" call sites, as opposed to <code>REPLACE</code> or <code>UPDATE</code>.</p>`,
  },
  {
    id: "a12q-workmanager-3",
    category: "framework",
    categoryLabel: "Framework",
    question: `Which statement about expedited WorkRequests on API 31+ (Android 12+) is correct?`,
    options: [`Expedited work always requires the app to show a foreground-service notification`, `Expedited work runs immediately and is exempt from any execution quota`, `Expedited work uses JobScheduler's native expedited jobs and is bound by a rolling per-app execution-time quota`, `Expedited work can only be scheduled while the app's process is already in the foreground`],
    answer: 2,
    explanationHtml: `<p>On API 31+, <code>setExpedited()</code> maps to JobScheduler's native expedited jobs (<code>EJ</code>), which run with elevated priority and reduced Doze deferral but are still bounded by a quota the OS tracks per app, refilled over time. No persistent foreground notification is required on API 31+ unlike the API 23-30 emulation path.</p>`,
  },
  {
    id: "a12q-workmanager-4",
    category: "framework",
    categoryLabel: "Framework",
    question: `A Worker's doWork() needs roughly 25 minutes to complete a one-shot task on a typical Android device. What is the most correct way to handle this with WorkManager?`,
    options: [`Just return Result.success() after starting the work asynchronously and let it continue past doWork() returning`, `Increase the WorkRequest's backoff delay to 25 minutes so the system waits before timing it out`, `Use setExpedited() so the system grants unlimited execution time for urgent work`, `Break the work into checkpointed chunks, or promote to a long-running foreground service via setForegroundAsync(), since plain WorkManager execution windows are capped at around 10 minutes`],
    answer: 3,
    explanationHtml: `<p>WorkManager enforces an execution time limit per run (about 10 minutes by default, enforced by the underlying JobScheduler execution window) before the system reclaims the wakelock and stops the job. For genuinely long, continuous work, a Worker should promote itself to a long-running foreground-backed Worker via <code>setForegroundAsync()</code>/<code>CoroutineWorker.setForeground()</code>, or the work should be checkpointed and resumed across multiple WorkManager runs.</p>`,
  },
  {
    id: "a12q-workmanager-5",
    category: "framework",
    categoryLabel: "Framework",
    question: `Why does WorkManager require explicit ExistingWorkPolicy handling for unique work names, rather than simply allowing duplicate unique names to coexist?`,
    options: [`Because Room (WorkManager's storage) enforces a uniqueness constraint that throws on duplicate names by default`, `Because allowing silent duplicates would risk the same logical job (e.g. "sync now") being scheduled redundantly every time a UI event re-triggers the enqueue call, wasting battery and causing race conditions`, `Because Android's JobScheduler itself rejects duplicate job IDs at the OS level`, `Because unique work names are only meaningful for periodic work, not one-time work`],
    answer: 1,
    explanationHtml: `<p>Unique work names exist specifically so call sites that may fire repeatedly (e.g. a button tap, a lifecycle callback, a periodic trigger) can declare intent — keep, replace, or append — about what to do with an already-pending or already-running instance of the same logical job, avoiding accidental duplicate scheduling.</p>`,
  },
  {
    id: "a12q-workmanager-6",
    category: "framework",
    categoryLabel: "Framework",
    question: `On Android 13+, an app in the RESTRICTED App Standby Bucket (due to user inactivity) enqueues a OneTimeWorkRequest with no constraints. What's the realistic execution behavior?`,
    options: [`The work runs immediately regardless of bucket, since WorkManager bypasses App Standby entirely`, `The work is dropped permanently and never executes`, `The work is deferred and throttled to a limited number of execution windows per day, consistent with the restricted bucket's job-execution allowance`, `The work is automatically promoted to expedited priority to compensate for the restriction`],
    answer: 2,
    explanationHtml: `<p>App Standby Buckets throttle how often the OS lets an app run jobs (including WorkManager-backed JobScheduler jobs) based on usage recency; the <code>restricted</code> bucket is the most aggressive, limiting job execution to roughly once a day or less. WorkManager doesn't escape this — it queues the request and waits for the OS to grant an execution window per the app's current bucket.</p>`,
  },
  {
    id: "a12q-workmanager-7",
    category: "framework",
    categoryLabel: "Framework",
    question: `What's the correct mental model for why a foreground service's android:foregroundServiceType="shortService" exists separately from other typed categories on Android 14?`,
    options: [`It's a legacy alias kept only for backward compatibility with API 30 apps`, `It lets short-lived foreground work avoid claiming a long-running type (like dataSync or mediaPlayback) it doesn't actually need, while enforcing a hard 3-minute cap so it can't be abused as an unbounded background-execution loophole`, `It grants the highest possible process priority, higher than any other foreground service type`, `It exempts the service from needing any notification at all, even after 10 seconds`],
    answer: 1,
    explanationHtml: `<p><code>shortService</code> was added so apps with brief, miscellaneous foreground needs aren't forced to mislabel themselves under a type like <code>dataSync</code> just to start a foreground service. The tradeoff is a strict ~3-minute ceiling per start, with <code>onTimeout()</code> called so the service can wrap up before the OS forcibly stops it (and raises an ANR if it doesn't).</p>`,
  },
];
