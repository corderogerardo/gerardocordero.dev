# Add project specific ProGuard rules here.
# Debug build is used for CI compile verification; release minification is off
# by default (see app/build.gradle.kts) so this file is currently a placeholder.
