// Batch 7 — cache-manager with Redis, HTTP response caching (@CacheInterceptor), query-level caching & invalidation strategies, and identifying slow request paths.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED7_FLASHCARDS: Flashcard[] = [
  {
    id: "a7-caching-perf-1",
    category: "perf",
    categoryLabel: "Performance",
    question: `When you wire up CacheModule.registerAsync with a Redis-backed store (current cache-manager v6/v7 uses a Keyv-based store, e.g. @keyv/redis wrapped in a Keyv instance and passed via the stores option), what actually changes about how @CacheInterceptor and the rest of the cache-manager API behave?`,
    answerHtml: `<p>Nothing at the API level &mdash; <code>cache-manager</code> is storage-agnostic, so swapping from the default in-memory store to Redis is purely a config-level change (passing a <code>Keyv</code> instance backed by <code>@keyv/redis</code> into the <code>stores</code> option of <code>registerAsync</code>). <code>@CacheInterceptor</code> and <code>cacheManager.get/set/del</code> all keep working unmodified. (Older guides reference <code>cache-manager-ioredis-yet</code>, but that package targets the pre-Keyv <code>cache-manager</code> v4/v5 API and is no longer the current integration path for <code>@nestjs/cache-manager</code> v3.x.)</p><p>What does change: keys now live outside the process, so they survive restarts and are shared across every replica behind a load balancer. The in-memory store gives each pod its own cache, which silently fragments hit rates once you scale horizontally &mdash; the main reason teams move to Redis isn't speed, it's <b>consistency across instances</b>.</p>`,
  },
  {
    id: "a7-caching-perf-2",
    category: "perf",
    categoryLabel: "Performance",
    question: `Why does naively enabling @CacheInterceptor globally on a REST API frequently cause cross-user data to leak through the cache?`,
    answerHtml: `<p>The default <code>CacheInterceptor</code> builds its cache key from the request URL (via <code>httpAdapter.getRequestUrl(request)</code>, effectively method + path + query string) for <code>GET</code> requests only. It ignores headers, cookies, and the authenticated principal. If two different users both hit <code>GET /api/profile</code> with the same URL but different auth cookies, the second request is served the <b>first user's cached response</b>.</p><p>Fix: override <code>trackBy()</code> to fold the user/tenant id into the key, or restrict the interceptor to routes that are genuinely public and identical for every caller.</p>`,
  },
  {
    id: "a7-caching-perf-3",
    category: "perf",
    categoryLabel: "Performance",
    question: `What's the difference between overriding trackBy() on @CacheInterceptor versus using @CacheKey() on a route, and when would you reach for each?`,
    answerHtml: `<p><code>@CacheKey('foo')</code> is a static, decorator-level override of the key for one handler &mdash; useful when you want a stable key regardless of query string noise, or want two different routes to intentionally share a cache entry. (Nest's own docs frame <code>@CacheKey</code> primarily for WebSocket/microservice contexts where there's no URL to derive a key from, but the interceptor's <code>trackBy</code> logic honors it for HTTP handlers too.)</p><p>Overriding <code>trackBy(context)</code> in a custom interceptor subclass is the dynamic, per-request hook: it lets you compute the key from anything on the request (user id, tenant id, Accept-Language header, role) and also lets you return <code>undefined</code> to skip caching entirely for a given request (e.g. skip when the user is unauthenticated or when a bypass header is present). Use <code>@CacheKey</code> for static naming, <code>trackBy</code> for conditional/parameterized caching logic.</p>`,
  },
  {
    id: "a7-caching-perf-4",
    category: "perf",
    categoryLabel: "Performance",
    question: `@CacheInterceptor only intercepts GET requests by default. What's the underlying reasoning, and what's the risk if you manually force caching onto a POST endpoint?`,
    answerHtml: `<p>The interceptor's base implementation (<code>isRequestCacheable</code>) checks the request method against an <code>allowedMethods</code> list that defaults to <code>['GET']</code> &mdash; this encodes the HTTP semantic that GET is safe/idempotent and therefore cacheable, while POST/PUT/PATCH/DELETE carry side effects or non-idempotent semantics that caching would silently corrupt (e.g. caching a POST /orders response would return a stale order id for every subsequent identical-looking request).</p><p>If you override the interceptor to also cache POST (some teams do this for expensive POST-as-query endpoints like search), you must build the cache key from the <b>request body</b>, not just the URL, and you take on the responsibility of proving the endpoint has no side effects &mdash; otherwise concurrent requests can read each other's cached side-effect-bearing responses.</p>`,
  },
  {
    id: "a7-caching-perf-5",
    category: "perf",
    categoryLabel: "Performance",
    question: `You set TTL: 30000 in CacheModule.register() globally, but a specific endpoint needs a 5-second TTL. What are the two ways to express that, and which one wins if both are present?`,
    answerHtml: `<p>Either set <code>@CacheTTL(5000)</code> on the handler (overrides the module default for that route only), or call <code>cacheManager.set(key, value, 5000)</code> directly if you're caching manually inside a service rather than via the interceptor.</p><p>The decorator-level <code>@CacheTTL</code> always wins over the module-level default when <code>@CacheInterceptor</code> is in play &mdash; the interceptor resolves TTL per-request by checking handler metadata, falling back to class-level metadata, and only then leaving it unset (in which case the store's own configured default TTL applies on write). There is no TTL inheritance/merging beyond that lookup chain; it's a simple override.</p>`,
  },
  {
    id: "a7-caching-perf-6",
    category: "perf",
    categoryLabel: "Performance",
    question: `Why is 'cache invalidation' for a Redis-backed read-through cache in Nest fundamentally harder than just calling cacheManager.del(key) on write?`,
    answerHtml: `<p><code>cacheManager.del(key)</code> only works when you know the <b>exact key</b> a stale value was stored under. The moment a resource is reachable through multiple cache keys &mdash; <code>GET /users/42</code>, <code>GET /users?role=admin</code> (which includes user 42), <code>GET /teams/7/members</code> (which also includes user 42) &mdash; a single mutation to user 42 has to invalidate (or know about) all of them.</p><p>This is why naive HTTP response caching via <code>@CacheInterceptor</code> degrades badly for resources with multiple access paths: you either under-invalidate (stale reads) or you maintain a tag/key registry per resource (more moving parts) or you switch that data to short TTLs and accept eventual consistency instead of explicit invalidation.</p>`,
  },
  {
    id: "a7-caching-perf-7",
    category: "perf",
    categoryLabel: "Performance",
    question: `Describe a tag-based (a.k.a. secondary-index) invalidation strategy for query-level caching in Redis, and what data structure in Redis makes it practical.`,
    answerHtml: `<p>Instead of inventing a key per query result and hoping you remember them all, you maintain a <b>reverse index</b>: for every cache key you write, also add that key to one or more Redis <code>SET</code>s named after the entities it depends on (e.g. <code>tag:user:42</code> &rarr; contains <code>cache:users:42</code>, <code>cache:teams:7:members</code>, <code>cache:users:list:role=admin</code>).</p><p>On mutation of user 42, you <code>SMEMBERS tag:user:42</code> to get every cache key that depends on it, then pipeline a <code>DEL</code> across all of them, then <code>DEL tag:user:42</code> itself. Redis sets give you O(1) membership tracking and an efficient way to fan a single invalidation event out to many derived cache entries.</p>`,
  },
  {
    id: "a7-caching-perf-8",
    category: "perf",
    categoryLabel: "Performance",
    question: `Compare cache-aside (lazy loading) vs. write-through caching for a NestJS service backed by Postgres + Redis. Which is the more common default, and why?`,
    answerHtml: `<p><b>Cache-aside</b>: the service reads from Redis first; on a miss it queries Postgres, then populates Redis, then returns. Writes go to Postgres and the service explicitly deletes (or updates) the Redis key. This is the dominant pattern in Nest apps because it's incremental &mdash; you only cache what's actually requested, and a Redis outage degrades to 'just hit Postgres' rather than breaking writes.</p><p><b>Write-through</b>: every write goes to the cache and the DB synchronously (or the cache writes through to the DB). Reads are always cache hits after the first write. It guarantees freshness but couples write latency to cache availability and wastes memory caching data nobody reads. Nest doesn't provide write-through out of the box &mdash; you'd hand-roll it in the service layer, and it's rarely worth the complexity outside of read-heavy, write-rare hot keys.</p>`,
  },
  {
    id: "a7-caching-perf-9",
    category: "perf",
    categoryLabel: "Performance",
    question: `What is the 'thundering herd' (cache stampede) problem, and how would you mitigate it for a NestJS endpoint backed by Redis when a hot key expires?`,
    answerHtml: `<p>When a popular cache key expires, every concurrent request that arrives in that window observes a miss simultaneously and all of them hit the origin (DB or expensive computation) at once &mdash; multiplying load exactly when the system is most vulnerable (a hot key implies high traffic).</p><p>Mitigations available without exotic infra: (1) a Redis-based distributed lock (<code>SET key value NX PX ttl</code>) so only one request recomputes while others wait or serve stale; (2) 'early refresh' &mdash; current <code>cache-manager</code> ships this natively via the <code>refreshThreshold</code> option, recomputing in the background once a value's remaining TTL drops below the threshold, before it actually expires; (3) jittered TTLs so keys for similar resources don't all expire at the same instant; (4) serve a stale-while-revalidate value if cache-manager's store/config supports grace periods.</p>`,
  },
  {
    id: "a7-caching-perf-10",
    category: "perf",
    categoryLabel: "Performance",
    question: `In a multi-instance NestJS deployment, why can a naive in-process mutex ('only one request recomputes the cache') not solve the thundering-herd problem, and what's the Redis-native fix?`,
    answerHtml: `<p>An in-process lock (e.g. a <code>Map</code> tracking in-flight keys, or a library like <code>p-limit</code>) only coordinates requests <b>within one Node process</b>. With N replicas behind a load balancer, each replica still independently believes it's 'the one' doing the recompute &mdash; you get N concurrent origin hits instead of 1, not zero.</p><p>The fix has to live in the shared store: a Redis distributed lock via <code>SET lock:key 1 NX PX 5000</code> (atomic 'set if not exists with expiry'). The instance that succeeds in acquiring it recomputes and repopulates the cache; the others either poll/wait briefly for the new value or fall back to a short blocking read, then retry the cache read.</p>`,
  },
  {
    id: "a7-caching-perf-11",
    category: "perf",
    categoryLabel: "Performance",
    question: `When using current cache-manager (v6/v7) in NestJS with a Redis-backed Keyv store, what happens on a Redis connection failure during a cacheManager.get() call inside a request handled by @CacheInterceptor, and why does this matter operationally?`,
    answerHtml: `<p><code>@CacheInterceptor</code> itself wraps its cache read/write in a try/catch and falls through to calling the live handler on any error &mdash; so a Redis outage during a <i>built-in interceptor</i> read degrades to 'serve uncached' rather than a 500, by design. The real risk is in <b>manual</b> cache usage: a service that calls <code>cacheManager.get()</code>/<code>set()</code> directly inside a handler, without its own try/catch, will let a Redis connection error propagate and turn a cache outage into a 500 on that route &mdash; the opposite of caching's purpose.</p><p>Operationally this means Redis can become a single point of failure for routes you intended to make faster, not more fragile, if you bypass the interceptor's built-in fail-open behavior. The fix for manual cache code is to wrap reads in try/catch and fail open to the origin, and to verify your store/Redis client config (connection retry behavior, timeouts) so transient Redis issues degrade gracefully instead of hard-failing requests.</p>`,
  },
  {
    id: "a7-caching-perf-12",
    category: "perf",
    categoryLabel: "Performance",
    question: `What does CACHE_MANAGER injection actually resolve to in Nest, and why can't you just \`import cache from 'cache-manager'\` and use it directly in a service?`,
    answerHtml: `<p><code>CACHE_MANAGER</code> is a DI token Nest's <code>CacheModule</code> (from <code>@nestjs/cache-manager</code>) registers a provider under, bound to the configured cache instance (in-memory or Redis-backed, per your <code>CacheModule.register()/registerAsync()</code> config). You inject it with <code>@Inject(CACHE_MANAGER) private cache: Cache</code>, importing both <code>CACHE_MANAGER</code> and the <code>Cache</code> type from <code>@nestjs/cache-manager</code>.</p><p>You technically <i>could</i> import the raw <code>cache-manager</code> package and instantiate your own store, but then you'd be maintaining a second, disconnected cache instance outside Nest's module graph &mdash; defeating config centralization (one Redis connection, one TTL policy) and losing the ability for <code>@CacheInterceptor</code> (which also resolves <code>CACHE_MANAGER</code> internally) to share the same cache as your manual reads/writes.</p>`,
  },
  {
    id: "a7-caching-perf-13",
    category: "perf",
    categoryLabel: "Performance",
    question: `You need per-request cache bypass (e.g. an internal admin tool wants fresh data via a \`Cache-Control: no-cache\` header). How would you implement that against @CacheInterceptor without disabling caching for everyone else?`,
    answerHtml: `<p>Subclass <code>CacheInterceptor</code> and override <code>isRequestCacheable(context)</code> to inspect <code>request.headers['cache-control']</code> in addition to the default method check; if it contains <code>no-cache</code>, return <code>false</code> so <code>trackBy</code> resolves to <code>undefined</code> and the interceptor skips both the cache lookup and the write-back for that single request, falling through to the live handler.</p><p>This keeps the optimization opt-out per-request rather than per-route, which matters for admin/debug tooling that needs to bypass staleness without forcing a global TTL drop that would hurt every other caller.</p>`,
  },
  {
    id: "a7-caching-perf-14",
    category: "perf",
    categoryLabel: "Performance",
    question: `Why is caching the result of a TypeORM/Prisma query by serializing the SQL string + params as the Redis key a fragile strategy at the database layer, even though it works at the HTTP layer for routes?`,
    answerHtml: `<p>Two problems compound: (1) ORMs frequently change generated SQL between versions/query-builder calls in ways that are semantically identical but textually different (column ordering, alias naming), silently fragmenting your cache into near-duplicate entries with low hit rates; (2) more seriously, query-string keys have no semantic link to the <b>rows</b> they returned, so you can't invalidate by entity — a write to one row can't tell you which of the many cached query strings included that row.</p><p>The more robust pattern is to key by a stable, application-defined identifier (e.g. <code>users:list:role=admin:page=2</code>) you control yourself, paired with the tag-based invalidation approach (recording which entity ids each query key depends on) rather than keying off raw SQL.</p>`,
  },
  {
    id: "a7-caching-perf-15",
    category: "perf",
    categoryLabel: "Performance",
    question: `What's the practical difference between Redis EXPIRE-based TTL eviction and cache-manager's \`ttl\` option when cache-manager is using an in-memory store vs. a Redis-backed store?`,
    answerHtml: `<p>With the in-memory store, cache-manager itself tracks expiry timestamps and evicts lazily (checked on read) or via its own sweep, optionally bounded by an entry-count limit &mdash; eviction is an app-process concern and disappears on restart.</p><p>With a Redis-backed store, cache-manager's Keyv adapter issues a Redis <code>SET</code> with a TTL (or equivalent <code>EXPIRE</code>), and Redis's own engine handles expiry server-side, independent of whether any Node process is alive. This means Redis-backed TTLs survive app restarts/redeploys (the data and its expiry both live in Redis), while in-memory TTLs are wiped on every restart along with the rest of the cache &mdash; a meaningful operational difference when you're doing rolling deploys.</p>`,
  },
  {
    id: "a7-caching-perf-16",
    category: "perf",
    categoryLabel: "Performance",
    question: `How would you identify which endpoints in a NestJS service are the slow-request culprits before reaching for caching as the fix at all?`,
    answerHtml: `<p>Don't guess &mdash; measure. The standard toolchain: (1) structured request-duration logging or an interceptor that records <code>Date.now()</code> at entry/exit per route and emits it to your metrics backend (Prometheus histogram, Datadog APM); (2) APM tracing (e.g. OpenTelemetry auto-instrumentation for Nest + the DB driver) to see whether time is spent in the handler, the DB query, or an outbound HTTP call; (3) Postgres's own <code>pg_stat_statements</code> or slow-query log to find expensive queries independent of which route triggers them.</p><p>Caching is the right fix only when profiling shows the cost is in <b>repeated, expensive, low-volatility</b> reads. If the slow path is a single N+1 query or a missing index, caching just hides the symptom and adds staleness risk for free — fix the query first.</p>`,
  },
  {
    id: "a7-caching-perf-17",
    category: "perf",
    categoryLabel: "Performance",
    question: `A request handler calls three independent services sequentially with \`await\`, each taking ~100ms, for a total of ~300ms. What's the caching-adjacent (not caching) fix, and when does caching still help on top of it?`,
    answerHtml: `<p>If the three calls are independent (don't depend on each other's results), the fix is parallelization: <code>await Promise.all([svcA.get(), svcB.get(), svcC.get()])</code> collapses ~300ms of serial latency to ~100ms (the slowest call). This is a request-shape fix, not a caching fix, and you should reach for it first since it has zero staleness cost.</p><p>Caching still helps on top once parallelized if any of those three calls hit expensive, infrequently-changing data — e.g. if <code>svcB</code> is a feature-flag lookup that rarely changes, caching it independently removes it from the critical path entirely (effectively 0ms on a hit) rather than just running it in parallel.</p>`,
  },
  {
    id: "a7-caching-perf-18",
    category: "perf",
    categoryLabel: "Performance",
    question: `Where in the NestJS request lifecycle does @CacheInterceptor actually run relative to guards, pipes, and the route handler, and why does that ordering matter for caching authenticated routes?`,
    answerHtml: `<p>Interceptors wrap the handler call: the execution order is <b>middleware &rarr; guards &rarr; interceptors (pre) &rarr; pipes &rarr; route handler &rarr; interceptors (post)</b>. So <code>@CacheInterceptor</code> runs <b>after</b> guards have already authenticated/authorized the request, meaning <code>request.user</code> is already populated and available when <code>trackBy()</code> executes &mdash; which is exactly what makes it possible to safely fold the user id into the cache key.</p><p>It also means a cached response is only served after auth succeeds; an interceptor short-circuiting to a cached value still does so post-guard, so you can't use response caching to skip authorization checks (good for security, but it also means caching gives you zero latency benefit on the auth/guard chain itself — only on the handler + downstream work).</p>`,
  },
  {
    id: "a7-caching-perf-19",
    category: "perf",
    categoryLabel: "Performance",
    question: `You're caching a GET /products/:id response. A PATCH to that product happens, and you call \`cacheManager.del('/products/' + id)\` in the same handler. What's the race condition that can still produce a stale read, and how do you close it?`,
    answerHtml: `<p>If a concurrent <code>GET /products/:id</code> request is in flight at the exact moment of the <code>PATCH</code> &mdash; specifically, if it already read stale data from Postgres (via a snapshot/transaction taken before the commit) but hasn't yet written its result back into Redis &mdash; that GET's write-back can land <i>after</i> your <code>del()</code>, repopulating the cache with the now-stale value. The delete 'wins' the race against the slow GET's commit, but loses against its later write-back.</p><p>Closing it fully requires either: a short negative-cache/lock window after invalidation (block repopulation for N ms), or invalidate-then-revalidate with a version/ETag check before writing back, or accept the small probability window and rely on TTL as the backstop. In practice most teams accept this race and bound its blast radius with a short TTL rather than building full read-your-writes consistency.</p>`,
  },
  {
    id: "a7-caching-perf-20",
    category: "perf",
    categoryLabel: "Performance",
    question: `What does cache-manager's \`wrap()\` (or the pattern it implements) buy you over manually doing get → miss → compute → set in a NestJS service method?`,
    answerHtml: `<p><code>cache.wrap(key, fn, ttl)</code> encapsulates the cache-aside read-through pattern in one call: check the key, return it on a hit, otherwise invoke <code>fn()</code>, store the result, and return it &mdash; removing the boilerplate of writing that branch by hand in every service method and reducing the chance of a bug where you forget the <code>set()</code> on the miss path or get the key wrong between the read and write.</p><p>It does <b>not</b> by itself solve the thundering-herd problem &mdash; current cache-manager's <code>wrap()</code> does not document built-in in-flight request de-duplication across concurrent misses, so the same key can still be recomputed multiple times concurrently. Pair it with <code>refreshThreshold</code> (background early-refresh) or an explicit distributed lock if stampede protection matters, rather than assuming <code>wrap()</code> covers it.</p>`,
  },
  {
    id: "a7-caching-perf-21",
    category: "perf",
    categoryLabel: "Performance",
    question: `Why might you choose Redis's native EXPIRE/TTL plus pub/sub keyspace notifications over polling, to drive cache-warming or invalidation logic in a NestJS app?`,
    answerHtml: `<p>Redis can publish events on key expiration/eviction (<code>notify-keyspace-events</code> config enabling <code>Ex</code> events) that a Nest service can subscribe to via a Redis client, reacting the instant a key dies rather than polling Redis on an interval to check staleness. This lets you implement 'refresh-ahead' caching: subscribe to expiry events for a known set of hot keys and proactively recompute them before the next request arrives, keeping P99 latency flat for hot paths instead of paying the recompute cost on the unlucky request that hits the miss.</p><p>The tradeoff is operational complexity and a new failure mode (a missed pub/sub event due to a disconnect means the proactive refresh silently doesn't happen) &mdash; it's a tool for genuinely hot, latency-critical keys, not a default for general HTTP caching.</p>`,
  },
  {
    id: "a7-caching-perf-22",
    category: "perf",
    categoryLabel: "Performance",
    question: `How do you correctly scope @CacheInterceptor + @CacheTTL when applied at the controller level vs. individual route handlers, and what's a common mistake teams make here?`,
    answerHtml: `<p>Nest decorator metadata for caching can be set at the controller class level (applies as a default to every handler in it) and overridden per-method. A common mistake is applying <code>@UseInterceptors(CacheInterceptor)</code> at the controller level on a resource controller that mixes safe, cacheable <code>GET</code> list/detail endpoints with sensitive ones (e.g. <code>GET /users/:id/payment-methods</code>) &mdash; the interceptor caches all of them uniformly (modulo the GET-only check), including the sensitive endpoint, unless you explicitly opt that handler out.</p><p>Safer default: apply the interceptor per-route on the specific cacheable handlers rather than at the controller level, or override <code>isRequestCacheable</code>/<code>trackBy</code> to default to <i>not</i> caching unless explicitly allow-listed.</p>`,
  },
  {
    id: "a7-caching-perf-23",
    category: "perf",
    categoryLabel: "Performance",
    question: `What is the operational risk of an unbounded in-memory cache-manager store in a containerized NestJS deployment, and how does this risk change once you move to Redis?`,
    answerHtml: `<p>In-memory: every cached entry lives in the Node process heap, competing with everything else for the container's memory limit. A store configured without a sane entry-count or size bound can push the process toward its cgroup memory limit, triggering an OOM kill &mdash; which, unlike a slow query, takes down the <b>entire instance</b>, not just one request.</p><p>Moving to Redis decouples cache memory from the app container's limit (Redis has its own memory budget and eviction policy, e.g. <code>maxmemory-policy allkeys-lru</code>), so a cache that grows unexpectedly degrades by evicting old entries (more cache misses) rather than crashing the API process &mdash; one of the operational reasons Redis is preferred for caching at any nontrivial scale beyond raw latency.</p>`,
  },
];

export const ADVANCED7_QUIZ: QuizQuestion[] = [
  {
    id: "a7q-caching-perf-1",
    category: "perf",
    categoryLabel: "Performance",
    question: `A NestJS API applies @CacheInterceptor globally with the default trackBy. Two different authenticated users send GET /api/dashboard with identical query strings but different session cookies. What happens?`,
    options: [`Each user gets their own cache entry because cookies are part of the default cache key`, `The interceptor skips caching entirely since the route is authenticated`, `The second user is served the first user's cached response, because the default key is built from the request URL only`, `Nest throws a runtime error because authenticated routes cannot be cached`],
    answer: 2,
    explanationHtml: `<p>The default <code>trackBy</code> key is derived from the request URL via <code>httpAdapter.getRequestUrl()</code> — it does not include cookies, headers, or <code>request.user</code>. Identical URLs collide into the same key regardless of who's authenticated, which is exactly how cross-user data leaks happen. Fixing this requires overriding <code>trackBy</code> to include the user/tenant identity.</p>`,
  },
  {
    id: "a7q-caching-perf-2",
    category: "perf",
    categoryLabel: "Performance",
    question: `Which scenario is the best fit for switching a NestJS service from the default in-memory cache-manager store to a Redis-backed store?`,
    options: [`The team wants lower per-request latency than the in-memory store can offer`, `The service runs as multiple replicas behind a load balancer and needs a shared, restart-durable cache`, `TypeScript type inference for cacheManager.get() only works with the Redis store`, `@CacheTTL decorators only function when the underlying store is Redis`],
    answer: 1,
    explanationHtml: `<p>The in-memory store is actually faster per-call (no network hop) — the reason to move to Redis is consistency and durability across multiple instances, not raw speed. With N replicas each running their own in-memory cache, hit rates fragment and invalidation across instances is impossible without a shared store.</p>`,
  },
  {
    id: "a7q-caching-perf-3",
    category: "perf",
    categoryLabel: "Performance",
    question: `A hot Redis-cached key expires under heavy concurrent traffic, and dozens of requests miss simultaneously and all hit the origin database at once. What is this failure mode called, and what's an effective Redis-native mitigation?`,
    options: [`Cache poisoning; mitigated by validating all cached values against a schema`, `Thundering herd / cache stampede; mitigated by a distributed lock via SET key val NX PX ttl so only one request recomputes`, `Split-brain; mitigated by enabling Redis Sentinel`, `Lock convoy; mitigated by increasing the in-memory store's max entry count`],
    answer: 1,
    explanationHtml: `<p>This is the classic thundering herd / cache stampede problem. Because it spans multiple Node replicas, an in-process mutex can't help — the coordination has to live in the shared store. A Redis distributed lock (atomic <code>SET NX PX</code>) lets exactly one instance recompute while others wait or serve stale, instead of all of them slamming the database at once.</p>`,
  },
  {
    id: "a7q-caching-perf-4",
    category: "perf",
    categoryLabel: "Performance",
    question: `You invalidate a cached entity by calling cacheManager.del() on its known key after a successful update. Why might a stale value still reappear in the cache shortly afterward?`,
    options: [`Redis ignores DEL commands issued within the same TTL window`, `cache-manager batches DEL calls and may silently drop them under load`, `A concurrent GET request that read stale data before the update committed can write that stale value back into the cache after the DEL runs`, `DEL only removes the key from the in-memory store, never from a Redis-backed store`],
    answer: 2,
    explanationHtml: `<p>This is a race condition, not a tooling bug: a concurrent read that already fetched the pre-update value from the database can complete its cache write-back <i>after</i> your invalidating <code>DEL</code>, repopulating the cache with stale data. Closing this fully needs a short post-invalidation lock/negative-cache window or version checks on write-back; most teams instead accept the narrow race and bound it with a modest TTL.</p>`,
  },
  {
    id: "a7q-caching-perf-5",
    category: "perf",
    categoryLabel: "Performance",
    question: `A resource (a user record) is reachable through several distinct cached query results: GET /users/42, GET /users?role=admin, and GET /teams/7/members. What's the most maintainable invalidation strategy when user 42 is updated?`,
    options: [`Set every cache TTL to 0 so nothing is ever cached for that entity type`, `Maintain a reverse index (e.g. a Redis SET per entity id) recording every cache key that depends on that entity, and fan out DEL across its members on mutation`, `Rely solely on the default @CacheInterceptor trackBy to detect the dependency automatically`, `Restart the application process after every mutation to clear the in-memory store`],
    answer: 1,
    explanationHtml: `<p>A single entity often backs multiple cached query results with no inherent structural link between them. The standard fix is a tag/reverse-index pattern: when writing a cache entry, also register its key under a set keyed by every entity it depends on (e.g. <code>tag:user:42</code>). On mutation, read that set's members and fan a <code>DEL</code> out across all of them. <code>trackBy</code> only controls key generation for HTTP responses — it has no concept of entity dependencies.</p>`,
  },
  {
    id: "a7q-caching-perf-6",
    category: "perf",
    categoryLabel: "Performance",
    question: `Before adding caching to a NestJS endpoint that's reported as slow, profiling shows three independent downstream service calls are awaited sequentially, each taking ~100ms, with no shared expensive computation. What should be fixed first?`,
    options: [`Wrap the whole handler in @CacheInterceptor with a long TTL to mask the latency`, `Parallelize the three independent calls with Promise.all, since caching would just hide a request-shape problem and add staleness risk for no benefit`, `Move all three calls into a single Redis MULTI transaction`, `Increase the cache-manager store's max entry count to reduce eviction pressure`],
    answer: 1,
    explanationHtml: `<p>When the latency is purely structural — independent calls run serially instead of concurrently — the correct fix is <code>Promise.all</code>, which has zero staleness cost and directly addresses the root cause. Caching here would mask a 3x latency multiplier behind a TTL instead of fixing it, and introduces staleness risk for data that didn't need to be cached in the first place.</p>`,
  },
  {
    id: "a7q-caching-perf-7",
    category: "perf",
    categoryLabel: "Performance",
    question: `What does the current cache-manager \`wrap(key, fn, ttl)\` helper guarantee, and what does it NOT guarantee that teams sometimes assume it does?`,
    options: [`It guarantees Redis persistence; it does not guarantee in-memory fallback`, `It guarantees the cache-aside read/compute/store sequence in one call; it does not guarantee in-flight de-duplication of concurrent misses (no built-in stampede protection)`, `It guarantees automatic cache invalidation on related entity writes; it does not guarantee TTL expiry`, `It guarantees distributed locking across replicas; it does not guarantee local in-memory caching`],
    answer: 1,
    explanationHtml: `<p><code>wrap()</code> collapses the get-miss-compute-set boilerplate into one call, which is its real value. It does not, by itself, deduplicate concurrent in-flight misses for the same key — two simultaneous requests can both miss and both recompute. Stampede protection needs an explicit mechanism like <code>refreshThreshold</code> (background early refresh) or a distributed lock, not an assumption baked into <code>wrap()</code>.</p>`,
  },
];
