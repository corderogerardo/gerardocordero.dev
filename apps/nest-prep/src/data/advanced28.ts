// Batch 28 — Security hardening (OWASP for Node): broken access control (A01), injection & Mongo-operator stripping, JWT verification/algorithm pinning/iss-aud/signed-not-encrypted, refresh-token rotation, token storage (httpOnly cookies vs localStorage) + Redis sessions, password hashing (Argon2id-first / bcrypt), user enumeration, RBAC vs ABAC, helmet, auth-endpoint rate limiting, secrets management, and supply-chain / prototype-pollution / SSRF.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED28_FLASHCARDS: Flashcard[] = [
  {
    id: "a28-broken-access-control-1",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why is broken access control (OWASP A01) the category to obsess over, and what is the single rule that prevents most of it?`,
    answerHtml: `<p>A01 sits at the top of the OWASP Top 10 because it's the <b>most prevalent</b> class of real-world breach — the app authenticates you fine, then fails to check whether <i>this</i> user may touch <i>this</i> resource. The rule: <b>enforce authorization on every protected route, server-side, and never trust client-provided authz data</b>. A guard/middleware that reads the role from the verified token (not from a request body or a hidden form field) and re-checks resource ownership on each request is the fix. <b>Red flag:</b> "the frontend hides the admin button, so users can't reach that endpoint" — the endpoint is a URL; hiding UI is not access control. <b>Pitch:</b> "Every protected route re-derives who you are and what you own from the verified token, on the server, every time — the client is never the source of truth for permissions."</p>`,
  },
  {
    id: "a28-idor-object-level-2",
    category: "auth",
    categoryLabel: "Auth",
    question: `What is IDOR (insecure direct object reference), and why does role-only checking miss it?`,
    answerHtml: `<p>IDOR is the classic A01 bug: <code>GET /invoices/1042</code> checks that you're a logged-in user with role <code>customer</code> — and stops there. Nothing verifies that invoice 1042 <i>belongs to you</i>, so incrementing the id walks the whole table. Role checks answer "what kind of user are you," but object-level access needs "do you own <i>this row</i>." The fix is an <b>ownership check in the query itself</b> — <code>where id = :id AND owner_id = :currentUser</code> — so a mismatched id returns 404, not someone else's data. <b>Red flag:</b> a guard that only looks at <code>user.role</code> and never at the resource. <b>Pitch:</b> "Authorization has two questions — what role, and which object — and IDOR is what happens when you only answer the first."</p>`,
  },
  {
    id: "a28-injection-boundary-validation-3",
    category: "auth",
    categoryLabel: "Auth",
    question: `What is the layered defense against injection, and where does input validation actually belong?`,
    answerHtml: `<p>Two independent layers, both required. <b>(1) Parameterized queries</b> — let the driver/ORM bind values so user input can never change the query's structure; never string-concatenate SQL. <b>(2) Validate and shape input at the trust boundary</b> — before it reaches business logic — with a schema validator (<b>Zod/Joi</b>, or Nest's <code>class-validator</code> + <code>ValidationPipe</code>). Validation rejects the wrong <i>shape</i>; parameterization neutralizes the wrong <i>content</i>. They're complementary: a validated string can still be a SQL payload if you concatenate it. Also never build a shell command by interpolating user input (command injection) — pass args as an array to <code>execFile</code>, not a string to <code>exec</code>. <b>Pitch:</b> "Parameterize so structure is fixed, validate at the edge so shape is known — one without the other still leaves a hole."</p>`,
  },
  {
    id: "a28-nosql-operator-injection-4",
    category: "auth",
    categoryLabel: "Auth",
    question: `How does NoSQL (Mongo operator) injection work, and why is "we don't write SQL, so we're safe" wrong?`,
    answerHtml: `<p>Mongo queries are JSON objects, so an attacker who controls a JSON value can smuggle in a <b>query operator</b>. A login body of <code>{ "username": "admin", "password": { "$ne": null } }</code> becomes <code>find({ username, password: { $ne: null } })</code> — "password not equal to null" matches any record, bypassing the check. The vector isn't a string escape, it's the attacker turning a scalar into an operator object. Defenses: <b>strip keys starting with <code>$</code> (or containing <code>.</code>)</b> from user input (e.g. <code>express-mongo-sanitize</code>), validate that fields are the expected primitive type (a password must be a <code>string</code>), and <b>never spread a raw request body directly into a query</b>. <b>Red flag:</b> <code>User.find(req.body)</code>. <b>Pitch:</b> "NoSQL injection isn't about quotes — it's about a string field arriving as <code>{ $ne: null }</code>; type-validate every value and strip operator keys at the boundary."</p>`,
  },
  {
    id: "a28-jwt-verify-not-decode-5",
    category: "auth",
    categoryLabel: "Auth",
    question: `What's the difference between jwt.verify and jwt.decode, and why must you pin the algorithm?`,
    answerHtml: `<p><code>jwt.decode</code> just base64-decodes the payload — it <b>checks nothing</b>: not the signature, not expiry. <code>jwt.verify</code> validates the signature against your key and enforces <code>exp</code>. Using <code>decode</code> for auth means accepting any token an attacker types out. Second trap: <b>pin the allowed algorithms explicitly</b> (<code>{ algorithms: ['RS256'] }</code>). Without pinning, a classic attack sets the header to <code>alg: none</code> (claiming "no signature, trust me") or downgrades an RS256 setup to HS256 and signs with your <i>public</i> key as the HMAC secret — both let a forger mint valid-looking tokens. <b>Red flag:</b> reading claims from <code>jwt.decode(token)</code>, or a verify call with no <code>algorithms</code> list. <b>Pitch:</b> "Decode is for logging, verify is for trust — and verify must be told exactly which algorithm to accept, or <code>alg:none</code> walks right in."</p>`,
  },
  {
    id: "a28-jwt-iss-aud-signed-not-encrypted-6",
    category: "auth",
    categoryLabel: "Auth",
    question: `Beyond signature and expiry, what should JWT verification check — and why is "JWTs are signed, not encrypted" a security fact you must design around?`,
    answerHtml: `<p>Verify <b>issuer (<code>iss</code>) and audience (<code>aud</code>)</b> too: a signature only proves "some holder of the key made this," so in a multi-service world a token minted for service A can be replayed against service B unless B insists <code>aud</code> names it. Checking <code>iss</code>/<code>aud</code> stops cross-service and cross-tenant token reuse. Second fact: a standard JWT is <b>signed, not encrypted</b> — the payload is base64, fully readable by anyone. So <b>never put secrets in the payload</b> (passwords, API keys, PII beyond an id). Signing guarantees integrity/authenticity, not confidentiality. <b>Red flag:</b> "the token's encrypted, so it's fine to store the user's SSN in it." <b>Pitch:</b> "A JWT is a tamper-proof postcard — anyone can read it, nobody can forge it; verify <code>iss</code>/<code>aud</code> so it's only trusted where it was meant to be used, and keep secrets out of it."</p>`,
  },
  {
    id: "a28-refresh-rotation-theft-detection-7",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why keep access tokens short-lived (≤15 min) with a refresh token, and what does refresh-token rotation buy you?`,
    answerHtml: `<p>A JWT access token is <b>self-contained and un-revocable</b> — once signed it's valid until <code>exp</code>, so a stolen one is usable for its whole lifetime. Keeping that lifetime short (<b>≤15 min</b>) caps the blast radius; a longer-lived refresh token, held more securely, silently mints new access tokens. <b>Rotation</b> makes refresh tokens single-use: each refresh returns a <i>new</i> refresh token and invalidates the old one. The payoff is <b>theft detection</b> — if an already-used (rotated-out) refresh token is presented again, that means two parties hold it, so you revoke the entire token family and force re-login. For distributed systems, sign with <b>RS256/ES256</b> (asymmetric): services verify with the public key and only the auth server holds the private key. <b>Pitch:</b> "Short access tokens limit the damage window; rotating refresh tokens turn a stolen token into an alarm instead of a skeleton key."</p>`,
  },
  {
    id: "a28-token-storage-cookies-vs-localstorage-8",
    category: "auth",
    categoryLabel: "Auth",
    question: `Where should a browser client store its tokens, and why is localStorage the wrong answer?`,
    answerHtml: `<p><b>localStorage is readable by any JavaScript on the page</b>, so a single XSS payload (or a compromised third-party script) exfiltrates the token — game over. Store session tokens in a cookie flagged <code>httpOnly</code> (JS can't read it, killing the XSS-exfil path), <code>Secure</code> (HTTPS only), and <code>SameSite=Strict</code>/<code>Lax</code> (blunts CSRF). The tradeoff you accept in return is CSRF surface, which <code>SameSite</code> plus anti-CSRF tokens covers. For <b>revocable</b> sessions, pair a session-ID cookie with a <b>server-side store (Redis)</b> — now "log out everywhere" or "kill this session" is a delete, which pure stateless JWTs can't do. <b>Red flag:</b> "we put the JWT in localStorage because cookies are vulnerable to CSRF" — trading a fully-mitigable risk (CSRF) for an unmitigable one (any XSS reads your token). <b>Pitch:</b> "httpOnly cookies take token theft off the XSS table; a Redis session store is what makes 'revoke this session now' actually possible."</p>`,
  },
  {
    id: "a28-password-hashing-argon2id-9",
    category: "auth",
    categoryLabel: "Auth",
    question: `What does OWASP currently recommend for password hashing, and what are the parameters and caveats to name?`,
    answerHtml: `<p>Never store or fast-hash passwords — use a <b>slow, salted</b> algorithm. Per the OWASP Password Storage Cheat Sheet, OWASP now lists <b>Argon2id first for new applications</b>; <b>bcrypt remains acceptable for legacy/existing systems</b>. Parameters to know: the work factor "should be as large as verification-server performance allows, with a <b>minimum of 10</b>," and <b>cost 12 is a common 2026 default</b> (PHP 8.4 raised its built-in bcrypt default from 10 to 12). Also flag bcrypt's <b>72-byte input limit</b> — it silently truncates longer passwords, so pre-hash or cap length. Important caveat to say out loud: <b>this guidance evolves — confirm the current OWASP Password Storage Cheat Sheet at interview/design time</b> rather than quoting a fixed number. <b>Pitch:</b> "Argon2id first for greenfield, bcrypt fine for legacy at cost ≥10 (12 today), mind bcrypt's 72-byte cap — and I'd re-check the OWASP cheat sheet because these numbers drift upward with hardware."</p>`,
  },
  {
    id: "a28-user-enumeration-10",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why should a login endpoint return the same response for "wrong user" and "wrong password," and where else does enumeration leak?`,
    answerHtml: `<p>If "no such user" and "wrong password" produce <b>different messages, status codes, or response times</b>, an attacker can probe which emails have accounts — <b>user enumeration</b> — which feeds targeted phishing and credential-stuffing. Return an identical generic error ("invalid email or password") for both, and guard timing: if the user doesn't exist, still perform a <b>dummy hash comparison</b> so the response time doesn't reveal whether the branch that hashes ran. The same leak hides in <b>signup</b> ("email already registered") and <b>password reset</b> ("if that email exists, we sent a link" is the safe wording). <b>Red flag:</b> distinct "user not found" vs "incorrect password" messages. <b>Pitch:</b> "Every auth response — login, signup, reset — should be identical whether or not the account exists, in both wording and timing; the difference is a free account-enumeration oracle."</p>`,
  },
  {
    id: "a28-rbac-vs-abac-11",
    category: "auth",
    categoryLabel: "Auth",
    question: `RBAC vs ABAC — what's the difference, and when do you reach for the more flexible one?`,
    answerHtml: `<p><b>RBAC (role-based)</b> grants permissions to roles and roles to users: "editors can publish posts." It's simple and auditable, but it can't express context — "editors can publish <i>their own</i> posts, in <i>their</i> region, during <i>business hours</i>" — without exploding into dozens of narrow roles. <b>ABAC (attribute-based)</b> evaluates a <b>policy over attributes</b> of the user, the resource, and the environment at request time, so ownership, tenant, status, and time all factor in. Reach for ABAC (or a policy engine like CASL/OPA) when authorization depends on the <i>relationship between subject and object</i>, not just a static role. Most systems are RBAC for coarse gating plus a sprinkle of attribute checks (ownership) for the fine-grained calls. <b>Pitch:</b> "RBAC answers 'what kind of user'; ABAC answers 'what may this user do to this specific object right now' — you graduate to ABAC the moment roles start multiplying to encode context."</p>`,
  },
  {
    id: "a28-helmet-security-headers-12",
    category: "auth",
    categoryLabel: "Auth",
    question: `What does helmet actually do, and which of its headers carry the most weight?`,
    answerHtml: `<p><code>helmet()</code> sets a bundle of <b>defensive HTTP response headers</b> so the browser enforces protections for you — it's one line of middleware, applied globally in <code>main.ts</code>, for a real baseline. The heavy hitters: <b>Content-Security-Policy</b> (restrict which origins can load scripts/styles — the strongest structural mitigation against XSS), <b>Strict-Transport-Security / HSTS</b> (force HTTPS, defeating SSL-strip downgrade), <b>X-Content-Type-Options: nosniff</b> (stop MIME-sniffing that turns an uploaded "image" into executed script), and frame-control headers to prevent clickjacking. helmet is <b>defense-in-depth, not a substitute</b> for encoding output and validating input — it hardens the browser's behavior, it doesn't sanitize your data. <b>Red flag:</b> shipping without CSP and assuming helmet's defaults "handle XSS." <b>Pitch:</b> "helmet is a one-line hardening baseline; CSP and HSTS are the two headers I'd tune deliberately rather than accept the defaults for."</p>`,
  },
  {
    id: "a28-rate-limit-auth-endpoints-13",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why do auth endpoints need tighter rate limits than the rest of the API, and what's a sane number?`,
    answerHtml: `<p>Login, token, password-reset, and OTP endpoints are the front door for <b>brute-force and credential-stuffing</b> — a botnet replaying leaked username/password pairs. A global limit generous enough for normal API traffic is far too loose here, so scope a stricter limit to auth routes: on the order of <b>5–10 requests per minute per IP</b> on token/login endpoints. Layer it — per-IP <i>and</i> per-account — since attackers spread across IPs (rotating proxies) and across accounts (one password against thousands of users). Back it with a <b>shared store (Redis)</b> so the limit holds across replicas, not per-process. Rate limiting alone isn't enough against distributed stuffing — pair it with MFA, breached-password checks, and CAPTCHA/step-up on anomalies. <b>Pitch:</b> "Auth endpoints get their own tight budget — ~5–10/min/IP, per-account too, in a shared store — because they're the one place brute force actually pays off."</p>`,
  },
  {
    id: "a28-secrets-management-14",
    category: "auth",
    categoryLabel: "Auth",
    question: `How should production secrets be managed, and what's the fail-fast rule at startup?`,
    answerHtml: `<p>Secrets (DB creds, JWT signing keys, API keys) live in a <b>dedicated secret store — AWS Secrets Manager or HashiCorp Vault — never in code, never committed, and ideally not baked into images</b> as plain env vars if you can inject at runtime. Rotate them on a schedule (<b>~90 days</b>, and immediately on any suspected compromise); a store makes rotation a config change instead of a redeploy. Critically, <b>fail fast</b>: validate that every required secret is present and well-formed <b>at startup</b> and refuse to boot if one is missing — a service that silently starts with an empty <code>JWT_SECRET</code> will sign tokens with <code>""</code> and accept forgeries. <b>Red flag:</b> secrets in <code>.env</code> committed to the repo, or code that falls back to a default secret when the env var is unset. <b>Pitch:</b> "Secrets come from a managed store, rotate on a clock, and the app crashes loudly at boot if one's missing — a service that starts without its signing key is worse than one that won't start."</p>`,
  },
  {
    id: "a28-supply-chain-prototype-pollution-ssrf-15",
    category: "auth",
    categoryLabel: "Auth",
    question: `What are the supply-chain, prototype-pollution, and SSRF risks a Node backend has to watch, and how do you defend each?`,
    answerHtml: `<p><b>Supply chain:</b> most of your code is other people's — audit it. Run <code>npm audit</code> and an SCA tool (<b>Snyk / OWASP Dependency-Check</b>) in CI, commit <b>lockfiles</b> so builds are reproducible, and <b>scan container images</b> on every build. <b>Prototype pollution:</b> untrusted keys like <code>__proto__</code> in a deep-merge or <code>Object.assign</code> can inject properties onto <code>Object.prototype</code>, silently changing behavior app-wide or enabling bypasses — validate/allow-list keys and avoid unsafe recursive merges of user input. <b>SSRF (server-side request forgery):</b> hottest in <b>webhook handlers</b> and any "fetch this URL for me" feature — an attacker points the URL at <code>169.254.169.254</code> (cloud metadata) or internal services and your server makes the request from inside the perimeter. Defend by <b>allow-listing destination hosts</b>, blocking private/link-local IP ranges, and disabling redirects to them. <b>Pitch:</b> "Your dependencies, your object prototypes, and your outbound webhook fetches are all attacker-reachable surface — audit the tree, reject <code>__proto__</code>, and allow-list where your server is allowed to make requests."</p>`,
  },
];

export const ADVANCED28_QUIZ: QuizQuestion[] = [
  {
    id: "a28-qz-1",
    category: "auth",
    categoryLabel: "Auth",
    question: `Which statement best describes broken access control (OWASP A01) and its primary defense?`,
    options: [
      `It's a rare, low-impact issue fixed by hiding privileged buttons in the frontend UI`,
      `It's the most prevalent OWASP category — the fix is enforcing authorization server-side on every protected route and never trusting client-provided authz data`,
      `It only affects APIs that skip HTTPS, so enabling TLS resolves it`,
      `It's a subclass of injection, prevented entirely by parameterized queries`,
    ],
    answer: 1,
    explanationHtml: `<p>A01 is the top, most-prevalent OWASP category: the app authenticates the user but fails to check whether they may access <i>this</i> resource. The defense is server-side authorization on every protected route (including object-level ownership checks) with the client never treated as the source of truth. Hiding UI is not access control — the endpoint is still a reachable URL.</p>`,
  },
  {
    id: "a28-qz-2",
    category: "auth",
    categoryLabel: "Auth",
    question: `A NestJS guard reads the user's role from a token via <code>jwt.decode(token)</code> and trusts it. What is the vulnerability?`,
    options: [
      `None — jwt.decode validates the signature and expiry before returning claims`,
      `jwt.decode only base64-decodes the payload without verifying the signature or expiry, so an attacker can forge any claims; use jwt.verify with a pinned algorithm instead`,
      `jwt.decode is slower than jwt.verify but equally safe`,
      `The only issue is that decode doesn't check the audience; adding an aud check makes it safe`,
    ],
    answer: 1,
    explanationHtml: `<p><code>jwt.decode</code> performs no verification — it just decodes base64, so any hand-crafted token is accepted. Auth must use <code>jwt.verify</code>, which checks the signature and expiry, and it must <b>pin the allowed algorithms</b> (e.g. <code>['RS256']</code>) to block <code>alg:none</code> and algorithm-downgrade forgeries. Decode is for inspection/logging only.</p>`,
  },
  {
    id: "a28-qz-3",
    category: "auth",
    categoryLabel: "Auth",
    question: `Per the current OWASP Password Storage guidance, which is the most accurate statement for a new NestJS application?`,
    options: [
      `Use SHA-256 with a salt — it's fast and collision-resistant`,
      `OWASP now lists Argon2id as the first choice for new apps; bcrypt is acceptable for legacy systems (work factor ≥10, cost 12 common in 2026), and this guidance should be re-confirmed since it evolves`,
      `bcrypt is deprecated and forbidden; only Argon2id is ever acceptable`,
      `Any hashing is fine as long as each password gets a unique salt`,
    ],
    answer: 1,
    explanationHtml: `<p>OWASP's Password Storage Cheat Sheet now lists <b>Argon2id first</b> for new applications and treats <b>bcrypt as acceptable for legacy</b> systems (work factor minimum 10, cost 12 a common 2026 default; mind bcrypt's 72-byte limit). Fast general-purpose hashes like SHA-256 are wrong for passwords. And because this guidance shifts with hardware, the senior move is to re-confirm the current cheat sheet rather than hard-code a number.</p>`,
  },
  {
    id: "a28-qz-4",
    category: "auth",
    categoryLabel: "Auth",
    question: `A Mongoose login handler runs <code>User.findOne(req.body)</code> and a client sends <code>{ "username": "admin", "password": { "$ne": null } }</code>. What happens and how is it prevented?`,
    options: [
      `Mongoose rejects the object because password must be a string, so nothing happens`,
      `The $ne operator makes the password clause match any non-null value, bypassing the check — prevent it by type-validating fields, stripping $-prefixed keys, and never spreading raw bodies into queries`,
      `It throws a syntax error and returns 500, which is safe`,
      `It's a SQL injection that parameterized queries would stop`,
    ],
    answer: 1,
    explanationHtml: `<p>This is NoSQL operator injection: the password value arrives as the operator object <code>{ $ne: null }</code>, so the query becomes "password not equal to null" and matches the record, bypassing authentication. Defenses: validate that fields are the expected primitive type (password must be a <code>string</code>), strip keys beginning with <code>$</code> (e.g. <code>express-mongo-sanitize</code>), and never pass <code>req.body</code> straight into a query.</p>`,
  },
  {
    id: "a28-qz-5",
    category: "auth",
    categoryLabel: "Auth",
    question: `A team stores JWTs in <code>localStorage</code> "because cookies are vulnerable to CSRF." What's the flaw in that reasoning?`,
    options: [
      `None — localStorage is the OWASP-recommended token store`,
      `localStorage is readable by any JavaScript, so a single XSS steals the token; that risk is unmitigable, whereas the CSRF risk of httpOnly+SameSite cookies is fully mitigable`,
      `localStorage tokens expire too quickly to be useful`,
      `Cookies can't hold JWTs because of size limits, so the comparison is moot`,
    ],
    answer: 1,
    explanationHtml: `<p>They traded a fully-mitigable risk for an unmitigable one. Any XSS on the page can read <code>localStorage</code> and exfiltrate the token. An <code>httpOnly</code>, <code>Secure</code>, <code>SameSite</code> cookie is unreadable by JS (killing the XSS-exfil path), and its CSRF exposure is covered by <code>SameSite</code> plus anti-CSRF tokens. For revocable sessions, pair a session-ID cookie with a server-side (Redis) store.</p>`,
  },
];
