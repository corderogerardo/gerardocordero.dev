// Batch 8 — Unit testing providers with mocked dependencies (Test.createTestingModule), e2e testing with Supertest, overriding providers for tests, and testing guards/interceptors in isolation.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED8_FLASHCARDS: Flashcard[] = [
  {
    id: "a8-testing-1",
    category: "testing",
    categoryLabel: "Testing",
    question: `When you call Test.createTestingModule({...}).compile(), what does Nest actually instantiate, and why does that matter for unit test isolation?`,
    answerHtml: `<p>It builds a <b>real Nest DI container</b> scoped only to the providers/controllers/imports you declare in that metadata object — it instantiates real classes and wires a real dependency graph, it just never bootstraps an HTTP listener.</p><p>This matters because <code>compile()</code> throws <i>"Nest can't resolve dependencies"</i> if any transitive dependency is missing, so you're forced to either supply the real provider, a mock, or use <code>overrideProvider</code> — there's no implicit auto-mocking of unresolved tokens.</p>`,
  },
  {
    id: "a8-testing-2",
    category: "testing",
    categoryLabel: "Testing",
    question: `What's the practical difference between registering a mock via providers: [{ provide: TOKEN, useValue: mock }] versus calling .overrideProvider(TOKEN).useValue(mock) on the TestingModuleBuilder?`,
    answerHtml: `<p>Both end up bound to the same token, but <code>overrideProvider</code> runs <b>after</b> the module metadata (including any imported real modules) is resolved, and it replaces whatever that import chain would have provided.</p><p>Plain <code>providers</code> array entries only work cleanly when the testing module is built from scratch without importing the real feature module — if you also import that module, you'll get a duplicate-provider conflict, which is exactly the case <code>overrideProvider</code> exists for.</p>`,
  },
  {
    id: "a8-testing-3",
    category: "testing",
    categoryLabel: "Testing",
    question: `Why do most NestJS unit tests build a bespoke object literal for createTestingModule instead of importing the production @Module() class directly?`,
    answerHtml: `<p>Importing the real module pulls in its entire <code>imports</code> array — database modules, config modules, other feature modules — turning what should be a fast, isolated unit test into a slow integration test that needs a real DB connection or env vars.</p><p>A hand-built metadata object lets you declare only the unit under test plus narrowly scoped mocks for its direct dependencies, keeping the test deterministic and fast (milliseconds, no I/O).</p>`,
  },
  {
    id: "a8-testing-4",
    category: "testing",
    categoryLabel: "Testing",
    question: `You mock a Repository dependency with a plain object of jest.fn()s instead of jest-mock-extended's mock<T>(). What type-safety gap does that introduce?`,
    answerHtml: `<p>A hand-rolled <code>{ find: jest.fn(), save: jest.fn() }</code> object isn't checked against the real <code>Repository&lt;Entity&gt;</code> interface, so if the production method signature changes (new param, renamed method, different return type) the mock silently drifts out of sync — TypeScript won't flag it, and the test keeps passing against a contract the real class no longer has.</p><p><code>mock&lt;Repository&lt;Entity&gt;&gt;()</code> auto-stubs every method matching the real interface, so a removed/renamed method becomes a compile error in the test file, not a runtime surprise in production.</p>`,
  },
  {
    id: "a8-testing-5",
    category: "testing",
    categoryLabel: "Testing",
    question: `Your service injects a custom provider via @Inject('CACHE_MANAGER'). How do you override it in a unit test, given there's no class to reference?`,
    answerHtml: `<p>String/symbol tokens are overridden the same way as class tokens — <code>overrideProvider('CACHE_MANAGER').useValue(mockCache)</code> or, in the providers array, <code>{ provide: 'CACHE_MANAGER', useValue: mockCache }</code>.</p><p>The token is whatever was used at the injection site (string, symbol, class, or even an enum), and Nest's container resolves purely by token identity — it has no special-case behavior for class-based vs. string-based tokens.</p>`,
  },
  {
    id: "a8-testing-6",
    category: "testing",
    categoryLabel: "Testing",
    question: `In an e2e test using Supertest against a bootstrapped Nest app, why is it critical to call app.init() (not just compile()) before issuing requests, and what does init() actually trigger?`,
    answerHtml: `<p><code>compile()</code> only builds the DI graph; it does not run <b>lifecycle hooks</b> (<code>OnModuleInit</code>, <code>OnApplicationBootstrap</code>) or apply <b>global pipes/filters/interceptors/guards</b> registered via <code>app.useGlobalPipes()</code> etc.</p><p><code>app.init()</code> triggers the full module initialization lifecycle and wires the Express/Fastify adapter so the in-memory HTTP server Supertest talks to behaves identically to <code>app.listen()</code> in production — skipping it means your e2e test silently bypasses ValidationPipe, exception filters, and any global middleware.</p>`,
  },
  {
    id: "a8-testing-7",
    category: "testing",
    categoryLabel: "Testing",
    question: `Supertest e2e tests typically hit the app via request(app.getHttpServer()) rather than request('http://localhost:3000'). Why does this distinction matter?`,
    answerHtml: `<p><code>app.getHttpServer()</code> returns the underlying Node <code>http.Server</code> instance <b>without binding it to a real port</b> — Supertest spins up an ephemeral listener internally, so tests run fully in-process with no network stack, no port conflicts in parallel CI jobs, and no risk of hitting a stale server left over from a previous run.</p><p>Pointing at a real URL requires the app to already be listening on a fixed port, which serializes test runs and reintroduces real network latency and flakiness.</p>`,
  },
  {
    id: "a8-testing-8",
    category: "testing",
    categoryLabel: "Testing",
    question: `Why is it considered bad practice to call app.useGlobalPipes(new ValidationPipe()) only in main.ts and assume it applies automatically in e2e tests?`,
    answerHtml: `<p><code>main.ts</code> is never executed in a test run — e2e tests build their own <code>INestApplication</code> via <code>Test.createTestingModule(...).compile()</code>, so any global setup (pipes, filters, interceptors, CORS, versioning) configured only inside <code>bootstrap()</code> in <code>main.ts</code> simply doesn't exist in the test app unless you re-apply it.</p><p>The fix is to extract that global setup into a shared function (e.g. <code>setupApp(app)</code>) called from both <code>main.ts</code> and the e2e test's <code>beforeAll</code>, so production and test configuration can't drift apart.</p>`,
  },
  {
    id: "a8-testing-9",
    category: "testing",
    categoryLabel: "Testing",
    question: `What's the risk of running e2e Supertest suites against a real, shared Postgres/Mongo instance instead of a per-suite ephemeral database, and what's the standard mitigation?`,
    answerHtml: `<p>Shared state across test files causes <b>order-dependent flakiness</b>: one suite's leftover rows change another suite's count-based or uniqueness-based assertions, and parallel test workers can race on the same rows.</p><p>The standard mitigation is an ephemeral database per run (Testcontainers spinning up a throwaway Postgres/Mongo container, or a schema-per-worker strategy) combined with truncating/resetting state in <code>beforeEach</code> or wrapping each test in a rolled-back transaction.</p>`,
  },
  {
    id: "a8-testing-10",
    category: "testing",
    categoryLabel: "Testing",
    question: `When testing a CanActivate guard in isolation (not through a full HTTP request), what object do you have to manually construct, and what's the easiest way to do it?`,
    answerHtml: `<p>You need an <code>ExecutionContext</code>, which is the interface a guard's <code>canActivate(context: ExecutionContext)</code> receives — it exposes <code>switchToHttp()</code> to get mocked <code>Request</code>/<code>Response</code> objects and <code>getHandler()</code>/<code>getClass()</code> for reflector-based metadata lookups.</p><p>Rather than implementing the full interface by hand, most teams use a small factory that returns a partial mock object with just <code>switchToHttp: () => ({ getRequest: () => mockReq })</code> and stubs for the methods the guard under test actually calls — you don't need to satisfy the whole interface, just what's invoked.</p>`,
  },
  {
    id: "a8-testing-11",
    category: "testing",
    categoryLabel: "Testing",
    question: `A guard calls this.reflector.getAllAndOverride(ROLES_KEY, [context.getHandler(), context.getClass()]) to read @Roles() metadata. How do you test this without spinning up a real controller class?`,
    answerHtml: `<p>You construct the guard with a real (or mocked) <code>Reflector</code> instance and mock <code>getAllAndOverride</code> (or <code>get</code>) to return whatever roles array the test case needs — the <code>ExecutionContext</code>'s <code>getHandler()</code>/<code>getClass()</code> can be stub functions returning dummy references since the mocked reflector ignores their actual content anyway.</p><p>This isolates the guard's authorization <i>logic</i> (does role X satisfy required roles Y?) from Nest's actual metadata-reflection machinery, which is framework code you don't need to re-verify.</p>`,
  },
  {
    id: "a8-testing-12",
    category: "testing",
    categoryLabel: "Testing",
    question: `Why can't you unit test a NestInterceptor's response-transformation logic by only asserting on its return value the way you would a guard's boolean return?`,
    answerHtml: `<p><code>intercept(context, next)</code> returns an <b>Observable</b> (via <code>next.handle()</code> piped through RxJS operators like <code>map</code>/<code>catchError</code>), not a plain value — you have to subscribe to it (or convert it with <code>firstValueFrom</code>/<code>lastValueFrom</code>) to actually trigger the pipeline and capture the transformed emission.</p><p>You also have to supply a fake <code>CallHandler</code> whose <code>handle()</code> returns <code>of(mockControllerResult)</code>, since the interceptor's whole job is to wrap whatever the route handler would have emitted.</p>`,
  },
  {
    id: "a8-testing-13",
    category: "testing",
    categoryLabel: "Testing",
    question: `What's the difference between overriding a guard with overrideGuard().useValue({ canActivate: () => true }) in an e2e test versus testing the guard's logic directly in a unit test?`,
    answerHtml: `<p><code>overrideGuard</code> in an e2e test <b>bypasses</b> the guard entirely so you can test downstream behavior (the controller/service logic) without needing valid auth on every single request — it answers "does the protected route work once authorized," not "does the guard correctly authorize."</p><p>A unit test instantiating the guard directly and calling <code>canActivate()</code> with crafted contexts is what actually verifies the guard's decision logic (e.g. role checks, JWT validation) — the two tests cover orthogonal concerns and neither substitutes for the other.</p>`,
  },
  {
    id: "a8-testing-14",
    category: "testing",
    categoryLabel: "Testing",
    question: `In an e2e suite, why might you reach for overrideGuard(AuthGuard).useValue(...) on the whole TestingModuleBuilder rather than mocking the underlying JwtService the guard depends on?`,
    answerHtml: `<p>Overriding the guard itself is a coarser, more stable seam — it doesn't care how the guard is implemented internally (JWT today, session cookies tomorrow), so refactoring the auth mechanism doesn't break every e2e test that depends on "the user is logged in."</p><p>Mocking <code>JwtService.verify()</code> is the right call when you specifically want to test token validation edge cases (expired, malformed, wrong signature) at the e2e layer; for everything else, bypassing the guard is less brittle and faster to write.</p>`,
  },
  {
    id: "a8-testing-15",
    category: "testing",
    categoryLabel: "Testing",
    question: `What does TestingModuleBuilder.overrideInterceptor() actually replace, and what's a scenario where you'd need it in an e2e test rather than just letting the real interceptor run?`,
    answerHtml: `<p>It swaps out a globally- or controller-bound interceptor's implementation for the duration of that test module, the same override pattern as guards and pipes.</p><p>A common case: a <code>CacheInterceptor</code> or a logging/timing interceptor that has side effects (writes to Redis, calls an external metrics service) you don't want firing during e2e assertions — you override it with a no-op pass-through (<code>intercept: (ctx, next) => next.handle()</code>) so the test stays hermetic.</p>`,
  },
  {
    id: "a8-testing-16",
    category: "testing",
    categoryLabel: "Testing",
    question: `When unit testing a service method that wraps a repository call in a try/catch and rethrows a custom NotFoundException, what two things should the test assert separately?`,
    answerHtml: `<p>First, that the mocked repository method was called with the <b>correct arguments</b> (e.g. <code>expect(repo.findOne).toHaveBeenCalledWith({ where: { id } })</code>) — verifying the service is querying correctly, not just handling errors correctly.</p><p>Second, that when the mock is configured to reject or return <code>null</code>, the service throws the specific exception type (<code>await expect(service.find(id)).rejects.toThrow(NotFoundException)</code>) rather than letting the raw repository error or a generic 500 leak through.</p>`,
  },
  {
    id: "a8-testing-17",
    category: "testing",
    categoryLabel: "Testing",
    question: `Why is mockReset() (or jest.clearAllMocks() in an afterEach) important when reusing the same TestingModule across multiple it() blocks in a describe?`,
    answerHtml: `<p>Jest mock functions accumulate call history and any <code>mockReturnValueOnce</code>/<code>mockResolvedValueOnce</code> queued values across the whole test file unless explicitly cleared — without resetting, a later test can silently consume a mock return value queued by an earlier test, or fail a <code>toHaveBeenCalledTimes</code> assertion because of calls from a previous case.</p><p><code>mockReset()</code> goes further than <code>clearAllMocks</code>: it also strips any custom implementation set via <code>mockImplementation</code>, returning the mock to a blank <code>jest.fn()</code> — important when different tests need genuinely different mock behavior for the same method.</p>`,
  },
  {
    id: "a8-testing-18",
    category: "testing",
    categoryLabel: "Testing",
    question: `You're unit testing a service that injects ConfigService to read an API key. What's the lazy-but-correct way to stub it versus loading real ConfigModule.forRoot() in the test?`,
    answerHtml: `<p>The lazy-correct approach is <code>{ provide: ConfigService, useValue: { get: jest.fn().mockReturnValue('fake-key') } }</code> — you only need the one method (<code>get</code>) your service actually calls, not a real config-loading pipeline reading <code>.env</code> files.</p><p>Loading the real <code>ConfigModule</code> in a unit test couples the test to environment setup (which <code>.env</code> file exists, what's in CI's environment) and turns a deterministic unit test into one that can fail for reasons unrelated to the code under test.</p>`,
  },
  {
    id: "a8-testing-19",
    category: "testing",
    categoryLabel: "Testing",
    question: `If your test module's mock provider object is missing a method that the service calls at runtime, at what point does the test actually fail?`,
    answerHtml: `<p>Nothing fails at <code>compile()</code> time — DI resolution only checks that the <i>token</i> exists, not that the mock's shape matches the real class's interface. The failure surfaces later, at the moment the test actually invokes the missing method, as a runtime <code>TypeError: mock.someMethod is not a function</code>.</p><p>This is the core argument for typed mocking helpers (<code>jest-mock-extended</code>'s <code>mock&lt;T&gt;()</code>, or a hand-written <code>Partial&lt;T&gt;</code> with explicit casting) — they push that failure to compile time in the test file instead of to an opaque runtime error mid-suite.</p>`,
  },
  {
    id: "a8-testing-20",
    category: "testing",
    categoryLabel: "Testing",
    question: `How do you unit test a custom ParamDecorator's extraction logic (e.g. @CurrentUser()) without going through a full controller + HTTP request?`,
    answerHtml: `<p>Param decorators created via <code>createParamDecorator</code> are just thin wrappers around a factory function. The common pattern is to export that underlying factory function separately and unit-test <i>it</i> directly by calling it with a mocked <code>ExecutionContext</code> — no decorator machinery involved.</p><p>This sidesteps needing a real route handler entirely: you call <code>currentUserFactory(undefined, mockExecutionContext)</code> and assert it returns <code>request.user</code> from your fake request object.</p>`,
  },
  {
    id: "a8-testing-21",
    category: "testing",
    categoryLabel: "Testing",
    question: `In an e2e test, what's the meaningful difference between asserting on response.body versus response.headers when verifying a custom interceptor that adds a response header (e.g. X-Request-Id)?`,
    answerHtml: `<p>Supertest's <code>response.body</code> only reflects the JSON payload after Nest's serialization (and any <code>ClassSerializerInterceptor</code> transforms) — it won't show you anything an interceptor wrote via <code>res.setHeader()</code> on the raw underlying response.</p><p>Header-mutating interceptors must be asserted via <code>response.headers['x-request-id']</code>, since interceptors that call <code>context.switchToHttp().getResponse()</code> to set headers operate on the platform-specific response object, which Supertest surfaces separately from the parsed body.</p>`,
  },
  {
    id: "a8-testing-22",
    category: "testing",
    categoryLabel: "Testing",
    question: `Why would you choose Test.createTestingModule({ imports: [AppModule] }) for an e2e test but explicitly avoid that pattern for a unit test of a single service?`,
    answerHtml: `<p>For e2e tests, importing the real <code>AppModule</code> (or a close variant) is the point — you're verifying the wired-together system behaves correctly end-to-end, including real module boundaries, global pipes, and route registration, which only exists if the full graph is built.</p><p>For a unit test, the same approach drags in every dependency transitively, including ones with side effects (DB connections, schedulers, external API clients) — the test becomes slow, flaky, and tests far more surface area than the single service under test, defeating the purpose of a unit test.</p>`,
  },
  {
    id: "a8-testing-23",
    category: "testing",
    categoryLabel: "Testing",
    question: `When testing a guard that depends on an async call (e.g. validating a token against a database session store), why must the test await the guard's canActivate() result even though the interface signature allows a synchronous boolean?`,
    answerHtml: `<p><code>CanActivate.canActivate()</code> is typed to return <code>boolean | Promise&lt;boolean&gt; | Observable&lt;boolean&gt;</code> — Nest's runtime handles all three, but a test calling it directly must match whichever form the specific guard returns, or it will assert against an unresolved <code>Promise</code> object (which is always truthy) instead of the actual boolean.</p><p>The safe pattern is simply <code>await guard.canActivate(ctx)</code> when you know it's promise-based — awaiting a plain boolean is also safe since <code>await</code> on a non-promise just resolves immediately.</p>`,
  },
  {
    id: "a8-testing-24",
    category: "testing",
    categoryLabel: "Testing",
    question: `What's a subtle reason a unit test for a service using @InjectRepository(Entity) might pass locally but the equivalent TypeORM query could fail in production?`,
    answerHtml: `<p>Mocking the repository's <code>findOne</code>/<code>save</code> methods verifies your service calls them with the arguments you expect, but it never validates that those arguments form a <b>valid TypeORM query</b> — a typo'd relation name in a <code>relations: ['authour']</code> option or an invalid <code>where</code> clause shape will happily pass a mocked unit test and only blow up against a real database connection.</p><p>This is exactly the gap e2e/integration tests with a real (or Testcontainers) database are meant to cover — unit tests verify your service's branching logic, not the correctness of the ORM query itself.</p>`,
  },
];

export const ADVANCED8_QUIZ: QuizQuestion[] = [
  {
    id: "a8q-testing-1",
    category: "testing",
    categoryLabel: "Testing",
    question: `A unit test builds its TestingModule by importing the real UsersModule (which itself imports TypeOrmModule.forFeature([User])) instead of declaring UsersService and a mock repository directly. What's the most likely consequence?`,
    options: [`The test runs identically either way since Nest tree-shakes unused providers at compile time`, `compile() throws because TypeOrmModule.forFeature requires a real database connection that isn't configured in the test, unless the repository token is overridden`, `Nest automatically mocks any provider that requires external I/O`, `The test will pass but run twice as slow with no other effect`],
    answer: 1,
    explanationHtml: `<p>Importing a module that wires up TypeORM pulls in real connection-dependent providers. Without a configured database or an <code>overrideProvider</code> for the repository token, <code>compile()</code> fails trying to resolve a connection that doesn't exist in the test context.</p>`,
  },
  {
    id: "a8q-testing-2",
    category: "testing",
    categoryLabel: "Testing",
    question: `In an e2e Supertest test, why is request(app.getHttpServer()) preferred over starting the app with app.listen(3000) and pointing Supertest at http://localhost:3000?`,
    options: [`getHttpServer() returns a server that automatically resets the database between requests`, `listen() is deprecated in NestJS 11`, `getHttpServer() runs in-process against an unbound server instance, avoiding port conflicts and real network overhead in parallel test runs`, `Supertest cannot make requests to a real bound port at all`],
    answer: 2,
    explanationHtml: `<p><code>app.getHttpServer()</code> exposes the raw <code>http.Server</code> without binding a fixed port; Supertest handles the ephemeral listening internally. This keeps e2e suites fast and avoids the port-collision issues that come from hardcoding a real listening port across parallel CI jobs.</p>`,
  },
  {
    id: "a8q-testing-3",
    category: "testing",
    categoryLabel: "Testing",
    question: `You override a route's guard in an e2e test using .overrideGuard(RolesGuard).useValue({ canActivate: () => true }). What does this test now NOT verify?`,
    options: [`Whether the controller method's business logic runs correctly`, `Whether the route is registered under the correct HTTP path`, `Whether RolesGuard correctly rejects requests from users without the required role`, `Whether the response body is serialized correctly`],
    answer: 2,
    explanationHtml: `<p>Forcing <code>canActivate</code> to always return <code>true</code> bypasses the guard's actual authorization decision entirely. The test now exercises everything downstream of the guard, but says nothing about whether the guard itself correctly distinguishes authorized from unauthorized requests — that needs a separate unit test on the guard.</p>`,
  },
  {
    id: "a8q-testing-4",
    category: "testing",
    categoryLabel: "Testing",
    question: `A NestInterceptor's intercept() method is being unit tested. The fake CallHandler's handle() returns of({ id: 1 }) from RxJS. Why is this necessary instead of just returning { id: 1 } directly?`,
    options: [`Because intercept() requires next.handle() to return an Observable, matching what a real route handler's pipeline produces`, `Because of() makes the test run faster than a plain object`, `Because Jest cannot mock plain object returns`, `Because TypeScript requires all mock return values to be wrapped in RxJS operators`],
    answer: 0,
    explanationHtml: `<p>Nest's interceptor contract is built on RxJS: <code>CallHandler.handle()</code> always returns an <code>Observable</code> representing the eventual route handler response, and the interceptor pipes operators onto it. A fake <code>CallHandler</code> must honor that contract or the interceptor's <code>.pipe(map(...))</code> chain has nothing valid to operate on.</p>`,
  },
  {
    id: "a8q-testing-5",
    category: "testing",
    categoryLabel: "Testing",
    question: `Why does main.ts's app.useGlobalPipes(new ValidationPipe()) have no effect on an e2e test that builds its own app via Test.createTestingModule(...).compile()?`,
    options: [`ValidationPipe is automatically global in every NestJS application regardless of where it's registered`, `main.ts's bootstrap() function is never executed during a test run, so any setup only performed there must be duplicated in the test`, `Global pipes only apply to GraphQL resolvers, not REST controllers`, `ValidationPipe requires a real HTTP port to function in any test`],
    answer: 1,
    explanationHtml: `<p>Test suites construct the <code>INestApplication</code> independently of <code>main.ts</code>. Any global configuration (pipes, filters, interceptors) applied only inside <code>bootstrap()</code> doesn't exist in the test's app instance unless explicitly re-applied, which is why teams extract that setup into a shared, reusable function.</p>`,
  },
  {
    id: "a8q-testing-6",
    category: "testing",
    categoryLabel: "Testing",
    question: `A guard's canActivate calls this.reflector.getAllAndOverride(IS_PUBLIC_KEY, [context.getHandler(), context.getClass()]). In a unit test for this guard, what's the correct way to control its behavior without a real controller?`,
    options: [`Mock the Reflector instance's getAllAndOverride method to return the desired value directly; the ExecutionContext's getHandler/getClass can be dummy stub functions`, `Instantiate a full NestJS application and make a real HTTP request to a decorated route`, `The guard cannot be unit tested in isolation; it requires e2e testing`, `Use Reflect.getMetadata directly instead of going through the guard, bypassing the guard's own logic`],
    answer: 0,
    explanationHtml: `<p>Mocking the <code>Reflector</code>'s method isolates the guard's decision logic from Nest's actual metadata-reflection machinery. The <code>ExecutionContext</code>'s handler/class accessors just need to return something (their actual content is irrelevant since the reflector mock ignores it), letting the test focus purely on "given this reflector result, does the guard allow or deny."</p>`,
  },
];
