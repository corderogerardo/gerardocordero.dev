// Batch 12 — Event loop phases (timers, poll, check), libuv thread pool, streams backpressure, and cluster/worker_threads for CPU-bound work in a Nest app.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED12_FLASHCARDS: Flashcard[] = [
  {
    id: "a12-node-internals-1",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Node's event loop has six phases per libuv. Name the phases in order and what runs in each.`,
    answerHtml: `<p>Per tick: <b>timers</b> (expired <code>setTimeout</code>/<code>setInterval</code> callbacks) → <b>pending callbacks</b> (deferred I/O callbacks like certain TCP errors) → <b>idle/prepare</b> (internal use) → <b>poll</b> (retrieve new I/O events, run their callbacks; can block here waiting for I/O) → <b>check</b> (<code>setImmediate</code> callbacks) → <b>close callbacks</b> (e.g. <code>socket.on('close')</code>). Between every phase transition and after every callback, Node drains the <b>microtask queues</b> (<code>process.nextTick</code> first, then Promise jobs).</p>`,
  },
  {
    id: "a12-node-internals-2",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Why can \`setImmediate\` fire before a \`setTimeout(fn, 0)\` in one context but after it in another?`,
    answerHtml: `<p>It depends on <b>where in the loop you are when you schedule them</b>. From the <b>main module's top level</b>, timer resolution and startup overhead make the order non-deterministic — either can win. From <b>inside an I/O callback</b> (poll phase), the order is deterministic: <code>setImmediate</code> always runs first because <b>check</b> is the very next phase, while the timer has to wait for the loop to wrap back around to the <b>timers</b> phase.</p>`,
  },
  {
    id: "a12-node-internals-3",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `What is the practical difference between \`process.nextTick()\` and a resolved \`Promise.then()\` callback, given both are microtasks?`,
    answerHtml: `<p>Both run before the event loop continues, but Node maintains them as <b>two separate queues</b>, and the <code>nextTick</code> queue is fully drained <b>first</b>, before the Promise microtask queue, after every callback. So a chain of recursive <code>process.nextTick</code> calls will <b>starve Promise callbacks</b> (and I/O) indefinitely — this is why libraries avoid unbounded <code>nextTick</code> recursion.</p>`,
  },
  {
    id: "a12-node-internals-4",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Why does recursive \`process.nextTick()\` scheduling starve I/O, and what's a concrete failure mode in a Nest interceptor or guard?`,
    answerHtml: `<p>The event loop won't move to the <b>poll phase</b> (where new connections/sockets are accepted) until the <code>nextTick</code> queue is empty. If a guard or interceptor recursively re-schedules work via <code>process.nextTick</code> (e.g. a retry loop or a buggy recursive async helper that isn't truly yielding to I/O), incoming HTTP requests stop being processed entirely — the server appears hung even though CPU usage looks idle-ish or pegged on one core.</p>`,
  },
  {
    id: "a12-node-internals-5",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `What kinds of operations actually use the libuv thread pool, versus running on the kernel's async I/O directly?`,
    answerHtml: `<p>The thread pool (default size <b>4</b>) backs operations that have no native async OS primitive: most of the <code>fs</code> module, <code>dns.lookup()</code> (not <code>dns.resolve*</code>, which uses c-ares and is non-blocking), <code>crypto.pbkdf2</code>/<code>scrypt</code>/<code>randomBytes</code> (async forms), and <code>zlib</code>. <b>TCP/UDP networking</b> (HTTP servers, most DB drivers) uses the OS's native async I/O (epoll/kqueue/IOCP) directly, bypassing the thread pool entirely — that's why network-bound Node services scale far better than naive intuition suggests.</p>`,
  },
  {
    id: "a12-node-internals-6",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `An endpoint calls \`bcrypt.hash()\` (async) for every login. Under load, response latency degrades non-linearly. Why, and what's the fix?`,
    answerHtml: `<p><code>bcrypt</code>'s async API still runs on the <b>4-thread libuv pool</b>, shared with all <code>fs</code>, <code>zlib</code>, and DNS-lookup work. Once 4 concurrent hash operations are in flight, the 5th queues behind them — even though the event loop itself is free. Fixes: raise <code>UV_THREADPOOL_SIZE</code> (env var, set before the pool initializes, max 1024), offload hashing to a <code>worker_threads</code> pool dedicated to CPU work, or move password hashing to a separate microservice so it doesn't contend with the API's other thread-pool consumers.</p>`,
  },
  {
    id: "a12-node-internals-7",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `How do you resize the libuv thread pool, and what's the catch with when it takes effect?`,
    answerHtml: `<p>Set the environment variable <code>UV_THREADPOOL_SIZE</code> (e.g. <code>UV_THREADPOOL_SIZE=16 node server.js</code>), max effective value is <b>1024</b> (libuv's <code>MAX_THREADPOOL_SIZE</code>). The catch: libuv reads this value <b>once, lazily, the first time the pool is needed</b> — setting <code>process.env.UV_THREADPOOL_SIZE</code> at runtime inside your app code works only if it happens before any thread-pool-backed call has fired; many apps set it too late (e.g. after an early <code>fs.readFile</code>) and the change silently doesn't apply.</p>`,
  },
  {
    id: "a12-node-internals-8",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `A Nest service does synchronous JSON.stringify on a 50MB object inside a request handler. Why does this hurt every other request, not just the slow one?`,
    answerHtml: `<p>Node is <b>single-threaded for JS execution</b>. A long synchronous operation blocks the <b>entire event loop</b> — no other request's callback, timer, or I/O completion can be processed until it returns, regardless of thread-pool or kernel async I/O. The fix is not "make it async" (sync CPU work doesn't become non-blocking by wrapping in a Promise) — it requires either chunking the work with yields, streaming serialization, or offloading to a <code>worker_thread</code>.</p>`,
  },
  {
    id: "a12-node-internals-9",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `When should you reach for \`cluster\` vs \`worker_threads\` in a Nest app — what's the actual decision axis?`,
    answerHtml: `<p><b>Cluster</b>: multiple independent <b>processes</b>, each with its own V8 heap and event loop, sharing a listening socket (via the OS or a round-robin dispatch in the primary). Best for <b>scaling out the whole HTTP-handling workload</b> across CPU cores — no shared memory, IPC only via serialization. <b>worker_threads</b>: multiple <b>threads in one process</b>, can share memory via <code>SharedArrayBuffer</code>/<code>MessageChannel</code> with structured clone (cheaper than IPC serialization). Best for <b>offloading one specific CPU-bound task</b> (image processing, crypto, parsing) from request handlers while keeping a single app instance/memory space.</p>`,
  },
  {
    id: "a12-node-internals-10",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Why is PM2 or Node's \`cluster\` module usually the wrong layer for scaling a containerized Nest service on Kubernetes?`,
    answerHtml: `<p>Kubernetes already gives you process-level isolation, independent restarts, and horizontal scaling via <b>replicas</b> at the pod level, with the orchestrator handling health checks and load balancing. Running <code>cluster</code> inside each pod duplicates that mechanism, complicates graceful shutdown and readiness-probe semantics (which worker is "ready"?), and makes per-pod memory/CPU limits harder to reason about. The common pattern is <b>one process per container</b> and let k8s do the multi-instance scaling.</p>`,
  },
  {
    id: "a12-node-internals-11",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `What does it mean for a \`Writable\` stream to return \`false\` from \`.write()\`, and what must the producer do in response?`,
    answerHtml: `<p><code>false</code> means the stream's internal buffer has exceeded <code>highWaterMark</code> — the consumer (e.g. disk, network socket) can't keep up. The producer <b>must stop writing</b> and wait for the <code>'drain'</code> event before calling <code>.write()</code> again. Ignoring this and writing anyway doesn't throw, but it defeats backpressure: the buffer grows unbounded, which is a classic Node memory-leak pattern when piping a fast source (e.g. a DB cursor) into a slow sink (e.g. a rate-limited HTTP upload) without using <code>.pipe()</code> or manually honoring <code>'drain'</code>.</p>`,
  },
  {
    id: "a12-node-internals-12",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Why does \`readableStream.pipe(writableStream)\` solve backpressure automatically, and what's the catch with \`pipe()\` and error handling?`,
    answerHtml: `<p><code>.pipe()</code> wires the readable's <code>'data'</code> flow to the writable's <code>.write()</code> return value internally: when <code>.write()</code> returns <code>false</code>, it calls <code>.pause()</code> on the source; on <code>'drain'</code>, it calls <code>.resume()</code>. The catch: <b>errors are not forwarded</b> — an error on the source stream does not propagate to the destination (or vice versa), so each stream needs its own <code>'error'</code> listener, or you reach for <code>stream.pipeline()</code> (from <code>node:stream</code>), which forwards errors and guarantees cleanup/destroy of all streams in the chain.</p>`,
  },
  {
    id: "a12-node-internals-13",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `In Nest, a controller streams a large CSV export by reading rows from Postgres and piping through a Transform to the HTTP response. What backpressure failure mode should you specifically guard against with \`res\` (the underlying Node \`ServerResponse\`)?`,
    answerHtml: `<p>If the client (e.g. a slow mobile connection or a stalled download) stops reading, <code>res.write()</code> starts returning <code>false</code>. If your DB cursor keeps pulling rows regardless (common when using <code>for await</code> over a cursor without checking the write's return value, or piping through a Transform with no <code>highWaterMark</code> tuning), rows accumulate in the Transform/response buffer — memory grows per slow client. The fix: use <code>stream.pipeline(dbCursorStream, transform, res, callback)</code> so backpressure and cancellation propagate end-to-end, and so a client disconnect (<code>res</code> closing) destroys the upstream DB cursor instead of leaking it.</p>`,
  },
  {
    id: "a12-node-internals-14",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `What is \`highWaterMark\` actually controlling, and what's a common misconception about it?`,
    answerHtml: `<p>It's the <b>buffer size threshold</b> (bytes for binary streams, default 64KB / 65536 bytes; object count for object-mode streams, default 16) at which <code>.write()</code> starts returning <code>false</code> or the internal read buffer is considered "full." Misconception: it is <b>not a hard cap</b> — a stream can still buffer somewhat beyond it (the producer can ignore the <code>false</code> return), and for <code>Readable</code> streams in flowing mode it's a <i>target</i> for how much to read ahead, not a strict limit on total memory used.</p>`,
  },
  {
    id: "a12-node-internals-15",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Why is \`worker_threads\` generally preferred over child processes (\`child_process.fork\`) for CPU-bound work inside a single Nest service instance?`,
    answerHtml: `<p><code>worker_threads</code> share the same process (lower spawn cost, no separate V8/Node runtime instantiation) and can pass data via <code>SharedArrayBuffer</code> or transferable <code>ArrayBuffer</code>s with <b>zero-copy transfer</b>, or structured-clone for everything else — both far cheaper than the <b>serialize-to-string IPC</b> that <code>child_process</code> uses. <code>fork()</code> still makes sense when you need true process isolation (a crash shouldn't take down the parent) or need to run a different executable/runtime.</p>`,
  },
  {
    id: "a12-node-internals-16",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `How do you actually integrate a \`worker_threads\` pool into a NestJS provider without blocking the event loop on coordination overhead?`,
    answerHtml: `<p>Wrap a fixed-size pool of <code>Worker</code> instances (e.g. via <code>piscina</code> or hand-rolled) behind an injectable service whose methods return Promises resolved on the worker's <code>'message'</code> event. Critically: the pool, not the request, should be the long-lived object — spawning a new <code>Worker</code> per request reintroduces heavy startup cost (V8 isolate + module re-evaluation), often making it slower than just doing the work on the main thread for small payloads.</p>`,
  },
  {
    id: "a12-node-internals-17",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `What's the difference in fault isolation between a worker thread throwing an uncaught exception vs. a cluster worker process crashing?`,
    answerHtml: `<p>An uncaught exception in a <b>worker thread</b> emits an <code>'error'</code> event on the corresponding <code>Worker</code> object in the parent and terminates only that thread — the parent process and other workers keep running, but you must explicitly handle the error and possibly respawn the worker. A <b>cluster worker process</b> crashing is fully isolated at the OS level: it dies independently, the primary gets an <code>'exit'</code> event on <code>cluster</code>, and in-flight requests on <i>other</i> workers are completely unaffected — process-level isolation is strictly stronger than thread-level for crash containment.</p>`,
  },
  {
    id: "a12-node-internals-18",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Why doesn't wrapping a CPU-heavy synchronous function in \`async\`/await or a \`Promise\` make it non-blocking?`,
    answerHtml: `<p><code>async</code> functions still execute their synchronous body <b>to completion in one go</b> on the main thread before yielding at the first genuine <code>await</code> of an async operation (timer, I/O, or another microtask). Wrapping a tight CPU loop in a Promise just defers when it starts (push to microtask queue) — once it starts running, it blocks the loop exactly as a plain function would. True non-blocking requires either cooperative yielding (chunking with <code>setImmediate</code>/<code>await new Promise(setImmediate)</code> between chunks) or moving the work off the main thread entirely (<code>worker_threads</code>).</p>`,
  },
  {
    id: "a12-node-internals-19",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `A Nest app under load shows high p99 latency but low average CPU usage. What event-loop-specific diagnostic would you reach for first, and what does it measure?`,
    answerHtml: `<p><b>Event loop lag/delay</b> — measured via <code>perf_hooks.monitorEventLoopDelay()</code> (a histogram of the gap between expected and actual timer fire times) or libraries like <code>toobusy-js</code>. High p99 with low average CPU is the signature of <b>event loop contention from bursts</b> — e.g. periodic synchronous work (a big <code>JSON.parse</code>, a synchronous regex with catastrophic backtracking, GC pauses) that's rare enough to not move the average but blocks every concurrent request when it happens.</p>`,
  },
  {
    id: "a12-node-internals-20",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Why can a poorly written regular expression in a NestJS validation pipe cause a full outage rather than just slow one request?`,
    answerHtml: `<p>Regex execution in V8 is <b>synchronous and runs on the main thread</b> with no preemption. A pattern vulnerable to <b>catastrophic backtracking</b> (e.g. nested quantifiers like <code>(a+)+b</code> against a crafted non-matching input) can take seconds to minutes for a single call. Since this runs inline in a <code>class-validator</code> decorator or a custom pipe during request handling, it blocks the event loop for <i>all</i> concurrent connections — a classic ReDoS (Regular Expression Denial of Service) vector, and a strong reason to fuzz-test or bound validation regexes used on user-controlled input.</p>`,
  },
  {
    id: "a12-node-internals-21",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `What's the difference between \`Readable\` stream "flowing" and "paused" modes, and why does attaching a \`'data'\` listener have a side effect you might not expect?`,
    answerHtml: `<p>In <b>paused mode</b>, you explicitly pull data via <code>.read()</code>. In <b>flowing mode</b>, data is pushed to consumers automatically as it arrives. Attaching a <code>'data'</code> event listener (or calling <code>.pipe()</code> or <code>.resume()</code>) <b>switches the stream into flowing mode</b> as a side effect — if you do this before you're ready to handle backpressure manually, data starts flowing immediately and can be lost if there's no listener attached fast enough, or it can overwhelm a slow consumer if you're not honoring <code>.pause()</code>/<code>.resume()</code> yourself.</p>`,
  },
  {
    id: "a12-node-internals-22",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Why does Node's HTTP server handle thousands of idle keep-alive connections cheaply, but a thread-pool-bound operation (like \`fs.readFile\`) doesn't scale the same way?`,
    answerHtml: `<p>Idle sockets cost the kernel a file descriptor and Node a small JS object — the OS's epoll/kqueue mechanism scales to tens of thousands of idle descriptors with O(1)-ish overhead per readiness check, and no thread is dedicated per connection. <code>fs.readFile</code>, by contrast, occupies one of only <b>4 (default) thread-pool slots</b> for its entire duration regardless of how "idle" the disk I/O looks to JS — so thread-pool-bound work has a hard concurrency ceiling that network I/O simply doesn't.</p>`,
  },
  {
    id: "a12-node-internals-23",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `When a NestJS app receives SIGTERM (e.g. Kubernetes rolling deploy) while a worker pool has in-flight tasks, what's the correct shutdown sequence?`,
    answerHtml: `<p>1) Stop accepting new HTTP connections (close the server's listen socket / use Nest's <code>enableShutdownHooks</code>). 2) Let in-flight requests drain. 3) For any active <code>worker_threads</code>, wait for outstanding tasks to finish (or apply a bounded timeout) before calling <code>worker.terminate()</code> — terminating mid-task abandons whatever computation was in flight and can leave the requesting HTTP response hanging or rejected ungracefully. Skipping the drain step is a common cause of 502s during rolling deploys under load.</p>`,
  },
];

export const ADVANCED12_QUIZ: QuizQuestion[] = [
  {
    id: "a12q-node-internals-1",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `From inside an \`fs.readFile\` callback in a Node/Nest app, you schedule both \`setTimeout(fn, 0)\` and \`setImmediate(fn2)\`. Which fires first, and why?`,
    options: [`setTimeout, because timers always have priority over check callbacks`, `setImmediate, because the poll phase transitions directly into the check phase, while the timer must wait for the loop to cycle back to the timers phase`, `It's random in every case, identical to scheduling them at the top level`, `They fire simultaneously since both are macrotasks of equal priority`],
    answer: 1,
    explanationHtml: `<p>Inside a poll-phase callback, the loop's very next phase is <b>check</b> (where <code>setImmediate</code> lives), so it fires deterministically before the loop wraps around to <b>timers</b>. This ordering guarantee only holds when scheduled from within I/O callbacks — at the top level it's non-deterministic.</p>`,
  },
  {
    id: "a12q-node-internals-2",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `A Nest service calls \`dns.lookup()\` heavily for outbound requests, and you notice it's contending with \`fs\` and \`crypto.pbkdf2\` calls for the same limited resource. What's the most direct explanation?`,
    options: [`dns.lookup() uses the OS network stack directly, so this contention is a red herring`, `dns.lookup() shares the libuv thread pool with fs and certain crypto operations, all bounded by UV_THREADPOOL_SIZE (default 4)`, `dns.lookup() runs on the V8 microtask queue, which is starved by crypto's macrotasks`, `dns.lookup() spawns its own OS thread per call, unrelated to libuv's pool`],
    answer: 1,
    explanationHtml: `<p>Unlike <code>dns.resolve*</code> (which uses the non-blocking c-ares library), <code>dns.lookup()</code> goes through libuv's <b>thread pool</b> just like most of <code>fs</code> and async <code>crypto</code> functions — all sharing the same default 4 threads, so heavy use of one starves the others.</p>`,
  },
  {
    id: "a12q-node-internals-3",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Why does \`stream.pipeline()\` get recommended over manual \`.pipe()\` chains for a Nest controller streaming a file transform to the HTTP response?`,
    options: [`pipeline() is faster because it bypasses backpressure checks`, `pipeline() automatically forwards errors across the whole chain and ensures all streams are properly destroyed/cleaned up, which raw pipe() does not do`, `pipeline() converts the stream to a Buffer first, which is more memory-efficient`, `pipeline() is required because Express/Nest cannot accept a Readable stream directly`],
    answer: 1,
    explanationHtml: `<p><code>.pipe()</code> doesn't propagate errors between streams or guarantee cleanup on failure — each piped stream needs its own <code>'error'</code> handler, and a mid-chain error can leave other streams open. <code>stream.pipeline()</code> (or <code>pipeline</code> from <code>node:stream/promises</code>) forwards errors and destroys every stream in the chain on either completion or failure.</p>`,
  },
  {
    id: "a12q-node-internals-4",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `A staff engineer proposes adding \`cluster.fork()\` calls inside a NestJS app that already runs as one container per pod under Kubernetes with \`replicas: 4\`. What's the strongest objection?`,
    options: [`cluster is deprecated and no longer ships with Node.js`, `It duplicates scaling/process-isolation work the orchestrator already does, and complicates graceful shutdown and readiness-probe semantics per worker`, `cluster workers cannot share a listening port, so it would simply fail to start`, `Kubernetes pods are not allowed to spawn child processes`],
    answer: 1,
    explanationHtml: `<p>Kubernetes already provides multi-replica scaling, independent restarts, and health-check-driven traffic routing at the pod level. Layering <code>cluster</code> inside each pod adds a second, redundant scaling mechanism and makes it ambiguous which intra-pod worker is "ready" or how resource limits should be interpreted — the standard pattern is one process per container.</p>`,
  },
  {
    id: "a12q-node-internals-5",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Your Nest app wraps a CPU-intensive synchronous image-resize loop in an \`async\` function and \`await\`s a \`Promise.resolve()\` before calling it, expecting this to keep the server responsive. What actually happens under concurrent requests?`,
    options: [`The event loop automatically time-slices the synchronous work across requests`, `The function still runs to completion synchronously once it starts, blocking the event loop and all other concurrent requests for its full duration`, `Node spawns a hidden thread-pool worker for any function marked async`, `The await Promise.resolve() defers the work to a worker_thread automatically`],
    answer: 1,
    explanationHtml: `<p>An <code>await Promise.resolve()</code> only defers the <i>start</i> of the function body to the next microtask tick — it does nothing to make synchronous CPU-bound code yield mid-execution. Once the loop runs, it blocks until completion. Real concurrency for CPU-bound work requires offloading to <code>worker_threads</code> or chunking with explicit yields.</p>`,
  },
  {
    id: "a12q-node-internals-6",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Why is \`process.nextTick()\` recursion considered more dangerous than \`setImmediate()\` recursion for starving I/O in a Node server?`,
    options: [`process.nextTick callbacks are drained completely before the loop can proceed to any other phase (including poll, where new I/O is accepted), while setImmediate only runs once per loop iteration in the check phase`, `process.nextTick is slower per call than setImmediate, so it accumulates backlog faster`, `setImmediate callbacks are automatically rate-limited by libuv, while nextTick callbacks are not`, `There is no real difference; both starve I/O identically`],
    answer: 0,
    explanationHtml: `<p>The <code>nextTick</code> queue is fully emptied after every callback, before the loop moves on at all — including to poll, where new connections are accepted. Recursive scheduling there can starve I/O indefinitely. <code>setImmediate</code> callbacks queued during check still let one full loop iteration (including poll) happen per cycle, so they're far less likely to fully starve I/O.</p>`,
  },
  {
    id: "a12q-node-internals-7",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `A worker_threads-based pool in a Nest provider shares state between workers using a \`SharedArrayBuffer\` and coordinates with \`Atomics.wait()\`/\`Atomics.notify()\`. Where is it safe to call \`Atomics.wait()\`?`,
    options: [`Only on the main thread, since workers can't access shared memory directly`, `Anywhere, since Atomics.wait() is always asynchronous and non-blocking`, `Only inside a worker thread — calling it on the main thread blocks the entire event loop, defeating the purpose of offloading work`, `Only inside process.nextTick callbacks`],
    answer: 2,
    explanationHtml: `<p><code>Atomics.wait()</code> is a genuinely <b>blocking</b> call. On the main thread it would freeze the whole event loop — the same problem worker_threads exist to avoid. It's only appropriate inside a worker thread waiting on another worker, never on the thread serving HTTP requests.</p>`,
  },
];
