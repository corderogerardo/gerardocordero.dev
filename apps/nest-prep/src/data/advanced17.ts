// Batch 17 — Circular dependencies: forwardRef() for providers and modules, using ModuleRef.get() to break a cycle, why circular deps usually signal a module-boundary smell, and refactoring strategies to remove them.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED17_FLASHCARDS: Flashcard[] = [
  {
    id: "a17-circular-deps-1",
    category: "di",
    categoryLabel: "DI",
    question: `What exactly is a circular dependency in NestJS's DI graph, and why can't the injector resolve it the normal way?`,
    answerHtml: `<p>A circular dependency exists when two (or more) providers or modules depend on each other directly or transitively — e.g. <code>ServiceA</code> needs <code>ServiceB</code> in its constructor and <code>ServiceB</code> needs <code>ServiceA</code> in its constructor. Nest resolves constructor injection bottom-up: to build <code>A</code> it needs a fully constructed <code>B</code>, but to build <code>B</code> it needs a fully constructed <code>A</code>. Neither can finish first, so there is no valid instantiation order and the resolver throws during bootstrap.</p>`,
  },
  {
    id: "a17-circular-deps-2",
    category: "di",
    categoryLabel: "DI",
    question: `How does forwardRef() actually break the cycle, mechanically?`,
    answerHtml: `<p><code>forwardRef(() => ServiceB)</code> wraps the reference in a lazily-evaluated function instead of a direct class reference. Nest doesn't need the actual class symbol at module-load/decorator-metadata time — it stores the thunk and invokes it during a later resolution pass once both classes are registered. This breaks the <i>evaluation order</i> problem, not the dependency itself: the runtime objects still reference each other, but Nest can now construct them by injecting a proxy reference that gets backfilled once the real instance exists.</p>`,
  },
  {
    id: "a17-circular-deps-3",
    category: "di",
    categoryLabel: "DI",
    question: `Where exactly does forwardRef() need to be applied when two providers in the same module depend on each other?`,
    answerHtml: `<p>On both sides of the cycle, inside the constructor parameter decorator: <code>constructor(@Inject(forwardRef(() => ServiceB)) private b: ServiceB)</code> in <code>ServiceA</code>, and the mirrored <code>@Inject(forwardRef(() => ServiceA))</code> in <code>ServiceB</code>. Wrapping only one side still leaves the other constructor parameter unresolved at the point Nest needs the class reference, so the build still fails.</p>`,
  },
  {
    id: "a17-circular-deps-4",
    category: "di",
    categoryLabel: "DI",
    question: `Where does forwardRef() need to be applied when the cycle is between two modules (not just two providers)?`,
    answerHtml: `<p>In each module's <code>imports</code> array, on both sides: <code>@Module({ imports: [forwardRef(() => ModuleB)] })</code> in <code>ModuleA</code>, and <code>@Module({ imports: [forwardRef(() => ModuleA)] })</code> in <code>ModuleB</code>. This is a separate concern from provider-level <code>forwardRef()</code> — a cross-module cycle additionally needs the consuming provider's class to be exported from its module, or injection still fails with a "not part of the X module" error even after the module-level cycle is resolved.</p>`,
  },
  {
    id: "a17-circular-deps-5",
    category: "di",
    categoryLabel: "DI",
    question: `If ServiceA and ServiceB live in different modules and depend on each other, what four forwardRef() touch points are typically required?`,
    answerHtml: `<p>1) <code>ModuleA</code> imports <code>forwardRef(() => ModuleB)</code>; 2) <code>ModuleB</code> imports <code>forwardRef(() => ModuleA)</code>; 3) <code>ServiceA</code>'s constructor injects <code>ServiceB</code> via <code>@Inject(forwardRef(() => ServiceB))</code>; 4) <code>ServiceB</code>'s constructor injects <code>ServiceA</code> the same way. Missing any one of the four still throws — this is exactly why teams treat the four-point dance as a strong signal to refactor instead.</p>`,
  },
  {
    id: "a17-circular-deps-6",
    category: "di",
    categoryLabel: "DI",
    question: `Does forwardRef() work with useFactory or useValue custom providers the same way it works with class providers?`,
    answerHtml: `<p>Yes, but the wrapping point differs: for a factory provider you wrap the <code>inject</code> array entries, e.g. <code>{ provide: 'TOKEN', useFactory: (b) => new Thing(b), inject: [forwardRef(() => ServiceB)] }</code>. The factory function itself still only runs once Nest has resolved the forward-referenced dependency, so the factory body sees a real instance, not a placeholder.</p>`,
  },
  {
    id: "a17-circular-deps-7",
    category: "di",
    categoryLabel: "DI",
    question: `What is ModuleRef, and what category of problem does it solve that forwardRef() doesn't?`,
    answerHtml: `<p><code>ModuleRef</code> is an injectable utility class giving programmatic access to the current module's provider container at runtime — <code>get()</code>, <code>resolve()</code> (for scoped/transient providers), and <code>create()</code> (to instantiate a class outside the container). Unlike <code>forwardRef()</code>, which still wires a real constructor-time dependency (just lazily), <code>ModuleRef.get()</code> lets you fetch a provider <i>on demand, after construction</i>, removing the dependency from the constructor signature entirely — useful when you only need the other service occasionally, not as a structural dependency.</p>`,
  },
  {
    id: "a17-circular-deps-8",
    category: "di",
    categoryLabel: "DI",
    question: `How do you use ModuleRef.get() to break a cycle instead of forwardRef()?`,
    answerHtml: `<p>Inject <code>ModuleRef</code> itself (no cycle there, since it's a framework-provided global-ish utility), then resolve the needed provider lazily inside a method body: <code>constructor(private moduleRef: ModuleRef) {}</code> ... <code>someMethod() { const b = this.moduleRef.get(ServiceB, { strict: false }); b.doThing(); }</code>. Because <code>ServiceB</code> is fetched at call time rather than declared as a constructor parameter, the two classes no longer have a static circular reference for Nest to fail on during instantiation.</p>`,
  },
  {
    id: "a17-circular-deps-9",
    category: "di",
    categoryLabel: "DI",
    question: `What does the { strict: false } option do on ModuleRef.get(), and when do you need it?`,
    answerHtml: `<p>By default <code>ModuleRef.get()</code> only looks in the <i>current module's</i> provider scope (the strict context). Passing <code>{ strict: false }</code> tells Nest to search the entire application's container graph for a matching provider, which is required when the provider you want lives in a different module than the one calling <code>get()</code> — the common case when breaking a cross-module cycle.</p>`,
  },
  {
    id: "a17-circular-deps-10",
    category: "di",
    categoryLabel: "DI",
    question: `Why can't ModuleRef.get() be called inside a constructor the way forwardRef()-injected dependencies can?`,
    answerHtml: `<p>At constructor time the DI container may not have finished instantiating every provider yet — that's the whole reason the cycle existed in the first place. <code>ModuleRef.get()</code> (and <code>resolve()</code>) are meant to be called after the module has fully bootstrapped, typically inside a request-handling method, a lifecycle hook such as <code>onApplicationBootstrap()</code>, or lazily on first use — not synchronously during construction, where the target provider may still be mid-instantiation or undefined.</p>`,
  },
  {
    id: "a17-circular-deps-11",
    category: "di",
    categoryLabel: "DI",
    question: `When would you reach for ModuleRef.resolve() instead of ModuleRef.get(), in the context of breaking a cycle involving a request-scoped provider?`,
    answerHtml: `<p><code>get()</code> only returns singleton (DEFAULT-scope) instances from the static container. If the provider you need to break the cycle with is <code>REQUEST</code> or <code>TRANSIENT</code> scoped, you must use <code>resolve()</code>, which is asynchronous and returns a unique instance per resolution (or per <code>contextId</code> if you pass one via <code>ContextIdFactory</code>), because scoped providers don't live in the static DI tree that <code>get()</code> reads from.</p>`,
  },
  {
    id: "a17-circular-deps-12",
    category: "di",
    categoryLabel: "DI",
    question: `Why is a circular dependency between two NestJS modules usually treated as an architectural smell rather than just a technical hurdle to route around?`,
    answerHtml: `<p>A module is supposed to represent a bounded, cohesive unit of domain logic with a one-directional dependency on the things it needs. If <code>OrdersModule</code> needs <code>UsersModule</code> and <code>UsersModule</code> needs <code>OrdersModule</code>, neither module can be understood, tested, or deployed independently of the other — the "module boundary" is fictional, because the two are really one tangled unit pretending to be two. <code>forwardRef()</code> silences the symptom (the bootstrap crash) without addressing the underlying coupling, so the modules stay hard to reason about, hard to extract into a separate service later, and prone to accumulating more cross-references over time.</p>`,
  },
  {
    id: "a17-circular-deps-13",
    category: "di",
    categoryLabel: "DI",
    question: `What's the most common root cause of a circular dependency between two feature modules in a typical NestJS app?`,
    answerHtml: `<p>Each feature module owns both its data-access logic and reaches directly into a sibling feature's service for cross-cutting behavior (e.g. <code>OrdersService</code> calls <code>UsersService</code> to validate a user, and <code>UsersService</code> calls <code>OrdersService</code> to compute an order count for a user profile). The fix is almost never "add forwardRef" — it's recognizing that the shared concept (here, perhaps a read-only user summary, or an event) shouldn't require two services to know each other's full APIs.</p>`,
  },
  {
    id: "a17-circular-deps-14",
    category: "di",
    categoryLabel: "DI",
    question: `What is the "extract a shared module" refactor for circular dependencies, and when does it fit?`,
    answerHtml: `<p>When both modules need the same lower-level piece — a shared interface, a small set of pure utility providers, or a narrow read-only service — pull that piece into its own <code>SharedModule</code> (or a more specifically named one, e.g. <code>UserLookupModule</code>) that both original modules import. Dependencies now flow one direction, from each feature module down into the shared module, instead of sideways between two peers. This fits when the entangled piece is genuinely small and stable, not when it's most of either module's logic.</p>`,
  },
  {
    id: "a17-circular-deps-15",
    category: "di",
    categoryLabel: "DI",
    question: `What is the "invert the dependency with an interface/token" refactor, and how does it remove the cycle without an event bus?`,
    answerHtml: `<p>Define an abstract interface (e.g. <code>UserLookup</code>) and a corresponding injection token in a shared, dependency-free location. The module that needs the capability depends only on the token/interface, not on the concrete sibling module. The sibling module provides the concrete implementation and binds it to the token via <code>{ provide: USER_LOOKUP, useExisting: UsersService }</code> or similar, typically wired up in a higher-level composition module. This is the Dependency Inversion Principle applied at the Nest module level: both modules now depend on an abstraction that neither owns exclusively, and the concrete coupling direction is decided once, at composition time.</p>`,
  },
  {
    id: "a17-circular-deps-16",
    category: "di",
    categoryLabel: "DI",
    question: `How does emitting domain events (via EventEmitterModule or a message bus) remove a circular dependency, and what's the tradeoff?`,
    answerHtml: `<p>Instead of <code>OrdersService</code> calling <code>UsersService.updateStats()</code> directly, <code>OrdersService</code> emits an <code>order.created</code> event and <code>UsersModule</code> registers an <code>@OnEvent('order.created')</code> listener that reacts independently. Neither module imports the other; the coupling becomes a shared event name/payload contract instead of a class reference. The tradeoff is that the interaction becomes implicit and asynchronous (fire-and-forget by default) — you lose the ability to synchronously return a value or guarantee ordering/atomicity with the emitting transaction unless you add extra machinery (transactional outbox, awaited emitters).</p>`,
  },
  {
    id: "a17-circular-deps-17",
    category: "di",
    categoryLabel: "DI",
    question: `How can re-drawing module boundaries around use cases instead of entities eliminate a circular dependency entirely?`,
    answerHtml: `<p>If <code>OrdersModule</code> and <code>UsersModule</code> are circular because a use case like "place an order, which both validates the user and updates user stats" spans both entities, the fix can be to create an <code>OrderPlacementModule</code> (or service) that imports both <code>UsersModule</code> and <code>OrdersModule</code> one-directionally and orchestrates the cross-entity logic itself. Neither original module imports the other anymore; the orchestration logic that caused the cycle moves to a layer above both, which is often a more honest reflection of what the code is actually doing (a workflow, not a property of either entity).</p>`,
  },
  {
    id: "a17-circular-deps-18",
    category: "di",
    categoryLabel: "DI",
    question: `Why does forwardRef() typically work fine for two providers within the SAME module, but become a much bigger smell across module boundaries?`,
    answerHtml: `<p>Within a single module, a circular reference between two closely related providers (e.g. a tree structure, a parent/child pair, or two services that genuinely co-design a tight protocol) is a local implementation detail — the module is still one cohesive, independently testable unit from the outside. Across modules, the same pattern means two supposedly independent application boundaries can't be loaded, tested, or replaced without each other, which defeats the entire purpose of having modules. The same syntax fixes both, but the cost it's papering over is very different.</p>`,
  },
  {
    id: "a17-circular-deps-19",
    category: "di",
    categoryLabel: "DI",
    question: `Can a circular dependency exist purely between modules with no actual provider-to-provider injection cycle? If so, how?`,
    answerHtml: `<p>Yes. If <code>ModuleA</code> simply imports <code>ModuleB</code> to re-export one of its providers (so consumers of <code>ModuleA</code> can use it), and <code>ModuleB</code> separately imports <code>ModuleA</code> for the same reason, the module graph is circular even if no single provider's constructor injects something from the other side. Nest still needs <code>forwardRef()</code> on both <code>imports</code> arrays to load the modules, because module registration order is itself dependent on both being defined first.</p>`,
  },
  {
    id: "a17-circular-deps-20",
    category: "di",
    categoryLabel: "DI",
    question: `What's a concrete failure mode you get if you apply forwardRef() to the provider injection but forget it's also needed on the module-level import?`,
    answerHtml: `<p>You'll typically still see the "can't resolve dependency" or "a circular dependency between modules" error at bootstrap, because the module graph itself couldn't be constructed — Nest never got far enough to even attempt resolving the individual provider. The fix has two independent layers (module imports and constructor injection) and both need wrapping if the cycle exists at both levels; fixing only one layer leaves the other unresolved.</p>`,
  },
  {
    id: "a17-circular-deps-21",
    category: "di",
    categoryLabel: "DI",
    question: `If you use ModuleRef.get() to break a cycle, what new testing/mocking obligation does that introduce compared to constructor injection?`,
    answerHtml: `<p>With constructor injection, a unit test can simply pass a mock instance as a constructor argument. With <code>ModuleRef.get()</code>, the dependency isn't visible in the constructor signature at all, so the test has to either provide a real (or test-double) <code>ModuleRef</code> that resolves to a mock, or use Nest's <code>Test.createTestingModule()</code> with the mock registered as a provider so <code>moduleRef.get()</code> finds it inside the compiled test module. This makes the dependency implicit and easier to miss when writing or reviewing tests — one more reason to prefer it only where forwardRef's constructor cycle is genuinely undesirable.</p>`,
  },
  {
    id: "a17-circular-deps-22",
    category: "di",
    categoryLabel: "DI",
    question: `Does NestJS's circular-dependency detection happen at compile time (TypeScript) or runtime, and what does that imply for catching it early?`,
    answerHtml: `<p>It's a runtime concern: TypeScript happily compiles two classes that reference each other's types (type-only circular imports are fine and common). The failure only surfaces when Nest's DI container actually tries to instantiate the providers during application bootstrap. This means a circular dependency bug isn't caught by <code>tsc --noEmit</code> or linting — it's caught by booting the app (or by a unit/integration test that builds the real Nest module via <code>Test.createTestingModule</code>), which is why CI should always include at least one test that compiles the full root module, not just isolated unit tests with everything mocked.</p>`,
  },
];

export const ADVANCED17_QUIZ: QuizQuestion[] = [
  {
    id: "a17q-circular-deps-1",
    category: "di",
    categoryLabel: "DI",
    question: `Two providers in the same NestJS module, ServiceA and ServiceB, inject each other via plain constructor parameters with no forwardRef(). What happens at application bootstrap?`,
    options: [`Nest silently picks an arbitrary instantiation order and both services work`, `Nest throws an unresolved-dependency error because there is no valid order to construct both classes`, `Nest lazily proxies both classes automatically, equivalent to using forwardRef() by default`, `TypeScript fails to compile because of the circular import`],
    answer: 1,
    explanationHtml: `<p>Nest resolves constructor injection eagerly and needs a fully built instance of each dependency before constructing its consumer. With a direct cycle and no <code>forwardRef()</code>, there's no valid bottom-up order, so bootstrap throws a "can't resolve dependencies" error. TypeScript itself compiles fine — this is purely a runtime DI-container problem.</p>`,
  },
  {
    id: "a17q-circular-deps-2",
    category: "di",
    categoryLabel: "DI",
    question: `What does wrapping a dependency in forwardRef(() => ServiceB) actually change about how Nest resolves it?`,
    options: [`It tells Nest to skip validating that ServiceB exists`, `It converts ServiceB into a singleton shared across all modules`, `It defers evaluation of the class reference to a later resolution pass instead of requiring it immediately`, `It makes ServiceB request-scoped automatically`],
    answer: 2,
    explanationHtml: `<p><code>forwardRef()</code> wraps the reference in a thunk so Nest doesn't need the actual class symbol at the moment metadata is first read. Both providers get registered, then Nest resolves the actual binding in a second pass once both sides exist — it doesn't change scope, singleton-ness, or validation behavior.</p>`,
  },
  {
    id: "a17q-circular-deps-3",
    category: "di",
    categoryLabel: "DI",
    question: `ServiceA (in ModuleA) and ServiceB (in ModuleB) depend on each other across a module boundary. Which forwardRef() touch points are required to fully resolve the cycle?`,
    options: [`Only on the two module-level imports arrays`, `Only on the two constructor-level @Inject decorators`, `On both the module-level imports (both directions) AND both constructor-level @Inject decorators (both directions)`, `Only on whichever side was defined first in the file system`],
    answer: 2,
    explanationHtml: `<p>A cross-module cycle has two independent layers: the module graph (ModuleA imports ModuleB and vice versa) and the provider graph (ServiceA injects ServiceB and vice versa). Each layer needs its own <code>forwardRef()</code> on both sides; fixing only one layer still leaves the other to throw at bootstrap.</p>`,
  },
  {
    id: "a17q-circular-deps-4",
    category: "di",
    categoryLabel: "DI",
    question: `Why is ModuleRef.get() generally unsafe to call directly inside a constructor to break a circular dependency?`,
    options: [`ModuleRef is not injectable inside constructors`, `The target provider may not yet be instantiated by the container at construction time, since that's the same ordering problem causing the cycle`, `get() always throws inside a constructor by design, even for non-circular providers`, `Constructors in NestJS cannot contain any logic beyond assignment`],
    answer: 1,
    explanationHtml: `<p><code>ModuleRef</code> itself is injectable in a constructor without issue. The problem is calling <code>.get()</code> on it synchronously during construction — the target provider might still be mid-instantiation or not yet created, which is exactly the ordering problem that caused the cycle. <code>ModuleRef.get()</code> is meant to be called later, e.g. inside a method body or a lifecycle hook after bootstrap completes.</p>`,
  },
  {
    id: "a17q-circular-deps-5",
    category: "di",
    categoryLabel: "DI",
    question: `Why is a circular dependency between two NestJS feature modules considered an architectural smell rather than just an inconvenience to wrap in forwardRef()?`,
    options: [`Because forwardRef() has measurable runtime performance overhead on every request`, `Because it means the two modules cannot be understood, tested, or potentially extracted independently — the module boundary is not actually enforcing separation`, `Because NestJS deprecated forwardRef() in recent major versions`, `Because circular dependencies always indicate a missing database transaction`],
    answer: 1,
    explanationHtml: `<p>A module boundary is supposed to represent a cohesive, independently reasoned-about unit. Mutual dependency between two feature modules means neither can be loaded, tested, or split out on its own — the boundary is fictional. <code>forwardRef()</code> fixes the bootstrap crash but leaves the coupling, and the count of cross-references tends to grow over time if untreated.</p>`,
  },
  {
    id: "a17q-circular-deps-6",
    category: "di",
    categoryLabel: "DI",
    question: `Which refactor removes a circular dependency by having both originally-coupled modules depend on a new abstraction instead of on each other directly?`,
    options: [`Wrapping every constructor parameter in forwardRef()`, `Converting all providers in both modules to REQUEST scope`, `Introducing an interface/injection token that both modules depend on, with the concrete binding wired at a higher composition layer`, `Merging both modules' code into a single God module`],
    answer: 2,
    explanationHtml: `<p>This is dependency inversion applied at the module level: each module depends only on an abstract token/interface rather than the concrete sibling class. The actual binding (which concrete implementation satisfies the token) is decided once, in a composition root, so neither module needs to import the other's concrete class.</p>`,
  },
  {
    id: "a17q-circular-deps-7",
    category: "di",
    categoryLabel: "DI",
    question: `A test suite mocks every dependency individually and never boots the full root AppModule. Why might this fail to catch a real circular dependency bug before it reaches CI/production?`,
    options: [`Circular dependencies are a TypeScript compile error, so tsc would have already caught it`, `Circular dependency resolution is a runtime concern in Nest's DI container, which only manifests when the real module graph is actually instantiated`, `Jest automatically detects circular dependencies and fails the test run regardless of what's mocked`, `Circular dependencies only affect production builds, never test builds`],
    answer: 1,
    explanationHtml: `<p>TypeScript permits circular type references at compile time. The DI failure only happens when Nest's container tries to instantiate providers in the real application graph. Tests that mock every dependency in isolation never exercise that real instantiation order, so at least one test should build the actual module tree via <code>Test.createTestingModule</code> (or boot the app) to catch this class of bug.</p>`,
  },
];
