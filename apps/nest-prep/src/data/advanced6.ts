// Batch 6 — Code-first vs schema-first setup, resolvers & field resolvers, DataLoader for N+1 in GraphQL, subscriptions (PubSub), and schema federation basics.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED6_FLASHCARDS: Flashcard[] = [
  {
    id: "a6-graphql-deep-1",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `In NestJS GraphQL, what is the practical difference between code-first and schema-first, beyond "decorators vs SDL files"?`,
    answerHtml: `<p>Code-first generates the GraphQL SDL <b>from TypeScript classes and decorators</b> (<code>@ObjectType</code>, <code>@Field</code>, <code>@Resolver</code>) at startup, so the schema is a build artifact and TS types are the single source of truth — no drift between types and schema is possible.</p><p>Schema-first starts from <code>.graphql</code> SDL files and Nest generates TypeScript interfaces from them (via <code>GraphQLAstExplorer</code> + a CLI plugin or <code>definitions</code> generation step), so the <b>schema is hand-authored</b> and the types are derived. Schema-first is better when the schema is a contract owned by a separate team or shared across non-Nest services; code-first is better for a Nest-owned API where DRY-ing types/validation/schema matters more than schema portability.</p>`,
  },
  {
    id: "a6-graphql-deep-2",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `Why does code-first NestJS GraphQL require both \`@ObjectType()\`/\`@Field()\` decorators AND, often, a duplicate \`class-validator\` decorated input — isn't that redundant with the TS types?`,
    answerHtml: `<p>It looks redundant but the decorators serve different runtime systems. <code>@Field()</code> exists for <b>GraphQL's schema reflection</b> at module init (emitting the SDL type from TS metadata, since TS types are erased at runtime). <code>class-validator</code> decorators (<code>@IsEmail</code>, <code>@MinLength</code>) exist for the <b>ValidationPipe</b> at request time.</p><p>GraphQL's own type system enforces shape and nullability, but it does not enforce business rules like "string length 8-64" or "valid email format" — those still need a validation pipe pass, same as REST DTOs. So an input class often carries both decorator sets simultaneously.</p>`,
  },
  {
    id: "a6-graphql-deep-3",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `What does the \`autoSchemaFile\` option control in \`GraphQLModule.forRoot()\`, and what's the tradeoff between writing it to disk vs generating it in memory?`,
    answerHtml: `<p><code>autoSchemaFile</code> tells the code-first driver where to emit the generated SDL. Setting it to a path (e.g. <code>'schema.gql'</code>) writes the schema to disk on every boot — useful for committing the schema for diffing in CI, federation tooling, or codegen for a separate frontend.</p><p>Setting it to <code>true</code> (or using an in-memory option) generates the schema in memory only, skipping the file write. The tradeoff: a committed schema file gives you a <b>reviewable diff</b> on every PR and a stable artifact for schema-diff/breaking-change CI checks; an in-memory schema is one less generated file to gitignore but means breaking changes are invisible until introspection or a federation compose step catches them.</p>`,
  },
  {
    id: "a6-graphql-deep-4",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `What is a field resolver in Nest GraphQL, and when is it required versus when does the default resolution just work?`,
    answerHtml: `<p>By default, GraphQL resolves a field by looking for a property of the same name on the object returned by the parent resolver — no field resolver needed if the field already exists on the entity (e.g. <code>user.email</code> from a flat row).</p><p>A field resolver (<code>@ResolveField()</code> inside a <code>@Resolver(() => User)</code> class) is required when the field is <b>not present on the underlying object</b> and must be computed or fetched: a relation that isn't eagerly loaded (<code>user.posts</code>), a derived value (<code>user.fullName</code>), or anything requiring a separate data source call. The resolver receives the parent via <code>@Parent()</code> and resolves just that one field, independent of how the parent query was resolved.</p>`,
  },
  {
    id: "a6-graphql-deep-5",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `In a field resolver method like \`resolvePosts(@Parent() user: User)\`, what's actually happening under the hood when this field is requested for a list of 50 users in one query?`,
    answerHtml: `<p>GraphQL execution resolves fields breadth-first per selection set. For a query returning 50 <code>User</code> objects with a nested <code>posts</code> field, the executor calls the parent resolver once (returns 50 users), then calls <code>resolvePosts</code> <b>once per user object</b> — 50 separate invocations, each receiving one user as <code>@Parent()</code>.</p><p>If <code>resolvePosts</code> does <code>this.postsService.findByUserId(user.id)</code>, that's 50 separate DB round-trips — the classic GraphQL N+1. This per-object resolver invocation is exactly why field resolvers are the place N+1 problems are introduced, and exactly why DataLoader batching exists.</p>`,
  },
  {
    id: "a6-graphql-deep-6",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `Why can't you just slap \`@UseInterceptors(CacheInterceptor)\` on a \`@ResolveField()\` to fix N+1, the way you might cache a REST endpoint?`,
    answerHtml: `<p>HTTP response caching keys on the request (URL + params), but a field resolver is invoked once per parent object within a <b>single</b> request/response cycle — there's no repeated identical request to cache against. The interceptor-level cache solves a different problem (repeated <i>requests</i> across time), not repeated <i>resolver calls within one response</i>.</p><p>What actually collapses N calls into one batched call within a single request is DataLoader: it intercepts synchronous-tick calls to <code>load(id)</code>, queues them, and fires one batched query before the current event-loop turn yields further — a request-scoped, call-coalescing cache, not a time-based response cache.</p>`,
  },
  {
    id: "a6-graphql-deep-7",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `Describe DataLoader's batching mechanism precisely: how does calling \`loader.load(id)\` 50 times synchronously result in exactly one batch function call?`,
    answerHtml: `<p>Each <code>load(key)</code> call returns a Promise immediately and pushes <code>(key, resolve, reject)</code> onto an internal queue — it does not call the batch function yet. DataLoader's default dispatch (<code>enqueuePostPromiseJob</code>) schedules the actual dispatch via <code>process.nextTick</code> in Node (falling back to a resolved-Promise <code>.then()</code> microtask in non-Node environments), deferring it until the <b>current synchronous execution frame finishes</b>.</p><p>Because all 50 <code>resolvePosts</code> invocations run synchronously back-to-back within the same GraphQL execution phase before any awaits resolve, all 50 keys land in the queue before the scheduled dispatch fires. It then calls the user-supplied <code>batchLoadFn(keys: string[])</code> exactly once with all 50 keys, and DataLoader maps the single array result back to each pending promise by key order.</p>`,
  },
  {
    id: "a6-graphql-deep-8",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `Why must a DataLoader instance be created per-request rather than as a singleton provider in the Nest DI container?`,
    answerHtml: `<p>DataLoader caches by key for the lifetime of the loader instance (memoization: calling <code>load(5)</code> twice returns the same promise/result without hitting the batch function again). If the loader were a singleton, that cache would persist <b>across requests and across users</b> — stale data, and worse, a potential data leak between tenants/users if cached results aren't re-authorized per request.</p><p>The fix is request-scoped construction: either register the loader with <code>{ scope: Scope.REQUEST }</code>, or — more commonly for perf — construct a fresh DataLoader instance per request inside the GraphQL context factory (<code>context: () => ({ loaders: createLoaders() })</code>) and pull it off <code>context</code> in resolvers via <code>@Context()</code>.</p>`,
  },
  {
    id: "a6-graphql-deep-9",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `What's wrong with creating a DataLoader as a request-scoped (\`Scope.REQUEST\`) NestJS provider injected into a resolver, performance-wise, compared to instantiating it in the GraphQL context factory?`,
    answerHtml: `<p>Request-scoped providers in Nest aren't free — Nest has to build a new DI subtree for every request that touches a request-scoped provider (or anything that depends on one), which adds resolution overhead proportional to the provider graph, and that scoping <b>bubbles up</b> to anything injecting the request-scoped service.</p><p>Building loaders directly in the <code>context</code> factory function of <code>GraphQLModule.forRoot()</code> sidesteps Nest's DI scoping machinery entirely — you're just instantiating plain objects per request and stashing them on the context, which is cheaper and keeps the rest of the provider graph singleton. The tradeoff is you lose DI (can't easily inject other Nest services into the loader's batch function unless you pass them in manually).</p>`,
  },
  {
    id: "a6-graphql-deep-10",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `A \`batchLoadFn(userIds: string[])\` for a DataLoader must return results matching the input in both order and count. Why is this constraint strict, and what happens if a queried ID has no matching row?`,
    answerHtml: `<p>DataLoader maps array index <code>i</code> of the batch function's result directly back to the promise created by the <code>i</code>-th <code>load()</code> call — it does <b>not</b> match by key/value inspection. If your SQL/ORM query does <code>WHERE id IN (...)</code> and a row is missing or the DB reorders results, the wrong user's data could be handed to the wrong promise.</p><p>The correct pattern is to fetch all rows, build a <code>Map&lt;id, row&gt;</code>, then map over the <i>original</i> <code>keys</code> array and return <code>map.get(key) ?? null</code> for each — explicitly preserving order and inserting <code>null</code> (or an <code>Error</code> instance, which DataLoader will reject just that one promise for) for missing keys, never silently shrinking the array.</p>`,
  },
  {
    id: "a6-graphql-deep-11",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `When is DataLoader's request-scoped caching actually a liability rather than a win, inside a single GraphQL operation?`,
    answerHtml: `<p>If a mutation inside the same request <b>updates</b> an entity that was already loaded earlier in that request (e.g. a resolver chain that loads a user, then a different field resolver triggers a mutation updating that user, then a third field re-reads it via the same loader), the loader returns its <b>stale cached promise</b> instead of refetching — because DataLoader's cache is purely identity/key based with no invalidation hook into your write path.</p><p>The fix is calling <code>loader.clear(id)</code> (or <code>clearAll()</code>) immediately after any mutation that changes data the loader has cached, so the next <code>load()</code> for that key bypasses the cache.</p>`,
  },
  {
    id: "a6-graphql-deep-12",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `What underlying transport mechanism does \`@nestjs/graphql\` use for GraphQL subscriptions, and what does that imply for infrastructure like load balancers?`,
    answerHtml: `<p>Subscriptions in Nest's GraphQL module are served over <b>WebSockets</b> by default (via <code>graphql-ws</code>, the modern protocol; the older <code>subscriptions-transport-ws</code> is legacy/deprecated). This is a long-lived, stateful connection per client, unlike the stateless request/response of queries and mutations over HTTP.</p><p>Operationally this means load balancers and reverse proxies need <b>sticky sessions or WebSocket-aware routing</b> (the connection must stay pinned to the server process holding the subscription's PubSub listener), health checks need to account for long-lived connections, and horizontal scaling requires a shared pub/sub backplane (Redis, etc.) rather than relying on in-process event emitters.</p>`,
  },
  {
    id: "a6-graphql-deep-13",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `What is the role of \`PubSub\` in a NestJS GraphQL subscription, and why is the default \`graphql-subscriptions\` in-memory \`PubSub\` implementation explicitly unsuitable for production?`,
    answerHtml: `<p><code>PubSub</code> is the event bus connecting a mutation (or any backend event) to active subscribers: a resolver calls <code>pubsub.publish('eventName', payload)</code>, and the <code>@Subscription()</code> resolver's <code>asyncIterator('eventName')</code> yields that payload to every connected client subscribed to it.</p><p>The default in-memory implementation keeps subscribers in a single Node process's memory. With more than one server instance (any real horizontally-scaled deployment), a mutation handled by instance A never reaches subscribers connected to instance B — events silently don't fan out. Production setups swap in <code>graphql-redis-subscriptions</code> or similar, which publishes through Redis pub/sub so all instances see all events.</p>`,
  },
  {
    id: "a6-graphql-deep-14",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `In a \`@Subscription()\` resolver, what is the purpose of the \`filter\` option, and why should filtering happen there instead of inside the handler that calls \`pubsub.publish()\`?`,
    answerHtml: `<p><code>filter: (payload, variables, context) => boolean</code> runs <b>per-subscriber</b> at delivery time, deciding whether a given connected client should receive this particular published event (e.g. only deliver a <code>commentAdded</code> event to clients subscribed to that specific <code>postId</code>).</p><p>Publishing already does the broadcast to all listeners of the channel name; doing the per-client filtering at the publish call site is impossible because the publisher doesn't know the full set of currently-connected subscribers or their individual subscription arguments. The subscription resolver is the only place that has both the event payload and that specific client's subscription variables in scope simultaneously.</p>`,
  },
  {
    id: "a6-graphql-deep-15",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `Why is authorization for GraphQL subscriptions structurally harder to get right than for queries/mutations, and where in the Nest pipeline must it actually be enforced?`,
    answerHtml: `<p>A query/mutation's auth check happens once, at request time, gating a single response. A subscription's auth check at connection time only proves the user <i>was</i> authorized when they subscribed — the WebSocket connection can live for hours, during which the user's role could change, their token could expire, or — critically — the <b>filter</b> logic decides per-event whether this specific payload should reach this specific client, which is itself an authorization decision (e.g. "can this user see this private post's comments?").</p><p>Correct enforcement is two-layered: connection-level auth in the WS connection handler (validating the token at <code>onConnect</code>) plus per-event authorization inside the <code>filter</code> function (or a guard wrapping the subscription resolver) re-checking entitlement against the live payload, not just cached connection-time state.</p>`,
  },
  {
    id: "a6-graphql-deep-16",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `What problem does Apollo Federation solve that simply splitting a monolithic GraphQL schema into multiple services and stitching them client-side does not?`,
    answerHtml: `<p>Schema stitching (the older approach) requires a gateway that has full knowledge of every subschema and manually wires together cross-service type extensions and resolvers — it doesn't scale past a handful of services and the gateway becomes a bottleneck for every schema change.</p><p>Federation lets each service (subgraph) <b>independently declare</b> which types it owns and which fields of types owned elsewhere it can extend/resolve, using directives like <code>@key</code> and <code>@external</code>. A gateway composes these subgraph schemas into one supergraph automatically and routes each field's execution to the owning subgraph at query time — teams can deploy subgraphs independently without the gateway needing a manual stitching config update.</p>`,
  },
  {
    id: "a6-graphql-deep-17",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `In Apollo Federation, what does the \`@key\` directive declare on a type, and what runtime mechanism actually uses it during query execution?`,
    answerHtml: `<p><code>@key(fields: "id")</code> on a type (e.g. <code>type Product @key(fields: "id")</code>) declares the field(s) that uniquely identify an entity across subgraphs — it marks the type as a federated <b>entity</b> that other subgraphs can reference and extend without owning.</p><p>At runtime, when the gateway needs to fetch fields of <code>Product</code> that live in a different subgraph than the one that resolved the base object, it calls that subgraph's auto-generated <code>_entities</code> resolver, passing a representation <code>{ __typename: "Product", id: "123" }</code> built from the <code>@key</code> fields — the subgraph's <code>@ResolveReference()</code> method (in Nest's federation package) receives that representation and returns the full entity for resolution to continue.</p>`,
  },
  {
    id: "a6-graphql-deep-18",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `In \`@nestjs/graphql\`'s federation support, what does \`@ResolveReference()\` do, and which class is it defined on?`,
    answerHtml: `<p><code>@ResolveReference()</code> is defined on the resolver class of a federated entity type (the one bearing <code>@Directive('@key(fields: "id")')</code> in code-first Nest). It implements the resolution of a <i>reference</i>: given the partial representation the gateway sends (just the <code>@key</code> fields plus <code>__typename</code>), this method loads and returns the full entity object from that subgraph's own data source.</p><p>This is what lets a subgraph that doesn't own a type still <b>extend</b> it: it receives a stub reference, hydrates it from its own DB, and the gateway merges the result with fields resolved by the owning subgraph into one object in the final response.</p>`,
  },
  {
    id: "a6-graphql-deep-19",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `Why does adopting federation usually force a change to how DataLoader batching is scoped, compared to a single monolithic GraphQL service?`,
    answerHtml: `<p>In a monolith, one DataLoader instance per request can batch across the entire query because every field resolver runs in the same process against the same context. In federation, the gateway splits a single client query into multiple sub-requests dispatched to different subgraph services — each subgraph handles its own slice of the query (including its own <code>_entities</code> batch resolution) in its <b>own process</b> with its own request lifecycle.</p><p>So batching only coalesces N+1 calls <i>within one subgraph's portion</i> of the query, not globally. A field that fans out across three subgraphs still produces three separate round-trips from the gateway, each independently batched (or not) by that subgraph's own DataLoader setup — federation trades one global N+1 problem for several smaller, per-subgraph N+1 problems that each need their own loader.</p>`,
  },
  {
    id: "a6-graphql-deep-20",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `When a resolver method needs both the parent object and a GraphQL argument — e.g. \`comments(parent, @Args('limit') limit: number)\` inside a \`@ResolveField()\` for \`Post.comments\` — what decorator pulls in the parent, and what's a common mistake when typing it?`,
    answerHtml: `<p><code>@Parent()</code> injects the object returned by the parent field's resolver (here, the <code>Post</code> being resolved). A common mistake is typing it as the GraphQL <code>ObjectType</code> class and assuming all its <code>@Field()</code>s are populated — but the parent object is whatever the <i>parent resolver actually returned</i>, which for a list query might be a lean DB projection missing fields that were never selected, or an entity not yet hydrated with relations.</p><p>This is why field resolvers should treat the parent as a partial/untrusted shape (often typed narrower than the full GraphQL type, e.g. <code>Pick&lt;Post, 'id'&gt;</code>) and explicitly fetch what they need by ID rather than assuming the parent already carries it.</p>`,
  },
  {
    id: "a6-graphql-deep-21",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `What does \`@Info()\` give you in a Nest GraphQL resolver, and what's a legitimate performance use case for inspecting it?`,
    answerHtml: `<p><code>@Info()</code> injects the raw <code>GraphQLResolveInfo</code> object — the parsed query AST, including which fields were actually requested in the current selection set, fragment spreads, and the full schema/type info for the current execution.</p><p>A legitimate use is <b>selective field loading</b>: inspecting <code>info.fieldNodes</code> to determine which sub-fields the client actually asked for, then only querying those columns/relations from the database instead of eagerly loading everything the type could theoretically expose. This avoids over-fetching at the DB layer even though GraphQL itself already avoids over-fetching at the API layer — without it, a resolver might join in five relations because the type <i>could</i> need them, even when the client requested only two scalar fields.</p>`,
  },
  {
    id: "a6-graphql-deep-22",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `How do \`@Query()\`, \`@Mutation()\`, and \`@Subscription()\` decorators differ in what they generate in the schema and how their return value is delivered, beyond the obvious "read vs write vs stream" distinction?`,
    answerHtml: `<p><code>@Query()</code> and <code>@Mutation()</code> both map to a single resolved value returned (or its promise) per call — schema-wise they're just root fields under <code>type Query</code> / <code>type Mutation</code>, and execution-wise mutations are guaranteed to run <b>sequentially</b> relative to each other in a single request (per the GraphQL spec) while queries at the same level may execute in parallel.</p><p><code>@Subscription()</code> is fundamentally different: the method doesn't return the payload directly — it returns (or is wrapped to return) an <b>AsyncIterator</b> (typically via <code>pubsub.asyncIterator()</code>), and the actual payload transformation usually happens in a separate companion resolver function or the same method's return shape feeds a long-lived stream rather than a single response.</p>`,
  },
  {
    id: "a6-graphql-deep-23",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `What's the difference between resolving a relation via a field resolver with DataLoader versus resolving it via an ORM's eager-loading/\`relations\` option (e.g. TypeORM's \`relations: ['posts']\`) directly in the root \`@Query()\` resolver?`,
    answerHtml: `<p>Eager loading in the root resolver fetches the relation <b>unconditionally</b> for every query that hits that resolver, even if the client's GraphQL selection set never asked for <code>posts</code> — this reintroduces the over-fetching problem GraphQL exists to solve, just moved from the API layer to the DB layer.</p><p>A field resolver with DataLoader only runs (and only fetches) when the client's query actually selects that field — GraphQL's executor simply never calls a field resolver for an unselected field. This makes field-resolver + DataLoader the combination that preserves "fetch exactly what was asked for" while still avoiding N+1 when the field <i>is</i> requested across many parents.</p>`,
  },
];

export const ADVANCED6_QUIZ: QuizQuestion[] = [
  {
    id: "a6q-graphql-deep-1",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `A \`@ResolveField()\` for \`Author.books\` calls \`booksService.findByAuthorId(parent.id)\` directly (no DataLoader) and is hit by a query returning 200 authors with their books. What is the precise execution behavior?`,
    options: [`GraphQL automatically batches all 200 calls into one query because they share the same field name`, `The resolver is invoked once per author object — 200 separate calls, each a separate DB round-trip`, `TypeORM's query cache deduplicates the repeated calls automatically`, `Nest throws a runtime warning when a field resolver is called more than 100 times per request`],
    answer: 1,
    explanationHtml: `<p>GraphQL's executor calls a field resolver once per parent object in the result set, with no automatic batching. Without DataLoader (or manual batching), 200 authors means 200 separate <code>findByAuthorId</code> calls — the textbook N+1. Nothing in vanilla GraphQL or TypeORM collapses this for you; DataLoader exists specifically to insert a batching layer between the resolver call and the DB call.</p>`,
  },
  {
    id: "a6q-graphql-deep-2",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `Why must a DataLoader's \`batchLoadFn\` return an array the same length as the input keys array, in the same order, even when some keys have no corresponding record?`,
    options: [`It's a style convention only — DataLoader will reorder by inspecting each result's \`id\` field automatically`, `DataLoader maps result array index to the corresponding load() call by position, so misaligned length or order silently resolves the wrong promise with the wrong data`, `Because GraphQL's spec requires all list fields to be non-nullable`, `It's required only when using TypeORM; other ORMs handle reordering internally`],
    answer: 1,
    explanationHtml: `<p>DataLoader has no insight into the shape of your data — it purely zips the <code>batchLoadFn</code>'s return array with the original <code>keys</code> array by index. If a row is missing and you simply omit it instead of inserting <code>null</code> at that position, every subsequent result shifts by one and gets paired with the wrong promise.</p>`,
  },
  {
    id: "a6q-graphql-deep-3",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `A team registers a DataLoader as a NestJS provider with \`{ scope: Scope.DEFAULT }\` (the implicit singleton default) and injects it into multiple resolvers. What bug does this introduce in production?`,
    options: [`No bug — DataLoader is explicitly designed to be a singleton across the application`, `The loader's per-key cache persists across requests and users, so a later request can receive another user's previously cached, and potentially unauthorized, data`, `Singleton scope causes the batch function to run synchronously, blocking the event loop`, `It only breaks subscriptions, not queries or mutations`],
    answer: 1,
    explanationHtml: `<p>DataLoader memoizes by key for as long as the instance lives. A singleton loader lives for the whole application lifetime, so its cache spans every request — stale or, worse, cross-tenant leaked data if authorization differs per request. DataLoader instances must be created fresh per request (request scope or built in the GraphQL context factory).</p>`,
  },
  {
    id: "a6q-graphql-deep-4",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `Why does Apollo Federation require a gateway-aware composition step rather than simply having a client query multiple independent GraphQL services and merging the JSON responses?`,
    options: [`Because GraphQL doesn't support more than one service per client by design`, `Because federation allows a single logical type's fields to be split and resolved across different subgraphs, requiring the gateway to plan a query execution graph and stitch partial entity data together via @key-based entity resolution, not just concatenate separate top-level responses`, `Because REST and GraphQL cannot be combined without a gateway`, `Because subgraphs cannot share a network without a gateway acting as a proxy`],
    answer: 1,
    explanationHtml: `<p>Federation's core feature is letting one GraphQL type (e.g. <code>Product</code>) have some fields owned by one subgraph and other fields owned by another, joined at the entity level via <code>@key</code>. The client sends one query for one type; the gateway must decompose it into sub-requests to multiple subgraphs and merge partial entity representations into a single coherent object — naive client-side merging of independent top-level responses can't do field-level joins on a shared entity.</p>`,
  },
  {
    id: "a6q-graphql-deep-5",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `In a NestJS GraphQL subscription using the default in-memory \`PubSub\`, a mutation is handled by server instance A while a subscribed client's WebSocket connection is pinned to server instance B (behind a load balancer). What happens?`,
    options: [`The event is delivered correctly because PubSub uses the database as a shared event log by default`, `The subscriber on instance B never receives the event, because in-memory PubSub only delivers to listeners within the same Node process`, `The gateway automatically proxies the event to instance B over HTTP/2`, `GraphQL subscriptions are stateless, so this scenario can't occur`],
    answer: 1,
    explanationHtml: `<p>The default <code>graphql-subscriptions</code> PubSub implementation is purely in-process memory — instance A publishing an event has no way to notify instance B's subscribers. This is exactly why production deployments need a shared backplane like Redis (<code>graphql-redis-subscriptions</code>) so every instance publishes/subscribes through a common channel.</p>`,
  },
  {
    id: "a6q-graphql-deep-6",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `What's the key risk with relying solely on a \`@UseGuards()\` check at WebSocket \`onConnect\` time to secure a GraphQL subscription, without any per-event filtering logic?`,
    options: [`onConnect guards are deprecated in graphql-ws and will throw at runtime`, `It only validates authorization at connection time; it can't re-check authorization for each individual published event over the connection's lifetime, so a user could keep receiving events for data they're no longer entitled to see`, `WebSocket connections can't carry auth tokens, so onConnect guards are a no-op`, `Guards can only be applied to Query and Mutation types, never to Subscription`],
    answer: 1,
    explanationHtml: `<p>A subscription connection can live for hours; connection-time auth is a single snapshot. Without per-event authorization (typically in the <code>filter</code> function, re-checking entitlement against the live payload), a permission revoked mid-connection, or a payload belonging to data the user shouldn't see, can still be pushed to them.</p>`,
  },
  {
    id: "a6q-graphql-deep-7",
    category: "graphql",
    categoryLabel: "GraphQL",
    question: `In code-first NestJS GraphQL, what is the actual source of truth that the GraphQL SDL schema is generated from at application bootstrap?`,
    options: [`A \`.graphql\` SDL file checked into the repo that Nest parses on startup`, `TypeScript classes decorated with @ObjectType/@Field/@InputType/@Resolver — reflection metadata from these decorators is read to build the schema`, `The Postgres database schema, introspected automatically via the configured ORM`, `JSON Schema definitions attached to each DTO via @ApiProperty`],
    answer: 1,
    explanationHtml: `<p>Code-first means the schema is derived <i>from code</i>: decorator metadata on TS classes is reflected at bootstrap to build the GraphQL schema (optionally writing it to disk via <code>autoSchemaFile</code>). Schema-first inverts this — SDL is the source and TS types are generated from it. <code>@ApiProperty</code> is Swagger/OpenAPI metadata, unrelated to GraphQL schema generation.</p>`,
  },
];
