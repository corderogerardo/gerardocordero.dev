// Batch 4 — Passport strategies (local, JWT, refresh token rotation), guards composing with strategies, RBAC vs CASL-based policy authorization, and session vs stateless JWT tradeoffs.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED4_FLASHCARDS: Flashcard[] = [
  {
    id: "a4-auth-deep-1",
    category: "auth",
    categoryLabel: "Auth",
    question: `In a NestJS LocalStrategy extending PassportStrategy(Strategy), why call super({ usernameField: 'email' }), and what happens if you skip it while the DTO uses 'email'?`,
    answerHtml: `<p>passport-local defaults to reading <code>username</code> and <code>password</code> off <code>req.body</code>. The <code>usernameField</code> option remaps which body key it reads into the first argument of <code>validate()</code>. Skip it while your DTO sends <code>email</code>, and <code>validate(username, password)</code> receives <code>undefined</code> for the first argument even though the client sent a valid value — you get a confusing generic 401 instead of a clear mismatch error.</p>`,
  },
  {
    id: "a4-auth-deep-2",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why does a Passport strategy's validate() method typically throw UnauthorizedException directly rather than returning null on failed credential checks?`,
    answerHtml: `<p>Passport's contract is: return a truthy value to populate <code>req.user</code>, or return/throw falsy to fail. Nest's <code>AuthGuard</code> wraps the callback via its <code>handleRequest</code> hook, which by default throws a generic <code>UnauthorizedException</code> on a null user anyway — but throwing explicitly inside <code>validate()</code> lets you attach a specific message or a different exception type (e.g. distinguishing 'user disabled' from 'bad password') before Nest's generic fallback ever runs.</p>`,
  },
  {
    id: "a4-auth-deep-3",
    category: "auth",
    categoryLabel: "Auth",
    question: `What concretely happens when you decorate a route with @UseGuards(AuthGuard('jwt')) — how does the string 'jwt' connect to your JwtStrategy class?`,
    answerHtml: `<p><code>PassportStrategy(Strategy, 'jwt')</code> registers your strategy instance under the name <code>'jwt'</code> in Passport's internal, process-wide strategy registry the moment Nest's DI container instantiates the provider (typically at module bootstrap). <code>AuthGuard('jwt')</code> is a factory that returns a guard class whose <code>canActivate</code> looks up that same name in the registry and delegates to <code>passport.authenticate('jwt', ...)</code>. If the strategy provider is never instantiated (e.g. not registered in any module's providers, or tree-shaken out because nothing injects it), the guard throws "Unknown authentication strategy 'jwt'" at request time, not at startup.</p>`,
  },
  {
    id: "a4-auth-deep-4",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why is it dangerous to put short-lived access-token validation logic and long-lived refresh-token validation logic in the same Passport JWT strategy?`,
    answerHtml: `<p>Access and refresh tokens have different trust levels, different expected audiences, and often different secrets/claims (refresh tokens may carry a <code>tokenVersion</code> or family id for rotation tracking). Sharing one strategy means a single <code>secretOrKey</code> and one <code>validate()</code> body has to branch on token type, which makes it easy to accidentally accept a refresh token as a bearer credential on a normal API route — since nothing in the JWT envelope itself stops it from passing standard signature checks. The fix is two distinct strategies (<code>'jwt'</code> and <code>'jwt-refresh'</code>) with separate secrets and separate guards, so a refresh token literally cannot satisfy the access-token strategy's verification.</p>`,
  },
  {
    id: "a4-auth-deep-5",
    category: "auth",
    categoryLabel: "Auth",
    question: `In refresh token rotation, why must the refresh endpoint invalidate the old refresh token even though the access token it issues is already short-lived?`,
    answerHtml: `<p>Rotation's actual security property is <b>reuse detection</b>, not just shortening exposure. Each refresh call issues a new refresh token and revokes (or marks used) the old one. If the old, already-used token is presented again, the server can infer it was stolen and revoke the entire token family — because under normal client behavior a refresh token is used exactly once. Without invalidation, a leaked refresh token is just as reusable as a non-rotating one; rotation only adds value when reuse is detectable and triggers revocation.</p>`,
  },
  {
    id: "a4-auth-deep-6",
    category: "auth",
    categoryLabel: "Auth",
    question: `Where should a NestJS API store a refresh token for a browser-based client, and why is localStorage the wrong answer for this specific token?`,
    answerHtml: `<p>An <code>httpOnly</code>, <code>Secure</code>, <code>SameSite=Strict</code> (or <code>Lax</code>) cookie scoped to the refresh endpoint's path. <code>localStorage</code> is readable by any JS running on the page, so a single XSS hole exfiltrates the refresh token — which is far more damaging than leaking a short-lived access token because it grants long-term re-authentication, not just a few minutes of access. Scoping the cookie's <code>path</code> to <code>/auth/refresh</code> also means the token isn't attached to every other request, shrinking the attack surface further.</p>`,
  },
  {
    id: "a4-auth-deep-7",
    category: "auth",
    categoryLabel: "Auth",
    question: `How does a NestJS guard composed of multiple strategies (e.g. a custom guard trying 'jwt' then 'api-key') typically get implemented, and what's the pitfall with extending AuthGuard('jwt') directly for this?`,
    answerHtml: `<p>You write a custom guard extending <code>AuthGuard(['jwt', 'api-key'])</code> — Nest's <code>AuthGuard</code> factory accepts an array and passes it through to <code>passport.authenticate</code>, which tries each strategy in order and succeeds on the first that authenticates. The pitfall: each strategy's <code>validate()</code> can attach a differently-shaped <code>req.user</code> (a human user vs. a service-account object), so downstream code reading <code>req.user.id</code> or relying on a specific guard (e.g. an <code>@Roles</code> check expecting a <code>roles</code> array) can break silently depending on which strategy won — you need a normalized user shape or explicit type discrimination after the guard runs.</p>`,
  },
  {
    id: "a4-auth-deep-8",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why does calling super.canActivate(context) inside a custom Nest guard matter when you override canActivate to extend AuthGuard('jwt')?`,
    answerHtml: `<p><code>AuthGuard</code>'s <code>canActivate</code> is what actually invokes Passport's <code>authenticate()</code> middleware, runs your strategy's <code>validate()</code>, and populates <code>req.user</code>. If you override <code>canActivate</code> in a subclass to add custom logic (say, checking a feature flag) but forget to call and await <code>super.canActivate(context)</code>, Passport never runs at all — <code>req.user</code> stays <code>undefined</code>, and any downstream code or guard that reads it either crashes or silently treats the request as unauthenticated.</p>`,
  },
  {
    id: "a4-auth-deep-9",
    category: "auth",
    categoryLabel: "Auth",
    question: `In a global JWT guard setup (APP_GUARD provider), how do you exempt specific routes like /auth/login or /health without removing the guard?`,
    answerHtml: `<p>A custom <code>@Public()</code> decorator that uses <code>SetMetadata</code> to tag the route/handler, then the guard's <code>canActivate</code> calls <code>Reflector.getAllAndOverride('isPublic', [context.getHandler(), context.getClass()])</code> and returns <code>true</code> immediately if the metadata is set — checking both handler and class lets you mark an entire controller public too. This is the standard pattern because a global guard via <code>APP_GUARD</code> runs on every route by default, including ones that must stay open (login, refresh, health checks, public webhooks).</p>`,
  },
  {
    id: "a4-auth-deep-10",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why can't a JWT-based @Roles guard simply trust the roles array embedded in the access token's payload for an RBAC check on a sensitive operation?`,
    answerHtml: `<p>If roles are baked into the JWT at issuance time, a demotion or permission revocation doesn't take effect until the token expires or is refreshed — the token is a stale snapshot, not a live grant. For low-stakes routes this staleness is an acceptable, deliberate tradeoff (it's the whole point of stateless JWTs), but for sensitive operations (changing billing, deleting an account) many systems re-fetch the current role/permission set from the DB inside the guard or a dedicated policy check, rather than trusting whatever the token claims.</p>`,
  },
  {
    id: "a4-auth-deep-11",
    category: "auth",
    categoryLabel: "Auth",
    question: `What is the core structural limitation of role-based RBAC ('admin can edit posts') that CASL-style attribute/policy-based authorization is designed to fix?`,
    answerHtml: `<p>Plain RBAC answers 'what can this role do' but can't naturally express conditions scoped to <i>which specific resource instance</i> — e.g. 'an editor can edit posts, but only posts they authored, unless they're also an admin.' Modeling that in RBAC means exploding roles combinatorially (<code>own-post-editor</code>, <code>any-post-editor</code>...) or sprinkling ad hoc <code>if (post.authorId === user.id)</code> checks across controllers. CASL (and ABAC/policy-based systems generally) defines abilities as <code>(action, subject, conditions)</code> tuples — e.g. <code>can('update', 'Post', { authorId: user.id })</code> — so ownership and other attribute conditions are first-class, not bolted on.</p>`,
  },
  {
    id: "a4-auth-deep-12",
    category: "auth",
    categoryLabel: "Auth",
    question: `In CASL, what's the difference between can('manage', 'all') and can('manage', Post), and why does getting this wrong create a security hole?`,
    answerHtml: `<p><code>can('manage', 'all')</code> grants every action on every subject — a true superuser rule. <code>can('manage', Post)</code> grants every action (<code>'manage'</code> is CASL's wildcard action) but scoped to the <code>Post</code> subject only. Confusing the two — e.g. accidentally writing <code>'all'</code> when you meant a specific entity — silently grants unrestricted access across subjects you never intended to expose (other entities, admin-only resources), because CASL has no separate 'I didn't mean that' check; the rule is evaluated exactly as declared.</p>`,
  },
  {
    id: "a4-auth-deep-13",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why does CASL distinguish can() and cannot() rules with explicit ordering, rather than just having you write narrower can() rules?`,
    answerHtml: `<p>CASL evaluates rules in declaration order and the <i>last matching rule wins</i>, so <code>cannot()</code> exists to carve out exceptions after a broad grant without having to enumerate every allowed case explicitly — e.g. <code>can('update', 'Post'); cannot('update', 'Post', { published: true })</code> reads as "editors can update posts, except published ones," which is far more maintainable than negating the condition inline in a single rule, especially as exception logic grows or comes from multiple sources (role rules + tenant rules + feature flags).</p>`,
  },
  {
    id: "a4-auth-deep-14",
    category: "auth",
    categoryLabel: "Auth",
    question: `How do you wire a CASL-based PoliciesGuard into NestJS so a controller method can declare 'this handler requires the ability to update this Post'?`,
    answerHtml: `<p>A <code>@CheckPolicies((ability) => ability.can('update', 'Post'))</code> decorator stores a policy-handler function via <code>SetMetadata</code>. A <code>PoliciesGuard</code> reads that metadata with <code>Reflector</code>, builds the requesting user's <code>Ability</code> instance (typically via an injected <code>CaslAbilityFactory.createForUser(req.user)</code>), and calls each policy handler with that ability, denying if any returns false. The subtlety: for instance-scoped checks (not just the class name <code>'Post'</code>), the guard alone often can't fetch the actual entity (it runs before the handler/params are resolved into a domain object), so many implementations do the coarse 'can update *some* Post' check in the guard and a finer per-instance <code>ability.can('update', loadedPost)</code> check inside the service/handler once the entity is loaded.</p>`,
  },
  {
    id: "a4-auth-deep-15",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why is ability.can('update', subject('Post', plainPostObject)) sometimes needed in CASL instead of just ability.can('update', plainPostObject)?`,
    answerHtml: `<p>CASL determines a subject's type by checking its constructor name or a <code>modelName</code> / <code>kind</code> property by default. A plain object returned from a raw DB query (or a DTO) often isn't an instance of the domain class and has no such marker, so CASL can't infer it's a <code>'Post'</code> and the rule won't match. The <code>subject()</code> helper explicitly tags a plain object with its subject type so condition-based rules (<code>{ authorId: user.id }</code>) evaluate correctly against it, which matters a lot when data comes from a query builder or aggregation pipeline rather than an ORM entity instance.</p>`,
  },
  {
    id: "a4-auth-deep-16",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why is server-side session authentication (cookie + server-stored session) often easier to revoke instantly than stateless JWT auth, and what does JWT lose by being stateless?`,
    answerHtml: `<p>A session is just a server-side record keyed by a session id in the cookie; deleting that record (or flipping a flag) instantly invalidates the session on the very next request, because every request re-checks the store. A stateless JWT carries its own validity in its signature — the server validates it cryptographically without a lookup, which is the whole performance/scalability win, but it also means there's no central record to delete. "Revoking" a JWT before its natural expiry requires bolting statefulness back on (a denylist, a token version/epoch check, or short expiry + refresh rotation), which gives back some of the scalability benefit you adopted JWTs for in the first place.</p>`,
  },
  {
    id: "a4-auth-deep-17",
    category: "auth",
    categoryLabel: "Auth",
    question: `When would a senior engineer choose server-side sessions over JWTs for a new NestJS API despite JWT's popularity, and what's the concrete deciding factor?`,
    answerHtml: `<p>When instant, centralized revocation is a hard requirement (e.g. regulated environments needing immediate logout-everywhere or compliance-driven account suspension) and the deployment is small/medium enough that a shared Redis session store isn't a meaningful scaling bottleneck. The deciding factor isn't security in the abstract — both can be made secure — it's whether the operational cost of a shared, always-consulted session store is acceptable in exchange for not having to engineer denylists/short-lived-token-plus-refresh machinery just to get the revocation guarantee sessions give you for free.</p>`,
  },
  {
    id: "a4-auth-deep-18",
    category: "auth",
    categoryLabel: "Auth",
    question: `What does ExtractJwt.fromAuthHeaderAsBearerToken() actually do in a NestJS JwtStrategy, and why might you replace it with a custom extractor for refresh tokens stored in a cookie?`,
    answerHtml: `<p>It's a Passport-JWT helper that reads the <code>Authorization: Bearer &lt;token&gt;</code> header and returns the token string, used as the strategy's <code>jwtFromRequest</code> option. For a refresh-token strategy where the token lives in an <code>httpOnly</code> cookie (not a header, since client JS can't read or attach it manually anyway), you instead pass a custom extractor function like <code>(req) =&gt; req.cookies?.refresh_token</code> — <code>jwtFromRequest</code> just needs to be any function returning the raw token string from wherever it actually lives on the request.</p>`,
  },
  {
    id: "a4-auth-deep-19",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why does passport-jwt's strategy verify the token's signature and expiry but NOT automatically check things like 'is this user still active' or 'has this token been revoked'?`,
    answerHtml: `<p>passport-jwt's job is purely cryptographic/structural: verify the signature against <code>secretOrKey</code>, check standard claims like <code>exp</code>/<code>nbf</code>/<code>aud</code>/<code>iss</code> if configured, and decode the payload — it has no knowledge of your domain (users, accounts, revocation lists). All of that lives in your strategy's <code>validate(payload)</code> callback, which runs only after signature verification passes; that's where you'd do a DB lookup for 'is this user disabled' or check a token denylist. Skipping that step means a structurally valid but logically stale token (banned user, revoked session) is accepted as long as it hasn't expired.</p>`,
  },
  {
    id: "a4-auth-deep-20",
    category: "auth",
    categoryLabel: "Auth",
    question: `What's the security implication of NOT pinning the JWT algorithm (e.g. algorithms: ['HS256']) when configuring a passport-jwt strategy?`,
    answerHtml: `<p>Without an explicit algorithm allowlist, some JWT libraries (historically) would accept whatever <code>alg</code> the token header itself declares — which opens the classic 'alg confusion' attack: if a server expects RS256 (asymmetric, verified with a public key) but accepts <code>alg: HS256</code> from the token header, an attacker can forge a token signed with HMAC using the server's public key (which is, well, public) as the HMAC secret, and the server's blind 'verify with whatever alg the token says' logic accepts it as valid. Always pin <code>algorithms: ['RS256']</code> or <code>['HS256']</code> explicitly rather than trusting the token header to declare it.</p>`,
  },
  {
    id: "a4-auth-deep-21",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why is a refresh token typically given a much longer expiry than an access token, and what risk does that long expiry reintroduce?`,
    answerHtml: `<p>The access token's short expiry (minutes) limits the blast radius of a leaked token without forcing the user to re-authenticate with credentials constantly; the refresh token's longer expiry (days/weeks) is what lets the session persist without repeated logins, exchanged periodically for fresh access tokens. The risk: the refresh token itself becomes the high-value, long-lived secret — exactly why it needs stronger protections (httpOnly cookie, rotation, reuse detection, narrow path scope) than the access token, since a leaked refresh token grants an attacker the same multi-week access a legitimate user has.</p>`,
  },
  {
    id: "a4-auth-deep-22",
    category: "auth",
    categoryLabel: "Auth",
    question: `In a CASL setup shared between NestJS (backend enforcement) and a frontend, why is it a mistake to treat the frontend's ability check as a security boundary?`,
    answerHtml: `<p>The frontend CASL ability is built from data sent to the client (the user's role/permission claims) and is used purely for UX — hiding buttons, disabling forms — but the client can be inspected, its JS modified, or API calls made directly with tools like curl/Postman, bypassing the UI entirely. The NestJS PoliciesGuard (or equivalent server-side check) is the only enforcement point that can't be bypassed by the client, so every authorization rule the frontend mirrors for UX must be independently re-checked server-side; sharing the CASL rule definitions between front and back end is fine for DRY-ness, but the server's evaluation is the only one that counts.</p>`,
  },
];

export const ADVANCED4_QUIZ: QuizQuestion[] = [
  {
    id: "a4q-auth-deep-1",
    category: "auth",
    categoryLabel: "Auth",
    question: `A NestJS app uses AuthGuard('jwt') globally via APP_GUARD. The /auth/refresh endpoint must accept only refresh tokens, never access tokens. What's the most robust way to enforce this distinction?`,
    options: [`Rely on the access token's short expiry so it naturally can't be misused as a refresh token`, `Use a single JwtStrategy that checks a \`type\` claim inside validate() and branches behavior accordingly`, `Register a separate strategy (e.g. 'jwt-refresh') with its own secret/options and guard the refresh route with that strategy specifically`, `Add a global interceptor that rejects any token used on the wrong route by inspecting the URL path`],
    answer: 2,
    explanationHtml: `<p>A distinct strategy with its own <code>secretOrKey</code> means an access token literally fails signature/option verification against the refresh strategy (and vice versa) before <code>validate()</code> even runs — the separation is structural, not just logical. A single strategy branching on a <code>type</code> claim still lets a same-secret token of the wrong type pass the cryptographic check and reach your manual branch, where one missed code path reintroduces the hole. Relying on expiry alone doesn't stop misuse within the access token's valid window.</p>`,
  },
  {
    id: "a4q-auth-deep-2",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why does Nest's AuthGuard call passport.authenticate() under the hood instead of NestJS reimplementing strategy verification itself?`,
    options: [`Because Passport strategies cannot be reimplemented due to licensing restrictions`, `Because AuthGuard is a thin adapter that lets Nest reuse Passport's existing strategy ecosystem (local, jwt, oauth2, etc.) and their \`validate\` contract rather than reinventing it`, `Because passport.authenticate() is required to populate req.session for every auth strategy`, `Because Nest guards cannot read the Authorization header without Passport's middleware`],
    answer: 1,
    explanationHtml: `<p><code>@nestjs/passport</code> exists specifically to bridge Nest's guard-based DI-friendly architecture onto Passport's large strategy ecosystem, so teams don't have to hand-roll OAuth2/JWT/local verification logic that Passport's community already maintains. Guards in Nest can read headers directly; Passport isn't required for that, and <code>req.session</code> is irrelevant to stateless strategies like JWT.</p>`,
  },
  {
    id: "a4q-auth-deep-3",
    category: "auth",
    categoryLabel: "Auth",
    question: `A RolesGuard checks \`req.user.roles.includes(requiredRole)\`. In production, a user demoted from admin to regular user mid-session still successfully calls an admin-only endpoint for the next 15 minutes. What's the most likely root cause?`,
    options: [`The RolesGuard is checking roles before the JwtStrategy's validate() runs`, `The access token's roles claim was baked in at issuance and the guard trusts the token's stale claim instead of re-checking current state`, `CASL is misconfigured and falling back to an 'allow all' default rule`, `The RolesGuard is missing a Reflector injection so it can't read @Roles metadata`],
    answer: 1,
    explanationHtml: `<p>This is the classic stateless-JWT staleness tradeoff: if <code>roles</code> is embedded in the JWT payload at login, a server-side demotion doesn't retroactively change an already-issued token — it remains valid (and 'admin' according to its own claims) until it expires or is refreshed. The fix for sensitive checks is either shorter access-token lifetimes, a server-side re-check against the DB, or a token-version/epoch invalidation scheme.</p>`,
  },
  {
    id: "a4q-auth-deep-4",
    category: "auth",
    categoryLabel: "Auth",
    question: `In CASL, given the rules \`can('update', 'Post'); cannot('update', 'Post', { published: true });\`, what does \`ability.can('update', somePublishedPost)\` evaluate to, and why?`,
    options: [`true, because can() rules are always evaluated last regardless of declaration order`, `false, because CASL evaluates rules in declaration order and the last matching rule (the cannot()) wins for a published post`, `It throws an error because can() and cannot() cannot target the same subject`, `true, because cannot() only applies to delete actions by convention`],
    answer: 1,
    explanationHtml: `<p>CASL resolves ability checks by scanning rules and letting the <b>last matching rule</b> win. The broad <code>can('update', 'Post')</code> matches first, but the more specific <code>cannot('update', 'Post', { published: true })</code> matches second for a published post and overrides it, so the result is <code>false</code>. This ordering is exactly why <code>cannot()</code> rules are typically declared after their corresponding broad <code>can()</code> rule, as an exception carve-out.</p>`,
  },
  {
    id: "a4q-auth-deep-5",
    category: "auth",
    categoryLabel: "Auth",
    question: `A team stores refresh tokens in localStorage 'because cookies are vulnerable to CSRF.' What is the strongest counterargument for using an httpOnly cookie instead, scoped correctly?`,
    options: [`httpOnly cookies are immune to all forms of token theft, including XSS`, `CSRF on a narrowly-scoped, SameSite cookie is mitigatable (SameSite=Strict/Lax, CSRF tokens on state-changing routes), while XSS against localStorage gives an attacker the raw token outright with no mitigation once the script runs`, `localStorage tokens expire automatically after the browser tab closes, weakening session persistence`, `Cookies cannot be scoped to a specific path like /auth/refresh, so this distinction doesn't matter`],
    answer: 1,
    explanationHtml: `<p>Both storage choices have a named risk, but they're not symmetric in mitigability: CSRF has well-established, layered defenses (<code>SameSite</code> attribute, double-submit/CSRF tokens, origin checks) that can reduce it to a non-issue, while a successful XSS against a page using <code>localStorage</code> hands the attacker the literal token string with no further barrier — <code>httpOnly</code> specifically prevents JS (including injected XSS payloads) from ever reading the cookie's value at all.</p>`,
  },
  {
    id: "a4q-auth-deep-6",
    category: "auth",
    categoryLabel: "Auth",
    question: `Why might a senior engineer deliberately choose stateful server-side sessions over stateless JWTs for an internal admin dashboard with under 50 concurrent users, even though the company's public API uses JWTs?`,
    options: [`Sessions are always faster to validate than JWTs regardless of scale`, `JWTs cannot be used for browser-based clients, only mobile apps`, `At that scale, a single shared session store isn't a bottleneck, and instant centralized revocation (immediate logout, fast incident response) outweighs the stateless-scaling benefit JWTs exist to provide`, `CASL-based authorization only works with session cookies, never with JWTs`],
    answer: 2,
    explanationHtml: `<p>JWT's core value proposition is avoiding a shared-state lookup on every request so auth scales horizontally without a bottleneck — a benefit that's irrelevant at 50 concurrent users on an internal tool. What does matter for an admin dashboard is the ability to instantly kill a session (compromised admin account, offboarded employee), which sessions give for free via a server-side delete, while JWTs need extra machinery (denylists, short expiry + rotation) to approximate the same guarantee.</p>`,
  },
  {
    id: "a4q-auth-deep-7",
    category: "auth",
    categoryLabel: "Auth",
    question: `A PoliciesGuard calls \`ability.can('update', 'Post')\` (the class/string form) and allows the request, but the service layer then lets a non-owner edit someone else's post. What's the architectural gap?`,
    options: [`CASL doesn't support ownership conditions, so this is unavoidable without a custom RBAC system`, `The guard performed a coarse, type-level check ('can update *some* Post') but no instance-level check against the loaded entity (e.g. ability.can('update', subject('Post', loadedPost))) was performed once the actual post was fetched`, `The guard should have used cannot() instead of can() to block non-owners`, `PoliciesGuard must run after the route handler, not before, to see the loaded entity`],
    answer: 1,
    explanationHtml: `<p>Guards typically run before route params are resolved into domain entities, so they can only check the coarse 'is this action ever permitted on this subject type' question. The fine-grained, condition-based check (e.g. <code>{ authorId: user.id }</code>) needs the actual loaded resource and is correctly performed inside the service/handler with <code>ability.can('update', subject('Post', loadedPost))</code> — omitting that second check is exactly the gap that lets a non-owner slip through.</p>`,
  },
];
