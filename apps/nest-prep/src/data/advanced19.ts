// Batch 19 — Node event-loop ordering drills: the six libuv phases (each FIFO), the microtask drain (nextTick then Promise) between every macrotask and phase, priority sync → nextTick → promises → macrotask, nextTick vs setImmediate vs setTimeout(0), recursive-nextTick starvation, and the .mjs top-level-microtask caveat. Predict-the-output practice; complements batch 12's thread-pool/cluster/worker material without repeating it.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED19_FLASHCARDS: Flashcard[] = [
  {
    id: "a19-phases-order-1",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Name the six libuv event-loop phases in order, and state the one property they all share.`,
    answerHtml: `<p>In order: <b>timers</b> → <b>pending callbacks</b> → <b>idle/prepare</b> (internal) → <b>poll</b> → <b>check</b> → <b>close callbacks</b>. Each phase owns a <b>FIFO callback queue</b>, so within a phase callbacks run in the order they were enqueued. Timers fires elapsed <code>setTimeout</code>/<code>setInterval</code>; pending runs a few deferred I/O callbacks (e.g. some TCP errors); poll is where ~90% of app code runs and where the loop blocks for new I/O; check runs <code>setImmediate</code>; close runs <code>'close'</code> handlers.</p><p>Why it matters: senior interviewers open with "walk me through the loop." <b>Red flag:</b> saying "Node is single-threaded so everything is just a queue" — the ordering across phases is the whole point, and not knowing it is the fastest way to read as mid-level.</p>`,
  },
  {
    id: "a19-microtask-drain-2",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `When exactly does Node drain the microtask queues, and in what order are the two microtask queues drained?`,
    answerHtml: `<p>Node drains microtasks <b>between every phase and between individual macrotask callbacks</b> — not just once per loop tick. Each drain empties two queues in a fixed order: <b>the <code>process.nextTick</code> queue first, then the Promise microtask queue</b> (<code>.then/.catch/.finally</code>, <code>queueMicrotask</code>). Both are drained <i>completely</i> before Node moves on to the next macrotask.</p><p>The quotable version: "One macrotask, then drain everything — nextTick before promises — repeat." <b>Red flag:</b> thinking microtasks run only once per full loop iteration; they run after each callback, which is why a flood of promises can delay the next timer.</p>`,
  },
  {
    id: "a19-priority-3",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `What is the top-to-bottom priority order for synchronous code, process.nextTick, Promise callbacks, and macrotasks?`,
    answerHtml: `<p><b>synchronous code → <code>process.nextTick</code> queue → Promise microtask queue → macrotask queue</b>. Synchronous code always finishes first (it's just the call stack). Then, before the loop advances, nextTick drains, then promises drain, and only then does a single macrotask (a timer, an I/O callback, a <code>setImmediate</code>) get to run.</p><p>Memorize it as one line you can recite under pressure: "sync, nextTick, promises, macrotask." Predicting mixed snippets is a near-universal senior screen — see the quiz drills.</p>`,
  },
  {
    id: "a19-nexttick-vs-immediate-4",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Contrast process.nextTick, setImmediate, and setTimeout(fn, 0) by where and when each fires.`,
    answerHtml: `<ul><li><code>process.nextTick</code> — a <b>microtask</b>, fires immediately after the current operation and before the loop continues; highest priority, not tied to any phase.</li><li><code>setImmediate</code> — a <b>macrotask</b>, fires in the <b>check</b> phase, after poll/I/O.</li><li><code>setTimeout(fn, 0)</code> — a <b>macrotask</b>, fires in the <b>timers</b> phase on a subsequent iteration.</li></ul><p>The trap in the names: <code>setImmediate</code> is <i>not</i> immediate and <code>nextTick</code> does <i>not</i> wait for the next tick. <b>Red flag:</b> claiming <code>nextTick</code> is a timer or that <code>setImmediate</code> beats <code>nextTick</code> — nextTick is a microtask and always wins.</p>`,
  },
  {
    id: "a19-immediate-vs-timeout-io-5",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Inside an I/O callback, does setImmediate or setTimeout(0) fire first — and why is the answer different at the top level?`,
    answerHtml: `<p><b>Inside an I/O callback, <code>setImmediate</code> always fires before <code>setTimeout(0)</code></b> — deterministically. You're already in the poll phase when the I/O callback runs, so the very next phase is check (<code>setImmediate</code>); timers only come around on the following iteration.</p><p>At the <b>top level</b> the order is <b>non-deterministic</b>: whether the ~1ms timer threshold has elapsed by the time the loop reaches the timers phase depends on process-startup timing, so you can see either order across runs. The interview-safe answer: "Top level? Racy. Inside I/O? <code>setImmediate</code> first, guaranteed."</p>`,
  },
  {
    id: "a19-nexttick-starvation-6",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `How can recursive process.nextTick starve the event loop, and what should you reach for instead?`,
    answerHtml: `<p>Because the nextTick queue is drained <b>completely</b> before the loop is allowed to advance, a callback that re-queues itself with <code>process.nextTick</code> keeps the queue perpetually non-empty. The loop <b>never reaches the poll phase</b>, so I/O callbacks, timers, and <code>setImmediate</code> never run — the process spins at 100% CPU doing nothing useful. This is loop <b>starvation</b>.</p><p>For "run this after the current operation but yield to I/O," use <code>setImmediate</code> instead — it schedules a macrotask in the check phase, letting the loop breathe between iterations. <b>Red flag:</b> reaching for recursive <code>nextTick</code> to build a work loop; that's a production incident waiting to happen.</p>`,
  },
  {
    id: "a19-esm-caveat-7",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Why does the same top-level snippet print in a different order in an .mjs (ESM) file than in a .cjs (CommonJS) file?`,
    answerHtml: `<p>In an <b>ESM</b> module the entire top-level body is wrapped as an <b>async operation</b> — it executes inside the Promise microtask queue rather than as plain synchronous startup code. So top-level statements interleave with other microtasks differently than in CommonJS, where the module body is straight synchronous code. The practical upshot: a mixed <code>nextTick</code>/<code>Promise</code>/<code>setTimeout</code> snippet at module top level can reorder between a <code>.mjs</code> and a <code>.cjs</code> file even though the code is identical.</p><p>Quotable: "In ESM, top-level <i>is</i> a microtask." <b>Red flag:</b> asserting an ordering as universal without asking "CJS or ESM?" — the module system is part of the answer.</p>`,
  },
  {
    id: "a19-predict-sync-order-8",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Predict the output (CommonJS): console.log('A'); process.nextTick(() =&gt; console.log('B')); Promise.resolve().then(() =&gt; console.log('C')); setTimeout(() =&gt; console.log('D'), 0);`,
    answerHtml: `<p><b>A, B, C, D.</b></p><ul><li><code>A</code> — synchronous, runs first on the call stack.</li><li><code>B</code> — <code>nextTick</code> microtask, drained before promises.</li><li><code>C</code> — Promise microtask, drained after the nextTick queue.</li><li><code>D</code> — <code>setTimeout</code> macrotask, the timers phase, runs only after all microtasks are drained.</li></ul><p>This is the canonical "priority order" drill: <b>sync → nextTick → promises → macrotask</b>. If you can narrate why each line lands where it does, you've demonstrated the mental model the phase list only hints at.</p>`,
  },
  {
    id: "a19-predict-immediate-timeout-9",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Predict the output (CommonJS): fs.readFile('x', () =&gt; { setTimeout(() =&gt; console.log('timeout'), 0); setImmediate(() =&gt; console.log('immediate')); });`,
    answerHtml: `<p><b>immediate, then timeout</b> — deterministically, every run.</p><p>The callback fires in the <b>poll</b> phase. From poll, the very next phase is <b>check</b>, where <code>setImmediate</code> runs — so <code>immediate</code> prints first. <code>setTimeout(0)</code> waits for the <b>timers</b> phase on the next loop iteration, so <code>timeout</code> prints second. Register order in the source doesn't matter here; phase order does.</p><p>Contrast with the same two calls at the <b>top level</b>, where the order is a coin flip driven by startup timing. "Inside I/O, <code>setImmediate</code> wins" is the line that shows you actually understand the phases, not just memorized a list.</p>`,
  },
  {
    id: "a19-predict-nested-nexttick-10",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Predict the output (CommonJS): Promise.resolve().then(() =&gt; console.log('P')); process.nextTick(() =&gt; { console.log('N1'); process.nextTick(() =&gt; console.log('N2')); });`,
    answerHtml: `<p><b>N1, N2, P.</b></p><p>The nextTick queue is drained <b>fully</b> before the Promise queue gets a turn. Running the first nextTick prints <code>N1</code> and <i>enqueues another nextTick</i>; because Node keeps draining the nextTick queue until it's empty, that new callback (<code>N2</code>) runs before Node ever looks at the Promise microtask queue. Only once the nextTick queue is truly empty does <code>P</code> run.</p><p>This is the mechanism behind starvation in miniature: nextTick can enqueue more nextTick work and monopolize the drain. <b>Red flag:</b> guessing <code>P</code> before <code>N2</code> — that assumes the queues alternate, which they don't.</p>`,
  },
  {
    id: "a19-macro-vs-micro-11",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Classify each as a macrotask or a microtask: setTimeout, setInterval, setImmediate, I/O callbacks, process.nextTick, Promise.then, queueMicrotask. Why does the distinction drive ordering?`,
    answerHtml: `<ul><li><b>Macrotasks:</b> <code>setTimeout</code>, <code>setInterval</code>, <code>setImmediate</code>, I/O callbacks. Exactly <b>one macrotask runs per loop tick</b> for a given phase's turn.</li><li><b>Microtasks:</b> <code>process.nextTick</code>, <code>Promise.then/.catch/.finally</code>, <code>queueMicrotask</code>. The whole microtask set is <b>drained completely between macrotasks</b>.</li></ul><p>The distinction is the ordering engine: microtasks always fully drain before the next macrotask, which is why a promise chain resolves before the next timer even fires. Note <code>nextTick</code> is technically its own queue that outranks the "real" microtask (Promise) queue.</p>`,
  },
  {
    id: "a19-poll-blocking-12",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `What does the poll phase do when its queue is empty, and how does a pending setImmediate change that decision?`,
    answerHtml: `<p>When the poll queue empties, poll has a choice. If there are <code>setImmediate</code> callbacks queued, it <b>ends poll and advances to the check phase</b> to run them. If there are none, it will <b>block and wait</b> for new I/O events to arrive (bounded by the nearest timer's threshold so timers aren't missed).</p><p>This is why poll is described as "where the loop sleeps": in an idle server, the process isn't spinning — it's parked in poll waiting on the OS for a socket to become readable. Understanding this explains why an idle Node process uses ~0% CPU while still being ready to respond instantly.</p>`,
  },
  {
    id: "a19-queuemicrotask-vs-nexttick-13",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `queueMicrotask and process.nextTick both schedule a microtask — so which runs first, and when should you prefer queueMicrotask?`,
    answerHtml: `<p><b><code>process.nextTick</code> runs first.</b> Node maintains the nextTick queue as a separate, higher-priority queue that is drained ahead of the "standard" microtask (Promise / <code>queueMicrotask</code>) queue. So a <code>nextTick</code> callback beats a <code>queueMicrotask</code> callback scheduled at the same time.</p><p>Prefer <code>queueMicrotask</code> when you want microtask timing that behaves like a resolved promise (the platform-standard, cross-runtime semantic) without jumping the queue ahead of promises — and to avoid the starvation footgun, since piling work into <code>nextTick</code> can delay I/O. Node's own guidance leans toward <code>queueMicrotask</code>/<code>setImmediate</code> over <code>nextTick</code> for most new code.</p>`,
  },
  {
    id: "a19-one-macrotask-per-tick-14",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `A phase's queue holds three setTimeout callbacks that all just re-schedule work with more setTimeout. Do all three run before any microtasks, and can they starve I/O the way recursive nextTick does?`,
    answerHtml: `<p>Within the timers phase, the queue is FIFO, so the callbacks already <i>due</i> run in order — but crucially, <b>microtasks drain between each of those callbacks</b>, and newly scheduled timers go to the <i>next</i> iteration's timers phase, not the current one. So the loop still advances through poll/check/close every iteration.</p><p>That's the key contrast with recursive <code>nextTick</code>: timers/<code>setImmediate</code> re-scheduling <b>cannot starve the loop</b> because each new callback lands on a future tick, giving I/O a turn every iteration. Recursive <code>nextTick</code> starves precisely because it stays in the <i>current</i> drain. "Macrotasks yield, nextTick doesn't" is the distinction worth quoting.</p>`,
  },
];

export const ADVANCED19_QUIZ: QuizQuestion[] = [
  {
    id: "a19-qz-1",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Predict the output (CommonJS):\n\nconsole.log('1');\nsetTimeout(() => console.log('2'), 0);\nPromise.resolve().then(() => console.log('3'));\nprocess.nextTick(() => console.log('4'));\nconsole.log('5');`,
    options: [`1 5 4 3 2`, `1 5 3 4 2`, `1 2 3 4 5`, `1 5 2 4 3`],
    answer: 0,
    explanationHtml: `<p>Order of priority is <b>sync → nextTick → promises → macrotask</b>. The synchronous logs run first in source order (<code>1</code>, then <code>5</code>). Then the microtask drain runs the <code>nextTick</code> queue before the Promise queue, so <code>4</code> then <code>3</code>. Finally the <code>setTimeout</code> macrotask fires in the timers phase: <code>2</code>. Result: <code>1 5 4 3 2</code>.</p>`,
  },
  {
    id: "a19-qz-2",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Predict the output (CommonJS):\n\nconst fs = require('fs');\nfs.readFile(__filename, () => {\n  setTimeout(() => console.log('timeout'), 0);\n  setImmediate(() => console.log('immediate'));\n});`,
    options: [`timeout, then immediate`, `immediate, then timeout`, `Non-deterministic — either order`, `Only 'immediate' prints; 'timeout' never runs`],
    answer: 1,
    explanationHtml: `<p>Inside an I/O callback the loop is in the <b>poll</b> phase, and the next phase is <b>check</b> (<code>setImmediate</code>), so <code>immediate</code> prints first; <code>setTimeout(0)</code> waits for the <b>timers</b> phase on the next iteration, printing <code>timeout</code> second. This is deterministic — the top-level version of the same code is the one that's racy.</p>`,
  },
  {
    id: "a19-qz-3",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `Predict the output (CommonJS):\n\nprocess.nextTick(() => {\n  console.log('A');\n  process.nextTick(() => console.log('B'));\n});\nPromise.resolve().then(() => console.log('C'));`,
    options: [`A C B`, `C A B`, `A B C`, `A C, then B never runs`],
    answer: 2,
    explanationHtml: `<p>The <code>nextTick</code> queue is drained <b>completely</b> before the Promise queue is touched. Running the first callback prints <code>A</code> and enqueues another <code>nextTick</code>; Node keeps draining until that queue is empty, so <code>B</code> runs next. Only then does the Promise microtask <code>C</code> run. Result: <code>A B C</code> — this is starvation in miniature.</p>`,
  },
  {
    id: "a19-qz-4",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `A colleague builds a busy work-loop where each unit of work re-schedules the next with recursive process.nextTick(), and reports the HTTP server has stopped responding to requests at 100% CPU. What is the root cause?`,
    options: [`process.nextTick is deprecated and silently drops callbacks under load`, `The nextTick queue is drained fully before the loop advances, so the loop never reaches the poll phase and I/O callbacks never run — the loop is starved`, `Recursive nextTick overflows the call stack, crashing the I/O subsystem`, `setImmediate should never be mixed with nextTick, corrupting the check phase`],
    answer: 1,
    explanationHtml: `<p>Because Node drains the entire <code>nextTick</code> queue before letting the loop advance, a callback that re-queues itself keeps the queue perpetually non-empty. The loop never reaches <b>poll</b>, so incoming socket I/O is never serviced — classic <b>starvation</b>. The fix is <code>setImmediate</code> (a check-phase macrotask), which lands the next unit of work on a future tick and lets I/O run every iteration.</p>`,
  },
  {
    id: "a19-qz-5",
    category: "node",
    categoryLabel: "Node.js Core",
    question: `The same top-level snippet mixing process.nextTick, Promise.then, and setTimeout prints in a different order when run as index.mjs than as index.cjs. What explains the difference?`,
    options: [`ESM disables process.nextTick, so its callbacks are silently reordered`, `In ESM the entire top-level module body executes as an async operation in the Promise microtask queue, so top-level statements interleave with microtasks differently than CommonJS's synchronous module body`, `.mjs files run in a Web Worker, which has no libuv phases`, `TypeScript compilation reorders the statements differently for the two module systems`],
    answer: 1,
    explanationHtml: `<p>In an <b>ESM</b> module the whole top-level body is wrapped as an async operation and runs within the Promise microtask queue, whereas a CommonJS module body is plain synchronous startup code. That changes how top-level statements interleave with <code>nextTick</code>/<code>Promise</code>/<code>setTimeout</code>, so an identical snippet can reorder between <code>.mjs</code> and <code>.cjs</code>. The interview-safe habit: always ask "CJS or ESM?" before committing to a top-level ordering.</p>`,
  },
];
