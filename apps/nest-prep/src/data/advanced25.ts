// Batch 25 — Distributed systems for Node backends: monolith-first decomposition along bounded contexts, message queues (RabbitMQ vs Kafka vs SQS), event-driven & CQRS with the outbox pattern, saga choreography vs orchestration, CAP framed as C-vs-A during a partition, eventual consistency, distributed locks (Redlock caveats), idempotency, and resilience (circuit breakers, bulkheads, timeouts, retries with backoff).
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED25_FLASHCARDS: Flashcard[] = [
  {
    id: "a25-monolith-first-1",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Why is "monolith-first" the senior default, and when do you actually extract a service?`,
    answerHtml: `<p>You extract services along <b>bounded contexts you already understand</b> — and early in a product you don't understand them yet. Splitting too soon freezes the wrong boundaries into network calls, and a wrong boundary is far more expensive to move across a wire than across a function call. The pitch: <i>"A monolith with clean module boundaries can become services later; premature services can't easily become a monolith again."</i></p><p>Extract when a piece has a genuinely different scaling profile, deploy cadence, team owner, or failure-isolation need — not because "microservices" is on the architecture slide. <b>Red flag:</b> a candidate who reaches for microservices on day one and can't name the specific problem distribution solves.</p>`,
  },
  {
    id: "a25-queues-broker-choice-2",
    category: "micro",
    categoryLabel: "Microservices",
    question: `RabbitMQ vs Kafka vs SQS — how do you choose the broker for a Node backend?`,
    answerHtml: `<p>Match the broker to the access pattern, not the hype:</p><ul><li><b>RabbitMQ</b> — flexible <b>routing</b> (exchanges, topic/direct/fanout) and <b>per-message ack</b>. Reach for it when different consumers need different subsets of messages and you want fine-grained redelivery. It's a message <i>broker</i>: once acked, a message is gone.</li><li><b>Kafka</b> — a durable, ordered, <b>replayable log</b> partitioned for high throughput. Reach for it when you need event replay, multiple independent consumer groups reading the same stream, or millions of events/sec. Messages persist by retention, not by consumption.</li><li><b>SQS</b> — a <b>managed</b> queue with almost no ops burden and a built-in DLQ. Reach for it on AWS when you want a queue and not a cluster to babysit; accept at-least-once delivery and no routing sophistication.</li></ul><p>The quotable line: <i>"RabbitMQ routes, Kafka replays, SQS just works — pick by whether you need routing, history, or zero ops."</i></p>`,
  },
  {
    id: "a25-kafka-partitions-3",
    category: "micro",
    categoryLabel: "Microservices",
    question: `In Kafka, what do partitions buy you, and what's the ordering catch seniors call out?`,
    answerHtml: `<p>Partitions are the unit of <b>parallelism</b>: a topic's throughput and consumer concurrency scale with partition count, because each partition is consumed by at most one consumer in a group. But ordering is guaranteed <b>only within a partition</b>, never across the topic. Messages are placed by the partition key's hash.</p><p>So the design lever is the key: to keep all events for one entity ordered (e.g. per-user or per-order), key by that entity id so they land on the same partition. <b>Red flag:</b> assuming "Kafka is ordered" globally — spread a user's events across partitions and you'll process their "cancel" before their "create."</p>`,
  },
  {
    id: "a25-sqs-dlq-4",
    category: "micro",
    categoryLabel: "Microservices",
    question: `What is a dead-letter queue, and why is it non-negotiable in a queue-based system?`,
    answerHtml: `<p>A DLQ is where a message goes after it has failed processing N times (SQS: <code>maxReceiveCount</code>). Without one, a single <b>poison message</b> — malformed, referencing deleted data, triggering a bug — gets redelivered forever, burning consumer capacity and blocking the queue behind it.</p><p>The DLQ turns an infinite retry loop into a bounded one plus an alertable backlog you can inspect and replay after a fix. The senior framing: <i>"A DLQ is not optional error handling — it's the difference between one bad message and a stalled pipeline."</i> Wire an alarm on DLQ depth; a silently filling DLQ is a silent outage.</p>`,
  },
  {
    id: "a25-cqrs-5",
    category: "micro",
    categoryLabel: "Microservices",
    question: `What problem does CQRS actually solve, and what does it cost?`,
    answerHtml: `<p>CQRS splits the <b>write model</b> from the <b>read model</b> so each is optimized independently: writes enforce invariants against a normalized model, reads serve denormalized projections shaped exactly for the query (often a separate store). It shines when read and write loads are wildly asymmetric or their ideal schemas diverge.</p><p>The cost is <b>eventual consistency</b> between the two sides — the read model lags the write model by the projection latency — plus the complexity of maintaining projections. <b>Red flag:</b> applying CQRS to a CRUD screen where reads and writes hit the same shape. The line: <i>"CQRS is a scalpel for read/write asymmetry, not a default architecture."</i></p>`,
  },
  {
    id: "a25-event-sourcing-6",
    category: "micro",
    categoryLabel: "Microservices",
    question: `What is event sourcing, and what's its headline benefit and headline pain?`,
    answerHtml: `<p>Instead of storing current state, you store the <b>immutable sequence of events</b> that produced it, and derive state by replaying them. The headline benefit is a perfect <b>audit trail</b> and time-travel — you can reconstruct state as of any moment and answer "how did we get here," which is gold for finance, compliance, and debugging.</p><p>The headline pain is <b>schema evolution</b>: events are forever, so a v1 event shape must still be replayable years later, forcing versioning/upcasting discipline. Rebuilds from a long log are slow without snapshots, and you can't just <code>UPDATE</code> a mistake — you append a correcting event. Pairs naturally with CQRS (events feed read projections).</p>`,
  },
  {
    id: "a25-outbox-pattern-7",
    category: "micro",
    categoryLabel: "Microservices",
    question: `The outbox pattern — what dual-write bug does it fix, and how?`,
    answerHtml: `<p>The bug is the <b>dual write</b>: your handler writes to the DB <i>and</i> publishes to a broker as two separate operations. If the process dies between them, you either committed data with no event published, or published an event for data that rolled back. There is no cross-system transaction to make them atomic.</p><p>The outbox fixes it by writing the event into an <code>outbox</code> table <b>in the same DB transaction</b> as the state change — one atomic commit. A separate relay process (poller or CDC/change-data-capture tailing the log) then reads the outbox and publishes to the broker, marking rows sent. The publish becomes <b>at-least-once</b> (the relay may retry), which is why downstream consumers must be idempotent. The line: <i>"You can't 2-phase-commit a database and a queue, so make the queue write part of the database write."</i></p>`,
  },
  {
    id: "a25-saga-8",
    category: "micro",
    categoryLabel: "Microservices",
    question: `What is a saga, and why do distributed transactions force you into one?`,
    answerHtml: `<p>Once a business operation spans multiple services, each with its own database, you can't wrap them in one ACID transaction. A <b>saga</b> replaces it with a sequence of <b>local transactions</b>, one per service, where each step publishes an event that triggers the next. If a later step fails, you run <b>compensating actions</b> that semantically undo the earlier committed steps (refund the charge, release the inventory) — you can't roll back, so you compensate forward.</p><p>The consequence seniors stress: there is no isolation. Intermediate states are <b>visible</b> to the outside world mid-saga, so you design for it (pending/reserved states) rather than pretend the operation is atomic.</p>`,
  },
  {
    id: "a25-saga-orchestration-9",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Saga choreography vs orchestration — what's the trade-off?`,
    answerHtml: `<p><b>Choreography</b>: no central coordinator — each service reacts to events and emits its own. It's decoupled and has no single point of failure, but the overall workflow is <b>emergent</b>, spread across services, and hard to see or debug. Great for a few simple steps; a nightmare to reason about at 8 steps with cyclic reactions.</p><p><b>Orchestration</b>: a central orchestrator explicitly drives each step and invokes compensations. The workflow lives in <b>one place</b> you can read, test, and observe, at the cost of a coordinator to build and keep available. The heuristic: <i>"Choreography for 2-3 steps, orchestration the moment the flow gets branchy or you can't answer 'where is this order right now' from one place."</i></p>`,
  },
  {
    id: "a25-cap-10",
    category: "micro",
    categoryLabel: "Microservices",
    question: `State the CAP theorem the way a senior does — and name the most common misuse.`,
    answerHtml: `<p>CAP says that <b>during a network partition</b>, a distributed system must choose between <b>consistency</b> (every read sees the latest write, or errors) and <b>availability</b> (every request gets a non-error response, possibly stale). You cannot have both while partitioned.</p><p>The misuse is treating it as "pick 2 of 3." <b>Partitions are not a choice</b> — over a real network they will happen, so P is a given. The real decision is C-vs-A <b>when</b> a partition occurs: a bank balance chooses C (refuse rather than serve stale money), a shopping cart chooses A (accept the write, reconcile later). The senior extension is <b>PACELC</b>: even with no partition (Else), you still trade <b>latency vs consistency</b>. The line: <i>"CAP isn't three dials; it's one decision you only make during a partition."</i></p>`,
  },
  {
    id: "a25-eventual-consistency-11",
    category: "micro",
    categoryLabel: "Microservices",
    question: `What does "eventual consistency" actually promise, and how do you make it safe to build on?`,
    answerHtml: `<p>It promises only that <b>if writes stop, all replicas will converge</b> to the same value — eventually. It says nothing about <i>when</i>, and reads in the meantime can be stale or see writes out of order. It's the price of choosing availability under CAP.</p><p>You make it livable by designing for staleness explicitly: show "pending" states, use <b>read-your-own-writes</b> where a user must see their own change immediately (route them to the primary or cache their write locally), make operations <b>commutative/idempotent</b> so out-of-order arrival still converges, and set SLAs on convergence lag. <b>Red flag:</b> a UI that assumes a write is instantly visible everywhere and shows the user a stale value as if it were fresh.</p>`,
  },
  {
    id: "a25-redlock-12",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Distributed locks with Redis (Redlock) — what are the two failure modes that bite people?`,
    answerHtml: `<p>A distributed lock (e.g. <code>SET key val NX PX ttl</code>, or the multi-node Redlock algorithm) coordinates "only one worker does X at a time" across processes. Two things break it:</p><ul><li><b>Lock expiry vs work duration</b> — the TTL exists so a crashed holder doesn't deadlock everyone, but if your work runs longer than the TTL, the lock expires <i>while you're still working</i>, a second worker acquires it, and now two run concurrently. You must either bound the work under the TTL or fence.</li><li><b>Clock skew / GC pauses</b> — Redlock's safety leans on bounded clock drift; a long GC pause or a clock jump can make a holder believe it still owns an expired lock. This is the crux of the famous Kleppmann critique.</li></ul><p>The robust fix is a monotonic <b>fencing token</b>: every lock grant returns an increasing number, and the protected resource rejects any write carrying a token lower than the highest it has seen — so a stale holder's write is refused even if it thinks it still holds the lock. The line: <i>"A lock with a TTL is a lease, not a mutex — treat expiry as a real possibility and fence the resource."</i></p>`,
  },
  {
    id: "a25-idempotency-13",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Why is "exactly-once" a myth, and how do idempotency keys give you the same effect?`,
    answerHtml: `<p>True exactly-once <b>delivery</b> is unachievable over an unreliable network: the sender can never distinguish "message lost" from "ack lost," so it must retry, which means at-least-once. Everyone who claims exactly-once is really doing at-least-once delivery plus idempotent processing.</p><p>So you design idempotent <b>consumers</b>: the client attaches a unique <b>idempotency key</b> per logical operation; on first processing you record a <b>completion record</b> (key → result) inside the same transaction as the side effect; on any retry you look up the key, see it's done, and return the stored result instead of doing the work again. That yields exactly-once <i>effect</i> on top of at-least-once delivery. The line: <i>"Stop chasing exactly-once delivery — make the handler idempotent and let it be delivered as many times as the network wants."</i> <b>Red flag:</b> a payment endpoint with no idempotency key, so a client retry double-charges.</p>`,
  },
  {
    id: "a25-circuit-breaker-14",
    category: "micro",
    categoryLabel: "Microservices",
    question: `What does a circuit breaker prevent, and what are its three states?`,
    answerHtml: `<p>It prevents a <b>cascading failure</b>: when a downstream dependency is failing, hammering it with retries piles up in-flight requests, exhausts your thread/connection pool, and takes <i>your</i> service down too — the outage spreads upstream. The breaker trips after a failure threshold and <b>fails fast</b> instead, giving the dependency room to recover and freeing your resources.</p><p>Three states: <b>Closed</b> (traffic flows, failures counted), <b>Open</b> (trip: reject immediately, often serving a fallback, for a cool-down), <b>Half-Open</b> (let a trial request or two through; success closes it, failure re-opens). The line: <i>"A circuit breaker converts a slow, resource-eating failure into a fast, contained one."</i></p>`,
  },
  {
    id: "a25-resilience-toolkit-15",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Beyond circuit breakers, name the resilience primitives and the one retry mistake that amplifies outages.`,
    answerHtml: `<p>The core toolkit:</p><ul><li><b>Timeouts</b> — every network call gets an aggressive one; an unbounded call is a resource leak waiting for a slow dependency. This is the cheapest, most-skipped fix.</li><li><b>Retries with exponential backoff + jitter</b> — retry transient failures, but space attempts out and randomize them.</li><li><b>Bulkheads</b> — isolate resources (separate connection pools/thread pools per dependency) so one saturated dependency can't drown the others, like watertight compartments in a ship.</li><li><b>Fallbacks</b> — a degraded-but-useful response (cached value, default, empty list) when the real call fails.</li></ul><p>The amplifying mistake is the <b>retry storm</b>: fixed-interval retries with no jitter, so thousands of clients retry in lockstep and DDoS a recovering service in synchronized waves. Jitter breaks the synchronization; a retry budget/circuit breaker caps the total. The line: <i>"Retries without backoff and jitter don't add resilience — they add a self-inflicted DDoS."</i></p>`,
  },
];

export const ADVANCED25_QUIZ: QuizQuestion[] = [
  {
    id: "a25-qz-1",
    category: "micro",
    categoryLabel: "Microservices",
    question: `A payment service must record a charge in its database and publish a "PaymentSucceeded" event to a broker. What is the correct way to avoid the dual-write inconsistency?`,
    options: [
      `Publish to the broker first, then write to the DB, so the event always exists`,
      `Wrap the DB write and the broker publish in a two-phase commit across both systems`,
      `Write the event to an outbox table in the same DB transaction as the charge, and let a relay publish it to the broker at-least-once`,
      `Publish the event and write the DB row in parallel to minimize the window between them`,
    ],
    answer: 2,
    explanationHtml: `<p>You can't atomically commit a database and a broker together, so the outbox pattern folds the event into the same DB transaction as the state change (one atomic commit), then a separate relay reads the outbox and publishes at-least-once. Publish-first or parallel writes both leave a window where a crash produces an event with no data or data with no event; a true 2PC across a DB and a queue is generally unavailable and fragile. Because the relay may retry, downstream consumers must be idempotent.</p>`,
  },
  {
    id: "a25-qz-2",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Which statement about the CAP theorem is the most accurate senior framing?`,
    options: [
      `You permanently pick two of Consistency, Availability, and Partition tolerance for your whole system`,
      `Partitions are unavoidable on a real network, so the real choice is Consistency vs Availability during a partition`,
      `A well-engineered system can achieve consistency, availability, and partition tolerance simultaneously`,
      `CAP only applies to SQL databases, not to message-based systems`,
    ],
    answer: 1,
    explanationHtml: `<p>CAP is not "pick 2 of 3." Over a real network, partitions <i>will</i> happen, so partition tolerance isn't optional — the actual decision is whether, <b>during a partition</b>, you sacrifice consistency (serve possibly-stale data to stay available) or availability (refuse requests to stay consistent). PACELC extends this: even without a partition you still trade latency vs consistency.</p>`,
  },
  {
    id: "a25-qz-3",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Why is achieving true "exactly-once delivery" in a distributed message system considered unrealistic, and what do teams do instead?`,
    options: [
      `It's a licensing limitation of Kafka; SQS achieves exactly-once natively`,
      `A sender can't distinguish a lost message from a lost ack, so it must retry (at-least-once); teams make consumers idempotent to get exactly-once effect`,
      `Exactly-once is trivially available if you disable retries entirely`,
      `Networks are reliable enough that exactly-once is the default; the myth is that it's hard`,
    ],
    answer: 1,
    explanationHtml: `<p>Because a lost message and a lost acknowledgement look identical to the sender, it must retry to be safe — which makes delivery at-least-once. So "exactly-once" is achieved as an <i>effect</i>, not a delivery guarantee: consumers use idempotency keys plus completion records (stored in the same transaction as the side effect) so a redelivered message returns the prior result instead of repeating the work.</p>`,
  },
  {
    id: "a25-qz-4",
    category: "micro",
    categoryLabel: "Microservices",
    question: `A worker acquires a Redis lock with a 30s TTL, then a long GC pause freezes it for 40s. What is the real risk, and what mitigates it?`,
    options: [
      `No risk — Redis automatically extends the TTL while the client is paused`,
      `The lock expires during the pause, a second worker acquires it, and both run concurrently; a monotonic fencing token that the resource checks mitigates it`,
      `The paused worker crashes Redis, so the mitigation is to disable TTLs on locks`,
      `Nothing runs twice because Redis guarantees the original holder always wins`,
    ],
    answer: 1,
    explanationHtml: `<p>A lock TTL is a lease: it exists so a dead holder doesn't deadlock everyone, but it means the lock can expire while a paused (or just slow) holder still believes it owns it. A second worker then acquires the lock and you get concurrent execution. Clock skew and GC pauses are exactly why Redlock's safety is debated. The robust mitigation is a monotonically increasing fencing token returned on each grant, which the protected resource uses to reject any write carrying a stale (lower) token.</p>`,
  },
  {
    id: "a25-qz-5",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Your service calls a downstream API that has become slow and is failing intermittently. Requests pile up and threaten to exhaust your connection pool. Which resilience pattern most directly prevents this failure from cascading into your own service?`,
    options: [
      `Increase the retry count so more requests eventually succeed`,
      `A circuit breaker that trips after a failure threshold and fails fast, freeing resources and giving the dependency room to recover`,
      `Remove all timeouts so requests wait as long as needed`,
      `Switch the downstream call from async to synchronous so it blocks less`,
    ],
    answer: 1,
    explanationHtml: `<p>A circuit breaker detects the elevated failure rate, trips to Open, and fails fast (often returning a fallback) instead of letting requests pile up and drain your connection/thread pool — that's precisely how it stops a downstream failure from cascading upstream into your service. More retries or removing timeouts do the opposite: they hold resources longer and amplify the pileup. Bulkheads (isolated pools per dependency) complement the breaker by containing the blast radius.</p>`,
  },
];
