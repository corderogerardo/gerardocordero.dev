// Batch 13 — Rate limiting/throttling (@nestjs/throttler), Helmet & CORS configuration, API versioning strategies (URI/header/media-type), and input sanitization against injection.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED13_FLASHCARDS: Flashcard[] = [
  {
    id: "a13-rate-limiting-1",
    category: "security",
    categoryLabel: "Security",
    question: `When you apply ThrottlerModule.forRoot() with a single { ttl, limit } pair, what key does @nestjs/throttler use by default to bucket requests, and why is that default dangerous behind a shared load balancer or NAT?`,
    answerHtml: `<p>By default the throttler keys on <b>the resolved client IP</b> (<code>req.ip</code>) plus the route. Behind a reverse proxy, NAT, or corporate gateway, many distinct users share one IP, so the default key punishes the whole group as one bucket once any of them trips the limit.</p><p>Two fixes: (1) configure <code>app.set('trust proxy', ...)</code> (Express) or the equivalent adapter option so <code>req.ip</code> reflects <code>X-Forwarded-For</code> correctly instead of the proxy's own IP, and (2) override <code>getTracker()</code> in a custom guard to key on something more specific, e.g. <code>userId</code> for authenticated routes.</p>`,
  },
  {
    id: "a13-rate-limiting-2",
    category: "security",
    categoryLabel: "Security",
    question: `How do named throttlers (multiple { name, ttl, limit } entries passed to ThrottlerModule.forRoot([...])) let you express both a short burst limit and a longer sustained-rate limit on the same endpoint?`,
    answerHtml: `<p>You register multiple throttler profiles, e.g. <code>{ name: 'short', ttl: 1000, limit: 3 }</code> and <code>{ name: 'long', ttl: 60000, limit: 100 }</code>. The global <code>ThrottlerGuard</code> evaluates <b>every</b> registered throttler against each request, so a request must pass all buckets simultaneously.</p><p>Per route you can override just one profile's numbers with <code>@Throttle({ short: { limit: 1, ttl: 1000 } })</code>, tightening the burst limit on a sensitive endpoint while the global sustained-rate throttler keeps applying unchanged.</p>`,
  },
  {
    id: "a13-rate-limiting-3",
    category: "security",
    categoryLabel: "Security",
    question: `Why does the default in-memory ThrottlerStorageService silently break rate-limit correctness once you scale to more than one Node.js instance, and what's the standard production fix?`,
    answerHtml: `<p>The default storage keeps hit counters in a process-local <code>Map</code>. Behind a load balancer, each instance has its own independent counter, so a client can get N requests per instance instead of N total — the effective limit multiplies by instance count and resets whenever a pod restarts or autoscaling adds a replica.</p><p>The fix is a shared external store: install <code>@nest-lab/throttler-storage-redis</code> (or write a custom <code>ThrottlerStorage</code>) and pass it as the <code>storage</code> option in <code>ThrottlerModule.forRoot()</code>, so all instances increment the same Redis-backed counter.</p>`,
  },
  {
    id: "a13-rate-limiting-4",
    category: "security",
    categoryLabel: "Security",
    question: `What is the practical difference between disabling the throttler with @SkipThrottle() versus setting a route's limit very high with @Throttle({ default: { limit: 1000, ttl: 60000 } })?`,
    answerHtml: `<p><code>@SkipThrottle()</code> removes the route (or the whole controller, or one named throttler via <code>@SkipThrottle({ short: true })</code>) from rate-limit tracking entirely — no counter is incremented, no storage write happens, and there's zero risk of the route ever throwing <code>ThrottlerException</code>. (Called with no arguments it defaults to <code>{ default: true }</code>, skipping the <code>'default'</code>-named throttler — the one a single unnamed <code>ThrottlerModule.forRoot()</code> config produces.)</p><p>A very high <code>@Throttle()</code> limit still tracks every request, still writes to storage on every call, and can still reject traffic under an actual flood — it's a higher ceiling, not an exemption. Use <code>@SkipThrottle()</code> for webhooks or health checks that must never 429; use a high limit when you still want abuse protection, just looser.</p>`,
  },
  {
    id: "a13-rate-limiting-5",
    category: "security",
    categoryLabel: "Security",
    question: `Your NestJS API sits behind an API gateway that already enforces rate limits per API key. What's the architectural argument for still running @nestjs/throttler inside the service, and what should it protect against that the gateway can't?`,
    answerHtml: `<p>A gateway-level limit is usually coarse (per API key or per IP, uniform across all routes) and configured by a separate team or system that can drift out of sync with the app. An in-process throttler can apply <b>per-route, per-cost</b> limits that understand the application's own hot paths — e.g. a stricter limit on <code>/auth/login</code> or a search endpoint that hits the database hard, independent of the gateway's blanket policy.</p><p>It also matters for <b>defense in depth</b>: if the gateway is misconfigured, bypassed (internal service-to-service calls, a staging gateway with looser rules), or simply doesn't exist in some deployment topology, the app-level guard is the last line stopping a single caller from exhausting connection pools or CPU.</p>`,
  },
  {
    id: "a13-rate-limiting-6",
    category: "security",
    categoryLabel: "Security",
    question: `What specific attack does helmet's contentSecurityPolicy middleware mitigate that helmet's other headers (X-Frame-Options, X-Content-Type-Options) do not, and why is CSP harder to adopt in a NestJS app that serves a SPA with inline scripts?`,
    answerHtml: `<p>CSP mitigates <b>cross-site scripting (XSS)</b> by restricting which sources the browser is allowed to load scripts, styles, images, and connections from — even if an attacker manages to inject a <code>&lt;script&gt;</code> tag into the page, the browser refuses to execute it unless its source matches the policy. <code>X-Frame-Options</code> only stops clickjacking (framing), and <code>X-Content-Type-Options: nosniff</code> only stops MIME-sniffing attacks — neither blocks injected script execution.</p><p>It's harder to adopt with SPAs because frameworks often inject inline <code>&lt;script&gt;</code> or use <code>eval</code>-like patterns (some bundlers, some hydration code), which a strict default-src CSP blocks. The fix is per-request <b>nonces</b> (helmet supports a nonce callback) or hashing the specific inline scripts, rather than disabling CSP or using <code>unsafe-inline</code>, which defeats the protection.</p>`,
  },
  {
    id: "a13-rate-limiting-7",
    category: "security",
    categoryLabel: "Security",
    question: `In a NestJS app, why must helmet() be applied as global middleware in main.ts (before app.listen()) rather than as a route-scoped NestMiddleware applied via consumer.apply(helmet()).forRoutes('api/*') in a feature module?`,
    answerHtml: `<p>Security headers like <code>Strict-Transport-Security</code>, CSP, and <code>X-Content-Type-Options</code> need to apply to <b>every response</b> the server sends — including 404s, error responses, static assets, and routes added later by other modules. Scoping helmet to specific routes creates gaps: any path not matched by the route pattern (a typo, a new module someone forgets to wire up, an error-handling fallback) ships without protection.</p><p>Applying it globally with <code>app.use(helmet())</code> in <code>main.ts</code>, before <code>app.listen()</code>, guarantees the headers are set on the response before any controller or exception filter runs, with no per-module opt-in required.</p>`,
  },
  {
    id: "a13-rate-limiting-8",
    category: "security",
    categoryLabel: "Security",
    question: `What does helmet's hsts (Strict-Transport-Security) option actually protect against, and why is preload: true a one-way door you should think carefully about before enabling?`,
    answerHtml: `<p>HSTS tells the browser "never connect to this origin over plain HTTP again, even if the user types <code>http://</code> or clicks an http link, for <code>maxAge</code> seconds" — it protects against <b>SSL-stripping man-in-the-middle attacks</b> where an attacker on the network downgrades an initial HTTP request before a redirect to HTTPS can happen.</p><p><code>preload: true</code> asks browsers to hardcode your domain into a baked-in list shipped with the browser itself, enforced even on the very first visit with no prior HSTS header seen. Submitting to the preload list is effectively permanent — removal takes months to propagate through browser release cycles — so enabling it on a domain that might need to serve plain HTTP later (or a subdomain you don't fully control) can lock you out of HTTP for a long time.</p>`,
  },
  {
    id: "a13-rate-limiting-9",
    category: "security",
    categoryLabel: "Security",
    question: `Why does enableCors({ origin: true, credentials: true }) in a NestJS app create a meaningfully different security posture than enableCors({ origin: '*' }), even though both "allow everyone"?`,
    answerHtml: `<p><code>origin: '*'</code> sends a literal wildcard <code>Access-Control-Allow-Origin: *</code> header. Per the Fetch/CORS spec, browsers <b>refuse to combine a wildcard origin with <code>Access-Control-Allow-Credentials: true</code></b> — so cookies, Authorization headers, and TLS client certs are never sent cross-origin, and the response is unreadable by the page if it tried to use credentials.</p><p><code>origin: true</code> makes the CORS middleware <b>reflect whatever Origin header the request sent</b> back as the allowed origin. Combined with <code>credentials: true</code>, this is valid per spec and browsers will allow it — meaning literally any website can make a credentialed request to your API using the visitor's cookies/session, which is a CSRF-adjacent vulnerability dressed up as a CORS config. The safe pattern is an explicit allowlist function that checks the Origin against known domains.</p>`,
  },
  {
    id: "a13-rate-limiting-10",
    category: "security",
    categoryLabel: "Security",
    question: `A NestJS API serves both a first-party web app and is consumed by third-party server-to-server integrations with API keys. Why is it wrong to apply one global CORS policy via app.enableCors() for both use cases?`,
    answerHtml: `<p>CORS is a <b>browser-enforced</b> mechanism — it does nothing for server-to-server calls, since those don't run in a browser and ignore <code>Access-Control-Allow-Origin</code> entirely. So the CORS policy is only ever relevant to the browser-based first-party traffic; configuring it permissively "for the integrations" has zero effect on them and only widens the attack surface for credentialed browser requests.</p><p>The correct split: keep CORS tight (explicit allowlist of your own web app's origin) for browser traffic, and authenticate server-to-server calls with API keys/mTLS/signed requests that don't depend on Origin checks at all — those clients never send a browser <code>Origin</code> header in the way CORS expects.</p>`,
  },
  {
    id: "a13-rate-limiting-11",
    category: "security",
    categoryLabel: "Security",
    question: `What's the difference between a CORS preflight request being allowed and an endpoint actually being safe from CSRF, and why can't CORS alone be your CSRF defense for cookie-authenticated routes?`,
    answerHtml: `<p>CORS only governs whether <b>JavaScript running on another origin can read the response</b> of a cross-origin request — it does not stop the request from being sent or executed server-side. A simple cross-origin <code>&lt;form&gt;</code> POST (or an <code>&lt;img&gt;</code>/fetch with <code>no-cors</code>) reaches your server and executes with the browser auto-attaching cookies, even though the attacker's page can never read the JSON response.</p><p>For any cookie-authenticated mutating endpoint, you still need a dedicated CSRF defense: <code>SameSite=Lax/Strict</code> cookies as a baseline, plus a CSRF token (double-submit cookie or synchronizer token) for state-changing requests, because CORS was designed to protect response confidentiality, not to prevent the request itself.</p>`,
  },
  {
    id: "a13-rate-limiting-12",
    category: "security",
    categoryLabel: "Security",
    question: `Compare URI versioning (/v1/users), header versioning (Accept-Version: 1), and media-type versioning (Accept: application/vnd.api.v1+json) in NestJS's VersioningType enum. What's the main tradeoff each makes for caching and routing?`,
    answerHtml: `<p><b>URI versioning</b> (<code>VersioningType.URI</code>) bakes the version into the path. It's the most cache-friendly and debuggable — CDNs, browser history, and logs naturally segment by version, and you can route different versions to entirely different deployed services at the infra layer. Downside: the resource's identity (its URL) changes per version, which is awkward from a pure-REST "URLs should identify the same resource forever" standpoint.</p><p><b>Header versioning</b> (<code>VersioningType.HEADER</code>, with a header name you configure yourself — Nest has no built-in default like <code>Accept-Version</code>) keeps URLs stable, which is good for REST purity and bookmarking, but it's invisible to URL-based caching layers (a CDN caching by URL alone will serve the wrong version) unless you add <code>Vary: &lt;your-version-header&gt;</code>, and it's harder to test by just pasting a URL in a browser.</p><p><b>Media-type versioning</b> (<code>VersioningType.MEDIA_TYPE</code>, a custom <code>Accept</code> with a vendor MIME type) is the most "correct" REST/HATEOAS approach — version is a property of representation negotiation — but it's the least ergonomic for API consumers and tooling (curl, Postman, browsers) that don't naturally manipulate <code>Accept</code>.</p>`,
  },
  {
    id: "a13-rate-limiting-13",
    category: "security",
    categoryLabel: "Security",
    question: `In NestJS, what's the practical effect of setting defaultVersion: '1' vs. leaving a controller's @Controller() with no version and no global defaultVersion, once app.enableVersioning({ type: VersioningType.URI }) is called?`,
    answerHtml: `<p>Once you call <code>app.enableVersioning({ type: VersioningType.URI })</code>, each route resolves its version as <code>@Version()</code> metadata if present, else the global <code>defaultVersion</code>, else nothing. Setting <code>defaultVersion: '1'</code> means any controller/handler without its own <code>version</code> automatically falls under that default and is reachable at <code>/v1/...</code>.</p><p>Without a <code>defaultVersion</code>, a route with no explicit version resolves to <i>no version at all</i> (not an error) — NestJS's route builder only prepends a <code>/v{n}</code> segment when a concrete version (or array of versions) is present, so the route simply stays mounted at its bare unversioned path, e.g. <code>/users</code> instead of <code>/v1/users</code>. It is never "unreachable"; the same fallback (no prefix) applies to <code>VERSION_NEUTRAL</code> routes. The practice that avoids surprises is still to set an explicit <code>defaultVersion</code> or mark legacy/internal routes with <code>version: VERSION_NEUTRAL</code>, so every route's URL shape is an intentional choice rather than an accident of omission.</p>`,
  },
  {
    id: "a13-rate-limiting-14",
    category: "security",
    categoryLabel: "Security",
    question: `A consumer hits POST /v1/orders and POST /v2/orders, both implemented in the same OrdersController. What two NestJS mechanisms let you implement this without duplicating the controller class, and when would you actually want two separate controller classes instead?`,
    answerHtml: `<p>(1) Per-method <code>@Version('1')</code> / <code>@Version('2')</code> decorators on separate handler methods within one controller, both mapped to the same path. (2) A single handler that reads <code>@Req() req</code> or injects version info and branches internally (less idiomatic, harder to test).</p><p>You'd split into two controller classes when v1 and v2 diverge enough in <b>validation DTOs, business logic, or dependencies</b> that keeping them in one class creates a god-class with version-conditional branches everywhere — at that point, separate controllers (possibly in separate modules, e.g. <code>OrdersV1Module</code>/<code>OrdersV2Module</code>) keep each version's code path independently testable and let you delete v1 cleanly when it's sunset.</p>`,
  },
  {
    id: "a13-rate-limiting-15",
    category: "security",
    categoryLabel: "Security",
    question: `Why is 'sanitize input against SQL injection' largely a non-issue when using TypeORM or Prisma's query builders, but still a live risk when a NestJS service drops to $queryRawUnsafe or a raw TypeORM query() call?`,
    answerHtml: `<p>ORM query builders (TypeORM's <code>.where('col = :val', { val })</code>, Prisma's generated client methods) <b>parameterize values automatically</b> — the value is sent to the database separately from the SQL text via prepared-statement placeholders, so user input can never be interpreted as SQL syntax no matter what characters it contains.</p><p>The risk reappears the moment a developer drops to raw SQL and does <b>string interpolation</b> instead of parameter binding — e.g. <code>connection.query(\`SELECT * FROM users WHERE email = '\${email}'\`)</code> or Prisma's <code>$queryRawUnsafe(sql)</code> with interpolated values. Prisma's <code>$queryRaw</code> tagged-template form is safe (it parameterizes), but <code>$queryRawUnsafe</code> is explicitly named "unsafe" because it takes a plain string and does not parameterize — it's on the developer to bind params manually.</p>`,
  },
  {
    id: "a13-rate-limiting-16",
    category: "security",
    categoryLabel: "Security",
    question: `What's the difference between input validation (class-validator + ValidationPipe) and input sanitization, and why doesn't a NestJS DTO with strict @IsString() @MaxLength(100) decorators alone protect against stored XSS?`,
    answerHtml: `<p><b>Validation</b> rejects input that doesn't conform to an expected shape/type/range — a string is a string whether it contains <code>&lt;script&gt;alert(1)&lt;/script&gt;</code> or plain text, so <code>@IsString()</code> and <code>@MaxLength()</code> happily accept malicious markup as long as it's a short string. Validation answers "is this the right shape," not "is this safe to render."</p><p><b>Sanitization</b> transforms or strips dangerous content from otherwise-valid input — e.g. stripping/escaping HTML tags before storing a user bio that will later be rendered. XSS is fundamentally an <b>output-encoding</b> problem: the real fix is contextual output escaping at render time (React/most template engines auto-escape by default) and/or sanitizing rich-text fields specifically meant to allow some HTML (via a library like <code>sanitize-html</code> with an explicit allowlist), not relying on input length/type checks.</p>`,
  },
  {
    id: "a13-rate-limiting-17",
    category: "security",
    categoryLabel: "Security",
    question: `Why does enabling ValidationPipe with whitelist: true and forbidNonWhitelisted: true matter as a security control, not just a data-hygiene one, especially for an endpoint backed by an ORM's create()/save() with a spread DTO?`,
    answerHtml: `<p><code>whitelist: true</code> strips any incoming property not declared on the DTO class; <code>forbidNonWhitelisted: true</code> goes further and <b>rejects the whole request</b> if it contains undeclared properties, rather than silently dropping them. Without this, if a handler does something like <code>this.repo.save({ ...dto, ownerId: currentUser.id })</code> but the DTO validation is loose, an attacker can smuggle in extra fields like <code>role: 'admin'</code> or <code>isVerified: true</code> in the JSON body that get persisted if the entity/save call isn't carefully field-limited — a classic <b>mass assignment</b> vulnerability.</p><p><code>forbidNonWhitelisted</code> is the stronger of the two for security because it surfaces the attempt (400 error, often logged) rather than quietly stripping it, which also helps catch integration bugs where a client is sending fields the API never asked for.</p>`,
  },
  {
    id: "a13-rate-limiting-18",
    category: "security",
    categoryLabel: "Security",
    question: `How does NoSQL injection differ from SQL injection in a NestJS app using Mongoose, and what's the concrete attack if a login handler does User.findOne({ email: req.body.email, password: req.body.password }) without validating types?`,
    answerHtml: `<p>SQL injection exploits string concatenation to alter SQL <i>syntax</i>. NoSQL injection (against MongoDB specifically) exploits the fact that MongoDB query objects accept <b>operator keys</b> like <code>$ne</code>, <code>$gt</code>, or <code>$regex</code> as object values — if the request body is parsed as JSON and passed straight into a query object, an attacker can send <code>{ "email": "admin@site.com", "password": { "$ne": null } }</code> instead of a string password.</p><p>Mongoose builds the query as <code>{ email: 'admin@site.com', password: { $ne: null } }</code>, which MongoDB interprets as "password is not null" — true for any account with a password set — bypassing authentication entirely. The fix is enforcing DTO types with <code>@IsString()</code> via <code>ValidationPipe</code> (so a nested object is rejected before it reaches the query) and/or using a library like <code>mongo-sanitize</code> to strip keys starting with <code>$</code> or containing <code>.</code>.</p>`,
  },
  {
    id: "a13-rate-limiting-19",
    category: "security",
    categoryLabel: "Security",
    question: `What is a ReDoS (Regular expression Denial of Service) vulnerability, and where does it commonly sneak into a NestJS app's input-sanitization layer via class-validator?`,
    answerHtml: `<p>ReDoS exploits regex patterns with <b>catastrophic backtracking</b> — patterns with nested quantifiers like <code>(a+)+</code> or ambiguous alternation that, for a crafted malicious string, cause the regex engine's matching time to grow exponentially with input length. A single request with a carefully built string (e.g. many repeated characters followed by one that fails to match) can pin a CPU core for seconds or minutes, becoming a single-request DoS.</p><p>It commonly sneaks in via custom <code>@Matches(/some-pattern/)</code> validators on DTOs (email-like patterns, URL patterns, password-complexity patterns are common offenders), or transitively through a validation library's built-in patterns. Mitigations: avoid hand-rolled complex regex for things like email validation (use <code>@IsEmail()</code>'s vetted implementation), test custom patterns against pathological inputs, cap input length with <code>@MaxLength()</code> <i>before</i> the regex runs, or use a regex-complexity linter / timeout-based regex execution.</p>`,
  },
  {
    id: "a13-rate-limiting-20",
    category: "security",
    categoryLabel: "Security",
    question: `Why is throttling alone insufficient defense against a credential-stuffing attack on POST /auth/login, and what additional NestJS-level controls typically accompany @nestjs/throttler for this specific endpoint?`,
    answerHtml: `<p>A flat per-IP rate limit (e.g. 10 requests/minute) caps a single attacker's speed but doesn't stop a <b>distributed</b> credential-stuffing attack using thousands of proxy IPs, each making only a handful of requests — well under any reasonable per-IP threshold, while the aggregate attack still tries millions of credential pairs against the login endpoint.</p><p>Layered defenses typically added alongside throttling: <b>account-level lockout/backoff</b> keyed on the target username/email rather than source IP (so repeated failures against one account are throttled regardless of source), <b>CAPTCHA or proof-of-work</b> triggered after N failures, structured <b>audit logging</b> of failed attempts for anomaly detection, and comparing failed-login patterns against known breached-credential lists — none of which <code>@nestjs/throttler</code> provides on its own since it only tracks request volume, not semantic failure outcomes.</p>`,
  },
  {
    id: "a13-rate-limiting-21",
    category: "security",
    categoryLabel: "Security",
    question: `When configuring helmet's crossOriginResourcePolicy and crossOriginEmbedderPolicy in a NestJS API that serves images/files to be embedded in a separate frontend's <img> or <canvas> elements, what breaks if you leave the defaults as same-origin?`,
    answerHtml: `<p><code>Cross-Origin-Resource-Policy: same-origin</code> (helmet's default) tells browsers to <b>block cross-origin loading</b> of that resource entirely at the network level — stricter than CORS, and enforced even for non-CORS requests like a plain <code>&lt;img src&gt;</code>. If your frontend is on a different origin from the API serving the images, those images will fail to load (broken image icons) even though there's no explicit CORS error, because COR-Policy and CORS are separate, both-must-pass mechanisms.</p><p>The fix is setting <code>crossOriginResourcePolicy: { policy: 'cross-origin' }</code> (or <code>'same-site'</code> if both are under one registrable domain) specifically for the routes/static-file middleware serving those assets, while leaving stricter defaults for everything else — not disabling the header globally, which would remove a protection against other sites hot-linking or side-channel-reading your resources.</p>`,
  },
  {
    id: "a13-rate-limiting-22",
    category: "security",
    categoryLabel: "Security",
    question: `In NestJS, what does the @nestjs/throttler ThrottlerGuard do differently when applied as a global guard (APP_GUARD provider) versus per-controller with @UseGuards(ThrottlerGuard), and why does the global form need ConfigModule/reflector wiring to allow exceptions?`,
    answerHtml: `<p>Registering <code>ThrottlerGuard</code> via <code>{ provide: APP_GUARD, useClass: ThrottlerGuard }</code> in a module's providers applies it to <b>every route in the application</b> automatically, including ones added later by other teams/modules — nobody can forget to decorate a new controller with <code>@UseGuards()</code>. Per-controller <code>@UseGuards(ThrottlerGuard)</code> only protects that controller, and a developer can simply forget to add it to a new one.</p><p>Because the global form covers everything by default, you need an opt-out mechanism for routes that legitimately shouldn't be throttled (health checks, internal webhooks) — that's what <code>@SkipThrottle()</code> is for, and it works via Nest's <code>Reflector</code> reading metadata the decorator attaches, which the guard checks before applying limits. Without wiring the guard through DI as <code>APP_GUARD</code>, this metadata-based override system has nothing to attach to consistently across the app.</p>`,
  },
];

export const ADVANCED13_QUIZ: QuizQuestion[] = [
  {
    id: "a13q-rate-limiting-1",
    category: "security",
    categoryLabel: "Security",
    question: `A NestJS service runs as 4 replicas behind a load balancer with @nestjs/throttler configured using the default in-memory storage and a limit of 100 requests/minute per IP. What is the actual effective limit a single client can achieve in steady state?`,
    options: [`Exactly 100 requests/minute, enforced correctly across all replicas`, `Up to roughly 400 requests/minute, since each replica tracks its own independent counter`, `0 requests/minute, because in-memory storage crashes under multiple replicas`, `100 requests/minute per replica pair, since Node clusters share memory automatically`],
    answer: 1,
    explanationHtml: `<p>The default <code>ThrottlerStorageService</code> keeps counts in a process-local <code>Map</code>. With 4 independent replicas and no shared store, a client routed round-robin across all 4 can get up to ~100 requests per replica before any single one blocks it — close to 4x the intended limit. A shared Redis-backed storage (e.g. <code>@nest-lab/throttler-storage-redis</code>) is required for correct cross-instance enforcement.</p>`,
  },
  {
    id: "a13q-rate-limiting-2",
    category: "security",
    categoryLabel: "Security",
    question: `Which CORS configuration allows a browser to send an authenticated (cookie-bearing) cross-origin request from any website to your NestJS API and have the response be readable by that website's JavaScript?`,
    options: [`app.enableCors({ origin: '*', credentials: true })`, `app.enableCors({ origin: '*' }) with no credentials option`, `app.enableCors({ origin: true, credentials: true })`, `app.enableCors({ origin: ['https://example.com'], credentials: true })`],
    answer: 2,
    explanationHtml: `<p><code>origin: '*'</code> combined with <code>credentials: true</code> is rejected by the CORS spec itself — browsers refuse to expose credentialed responses when the allow-origin header is a literal wildcard. <code>origin: true</code> makes the middleware reflect whatever Origin the request sent as the allowed origin, which passes the spec's check and, combined with <code>credentials: true</code>, lets any site make authenticated requests using the visitor's cookies — an effective CSRF-enabling misconfiguration. An explicit allowlist (option 4) is the safe pattern.</p>`,
  },
  {
    id: "a13q-rate-limiting-3",
    category: "security",
    categoryLabel: "Security",
    question: `A login endpoint using Mongoose receives a JSON body where the password field is { "$ne": null } instead of a string, and the handler passes req.body directly into User.findOne(). What is the most direct, idiomatic NestJS fix?`,
    options: [`Add a global helmet() middleware to block the request`, `Apply ValidationPipe with a DTO that types password as @IsString(), so an object value is rejected before it reaches the query`, `Lower the @nestjs/throttler limit on the login route`, `Switch the database from MongoDB to PostgreSQL`],
    answer: 1,
    explanationHtml: `<p>This is a NoSQL injection via MongoDB operator smuggling. The fix is type enforcement at the validation layer: a DTO with <code>@IsString()</code> on <code>password</code>, validated by <code>ValidationPipe</code>, rejects a request where <code>password</code> is an object (containing <code>$ne</code>) with a 400 before it ever reaches <code>findOne()</code>. Helmet (security headers) and throttling (rate limits) don't address a type-confusion query injection at all.</p>`,
  },
  {
    id: "a13q-rate-limiting-4",
    category: "security",
    categoryLabel: "Security",
    question: `Comparing NestJS's three built-in API versioning strategies, which statement correctly identifies a caching tradeoff?`,
    options: [`Header versioning is naturally cache-friendly for CDNs because version is part of the cache key by default`, `URI versioning is naturally cache-friendly because the version is embedded in the path, which standard URL-based caches already key on`, `Media-type versioning eliminates all caching concerns because it relies on the Accept header exclusively`, `All three versioning strategies behave identically with respect to HTTP caching`],
    answer: 1,
    explanationHtml: `<p>URI versioning (<code>/v1/users</code> vs <code>/v2/users</code>) puts the version in the path, which is exactly what standard URL-keyed caches (browser cache, most CDNs by default) already use to differentiate cached responses — no extra configuration needed. Header-based and media-type versioning put the differentiator in a header, which caches ignore by default unless you explicitly add a <code>Vary</code> header for it, or the wrong version gets served from cache.</p>`,
  },
  {
    id: "a13q-rate-limiting-5",
    category: "security",
    categoryLabel: "Security",
    question: `Why is forbidNonWhitelisted: true in NestJS's ValidationPipe considered a stronger security control than whitelist: true alone, for an endpoint vulnerable to mass assignment?`,
    options: [`forbidNonWhitelisted is faster at runtime because it skips validation entirely`, `whitelist alone silently strips unknown properties, while forbidNonWhitelisted rejects the entire request, surfacing and logging the attempted injection of unexpected fields`, `forbidNonWhitelisted automatically encrypts all request bodies`, `There is no functional difference; forbidNonWhitelisted is purely cosmetic for error messages`],
    answer: 1,
    explanationHtml: `<p><code>whitelist: true</code> quietly drops any property not declared on the DTO — the request still succeeds, so an attempt to smuggle in e.g. <code>role: 'admin'</code> goes unnoticed. <code>forbidNonWhitelisted: true</code> makes the same situation throw a 400 error instead, which both blocks the attempt outright and creates a signal (an error response, typically logged) that something tried to send unexpected fields — useful for detecting probing/attack attempts, not just sanitizing them away.</p>`,
  },
  {
    id: "a13q-rate-limiting-6",
    category: "security",
    categoryLabel: "Security",
    question: `An API serves images from a different origin than the frontend that embeds them via <img src>. After adding helmet() with its defaults, the images stop loading even though there's no CORS error in the console. What is the most likely cause?`,
    options: [`The ThrottlerGuard is blocking image requests due to rate limiting`, `Helmet's default Cross-Origin-Resource-Policy: same-origin header blocks cross-origin loading of the resource at the network level, independent of CORS`, `The images were deleted when helmet() was added`, `CSP's default-src directive always blocks all images regardless of configuration`],
    answer: 1,
    explanationHtml: `<p>Helmet's default <code>Cross-Origin-Resource-Policy: same-origin</code> header is a separate browser enforcement mechanism from CORS — it blocks cross-origin loads of the resource (even simple <code>&lt;img&gt;</code> tags, which don't trigger CORS preflight checks at all) at the network layer. The fix is explicitly setting <code>crossOriginResourcePolicy: { policy: 'cross-origin' }</code> for the routes serving those assets, not a CORS or throttling change.</p>`,
  },
  {
    id: "a13q-rate-limiting-7",
    category: "security",
    categoryLabel: "Security",
    question: `Which scenario best illustrates why @nestjs/throttler alone is an insufficient defense against credential stuffing on a login endpoint?`,
    options: [`An attacker sends 50 requests/second from a single IP and gets blocked after the configured limit, as expected`, `An attacker distributes login attempts across thousands of proxy IPs, each staying under the per-IP limit, so the aggregate attack proceeds unthrottled`, `The throttler guard throws an unhandled exception under any load, crashing the server`, `@nestjs/throttler only works for GET requests, so POST /auth/login was never covered in the first place`],
    answer: 1,
    explanationHtml: `<p>Per-IP throttling caps a single source's request rate but does nothing against a distributed attack where each of thousands of IPs sends only a few requests — individually under any reasonable threshold, while the aggregate volume of credential guesses against the login endpoint remains effectively unthrottled. Defending against this requires account-level lockout/backoff, CAPTCHA after repeated failures, and anomaly-detection logging in addition to IP-based rate limiting.</p>`,
  },
];
