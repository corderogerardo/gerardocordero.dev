// Batch 2 — Custom param decorators (createParamDecorator), Reflector & SetMetadata for custom metadata, combining decorators (applyDecorators), and dynamic modules (forRoot/forRootAsync patterns).
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED2_FLASHCARDS: Flashcard[] = [
  {
    id: "a2-decorators-metadata-1",
    category: "core",
    categoryLabel: "Core",
    question: `When you write \`createParamDecorator((data, ctx) => ...)\`, what is the second argument actually carrying, and how do you get the underlying Express/Fastify request out of it?`,
    answerHtml: `<p>The second argument is an <code>ExecutionContext</code>, the same object interceptors and guards receive. It's deliberately generic across transports (HTTP, RPC, WS), so to reach the raw request you must narrow it first: <code>ctx.switchToHttp().getRequest()</code>. Calling <code>getRequest()</code> directly on the context (without <code>switchToHttp()</code>) doesn't exist on the base type — the host wrapper is what exposes the transport-specific accessor.</p>`,
  },
  {
    id: "a2-decorators-metadata-2",
    category: "core",
    categoryLabel: "Core",
    question: `Why does \`createParamDecorator\` need a separate factory function instead of just letting you write a normal decorator that reads \`req.user\`?`,
    answerHtml: `<p>Custom param decorators run inside Nest's pipeline so the returned value can be <b>piped through validation/transformation pipes</b> like any other route parameter, and so the framework can resolve the correct <code>ExecutionContext</code> per-request regardless of transport. A hand-rolled decorator applied to a method parameter has no hook into Nest's argument-resolution metadata — Nest wouldn't know to call it at request time or where to inject its result.</p>`,
  },
  {
    id: "a2-decorators-metadata-3",
    category: "core",
    categoryLabel: "Core",
    question: `You write \`createParamDecorator((data: string, ctx) => ...)\` and use it as \`@CurrentUser('email')\`. What determines what \`data\` is bound to, and what happens if you call \`@CurrentUser()\` with no argument?`,
    answerHtml: `<p><code>data</code> is bound to whatever is passed inside the decorator's parentheses at the call site — it's just the decorator's own parameter, not framework-injected. If called with no argument, <code>data</code> is <code>undefined</code>, so the factory must guard for that (e.g. return the whole user object when <code>data</code> is falsy, or a specific property when it's a string key).</p>`,
  },
  {
    id: "a2-decorators-metadata-4",
    category: "core",
    categoryLabel: "Core",
    question: `Can you compose a custom param decorator out of several built-in ones (e.g. combine logic from \`@Headers()\` and a manual context read) in a single reusable decorator?`,
    answerHtml: `<p>Yes — <code>createParamDecorator</code>'s factory function has full access to <code>ExecutionContext</code>, so you can manually replicate what built-ins do (read headers, query, params) inside one factory. What you <i>cannot</i> do is literally nest other param decorators inside it, since those are designed to be applied directly to a method parameter, not invoked as plain functions. Instead you re-implement the extraction logic against the raw request inside your factory.</p>`,
  },
  {
    id: "a2-decorators-metadata-5",
    category: "core",
    categoryLabel: "Core",
    question: `What's the practical difference between throwing inside a custom param decorator factory versus relying on a pipe attached to that same parameter?`,
    answerHtml: `<p>Both execute before the route handler body runs, but a throw inside the factory happens during <b>argument resolution</b>, before any pipes attached to that parameter run; pipes execute after the param decorator's raw value is produced. Practically: validation/transformation concerns belong in a pipe (composable, reusable, testable independently via <code>PipeTransform</code>), while the decorator factory should stay focused on <i>extracting</i> the value from the context, not validating it — mixing the two makes the decorator hard to reuse with different validation needs.</p>`,
  },
  {
    id: "a2-decorators-metadata-6",
    category: "core",
    categoryLabel: "Core",
    question: `What does \`Reflector.get()\` actually do under the hood, and why does it need both a metadata key and a target (class or handler)?`,
    answerHtml: `<p><code>Reflector</code> is a thin wrapper around Node's <code>reflect-metadata</code> polyfill. <code>SetMetadata(key, value)</code> calls <code>Reflect.defineMetadata(key, value, target)</code> where the target is the class or method the decorator is applied to. <code>reflector.get(key, target)</code> then calls <code>Reflect.getMetadata</code> against that same target. Both key and target are required because metadata is stored in a map keyed by <i>target object</i> internally — there's no global metadata store, so you must point at the exact same object reference the decorator attached metadata to.</p>`,
  },
  {
    id: "a2-decorators-metadata-7",
    category: "core",
    categoryLabel: "Core",
    question: `Why does \`Reflector.getAllAndOverride()\` exist instead of just calling \`reflector.get()\` twice (once for the handler, once for the class) and merging manually?`,
    answerHtml: `<p><code>getAllAndOverride(key, [handlerTarget, classTarget])</code> encodes the common <b>"method-level metadata wins over class-level"</b> override pattern in one call: it checks each target in order and returns the first non-undefined result. Writing this manually with two <code>.get()</code> calls is fine functionally, but <code>getAllAndOverride</code> is the idiomatic, less error-prone form used throughout Nest's own guards (e.g. <code>JwtAuthGuard</code> checking <code>IS_PUBLIC_KEY</code>), and it short-circuits instead of always evaluating both.</p>`,
  },
  {
    id: "a2-decorators-metadata-8",
    category: "core",
    categoryLabel: "Core",
    question: `What's the difference between \`Reflector.getAllAndOverride()\` and \`Reflector.getAllAndMerge()\`, and when would \`getAllAndMerge\` actually matter?`,
    answerHtml: `<p><code>getAllAndOverride</code> returns the <b>first defined value</b> found across targets (method overrides class). <code>getAllAndMerge</code> instead <b>combines</b> all values — for arrays it concatenates them, for objects it shallow-merges them. It matters when metadata is additive rather than exclusive, e.g. roles set at both class level (<code>@Roles('user')</code>) and method level (<code>@Roles('admin')</code>) where you want a guard to see <code>['user', 'admin']</code> combined rather than only the method-level array.</p>`,
  },
  {
    id: "a2-decorators-metadata-9",
    category: "core",
    categoryLabel: "Core",
    question: `A guard reads custom metadata via \`this.reflector.get(ROLES_KEY, context.getHandler())\` but it always returns \`undefined\` even though \`@Roles('admin')\` is clearly on the controller class. What's wrong?`,
    answerHtml: `<p><code>context.getHandler()</code> returns the route <b>method</b>, not the controller class. If <code>@Roles()</code> was applied at the class level, the metadata lives on the class constructor, retrievable only via <code>context.getClass()</code>. The guard needs to check both — which is exactly why <code>getAllAndOverride(ROLES_KEY, [context.getHandler(), context.getClass()])</code> is the standard pattern, rather than a single <code>.get()</code> call against just one target.</p>`,
  },
  {
    id: "a2-decorators-metadata-10",
    category: "core",
    categoryLabel: "Core",
    question: `Why is \`SetMetadata\` rarely used directly in production code — what's the idiomatic alternative, and why is it preferred?`,
    answerHtml: `<p>Calling <code>@SetMetadata('roles', ['admin'])</code> directly scatters magic string keys across the codebase with no compile-time safety. The idiomatic pattern is to wrap it in a typed factory decorator: <code>export const Roles = (...roles: string[]) => SetMetadata(ROLES_KEY, roles)</code>. This centralizes the key as a constant, gives the decorator a typed, autocompletable call signature (<code>@Roles('admin')</code> vs raw string arrays), and keeps the guard's lookup key in one place rather than duplicated as a string literal at every call site.</p>`,
  },
  {
    id: "a2-decorators-metadata-11",
    category: "core",
    categoryLabel: "Core",
    question: `What problem does \`applyDecorators()\` solve, and what does it NOT do that you might assume it does?`,
    answerHtml: `<p><code>applyDecorators(...decorators)</code> merges several decorators into one reusable composite — e.g. combining <code>UseGuards(AuthGuard)</code>, <code>SetMetadata(...)</code>, and <code>ApiBearerAuth()</code> into a single <code>@Auth()</code> decorator. It does NOT change semantics in the sense of inventing new behavior — it just calls each decorator function against the target, exactly as if you'd applied them yourself. It's purely a DRY/readability tool, not a different composition mechanism; the underlying <code>Reflect.defineMetadata</code>/decorator calls happen the same way (though, see the next card, the *order* it applies them in is not the same as writing them stacked).</p>`,
  },
  {
    id: "a2-decorators-metadata-12",
    category: "core",
    categoryLabel: "Core",
    question: `Inside \`applyDecorators(Dec1, Dec2, Dec3)\`, in what order do the individual decorators actually execute relative to writing them stacked manually as \`@Dec1() @Dec2() @Dec3()\`?`,
    answerHtml: `<p>They are NOT equivalent. <code>applyDecorators</code>'s implementation is a simple <code>for (const decorator of decorators) decorator(target, ...)</code> loop, so <code>applyDecorators(Dec1, Dec2, Dec3)</code> applies <b>Dec1, then Dec2, then Dec3</b> — left-to-right array order. Stacked syntax is the opposite: per JS/TS decorator evaluation rules, decorators stacked top-to-bottom are *applied* bottom-to-top, so <code>@Dec1() @Dec2() @Dec3()</code> applies <b>Dec3, then Dec2, then Dec1</b>. So <code>applyDecorators(Dec1, Dec2, Dec3)</code> actually matches the stacked form written in <i>reverse</i> — <code>@Dec3() @Dec2() @Dec1()</code> — not the form it visually resembles. This only matters when decorators have order-dependent side effects (rare for metadata-only decorators, but real for things that wrap/replace the descriptor).</p>`,
  },
  {
    id: "a2-decorators-metadata-13",
    category: "core",
    categoryLabel: "Core",
    question: `You're building a custom \`@ApiPaginatedResponse()\` decorator for Swagger that needs to combine \`ApiQuery\` calls for \`page\` and \`limit\` plus an \`ApiOkResponse\`. Why reach for \`applyDecorators\` instead of just exporting an array of decorators and spreading them?`,
    answerHtml: `<p>TypeScript decorator syntax doesn't support spreading an array of decorators (<code>@[...decorators]</code> isn't valid). <code>applyDecorators</code> exists specifically to turn a decorator <i>array</i> into a single callable decorator <i>function</i> that TypeScript's decorator position accepts. Without it you'd have to manually stack each decorator at every call site, defeating the reuse goal.</p>`,
  },
  {
    id: "a2-decorators-metadata-14",
    category: "core",
    categoryLabel: "Core",
    question: `What is the core problem a dynamic module (\`forRoot\`/\`forRootAsync\`) solves that a plain static \`@Module({...})\` cannot?`,
    answerHtml: `<p>A static module's metadata (providers, imports, exports) is fixed at class-definition time via the decorator — it can't take runtime configuration. A dynamic module is a static method that <i>returns</i> a <code>DynamicModule</code> object built from arguments passed by the consumer (e.g. a connection string, a list of entities), letting the same module class produce differently-configured provider sets per import. It's the mechanism behind every configurable library module (\`TypeOrmModule.forRoot()\`, \`ConfigModule.forRoot()\`, etc).</p>`,
  },
  {
    id: "a2-decorators-metadata-15",
    category: "core",
    categoryLabel: "Core",
    question: `What exact shape must the object returned from a \`forRoot()\` static method have, and which field is easy to forget that silently breaks DI for consumers?`,
    answerHtml: `<p>It must satisfy the <code>DynamicModule</code> interface: <code>{ module: SomeModule, providers?, imports?, controllers?, exports?, global? }</code>. The <code>module</code> field (a reference back to the module class itself) is mandatory and easy to typo or omit if you copy-paste from a different module. The other commonly-forgotten field is <code>exports</code> — if the dynamically-registered provider isn't listed there, importing modules can't inject it even though the provider was correctly added to <code>providers</code>.</p>`,
  },
  {
    id: "a2-decorators-metadata-16",
    category: "core",
    categoryLabel: "Core",
    question: `Why does \`forRootAsync()\` typically accept \`useFactory\` + \`inject\` rather than just taking an async function directly as its single argument?`,
    answerHtml: `<p><code>useFactory</code> mirrors the standard custom-provider factory shape so the config-building function can itself receive <b>dependency-injected services</b> (e.g. a <code>ConfigService</code> already resolved by Nest) via the <code>inject</code> array — Nest resolves those tokens and passes them as positional arguments to <code>useFactory</code>. If you instead took a bare async function, it would have no way to receive injected dependencies; you'd be forced to manually instantiate or import things outside the DI container, breaking testability and module encapsulation.</p>`,
  },
  {
    id: "a2-decorators-metadata-17",
    category: "core",
    categoryLabel: "Core",
    question: `In a \`forRootAsync()\` implementation, why is the options provider (e.g. \`{ provide: MODULE_OPTIONS_TOKEN, useFactory: ... }\`) almost always defined as a *separate* provider from the thing that consumes it, rather than inlining the factory logic into the final service's own provider?`,
    answerHtml: `<p>Separating them lets the options resolution be <b>injected</b> (via <code>@Inject(MODULE_OPTIONS_TOKEN)</code>) into any number of other providers in the module — a connection provider, a logger, a guard — without duplicating the async config-resolution logic in each. It also keeps the public API stable: consumers configure via <code>forRootAsync</code> once, and every internal provider in the dynamic module just injects the resolved options token, decoupling "how options are obtained" from "who needs them."</p>`,
  },
  {
    id: "a2-decorators-metadata-18",
    category: "core",
    categoryLabel: "Core",
    question: `If a dynamic module's \`forRoot()\` is called from two different feature modules with different configuration objects, do they get two independent sets of providers, or does Nest dedupe by module class and keep only one?`,
    answerHtml: `<p>Each call to <code>forRoot(options)</code> returns a fresh <code>DynamicModule</code> object describing its own provider set — Nest does not dedupe dynamic module registrations by class identity the way it might cache a plain static module reference. However, if the dynamic providers use <b>fixed injection tokens</b> (not scoped per-call), importing the module twice with different options in the same DI subtree can cause provider collisions/overwrites — this is why a \`forRoot\` intended for single global registration is typically paired with \`global: true\` and documented as call-once, while per-feature configuration usually uses \`forFeature()\` with module-scoped tokens instead.</p>`,
  },
  {
    id: "a2-decorators-metadata-19",
    category: "core",
    categoryLabel: "Core",
    question: `What does setting \`global: true\` on a returned \`DynamicModule\` actually change, and why is it normally reserved for root-level config modules rather than feature modules?`,
    answerHtml: `<p><code>global: true</code> registers the module's exported providers into Nest's global injector scope, making them available to every module in the application <i>without</i> each one needing to explicitly \`imports: [ThatModule]\`. It bypasses Nest's normal module-encapsulation/explicit-import model, which is exactly why it's reserved for things like \`ConfigModule\` that legitimately need app-wide availability — overusing it for feature modules reintroduces the implicit global-coupling problem Nest's module system exists to prevent.</p>`,
  },
  {
    id: "a2-decorators-metadata-20",
    category: "core",
    categoryLabel: "Core",
    question: `How does \`ConfigurableModuleBuilder\` (from \`@nestjs/common\`) relate to writing \`forRoot\`/\`forRootAsync\` by hand — what does it actually generate for you?`,
    answerHtml: `<p><code>ConfigurableModuleBuilder().build()</code> generates a base class with <code>register</code>/<code>registerAsync</code> (or aliased to \`forRoot\`/\`forRootAsync\` via \`.setClassMethodName('forRoot')\`) already implemented, plus the options injection token and an \`OPTIONS_TYPE\`/\`ASYNC_OPTIONS_TYPE\` pair for typing. You extend your module class from that generated base instead of hand-writing the static methods and the \`useFactory\`/\`inject\` plumbing yourself. It removes boilerplate but you still write the providers that consume the resolved options token — it doesn't replace dynamic modules as a concept, it just codegens the repetitive scaffolding around them.</p>`,
  },
  {
    id: "a2-decorators-metadata-21",
    category: "core",
    categoryLabel: "Core",
    question: `Why can't a custom param decorator created with \`createParamDecorator\` be used as a guard or interceptor replacement to short-circuit a request (e.g. reject it before the handler runs)?`,
    answerHtml: `<p>Param decorators run during <b>argument binding</b>, immediately before the handler is invoked, but they're not part of the guard/interceptor pipeline that Nest evaluates for request short-circuiting. Throwing inside the factory does abort the request (the exception filter catches it), but you lose access to the broader lifecycle hooks interceptors get (pre- and post-handler, response transformation) and guards get (running before pipes/interceptors, able to use other guards' results via \`canActivate\` composition). It's the wrong tool for authorization decisions that should block multiple routes consistently — that's what guards plus metadata-driven \`Reflector\` checks are for.</p>`,
  },
  {
    id: "a2-decorators-metadata-22",
    category: "core",
    categoryLabel: "Core",
    question: `When combining \`SetMetadata\` with \`applyDecorators\` to build something like \`@Auth(Role.Admin)\`, why is it important that the metadata-setting decorator and the \`UseGuards\` decorator both be included in the same composite rather than requiring callers to add \`@UseGuards(RolesGuard)\` separately?`,
    answerHtml: `<p>If a caller forgets to also slap on \`@UseGuards(RolesGuard)\`, the \`@Roles()\` metadata is set but nothing ever reads it — the route is silently <b>unprotected</b> despite looking guarded in the source. Bundling both into one \`applyDecorators\` composite makes the metadata and its enforcement mechanism inseparable at the call site, which is a deliberate API-safety design: it's impossible to apply the role restriction without also wiring up the guard that enforces it.</p>`,
  },
  {
    id: "a2-decorators-metadata-23",
    category: "core",
    categoryLabel: "Core",
    question: `Two custom param decorators are applied to the same handler parameter — is that even valid, and if not, what's the actual constraint on combining multiple decorators on one parameter?`,
    answerHtml: `<p>You cannot apply two separate \`createParamDecorator\`-based decorators to the <i>same</i> parameter slot — each parameter position can only resolve to one argument-factory result; stacking two would mean Nest tries to bind two different values to one slot, which isn't supported. What you <i>can</i> do is apply multiple different param decorators to <i>different</i> parameters of the same handler (e.g. \`(@CurrentUser() user, @Pagination() page)\`), or compose the extraction logic of several concerns inside a single factory function if they need to live on the same parameter.</p>`,
  },
];

export const ADVANCED2_QUIZ: QuizQuestion[] = [
  {
    id: "a2q-decorators-metadata-1",
    category: "core",
    categoryLabel: "Core",
    question: `Inside a \`createParamDecorator\` factory, you need the raw Express \`Response\` object to set a cookie manually. What's the correct way to get it from \`ExecutionContext\`?`,
    options: [`ctx.getResponse()`, `ctx.switchToHttp().getResponse()`, `ctx.getArgByIndex(1)`, `ctx.switchToRpc().getResponse()`],
    answer: 1,
    explanationHtml: `<p><code>ExecutionContext</code> is transport-agnostic, so you must first narrow it with <code>switchToHttp()</code> before transport-specific accessors like <code>getResponse()</code> (or <code>getRequest()</code>) become available. <code>getArgByIndex</code> exists but bypasses the typed host wrapper and is discouraged for HTTP code.</p>`,
  },
  {
    id: "a2q-decorators-metadata-2",
    category: "core",
    categoryLabel: "Core",
    question: `A guard needs to check for \`@Roles()\` metadata that may be set on either the handler method or the controller class, with method-level taking precedence. Which \`Reflector\` call is the idiomatic implementation?`,
    options: [`this.reflector.get(ROLES_KEY, context.getClass())`, `this.reflector.getAll(ROLES_KEY, [context.getHandler(), context.getClass()])`, `this.reflector.getAllAndOverride(ROLES_KEY, [context.getHandler(), context.getClass()])`, `this.reflector.getAllAndMerge(ROLES_KEY, [context.getClass(), context.getHandler()])`],
    answer: 2,
    explanationHtml: `<p><code>getAllAndOverride</code> returns the first defined value scanning targets in the given order, implementing exactly the "method overrides class" precedence rule. <code>getAllAndMerge</code> would combine both instead of letting one win, and a single <code>.get()</code> call only checks one target.</p>`,
  },
  {
    id: "a2q-decorators-metadata-3",
    category: "core",
    categoryLabel: "Core",
    question: `You write \`applyDecorators(SetMetadata('roles', roles), UseGuards(RolesGuard))\` for a composite \`@Auth()\` decorator. What does this actually produce, and how does its execution order compare to writing \`@SetMetadata('roles', roles) @UseGuards(RolesGuard)\` stacked?`,
    options: [`A single decorator function that applies SetMetadata then UseGuards, in that order — the SAME order as the stacked form, since decorators apply top-to-bottom`, `A single decorator function that applies SetMetadata then UseGuards (array order) — the OPPOSITE of the stacked form, since stacked decorators apply bottom-to-top`, `A new RolesGuard subclass with the metadata baked into its constructor`, `A factory provider token usable in forRootAsync's inject array`],
    answer: 1,
    explanationHtml: `<p><code>applyDecorators</code>'s implementation loops through its arguments left-to-right and applies each in turn, so <code>applyDecorators(A, B)</code> applies A then B. But JS/TS decorator stacking applies bottom-to-top, so the visually-similar stacked form <code>@A() @B()</code> applies B then A — the reverse. For order-independent decorators like these (pure metadata + guard registration) it doesn't change behavior, but the order claim itself is the opposite of what stacking would do.</p>`,
  },
  {
    id: "a2q-decorators-metadata-4",
    category: "core",
    categoryLabel: "Core",
    question: `A \`DynamicModule\` object returned from \`forRoot()\` defines a provider but the team reports that other modules importing it via \`imports: [MyModule.forRoot(opts)]\` can't inject that provider. What's the most likely missing piece?`,
    options: [`The provider needs \`scope: Scope.DEFAULT\` explicitly set`, `The DynamicModule object is missing the provider's token in its \`exports\` array`, `forRoot() must be marked \`async\` even if it does no async work`, `Dynamic modules can never export providers, only static modules can`],
    answer: 1,
    explanationHtml: `<p>Adding a provider to \`providers\` only makes it injectable <i>within</i> that module. To let importing modules inject it, the token must also appear in \`exports\`. This is a common omission distinct from the (also required) \`module\` field.</p>`,
  },
  {
    id: "a2q-decorators-metadata-5",
    category: "core",
    categoryLabel: "Core",
    question: `Why does \`forRootAsync()\` typically use a \`useFactory\` + \`inject\` pattern rather than accepting a plain \`Promise<Options>\` directly as an argument?`,
    options: [`Promises can't be serialized across the DI container boundary`, `useFactory lets the options-building function receive other DI-resolved providers (like ConfigService) as arguments, which a bare Promise value couldn't express`, `NestJS forbids passing Promises into static module methods at the type level`, `It's purely a stylistic convention with no functional difference from accepting a Promise`],
    answer: 1,
    explanationHtml: `<p>\`useFactory\` plus \`inject\` mirrors Nest's standard custom-provider pattern, letting Nest resolve dependencies from the container and pass them into the factory function. A raw Promise argument would have to be constructed outside the DI system, losing access to other injectable services needed to build the config.</p>`,
  },
  {
    id: "a2q-decorators-metadata-6",
    category: "core",
    categoryLabel: "Core",
    question: `Inside a custom param decorator, you write \`@CurrentUser('email')\` to get only the user's email, and \`@CurrentUser()\` to get the full user object. What determines the value of the \`data\` parameter in the factory function?`,
    options: [`Nest infers it from the TypeScript type annotation on the decorated parameter`, `It's whatever value the caller passes as the decorator's own argument at the call site — undefined if omitted`, `It's automatically set to the route's path parameter with the same name`, `It's resolved by running the previous pipe in the chain and passing its output`],
    answer: 1,
    explanationHtml: `<p>The factory's first argument is simply whatever was passed when the decorator itself was invoked, e.g. \`'email'\` in \`@CurrentUser('email')\`, or \`undefined\` if called as \`@CurrentUser()\`. It has nothing to do with type inference, route params, or pipes — those are separate mechanisms.</p>`,
  },
  {
    id: "a2q-decorators-metadata-7",
    category: "core",
    categoryLabel: "Core",
    question: `Setting \`global: true\` on a DynamicModule's return value does what, precisely?`,
    options: [`Makes the module a singleton across the whole Node process, even across multiple Nest applications`, `Registers the module's exports into Nest's global injector scope so other modules can inject them without explicitly importing the module`, `Forces all providers in the module to use Scope.DEFAULT instead of REQUEST scope`, `Automatically calls forRoot() with default options if no consumer imports the module explicitly`],
    answer: 1,
    explanationHtml: `<p>\`global: true\` opts the module's exported providers into app-wide availability, bypassing the need for every consuming module to list it under \`imports\`. It doesn't affect provider scope, cross-process singleton behavior, or auto-invocation — those are unrelated concerns.</p>`,
  },
];
