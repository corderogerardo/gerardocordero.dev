// Batch 15 — Swagger/OpenAPI documentation: @nestjs/swagger decorators (@ApiProperty, @ApiResponse, @ApiTags, @ApiOperation), generating docs from DTOs vs interfaces, securing the docs endpoint, and versioned API documentation.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED15_FLASHCARDS: Flashcard[] = [
  {
    id: "a15-swagger-docs-1",
    category: "core",
    categoryLabel: "Core",
    question: `What package provides Swagger/OpenAPI support in NestJS, and what is the minimal bootstrap to expose docs?`,
    answerHtml: `<p><code>@nestjs/swagger</code>. In <code>main.ts</code>, build a <code>DocumentBuilder</code> config, call <code>SwaggerModule.createDocument(app, config)</code> to introspect the running app and produce an OpenAPI document object, then <code>SwaggerModule.setup('docs', app, document)</code> to mount Swagger UI (and the raw JSON at <code>/docs-json</code>).</p><p>It walks the Nest module tree at bootstrap time reading controller and decorator metadata to build the spec; it does not parse your TypeScript source files directly.</p>`,
  },
  {
    id: "a15-swagger-docs-2",
    category: "core",
    categoryLabel: "Core",
    question: `Why does @nestjs/swagger need decorators like @ApiProperty() if the DTO already has TypeScript types?`,
    answerHtml: `<p>TypeScript types are erased at compile time, so at runtime there is no type information left for reflection unless <code>emitDecoratorMetadata</code> captured it via <code>reflect-metadata</code>. That captured metadata only gives the bare design type (e.g. <code>String</code>, <code>Number</code>, a class reference) — it has no notion of OpenAPI-specific concerns like example values, format, enum members, or descriptions.</p><p><code>@ApiProperty()</code> supplies that extra layer of documentation metadata the type system cannot express on its own.</p>`,
  },
  {
    id: "a15-swagger-docs-3",
    category: "core",
    categoryLabel: "Core",
    question: `What does the @nestjs/swagger CLI plugin do, and why enable it on a real project?`,
    answerHtml: `<p>It is a TypeScript transformer registered in <code>nest-cli.json</code> (<code>compilerOptions.plugins: ["@nestjs/swagger"]</code>) that statically analyzes DTO and entity classes at build time and auto-generates <code>@ApiProperty()</code> metadata for properties that lack explicit decorators — inferring the type, optionality from <code>?</code>, and default values.</p><p>It removes the need to hand-annotate every field with <code>@ApiProperty()</code> for the common case, while explicit decorators still win when present. It can also pull a property's leading comment in as its description, but only if you opt in: that requires explicitly setting <code>"introspectComments": true</code> in the plugin's options in <code>nest-cli.json</code> — it is <code>false</code> by default.</p>`,
  },
  {
    id: "a15-swagger-docs-4",
    category: "core",
    categoryLabel: "Core",
    question: `What is the practical difference between generating Swagger docs from a class-based DTO versus a TypeScript interface?`,
    answerHtml: `<p>Interfaces are purely a compile-time construct — they vanish entirely from emitted JavaScript, so <code>@nestjs/swagger</code>'s reflection-based introspection has nothing to read at runtime: no properties, no decorators, nothing. A class survives compilation as a real constructor function, so its properties and any <code>@ApiProperty()</code> decorators are present in the running process for <code>SwaggerModule</code> to inspect.</p><p>This mirrors why <code>ValidationPipe</code> requires DTO classes too — both class-validator and the Swagger document builder depend on the same runtime artifact that only a class provides.</p>`,
  },
  {
    id: "a15-swagger-docs-5",
    category: "core",
    categoryLabel: "Core",
    question: `What does @ApiProperty() control beyond marking a field as documented, and what happens if you omit it on a required field?`,
    answerHtml: `<p><code>@ApiProperty()</code> accepts options like <code>type</code>, <code>example</code>, <code>description</code>, <code>enum</code>, <code>format</code>, <code>minimum</code>/<code>maximum</code>, <code>isArray</code>, and <code>required</code> (defaults to <code>true</code>). Omitting it entirely on a property means that property is invisible in the generated schema unless the CLI plugin's static analysis fills it in automatically.</p><p>For genuinely optional fields, pair the TypeScript <code>?</code> with <code>@ApiPropertyOptional()</code> (sugar for <code>@ApiProperty({ required: false })</code>) so the generated schema's <code>required</code> array matches reality.</p>`,
  },
  {
    id: "a15-swagger-docs-6",
    category: "core",
    categoryLabel: "Core",
    question: `How do @ApiTags() and @ApiOperation() differ in what they document, and where do you place each?`,
    answerHtml: `<p><code>@ApiTags('users')</code> is applied at the controller (class) level and groups every route in that controller under a named section in the Swagger UI sidebar — purely organizational, no effect on the schema of any individual operation.</p><p><code>@ApiOperation({ summary, description })</code> is applied per route handler (method level) and documents that single endpoint's intent — it's the human-readable summary shown next to the HTTP verb/path, distinct from response shape or parameters.</p>`,
  },
  {
    id: "a15-swagger-docs-7",
    category: "core",
    categoryLabel: "Core",
    question: `What's the difference between @ApiResponse() and the more specific decorators like @ApiOkResponse() / @ApiCreatedResponse() / @ApiNotFoundResponse()?`,
    answerHtml: `<p><code>@ApiResponse({ status, description, type })</code> is the generic, fully configurable form — you specify the HTTP status code explicitly. The status-specific decorators (<code>@ApiOkResponse</code>, <code>@ApiCreatedResponse</code>, <code>@ApiNotFoundResponse</code>, <code>@ApiBadRequestResponse</code>, etc.) are thin wrappers that hardcode the status code and just ask for <code>description</code>/<code>type</code>, trading flexibility for readability and less chance of a typo'd status code.</p><p>Both ultimately populate the same <code>responses</code> object in the generated OpenAPI document for that operation.</p>`,
  },
  {
    id: "a15-swagger-docs-8",
    category: "core",
    categoryLabel: "Core",
    question: `A controller method returns a paginated array of UserDto. How do you correctly document that shape with @ApiResponse()?`,
    answerHtml: `<p>Use the <code>isArray</code> option alongside <code>type</code>: <code>@ApiResponse({ status: 200, type: UserDto, isArray: true })</code>. Passing just <code>type: [UserDto]</code> as an array literal also works in <code>@nestjs/swagger</code> and produces the same <code>items: { $ref: ... }</code> array schema.</p><p>For a custom pagination envelope (e.g. <code>{ data: UserDto[], total: number, page: number }</code>), you typically need a dedicated wrapper DTO decorated with <code>@ApiProperty({ type: [UserDto] })</code> on its <code>data</code> field, since Swagger can't infer generic wrapper shapes automatically.</p>`,
  },
  {
    id: "a15-swagger-docs-9",
    category: "core",
    categoryLabel: "Core",
    question: `What's the catch with documenting generic response wrappers (e.g. ApiResponse<T>) in NestJS Swagger, and what's the standard workaround?`,
    answerHtml: `<p>TypeScript generics are also erased at compile time, so a generic class like <code>class ApiResponseDto<T> { data: T }</code> has no way to know what <code>T</code> is at runtime for Swagger to introspect — the schema would show <code>data</code> as an empty object.</p><p>The standard workaround is a factory function that returns a concrete decorated class per call site, commonly named <code>ApiResponseOf(UserDto)</code> or similar, using <code>applyDecorators</code> and <code>getSchemaPath</code>/<code>extraModels</code> to register the resolved generic shape with the document builder for that specific usage.</p>`,
  },
  {
    id: "a15-swagger-docs-10",
    category: "core",
    categoryLabel: "Core",
    question: `How do you document a path parameter, a query parameter, and a request header separately in @nestjs/swagger?`,
    answerHtml: `<p><code>@ApiParam({ name: 'id', type: String, description })</code> for route path segments (<code>:id</code>), <code>@ApiQuery({ name: 'page', required: false, type: Number })</code> for querystring parameters, and <code>@ApiHeader({ name: 'x-api-key', description })</code> for expected request headers. Each maps to its respective <code>in: path | query | header</code> entry in the OpenAPI <code>parameters</code> array for that operation.</p><p>None of these are inferred automatically from the route signature — Nest's runtime routing doesn't carry rich enough metadata, so each must be explicitly declared if you want it documented.</p>`,
  },
  {
    id: "a15-swagger-docs-11",
    category: "core",
    categoryLabel: "Core",
    question: `What does @ApiBody() do, and when is it necessary versus redundant?`,
    answerHtml: `<p><code>@ApiBody({ type: CreateUserDto })</code> explicitly documents the request body schema for an operation. It's usually redundant for a simple <code>@Body() dto: CreateUserDto</code> parameter because <code>@nestjs/swagger</code> infers the body schema from that parameter's type automatically (especially with the CLI plugin enabled).</p><p>It becomes necessary when the body type can't be inferred — e.g. <code>@Body() body: any</code>, a union of possible DTOs, file-upload multipart bodies, or when you want to override the inferred schema with a different example/description.</p>`,
  },
  {
    id: "a15-swagger-docs-12",
    category: "core",
    categoryLabel: "Core",
    question: `How does @nestjs/swagger represent a DTO field that's a nested object or an array of another DTO?`,
    answerHtml: `<p>For a nested single object: <code>@ApiProperty({ type: () => AddressDto })</code> on the property, with the property typed as <code>AddressDto</code>. For an array of DTOs: <code>@ApiProperty({ type: () => AddressDto, isArray: true })</code> or equivalently <code>@ApiProperty({ type: [AddressDto] })</code>.</p><p>The <code>() => AddressDto</code> lazy-reference form matters when there's a circular reference between DTOs (A references B which references A) — a direct class reference would try to evaluate before the other class is defined.</p>`,
  },
  {
    id: "a15-swagger-docs-13",
    category: "core",
    categoryLabel: "Core",
    question: `How do you document a polymorphic field that can be one of several DTO shapes (a discriminated union)?`,
    answerHtml: `<p>Use <code>@ApiExtraModels()</code> to register all the possible variant classes with the document builder, then on the property use <code>@ApiProperty({ oneOf: [{ $ref: getSchemaPath(CardPayment) }, { $ref: getSchemaPath(BankTransfer) }] })</code>, optionally with a <code>discriminator</code> mapping keyed by a type field.</p><p>Without registering the variants via <code>@ApiExtraModels</code>, <code>getSchemaPath()</code> would reference a schema component that was never added to the document's <code>components.schemas</code>, producing a broken <code>$ref</code>.</p>`,
  },
  {
    id: "a15-swagger-docs-14",
    category: "core",
    categoryLabel: "Core",
    question: `How do you secure the /docs endpoint so the Swagger UI and JSON spec aren't publicly browsable in production?`,
    answerHtml: `<p>Common approaches: (1) gate <code>SwaggerModule.setup(...)</code> itself behind an environment check so it's never registered outside dev/staging; (2) put HTTP Basic Auth in front of the docs route using middleware (e.g. <code>express-basic-auth</code>) applied only to the docs path before calling <code>setup</code>; (3) put the docs path behind the same JWT/API-key guard used for the rest of the API, or behind a reverse-proxy/VPN rule so it's unreachable from the public internet entirely.</p><p>A frequent senior-interview follow-up: the JSON spec at <code>/docs-json</code> needs the same protection as the UI — securing only the HTML page while leaving the machine-readable JSON open defeats the purpose.</p>`,
  },
  {
    id: "a15-swagger-docs-15",
    category: "core",
    categoryLabel: "Core",
    question: `Concretely, how do you add Basic Auth in front of Swagger UI in a NestJS app using Express?`,
    answerHtml: `<p>Install <code>express-basic-auth</code> and, in <code>main.ts</code> before calling <code>SwaggerModule.setup</code>, apply it as middleware scoped to the docs path: <code>app.use(['/docs', '/docs-json'], basicAuth({ users: { admin: process.env.SWAGGER_PASSWORD }, challenge: true }))</code>. Then call <code>SwaggerModule.setup('docs', app, document)</code> as normal — the middleware intercepts requests to that path before they reach the Swagger UI handler.</p><p>The credentials should come from environment variables/secrets manager, never hardcoded, and the same path array must cover both the UI route and the JSON route.</p>`,
  },
  {
    id: "a15-swagger-docs-16",
    category: "core",
    categoryLabel: "Core",
    question: `Why is exposing Swagger docs publicly in production a real security concern, beyond "it's extra attack surface"?`,
    answerHtml: `<p>The OpenAPI document is effectively a map of your entire API surface: every endpoint, every parameter, every DTO field name and type, often including internal/admin-only routes that aren't linked anywhere in a public client. An attacker gets a structured target list for free instead of having to fuzz or reverse-engineer the API.</p><p>It can also leak implementation details via examples/descriptions (internal service names, business logic hints) and, if <code>@ApiBearerAuth()</code>/<code>@ApiSecurity()</code> schemes are documented with example tokens left in by accident, outright credentials.</p>`,
  },
  {
    id: "a15-swagger-docs-17",
    category: "core",
    categoryLabel: "Core",
    question: `How do you document that an endpoint requires a Bearer JWT, and what two things does that actually require?`,
    answerHtml: `<p>Two parts are needed: (1) register the security scheme globally on the <code>DocumentBuilder</code> with <code>.addBearerAuth()</code>, which adds a reusable <code>components.securitySchemes</code> entry (default name <code>'bearer'</code>); (2) apply <code>@ApiBearerAuth()</code> on the controller or route handler to reference that scheme for this operation, which both documents it and adds an "Authorize" lock icon plus the field to paste a token into in Swagger UI.</p><p>Forgetting step 2 on a guarded route means the docs render it as if it needs no auth, even though the actual <code>AuthGuard</code> will reject unauthenticated requests — the documentation and the real behavior silently diverge.</p>`,
  },
  {
    id: "a15-swagger-docs-18",
    category: "core",
    categoryLabel: "Core",
    question: `What is @ApiExcludeEndpoint() / @ApiExcludeController() for, and when would you use it?`,
    answerHtml: `<p><code>@ApiExcludeEndpoint()</code> (method level) and <code>@ApiExcludeController()</code> (class level) remove a route or an entire controller from the generated OpenAPI document entirely — it still works at runtime, it simply doesn't appear in Swagger UI or the JSON spec.</p><p>Typical uses: internal-only or debug endpoints you don't want documented for external consumers, webhook receivers with no meaningful request/response contract to show, or health-check/metrics endpoints that would just add noise to the public-facing docs.</p>`,
  },
  {
    id: "a15-swagger-docs-19",
    category: "core",
    categoryLabel: "Core",
    question: `How do you set up versioned API documentation in NestJS when you have /v1 and /v2 routes coexisting?`,
    answerHtml: `<p>Combine Nest's built-in URI/header/media-type versioning (<code>app.enableVersioning({ type: VersioningType.URI })</code> plus <code>@Controller({ version: '1' })</code>) with separate <code>DocumentBuilder</code>/<code>SwaggerModule.createDocument</code> calls per version, each scoped via the <code>include</code> option to only the modules belonging to that version, then mounted at distinct paths (<code>SwaggerModule.setup('docs/v1', app, v1Document)</code>, <code>SwaggerModule.setup('docs/v2', app, v2Document)</code>).</p><p>Without scoping via <code>include</code>, a single combined document mixes v1 and v2 routes together, which is technically valid OpenAPI but confusing and defeats the purpose of having separate version docs.</p>`,
  },
  {
    id: "a15-swagger-docs-20",
    category: "core",
    categoryLabel: "Core",
    question: `What does the include option on createDocument actually do, and why does it matter for multi-module apps?`,
    answerHtml: `<p><code>createDocument(app, config, { include: [UsersModule, OrdersModule] })</code> restricts the generated OpenAPI document to only the routes belonging to the listed modules, instead of every controller registered anywhere in the application.</p><p>This is the mechanism that makes per-version or per-audience documentation possible (e.g. an internal-only doc vs a public partner-facing doc) without needing separate running app instances — you generate multiple documents from the same app graph, each scoped differently.</p>`,
  },
  {
    id: "a15-swagger-docs-21",
    category: "core",
    categoryLabel: "Core",
    question: `Besides URI versioning, what other API versioning strategies does Nest support, and does Swagger doc generation handle them all equally well?`,
    answerHtml: `<p>Nest's <code>VersioningType</code> enum supports <code>URI</code> (<code>/v1/users</code>), <code>HEADER</code> (a custom request header carries the version), <code>MEDIA_TYPE</code> (versioned via the <code>Accept</code> header, e.g. <code>application/json;v=2</code>), and <code>CUSTOM</code> (your own extractor function).</p><p>Swagger UI has no native concept of header- or media-type-based versioning in its document model — it documents distinct paths, not the same path branching by header. So in practice, URI versioning maps cleanly to separate Swagger documents/paths; header and media-type versioning usually need the <code>@ApiHeader()</code> decorator to at least document the expected header, since the routes themselves look identical to Swagger.</p>`,
  },
  {
    id: "a15-swagger-docs-22",
    category: "core",
    categoryLabel: "Core",
    question: `What's the role of DocumentBuilder beyond setting the title and description of the API?`,
    answerHtml: `<p><code>DocumentBuilder</code> is a fluent builder for the OpenAPI document's top-level metadata: <code>.setTitle()</code>, <code>.setDescription()</code>, <code>.setVersion()</code> (the API version string shown in docs, independent of Nest's routing <code>VersioningType</code>), <code>.addTag()</code> to pre-register tag descriptions, <code>.addServer()</code> for base-URL variants (e.g. staging vs production), and the security scheme registrars (<code>.addBearerAuth()</code>, <code>.addApiKey()</code>, <code>.addOAuth2()</code>, <code>.addCookieAuth()</code>).</p><p>It produces a plain config object consumed by <code>SwaggerModule.createDocument</code> — it does not itself touch the app or generate anything until passed there.</p>`,
  },
  {
    id: "a15-swagger-docs-23",
    category: "core",
    categoryLabel: "Core",
    question: `What's the relationship between the OpenAPI document NestJS generates and contract testing or client SDK generation?`,
    answerHtml: `<p>Because <code>SwaggerModule.createDocument</code> produces a standard OpenAPI 3.x document (just an object, can be written to JSON/YAML), it's directly consumable by standard OpenAPI tooling outside Nest — <code>openapi-generator-cli</code> or <code>orval</code> to generate a typed frontend client, or <code>Dredd</code>/<code>Schemathesis</code> to run contract/fuzz tests asserting the live API matches the documented schema.</p><p>This is the practical payoff of keeping <code>@ApiProperty()</code> metadata accurate rather than treating it as decoration: an inaccurate schema produces a client SDK with wrong types or a contract test that wrongly passes/fails.</p>`,
  },
];

export const ADVANCED15_QUIZ: QuizQuestion[] = [
  {
    id: "a15q-swagger-docs-1",
    category: "core",
    categoryLabel: "Core",
    question: `A DTO field is typed as \`email: string\` with no \`@ApiProperty()\` decorator, and the CLI plugin is NOT enabled. What does the generated Swagger schema show for that field?`,
    options: [`The field is omitted from the schema entirely`, `The field appears with type string but a generic, possibly empty, description`, `Nest throws a startup error because every property must be explicitly decorated`, `The field is inferred as type object since decorators are required for primitives`],
    answer: 0,
    explanationHtml: `<p>Without the CLI plugin and without an explicit <code>@ApiProperty()</code>, <code>@nestjs/swagger</code> has no metadata to read for that property, so it does not appear in the generated schema at all — it's silently missing, not shown with a generic type. This is exactly why the CLI plugin or explicit decorators matter for completeness.</p>`,
  },
  {
    id: "a15q-swagger-docs-2",
    category: "core",
    categoryLabel: "Core",
    question: `Why must a NestJS DTO be defined as a class rather than a TypeScript interface for Swagger documentation to work?`,
    options: [`Classes compile faster than interfaces under ts-node`, `Interfaces are erased at compile time, leaving no runtime artifact for reflection-based introspection to read`, `Interfaces cannot have optional properties, which Swagger requires`, `Swagger UI only renders schemas whose names start with an uppercase letter, which interfaces don't enforce`],
    answer: 1,
    explanationHtml: `<p>Interfaces exist only at compile time and produce no JavaScript output. <code>SwaggerModule</code> (like <code>ValidationPipe</code>) relies on reflecting a real runtime constructor and its decorator metadata — a class survives compilation as exactly that; an interface leaves nothing behind to inspect.</p>`,
  },
  {
    id: "a15q-swagger-docs-3",
    category: "core",
    categoryLabel: "Core",
    question: `You add \`.addBearerAuth()\` to your \`DocumentBuilder\` config but forget \`@ApiBearerAuth()\` on a JWT-protected controller. What's the practical consequence?`,
    options: [`The app fails to start because the security scheme is unused`, `Swagger UI shows that endpoint as requiring no authentication, even though the real AuthGuard will reject unauthenticated calls`, `The endpoint is automatically excluded from the generated document`, `Nest auto-applies @ApiBearerAuth() to every controller once a scheme is registered, so there's no actual consequence`],
    answer: 1,
    explanationHtml: `<p><code>.addBearerAuth()</code> only registers the security scheme definition in <code>components.securitySchemes</code> — it does not attach that scheme to any specific operation. Without <code>@ApiBearerAuth()</code> on the route/controller, the generated document and the real guarded behavior diverge: docs look open, the API isn't.</p>`,
  },
  {
    id: "a15q-swagger-docs-4",
    category: "core",
    categoryLabel: "Core",
    question: `What is the main limitation when trying to document a generic response wrapper class like \`ApiResponseDto<T>\` directly with \`@ApiProperty()\` on its \`data: T\` field?`,
    options: [`@nestjs/swagger does not support classes with constructors`, `Generic type parameters are erased at compile time, so there's no runtime information about what T actually is for that call site`, `Generics require a separate npm package not included in @nestjs/swagger`, `@ApiProperty() can only be applied to primitive types, never to type parameters`],
    answer: 1,
    explanationHtml: `<p>Like interfaces, generic type parameters don't survive TypeScript compilation — <code>T</code> is gone at runtime. The standard fix is a factory function that produces a concrete decorated class per usage (e.g. <code>ApiResponseOf(UserDto)</code>), often combined with <code>@ApiExtraModels</code> and <code>getSchemaPath</code>.</p>`,
  },
  {
    id: "a15q-swagger-docs-5",
    category: "core",
    categoryLabel: "Core",
    question: `Why is securing only the Swagger UI HTML page (e.g. with Basic Auth) but leaving \`/docs-json\` unprotected an incomplete fix?`,
    options: [`Swagger UI always proxies through the JSON endpoint, so it would break anyway`, `The JSON document contains the exact same API surface information and is fully machine-readable, so it must be protected with the same rule`, `/docs-json is automatically rate-limited by NestJS, so it's already safe`, `There is no /docs-json endpoint in modern @nestjs/swagger versions`],
    answer: 1,
    explanationHtml: `<p><code>SwaggerModule.setup</code> exposes both the interactive UI and a raw JSON document at a sibling path. The JSON contains identical schema/route information and is even easier to scrape programmatically, so any auth middleware protecting the docs path must cover both routes, not just the UI's HTML entry point.</p>`,
  },
  {
    id: "a15q-swagger-docs-6",
    category: "core",
    categoryLabel: "Core",
    question: `When generating separate versioned API documentation for /v1 and /v2 routes that live in different NestJS modules, what option on \`SwaggerModule.createDocument\` scopes each generated document to only its own version's routes?`,
    options: [`The \`deepScanRoutes\` flag`, `The \`include\` option, passing the array of modules belonging to that version`, `Setting \`.setVersion()\` on DocumentBuilder, which filters routes automatically`, `There is no such option; you must run a fully separate NestJS process per version`],
    answer: 1,
    explanationHtml: `<p><code>createDocument(app, config, { include: [V1UsersModule, V1OrdersModule] })</code> restricts the generated document to routes from the listed modules. <code>.setVersion()</code> only sets the version label/string shown in the doc metadata — it doesn't filter anything. This lets one running app produce multiple distinct OpenAPI documents.</p>`,
  },
  {
    id: "a15q-swagger-docs-7",
    category: "core",
    categoryLabel: "Core",
    question: `An endpoint returns \`UserDto[]\`. Which is the correct way to document that with \`@ApiResponse()\`?`,
    options: [`@ApiResponse({ status: 200, type: UserDto })  — arrays are inferred automatically from the return type`, `@ApiResponse({ status: 200, type: UserDto, isArray: true })`, `@ApiResponse({ status: 200, type: 'array' })`, `Arrays of DTOs cannot be documented; you must wrap them in a non-array DTO first`],
    answer: 1,
    explanationHtml: `<p>Swagger needs the <code>isArray: true</code> flag (or the equivalent <code>type: [UserDto]</code> array-literal shorthand) to know the response schema is a list of that DTO's schema rather than a single instance — it isn't inferred from the controller method's actual TypeScript return type, since that's compile-time-only information.</p>`,
  },
];
