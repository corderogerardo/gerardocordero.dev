// Batch 3 — TypeORM/Prisma transactions (QueryRunner, interactive transactions), migrations strategy, repository pattern vs active record, N+1 query detection, and optimistic/pessimistic locking.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED3_FLASHCARDS: Flashcard[] = [
  {
    id: "a3-db-patterns-1",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `Why does a TypeORM \`QueryRunner\`-based transaction exist when \`dataSource.transaction(cb)\` already wraps a callback in COMMIT/ROLLBACK?`,
    answerHtml: `<p><code>dataSource.transaction()</code> only works when the entire unit of work fits inside one synchronous callback scope. A <code>QueryRunner</code> gives you a manually-managed connection (<code>connect()</code>, <code>startTransaction()</code>, <code>commit()</code>, <code>rollback()</code>, <code>release()</code>) that can be <b>passed across method/service boundaries</b> — e.g. created in a controller, threaded through two service calls, then committed. That's the only way to span a transaction across logic that isn't a single inline closure, such as a multi-step saga or a request-scoped unit-of-work pattern.</p>`,
  },
  {
    id: "a3-db-patterns-2",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `What is the single most common bug when using \`QueryRunner\` directly, and why does it corrupt data silently rather than throwing?`,
    answerHtml: `<p>Forgetting to pass <code>queryRunner.manager</code> into repository calls and instead using the global injected <code>Repository</code> (or <code>dataSource.manager</code>). Those operate on a <b>different pooled connection</b> outside the transaction, so the write succeeds and commits immediately, independent of whatever the transaction later does. There's no error — you just end up with partial, non-atomic writes that violate the invariant you thought you had.</p>`,
  },
  {
    id: "a3-db-patterns-3",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `In a \`try/catch/finally\` around a \`QueryRunner\` transaction, why must \`queryRunner.release()\` live in \`finally\`, not after the \`commit()\` call?`,
    answerHtml: `<p><code>QueryRunner</code> instances hold a dedicated connection checked out from the pool. If an error path returns or throws before reaching a line placed after <code>commit()</code>, that connection is never returned to the pool. Under load this leaks connections until the pool is exhausted and the app starts timing out on every query. <code>finally</code> guarantees release happens on both the success and rollback paths.</p>`,
  },
  {
    id: "a3-db-patterns-4",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `How does Prisma's interactive transaction (\`prisma.$transaction(async (tx) => {...})\`) differ structurally from TypeORM's \`QueryRunner\`?`,
    answerHtml: `<p>Prisma's <code>tx</code> callback parameter is a <b>scoped client</b> bound to one reserved connection for the callback's lifetime — you call <code>tx.user.update(...)</code> instead of <code>prisma.user.update(...)</code>, and the engine commits/rolls back automatically based on whether the callback throws. There's no manual <code>startTransaction</code>/<code>commit</code>/<code>release</code> lifecycle to manage and no way to leak a connection by forgetting a step — but it also means you can't easily hold the transaction open across an HTTP request boundary the way a raw <code>QueryRunner</code> can.</p>`,
  },
  {
    id: "a3-db-patterns-5",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `What is the default transaction timeout behavior in Prisma's interactive transactions, and why does it exist?`,
    answerHtml: `<p>Prisma defaults to a <code>timeout</code> of 5000ms (and a <code>maxWait</code> of 2000ms to acquire the connection) for <code>$transaction</code>. It exists because interactive transactions hold a connection from the pool for the entire callback duration — if you accidentally do a slow external API call or heavy computation inside the callback, you starve the pool. The timeout forces you to keep transaction callbacks to pure DB work and fail fast instead of silently degrading throughput for every other request.</p>`,
  },
  {
    id: "a3-db-patterns-6",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `Why is calling \`await fetch(...)\` or any network I/O inside a Prisma interactive transaction considered an anti-pattern even if it technically works?`,
    answerHtml: `<p>The transaction holds a real database connection (and on some DBs, row/table locks) for as long as the callback runs. A slow external call turns a millisecond-scale lock hold into a multi-second one, which under concurrent load causes lock contention and connection-pool starvation — other requests queue up waiting for a connection that's idle from the DB's perspective but pinned by your code. The fix is to resolve all external data <i>before</i> entering <code>$transaction</code>, then do only DB writes inside it.</p>`,
  },
  {
    id: "a3-db-patterns-7",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `In NestJS, what's the practical problem with injecting a request-scoped \`QueryRunner\` provider for "automatic" transaction management, and what do most teams use instead?`,
    answerHtml: `<p>Request-scoped providers force NestJS to rebuild the entire dependency chain (every provider that depends on it, transitively) on every request, which measurably hurts throughput under load. Most teams instead use an explicit transaction-passing pattern: a decorator/interceptor (e.g. a \`@Transactional()\` interceptor using \`cls-hooked\`/\`AsyncLocalStorage\`) or simply threading the \`EntityManager\` through service method parameters explicitly, avoiding the request-scope DI tax entirely.</p>`,
  },
  {
    id: "a3-db-patterns-8",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `Why is \`synchronize: true\` in TypeORM considered unsafe for any environment beyond local prototyping?`,
    answerHtml: `<p><code>synchronize</code> auto-diffs your entity metadata against the live schema and applies DDL changes on every app boot. In production this can <b>drop columns or tables</b> the moment an entity is renamed or removed, with no review step, no rollback, and no record of what changed. It also races multiple app instances booting concurrently against the same DB. Migrations exist specifically to make schema change reviewable, ordered, and reversible — \`synchronize\` bypasses all of that.</p>`,
  },
  {
    id: "a3-db-patterns-9",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `What's the core difference between TypeORM migrations and Prisma Migrate in how they generate the SQL diff?`,
    answerHtml: `<p>TypeORM's <code>migration:generate</code> diffs your <b>current entity metadata</b> against the live database schema (introspection-based), so it can drift if entities and DB get out of sync between generations. Prisma Migrate diffs your <code>schema.prisma</code> against the migration history already recorded in <code>_prisma_migrations</code> (a declarative target vs. an append-only log), which is generally more deterministic but means a manually-edited DB that isn't reflected in a migration file will desync the history.</p>`,
  },
  {
    id: "a3-db-patterns-10",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `Why should an expand/contract (a.k.a. parallel-change) strategy be used for a column rename in a zero-downtime deploy, instead of a single \`ALTER TABLE ... RENAME COLUMN\` migration?`,
    answerHtml: `<p>During a rolling deploy, old and new application code run concurrently against the same database. A single rename migration breaks the old code instantly (it queries a column that no longer exists) the moment the migration runs, before the new code is even fully deployed. Expand/contract splits it into three deploys: (1) add the new column and dual-write/backfill, (2) deploy code that reads the new column, (3) a later migration drops the old column once no code references it — each step is independently safe for whichever app version is currently running.</p>`,
  },
  {
    id: "a3-db-patterns-11",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `What is the danger of running \`ALTER TABLE ... ADD COLUMN ... NOT NULL DEFAULT <value>\` on a large, actively-written Postgres table, and how do modern Postgres versions change this calculus?`,
    answerHtml: `<p>Historically this required a full table rewrite plus an <code>ACCESS EXCLUSIVE</code> lock for the duration, blocking all reads and writes on a large table for potentially minutes. Postgres 11+ optimized adding a column with a <i>constant</i> default to be a metadata-only change (no rewrite) — the default is stored in catalog metadata and applied lazily on read. But a <i>volatile</i> default (e.g. <code>now()</code>, a sequence, or anything not immutable) still forces a full rewrite. Note: adding a \`NOT NULL\` column with <i>no</i> default at all isn't a slow operation, it's an immediate constraint-violation error on any table that already has rows — so the safe pattern for a non-constant default is still: add nullable, backfill in batches, then add the \`NOT NULL\` constraint (or use \`NOT NULL NOT VALID\` + \`VALIDATE CONSTRAINT\` to avoid a long-held lock).</p>`,
  },
  {
    id: "a3-db-patterns-12",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `Why do TypeORM's migration generator and a non-interactive Prisma migration diff treat a column rename as a drop + add rather than detecting it automatically?`,
    answerHtml: `<p>From the database's perspective, a rename and a drop-then-add of a same-typed column look structurally different in metadata but a pure schema-diff algorithm can't reliably distinguish "rename" from "coincidentally similar drop+add" without semantic knowledge of intent. TypeORM's \`migration:generate\` always takes this safe-default path — you must manually edit the generated migration (e.g. swap in \`RENAME COLUMN\`) to preserve data. Prisma is the partial exception: in an interactive terminal, \`prisma migrate dev\` runs a heuristic and will prompt "Did you rename X to Y?", letting you confirm a rename instead of losing data — but in non-interactive contexts (CI, scripts) it falls back to the same drop+add behavior.</p>`,
  },
  {
    id: "a3-db-patterns-13",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `What is the data-loss risk of letting Prisma's \`prisma migrate dev\` run against a shared development or staging database, and what command exists specifically to avoid it?`,
    answerHtml: `<p><code>migrate dev</code> will, when it detects drift or a non-additive change it can't apply cleanly, prompt to <b>reset the database</b> (drop and recreate from migration history) — destroying all data in shared environments other developers depend on. <code>prisma migrate deploy</code> is the non-interactive, no-reset command meant for CI/CD and shared/production environments: it only applies pending migrations and never resets or prompts.</p>`,
  },
  {
    id: "a3-db-patterns-14",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `What problem does the Repository pattern (as TypeORM's \`Repository<T>\` or a custom repository) solve that Active Record (TypeORM's \`BaseEntity\` style) structurally cannot?`,
    answerHtml: `<p>Repository decouples persistence logic from the domain/entity class, so the entity is a plain data shape and all querying lives in an injectable, mockable collaborator. Active Record bakes \`save()\`, \`find()\`, etc. directly onto the entity (\`User.find()\`, \`user.save()\`), which means the entity class itself has a hard dependency on the ORM and the live DB connection — you can't unit test domain logic against a plain object without either hitting a real DB or stubbing static methods, which is far clumsier than injecting a fake repository.</p>`,
  },
  {
    id: "a3-db-patterns-15",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `In a NestJS service using the Repository pattern, why is injecting \`Repository<User>\` directly into multiple unrelated services (rather than behind a custom repository class or a domain-specific service) a code smell at scale?`,
    answerHtml: `<p><code>Repository<User>.find()</code> exposes the full generic TypeORM query surface to every consumer, so query logic (joins, filters, pagination rules) gets duplicated and drifts across services instead of living in one place. A custom repository (extending \`Repository<T>\` or wrapping it) centralizes the actual query shapes the domain needs as named methods (\`findActiveByOrgId\`), giving you one place to fix an N+1 or add an index hint instead of N call sites.</p>`,
  },
  {
    id: "a3-db-patterns-16",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `What is the precise mechanism by which an N+1 query happens when fetching \`posts\` with their \`author\` via TypeORM's lazy relations, vs. via Prisma without \`include\`?`,
    answerHtml: `<p>In both ORMs, fetching a list of N parent rows first runs 1 query for the list. If the relation is then accessed per-row without eager loading — TypeORM's lazy relation property triggers a DB round-trip on first property access; Prisma simply has no data for a relation you didn't \`include\`, so a naive resolver doing \`prisma.user.findUnique({ where: { id: post.authorId } })\` per post fires one query per row — you get 1 (list) + N (one per row) queries instead of 2 total (list + a single batched relation fetch).</p>`,
  },
  {
    id: "a3-db-patterns-17",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `Why does GraphQL field-resolver architecture make N+1 queries especially easy to introduce accidentally, and what's the standard fix?`,
    answerHtml: `<p>Each field resolver (e.g. \`Post.author\`) is invoked independently per parent object in the result set by design — the resolver has no visibility into the sibling list it's part of, so a naive implementation issues one query per parent. The standard fix is a <code>DataLoader</code>: it batches all \`author\` lookups requested within a single tick into one \`WHERE id IN (...)\` query and caches per-request, without the resolver code needing to know about batching.</p>`,
  },
  {
    id: "a3-db-patterns-18",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `Concretely, how do you eliminate an N+1 when listing orders with their line items in TypeORM, and what's the tradeoff of the eager fix versus pagination?`,
    answerHtml: `<p>Use \`relations: ['lineItems']\` (or a \`leftJoinAndSelect\` in a query builder) so the join happens in one SQL statement instead of one query per order. The tradeoff: a single joined query returns a <b>row per line item</b>, multiplying the result set size (an order with 50 items returns 50 rows that TypeORM then has to de-duplicate/hydrate back into 1 order object), which can be slower or more memory-heavy than N+1 for orders with very large fan-out — at which point a separate batched second query (\`WHERE orderId IN (...)\`) is often the better-scaling fix.</p>`,
  },
  {
    id: "a3-db-patterns-19",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `What's the structural difference between optimistic and pessimistic locking, and which one trades latency for throughput?`,
    answerHtml: `<p>Pessimistic locking acquires a DB-level lock (e.g. \`SELECT ... FOR UPDATE\`) at read time, blocking other transactions from touching the row until the lock holder commits or rolls back — it trades throughput (other writers queue/wait) for guaranteed serialized access with zero retry logic needed. Optimistic locking takes no lock; it reads a version/timestamp, and the update only succeeds if that version still matches at write time (\`UPDATE ... WHERE id = ? AND version = ?\`), trading a small chance of failed updates (requiring app-level retry) for much higher concurrent throughput since readers never block each other.</p>`,
  },
  {
    id: "a3-db-patterns-20",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `How does TypeORM implement optimistic locking with \`@VersionColumn()\`, and what actually happens at the SQL level on a conflicting update?`,
    answerHtml: `<p>\`@VersionColumn()\` adds an integer column TypeORM auto-increments on every save. When you call \`repository.save(entity)\` on an entity loaded with version N, TypeORM issues \`UPDATE ... SET version = N+1 WHERE id = ? AND version = N\`. If another transaction already bumped the version, that \`WHERE\` clause matches zero rows, TypeORM detects the affected-row-count mismatch, and throws \`OptimisticLockVersionMismatchError\` — no DB-level lock or exception from Postgres itself, it's purely a conditional-update row-count check.</p>`,
  },
  {
    id: "a3-db-patterns-21",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `When does \`SELECT ... FOR UPDATE\` (pessimistic write lock) risk a deadlock, and what's the standard mitigation?`,
    answerHtml: `<p>Deadlock risk appears when two transactions lock the same set of rows in different orders — e.g. transaction A locks row 1 then tries row 2, while transaction B locks row 2 then tries row 1; each waits on the other indefinitely until the DB's deadlock detector kills one. The standard mitigation is a <b>consistent lock acquisition order</b> across all code paths (e.g. always lock rows sorted by primary key), so no two transactions can be waiting on each other in a cycle.</p>`,
  },
  {
    id: "a3-db-patterns-22",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `What does \`SELECT ... FOR UPDATE SKIP LOCKED\` do differently from plain \`FOR UPDATE\`, and what's its canonical use case?`,
    answerHtml: `<p>Plain \`FOR UPDATE\` makes a transaction <b>wait</b> for a lock held by another transaction. \`SKIP LOCKED\` instead silently excludes already-locked rows from the result set, returning immediately with whatever's free. This is the standard pattern for a job-queue table: multiple worker processes each run \`SELECT ... FOR UPDATE SKIP LOCKED LIMIT 1\` to grab the next unclaimed job without blocking each other or double-processing the same row.</p>`,
  },
  {
    id: "a3-db-patterns-23",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `Why is optimistic locking generally preferred over pessimistic locking for a typical HTTP request/response web API, but pessimistic locking preferred for batch/financial reconciliation jobs?`,
    answerHtml: `<p>Web requests have unpredictable client-side think time and connection patterns; holding a DB lock for the duration of a request (especially if anything slow happens before commit) risks long lock waits cascading into request timeouts under load. Optimistic locking only fails at the final write, so locks are never held across request latency. Batch/financial jobs, by contrast, run in tight, predictable code paths with no external waiting, so the throughput cost of pessimistic locking is small while the certainty of avoiding a lost-update race (e.g. double-debiting a balance) is worth the cost.</p>`,
  },
  {
    id: "a3-db-patterns-24",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `How does Prisma express optimistic concurrency control, given it has no built-in \`@VersionColumn()\` decorator like TypeORM?`,
    answerHtml: `<p>You model it manually: add an \`Int\` or \`DateTime\` version/updatedAt field in \`schema.prisma\`, then write the update as \`prisma.row.update({ where: { id, version: expectedVersion }, data: { ...changes, version: { increment: 1 } } })\`. Prisma's \`update\` throws \`P2025\` ("record not found") if the \`where\` clause (including the version) matches zero rows, which you catch and treat as a concurrency conflict — the same conditional-update mechanics as TypeORM, just hand-rolled rather than decorator-driven.</p>`,
  },
];

export const ADVANCED3_QUIZ: QuizQuestion[] = [
  {
    id: "a3q-db-patterns-1",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `A NestJS service receives an injected \`QueryRunner\` and calls \`queryRunner.startTransaction()\`, then inside the try block calls \`this.userRepository.save(user)\` using the globally-injected \`Repository<User>\` instead of \`queryRunner.manager.save(user)\`. What actually happens?`,
    options: [`TypeORM throws at runtime because the repository isn't bound to the active transaction`, `The save commits immediately on a separate connection, outside the transaction, regardless of later rollback`, `TypeORM automatically detects the active QueryRunner via AsyncLocalStorage and routes the save into it`, `The save is queued and only executes when queryRunner.commitTransaction() is called`],
    answer: 1,
    explanationHtml: `<p>The injected \`Repository<User>\` uses the default \`DataSource\` connection/pool, completely independent of your manually-created \`QueryRunner\`. The write goes through and commits on its own connection immediately — it is not part of the transaction and won't be rolled back if the transaction later fails. There's no automatic context propagation; you must explicitly use \`queryRunner.manager\` for every operation that needs to be transactional.</p>`,
  },
  {
    id: "a3q-db-patterns-2",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `Why does Prisma's \`$transaction(async (tx) => {...})\` impose a default 5-second timeout on the callback?`,
    options: [`Postgres itself enforces a hard 5-second transaction limit`, `Prisma needs the timeout to decide whether to use a pessimistic or optimistic lock internally`, `The callback holds a reserved connection from the pool for its entire duration, so slow work inside it starves other requests of connections`, `It's a GraphQL-specific limit unrelated to raw SQL transactions`],
    answer: 2,
    explanationHtml: `<p>Interactive transactions pin one pool connection for the whole callback. If the callback does slow work (e.g. an awaited network call), that connection — and potentially row locks — stay held, starving the pool for other requests. The timeout is a guardrail that fails the transaction fast rather than letting it silently degrade throughput app-wide.</p>`,
  },
  {
    id: "a3q-db-patterns-3",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `Why is \`synchronize: true\` considered dangerous specifically in production, beyond "it's just risky in general"?`,
    options: [`It only works with SQLite, not Postgres or MySQL`, `It diffs entity metadata against the live schema and applies DDL automatically on boot, which can drop columns/tables with no review or rollback, and can race across concurrently booting instances`, `It disables foreign key constraints permanently`, `It silently converts all columns to nullable`],
    answer: 1,
    explanationHtml: `<p>\`synchronize\` auto-applies whatever DDL is needed to match entities to the DB on every boot — including drops — with no migration file, no review step, and no coordination between multiple instances booting at once during a rolling deploy. Migrations exist to make every schema change explicit, ordered, and reviewable instead.</p>`,
  },
  {
    id: "a3q-db-patterns-4",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `A team needs to rename a \`users.name\` column to \`users.full_name\` with zero downtime during a rolling deploy where old and new app versions run simultaneously. What is the correct migration strategy?`,
    options: [`A single migration with ALTER TABLE ... RENAME COLUMN, run before deploying new code`, `Expand/contract: add full_name as a new column with dual-write/backfill, deploy code reading the new column, then drop name in a later migration once nothing references it`, `Drop the name column and recreate it as full_name in one migration, then redeploy`, `Use synchronize: true so TypeORM reconciles the rename automatically across instances`],
    answer: 1,
    explanationHtml: `<p>A single rename instantly breaks whichever app version is still running old code that queries the old column name. Expand/contract splits the change into independently-safe deploys: both columns coexist while old and new code run side by side, and the old column is only dropped once no deployed code references it.</p>`,
  },
  {
    id: "a3q-db-patterns-5",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `What is the key structural reason the Repository pattern is easier to unit test than Active Record in a NestJS/TypeORM codebase?`,
    options: [`Repository pattern entities have no properties, only methods`, `Active Record is deprecated in TypeORM and throws at runtime in tests`, `Repository pattern injects persistence as a dependency (mockable via DI), while Active Record bakes static/instance persistence methods directly onto the entity class, requiring module-level monkey-patching to stub`, `Repository pattern doesn't require a database connection ever, even in production`],
    answer: 2,
    explanationHtml: `<p>With Repository, you inject \`Repository<T>\` (or a custom repository) and can override it with a test double via Nest's DI container in \`Test.createTestingModule\`. Active Record methods live directly on the entity class (e.g. static \`User.find()\`), so testing requires patching the class itself rather than swapping an injected dependency — a fundamentally less composable seam.</p>`,
  },
  {
    id: "a3q-db-patterns-6",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `A GraphQL API has a \`Post.author\` field resolver that runs \`prisma.user.findUnique({ where: { id: post.authorId } })\` independently for each post in a list of 100. What is the standard fix and why does it work?`,
    options: [`Add a database index on authorId — this eliminates the extra queries entirely`, `Switch to TypeORM, which doesn't have this problem`, `Use a DataLoader to batch all author lookups requested within one tick into a single WHERE id IN (...) query, since each resolver invocation has no visibility into its siblings`, `Add @VersionColumn() to the Post entity to enable batching`],
    answer: 2,
    explanationHtml: `<p>GraphQL field resolvers are invoked per-parent-object by design, with no built-in awareness of the sibling list they belong to — that's the structural cause of the N+1. An index speeds up each individual query but doesn't reduce the count from 100 to 1. DataLoader collects all keys requested in the same tick and issues one batched query, which is the standard fix regardless of ORM.</p>`,
  },
  {
    id: "a3q-db-patterns-7",
    category: "data",
    categoryLabel: "Data & ORM",
    question: `Two transactions both run SELECT ... FOR UPDATE to lock rows 1 and 2, but transaction A locks row 1 then waits on row 2, while transaction B locks row 2 then waits on row 1. What does the database do, and what's the standard prevention?`,
    options: [`Both transactions succeed because FOR UPDATE locks are reentrant`, `The database detects the deadlock and aborts one transaction; prevention is to always acquire locks in a consistent order (e.g. sorted by primary key) across all code paths`, `The second transaction silently skips the locked rows via SKIP LOCKED by default`, `PostgreSQL automatically merges the two transactions into one`],
    answer: 1,
    explanationHtml: `<p>This is a classic deadlock: each transaction holds a lock the other needs and waits indefinitely. The database's deadlock detector breaks the cycle by killing one transaction (which must then retry). The only real prevention is ensuring every code path acquires locks in the same order, so a cyclic wait can never form. SKIP LOCKED is opt-in behavior, not a default fallback.</p>`,
  },
];
