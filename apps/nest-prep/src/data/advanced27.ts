// Batch 27 — Profiling & diagnostics: the diagnostic loop (alert on p99 / event-loop lag → clinic doctor → drill → fix → validate under the same load → instrument with perf_hooks), CPU/async/memory profiling (clinic flame·0x·--prof, bubbleprof, heap-snapshot diffing), event-loop lag as the canary, keeping the loop unblocked (worker threads over fs.readFileSync), and load testing (k6·Artillery·autocannon; ratchet vs sawtooth memory).
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED27_FLASHCARDS: Flashcard[] = [
  {
    id: "a27-diagnostic-loop-1",
    category: "perf",
    categoryLabel: "Performance",
    question: `Walk the diagnostic loop for a production Node service that just paged you. What's the ordered flow from alert to confirmed fix?`,
    answerHtml: `<p>Have a <b>loop</b>, not a guess: <b>(1) Alert</b> fires on p99 latency or event-loop lag &gt; 50ms. <b>(2) Categorize</b> — run <code>clinic doctor</code> to bucket the problem as CPU, I/O, or memory before touching code. <b>(3) Drill</b> into that bucket with the right tool (<code>clinic flame</code> for CPU, <code>bubbleprof</code> for async, heap snapshots for memory). <b>(4) Fix</b> the one hot frame. <b>(5) Validate under the <i>same</i> load</b> that triggered the alert — not a toy benchmark. <b>(6) Instrument</b> with <code>perf_hooks</code> so the metric that caught it stays on the dashboard.</p><p><b>Pitch:</b> "I don't optimize by vibes — I categorize with clinic doctor first, so I'm not tuning I/O when the problem is GC." <b>Red flag:</b> jumping straight to a fix, or validating on different load than the one that fired the alert.</p>`,
  },
  {
    id: "a27-clinic-doctor-2",
    category: "perf",
    categoryLabel: "Performance",
    question: `Why is \`clinic doctor\` the first tool you reach for, not \`clinic flame\`?`,
    answerHtml: `<p><code>clinic doctor</code> is the <b>triage</b> step: it watches the process under load and tells you <i>which category</i> of problem you have — CPU-bound, I/O-bound, event-loop blocked, or GC/memory pressure — then recommends the next tool. Reaching for a flame graph first assumes it's CPU; if the real problem is a slow downstream call or GC thrash, you'll stare at a flame graph that shows the loop mostly idle and learn nothing.</p><p><b>Framework:</b> doctor categorizes → flame/0x for CPU → bubbleprof for async → heap snapshots for memory. <b>Red flag:</b> "I profiled it" with no mention of first deciding what <i>kind</i> of problem it was.</p>`,
  },
  {
    id: "a27-cpu-flamegraph-3",
    category: "perf",
    categoryLabel: "Performance",
    question: `You've confirmed a CPU-bound hotspot. How do you find the exact culprit, and what does a flame graph's shape tell you?`,
    answerHtml: `<p>Capture a CPU profile with <code>clinic flame</code> or <code>0x</code> (both wrap V8's sampling profiler) and read the <b>flame graph</b>: the x-axis is share of samples, so <b>wide bars = where the CPU actually spends time</b> — the widest frame at the top is your hotspot. Depth is just call-stack nesting, not cost. The usual culprits at the wide bar are an <b>inefficient / catastrophic-backtracking regex</b> or a <b>heavy <code>JSON.parse</code>/<code>stringify</code></b> on a multi-MB payload. For a no-extra-deps option, <code>node --prof</code> writes a V8 tick log and <code>node --prof-process</code> turns it into a readable summary.</p><p><b>Red flag:</b> reading the <i>tallest</i> stack instead of the <i>widest</i> bar — depth is not cost.</p>`,
  },
  {
    id: "a27-node-prof-4",
    category: "perf",
    categoryLabel: "Performance",
    question: `What does the built-in \`node --prof\` / \`--prof-process\` pair give you, and when would you use it over clinic/0x?`,
    answerHtml: `<p><code>node --prof</code> runs the app with V8's tick-based profiler and drops an <code>isolate-*.log</code> of raw sample ticks; <code>node --prof-process isolate-*.log</code> post-processes it into a human-readable breakdown (ticks by function, plus JS vs C++ vs GC time). It's the <b>zero-dependency</b> profiler — nothing to <code>npm install</code>, so it's what you use inside a locked-down container or CI box where you can't add <code>clinic</code>/<code>0x</code>. The trade-off is ergonomics: clinic/0x render the interactive flame graph for you; <code>--prof-process</code> gives you a text table you read yourself.</p>`,
  },
  {
    id: "a27-bubbleprof-async-5",
    category: "perf",
    categoryLabel: "Performance",
    question: `Your service isn't CPU-bound but requests are slow. What does \`clinic bubbleprof\` reveal that a flame graph can't?`,
    answerHtml: `<p>A flame graph shows <b>CPU</b> time; when the loop is mostly idle but latency is high, the time is going into <b>async waiting</b>, and that's what <code>clinic bubbleprof</code> visualizes — it maps how async operations chain together over time. The classic finding is <b>serial async that should be parallel</b>: three independent <code>await</code>ed calls done one-after-another (300ms) that should have been one <code>Promise.all</code> (100ms). Bubbleprof makes that sequential dependency chain visible as a long line of bubbles instead of a wide one.</p><p><b>Pitch:</b> "Flame graphs answer 'what's burning CPU'; bubbleprof answers 'what's waiting on what' — most slow endpoints are the second problem." <b>Red flag:</b> unnecessary sequential <code>await</code> on independent promises.</p>`,
  },
  {
    id: "a27-heap-snapshot-diff-6",
    category: "perf",
    categoryLabel: "Performance",
    question: `Memory keeps climbing. Describe the heap-snapshot technique that actually finds the leak — not just "take a snapshot."`,
    answerHtml: `<p>One snapshot tells you almost nothing; the signal is in the <b>diff</b>. Take <b>two snapshots — before and after applying load</b> — via <code>v8.writeHeapSnapshot()</code> (or attach with <code>--inspect</code> and use Chrome DevTools → Memory), load both, and open the <b>Comparison view</b>. That surfaces objects that were <b>allocated during the load and never freed</b> — the delta, sorted by retained size. Pick the growing constructor, then <b>follow the retainer chain up to the GC root</b>: the reference keeping it alive (a module-level array, an un-removed event listener, a timer, an unbounded cache) is your leak.</p><p><b>Framework:</b> two snapshots → Comparison view → growing constructor → retainer chain → GC root. <b>Red flag:</b> eyeballing a single snapshot's totals instead of diffing two.</p>`,
  },
  {
    id: "a27-retainer-chain-7",
    category: "perf",
    categoryLabel: "Performance",
    question: `In heap analysis, what is the "retainer chain" and why is it the thing you follow, not the object itself?`,
    answerHtml: `<p>The leaking object isn't the bug — <b>whatever still points at it</b> is. GC frees anything unreachable from a root, so an object survives only because some chain of references reaches it from a <b>GC root</b> (globals, the active stack, module scope). The <b>retainer chain</b> is that path of references. You follow it upward until you hit the root holding everything alive — typically a long-lived container (a module-level <code>Map</code>, an <code>EventEmitter</code>'s listener array, a <code>setInterval</code> closure). Fixing the leak means breaking that one reference (bound the cache, <code>removeListener</code>, <code>clearInterval</code>), not the leaf object.</p>`,
  },
  {
    id: "a27-loop-lag-canary-8",
    category: "perf",
    categoryLabel: "Performance",
    question: `Why is event-loop lag called "the canary," and how do you turn it into a real alert?`,
    answerHtml: `<p>Everything in one Node process rides one thread, so the moment anything blocks the loop — a sync call, a heavy parse, a runaway regex — <b>every</b> in-flight request stalls together. Event-loop lag is the <b>earliest</b> symptom: it climbs before user-facing p99 latency does, which makes it the canary in the coal mine. Measure it with <code>perf_hooks.monitorEventLoopDelay()</code> (a high-res histogram of how late the loop runs), <b>export it as a Prometheus metric/gauge</b>, and alert when it crosses ~50ms. That alert is what kicks off the diagnostic loop <i>before</i> customers notice.</p><p><b>Pitch:</b> "Loop lag is a leading indicator; latency is a lagging one — I alert on the leading one."</p>`,
  },
  {
    id: "a27-never-block-sync-9",
    category: "perf",
    categoryLabel: "Performance",
    question: `Why is a single \`fs.readFileSync\` on the request path a production incident waiting to happen?`,
    answerHtml: `<p>Sync APIs (<code>fs.readFileSync</code>, <code>crypto.pbkdf2Sync</code>, sync <code>zlib</code>) <b>block the one thread until they return</b>, so while that 40ms disk read runs, <i>every</i> concurrent request is frozen — throughput collapses and loop lag spikes. Sync is fine <b>once at startup</b> (loading config before the server listens), never inside a hot handler. Use the async variant so the loop keeps serving while libuv's thread pool does the work.</p><p><b>Red flag:</b> any <code>*Sync</code> call inside a controller/handler. It won't show up under light test load — only under the concurrency of production.</p>`,
  },
  {
    id: "a27-worker-threads-cpu-10",
    category: "perf",
    categoryLabel: "Performance",
    question: `A handler does genuine CPU-bound work (image resize, PDF gen, hashing) and it's blocking the loop. What's the fix, and what's the boundary you must respect?`,
    answerHtml: `<p><b>Offload CPU-bound work to worker threads</b> (a pool like <code>piscina</code>) or a separate service — the handler posts the task and <code>await</code>s the result while the event loop stays free for other requests. The boundary: worker threads are for <b>compute</b>, not I/O. Node already handles I/O efficiently on the async thread pool, so wrapping a DB call in a worker just adds serialization overhead for nothing. Cap the pool at ~CPU-core count and add backpressure (reject/queue when saturated) so you don't oversubscribe the machine.</p><p><b>Framework:</b> CPU-bound &amp; needs the result now → worker-thread pool; CPU-bound &amp; fire-and-forget → queue (BullMQ) + <code>202 + jobId</code>. <b>Red flag:</b> "put it in a worker thread" for an I/O-bound task.</p>`,
  },
  {
    id: "a27-load-testing-tools-11",
    category: "perf",
    categoryLabel: "Performance",
    question: `You need to validate a fix under load. What are k6, Artillery, and autocannon each good for, and why does "same load" matter?`,
    answerHtml: `<p>All three generate HTTP load; pick by need. <b>autocannon</b> — dead-simple CLI throughput/latency benchmark, the fastest way to get p99 numbers for a single endpoint. <b>k6</b> — scripted scenarios in JS with thresholds/stages, good for realistic multi-step user journeys and CI gating. <b>Artillery</b> — YAML-driven scenarios and phased ramps, easy to express arrival-rate patterns. The diagnostic-loop rule is <b>validate under the same load that triggered the alert</b>: reproduce the concurrency, payload size, and duration that caused the regression, or a green benchmark on unrealistic traffic just gives false confidence.</p>`,
  },
  {
    id: "a27-ratchet-vs-sawtooth-12",
    category: "perf",
    categoryLabel: "Performance",
    question: `While load testing, you watch process memory over time. What do a "ratchet" and a "sawtooth" pattern each tell you?`,
    answerHtml: `<p>Read the <b>shape</b> of RSS/heap under sustained load. <b>Sawtooth</b> — memory rises, then GC drops it back near a stable baseline, over and over. That's <b>healthy</b>: allocations are being reclaimed. <b>Ratchet</b> — memory steps up and never fully returns; each GC leaves the floor a little higher, so the baseline creeps up until OOM. That's a <b>leak</b> (retained objects the collector can't free). The discipline is to run load <i>long enough</i> to distinguish the two — a short test shows the rising edge of a sawtooth and looks identical to a leak.</p><p><b>Pitch:</b> "Under sustained load I don't watch peak memory, I watch the <i>baseline after GC</i> — a rising floor is a leak, a stable floor is fine." <b>Red flag:</b> declaring "no leak" after a 30-second test.</p>`,
  },
  {
    id: "a27-perf-hooks-instrument-13",
    category: "perf",
    categoryLabel: "Performance",
    question: `The last step of the diagnostic loop is "instrument with perf_hooks." What does that buy you that ad-hoc \`Date.now()\` timing doesn't?`,
    answerHtml: `<p><code>perf_hooks</code> gives you <b>high-resolution, monotonic</b> measurement that survives in production, not throwaway console timing. <code>performance.now()</code> is sub-millisecond and immune to wall-clock adjustments; <code>PerformanceObserver</code> + <code>mark()</code>/<code>measure()</code> let you name spans and stream them to metrics; <code>monitorEventLoopDelay()</code> tracks loop health as a histogram. The point of instrumenting <i>after</i> the fix is that the same metric which caught the regression stays on the dashboard, so the next occurrence pages you instead of surprising a customer — you close the loop instead of firefighting the same issue twice.</p>`,
  },
];

export const ADVANCED27_QUIZ: QuizQuestion[] = [
  {
    id: "a27-qz-1",
    category: "perf",
    categoryLabel: "Performance",
    question: `An alert fires on high event-loop lag. Following the diagnostic loop, what's the correct FIRST profiling action?`,
    options: [
      `Immediately capture a clinic flame CPU flame graph`,
      `Run clinic doctor to categorize the problem as CPU, I/O, or memory`,
      `Take two heap snapshots and open the Comparison view`,
      `Add --max-old-space-size to give V8 more heap`,
    ],
    answer: 1,
    explanationHtml: `<p><code>clinic doctor</code> is the triage step — it buckets the issue (CPU / I/O / memory) so you drill with the right tool next. Jumping straight to a flame graph assumes it's CPU; if it's a slow downstream call or GC pressure the flame graph shows an idle loop and teaches you nothing.</p>`,
  },
  {
    id: "a27-qz-2",
    category: "perf",
    categoryLabel: "Performance",
    question: `On a CPU flame graph from clinic flame or 0x, which visual feature identifies the hotspot to optimize?`,
    options: [
      `The tallest stack — the deepest call chain`,
      `The widest bar — the frame with the largest share of samples`,
      `The bar with the most distinct colors`,
      `The frame closest to the bottom of the graph`,
    ],
    answer: 1,
    explanationHtml: `<p>The x-axis is share of CPU samples, so <b>width = time spent</b>. The widest frame is the hotspot (often a catastrophic regex or a heavy <code>JSON.parse</code>). Depth is just call-stack nesting, not cost — reading the tallest stack is a common mistake.</p>`,
  },
  {
    id: "a27-qz-3",
    category: "perf",
    categoryLabel: "Performance",
    question: `Requests are slow but a flame graph shows the event loop mostly idle. Which tool best diagnoses this, and what does it typically reveal?`,
    options: [
      `node --prof-process; it reveals excessive garbage collection`,
      `clinic bubbleprof; it reveals serial async operations that should run in parallel`,
      `clinic flame; it reveals an inefficient regex`,
      `v8.writeHeapSnapshot; it reveals a retained-object leak`,
    ],
    answer: 1,
    explanationHtml: `<p>An idle loop with high latency means time is spent <b>waiting</b>, not computing. <code>clinic bubbleprof</code> maps how async operations chain over time and surfaces the classic problem: independent awaited calls done sequentially that should have been one <code>Promise.all</code>.</p>`,
  },
  {
    id: "a27-qz-4",
    category: "perf",
    categoryLabel: "Performance",
    question: `You're hunting a memory leak with heap snapshots. Which procedure actually isolates it?`,
    options: [
      `Take one snapshot at peak memory and sort objects by retained size`,
      `Take two snapshots before and after load, open the Comparison view, then follow the growing object's retainer chain to the GC root`,
      `Enable --trace-gc and read the collection timings`,
      `Restart the process and measure RSS at boot`,
    ],
    answer: 1,
    explanationHtml: `<p>The signal is the <b>diff</b>, not a single snapshot. Two snapshots (before/after load) in the Comparison view surface objects allocated during load and never freed; following the <b>retainer chain</b> up to the GC root names the reference (module-level Map, un-removed listener, uncleared timer) keeping them alive.</p>`,
  },
  {
    id: "a27-qz-5",
    category: "perf",
    categoryLabel: "Performance",
    question: `During a sustained autocannon load test you watch process memory. What distinguishes a healthy service from one with a leak?`,
    options: [
      `Healthy memory ratchets steadily upward; a leak shows a sawtooth that returns to baseline`,
      `Healthy memory is a sawtooth returning to a stable baseline after GC; a leak ratchets up with a baseline that never returns`,
      `Healthy memory stays perfectly flat; any movement at all indicates a leak`,
      `Both look identical, so memory shape can't diagnose leaks`,
    ],
    answer: 1,
    explanationHtml: `<p><b>Sawtooth</b> (rise, then GC drops it back to a stable floor) is healthy reclamation. <b>Ratchet</b> (each GC leaves the baseline higher until OOM) is a leak. You must run load long enough to tell them apart — a short test only shows the rising edge of a sawtooth and looks like a leak.</p>`,
  },
];
