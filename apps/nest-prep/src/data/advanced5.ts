// Batch 5 — Transport options (TCP, Redis, Kafka, RabbitMQ, gRPC), hybrid application (HTTP + microservice), message vs event patterns (@MessagePattern vs @EventPattern), and request-response correlation.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED5_FLASHCARDS: Flashcard[] = [
  {
    id: "a5-microservices-1",
    category: "micro",
    categoryLabel: "Microservices",
    question: `In NestJS's TCP transport, how does the framework distinguish a request-response (@MessagePattern) exchange from a fire-and-forget event (@EventPattern) at the wire level?`,
    answerHtml: `<p>Both travel over the same TCP socket using the same JSON-serialized envelope (<code>{ pattern, data, id }</code>), but the <b>id</b> field is what matters. For a <code>@MessagePattern</code> call, the client generates a unique correlation <b>id</b> and keeps a pending callback keyed by it; the server's response frame echoes that same <b>id</b> back so the client can resolve the right Observable/Promise. For <code>@EventPattern</code>, the client emits the packet and never registers a response callback — there's no <code>id</code> to correlate against, so the connection is purely one-directional and the server's handler return value is discarded.</p>`,
  },
  {
    id: "a5-microservices-2",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Why does ClientProxy.send() in NestJS return a cold Observable instead of a Promise, and what bug does forgetting to subscribe cause?`,
    answerHtml: `<p>It's cold so the request is only actually dispatched over the transport when something subscribes — this lets Nest defer serialization/connection setup and lets callers compose with RxJS operators (<code>timeout</code>, <code>retry</code>, <code>catchError</code>) before the call fires. If you call <code>.send()</code> and never <code>subscribe()</code> (or convert with <code>firstValueFrom</code>/<code>lastValueFrom</code>), <b>no message is ever sent</b> — a classic bug where a microservice call silently does nothing because the caller treated it like a Promise.</p>`,
  },
  {
    id: "a5-microservices-3",
    category: "micro",
    categoryLabel: "Microservices",
    question: `What happens if two different controllers in the same microservice register a handler for the identical @MessagePattern string?`,
    answerHtml: `<p>Nest's pattern registry is a flat map from serialized pattern to handler, built at bootstrap. The <b>last one registered wins silently</b> — there's no runtime error, no warning by default. This is a common source of "my handler isn't firing" bugs when two modules independently define overlapping patterns (e.g. both using a wildcard or duplicating a string literal instead of sharing a constant); the common mitigation is namespacing patterns per controller (e.g. <code>orders.create</code> instead of a bare <code>create</code>).</p>`,
  },
  {
    id: "a5-microservices-4",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Why is the Redis transport in NestJS often described as 'at-most-once' delivery, and how does that constrain its use as a request-response transport?`,
    answerHtml: `<p>The default Redis transport uses <b>Pub/Sub</b> channels, not Redis Streams or lists — Pub/Sub has no persistence or acknowledgment; if no subscriber is connected at publish time, the message is simply gone. For request-response (<code>@MessagePattern</code>), this means a momentarily disconnected or restarting consumer silently drops requests rather than queuing them, unlike RabbitMQ where an unacked message stays in the queue. Production setups needing durability typically swap to RabbitMQ/Kafka or a Redis Streams-based transport (a community package exists alongside the built-in Pub/Sub transporter).</p>`,
  },
  {
    id: "a5-microservices-5",
    category: "micro",
    categoryLabel: "Microservices",
    question: `In a hybrid NestJS application (HTTP + microservice on the same process), what's the actual relationship between the Nest IoC container and the two transport layers?`,
    answerHtml: `<p>There is exactly <b>one application context and one DI container</b> shared by both. <code>app.connectMicroservice()</code> attaches an additional transport listener to the existing <code>INestApplication</code> rather than spinning up a second Nest app — so the same providers registered at the module level are visible to both HTTP controllers and microservice <code>@MessagePattern</code>/<code>@EventPattern</code> handlers. (Provider resolution is shared; that's separate from whether global pipes/guards/interceptors/filters configured on the HTTP app apply to the microservice — by default they don't, see the <code>inheritAppConfig</code> flashcard.)</p>`,
  },
  {
    id: "a5-microservices-6",
    category: "micro",
    categoryLabel: "Microservices",
    question: `When you call connectMicroservice() multiple times to attach several transports (e.g. TCP + Redis) to one hybrid app, what must you remember to do before traffic flows, and why is it easy to get wrong?`,
    answerHtml: `<p>You must call <code>await app.startAllMicroservices()</code> in addition to <code>app.listen()</code>. It's easy to get wrong because forgetting it produces no error — the HTTP server starts fine and looks healthy, but every microservice transport is simply never bound, so <code>@MessagePattern</code>/<code>@EventPattern</code> handlers never receive traffic, and the failure only surfaces when a client times out calling the service.</p>`,
  },
  {
    id: "a5-microservices-7",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Why can a NestJS hybrid app's global ValidationPipe behave differently for an HTTP route versus a @MessagePattern handler, even though both are 'globally' registered?`,
    answerHtml: `<p>By default, a hybrid app's microservice does <b>not</b> inherit the global pipes/interceptors/guards/filters configured on the main HTTP <code>INestApplication</code> — when <code>connectMicroservice()</code> creates the microservice instance, it gets built with its own fresh <code>ApplicationConfig</code> rather than sharing the HTTP app's. The documented fix is passing <code>{ inheritAppConfig: true }</code> as the second argument to <code>connectMicroservice()</code>, e.g. <code>app.connectMicroservice(options, { inheritAppConfig: true })</code> — without it, a <code>useGlobalPipes()</code> call on the main app simply has no effect on microservice handlers, a frequent source of "validation isn't running" bugs in hybrid setups.</p>`,
  },
  {
    id: "a5-microservices-8",
    category: "micro",
    categoryLabel: "Microservices",
    question: `What does NestJS's gRPC transport require beyond the .proto file that the other transports (TCP, Redis, RabbitMQ) don't, and what's the practical implication for the @MessagePattern decorator value?`,
    answerHtml: `<p>gRPC requires the service to physically implement the RPC method names defined in the <b>.proto</b> contract — Nest's <code>@GrpcMethod('ServiceName', 'MethodName')</code> (or <code>@GrpcStreamMethod</code>) binds a handler to a specific service/method pair from the compiled descriptor, not an arbitrary string pattern, and replaces <code>@MessagePattern</code> entirely for gRPC handlers (it isn't used alongside it). Unlike TCP/Redis where <code>@MessagePattern('any.string')</code> is purely a runtime convention, gRPC method names are checked against the loaded proto definition at startup, so a typo or contract drift fails fast rather than silently going unmatched.</p>`,
  },
  {
    id: "a5-microservices-9",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Why would a team choose Kafka over RabbitMQ as a NestJS microservice transport when the requirement is 'replay the last 24 hours of events for a new consumer'?`,
    answerHtml: `<p>Kafka retains messages on disk per-partition for a configured retention window regardless of consumption (log-based), so a brand-new consumer group can seek to an earlier offset and replay history. RabbitMQ queues are <b>consumption-based</b> — once a message is acked and removed (or expires via TTL), it's gone; RabbitMQ has no built-in replay semantics. NestJS's Kafka transport exposes this via consumer group + partition assignment, whereas its RabbitMQ transport models classic exchange/queue/ack semantics, not a log.</p>`,
  },
  {
    id: "a5-microservices-10",
    category: "micro",
    categoryLabel: "Microservices",
    question: `In NestJS's Kafka transport, why does @MessagePattern need a reply topic, and how is request-response correlation actually implemented on top of Kafka's inherently async log model?`,
    answerHtml: `<p>Kafka has no built-in RPC concept — producers publish to topics, consumers read from topics, with no native 'send a reply to the caller' channel. Nest's <code>ClientKafka</code> implements request-response by having the client publish to a request topic carrying a <code>kafka_correlationId</code> header, then <b>subscribe to a reply topic</b> (by convention, the request topic suffixed <code>.reply</code>) the server publishes to; the client maintains a map of pending correlation ids to resolve the right call when a matching reply arrives. This is why you must call <code>client.subscribeToResponseOf(topic)</code> — typically in <code>onModuleInit()</code> — before sending, so the reply subscription is registered ahead of time.</p>`,
  },
  {
    id: "a5-microservices-11",
    category: "micro",
    categoryLabel: "Microservices",
    question: `What's the functional difference between RabbitMQ's queue-per-service model and the 'topic'-based pattern matching NestJS exposes via @MessagePattern/@EventPattern when using the RabbitMQ transport?`,
    answerHtml: `<p>Nest's RabbitMQ transport binds one <b>queue</b> per microservice instance (configured via the <code>queue</code> option) and by default routes incoming messages to handlers by matching the <code>pattern</code> field inside the message payload — application-level dispatch, not RabbitMQ routing keys. Nest does support native topic-exchange wildcard routing via the <code>wildcards: true</code> option, but it's opt-in; without it, pattern dispatch is purely a Nest-level concern layered on top of whatever AMQP queue/exchange topology you set up, which is a common point of confusion for engineers expecting RabbitMQ's topic-exchange wildcard routing to apply automatically.</p>`,
  },
  {
    id: "a5-microservices-12",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Why does an @EventPattern handler in NestJS that throws an unhandled exception not return an error to any caller, and what's the operational risk?`,
    answerHtml: `<p>Events are fire-and-forget — there is no response channel for the originating publisher to receive an error on, since <code>@EventPattern</code> handlers don't echo a correlation id back. An unhandled exception is caught by the microservice's exception filter (logged, possibly emitting an 'error' status internally) but the <b>publisher has no idea the event failed</b>. The operational risk is silent data loss: if the handler was supposed to, say, update a read model, a thrown error there with no DLQ/retry strategy configured at the transport level (e.g. RabbitMQ nack-and-requeue) just drops the work.</p>`,
  },
  {
    id: "a5-microservices-13",
    category: "micro",
    categoryLabel: "Microservices",
    question: `When should you reach for @EventPattern instead of @MessagePattern in a NestJS microservices architecture, beyond the surface-level 'events don't need a response'?`,
    answerHtml: `<p>Beyond not needing a reply, <code>@EventPattern</code> is the right fit when you want <b>multiple independent consumers</b> to react to the same occurrence without the publisher coupling to how many or which services care (classic pub/sub fan-out) — e.g. an <code>order.created</code> event consumed by billing, inventory, and notifications services independently. <code>@MessagePattern</code> implies a single logical responder and a caller that's blocked/awaiting a specific answer, which doesn't fit a one-to-many notification use case and would force artificial 1:1 request/response calls per downstream consumer.</p>`,
  },
  {
    id: "a5-microservices-14",
    category: "micro",
    categoryLabel: "Microservices",
    question: `What does NestJS's @Payload() decorator extract differently from @Ctx() in a microservice handler, and why would you ever need @Ctx() at all?`,
    answerHtml: `<p><code>@Payload()</code> extracts the deserialized <code>data</code> portion of the incoming message (optionally a specific property via <code>@Payload('key')</code>). <code>@Ctx()</code> gives you the transport-specific <b>context object</b> — e.g. <code>RmqContext</code> exposing the raw AMQP message and channel for manual ack/nack, <code>KafkaContext</code> exposing partition/offset/headers, or <code>RedisContext</code>/<code>TcpContext</code>. You need <code>@Ctx()</code> whenever business logic depends on transport metadata the payload doesn't carry — most commonly manual acknowledgment in RabbitMQ when <code>noAck: false</code> is configured, since forgetting to ack there means the broker redelivers indefinitely.</p>`,
  },
  {
    id: "a5-microservices-15",
    category: "micro",
    categoryLabel: "Microservices",
    question: `In NestJS's RabbitMQ transport, what's the consequence of setting noAck: false without ever calling channel.ack() inside the handler via @Ctx()?`,
    answerHtml: `<p>The message stays <b>unacknowledged</b> in RabbitMQ's eyes. If the consumer connection drops or the handler simply never calls <code>channel.ack(originalMsg)</code>, RabbitMQ will eventually redeliver the message to a consumer (the same or another instance) once the connection closes — and if the handler isn't idempotent, that causes duplicate processing (e.g. double-charging, double-incrementing a counter). This is the classic at-least-once delivery trap: manual ack mode trades 'never silently lose a message' for 'you must design handlers to tolerate redelivery'.</p>`,
  },
  {
    id: "a5-microservices-16",
    category: "micro",
    categoryLabel: "Microservices",
    question: `How does NestJS implement client-side timeout for a @MessagePattern call, and what happens to the in-flight request on the server when the client times out?`,
    answerHtml: `<p>Timeout is purely a <b>client-side RxJS concern</b> — you pipe <code>.send(pattern, data).pipe(timeout(5000))</code>, and if no response arrives within the window, the Observable errors with a <code>TimeoutError</code> on the client. Nothing is communicated to the server: the server-side handler keeps executing to completion (or failure) with no idea the caller gave up, and any response it eventually sends back simply has no pending callback left to resolve it (silently dropped). This is why timeouts alone don't give you cancellation — you'd need an explicit cancellation message/pattern if you actually need the server to stop work.</p>`,
  },
  {
    id: "a5-microservices-17",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Why is 'message ordering' a meaningfully different guarantee across NestJS's Kafka vs RabbitMQ vs Redis transports?`,
    answerHtml: `<p><b>Kafka</b> guarantees ordering only <i>within a partition</i> — messages with the same key land on the same partition and are consumed in order, but ordering across partitions/topics is undefined, so partition key choice (e.g. aggregate/entity id) is a deliberate design decision. <b>RabbitMQ</b> guarantees FIFO ordering within a single queue with a single consumer, but adding concurrent consumers (prefetch > 1) breaks strict ordering since messages are processed in parallel. <b>Redis Pub/Sub</b> has no ordering guarantee across multiple subscriber processes and no persistence to even reason about replay order. Choosing a transport for an ordering-sensitive workflow (e.g. event-sourced aggregate updates) has to account for this, not just throughput.</p>`,
  },
  {
    id: "a5-microservices-18",
    category: "micro",
    categoryLabel: "Microservices",
    question: `What's the architectural risk of using TCP as the transport for a NestJS microservice that needs to scale to multiple instances behind a load balancer, compared to a broker-based transport?`,
    answerHtml: `<p>Nest's TCP transport is a <b>direct point-to-point</b> connection — the client dials a specific host:port. There's no built-in service discovery, load balancing, or failover; scaling consumers means the client itself (or an external layer like a service mesh/DNS round-robin/k8s Service) has to handle distributing requests across instances, and a downed instance just means connection refused with no automatic retry-to-another-node. Broker-based transports (RabbitMQ, Kafka) push that concern into the broker: multiple consumer instances can bind to the same queue or consumer group and the broker handles distributing work and rebalancing on instance failure.</p>`,
  },
  {
    id: "a5-microservices-19",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Why does NestJS recommend wrapping cross-cutting microservice error handling in an RpcExceptionFilter rather than relying on the default HTTP exception filter in a hybrid app?`,
    answerHtml: `<p>The default <code>ExceptionFilter</code> machinery assumes an HTTP <code>Response</code> object to format and send an error onto — that doesn't exist in a microservice context. Nest provides <code>RpcException</code> plus a base exception filter (<code>BaseRpcExceptionFilter</code>) that instead wraps the error into the transport's reply envelope so it propagates correctly to <code>ClientProxy</code> callers as an Observable error. Without it, an uncaught exception in a <code>@MessagePattern</code> handler in a hybrid app can crash through the wrong filter chain or surface an HTTP-shaped error object to a non-HTTP caller, which client SDKs don't know how to parse.</p>`,
  },
  {
    id: "a5-microservices-20",
    category: "micro",
    categoryLabel: "Microservices",
    question: `What is the practical difference between configuring a NestJS microservice as the sole app (NestFactory.createMicroservice) versus attaching it to an existing app (NestFactory.create + connectMicroservice) for hybrid setups?`,
    answerHtml: `<p><code>createMicroservice()</code> bootstraps an app whose <i>only</i> transport is the microservice listener — there's no HTTP server, no separate REST surface, and the resulting app's lifecycle is entirely driven by calling <code>listen()</code> on the microservice itself. <code>create()</code> + <code>connectMicroservice()</code> + <code>startAllMicroservices()</code> produces a <b>hybrid</b> app where HTTP and one-or-more microservice transports coexist in one process/container sharing the DI graph — used when you want the same service to expose both a REST/GraphQL surface for external clients and an internal RPC surface for service-to-service calls, without running two separate processes.</p>`,
  },
  {
    id: "a5-microservices-21",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Why would custom serialization/deserialization matter more for the Kafka and gRPC NestJS transports than for TCP, and what hooks does Nest provide?`,
    answerHtml: `<p>TCP's default serializer just JSON-stringifies the packet, which is fine for loosely-typed internal calls. <b>Kafka</b> often needs to interoperate with non-Nest producers/consumers (other languages, schema registries using Avro/Protobuf), and <b>gRPC</b> is binary Protobuf by contract — so the default JSON serializer is actively wrong for gRPC and frequently wrong for Kafka in a polyglot system. Nest exposes <code>serializer</code>/<code>deserializer</code> options on the transport config (implementing <code>Serializer</code>/<code>Deserializer</code> interfaces) precisely so you can plug in Avro/Protobuf/custom binary encoding without touching handler code.</p>`,
  },
  {
    id: "a5-microservices-22",
    category: "micro",
    categoryLabel: "Microservices",
    question: `How does NestJS's ClientsModule.registerAsync differ from register when configuring a microservice client, and when is the async form actually required rather than just nicer?`,
    answerHtml: `<p><code>register()</code> takes static transport options resolved at module-definition time. <code>registerAsync()</code> takes a factory (optionally injecting a <code>ConfigService</code> or other provider) so transport options — host, port, broker URLs, credentials — can be resolved from configuration that itself depends on DI, e.g. pulled from a <code>ConfigModule</code> that reads environment variables or a secrets provider. It's <b>required</b>, not just nicer, whenever connection details aren't known as static values at compile/module-load time, e.g. when broker URLs differ per environment and are injected via async config rather than hardcoded.</p>`,
  },
  {
    id: "a5-microservices-23",
    category: "micro",
    categoryLabel: "Microservices",
    question: `What's a subtle gotcha with using @MessagePattern wildcards or object-shaped patterns (e.g. { cmd: 'create', role: '*' }) in NestJS microservices?`,
    answerHtml: `<p>Nest matches object patterns by serializing them to a stable string key, so pattern matching is <b>exact-match on the serialized object</b> by default — wildcard-style matching only works for the limited tokens Nest's own pattern-matching utility explicitly supports (e.g. transport-specific <code>wildcards: true</code> options, or string patterns like <code>'time.us.*'</code>), not arbitrary glob/deep matching. Relying on wildcard intuition copied from RabbitMQ topic-exchange routing keys doesn't transfer automatically to <code>@MessagePattern</code> — the pattern object's shape and key order matter, and a mismatched object structure (extra/missing key) is a silent non-match, not an error.</p>`,
  },
  {
    id: "a5-microservices-24",
    category: "micro",
    categoryLabel: "Microservices",
    question: `In a NestJS hybrid app, why might guards or interceptors that rely on ExecutionContext.switchToHttp() throw or behave incorrectly when accidentally applied to a microservice route?`,
    answerHtml: `<p><code>ExecutionContext</code> is transport-agnostic at the type level, but <code>switchToHttp()</code> only returns a meaningful <code>HttpArgumentsHost</code> when the context actually originated from the HTTP adapter; calling it from a context created by <code>@MessagePattern</code>/<code>@EventPattern</code> dispatch returns an object whose methods (<code>getRequest()</code>, <code>getResponse()</code>) yield <code>undefined</code>/throw when used, because there is no underlying Express/Fastify request object. The correct pattern is to branch on <code>context.getType()</code> (or use <code>switchToRpc()</code> for microservice contexts) inside any guard/interceptor meant to be shared across both transport kinds in a hybrid app.</p>`,
  },
];

export const ADVANCED5_QUIZ: QuizQuestion[] = [
  {
    id: "a5q-microservices-1",
    category: "micro",
    categoryLabel: "Microservices",
    question: `A NestJS service uses ClientProxy.send() against a TCP microservice, piped through timeout(3000). The call times out client-side, but the server handler was mid-execution and eventually completes successfully, calling its return statement. What happens to that result?`,
    options: [`The server resends it and the client's Observable re-emits successfully despite the prior timeout`, `The response is sent back over the TCP socket but discarded client-side since no pending callback exists for that correlation id anymore`, `The server detects the client disconnected and throws an exception instead of returning`, `Nest automatically retries the original request using the late response as a cache hit`],
    answer: 1,
    explanationHtml: `Timeout is a client-only RxJS concept layered on top of a request-response correlation map. The server has no notion the client gave up, finishes its work, and sends a response frame with the original correlation <code>id</code> — but the client already cleared (or errored out) that pending entry when the timeout fired, so the late response arrives and is simply dropped, never reaching application code.`,
  },
  {
    id: "a5q-microservices-2",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Why is Kafka often chosen over RabbitMQ for a NestJS service that needs a new analytics consumer to backfill against 30 days of historical order events?`,
    options: [`Kafka brokers compress messages better, reducing storage cost enough to retain a month of data`, `RabbitMQ queues cannot exceed a fixed message count, while Kafka has no limit`, `Kafka stores events in a persistent, replayable log per partition for a configurable retention window, while RabbitMQ removes messages once consumed/acked`, `NestJS's RabbitMQ transport does not support multiple consumers on the same queue`],
    answer: 2,
    explanationHtml: `Kafka's core model is a durable, ordered log retained per the topic's retention policy, so a new consumer group can rewind to an earlier offset and replay history. RabbitMQ is fundamentally a consumption-based queue — once a message is acknowledged (or its TTL expires) it's gone, with no native replay mechanism.`,
  },
  {
    id: "a5q-microservices-3",
    category: "micro",
    categoryLabel: "Microservices",
    question: `In NestJS's Kafka transport, what is the purpose of calling client.subscribeToResponseOf(requestTopic) before sending a @MessagePattern-style request?`,
    options: [`It tells the Kafka broker to increase the partition count for that topic`, `It pre-registers the client's subscription to the reply topic so request-response correlation can resolve incoming replies`, `It enables exactly-once delivery semantics for that specific topic`, `It converts the request from asynchronous to synchronous at the network level`],
    answer: 1,
    explanationHtml: `Kafka has no native RPC concept, so Nest implements request-response by publishing to a request topic and having the server publish replies (tagged with a correlation id) to a reply topic. The client must already be subscribed to that reply topic before sending, or it has nowhere to receive and correlate the response — this is why <code>subscribeToResponseOf</code> must run ahead of the actual send, typically in <code>onModuleInit()</code>.`,
  },
  {
    id: "a5q-microservices-4",
    category: "micro",
    categoryLabel: "Microservices",
    question: `A team configures RabbitMQ with noAck: false in a NestJS microservice but the handler never calls channel.ack() via the injected RmqContext. What's the operational consequence?`,
    options: [`RabbitMQ silently drops the message after one delivery attempt`, `The message is redelivered (potentially to another consumer instance) once the connection closes, risking duplicate processing if the handler isn't idempotent`, `Nest automatically acks on the handler's behalf after a default 30-second window`, `The producer receives an immediate NACK and must resend manually`],
    answer: 1,
    explanationHtml: `Manual ack mode is an at-least-once delivery contract: an unacknowledged message stays claimed until the consumer's channel closes, at which point RabbitMQ redelivers it. This protects against losing work on a crash, but it means handlers must tolerate redelivery (idempotency) since the same message can be processed more than once.`,
  },
  {
    id: "a5q-microservices-5",
    category: "micro",
    categoryLabel: "Microservices",
    question: `In a NestJS hybrid application, what does calling app.connectMicroservice() actually do to the application's dependency injection setup, and does it also extend the main app's global pipes/guards to the microservice?`,
    options: [`It creates a second, isolated Nest application context dedicated to the microservice transport, with no DI sharing at all`, `It attaches an additional transport listener to the same INestApplication so providers are shared from one DI container, but global pipes/guards/interceptors/filters are NOT inherited unless inheritAppConfig: true is passed`, `It clones all providers into a new container scoped to the microservice listener, and global pipes are always inherited automatically`, `It requires every microservice provider to be re-registered with the @MicroserviceController decorator`],
    answer: 1,
    explanationHtml: `A hybrid app remains a single application context, so <code>connectMicroservice()</code> layers another transport listener on top of the existing <code>INestApplication</code> and providers resolve from the same DI graph. However, the microservice instance is built with its own fresh <code>ApplicationConfig</code> by default, so global pipes/guards/interceptors/filters registered via <code>useGlobalPipes()</code> etc. on the HTTP app do NOT automatically apply to microservice handlers — you must opt in with <code>connectMicroservice(options, { inheritAppConfig: true })</code>.`,
  },
  {
    id: "a5q-microservices-6",
    category: "micro",
    categoryLabel: "Microservices",
    question: `Why does an unhandled exception thrown inside an @EventPattern handler in NestJS not propagate any error back to the original publisher?`,
    options: [`Events use a different serialization format that strips exceptions automatically`, `There is no response channel for events — the publisher never attached a correlation id or pending callback to receive a reply on`, `NestJS automatically retries event handlers up to 3 times before giving up silently`, `@EventPattern handlers run in a sandboxed worker thread that isolates failures from the main process`],
    answer: 1,
    explanationHtml: `Events are fire-and-forget by design: the client's <code>emit()</code> never registers a pending response callback the way <code>send()</code> does for messages. An exception thrown in the handler is caught by the microservice's own exception filter and logged/handled locally, but structurally there is no path back to tell the publisher anything went wrong — making explicit retry/DLQ strategy at the transport level necessary for reliability.`,
  },
];
