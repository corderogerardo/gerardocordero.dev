// Batch 18 — Server-Sent Events: the @Sse() decorator and returning an Observable<MessageEvent>, SSE vs WebSockets vs long-polling tradeoffs, reconnection semantics (Last-Event-ID), and when SSE is the right choice for one-way server push.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED18_FLASHCARDS: Flashcard[] = [
  {
    id: "a18-sse-realtime-1",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `What does the @Sse() decorator do in NestJS, and what must the decorated handler return?`,
    answerHtml: `<p><code>@Sse('events')</code> marks a controller method as a <b>Server-Sent Events endpoint</b>. Nest sets the response headers (<code>Content-Type: text/event-stream</code>, <code>Connection: keep-alive</code>, <code>Cache-Control: no-cache</code>) and keeps the HTTP response open. The handler must return an <code>Observable&lt;MessageEvent&gt;</code> (Nest's own <code>MessageEvent</code> type from <code>@nestjs/common</code>, not the DOM one) — Nest subscribes to it and serializes each emitted value as one SSE frame.</p>`,
  },
  {
    id: "a18-sse-realtime-2",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `What shape does NestJS's MessageEvent interface have, and which field actually becomes the wire payload?`,
    answerHtml: `<p>It has a required <code>data</code> field plus optional <code>id</code>, <code>type</code>, and <code>retry</code> fields. Nest JSON-stringifies <code>data</code> and writes it as the <code>data:</code> line(s) of the frame. <code>id</code> becomes the event's <code>id:</code> line (used for <code>Last-Event-ID</code> on reconnect), <code>type</code> becomes a named <code>event:</code> line (so the client can use <code>addEventListener('type', ...)</code> instead of the generic <code>onmessage</code>), and <code>retry</code> overrides the client's reconnection delay in milliseconds.</p>`,
  },
  {
    id: "a18-sse-realtime-3",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `Why does an SSE Observable in Nest typically never call complete(), and what happens if it does?`,
    answerHtml: `<p>SSE is meant to be a <b>long-lived push channel</b>; calling <code>complete()</code> tells Nest the stream is done, so it ends the HTTP response and the browser's <code>EventSource</code> sees the connection close. Depending on timing, the client's auto-reconnect may immediately re-open a new request — so an unintentional <code>complete()</code> can produce a reconnect storm rather than a clean shutdown. Most real handlers only emit and rely on the client disconnecting (or an explicit unsubscribe) to end the stream.</p>`,
  },
  {
    id: "a18-sse-realtime-4",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `How do you correctly clean up resources (intervals, subscriptions) when an SSE client disconnects in Nest?`,
    answerHtml: `<p>Build the <code>Observable</code> with the constructor and return a teardown function, e.g. <code>new Observable(subscriber => { const id = setInterval(...); return () => clearInterval(id); })</code>. When the client disconnects, the underlying HTTP <code>close</code> event triggers RxJS to unsubscribe, running the teardown. Using <code>interval()</code>/<code>fromEvent()</code> operators without verifying teardown logic, or attaching a raw <code>setInterval</code> with no cleanup, leaks timers per connected client.</p>`,
  },
  {
    id: "a18-sse-realtime-5",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `Can an @Sse() route also accept path/query parameters and use guards, like a normal controller method?`,
    answerHtml: `<p>Yes. <code>@Sse()</code> is just a route-handler decorator alongside <code>@Get()</code> etc., so <code>@Param()</code>, <code>@Query()</code>, <code>@Headers()</code>, <code>@UseGuards()</code>, and <code>@UseInterceptors()</code> all work normally. The one constraint is the return type — it must resolve to <code>Observable&lt;MessageEvent&gt;</code>, so any auth/validation logic runs before you construct and return that observable.</p>`,
  },
  {
    id: "a18-sse-realtime-6",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `Why is SSE fundamentally one-way, and what is the practical consequence for a chat-like feature?`,
    answerHtml: `<p>SSE rides a single, regular HTTP response stream — there is no mechanism for the client to send data back over that same connection. The browser's <code>EventSource</code> API has no <code>send()</code> method. For a feature that needs the client to push data (typing indicators, message sends), you pair SSE for the server-to-client leg with ordinary HTTP <code>POST</code> requests for the client-to-server leg, rather than reaching for SSE alone.</p>`,
  },
  {
    id: "a18-sse-realtime-7",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `What is the core architectural difference between SSE and WebSockets at the protocol level?`,
    answerHtml: `<p>SSE is <b>plain HTTP</b> — a long-held GET response using the standard request/response model, text-only, one-directional. WebSockets perform an HTTP Upgrade handshake (101 Switching Protocols) into a completely separate, full-duplex, binary-capable framed protocol on the same TCP socket. That means SSE inherits HTTP semantics for free (status codes, headers, HTTP/2 multiplexing, standard proxying), while WebSockets need their own framing, ping/pong keep-alive, and proxy/load-balancer awareness.</p>`,
  },
  {
    id: "a18-sse-realtime-8",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `Give three concrete operational advantages SSE has over WebSockets that matter in a production backend.`,
    answerHtml: `<ul><li><b>Infra compatibility</b>: it's HTTP, so it passes through corporate proxies, CDNs, and load balancers that don't speak the WebSocket Upgrade without special configuration.</li><li><b>Built-in reconnection</b>: the browser's <code>EventSource</code> auto-reconnects with backoff and resends <code>Last-Event-ID</code>; WebSocket reconnect/backoff/resume logic must be hand-rolled.</li><li><b>HTTP/2 multiplexing</b>: many SSE streams can share one HTTP/2 connection instead of each needing its own TCP/TLS handshake, which matters when a browser opens several tabs to the same origin.</li></ul>`,
  },
  {
    id: "a18-sse-realtime-9",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `What is the classic six-connections-per-origin pitfall with SSE over HTTP/1.1, and how is it avoided?`,
    answerHtml: `<p>Browsers cap concurrent connections per origin at roughly 6 under HTTP/1.1. Each open <code>EventSource</code> consumes one of those slots for the life of the stream, so a handful of tabs to the same origin (each holding an SSE connection) can exhaust the pool and block ordinary page requests. The fix is serving over <b>HTTP/2</b>, where the server's SSE responses are multiplexed as streams on a single shared connection, removing the per-origin slot pressure.</p>`,
  },
  {
    id: "a18-sse-realtime-10",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `Why is binary data a friction point for SSE, and how do WebSockets compare?`,
    answerHtml: `<p>SSE's wire format is strictly UTF-8 text — frames are <code>field: value\\n</code> lines terminated by a blank line, so binary payloads (images, protobuf, audio chunks) must be base64-encoded into the <code>data:</code> field, inflating size by roughly a third and adding encode/decode CPU cost. WebSockets support native binary frames (<code>ArrayBuffer</code>/<code>Blob</code>), so a backend streaming binary media or compact binary protocols gets a real efficiency win from WebSockets that SSE can't match.</p>`,
  },
  {
    id: "a18-sse-realtime-11",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `When is long-polling still the right tool instead of SSE, despite SSE being strictly newer?`,
    answerHtml: `<p>Long-polling is the fallback when the transport layer between client and server can't be trusted to keep a streaming HTTP response open — some older proxies, certain enterprise network appliances, or environments that aggressively buffer/timeout idle responses. It's also simpler to reason about when push events are rare and latency tolerance is loose, since each request is a self-contained, fully bounded HTTP exchange that any HTTP client library already understands, with no special parsing of an event stream.</p>`,
  },
  {
    id: "a18-sse-realtime-12",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `Compare the per-message overhead of long-polling versus SSE for frequent, small server-push events.`,
    answerHtml: `<p>Long-polling pays a full HTTP request/response cycle (TCP if not pooled, TLS handshake amortized at best, full header set including cookies) for every single event, then immediately reopens another request to keep waiting. SSE pays that cost <b>once</b> per connection and then streams subsequent events as a few bytes of <code>data: ...\\n\\n</code> on the already-open response. For high-frequency push (multiple events per second), SSE's per-event overhead is dramatically lower.</p>`,
  },
  {
    id: "a18-sse-realtime-13",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `What is the Last-Event-ID mechanism, and which side is responsible for using it correctly?`,
    answerHtml: `<p>Every SSE frame can carry an <code>id:</code> line. The browser's <code>EventSource</code> remembers the most recent <code>id</code> it has received, and on reconnect (automatic or otherwise) it sends that value back as a <code>Last-Event-ID</code> request header. The <b>server</b> is responsible for reading that header on the new request and replaying any events the client missed since that id — the protocol only standardizes the handshake; recovering and resending missed events is entirely application logic.</p>`,
  },
  {
    id: "a18-sse-realtime-14",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `In a NestJS @Sse() handler, how would you access Last-Event-ID to support resumable streams, and what must you design around it?`,
    answerHtml: `<p>Inject the request and read <code>@Headers('Last-Event-ID')</code> (or <code>req.headers['last-event-id']</code>) inside the handler before building the <code>Observable</code>. To make it useful you need a backing store of recent events keyed by id (a ring buffer, Redis stream, or a durable log) so the handler can replay everything after that id before switching to live emission — without that store, the header is present but there is nothing to replay from.</p>`,
  },
  {
    id: "a18-sse-realtime-15",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `What reconnection delay does EventSource use by default, and how can the server change it?`,
    answerHtml: `<p>Browsers default to roughly a 3-second reconnect delay after the connection drops (implementation-defined, but ~3000ms is typical). The server can override this per-stream by sending a <code>retry: &lt;milliseconds&gt;</code> field in any event frame — the browser adopts that value for subsequent reconnect attempts, which lets a server slow down reconnects under load (a primitive form of backoff) or speed them up for latency-sensitive streams.</p>`,
  },
  {
    id: "a18-sse-realtime-16",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `If a NestJS server returns a non-2xx status (e.g. 401, 503) on an SSE endpoint, what does the browser's EventSource do?`,
    answerHtml: `<p>Per the WHATWG spec, a non-2xx status (or a <code>Content-Type</code> other than <code>text/event-stream</code>) makes the EventSource <b>fail the connection permanently</b>: <code>readyState</code> moves to <code>CLOSED</code>, an <code>error</code> event fires once, and — unlike a network-level drop — the client does <i>not</i> schedule an automatic reconnect. That means a transient 503 won't self-heal the way a dropped TCP connection would; the application has to detect the <code>error</code>/closed state and explicitly create a new <code>EventSource</code> (e.g. after a delay or once auth is refreshed) to resume the stream.</p>`,
  },
  {
    id: "a18-sse-realtime-17",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `Why is SSE a poor fit for collaborative, low-latency bidirectional features like live cursors or multiplayer editing?`,
    answerHtml: `<p>Those features need sub-100ms round-trip updates flowing in <i>both</i> directions on the same logical channel — every client action (cursor move, keystroke) must reach peers quickly. SSE only solves the server-to-client half; routing client updates back through separate POST requests adds HTTP overhead and breaks the ordering/timing guarantees a single full-duplex socket gives you. WebSockets (or WebRTC data channels for peer-to-peer) are the correct choice there.</p>`,
  },
  {
    id: "a18-sse-realtime-18",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `Name three realistic backend use cases where @Sse() is the right choice over WebSockets.`,
    answerHtml: `<ul><li><b>Progress/status streaming</b> — reporting steps of a long-running job (file processing, AI generation token-by-token, batch export) back to the initiating client.</li><li><b>Live dashboards/notifications</b> — server-driven metrics, price tickers, or notification feeds where the client never needs to talk back over the same channel.</li><li><b>Build/deploy log tailing</b> — streaming append-only log lines from a CI job or container to a UI in real time.</li></ul>`,
  },
  {
    id: "a18-sse-realtime-19",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `How does an SSE connection interact with a load-balanced NestJS deployment running multiple replicas?`,
    answerHtml: `<p>Each open SSE connection pins the client to whichever replica's process is holding that HTTP response — there's no built-in cross-instance routing the way some WebSocket gateways use a Redis adapter for. If the event source of truth lives outside the process (a message queue, Redis pub/sub, a DB change stream), the replica handling the SSE connection must itself subscribe to that source and forward matching events into its own <code>Observable</code>, otherwise events published while a client is connected to a different replica are silently missed.</p>`,
  },
  {
    id: "a18-sse-realtime-20",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `What happens to keep-alive on an SSE connection if the server emits nothing for a long time, and why does that matter?`,
    answerHtml: `<p>An idle SSE stream can be silently dropped by intermediary proxies or load balancers that close connections with no traffic after a timeout (commonly 30-60s), even though neither client nor server intended to disconnect. The common mitigation is sending periodic <b>comment lines</b> (<code>: keep-alive\\n\\n</code> — anything starting with a colon is ignored by the EventSource parser but still counts as traffic) on an interval shorter than the proxy's idle timeout.</p>`,
  },
  {
    id: "a18-sse-realtime-21",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `How does NestJS serialize the data field of a MessageEvent, and what's the implication for sending structured objects?`,
    answerHtml: `<p>Nest runs <code>JSON.stringify()</code> on whatever you put in <code>data</code> before writing it to the response, so you can emit a plain object (<code>{ data: { progress: 42 } }</code>) and the client receives a JSON string in <code>event.data</code> that it must <code>JSON.parse()</code> itself — Nest does not parse it back for you on the client side, since the client side isn't Nest. Multi-line strings in <code>data</code> are automatically split across multiple <code>data:</code> lines per the SSE spec, which the EventSource reassembles with newlines.</p>`,
  },
  {
    id: "a18-sse-realtime-22",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `What CORS consideration is specific to SSE that doesn't apply to a normal JSON REST endpoint?`,
    answerHtml: `<p><code>EventSource</code> requests are simple GET requests, but <code>withCredentials: true</code> (needed to send cookies cross-origin) requires the server's CORS response to include an exact <code>Access-Control-Allow-Origin</code> (not <code>*</code>) plus <code>Access-Control-Allow-Credentials: true</code>, same as any credentialed fetch. The extra wrinkle is that a long-lived SSE response can't easily be 'fixed up' after the fact if CORS headers are wrong — the browser rejects the stream at connection time and the client just sees a failed/retrying connection with no readable error detail in the EventSource API.</p>`,
  },
  {
    id: "a18-sse-realtime-23",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `What does it mean that EventSource has no way to set custom request headers, and how does that constrain auth design for @Sse() endpoints?`,
    answerHtml: `<p>The standard <code>EventSource</code> constructor only accepts a URL (and a <code>withCredentials</code> flag) — you cannot attach an <code>Authorization: Bearer ...</code> header like you could with <code>fetch</code>. Nest's <code>@Sse()</code> guard logic therefore typically authenticates via a <b>cookie-based session</b> (so <code>withCredentials</code> carries it automatically) or a <b>short-lived token in the query string</b>, since query params are the only other channel <code>EventSource</code> exposes — both have tradeoffs (cookie CSRF surface vs. tokens leaking into server/proxy access logs).</p>`,
  },
  {
    id: "a18-sse-realtime-24",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `Within @nestjs/common, how would you fan out the same SSE Observable to multiple subscribers/clients efficiently rather than recomputing it per connection?`,
    answerHtml: `<p>If the event source is shared (e.g. one upstream RxJS subject broadcasting to every connected client), use <code>share()</code> or a <code>Subject</code>/<code>ReplaySubject</code> as the backing multicast source, and have each <code>@Sse()</code> invocation return that shared observable (optionally piped through per-client filtering). Without sharing, returning a cold observable per request means each client independently triggers its own upstream side effects (e.g. its own DB poll or its own interval), which doesn't scale with connection count.</p>`,
  },
];

export const ADVANCED18_QUIZ: QuizQuestion[] = [
  {
    id: "a18q-sse-realtime-1",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `In NestJS, what is the required return type of a handler decorated with @Sse()?`,
    options: [`Promise<string>`, `Observable<MessageEvent>`, `ReadableStream<Uint8Array>`, `AsyncIterator<any>`],
    answer: 1,
    explanationHtml: `<p><code>@Sse()</code> handlers must return an <code>Observable&lt;MessageEvent&gt;</code> (Nest's own type from <code>@nestjs/common</code>). Nest subscribes to it and writes each emission as an SSE frame on the open HTTP response.</p>`,
  },
  {
    id: "a18q-sse-realtime-2",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `Which field of a NestJS MessageEvent, when set on the client's last received frame, gets echoed back by the browser's EventSource as the Last-Event-ID request header on reconnect?`,
    options: [`retry`, `type`, `id`, `data`],
    answer: 2,
    explanationHtml: `<p>The <code>id</code> field becomes the frame's <code>id:</code> line. <code>EventSource</code> tracks the most recent id it has seen and resends it as the <code>Last-Event-ID</code> header on any reconnect attempt, letting the server replay missed events.</p>`,
  },
  {
    id: "a18q-sse-realtime-3",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `Why can't EventSource clients attach an Authorization: Bearer header to an SSE request?`,
    options: [`The SSE spec forbids the Authorization header specifically for security reasons`, `NestJS's @Sse() decorator strips all custom headers before the handshake`, `The standard EventSource constructor only takes a URL and a withCredentials flag, with no header API`, `CORS preflight always blocks Authorization headers on text/event-stream responses`],
    answer: 2,
    explanationHtml: `<p>The browser's native <code>EventSource</code> API exposes no way to set arbitrary request headers — only the URL and <code>withCredentials</code>. That's why SSE auth typically relies on cookies (with <code>withCredentials: true</code>) or a token passed in the query string.</p>`,
  },
  {
    id: "a18q-sse-realtime-4",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `A NestJS @Sse() endpoint needs to stream binary audio chunks to the browser as efficiently as possible. What's the key tradeoff versus using a WebSocket for this?`,
    options: [`SSE has no tradeoff here — it natively supports binary frames just like WebSockets`, `SSE is UTF-8 text only, so binary data must be base64-encoded into the data field, adding size and CPU overhead that native WebSocket binary frames avoid`, `WebSockets cannot stream audio at all, only JSON payloads`, `SSE compresses binary data automatically via HTTP/2, eliminating the overhead WebSockets have`],
    answer: 1,
    explanationHtml: `<p>SSE's wire format is strictly text (<code>field: value\\n</code> lines), so binary payloads must be base64-encoded, inflating size by about a third plus encode/decode cost. WebSockets carry native binary frames, making them the better fit for binary-heavy streaming.</p>`,
  },
  {
    id: "a18q-sse-realtime-5",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `An SSE connection sits idle (no events) for several minutes behind a corporate proxy and gets silently dropped. What's the standard mitigation?`,
    options: [`Switch the retry field to 0 so the client reconnects instantly after any drop`, `Periodically send comment lines (e.g. ": keep-alive\\n\\n") on an interval shorter than the proxy's idle timeout`, `Send an empty MessageEvent with no data field every second`, `Set the retry field to a very large value so the proxy never times out`],
    answer: 1,
    explanationHtml: `<p>SSE comment lines (lines starting with <code>:</code>) are ignored by the EventSource parser but still count as traffic on the connection, so sending them periodically keeps idle-timeout proxies/load balancers from closing the stream.</p>`,
  },
  {
    id: "a18q-sse-realtime-6",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `A NestJS backend runs three load-balanced replicas behind a round-robin LB. A client holds an open SSE connection to replica A. An event is published to a Redis pub/sub channel while replica B happens to receive the publish. What must be true for the SSE client to actually receive that event?`,
    options: [`Nothing extra is needed — @Sse() automatically synchronizes Observables across all replicas of the same app`, `The load balancer must be configured to route all SSE GET requests to a single fixed replica`, `Replica A's process must itself be subscribed to that Redis channel so it can forward the event into the Observable it's already streaming to the client`, `SSE connections are stateless per request, so the next event will automatically be delivered on the client's next reconnect regardless of which replica handles it`],
    answer: 2,
    explanationHtml: `<p>Unlike some WebSocket gateways with a built-in cross-instance adapter, plain @Sse() has no such mechanism. Whichever replica is holding the client's open HTTP response must itself be subscribed to the upstream event source (Redis, a queue, etc.) and forward matching events into the Observable it returned for that connection.</p>`,
  },
  {
    id: "a18q-sse-realtime-7",
    category: "ws",
    categoryLabel: "WebSockets",
    question: `A NestJS @Sse() endpoint returns a 401 because the client's session expired. What does the browser's EventSource do next, per the WHATWG spec?`,
    options: [`It fails the connection permanently — readyState becomes CLOSED, one error event fires, and it does not automatically reconnect`, `It automatically retries the same request forever on the default ~3-second reconnect timer until it gets a 2xx`, `It falls back to long-polling against the same URL`, `It silently treats the 401 body as event data and keeps the stream open`],
    answer: 0,
    explanationHtml: `<p>Per the WHATWG EventSource spec, any non-2xx status (or wrong Content-Type) causes the user agent to <b>fail the connection</b>: readyState moves to CLOSED, an error event fires once, and — unlike a network-level drop — no automatic reconnect is scheduled. The application must detect this and explicitly construct a new EventSource to resume the stream (e.g. after refreshing auth).</p>`,
  },
];
