// Batch 11 — class-validator/class-transformer edge cases (nested DTOs, conditional validation, custom validators), ConfigModule validation schemas (Joi), and pipe-level transformation vs validation.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED11_FLASHCARDS: Flashcard[] = [
  {
    id: "a11-validation-dto-1",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `A nested DTO property is typed as \`address: AddressDto\`. Why do validators on \`AddressDto\`'s own fields silently never run unless you add one more decorator, and which one fixes it?`,
    answerHtml: `<p>class-validator inspects the <b>runtime instance</b> it's handed, not the TypeScript type. Without transformation, <code>address</code> arrives as a plain object, so <code>@ValidateNested()</code> alone tells Nest to recurse into the property, but the recursion has no class metadata to read decorators from. You need <code>@Type(() =&gt; AddressDto)</code> from class-transformer alongside <code>@ValidateNested()</code> so the plain object is first instantiated as a real <code>AddressDto</code> &mdash; only then do its own <code>@IsString()</code>/<code>@IsEmail()</code> etc. decorators get evaluated.</p>`,
  },
  {
    id: "a11-validation-dto-2",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `Given \`items: ItemDto[]\` decorated with \`@ValidateNested({ each: true })\` but missing \`@Type(() => ItemDto)\`, what happens when an array of malformed plain objects is submitted?`,
    answerHtml: `<p>The request passes validation with no nested errors reported. <code>each: true</code> only tells class-validator to iterate the array and attempt nested validation per element &mdash; it does not transform the elements into <code>ItemDto</code> instances. Each element stays a plain object, so the per-property decorators on <code>ItemDto</code> have nothing to attach to and effectively don't run. The bug is invisible in testing with valid payloads and only shows up when a consumer sends bad nested data that should have been rejected.</p>`,
  },
  {
    id: "a11-validation-dto-3",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `How do you make one DTO field required only when a sibling field has a specific value, without splitting the DTO into two classes?`,
    answerHtml: `<p>Use <code>@ValidateIf((object, value) =&gt; condition)</code> above the conditional field. The callback receives the whole object being validated, so you can branch on a sibling, e.g. <code>@ValidateIf(o =&gt; o.type === 'CARD') @IsNotEmpty() cardNumber: string;</code>. If the condition returns false, class-validator skips every other decorator on that property entirely &mdash; it's not just "allow undefined," the property is excluded from validation altogether for that request, which also means <code>@IsOptional()</code> is usually unnecessary alongside it.</p>`,
  },
  {
    id: "a11-validation-dto-4",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `What's the practical difference between \`@IsOptional()\` and \`@ValidateIf()\` when a field is \`undefined\`?`,
    answerHtml: `<p><code>@IsOptional()</code> short-circuits validation specifically when the value is <code>null</code> or <code>undefined</code> (it does not skip for empty string or <code>0</code>). <code>@ValidateIf()</code> is a general-purpose gate driven by an arbitrary predicate over the whole object &mdash; it can skip validation based on other fields, environment, or any condition you write, regardless of whether the value itself is present. <code>@IsOptional()</code> is really sugar for <code>@ValidateIf((o, v) =&gt; v !== null &amp;&amp; v !== undefined)</code> on that single property.</p>`,
  },
  {
    id: "a11-validation-dto-5",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `How do you write a custom synchronous class-validator constraint that needs access to another property on the same object (cross-field validation), e.g. "confirmPassword must equal password"?`,
    answerHtml: `<p>Implement <code>ValidatorConstraintInterface</code> in a class decorated with <code>@ValidatorConstraint({ name: 'matchesProperty', async: false })</code>, then read the sibling value off <code>args.object</code> inside <code>validate(value, args)</code> &mdash; e.g. <code>(args.object as RegisterDto).password</code>. Expose it via a decorator factory function using <code>registerDecorator</code> so it can be applied as <code>@Match('password')</code> on <code>confirmPassword</code>. The key insight is <code>args.object</code> gives the whole DTO instance, not just the decorated property's value, which is what makes cross-field rules possible.</p>`,
  },
  {
    id: "a11-validation-dto-6",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `When would you reach for an async custom validator (\`async: true\`) instead of a sync one, and what's the catch with \`ValidationPipe\`'s default config?`,
    answerHtml: `<p>Async validators are for rules requiring I/O &mdash; e.g. checking a username's uniqueness against the database via an injected repository. The catch: the validator class itself needs DI, so it must be registered as a provider (often with <code>@Injectable()</code> and <code>{ name: 'IsUniqueUsername', async: true }</code>), and you typically wire it through <code>useContainer(app, { fallbackOnErrors: true })</code> from class-validator so Nest's DI container resolves constructor dependencies inside the constraint class &mdash; by default class-validator instantiates constraint classes itself, bypassing Nest's container entirely.</p>`,
  },
  {
    id: "a11-validation-dto-7",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `What does \`useContainer(app, { fallbackOnErrors: true })\` actually do, and why is the \`fallbackOnErrors\` option there?`,
    answerHtml: `<p>It tells class-validator to resolve custom constraint classes through Nest's DI container instead of via <code>new SomeValidator()</code>, which is required if a custom validator injects a service. <code>fallbackOnErrors: true</code> makes class-validator fall back to its own default instantiation if container resolution throws &mdash; this matters because class-validator may try to resolve constraint classes before the Nest application context is fully bootstrapped (e.g. during early metadata scanning), and without the fallback a timing failure there can crash validation entirely instead of degrading gracefully.</p>`,
  },
  {
    id: "a11-validation-dto-8",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `Why can a numeric value sent as a query-string parameter fail \`@IsEnum()\` against a numeric enum, even when the value is semantically valid?`,
    answerHtml: `<p>Query-string params arrive as raw strings &mdash; <code>?status=1</code> gives <code>value === "1"</code>, not the number <code>1</code>. <code>@IsEnum()</code> checks membership via <code>Object.values(enumObject)</code>, and for a numeric enum (e.g. <code>enum Status { Active = 0, Inactive = 1 }</code>) TypeScript emits a reverse mapping object containing both the numeric values <i>and</i> their string keys, but not the stringified numbers as separate valid entries. So the raw string <code>"1"</code> doesn't strictly equal the number <code>1</code> in the values array and fails validation, even though <code>1</code> is a real enum member. The fix is coercing the param to a number before the check &mdash; <code>@Type(() =&gt; Number)</code> on the field (with <code>ValidationPipe({ transform: true })</code>) or <code>enableImplicitConversion</code>, so <code>@IsEnum()</code> evaluates the coerced numeric value instead of the raw string.</p>`,
  },
  {
    id: "a11-validation-dto-9",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `A DTO has \`@IsOptional() @IsString() age?: string\` but the controller receives \`age\` as a number from a JSON body due to a custom interceptor. Why does ValidationPipe still throw, and what governs string/number coercion at the pipe level?`,
    answerHtml: `<p><code>ValidationPipe</code> by default does not coerce types for you unless <code>transform: true</code> is set, and even then, basic-type coercion for query/param strings into numbers/booleans happens via class-transformer's <code>@Type()</code> decorators or <code>enableImplicitConversion</code>, not automatically for JSON bodies. If the upstream interceptor delivers a real JS number for <code>age</code>, <code>@IsString()</code> correctly fails because the value's actual runtime type is <code>number</code>. The decorator validates the value as received post-transform, not the DTO's declared TypeScript type &mdash; TypeScript types are erased and irrelevant at runtime.</p>`,
  },
  {
    id: "a11-validation-dto-10",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `What's the actual mechanical difference between \`ValidationPipe\`'s \`transform: true\` option and explicitly using \`@Type()\` decorators from class-transformer?`,
    answerHtml: `<p><code>transform: true</code> makes <code>ValidationPipe</code> call class-transformer's <code>plainToInstance</code> on the incoming payload so it becomes a real class instance of the DTO (enabling nested validation, custom methods, and <code>instanceof</code> checks downstream). <code>@Type(() =&gt; X)</code> is a per-property hint consumed during that same transform step, telling class-transformer which class to instantiate a nested or polymorphic property as. Without <code>transform: true</code>, <code>@Type()</code> decorators on the DTO are inert &mdash; nothing ever calls <code>plainToInstance</code>, so the decorator metadata is never read.</p>`,
  },
  {
    id: "a11-validation-dto-11",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `Why is \`forbidNonWhitelisted: true\` combined with \`whitelist: true\` considered a security-relevant pair, not just a cleanliness setting?`,
    answerHtml: `<p><code>whitelist: true</code> strips any property not decorated with a class-validator decorator from the resulting object &mdash; silently. That alone prevents mass-assignment-style payload pollution (e.g. a client sneaking <code>role: 'admin'</code> into a body), but a client probing the API gets no feedback that their extra field was dropped. <code>forbidNonWhitelisted: true</code> changes the behavior from silent-strip to a 400 error when unknown properties are present, which surfaces attempted privilege-escalation or schema-probing attempts as explicit failures instead of swallowing them.</p>`,
  },
  {
    id: "a11-validation-dto-12",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `Why can \`whitelist: true\` break a DTO that intentionally has a property with no class-validator decorators (e.g. a field only used by \`@Type()\` or a getter)?`,
    answerHtml: `<p><code>whitelist: true</code> strips properties unless they carry at least one class-validator decorator &mdash; having only a class-transformer decorator like <code>@Type()</code> or <code>@Expose()</code> isn't enough by itself, since the whitelist mechanism walks class-validator's own metadata storage. A field meant to pass through untouched (e.g. metadata you intentionally don't validate) needs at least <code>@IsOptional()</code> or another no-op-ish class-validator decorator to survive whitelisting; otherwise it's silently dropped even though it was a legitimate part of the payload.</p>`,
  },
  {
    id: "a11-validation-dto-13",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `What's the difference between a \`PipeTransform\` used for transformation (like \`ParseIntPipe\`) and \`ValidationPipe\`, in terms of where each sits in the pipe pipeline and what happens on failure?`,
    answerHtml: `<p>Both implement the same <code>PipeTransform</code> interface and run in the same pipe stage (param-binding pipes before the handler executes), but they serve different roles. <code>ParseIntPipe</code> (and friends like <code>ParseBoolPipe</code>, <code>ParseUUIDPipe</code>) does a narrow, single-value coercion and throws a <code>BadRequestException</code> immediately if the value can't be coerced &mdash; it never produces a partially-valid result. <code>ValidationPipe</code> works against a whole DTO/class, runs all class-validator decorators, accumulates every failing constraint into one <code>BadRequestException</code> with a structured error array, and optionally performs the <code>plainToInstance</code> transformation as a side effect. Order matters when both are used on the same parameter &mdash; pipes execute in the array order given to <code>@UsePipes()</code>/<code>@Param()</code>.</p>`,
  },
  {
    id: "a11-validation-dto-14",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `Why does applying a global \`ValidationPipe\` with \`transform: true\` not change the runtime type of \`@Param('id') id: string\` in a route like \`GET /users/:id\` even without a \`ParseIntPipe\`?`,
    answerHtml: `<p><code>transform: true</code> on <code>ValidationPipe</code> only triggers <code>plainToInstance</code>-based transformation when there's a metatype it recognizes as worth transforming (a custom class, not <code>String</code>/<code>Number</code>/<code>Boolean</code>/<code>Array</code>/<code>Object</code>). A bare <code>@Param('id') id: string</code> has metatype <code>String</code>, which <code>ValidationPipe</code> deliberately skips transforming/validating against &mdash; this is a common trap: people expect global <code>ValidationPipe</code> to validate primitive route params and it silently no-ops on them unless wrapped in a DTO or paired with an explicit <code>Parse*Pipe</code>.</p>`,
  },
  {
    id: "a11-validation-dto-15",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `What's the risk of putting business-logic side effects (e.g. logging, metrics) inside a custom \`@Transform()\` decorator on a DTO?`,
    answerHtml: `<p><code>@Transform()</code> callbacks run during <code>plainToInstance</code>, which executes on every request that reaches a <code>ValidationPipe</code>-bound handler &mdash; including requests that ultimately fail validation. A side effect there fires even for malformed, rejected, or malicious payloads, and it runs synchronously inside the pipe stage before any guard-level authorization context might be fully relevant to your logging needs. Transform functions are meant to be pure value mappers; anything stateful belongs in the handler or a dedicated interceptor where you control exactly when it executes relative to validation success.</p>`,
  },
  {
    id: "a11-validation-dto-16",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `How does class-transformer's \`excludeExtraneousValues: true\` (used with \`@Expose()\`/\`@Exclude()\`) interact with \`ValidationPipe\`'s \`whitelist: true\`, and why might reusing one DTO for both input and output produce confusing double-filtering?`,
    answerHtml: `<p><code>whitelist: true</code> strips properties without class-validator decorators during <code>ValidationPipe</code>'s pass on incoming data. <code>excludeExtraneousValues: true</code> is a class-transformer option (typically used for response serialization via <code>ClassSerializerInterceptor</code>) that strips properties without <code>@Expose()</code> on outgoing data. If a DTO is reused for both incoming validation and outgoing serialization with mixed decorators, a field that's <code>@Expose()</code>-marked but has no validator decorator survives serialization filtering yet gets silently dropped on the way in by <code>whitelist</code>, producing the illusion of an inconsistent schema. The fix is to keep input DTOs and output/response DTOs as separate classes rather than reusing one class for both directions.</p>`,
  },
  {
    id: "a11-validation-dto-17",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `Why is it considered an anti-pattern to put \`@IsString()\`/\`@IsNumber()\` decorators directly on environment variable config classes validated by Nest's \`ConfigModule\`, when the underlying source is \`process.env\`?`,
    answerHtml: `<p>Every value in <code>process.env</code> is always a string, regardless of what it semantically represents, so <code>@IsNumber()</code> on a config class fails for <code>PORT=3000</code> unless something coerces it first &mdash; there's no implicit numeric parsing the way there is for query params under <code>enableImplicitConversion</code>. This is exactly why Nest's official <code>ConfigModule</code> pattern favors a Joi schema (<code>Joi.number()</code>) for env validation: Joi's <code>.number()</code> validator coerces a numeric-looking string during validation, returning a real number, which class-validator decorators don't do by default without combining <code>@Type(() =&gt; Number)</code> plus an explicit <code>plainToInstance</code> call with implicit conversion enabled.</p>`,
  },
  {
    id: "a11-validation-dto-18",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `In \`ConfigModule.forRoot({ validationSchema })\`, what determines whether a missing required env var crashes the app at boot, and why is failing fast at startup the correct default for config?`,
    answerHtml: `<p>By default, <code>ConfigModule</code> runs the Joi schema's <code>.validate()</code> against <code>process.env</code> during module initialization and throws if the result has an error. A required field declared with <code>Joi.string().required()</code> and no <code>.default()</code> makes a missing var a hard boot-time failure. This is intentional: config errors caught at startup fail loudly in CI/deploy before traffic is served, versus the alternative of the app booting successfully and only failing deep in a request path the first time the missing value is dereferenced &mdash; a much harder bug to trace in production.</p>`,
  },
  {
    id: "a11-validation-dto-19",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `What's the purpose of \`Joi.object({...}).unknown(true)\` (or the \`allowUnknown\` validation option) in a \`ConfigModule\` Joi schema, and what breaks if you forget it?`,
    answerHtml: `<p><code>process.env</code> includes far more variables than the ones your app cares about &mdash; shell-exported vars, CI variables, PATH, etc. A Joi schema is closed by default: without <code>.unknown(true)</code> (or <code>validationOptions: { allowUnknown: true }</code>), Joi's <code>object()</code> validator rejects any key present in the input that isn't described in the schema. Forgetting it means the app fails to boot in any environment whose ambient environment variables don't exactly match your schema's keys &mdash; which in practice is almost every real deployment target, since CI/host platforms inject their own vars.</p>`,
  },
  {
    id: "a11-validation-dto-20",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `Why does Nest's documented \`ConfigModule\` pattern recommend writing the Joi schema as a plain exported \`Joi.object({...})\` constant rather than inlining it in \`forRoot()\`, beyond style preference?`,
    answerHtml: `<p>The practical reason is testability and reuse: a standalone schema can be imported and run directly against a mock <code>process.env</code> shape in a unit test to assert that the contract (required keys, defaults, types) hasn't regressed, without bootstrapping the whole Nest application context. It also lets you validate config shape in scripts (migrations, seed jobs, CLI tools) that need the same guarantees but don't go through <code>ConfigModule.forRoot()</code> at all &mdash; the schema becomes a portable artifact instead of something only reachable through Nest's DI bootstrap.</p>`,
  },
  {
    id: "a11-validation-dto-21",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `How do you validate a NestJS config object using class-validator instead of Joi (the \`validate\` factory function approach), and what extra step does this path require that Joi's path doesn't?`,
    answerHtml: `<p>Pass a <code>validate</code> function to <code>ConfigModule.forRoot({ validate })</code> that takes the raw <code>process.env</code>-like record, runs <code>plainToInstance(EnvironmentVariables, config, { enableImplicitConversion: true })</code> to coerce strings into the class's typed fields, then calls <code>validateSync()</code> from class-validator and throws if errors are returned. The extra step Joi doesn't need: you must explicitly call <code>plainToInstance</code> yourself first, because nothing wires class-transformer into <code>ConfigModule</code> automatically the way Joi's schema validation is self-contained &mdash; skip that step and every field validates as a string regardless of its decorator.</p>`,
  },
  {
    id: "a11-validation-dto-22",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `How would you validate that exactly one of two optional fields is provided (an XOR constraint) across a DTO, e.g. \`email\` or \`phone\` but not both and not neither?`,
    answerHtml: `<p>class-validator has no built-in XOR decorator, so this is a custom constraint: implement <code>ValidatorConstraint</code> with a <code>validate(value, args)</code> that inspects <code>args.object</code> for both fields, e.g. <code>return (!!obj.email) !== (!!obj.phone)</code> (boolean XOR), and apply it once at the class level via <code>@Validate(XorConstraint)</code> on the class itself (class-validator supports class-level decorators, not just property-level) so the rule isn't tied to validating from a single property's perspective. Splitting it into two <code>@ValidateIf</code> chains per-property is possible but gets awkward for expressing "not both."</p>`,
  },
  {
    id: "a11-validation-dto-23",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `A controller method has two DTOs from different sources bound via \`@Body()\` and \`@Query()\`. Why can't a single shared \`ValidationPipe\` instance enforce cross-source rules like "if query.mode is 'bulk', body.items must be non-empty"?`,
    answerHtml: `<p><code>ValidationPipe</code> validates each bound parameter independently and in isolation &mdash; it has no visibility across separate <code>@Body()</code>/<code>@Query()</code>/<code>@Param()</code> arguments because each invocation only receives that one value's metatype and content. Cross-parameter rules can't live in decorator metadata on two unrelated DTOs. The fix is to either merge both inputs into one DTO validated together, or move the cross-source check into the handler/a guard/an interceptor that has access to the full <code>ExecutionContext</code>, after individual pipe-level validation has already run on each piece.</p>`,
  },
  {
    id: "a11-validation-dto-24",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `What does \`skipMissingProperties: true\` on \`ValidationPipe\` actually change, and why is it dangerous to set globally rather than per-DTO?`,
    answerHtml: `<p>It tells class-validator to skip running any validator on a property that's <code>undefined</code> in the input, across every DTO validated by that pipe instance &mdash; effectively turning every field implicitly optional unless it's present. Setting it globally silently weakens every required-field check application-wide, including DTOs where a field's absence should be a hard validation error (e.g. a required <code>userId</code>). It's meant for narrow cases like PATCH-style partial updates, and should be scoped to the specific route/pipe instance handling that DTO (or better, modeled with <code>PartialType()</code> from <code>@nestjs/mapped-types</code> on a dedicated update DTO) rather than applied as the global default.</p>`,
  },
];

export const ADVANCED11_QUIZ: QuizQuestion[] = [
  {
    id: "a11q-validation-dto-1",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `A DTO has \`@ValidateNested() address: AddressDto\` but no \`@Type()\` decorator. A request sends a malformed nested \`address\` object missing a required \`city\` field defined on \`AddressDto\`. What happens?`,
    options: [`ValidationPipe throws a 400 listing the missing \`city\` field`, `Validation passes silently because the plain object never becomes an \`AddressDto\` instance for nested decorators to run against`, `Nest throws a 500 because \`@ValidateNested()\` requires \`@Type()\` to even bootstrap`, `class-validator infers the nested class from TypeScript's emitted type metadata automatically`],
    answer: 1,
    explanationHtml: `<p>Without <code>@Type(() =&gt; AddressDto)</code>, class-transformer never instantiates the nested plain object as an <code>AddressDto</code>, so class-validator has no decorator metadata to validate against for that nested object's properties. TypeScript type annotations are erased at runtime and are not a source of truth for either library.</p>`,
  },
  {
    id: "a11q-validation-dto-2",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `Why does Nest's \`ConfigModule\` documentation favor Joi over a class-validator-based schema as the default recommended approach for validating \`process.env\`?`,
    options: [`Joi is faster at runtime than class-validator for large objects`, `class-validator cannot be used outside of a NestJS DI context at all`, `Joi natively coerces string env values into typed values (e.g. numeric strings to numbers) during validation, which class-validator decorators don't do without extra transform setup`, `Joi is the only library NestJS officially supports for any validation use case`],
    answer: 2,
    explanationHtml: `<p>Every <code>process.env</code> value is a string. Joi's <code>.number()</code>/<code>.boolean()</code> validators parse and coerce during validation, returning real typed values. A class-validator DTO needs <code>plainToInstance</code> with <code>enableImplicitConversion</code> (or explicit <code>@Type()</code>) run first, or every field is checked as a raw string against decorators like <code>@IsNumber()</code> and fails.</p>`,
  },
  {
    id: "a11q-validation-dto-3",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `\`ValidationPipe\` is configured globally with \`{ whitelist: true }\` (no \`forbidNonWhitelisted\`). A client sends a DTO payload with an extra, undeclared property \`isAdmin: true\`. What's the resulting behavior?`,
    options: [`The request is rejected with a 400 Bad Request`, `The extra property is silently stripped from the transformed object before it reaches the handler`, `The extra property is passed through unchanged because whitelist only affects validation, not the object shape`, `Nest throws a 500 Internal Server Error due to the unrecognized key`],
    answer: 1,
    explanationHtml: `<p><code>whitelist: true</code> alone strips properties lacking class-validator decorators without erroring &mdash; it's a silent mass-assignment guard. To turn unknown properties into an explicit client-facing error, you must also set <code>forbidNonWhitelisted: true</code>.</p>`,
  },
  {
    id: "a11q-validation-dto-4",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `A route handler uses \`@Param('id') id: string\` with only a global \`ValidationPipe({ transform: true })\` applied &mdash; no \`ParseIntPipe\`, no DTO class wrapping the param. The client requests \`/users/abc\`. What happens?`,
    options: [`ValidationPipe throws a 400 because \`abc\` isn't a valid numeric ID`, `\`id\` is coerced to \`NaN\` and passed to the handler`, `ValidationPipe skips transformation/validation entirely for this param because its metatype is the primitive \`String\`, so \`abc\` reaches the handler unchanged`, `Nest infers the intended type from the route path token \`:id\` and validates it as a number`],
    answer: 2,
    explanationHtml: `<p><code>ValidationPipe</code> only acts on metatypes it considers "to validate" &mdash; custom classes, not the built-in <code>String</code>/<code>Number</code>/<code>Boolean</code>/<code>Array</code>/<code>Object</code> wrappers. A bare <code>@Param('id') id: string</code> is skipped entirely; you need an explicit <code>ParseIntPipe</code> (or similar) or a DTO class to get param-level validation.</p>`,
  },
  {
    id: "a11q-validation-dto-5",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `Which combination correctly implements "a field is required only when a sibling discriminator field equals a specific value," using class-validator idioms?`,
    options: [`\`@IsOptional()\` alone on the conditional field`, `\`@ValidateIf((o) => o.type === 'CARD')\` followed by \`@IsNotEmpty()\` on the conditional field`, `\`@IsDefined()\` combined with \`@IsEnum()\` on the discriminator field only`, `Two separate \`@IsNotEmpty()\` decorators, one per possible value of the discriminator`],
    answer: 1,
    explanationHtml: `<p><code>@ValidateIf</code> takes the whole object being validated and a boolean predicate; when it returns false, every other validator on that property is skipped for that request. This is the standard idiom for conditional/discriminated validation in class-validator. <code>@IsOptional()</code> alone has no awareness of sibling fields.</p>`,
  },
  {
    id: "a11q-validation-dto-6",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `In a custom async class-validator constraint that injects a repository to check DB uniqueness, what is \`useContainer(app, { fallbackOnErrors: true })\` responsible for?`,
    options: [`Catching and swallowing all validation errors so the app never returns a 400`, `Telling class-validator to resolve the constraint class through Nest's DI container instead of instantiating it itself, so constructor-injected dependencies work, with a safe fallback if container resolution fails`, `Allowing class-validator to run validators in parallel for performance`, `Disabling validation entirely in non-production environments`],
    answer: 1,
    explanationHtml: `<p>By default class-validator instantiates constraint classes itself (<code>new Constraint()</code>), bypassing Nest's DI container, so injected dependencies would be undefined. <code>useContainer</code> redirects instantiation through Nest's container; <code>fallbackOnErrors: true</code> prevents a hard crash if container resolution isn't available at the moment class-validator tries to resolve it (e.g. very early in bootstrap).</p>`,
  },
  {
    id: "a11q-validation-dto-7",
    category: "config",
    categoryLabel: "Config & Validation",
    question: `A Joi schema for \`ConfigModule.forRoot({ validationSchema })\` is written as \`Joi.object({ PORT: Joi.number().default(3000) })\` with no \`.unknown(true)\` and no \`validationOptions\` override. What's the most likely operational symptom?`,
    options: [`The app boots fine in every environment since Joi ignores extra keys by default`, `The app fails to start in most real deployments because ambient env vars (CI, shell, host platform) not listed in the schema cause Joi's closed-object validation to reject the input`, `PORT silently defaults to 3000 even when explicitly set in the environment`, `Joi throws a TypeScript compile-time error for the missing \`.unknown(true)\` call`],
    answer: 1,
    explanationHtml: `<p>Joi's <code>object()</code> schema is closed by default &mdash; any key in the validated input that isn't described in the schema causes a validation error unless <code>.unknown(true)</code> (or the <code>allowUnknown</code> validation option) is set. Since <code>process.env</code> always contains far more than your declared app config keys, omitting this almost always breaks real deployments while looking fine in a minimal local test.</p>`,
  },
];
