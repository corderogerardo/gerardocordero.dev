// Batch 10 — BullMQ producers/consumers/processors, retry & backoff strategies, CQRS basics with @nestjs/cqrs, and the transactional outbox pattern for reliable events.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED10_FLASHCARDS: Flashcard[] = [
  {
    id: "a10-event-driven-1",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `In BullMQ, what is the practical difference between a Queue, a Worker, and a QueueEvents instance, and why does Nest wire them as separate providers?`,
    answerHtml: `<p>A <b>Queue</b> is a producer handle — it only writes jobs into Redis (<code>add</code>, <code>addBulk</code>) and never processes anything. A <b>Worker</b> is a separate process/connection that pulls jobs off the queue and runs a processor function; it owns its own Redis connection because, internally, BullMQ uses blocking Redis Streams reads (<code>XREAD</code> with a block timeout) to wait efficiently for new jobs rather than polling. <b>QueueEvents</b> is a third connection that subscribes to Redis pub/sub to observe lifecycle events (completed, failed, progress) from <i>outside</i> the worker process, which matters when the consumer that needs to react to a result is not the same process that ran the job.</p><p>Nest splits these because in production they often live in different processes (API pods produce, worker pods consume), so coupling them into one class would force co-location.</p>`,
  },
  {
    id: "a10-event-driven-2",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `Why does BullMQ require a Redis connection configured with <code>maxRetriesPerRequest: null</code> for Workers, and what breaks if you leave the default?`,
    answerHtml: `<p>Workers issue blocking commands to wait for new jobs. ioredis's default <code>maxRetriesPerRequest</code> applies to <i>every</i> command including blocking ones, so a transient reconnect can cause the blocking call to be retried and time out, silently stalling job pickup or throwing <code>ReachedMaxRetriesPerRequestError</code>. Setting it to <code>null</code> tells ioredis to retry blocking commands indefinitely instead of giving up, which is required for a Worker's connection (Queue/Scheduler connections don't need this).</p>`,
  },
  {
    id: "a10-event-driven-3",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `What does BullMQ's <code>removeOnComplete</code> / <code>removeOnFail</code> job option actually control, and why is leaving both unset a production risk?`,
    answerHtml: `<p>They control whether finished job data is deleted from Redis immediately, kept as a capped count/age (<code>{ count: 1000 }</code> or <code>{ age: 3600 }</code>), or kept forever (default: completed jobs are never auto-removed unless configured). Since Redis stores each job as a hash plus index entries in sorted sets, an unbounded backlog of completed/failed jobs grows Redis memory linearly with throughput — a high-volume queue left at defaults can OOM the Redis instance over weeks. Production configs almost always cap both.</p>`,
  },
  {
    id: "a10-event-driven-4",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `Explain the difference between BullMQ's <code>attempts</code> + <code>backoff</code> job options and a Dead Letter Queue (DLQ) — and why BullMQ doesn't give you a DLQ out of the box.`,
    answerHtml: `<p><code>attempts</code> caps how many times BullMQ will re-run a job after a thrown error, and <code>backoff</code> (<code>{ type: 'exponential', delay: 1000 }</code> or a custom function) controls the wait between retries. Once attempts are exhausted, the job moves to the <b>failed</b> set — it is <i>not</i> automatically routed anywhere else. A DLQ is a separate queue you build yourself: typically a <code>QueueEvents</code> listener on <code>failed</code>, or logic inside the processor's <code>catch</code> on the last attempt, that explicitly <code>add()</code>s the payload + error context to a dedicated <code>*-dlq</code> queue for manual triage. BullMQ treats this as an app-level concern because what 'failed enough' means (alerting, replay UI, compensating action) is domain-specific.</p>`,
  },
  {
    id: "a10-event-driven-5",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `What is the difference between exponential backoff and exponential backoff with jitter, and what specific failure mode does jitter prevent in a job queue context?`,
    answerHtml: `<p>Plain exponential backoff retries at <code>delay * 2^attempt</code> with no randomness. If many jobs fail simultaneously (e.g. a downstream API outage), they all computed the same delay sequence and retry in <b>synchronized bursts</b> — a thundering herd that can re-trigger the same overload the moment the dependency comes back. Jitter adds randomness (full jitter: <code>random(0, delay)</code>; equal jitter: <code>delay/2 + random(0, delay/2)</code>) so retries spread out over time instead of arriving as a synchronized spike. BullMQ's built-in backoff strategies don't include jitter by default — it requires a custom backoff function.</p>`,
  },
  {
    id: "a10-event-driven-6",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `How do you implement a custom backoff strategy in BullMQ, and when would you need one over the built-in 'fixed'/'exponential' types?`,
    answerHtml: `<p>You register a <code>settings.backoffStrategies</code> map on the Worker, keyed by a strategy name, where each function receives <code>(attemptsMade, type, err, job)</code> and returns the delay in ms (or a Promise that resolves to one). A job opts into it via <code>backoff: { type: '&lt;strategy-name&gt;' }</code>, referencing the same key. You need a custom strategy when retry delay should depend on the <i>error type</i> (e.g. respect a 429's <code>Retry-After</code> header verbatim instead of a fixed curve), when you want jitter, or when downstream rate-limit state should influence the delay rather than a pure function of attempt count.</p>`,
  },
  {
    id: "a10-event-driven-7",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `What is the difference between a job throwing an error (triggering BullMQ's retry) and a job catching the error and returning normally with a 'soft failure' result?`,
    answerHtml: `<p>Throwing inside the processor is the only thing that triggers BullMQ's built-in retry/backoff machinery — it catches the exception, increments <code>attemptsMade</code>, and re-schedules per the backoff config. If the processor instead <i>catches</i> the error internally and returns a normal result object (e.g. <code>{ ok: false, reason }</code>), BullMQ considers the job <b>completed</b> — no retry happens, because from the queue's perspective the function ran to completion. This is a common bug: swallowing an error to 'log it' inside the processor silently disables retries for that failure class.</p>`,
  },
  {
    id: "a10-event-driven-8",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `Why must a BullMQ Worker's concurrency setting be considered together with the job's idempotency guarantees, specifically around the 'stalled job' mechanism?`,
    answerHtml: `<p>BullMQ workers send periodic heartbeats (<code>lockRenewTime</code>) while processing a job. If a worker process crashes or is paused beyond <code>stalledInterval</code> without renewing the lock, BullMQ's stalled-job checker assumes the job died mid-processing and <b>re-queues it for another worker to pick up</b> — even though the original worker might have completed 90% of side effects (e.g. already called a payment API) before dying. With concurrency &gt; 1, this means two workers can end up processing logically-the-same job at overlapping times. Processors must be idempotent (dedupe key, upsert semantics) precisely because 'at-least-once' delivery is the queue's real guarantee, not 'exactly-once'.</p>`,
  },
  {
    id: "a10-event-driven-9",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `What does BullMQ's <code>deduplication</code> (job ID-based) option actually deduplicate, and what is its key limitation versus true idempotency at the consumer?`,
    answerHtml: `<p>Passing an explicit <code>jobId</code> (or the newer <code>deduplication: { id }</code> option) prevents BullMQ from <i>enqueuing a second job</i> with the same ID while one with that ID is still waiting/active/delayed — it's an <b>insertion-time</b> guard, implemented via a Redis key check. It does nothing once the original job has completed: calling <code>add()</code> again afterward with the same ID creates a fresh job, since the dedupe lock is typically released on completion (or after a configurable TTL). It also doesn't protect against the stalled-job re-delivery case above, which happens entirely after insertion. True idempotency still has to live in the processor (e.g. check-then-upsert against a unique business key in your DB).</p>`,
  },
  {
    id: "a10-event-driven-10",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `In @nestjs/bullmq, what is the role of <code>@Processor</code> versus the older <code>@Process</code>/<code>@OnQueueCompleted</code> decorators from <code>@nestjs/bull</code>?`,
    answerHtml: `<p>In <code>@nestjs/bullmq</code> (the BullMQ-based package, distinct from the deprecated Bull-based <code>@nestjs/bull</code>), a class decorated with <code>@Processor('queueName')</code> must extend <code>WorkerHost</code> and implement a single <code>process(job: Job)</code> method — there's exactly one entry point per processor class, not multiple <code>@Process()</code>-decorated handlers like the old Bull API allowed. Lifecycle hooks (completed/failed/active) are attached via <code>@OnWorkerEvent('completed')</code> methods on the same class instead of <code>@OnQueueCompleted()</code>. The switch reflects BullMQ's own API: one Worker instance per queue, with a single processor function, rather than Bull's multi-named-job-type processor registration.</p>`,
  },
  {
    id: "a10-event-driven-11",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `Why does BullMQ recommend running Workers in a separate Node.js process (or even separate deployment) from the API server, and what Nest application pattern supports this?`,
    answerHtml: `<p>Job processing is CPU/I-O bound work that runs on the same single-threaded event loop as HTTP request handling. A heavy job processor can starve the event loop and degrade API latency/throughput for unrelated requests, and a worker crash from an unhandled job error shouldn't take down the HTTP server. Nest supports this via <code>NestFactory.createApplicationContext()</code> — bootstrapping a Nest app with no HTTP listener, just the DI container and registered providers (including BullMQ processors) — typically built as a separate entrypoint (e.g. <code>worker.ts</code>) deployed as its own process/pod, sharing the same module graph as the API.</p>`,
  },
  {
    id: "a10-event-driven-12",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `What is a 'fan-out' job pattern in BullMQ (parent/child jobs via FlowProducer), and what guarantee does it give you that manually adding N independent jobs does not?`,
    answerHtml: `<p><code>FlowProducer</code> lets you add a parent job along with an array of child jobs atomically, where the parent only moves to active once <i>all</i> children have completed (their results are aggregated and passed to the parent's processor via <code>job.getChildrenValues()</code>). Manually queuing N independent jobs gives you no aggregation or ordering guarantee — you'd have to build your own counter/barrier (e.g. in Redis or your DB) to know when all N are done. FlowProducer gives you that join semantics natively, which is the BullMQ equivalent of a fan-out/fan-in saga step.</p>`,
  },
  {
    id: "a10-event-driven-13",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `How does BullMQ's rate limiter (<code>limiter: { max, duration }</code>) interact with retries — does a rate-limited job count as a failed attempt?`,
    answerHtml: `<p>No. The queue-level <code>limiter: { max, duration }</code> option throttles how fast the Worker pulls and starts jobs in the first place — jobs that haven't started yet simply wait longer in the 'waiting' state, with no attempt consumed. For rate limits discovered <i>during</i> execution (e.g. a downstream API returns 429), BullMQ gives the processor an explicit escape hatch: call <code>await worker.rateLimit(ms)</code> and throw the special <code>Worker.RateLimitError()</code>, which moves the job back to delayed/wait for <code>ms</code> without incrementing <code>attemptsMade</code>. Throwing any other error from the processor, by contrast, <i>does</i> consume an attempt under your retry/backoff config. Conflating the two is a common design mistake: queue-level rate limiting controls your own throughput against the queue, while the RateLimitError path (or your own backoff) handles downstream throttling signals received during execution.</p>`,
  },
  {
    id: "a10-event-driven-14",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `What is CQRS, distilled to its actual architectural claim — and what does @nestjs/cqrs's <code>CommandBus</code>/<code>QueryBus</code> split give you that a single service method doesn't?`,
    answerHtml: `<p>CQRS (Command Query Responsibility Segregation) is the claim that the model used to <b>write</b> state and the model used to <b>read</b> state don't have to be the same object/schema — they can be optimized independently. In <code>@nestjs/cqrs</code>, a <code>Command</code> is dispatched through <code>CommandBus.execute()</code> to exactly one <code>CommandHandler</code> (write side, can validate/mutate/emit events), while a <code>Query</code> goes through <code>QueryBus.execute()</code> to exactly one <code>QueryHandler</code> (read side, often hitting a denormalized read model or cache). The value over a plain service method is the explicit boundary: write-path code physically cannot be called from the read path and vice versa, which keeps a complex domain from having queries that accidentally mutate state or commands that return rich projections.</p>`,
  },
  {
    id: "a10-event-driven-15",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `In @nestjs/cqrs, what is the difference between a Command Handler returning a value and a Saga reacting to an event — and why shouldn't business logic live inside a Saga?`,
    answerHtml: `<p>A <code>CommandHandler</code>'s <code>execute()</code> can return a value synchronously to the original caller (e.g. the new entity's ID back to a controller) — it's a direct, awaited call through the bus. A <code>Saga</code> is a class method decorated with <code>@Saga()</code> that takes an Observable of <i>all</i> dispatched events and maps a subset of them to a new Observable of commands to dispatch next (<code>ofType(SomeEvent)</code>, <code>map(event => new NextCommand(...))</code>) — it's reactive and asynchronous, decoupled from the original request. Sagas should contain <b>only orchestration</b> (which command to fire next, given which event) — domain logic belongs in the command handler or aggregate, because sagas are about process flow, not business rules, and mixing the two makes the flow untestable in isolation from the side effects.</p>`,
  },
  {
    id: "a10-event-driven-16",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `What is the 'aggregate publishes events, repository/handler commits them' ordering problem that @nestjs/cqrs's <code>AggregateRoot</code> + <code>apply()</code> pattern is designed around?`,
    answerHtml: `<p><code>AggregateRoot.apply(event)</code> immediately invokes any registered <code>@EventsHandler</code> for that event type (in-process, synchronously by default) <i>before</i> you've necessarily persisted the aggregate's new state to the database. If a handler reacting to that event does something externally visible (sends an email, publishes to a message broker) and then the actual DB save inside the command handler fails or rolls back, you've told the world about a state change that never durably happened. The library itself doesn't solve this — it's why event-sourced/CQRS systems pair <code>apply()</code> with either committing events to an event store as the source of truth (so 'applied' and 'persisted' are the same operation), or call <code>commit()</code> only after the DB transaction succeeds, deferring side-effecting subscribers until then.</p>`,
  },
  {
    id: "a10-event-driven-17",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `Why is @nestjs/cqrs's default EventBus an in-memory, in-process RxJS Subject — and what production limitation does that imply for a multi-instance deployment?`,
    answerHtml: `<p>By default, <code>EventBus.publish()</code> pushes onto an RxJS <code>Subject</code> that only <code>@EventsHandler</code>s registered in the <i>same Node process</i> are subscribed to. If you run multiple replicas of the service (standard for horizontal scaling), an event published on instance A never reaches handlers registered only on instance B — there's no cross-process delivery built in. For production CQRS at scale, you either keep all reactive logic colocated with the command handler that publishes it (no cross-instance dependency), or swap the bus for one backed by an actual broker (Nest's <code>IEventPublisher</code> interface is the extension point — implementations publish to Kafka/RabbitMQ/Redis Streams instead of the in-memory Subject).</p>`,
  },
  {
    id: "a10-event-driven-18",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `What problem does the transactional outbox pattern solve, stated precisely in terms of the two systems involved?`,
    answerHtml: `<p>It solves the <b>dual-write problem</b>: you cannot atomically (1) commit a change to your database and (2) publish a message to an external broker (Kafka, RabbitMQ, SQS) as a single transaction, because they're different systems with no shared 2PC coordinator in practice. If you publish first and the DB commit fails, consumers act on a change that never happened. If you commit first and the publish fails (process crash between the two calls), the broker never finds out and downstream systems silently desync. The outbox pattern collapses this to a single local transaction: write the business row <i>and</i> a row describing the event into an <code>outbox</code> table in the <b>same DB transaction</b>, so they're atomic by construction (ordinary ACID guarantees), then a separate relay process asynchronously reads the outbox and publishes to the broker.</p>`,
  },
  {
    id: "a10-event-driven-19",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `What are the two main mechanisms for relaying rows out of an outbox table to a broker, and what's the core tradeoff between them?`,
    answerHtml: `<p><b>Polling publisher:</b> a background job (could itself be a BullMQ repeatable job) periodically <code>SELECT</code>s unpublished rows from the outbox table, publishes them, then marks them published (or deletes them) — simple to build with infrastructure you already have, but adds publish latency equal to the poll interval and puts repeated read load on the DB. <b>Transaction Log Tailing (CDC):</b> a tool like Debezium reads the database's write-ahead/binlog directly and streams outbox inserts to Kafka with near-zero added latency and no polling load — but requires operating a CDC pipeline (Debezium + Kafka Connect or similar), which is meaningfully more infrastructure than a cron-style poller. Most mid-size systems start with polling and only adopt CDC once poll latency or DB load actually becomes a measured problem.</p>`,
  },
  {
    id: "a10-event-driven-20",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `In a NestJS service using TypeORM/Prisma, where exactly does the outbox-row insert have to happen relative to the business-entity write, and why does 'publish after the transaction commits, in a try/catch' not satisfy the outbox guarantee?`,
    answerHtml: `<p>The outbox row insert must happen <i>inside the same database transaction</i> as the business write (e.g. both inside one TypeORM <code>QueryRunner</code>/<code>EntityManager.transaction()</code> block) — that's the entire mechanism that makes them atomic. 'Commit the business write, then in a try/catch attempt to publish, and on failure insert into an outbox-as-fallback table' is not the same pattern: there's still a window between the commit and the publish attempt where a process crash loses the event entirely, because the fallback insert is a <i>second</i>, non-atomic operation relative to the first commit. The defining property of the outbox pattern is that the event's durability is guaranteed by the same transaction as the state change, with zero gap.</p>`,
  },
  {
    id: "a10-event-driven-21",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `How do you guarantee a relay process doesn't publish the same outbox row twice (e.g. crash after publish but before marking the row processed), and why is this acceptable in an outbox-based architecture?`,
    answerHtml: `<p>You generally don't prevent it — you accept <b>at-least-once delivery</b> from the relay and push deduplication to the consumer side, the same way BullMQ's stalled-job redelivery does. Each outbox row carries a stable event ID (often the row's own primary key or a UUID assigned at insert time); consumers track processed event IDs (a dedupe table, or an idempotent upsert keyed on that ID) so a duplicate publish is a no-op downstream. Trying to make the relay itself exactly-once (e.g. publish-then-mark as a single atomic step against two different systems) reintroduces the original dual-write problem one level down — so the pattern deliberately punts to idempotent consumers instead.</p>`,
  },
  {
    id: "a10-event-driven-22",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `How would you combine the transactional outbox pattern with BullMQ specifically — what does each piece own?`,
    answerHtml: `<p>The outbox table is the durability boundary: a Nest service writes the business row and an outbox row in one DB transaction, guaranteeing the event is never lost relative to the state change. A relay (could be a BullMQ repeatable/cron job, or a simple <code>setInterval</code> worker) polls unpublished outbox rows and calls <code>queue.add()</code> to enqueue them into BullMQ. From that point on, BullMQ owns delivery semantics to the actual job processor — retries, backoff, concurrency — while the outbox table has already done its job of guaranteeing the event reached Redis at least once. The two patterns are complementary, not redundant: outbox solves DB-to-broker atomicity, BullMQ solves broker-to-consumer reliability.</p>`,
  },
  {
    id: "a10-event-driven-23",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `Why is 'just publish the Kafka/queue message inside an ORM lifecycle hook like <code>@AfterInsert</code>' not a substitute for the outbox pattern?`,
    answerHtml: `<p>ORM lifecycle hooks like TypeORM's <code>@AfterInsert</code> fire as part of the same in-memory operation but the actual publish call is still a separate network round-trip to the broker, executed <i>after</i> the SQL <code>INSERT</code> has been sent — and depending on the ORM/driver, the hook can fire before the surrounding transaction has actually committed, or the transaction can still fail/roll back after the hook already ran. Either way you're back to two independent operations with a failure gap between them: a crash or broker outage between the hook firing and the transaction's final commit either double-counts the event or loses it. The outbox pattern's atomicity comes specifically from writing the event as a <i>row in the same transaction</i>, not from hooking a lifecycle callback.</p>`,
  },
  {
    id: "a10-event-driven-24",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `What's the difference between using BullMQ as a generic background-job queue versus using it to implement an event bus for CQRS sagas — and why might you choose BullMQ over the in-memory EventBus for saga-triggered commands?`,
    answerHtml: `<p>The in-memory <code>@nestjs/cqrs</code> EventBus is synchronous-by-default, in-process, and has no persistence — if the process crashes after publishing but before a saga's resulting command executes, that command is lost. Routing saga-triggered commands through a BullMQ queue instead (saga maps event → adds a job rather than dispatching a command directly) gives you durability (the job survives a process restart since it's in Redis), retry/backoff on the side effect, and decoupled scaling (a separate worker pool consumes the jobs). The tradeoff is added latency and eventual consistency — the command no longer executes synchronously within the same tick as the triggering event, so any code assuming immediate execution order has to be rewritten around at-least-once, asynchronous delivery.</p>`,
  },
];

export const ADVANCED10_QUIZ: QuizQuestion[] = [
  {
    id: "a10q-event-driven-1",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `A BullMQ job processor catches every error internally, logs it, and returns \`{ success: false }\` instead of rethrowing. What happens to BullMQ's configured \`attempts\` and \`backoff\` for that job?`,
    options: [`They still apply because BullMQ inspects the return value for a \`success\` field`, `They never engage — the job is marked completed since the processor function returned without throwing`, `BullMQ retries once regardless, then gives up`, `The job moves directly to the failed set without consuming an attempt`],
    answer: 1,
    explanationHtml: `<p>BullMQ's retry machinery is driven entirely by whether the processor <i>throws</i>. A processor that swallows the error and returns normally — even with a payload indicating logical failure — looks identical to a successful job to BullMQ, so it's marked <code>completed</code> and no retry/backoff logic ever runs. This is a common production bug: defensive try/catch inside a processor silently disables retries.</p>`,
  },
  {
    id: "a10q-event-driven-2",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `Why is plain exponential backoff (no jitter) considered insufficient for retries against a shared downstream dependency that just recovered from an outage?`,
    options: [`Exponential backoff delays grow too slowly to ever reduce load`, `BullMQ's exponential backoff caps out at a fixed maximum that's too short`, `All jobs that failed around the same time compute the same delay sequence and retry in synchronized bursts, re-triggering the overload`, `Exponential backoff is incompatible with BullMQ's \`attempts\` option`],
    answer: 2,
    explanationHtml: `<p>Without randomness, every job that failed at roughly the same time follows the same deterministic delay curve, so their retries arrive as synchronized spikes — a thundering herd — right as the dependency recovers. Jitter (randomizing the delay within a range) spreads retries out so they don't reconverge into another overload.</p>`,
  },
  {
    id: "a10q-event-driven-3",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `A BullMQ Worker crashes mid-job without renewing its lock. What does BullMQ do with that job once \`stalledInterval\` elapses, and what does this imply for processor design?`,
    options: [`The job is permanently marked failed with no retry, requiring manual replay`, `The job is re-queued for another worker to pick up, so processors must be idempotent since side effects from the dead attempt may have already run`, `BullMQ pauses the entire queue until an operator manually intervenes`, `The job's data is deleted to prevent inconsistent state`],
    answer: 1,
    explanationHtml: `<p>The stalled-job checker assumes a worker that stopped heartbeating died mid-processing and re-queues the job for another worker. Because the original worker may have already completed some external side effects (e.g. an API call) before dying, this re-delivery means BullMQ's real guarantee is <b>at-least-once</b>, not exactly-once — processors must be written to be safely re-runnable.</p>`,
  },
  {
    id: "a10q-event-driven-4",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `Why can't you simply publish a message to Kafka/RabbitMQ inside the same application code right after committing a database write, as a substitute for the transactional outbox pattern?`,
    options: [`Message brokers don't support synchronous publish calls`, `There's a failure window between the DB commit and the publish call (e.g. a crash) where the state change happens but no event is ever sent, since the two operations aren't atomic`, `It works fine and is exactly what the outbox pattern does internally`, `Databases don't allow application code to run after a commit`],
    answer: 1,
    explanationHtml: `<p>Committing the DB write and publishing to the broker are two independent operations against two different systems with no shared transaction. If the process crashes (or the broker call simply fails) after the commit but before the publish completes, the state change becomes permanent while the notification is silently lost — exactly the dual-write problem the outbox pattern exists to eliminate by making the event durable in the same local transaction as the state change.</p>`,
  },
  {
    id: "a10q-event-driven-5",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `In @nestjs/cqrs, what is the architecturally correct place to put orchestration logic like "when OrderPlacedEvent fires, dispatch a ReserveInventoryCommand"?`,
    options: [`Inside the OrderPlacedEvent class itself, as a method`, `Inside the QueryHandler that reads order state`, `Inside a Saga, using \`@Saga()\` to map the event Observable to a command Observable`, `Inside the CommandHandler for ReserveInventoryCommand, triggered by a manual EventBus.subscribe() call`],
    answer: 2,
    explanationHtml: `<p>Sagas are the dedicated mechanism in @nestjs/cqrs for event-to-command orchestration: a \`@Saga()\`-decorated method receives an Observable of all published events and maps relevant ones to new commands to dispatch. Domain/business logic should stay in command handlers or the aggregate; the saga's job is purely process flow.</p>`,
  },
  {
    id: "a10q-event-driven-6",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `A team runs three replicas of a NestJS service using @nestjs/cqrs's default EventBus. A command handler on replica A publishes an event, and an \`@EventsHandler\` for that event type is registered only on replica B. What happens?`,
    options: [`The event is delivered to replica B via Redis pub/sub automatically`, `Nest round-robins event delivery across replicas by default`, `The handler on replica B never receives the event — the default EventBus is an in-process RxJS Subject with no cross-process delivery`, `The event is queued and delivered once replica B's next request comes in`],
    answer: 2,
    explanationHtml: `<p>The default @nestjs/cqrs EventBus is backed by an in-memory RxJS Subject scoped to a single process. There's no built-in cross-instance transport, so a handler registered only on another replica simply never sees the event. Production multi-instance deployments either colocate all reactive logic with the publisher or implement a custom \`IEventPublisher\` backed by a real broker.</p>`,
  },
  {
    id: "a10q-event-driven-7",
    category: "queues",
    categoryLabel: "Queues & Jobs",
    question: `What does BullMQ's \`FlowProducer\` (parent/child jobs) provide that adding N independent jobs to a queue does not?`,
    options: [`Guaranteed exactly-once processing for each child job`, `Automatic horizontal scaling of worker count based on child job volume`, `Atomic addition of a parent plus its children, with the parent only becoming active after all children complete and their results aggregated`, `Automatic conversion of failed child jobs into a dead letter queue`],
    answer: 2,
    explanationHtml: `<p>FlowProducer gives you join semantics: the parent job is only processed once every child has completed, and the parent's processor can read aggregated child results via \`job.getChildrenValues()\`. Independently queued jobs have no such barrier/aggregation — you'd have to hand-roll a counter to know when all of them finished.</p>`,
  },
];
