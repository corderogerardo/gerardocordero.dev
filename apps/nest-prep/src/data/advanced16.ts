// Batch 16 — File uploads: Multer integration, FileInterceptor/FilesInterceptor, validating file type/size with a custom ParseFilePipe, streaming large uploads, and direct-to-S3 presigned-URL upload patterns.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED16_FLASHCARDS: Flashcard[] = [
  {
    id: "a16-file-uploads-1",
    category: "core",
    categoryLabel: "Core",
    question: `What does NestJS's <code>FileInterceptor</code> actually do under the hood?`,
    answerHtml: `<p><code>FileInterceptor('field', options)</code> is a factory returning a <code>NestInterceptor</code> that wraps <b>Multer</b> as platform middleware. At request time it invokes Multer's <code>single(fieldName)</code> handler before your route runs, which parses the <code>multipart/form-data</code> body, writes the file via the configured storage engine (memory or disk), and attaches the result to <code>request.file</code>. The interceptor then exposes it through <code>@UploadedFile()</code>. It performs no type/size validation on its own &mdash; that's left to Multer's <code>limits</code>/<code>fileFilter</code> options or a chained <code>ParseFilePipe</code>.</p>`,
  },
  {
    id: "a16-file-uploads-2",
    category: "core",
    categoryLabel: "Core",
    question: `When do you reach for <code>FilesInterceptor</code> vs <code>FileFieldsInterceptor</code> vs <code>AnyFilesInterceptor</code>?`,
    answerHtml: `<ul><li><b>FilesInterceptor('field', maxCount)</b> &mdash; many files under one field name, e.g. a photo gallery upload.</li><li><b>FileFieldsInterceptor([{name, maxCount}, ...])</b> &mdash; several distinct named fields each with its own count, e.g. <code>avatar</code> and <code>resume</code> in one form; populates <code>request.files</code> as an object keyed by field name.</li><li><b>AnyFilesInterceptor()</b> &mdash; accepts any field name with no schema, giving a flat array. Use only when field names are genuinely dynamic, since you lose the structure and validation surface named fields give you.</li></ul>`,
  },
  {
    id: "a16-file-uploads-3",
    category: "core",
    categoryLabel: "Core",
    question: `Why does <code>@UploadedFile()</code> come back <code>undefined</code> even though the client clearly sent a file?`,
    answerHtml: `<p>Almost always a <b>field-name mismatch</b>: <code>FileInterceptor('avatar')</code> only looks at the multipart part literally named <code>avatar</code>; the rest is silently ignored by Multer. The other common cause is the client sending <code>Content-Type: application/json</code> or a manually-set <code>multipart/form-data</code> header without the boundary parameter &mdash; Multer can't parse the body and the field is never populated. Always let the HTTP client set the multipart boundary itself rather than hardcoding the header.</p>`,
  },
  {
    id: "a16-file-uploads-4",
    category: "core",
    categoryLabel: "Core",
    question: `What is the difference between Multer's <code>memoryStorage</code> and <code>diskStorage</code>, and what does each cost you?`,
    answerHtml: `<p><code>memoryStorage</code> buffers the whole file into a <code>Buffer</code> on <code>file.buffer</code>, which is convenient for piping straight to S3 or doing in-memory transforms, but it means request memory usage scales with concurrent upload size &mdash; a handful of large concurrent uploads can OOM a small container. <code>diskStorage</code> streams to a configured directory and only exposes <code>file.path</code>/<code>file.filename</code> (no <code>file.buffer</code>), keeping memory flat regardless of file size, but it requires writable disk, cleanup of orphaned files on failure, and doesn't work on read-only/ephemeral filesystems (e.g. many serverless platforms).</p>`,
  },
  {
    id: "a16-file-uploads-5",
    category: "core",
    categoryLabel: "Core",
    question: `How does NestJS's <code>ParseFilePipe</code> fit into the validation pipeline for an uploaded file?`,
    answerHtml: `<p><code>ParseFilePipe</code> is a built-in pipe you pass to <code>@UploadedFile(new ParseFilePipe({ validators: [...] }))</code>. It runs <i>after</i> Multer has already written/buffered the file but <i>before</i> your handler body executes, iterating its <code>validators</code> array and throwing a <code>BadRequestException</code> (or a custom exception via <code>exceptionFactory</code>) on the first failure. It composes with built-ins like <code>MaxFileSizeValidator</code> and <code>FileTypeValidator</code>, or any class implementing <code>FileValidator</code>.</p>`,
  },
  {
    id: "a16-file-uploads-6",
    category: "core",
    categoryLabel: "Core",
    question: `Is Nest's built-in <code>FileTypeValidator</code> safe to rely on for security-sensitive uploads?`,
    answerHtml: `<p>In current NestJS, <b>yes by default</b>: <code>FileTypeValidator</code> sniffs the file's magic numbers via the <code>file-type</code> package and compares the detected MIME type against your allowlist &mdash; it does not simply trust the client-supplied <code>Content-Type</code> part header. The catch is the two opt-outs: if you pass <code>skipMagicNumbersValidation: true</code> it falls back to matching the raw <code>file.mimetype</code> (fully client-controlled and trivially spoofable), and if no <code>file.buffer</code> is available (which is the case with <code>diskStorage</code>, since it only gives you a path) it can only fall back to the mimetype string when you set <code>fallbackToMimetype: true</code> &mdash; otherwise validation fails closed. So: use <code>memoryStorage</code> (or read the buffer yourself) and leave the defaults alone to get real content-sniffing; explicitly opting into either fallback re-opens the spoofing hole.</p>`,
  },
  {
    id: "a16-file-uploads-7",
    category: "core",
    categoryLabel: "Core",
    question: `Walk through how to write a custom <code>FileValidator</code> for magic-number-based content sniffing.`,
    answerHtml: `<p>Extend the abstract <code>FileValidator&lt;Options&gt;</code> class and implement two members: <code>isValid(file): Promise&lt;boolean&gt;</code>, where you call something like <code>await fileTypeFromBuffer(file.buffer)</code> and compare the detected <code>mime</code> against an allowlist, and <code>buildErrorMessage(file): string</code> for the failure message. Pass an instance into <code>ParseFilePipe</code>'s <code>validators</code> array. NestJS's own built-in <code>FileTypeValidator</code> already does exactly this (via the same <code>file-type</code> package) as of current versions, so a custom validator is mainly worth writing when you need extra checks the built-in doesn't cover &mdash; e.g. rejecting specific allowed-but-risky subtypes, or content-scanning beyond the file signature. Like the built-in, it only works cleanly with <code>memoryStorage</code>, since <code>diskStorage</code> only gives you a file path, requiring a separate read of the first chunk from disk.</p>`,
  },
  {
    id: "a16-file-uploads-8",
    category: "core",
    categoryLabel: "Core",
    question: `How do you make <code>ParseFilePipe</code> return a 400 with your own error shape instead of Nest's default exception?`,
    answerHtml: `<p>Pass <code>exceptionFactory</code> in the options: <code>new ParseFilePipe({ validators: [...], exceptionFactory: (error) =&gt; new BadRequestException({ code: 'UPLOAD_INVALID', message: error }) })</code>. By default <code>ParseFilePipe</code> throws a plain <code>BadRequestException</code> with the validator's error string; <code>exceptionFactory</code> lets you wrap that into a structured error body consistent with the rest of your API's error contract.</p>`,
  },
  {
    id: "a16-file-uploads-9",
    category: "core",
    categoryLabel: "Core",
    question: `What happens if you don't set Multer's <code>limits.fileSize</code>, and why is that dangerous in production?`,
    answerHtml: `<p>Without <code>limits.fileSize</code>, Multer will buffer or stream an upload of <i>any</i> size &mdash; a client can send a multi-gigabyte body and either exhaust container memory (with <code>memoryStorage</code>) or fill disk (with <code>diskStorage</code>), a trivial denial-of-service vector. <code>FileInterceptor('field', { limits: { fileSize: 5 * 1024 * 1024 } })</code> caps it at the Multer layer, which rejects oversized uploads <i>during</i> streaming rather than after the full body has already been received, which is more efficient than only checking size with <code>MaxFileSizeValidator</code> after the fact.</p>`,
  },
  {
    id: "a16-file-uploads-10",
    category: "core",
    categoryLabel: "Core",
    question: `Why is buffering an entire large file in memory before forwarding it to S3 a bad pattern, and what's the streaming alternative?`,
    answerHtml: `<p>With <code>memoryStorage</code> + a single <code>PutObjectCommand</code>, the whole file sits in process memory at once, and several concurrent large uploads can push memory usage past container limits, causing OOM kills. The streaming alternative is the AWS SDK v3 <code>@aws-sdk/lib-storage</code> <code>Upload</code> class, which accepts a readable stream and internally performs S3 <b>multipart upload</b> &mdash; splitting into parts uploaded as they arrive, keeping a bounded memory footprint regardless of total file size, with built-in retry per part.</p>`,
  },
  {
    id: "a16-file-uploads-11",
    category: "core",
    categoryLabel: "Core",
    question: `What is a presigned URL, and what problem does it solve for file uploads compared to routing the file through your NestJS API?`,
    answerHtml: `<p>A presigned URL is a time-limited, cryptographically signed URL generated server-side (via <code>getSignedUrl</code> from <code>@aws-sdk/s3-request-presigner</code>) that grants the bearer permission to perform one specific S3 operation (e.g. <code>PutObject</code> to one key) without needing AWS credentials themselves. The client uploads <b>directly to S3</b>, bypassing your API entirely for the file bytes. This solves the problem of your Node process being a bandwidth/memory bottleneck and a single point of failure for every upload, and removes the need to ever hold AWS credentials client-side.</p>`,
  },
  {
    id: "a16-file-uploads-12",
    category: "core",
    categoryLabel: "Core",
    question: `Describe the request flow for a direct-to-S3 presigned-URL upload, end to end.`,
    answerHtml: `<p>(1) Client calls a Nest endpoint like <code>POST /uploads/presign</code> with metadata (filename, content type, size). (2) The service validates that metadata, generates a unique S3 key (never trust a client-supplied key/path), and calls <code>getSignedUrl(s3Client, new PutObjectCommand({ Bucket, Key, ContentType }), { expiresIn: 60 })</code>. (3) The API returns the presigned URL (and the key) to the client. (4) The client issues a <code>PUT</code> directly to that URL with the file body. (5) The client notifies the API the upload completed (or the API polls/uses an S3 event notification) so it can persist a DB record pointing at the object.</p>`,
  },
  {
    id: "a16-file-uploads-13",
    category: "core",
    categoryLabel: "Core",
    question: `How do you prevent a presigned PUT URL from being used to upload an arbitrarily large or wrong-type file, since the API never sees the bytes?`,
    answerHtml: `<p>S3 presigned URLs support constraining the signed request: when generating with <code>PutObjectCommand</code>, you can pin <code>ContentType</code> and (via <code>ContentLength</code> conditions on a presigned <b>POST</b> policy rather than a PUT) a size range, and S3 will reject the upload if the actual request doesn't match. For finer-grained control (exact byte-range limits, multiple field conditions), use <code>createPresignedPost</code> instead of a presigned PUT, which lets you set explicit <code>conditions</code> like <code>['content-length-range', 0, 5242880]</code>. Whatever you don't enforce at signing time, you must re-validate after the fact via an S3 event trigger (e.g. Lambda) that inspects the uploaded object and deletes/flags it if it violates policy.</p>`,
  },
  {
    id: "a16-file-uploads-14",
    category: "core",
    categoryLabel: "Core",
    question: `Why is it unsafe to let the client dictate the final S3 object key in a presigned-upload flow?`,
    answerHtml: `<p>If the API trusts a client-supplied key verbatim, a malicious client can pass a path that <b>overwrites another user's object</b> (key collision/IDOR) or path-traverses into a prefix it shouldn't have access to, since S3 keys are just strings with no enforced ownership at the object level. The fix is to always generate the key server-side &mdash; typically <code>{userId}/{uuid}-{sanitizedFilename}</code> &mdash; and only let presigning attach to that generated key, never to client input.</p>`,
  },
  {
    id: "a16-file-uploads-15",
    category: "core",
    categoryLabel: "Core",
    question: `What is the role of S3 bucket CORS configuration in a direct-to-S3 presigned upload, and what commonly breaks here?`,
    answerHtml: `<p>Since the browser issues the <code>PUT</code>/<code>POST</code> directly to <code>*.s3.amazonaws.com</code>, that's a cross-origin request from your app's domain, so the bucket's CORS policy must explicitly allow your origin, the <code>PUT</code> (or <code>POST</code>) method, and the headers you sign (commonly <code>Content-Type</code>). The most common failure mode is signing the URL with headers the CORS policy doesn't list under <code>AllowedHeaders</code>, which causes the preflight <code>OPTIONS</code> to fail in-browser even though the same request works fine from a server or curl.</p>`,
  },
  {
    id: "a16-file-uploads-16",
    category: "core",
    categoryLabel: "Core",
    question: `When validating an uploaded file's MIME type, why can't you just trust the client-declared content type for any security-relevant decision?`,
    answerHtml: `<p>Whatever the client puts in the <code>Content-Type</code> header of a multipart part (surfaced as <code>file.mimetype</code> on Multer's <code>File</code> object) is fully client-controlled and can be spoofed trivially &mdash; it's a hint, not a guarantee, of the actual bytes. Trusting it for any decision that affects how the file is later served or executed (e.g. allowing SVG/HTML uploads that get rendered, or deciding whether to set an inline <code>Content-Disposition</code>) opens stored-XSS and content-spoofing vectors. That's exactly why current NestJS's built-in <code>FileTypeValidator</code> sniffs magic bytes via the <code>file-type</code> package by default instead of trusting <code>mimetype</code> &mdash; real validation requires inspecting the actual bytes, whether via that default or a custom validator.</p>`,
  },
  {
    id: "a16-file-uploads-17",
    category: "core",
    categoryLabel: "Core",
    question: `Why are SVG uploads a special-case security risk even after you've validated the MIME type correctly?`,
    answerHtml: `<p>SVG is a legitimate image MIME type (<code>image/svg+xml</code>) but its payload is <b>XML that can embed <code>&lt;script&gt;</code> tags or event handlers</b>. A correctly-typed, magic-number-validated SVG can still be a stored XSS payload if it's ever served with a content type that lets a browser render/execute it inline. Mitigations: serve uploaded SVGs with <code>Content-Disposition: attachment</code>, sanitize the SVG markup server-side (strip scripts/foreign objects) before storage, or simply exclude SVG from the allowlist for user-generated content.</p>`,
  },
  {
    id: "a16-file-uploads-18",
    category: "core",
    categoryLabel: "Core",
    question: `What's the difference between handling file uploads with the Express adapter vs the Fastify adapter in NestJS, and why does it matter?`,
    answerHtml: `<p>On the Express adapter, Nest's file interceptors use <code>multer</code> directly. On the Fastify adapter, Nest uses <code>@fastify/multipart</code> under <code>@nest/platform-fastify</code>'s interceptor implementation instead, because Fastify doesn't run Express middleware. The decorators (<code>@UploadedFile()</code>, <code>FileInterceptor</code>) present the same API, but registering the underlying plugin (<code>app.register(fastifyMultipart)</code>) and some option names differ, so code that assumes Multer-specific storage engine options can silently fail to apply if the app is later switched to Fastify.</p>`,
  },
  {
    id: "a16-file-uploads-19",
    category: "core",
    categoryLabel: "Core",
    question: `How should you validate the <code>Content-Length</code>/declared size of a file before allowing a stream to start writing to disk or S3, to avoid resource exhaustion?`,
    answerHtml: `<p>Check the client-declared size as an early, cheap rejection (reject obviously-oversized requests via the <code>Content-Length</code> header or a metadata field before any bytes are streamed), but never trust it as the sole guard, since it can be omitted, wrong, or the body can be chunked-encoded. The authoritative guard is a hard limit enforced <i>while streaming</i> &mdash; Multer's <code>limits.fileSize</code>, or for direct-to-S3, a presigned POST's <code>content-length-range</code> condition &mdash; so the upload is aborted mid-stream the moment it exceeds the cap rather than after the full body lands.</p>`,
  },
  {
    id: "a16-file-uploads-20",
    category: "core",
    categoryLabel: "Core",
    question: `Why might you choose a presigned-URL pattern over routing uploads through your NestJS server even for moderately small files, from an architecture standpoint?`,
    answerHtml: `<p>Even small files add up under concurrency: every upload through the API occupies a Node connection/event-loop slot and counts against your container's CPU/memory and any load balancer's request-duration budget, and it forces your API instances to scale with upload traffic rather than business logic traffic. Presigned uploads decouple those concerns &mdash; S3 absorbs the bandwidth and concurrency, your API only does cheap signing calls, and you can horizontally scale the two independently.</p>`,
  },
  {
    id: "a16-file-uploads-21",
    category: "core",
    categoryLabel: "Core",
    question: `What does <code>FileTypeValidator</code>'s constructor option actually accept, and what's a common mistake when configuring it?`,
    answerHtml: `<p><code>new FileTypeValidator({ fileType: 'image/jpeg' })</code> accepts a string or regex matched against the file's <b>magic-number-detected</b> MIME type by default (via the <code>file-type</code> package), falling back to the declared <code>mimetype</code> only if you opt in with <code>fallbackToMimetype</code> (when no buffer is available, e.g. <code>diskStorage</code>) or <code>skipMagicNumbersValidation</code>. A common mistake is passing a bare extension like <code>'jpeg'</code> expecting it to match the file extension &mdash; it's matched as a MIME-type pattern, so an unanchored regex like <code>/jpeg/</code> can unintentionally also match unrelated types containing that substring, e.g. a crafted/declared <code>Content-Type: application/jpeg-exploit</code> in a fallback path.</p>`,
  },
  {
    id: "a16-file-uploads-22",
    category: "core",
    categoryLabel: "Core",
    question: `In a direct-to-S3 flow, how does your database end up knowing an upload actually succeeded, given the API never sees the file bytes?`,
    answerHtml: `<p>Two common patterns: (1) <b>client-confirmed</b> &mdash; after the direct <code>PUT</code> to S3 returns 200, the client calls back to the API (e.g. <code>POST /uploads/:id/complete</code>) which can issue a <code>HeadObjectCommand</code> to confirm the object exists before marking the DB row complete; this is simple but trusts the client to call back. (2) <b>event-driven</b> &mdash; S3 emits an <code>s3:ObjectCreated</code> event to SQS/EventBridge/Lambda, which updates the DB record server-side without relying on the client at all, closing the gap where a client could abandon the flow after uploading (or never uploading) and leave the API's state inconsistent.</p>`,
  },
];

export const ADVANCED16_QUIZ: QuizQuestion[] = [
  {
    id: "a16q-file-uploads-1",
    category: "core",
    categoryLabel: "Core",
    question: `A NestJS endpoint uses <code>@UseInterceptors(FileInterceptor('avatar'))</code> with default options and no <code>ParseFilePipe</code>. A client uploads a 2GB file. What happens?`,
    options: [`Nest automatically rejects it because the default body limit is 1MB`, `Multer buffers/streams the entire 2GB file with no size restriction, since no <code>limits.fileSize</code> or validator was configured`, `TypeScript's type system prevents files larger than the DTO's declared size`, `The request hangs indefinitely until the client disconnects`],
    answer: 1,
    explanationHtml: `<p>Without <code>limits: { fileSize }</code> on the interceptor options or a <code>MaxFileSizeValidator</code> in a <code>ParseFilePipe</code>, Multer has no built-in cap and will process the full upload, which is a real DoS/resource-exhaustion risk in production.</p>`,
  },
  {
    id: "a16q-file-uploads-2",
    category: "core",
    categoryLabel: "Core",
    question: `By default, how does current NestJS's built-in <code>FileTypeValidator</code> determine a file's type?`,
    options: [`It always trusts the client-supplied <code>Content-Type</code> header verbatim, with no way to change that`, `It sniffs the file's magic numbers via the <code>file-type</code> package against <code>file.buffer</code>, only falling back to the client-declared <code>mimetype</code> if you explicitly opt in (<code>fallbackToMimetype</code> or <code>skipMagicNumbersValidation</code>) or no buffer is available`, `It uploads the file to a third-party virus-scanning API before allowing it through`, `It infers type purely from the file extension in the original filename`],
    answer: 1,
    explanationHtml: `<p>As of current NestJS, <code>FileTypeValidator</code> defaults to magic-number sniffing via <code>file-type</code> (<code>skipMagicNumbersValidation: false</code>) &mdash; it does not simply trust the spoofable <code>mimetype</code> header. The mimetype-only behavior only kicks in if you opt out of magic-number checking, or the buffer isn't available (e.g. <code>diskStorage</code>) and you set <code>fallbackToMimetype: true</code>.</p>`,
  },
  {
    id: "a16q-file-uploads-3",
    category: "core",
    categoryLabel: "Core",
    question: `What is the primary architectural benefit of a direct-to-S3 presigned-URL upload over routing the file body through the NestJS API?`,
    options: [`It removes the need for any server-side authorization checks`, `It guarantees the file is virus-scanned automatically by S3`, `The API never has to absorb the upload's bandwidth/memory cost; S3 handles the transfer directly with the client`, `It allows skipping HTTPS for the upload request`],
    answer: 2,
    explanationHtml: `<p>The API only does a cheap signing call; the actual file bytes flow directly from client to S3, so the API's compute/memory/connection budget doesn't scale with upload traffic. Authorization for <i>who can request a presigned URL</i> still has to happen server-side before signing.</p>`,
  },
  {
    id: "a16q-file-uploads-4",
    category: "core",
    categoryLabel: "Core",
    question: `Why should the S3 object key in a presigned-upload flow always be generated server-side rather than accepted from the client?`,
    options: [`S3 keys must be valid UUIDs or the SDK throws`, `Client-supplied keys can't contain slashes`, `A client-controlled key lets an attacker overwrite another user's existing object or write outside their intended prefix`, `Server-generated keys are required for S3 versioning to function`],
    answer: 2,
    explanationHtml: `<p>S3 has no per-object ownership enforcement beyond IAM/bucket policy on the key namespace, so if the client picks the key, it can collide with or overwrite another user's object. The server must derive the key (e.g. from the authenticated user ID plus a generated UUID).</p>`,
  },
  {
    id: "a16q-file-uploads-5",
    category: "core",
    categoryLabel: "Core",
    question: `An upload endpoint buffers files into memory with <code>memoryStorage</code> and forwards them to S3 with a single <code>PutObjectCommand</code> built from <code>file.buffer</code>. Under load with several large concurrent uploads, what is the most likely failure mode?`,
    options: [`S3 silently truncates large objects`, `The Node process runs out of memory because every concurrent upload is fully buffered before any bytes are sent onward`, `Multer automatically streams in this configuration, so there is no risk`, `TypeScript throws a compile-time error for large buffers`],
    answer: 1,
    explanationHtml: `<p><code>memoryStorage</code> holds the whole file as a <code>Buffer</code> in process memory; with <code>PutObjectCommand</code> (not the multipart <code>Upload</code> helper) nothing streams incrementally, so memory usage is proportional to total concurrent upload size and can OOM the process.</p>`,
  },
  {
    id: "a16q-file-uploads-6",
    category: "core",
    categoryLabel: "Core",
    question: `Which statement correctly distinguishes <code>FilesInterceptor</code> from <code>FileFieldsInterceptor</code> in NestJS?`,
    options: [`<code>FilesInterceptor</code> handles multiple files under a single field name; <code>FileFieldsInterceptor</code> handles multiple distinct named fields, each with its own max count`, `They are aliases for the same implementation with no functional difference`, `<code>FileFieldsInterceptor</code> only works with Fastify, while <code>FilesInterceptor</code> only works with Express`, `<code>FilesInterceptor</code> requires a <code>ParseFilePipe</code> while <code>FileFieldsInterceptor</code> does not`],
    answer: 0,
    explanationHtml: `<p><code>FilesInterceptor('photos', 10)</code> expects up to 10 files all under the <code>photos</code> field, populating an array. <code>FileFieldsInterceptor([{name:'avatar',maxCount:1},{name:'resume',maxCount:1}])</code> expects distinct fields, populating <code>request.files</code> as an object keyed by field name.</p>`,
  },
  {
    id: "a16q-file-uploads-7",
    category: "core",
    categoryLabel: "Core",
    question: `In a presigned POST upload to S3, how can you enforce a maximum file size without ever routing the bytes through your NestJS API?`,
    options: [`Set a <code>MaxFileSizeValidator</code> in a <code>ParseFilePipe</code> on the presign endpoint`, `Pass a <code>content-length-range</code> condition when generating the presigned POST policy, so S3 itself rejects oversized uploads`, `It is not possible; size can only be enforced after download`, `Rely on the client's JavaScript to check <code>file.size</code> before uploading`],
    answer: 1,
    explanationHtml: `<p><code>createPresignedPost</code> supports a <code>conditions</code> array including <code>['content-length-range', min, max]</code>, which S3 enforces server-side at upload time regardless of what the client's JS does (client-side checks are only a UX nicety, not a security boundary).</p>`,
  },
];
