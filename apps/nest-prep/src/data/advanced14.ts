// Batch 14 — Request-scoped providers & their performance cost, AsyncLocalStorage for request context (multi-tenancy, correlation ids without prop-drilling), and module boundary design for a modular monolith.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED14_FLASHCARDS: Flashcard[] = [
  {
    id: "a14-multi-tenancy-1",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What exactly does REQUEST scope cost you in NestJS, mechanically — not just "it's slower"?`,
    answerHtml: `<p>A <code>DEFAULT</code> (singleton) provider is instantiated once at bootstrap and its entire dependency subtree is built once. A <code>REQUEST</code>-scoped provider forces Nest to <b>re-instantiate that provider and every provider that depends on it</b>, for every single inbound request — Nest walks back up the injection graph and scope-bubbles: any provider injecting a request-scoped provider becomes request-scoped too, transitively. Concretely that's a new object graph constructed (and GC'd) per request instead of per process, plus Nest has to resolve the request-scoped subtree from a per-request DI sub-container rather than a cached singleton lookup.</p>`,
  },
  {
    id: "a14-multi-tenancy-2",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why does injecting one REQUEST-scoped provider into a controller make the whole controller request-scoped, and why does that matter for a hot path?`,
    answerHtml: `<p>Nest's DI resolves scope bottom-up: a consumer's effective scope is the loosest (most ephemeral) scope among its dependencies. If <code>OrderController</code> injects a request-scoped <code>TenantContextService</code>, the controller itself can no longer be a singleton — Nest must construct a new <code>OrderController</code> instance per request too. If that controller also depends on five other services, and any of <i>those</i> depend on the same request-scoped provider, the contagion spreads further up the graph than people expect. On a high-RPS endpoint this turns one cheap DI lookup into a recursive re-construction of a large subgraph, request after request.</p>`,
  },
  {
    id: "a14-multi-tenancy-3",
    category: "arch",
    categoryLabel: "Architecture",
    question: `How would you quantify the perf cost of REQUEST scope before deciding whether to use it?`,
    answerHtml: `<p>Don't guess — benchmark with autocannon/k6 against the same route with the provider scoped <code>DEFAULT</code> vs <code>REQUEST</code>. In practice the overhead is dominated by constructor/object-allocation cost across the affected subtree, so it scales with how many providers got dragged into request scope, not with the framework itself. A single request-scoped leaf provider with few dependents is often negligible; making a widely-injected, deep service (e.g. your core <code>DatabaseService</code>) request-scoped is the version that shows up in p99 latency and GC pressure under load.</p>`,
  },
  {
    id: "a14-multi-tenancy-4",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What is the standard escape hatch for needing per-request data (current user, tenant id) without paying request scope on your services?`,
    answerHtml: `<p>Use <code>AsyncLocalStorage</code> (Node's continuation-local storage) to carry per-request context implicitly through the call stack, and keep all providers <code>DEFAULT</code>-scoped. A single middleware wraps each request in <code>als.run(store, next)</code>; any singleton service can later call <code>als.getStore()</code> to read the current request's data without it being threaded through constructors or method parameters. This decouples "per-request data" from "per-request object lifetime" — you get the data without re-instantiating the dependency graph.</p>`,
  },
  {
    id: "a14-multi-tenancy-5",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Mechanically, how does AsyncLocalStorage manage to make a value available deep in a call stack without passing it as an argument?`,
    answerHtml: `<p><code>AsyncLocalStorage</code> hooks into Node's <code>async_hooks</code> machinery, which tracks the parent-child relationship between async resources (promises, timers, the underlying continuation of an async function). When you call <code>als.run(store, callback)</code>, the store is associated with the <i>async execution context</i> that <code>callback</code> and everything it asynchronously spawns belongs to — not with a specific function call frame. Any code running later inside that same logical chain of awaits/callbacks, however deeply nested, can call <code>als.getStore()</code> and get the same store object back, because the runtime propagates the association across each async boundary (microtask, <code>setTimeout</code>, promise continuation) automatically.</p>`,
  },
  {
    id: "a14-multi-tenancy-6",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What's the real performance profile of AsyncLocalStorage in modern Node — is it free?`,
    answerHtml: `<p>It is not free, but it's cheap and has improved significantly. Older Node versions had measurable overhead from <code>async_hooks</code> tracking every async resource. Node 16+ rewrote <code>AsyncLocalStorage</code> to avoid the old <code>AsyncWrap</code> overhead for the common case, and recent benchmarks (Node 22–24) put the cost of robust context propagation at roughly single-digit percent overhead on throughput, with Node 24 narrowing it further. It's still nonzero — every promise/timer in the request's lifetime carries a bit of bookkeeping — so for ultra-hot, allocation-sensitive paths you measure, but it is the recommended default over request-scoped DI for context propagation.</p>`,
  },
  {
    id: "a14-multi-tenancy-7",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Sketch the implementation pattern for request-scoped context via AsyncLocalStorage in a NestJS app: where does the store get populated, and what's in it?`,
    answerHtml: `<p>A global middleware (or an interceptor, if you need access to the resolved route/handler metadata first) creates a fresh store object per request — typically <code>{ requestId, tenantId, userId, traceId }</code> — and calls <code>als.run(store, () => next())</code>. The store is usually a plain object or <code>Map</code> so later code can both read and mutate it (e.g. attaching the authenticated user after a guard runs). A thin singleton wrapper service (often itself called something like <code>ClsService</code> or a hand-rolled <code>RequestContextService</code>) exposes <code>get()</code>/<code>set()</code> over the underlying ALS instance so consumers don't import <code>async_hooks</code> directly.</p>`,
  },
  {
    id: "a14-multi-tenancy-8",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What does the \`nestjs-cls\` package give you over a hand-rolled AsyncLocalStorage wrapper?`,
    answerHtml: `<p><code>nestjs-cls</code> wraps <code>AsyncLocalStorage</code> behind a <code>ClsModule</code>/<code>ClsService</code> with Nest-idiomatic ergonomics: a middleware or interceptor that auto-initializes the store per request, a <code>setup()</code> hook to seed it (e.g. extract <code>x-tenant-id</code>), typed get/set, and integration points for guards/interceptors/microservice transports (not just HTTP). It also offers a Proxy Provider API (<code>@InjectableProxy()</code>) that lets you inject a singleton proxy which delegates to a per-request instance under the hood — giving you the DX of request scope without Nest re-creating the whole DI subtree per request.</p>`,
  },
  {
    id: "a14-multi-tenancy-9",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why is AsyncLocalStorage specifically the right tool for multi-tenancy context (tenant id, tenant DB connection) versus passing tenant id as a method parameter everywhere?`,
    answerHtml: `<p>Prop-drilling tenant id through every service method signature pollutes the domain API (every method gains a parameter that has nothing to do with its business logic) and is easy to forget at any new call site — a forgotten parameter silently defaults to undefined rather than failing loudly. ALS instead makes tenant context an ambient property of "the current request," readable from anywhere in the call chain (including deep in a repository or a query builder) without changing any function signature. The risk it trades in is implicitness: a method's tenant-safety is no longer visible from its type signature, so you need a deliberate convention (e.g. always resolve tenant context in a single low-level data-access chokepoint) rather than scattering raw ALS reads.</p>`,
  },
  {
    id: "a14-multi-tenancy-10",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What's the most dangerous bug class when using AsyncLocalStorage for multi-tenant row-level scoping, and how do you defend against it?`,
    answerHtml: `<p>Cross-tenant data leakage: a query path that doesn't go through the chokepoint that applies the tenant filter, or a piece of code that runs outside the ALS context (a bootstrap-time singleton, a queue consumer that didn't re-establish context) silently returns or writes unscoped data. Defenses: (1) never trust ALS alone — pair it with a DB-level guarantee like Postgres Row-Level Security so a missing application filter still can't leak rows, (2) centralize all tenant-scoped queries through one repository layer that reads ALS and fails closed (throws) if no tenant is in context rather than running unscoped, (3) explicitly re-seed the ALS store at the entry point of every execution context — HTTP middleware, queue job handler, cron job — since ALS context does not survive a true process boundary (a new job consumer call) unless you re-run it.</p>`,
  },
  {
    id: "a14-multi-tenancy-11",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Does AsyncLocalStorage context survive into a BullMQ job processor or a Kafka consumer handler automatically?`,
    answerHtml: `<p>No. ALS context propagates along the chain of async operations rooted in the <code>als.run()</code> call — once a request finishes and its response is sent, that async chain ends. A queue job processed later by a worker (possibly in the same process, possibly a different one) starts a brand-new async context with nothing in it. If you need the original request's correlation id or tenant id inside the job, you must explicitly serialize it into the job payload when enqueuing, then call <code>als.run()</code> again at the start of the job processor to re-establish a (new) ALS context from that payload.</p>`,
  },
  {
    id: "a14-multi-tenancy-12",
    category: "arch",
    categoryLabel: "Architecture",
    question: `How does correlation-id propagation via AsyncLocalStorage interact with distributed tracing (OpenTelemetry / traceparent headers)?`,
    answerHtml: `<p>They solve adjacent but distinct problems. OpenTelemetry's Node SDK context API is itself built on <code>AsyncLocalStorage</code> under the hood — its default context manager, <code>AsyncLocalStorageContextManager</code> (from <code>@opentelemetry/context-async-hooks</code>), uses ALS to propagate the active span across async boundaries within one process, which is how auto-instrumentation attaches child spans to the right parent without manual threading. Your own correlation id (often equal to or derived from the trace id) is typically just stashed in the same or a sibling ALS store so application logs can be tagged with it. Cross-process propagation is a separate concern handled by the <code>traceparent</code> HTTP header (W3C Trace Context) — ALS never crosses a network call by itself; you must read it and serialize it into outgoing request headers, and the receiving service must parse it back into its own ALS/OTel context.</p>`,
  },
  {
    id: "a14-multi-tenancy-13",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Contrast REQUEST scope and AsyncLocalStorage on testability — which is easier to unit test and why?`,
    answerHtml: `<p>REQUEST-scoped providers are awkward to unit test in isolation because their constructor depends on the injected <code>REQUEST</code> object (or <code>CONTEXT</code> for GraphQL/microservices), so a plain <code>new MyService(mockRequest)</code> works but anything using Nest's testing module needs <code>ContextIdFactory</code> tricks to simulate per-request resolution. ALS-based context is arguably easier to test at the unit level — you call <code>als.run(fakeStore, () => testFn())</code> to set up context for a single test — but it introduces a different testability risk: the dependency on context is invisible in the function's signature, so a test can pass while forgetting to set up ALS at all if the code path doesn't actually touch it, masking missing-context bugs until production.</p>`,
  },
  {
    id: "a14-multi-tenancy-14",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What is TRANSIENT scope in NestJS, and how is it different from both DEFAULT and REQUEST?`,
    answerHtml: `<p><code>TRANSIENT</code> providers get a new instance every time they're <i>injected</i> — i.e., each consumer gets its own private instance, but unlike <code>REQUEST</code> scope, that instantiation is not tied to an inbound request lifecycle; it happens once per injection at module-resolution time, not once per HTTP call. It's the right tool for stateful-but-not-request-bound utilities (e.g. a builder object, a per-consumer logger context) where you want isolation between consumers without paying the per-request re-instantiation cost that <code>REQUEST</code> scope implies.</p>`,
  },
  {
    id: "a14-multi-tenancy-15",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Can a WebSocket gateway in NestJS use a REQUEST-scoped provider the same way an HTTP controller can, and what is the \`CONTEXT\` token actually for?`,
    answerHtml: `<p>No — the official guidance is the opposite: gateways should generally avoid request-scoped providers because a <code>@WebSocketGateway()</code> must behave as a singleton, encapsulating one real underlying socket server, and can't be re-instantiated per message the way a controller is re-instantiated per HTTP request; there's no WebSocket equivalent of the <code>REQUEST</code> token. The <code>CONTEXT</code> token instead exists for <i>other</i> non-HTTP transports: <code>CONTEXT</code> from <code>@nestjs/microservices</code> gives a request-scoped provider access to an inbound message's pattern/payload, and <code>CONTEXT</code> from <code>@nestjs/graphql</code> gives access to the GraphQL execution context — both are HTTP-request analogues for microservices and GraphQL specifically, not for gateways.</p>`,
  },
  {
    id: "a14-multi-tenancy-16",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What problem does \`ContextIdFactory.create()\` / a custom \`ContextIdStrategy\` solve, and when would you reach for it?`,
    answerHtml: `<p>By default, Nest creates a brand-new request-scoped DI subtree for every inbound request — even if 1,000 requests logically belong to the same unit of work. A custom <code>ContextIdStrategy</code> (registered via <code>ContextIdFactory.apply()</code>) lets you control how Nest buckets requests into DI contexts — for example, the documented "durable providers" pattern: tag a tenant-scoped provider as <code>durable: true</code> so Nest reuses one DI subtree per <i>tenant</i> instead of per request, cutting the request-scope re-instantiation cost down from O(requests) to O(tenants) while still giving each tenant isolated instances. This approach scales to however many distinct tenants you keep subtrees for in memory at once — it's not meant for unboundedly large tenant counts without an eviction strategy.</p>`,
  },
  {
    id: "a14-multi-tenancy-17",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What are NestJS "durable providers," and what specific REQUEST-scope cost do they avoid?`,
    answerHtml: `<p>Durable providers (<code>{ scope: Scope.REQUEST, durable: true }</code> combined with a custom <code>ContextIdStrategy</code>) let request-scoped DI subtrees be keyed by a stable dimension you choose — typically tenant id — rather than by the raw, ever-unique request. Once a tenant's subtree is built, subsequent requests from the same tenant reuse it instead of triggering a fresh construction of the whole dependency chain. This is the direct fix for the "request scope re-instantiates everything every time" cost in a multi-tenant app where you genuinely want tenant-scoped instances (e.g. a tenant-specific Prisma client) but don't want per-request construction overhead.</p>`,
  },
  {
    id: "a14-multi-tenancy-18",
    category: "arch",
    categoryLabel: "Architecture",
    question: `In a modular monolith, what defines a "module boundary" in NestJS terms, and what's the failure mode when it's not enforced?`,
    answerHtml: `<p>A module boundary should map to a bounded context: the module owns its own entities/tables, exposes a small, deliberate public API (the providers it puts in <code>exports</code>), and everything else stays private (declared but not exported). The failure mode without enforcement is the "distributed monolith" or "big ball of mud" — modules import each other's internal services directly (because Nest's DI doesn't stop you from exporting everything), domain logic for one bounded context leaks into another's services, and eventually no module can be understood, tested, refactored, or extracted to its own service without touching the rest of the codebase.</p>`,
  },
  {
    id: "a14-multi-tenancy-19",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Nest's module system lets you export anything from a module. Why isn't that enough to enforce module boundaries, and what closes the gap?`,
    answerHtml: `<p><code>exports</code> controls what's <i>injectable elsewhere via DI</i>, but nothing stops a developer from reaching past it with a raw TypeScript import — \`import { OrderInternalRepository } from '../orders/order-internal.repository'\` compiles fine and bypasses the module's public API entirely, because TypeScript's module system and Nest's <code>@Module()</code> system are two separate, unconnected boundaries. Closing the gap needs a static-analysis layer on top: <code>dependency-cruiser</code> or ESLint's <code>import/no-restricted-paths</code> / <code>eslint-plugin-boundaries</code> configured so that, e.g., files under <code>src/orders/internal/**</code> cannot be imported from outside <code>src/orders/**</code>, enforced in CI so the architecture is actually load-bearing rather than a convention people forget.</p>`,
  },
  {
    id: "a14-multi-tenancy-20",
    category: "arch",
    categoryLabel: "Architecture",
    question: `In a modular monolith, how should two modules that need to talk to each other be wired — direct cross-module service injection, or something more deliberate?`,
    answerHtml: `<p>The lazy-but-correct default is: Module A imports Module B's \`*.module.ts\`, and only injects the small set of providers B explicitly exports as its public API (often a single \`XxxFacadeService\` or \`XxxPublicService\`, not B's internal repositories). This keeps the call in-process (cheap, transactional if needed) while still respecting the bounded context — A is coupled to B's contract, not its implementation. The escape hatch for looser coupling (e.g. to make a future extraction to a real microservice painless) is to have B emit domain events (via Nest's \`EventEmitter2\` or an outbox) that A subscribes to, trading immediate consistency for decoupling — reserve that for cases where A reacting synchronously isn't actually required.</p>`,
  },
  {
    id: "a14-multi-tenancy-21",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What's the concrete trigger for splitting a module out of a modular monolith into its own deployable service — and what doesn't qualify?`,
    answerHtml: `<p>Qualifying triggers are operational, not aesthetic: the module needs independent scaling (it's CPU/memory-hungry and drags the rest of the app's autoscaling with it), independent deployment cadence (a different team ships it many times a day and can't wait on the monolith's release train), fault isolation (you need a crash in this code to not take down unrelated endpoints), or a genuine team-ownership/Conway's Law split. "The code feels big" or "it would be cleaner as its own thing" do not qualify — that's a refactor inside the monolith, not an extraction, and premature extraction pays full distributed-systems tax (network calls, partial failure, eventual consistency, deploy coordination) for zero benefit.</p>`,
  },
  {
    id: "a14-multi-tenancy-22",
    category: "arch",
    categoryLabel: "Architecture",
    question: `When you do extract a well-bounded Nest module to its own service, why does respecting module boundaries up front make that extraction "easy"?`,
    answerHtml: `<p>If the module's only point of contact with the rest of the app was its exported public API (a facade interface), extraction is a matter of standing up that same interface behind a transport client (HTTP/gRPC/message broker) instead of an in-process Nest provider — callers were already coded against the interface, not the implementation, so swapping the injected implementation is a DI wiring change, not a rewrite of every call site. This is the practical payoff of the ports-and-adapters discipline: the “port” (interface + DI token) stays identical, only the “adapter” (in-process vs. network) changes.</p>`,
  },
  {
    id: "a14-multi-tenancy-23",
    category: "arch",
    categoryLabel: "Architecture",
    question: `How do circular module dependencies happen in Nest, and what are the two ways to break them?`,
    answerHtml: `<p>They happen when two modules each need a provider from the other — e.g. \`UsersModule\` needs \`OrdersService\` for a user's order history, and \`OrdersModule\` needs \`UsersService\` to validate the buyer — creating an import cycle Nest can't resolve at module-instantiation time without help. Nest's built-in escape hatch is \`forwardRef()\` on both sides of the \`imports\` array and the constructor injection, which defers resolution until both modules exist (Nest's own docs warn this combines badly with \`Scope.REQUEST\` dependencies, which can resolve as undefined). The architecturally better fix is usually to recognize the cycle as a boundary smell: extract the shared concern into a third module both depend on one-way, or replace the synchronous cross-call with an event (one module emits, the other listens) so the dependency arrow only points one direction.</p>`,
  },
  {
    id: "a14-multi-tenancy-24",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why is "one module per bounded context, not one module per technical layer" the right way to organize a modular monolith?`,
    answerHtml: `<p>Layer-based organization (a top-level \`controllers/\`, \`services/\`, \`repositories/\` per the whole app) scatters a single business capability across folders that have no enforced relationship, so there's no natural boundary to enforce or extract — every "feature" silently depends on every other because they all live in the same flat layer. Bounded-context modules (\`orders/\`, \`billing/\`, \`inventory/\`, each with its own controllers/services/entities inside) make the dependency graph explicit: you can see and lint which modules import which, and a module is a real unit you could plausibly delete, replace, or extract.</p>`,
  },
];

export const ADVANCED14_QUIZ: QuizQuestion[] = [
  {
    id: "a14q-multi-tenancy-1",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Injecting a REQUEST-scoped provider into a singleton (DEFAULT-scoped) controller in NestJS results in:`,
    options: [`A compile-time error, since scopes must match exactly`, `The controller silently stays a singleton and shares the request-scoped instance across requests`, `The controller's effective scope bubbles up to REQUEST, so it too is re-instantiated per request`, `Nest throws at runtime unless the provider is also marked TRANSIENT`],
    answer: 2,
    explanationHtml: `<p>Nest resolves a consumer's effective scope as the loosest scope among its dependencies. A controller injecting a request-scoped provider becomes request-scoped itself, and that effect propagates further up the injection graph to anything that depends on the controller — this is the main mechanism behind request scope's cost: it's not localized to the one provider you marked, it spreads.</p>`,
  },
  {
    id: "a14q-multi-tenancy-2",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What is the primary advantage of using AsyncLocalStorage over REQUEST-scoped providers for carrying per-request context like tenant id or correlation id?`,
    options: [`AsyncLocalStorage is part of the HTTP spec, so it works across network calls automatically`, `It avoids re-instantiating the dependency graph per request — providers stay singletons while still getting per-request data via an ambient store`, `AsyncLocalStorage guarantees stronger type safety than constructor injection`, `It removes the need for any kind of request-scoped state entirely`],
    answer: 1,
    explanationHtml: `<p>The whole point is decoupling "per-request data" from "per-request object lifetime." Services stay <code>DEFAULT</code>-scoped (built once), and <code>AsyncLocalStorage.getStore()</code> gives any of them ambient access to the current request's context without DI re-instantiation. It does not cross network boundaries (that's what <code>traceparent</code> propagation is for) and doesn't eliminate request-scoped state conceptually — it just avoids paying for it via Nest's DI scoping.</p>`,
  },
  {
    id: "a14q-multi-tenancy-3",
    category: "arch",
    categoryLabel: "Architecture",
    question: `A BullMQ job processor reads \`als.getStore()\` expecting the tenant id from the request that enqueued the job, but gets \`undefined\`. Why?`,
    options: [`AsyncLocalStorage stores are cleared automatically after 30 seconds`, `BullMQ jobs run in a different async execution context than the original request; ALS context doesn't survive across that boundary unless explicitly re-established`, `The job processor needs to be marked REQUEST-scoped to access ALS`, `AsyncLocalStorage only works inside Express middleware, not in queue consumers`],
    answer: 1,
    explanationHtml: `<p>ALS propagates along one continuous async chain rooted at an <code>als.run()</code> call. Once the HTTP response is sent, that chain effectively ends. A queue job is a new, independent async context — to get the original request's data into the job, you must serialize it into the job payload at enqueue time and call <code>als.run()</code> again inside the processor to rebuild a (new) context from it.</p>`,
  },
  {
    id: "a14q-multi-tenancy-4",
    category: "arch",
    categoryLabel: "Architecture",
    question: `In a NestJS modular monolith, what is the gap between using \`exports\` in a \`@Module()\` and actually enforcing a module boundary?`,
    options: [`exports has no effect at runtime, only at compile time, so it's purely documentation`, `exports controls Nest DI visibility, but TypeScript imports can still reach into a module's internal files directly — a static-analysis tool like dependency-cruiser or an ESLint boundaries plugin is needed to forbid that in CI`, `exports can only be used for controllers, not services, so service boundaries are unenforceable in Nest`, `Nest automatically prevents cross-module imports of non-exported files at the bundler level`],
    answer: 1,
    explanationHtml: `<p><code>exports</code> is a DI-visibility mechanism — it decides what's injectable from outside the module. It does nothing to stop a plain <code>import</code> statement from reaching past it into another module's internal files, because TypeScript's module resolution and Nest's module system are separate, unconnected boundaries. Enforcing the boundary for real requires a static-analysis tool (dependency-cruiser, ESLint <code>import/no-restricted-paths</code> or a boundaries plugin) wired into CI.</p>`,
  },
  {
    id: "a14q-multi-tenancy-5",
    category: "arch",
    categoryLabel: "Architecture",
    question: `What are NestJS "durable providers" specifically designed to optimize?`,
    options: [`They make a provider survive application restarts by persisting its state to disk`, `They let request-scoped DI subtrees be keyed by a custom dimension (e.g. tenant id) via a ContextIdStrategy, so the subtree is reused across requests sharing that key instead of rebuilt every request`, `They guarantee a provider is never garbage collected during the process lifetime`, `They convert a TRANSIENT provider into a DEFAULT (singleton) provider automatically`],
    answer: 1,
    explanationHtml: `<p>Durable providers (<code>{ scope: Scope.REQUEST, durable: true }</code> plus a custom <code>ContextIdStrategy</code> via <code>ContextIdFactory.apply()</code>) let Nest bucket DI subtree construction by a stable dimension like tenant id rather than per raw request. That cuts the re-instantiation cost from O(requests) down to O(distinct tenants) while still preserving per-tenant isolation — directly addressing request scope's main performance complaint in multi-tenant systems.</p>`,
  },
  {
    id: "a14q-multi-tenancy-6",
    category: "arch",
    categoryLabel: "Architecture",
    question: `A team wants to extract a module from their NestJS modular monolith into a standalone microservice. Which property of the existing module design makes that extraction cheap?`,
    options: [`The module used REQUEST scope for all its providers`, `The module only ever exposed a small public API (facade) to other modules, and all cross-module callers depended on that interface rather than the module's internal implementation`, `The module had zero unit test coverage, so there's nothing to migrate`, `The module's entities were defined with TypeORM instead of Prisma`],
    answer: 1,
    explanationHtml: `<p>If every consumer of the module only ever talked to its exported facade/interface (a port), swapping that port's implementation from an in-process Nest provider to a network client (HTTP/gRPC/queue) behind the same interface is a DI wiring change, not a rewrite of every call site. This is the practical case for treating module exports as a deliberate public API from day one, even inside a monolith.</p>`,
  },
  {
    id: "a14q-multi-tenancy-7",
    category: "arch",
    categoryLabel: "Architecture",
    question: `Why should a NestJS WebSocket gateway generally avoid REQUEST-scoped providers, unlike an HTTP controller?`,
    options: [`WebSocket gateways must act as singletons — one gateway instance wraps the real underlying socket server, so it can't be re-instantiated per message the way a request-scoped controller is re-instantiated per HTTP request`, `WebSocket connections don't support dependency injection at all`, `REQUEST-scoped providers are deprecated in NestJS as of v10 and only TRANSIENT scope remains`, `Gateways automatically convert any REQUEST-scoped provider to DEFAULT scope silently`],
    answer: 0,
    explanationHtml: `<p>Official NestJS guidance is explicit: gateways should generally not use request-scoped providers because a <code>@WebSocketGateway()</code> must behave as a singleton encapsulating one real socket server — there's no per-message request boundary to key a fresh DI subtree on the way HTTP has. (The <code>CONTEXT</code> injection token exists for microservices and GraphQL, not for WebSocket gateways.)</p>`,
  },
];
