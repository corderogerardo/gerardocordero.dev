// Batch 9 — Structured logging (pino/winston + correlation ids), OpenTelemetry tracing, health checks (@nestjs/terminus), and multi-stage Docker builds for a Nest app.
import type { Flashcard } from "./flashcards";
import type { QuizQuestion } from "./quiz";

export const ADVANCED9_FLASHCARDS: Flashcard[] = [
  {
    id: "a9-observability-1",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `Why does pino outperform winston in high-throughput Node services, and what's the architectural tradeoff?`,
    answerHtml: `<p>Pino writes newline-delimited JSON with a minimal-allocation serializer and avoids synchronous string formatting on the hot path; expensive work like pretty-printing or shipping logs elsewhere is pushed into a <b>separate worker thread via transports</b> instead of running inline in the request-handling process. Winston, by default, runs its formatting/transport pipeline synchronously in-process, which costs more CPU per call under load.</p><p>The tradeoff: pino's raw output is JSON-only and unreadable in a terminal without piping through <code>pino-pretty</code>, and its plugin ecosystem for things like rotating files is thinner than winston's. Winston is more batteries-included (multiple formats, multiple transports, per-transport levels) but that flexibility is exactly what costs throughput.</p>`,
  },
  {
    id: "a9-observability-2",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `In a NestJS app using nestjs-pino, how does a request's correlation id end up on every log line without manually passing it through every service call?`,
    answerHtml: `<p><code>LoggerModule.forRoot</code> wraps <code>pino-http</code>, which generates (or reads from an inbound header like <code>x-request-id</code>) a request id and binds a <b>child logger</b> scoped to that request. nestjs-pino deliberately avoids Nest's <b>REQUEST-scoped DI</b> for this (request scope re-instantiates the provider graph on every request, which is a known throughput hit) — instead it stores the child logger in <b>AsyncLocalStorage</b>, so even singleton-scoped services calling the injected <code>PinoLogger</code> transparently read the right per-request child instance.</p><p>This depends on Node's AsyncLocalStorage to carry the request context across <code>await</code> boundaries and callback chains, so the id survives without being threaded through function signatures by hand, and without paying the request-scope instantiation cost.</p>`,
  },
  {
    id: "a9-observability-3",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `What's the practical difference between a correlation id and a distributed trace id, and why might a system carry both?`,
    answerHtml: `<p>A <b>correlation id</b> is usually one opaque string assigned at the edge (gateway or first service) and propagated unchanged through every hop of one logical request, primarily for grepping logs across services for that request. A <b>trace id</b> is part of a structured tracing model (e.g. W3C Trace Context's <code>traceparent</code>) where each hop also gets its own <b>span id</b>, parent-span pointers, and timing, so a tracing backend can reconstruct a timed call tree, not just a flat log search.</p><p>Systems often carry both because logs and traces serve different debugging modes: logs answer "what happened and what was the state," traces answer "where did the latency go." Many shops simplify by making the trace id double as the correlation id once OpenTelemetry is fully adopted.</p>`,
  },
  {
    id: "a9-observability-4",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `A NestJS interceptor sets a correlation id using a module-level variable instead of AsyncLocalStorage. What breaks under concurrent load, and why?`,
    answerHtml: `<p>A module-level (or any process-global, non-request-scoped) variable is shared across every in-flight request on that Node process. Because Node is single-threaded but interleaves async work, two concurrent requests will race: request B's <code>await</code> can resume after request A overwrote the global, so A's later logs (or downstream calls) pick up B's correlation id. This produces silently cross-contaminated logs that look correct individually but attribute work to the wrong request.</p><p>AsyncLocalStorage avoids this because each <code>run()</code> call creates an isolated context that is automatically threaded through the async call graph the way a thread-local would be in a multi-threaded runtime, with no shared mutable cell.</p>`,
  },
  {
    id: "a9-observability-5",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `Why is logging the full request body a common production incident, and what's the standard mitigation?`,
    answerHtml: `<p>Request bodies routinely contain passwords, tokens, full card numbers, or PII, and logs typically flow to less access-controlled, longer-retention storage than the database itself — so logging bodies verbatim turns a structured-logging win into a compliance and breach-surface liability (PCI-DSS and GDPR both treat this as a violation class).</p><p>The standard mitigation is a <b>redaction layer</b> on the logger itself — pino's <code>redact</code> option (or a winston format) that masks configured paths like <code>req.headers.authorization</code> or <code>req.body.password</code> at serialization time, plus an allowlist-by-default policy for which fields are even eligible to log, rather than trying to remember to scrub every call site.</p>`,
  },
  {
    id: "a9-observability-6",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `Where should the correlation id be created in a multi-service NestJS architecture: at the API gateway, or at each service's entry middleware?`,
    answerHtml: `<p>It should be created once at the <b>edge</b> (API gateway, load balancer, or the first service that receives external traffic) and then propagated as a header (commonly <code>x-correlation-id</code> or via <code>traceparent</code>) to every downstream service. Each downstream service's middleware should <b>read the inbound header if present, and only generate a new id as a fallback</b> for traffic that didn't go through the gateway (e.g. direct internal calls, cron jobs, queue consumers).</p><p>Generating a fresh id at every hop defeats the entire purpose — you'd get a different id per service and lose the ability to grep one request's full path across the system.</p>`,
  },
  {
    id: "a9-observability-7",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `How does pino's child-logger pattern (logger.child({...})) differ from just adding fields to every individual log call, and why does it matter for performance?`,
    answerHtml: `<p><code>logger.child({ requestId })</code> creates a new logger instance that pre-serializes the bound fields <b>once</b> and prepends them to every subsequent log line's output buffer, rather than re-serializing the same key-value pairs on every <code>.log()</code> call. This is both an ergonomics win (callers don't need to remember to pass <code>requestId</code> every time) and a performance win (pino avoids redundant JSON.stringify work for static context).</p><p>It also scopes correctly with the request context: a logger bound to AsyncLocalStorage can hold one child logger for the lifetime of the request, so every service injected with it automatically tags its output — this is the mechanism nestjs-pino builds on.</p>`,
  },
  {
    id: "a9-observability-8",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `In OpenTelemetry terms, what is a Span, and what's the minimum set of fields a span needs to be useful in a trace waterfall?`,
    answerHtml: `<p>A <b>Span</b> represents one unit of work with a start and end time — an HTTP handler, a DB query, an outbound call. Spans nest via parent-child relationships to form a <b>trace</b>, which visualizes as a waterfall of nested time bars.</p><p>Minimum useful fields: a <code>traceId</code> (shared by every span in the request), a <code>spanId</code> (unique to this span), a <code>parentSpanId</code> (links it into the tree), start/end timestamps, a <code>name</code>, and a <code>status</code> (ok/error). Attributes (key-value metadata like <code>http.route</code> or <code>db.statement</code>) and events (timestamped log-like entries within the span) are optional but what make spans actually diagnostic rather than just timing bars.</p>`,
  },
  {
    id: "a9-observability-9",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `What's the practical difference between OpenTelemetry auto-instrumentation and manual instrumentation in a NestJS service, and when do you need the latter?`,
    answerHtml: `<p><b>Auto-instrumentation</b> (e.g. <code>@opentelemetry/instrumentation-http</code>, <code>-express</code>, <code>-nestjs-core</code>, <code>-pg</code>) monkey-patches well-known library entry points at startup to create spans for HTTP requests, outbound calls, and DB queries with zero application code changes — it covers the "shape" of a typical request automatically.</p><p>You need <b>manual instrumentation</b> (calling the Tracer API directly to start/end a span) whenever the unit of work that matters isn't a library call the auto-instrumenter knows about — a business-logic step inside a single function, a loop processing a batch, or anything you want to appear as its own named span with custom attributes (e.g. <code>order.itemCount</code>). Auto-instrumentation gives you the skeleton; manual spans give you the business-meaningful detail.</p>`,
  },
  {
    id: "a9-observability-10",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `Why must the OpenTelemetry SDK be initialized before NestJS (and the modules it imports, like a DB driver) loads, rather than inside a Nest module's constructor?`,
    answerHtml: `<p>Auto-instrumentation works by patching the exported functions/prototypes of target modules (e.g. <code>pg</code>, <code>http</code>) <b>at the moment those modules are first <code>require()</code>'d</b>. If NestJS and its providers have already imported and cached references to the unpatched module before the OTel SDK registers its instrumentation, those cached references bypass the patch entirely — instrumentation silently does nothing for that module.</p><p>This is why the standard pattern is a separate <code>tracing.ts</code> file that calls <code>NodeSDK.start()</code> and is required via <code>node -r ./tracing.js dist/main.js</code> (or imported as the very first line of <code>main.ts</code>, before any Nest imports) — it has to run before the module graph that needs patching is resolved.</p>`,
  },
  {
    id: "a9-observability-11",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `What is the OpenTelemetry Collector, and why would you route spans through it instead of exporting directly from each NestJS pod to your tracing backend (e.g. Jaeger, Honeycomb)?`,
    answerHtml: `<p>The <b>Collector</b> is a standalone process (sidecar or gateway deployment) that receives telemetry via OTLP, can batch/sample/filter/transform it, and fans it out to one or more backends. Exporting directly from the app means every pod opens its own connection to the backend, bakes the backend's endpoint/auth into app config, and pays the export cost (batching, retries, backpressure) on the application's own resources.</p><p>Routing through a Collector decouples the app from the backend (swap Jaeger for Honeycomb by reconfiguring the Collector, not redeploying every service), centralizes sampling decisions (e.g. tail-based sampling, which needs to see all spans of a trace before deciding to keep it — impossible to do per-pod), and isolates export failures/backpressure from request-handling latency.</p>`,
  },
  {
    id: "a9-observability-12",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `What's the difference between head-based and tail-based sampling in distributed tracing, and why does tail-based sampling require a Collector tier?`,
    answerHtml: `<p><b>Head-based sampling</b> decides whether to record a trace at the very first span (e.g. "sample 10% of requests"), before knowing how the request turns out. It's cheap and simple but means most error or high-latency traces — the ones you actually want — are dropped by the same random chance as everything else.</p><p><b>Tail-based sampling</b> defers the decision until the whole trace is complete, so it can keep "interesting" traces (errors, p99 latency) at a much higher rate than routine ones. This requires a Collector (or a tier of them) to buffer all spans for a trace in memory until it's complete, because the decision needs the full picture — a single app instance only sees its own spans, not the full cross-service trace.</p>`,
  },
  {
    id: "a9-observability-13",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `Why is the W3C Trace Context traceparent header significant for a NestJS service that calls (or is called by) services written in other languages?`,
    answerHtml: `<p><code>traceparent</code> is a vendor-neutral, standardized HTTP header format (<code>version-traceId-parentId-traceFlags</code>) adopted across OpenTelemetry SDKs for every language. Before it existed, propagating trace context across a polyglot system meant either standardizing on one vendor's proprietary header (e.g. an APM vendor's custom format) or writing translation shims at every service boundary.</p><p>Because NestJS's OTel HTTP instrumentation reads/writes <code>traceparent</code> automatically on inbound/outbound requests, a trace started in a Go service, passed through a NestJS service, and finished in a Python service stitches into one continuous trace with zero custom glue code — as long as every hop propagates the header through (which is the actual operational risk: any proxy or queue that strips unknown headers breaks the chain).</p>`,
  },
  {
    id: "a9-observability-14",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `Why do @nestjs/terminus health checks distinguish between liveness and readiness, and what's the consequence of conflating the two in a Kubernetes deployment?`,
    answerHtml: `<p><b>Liveness</b> asks "is this process alive and not deadlocked" — failing it tells Kubernetes to <b>kill and restart the pod</b>. <b>Readiness</b> asks "can this pod currently serve traffic" — failing it tells Kubernetes to <b>pull the pod out of the Service's load-balancing pool</b> without killing it.</p><p>If you wire one combined health check to both probes, and that check includes a downstream dependency (e.g. the database), a transient DB blip will cause Kubernetes to <b>restart every pod</b> instead of just routing around them — which is the wrong response: restarting doesn't fix a dead database, it just adds container-startup churn and can cause a thundering herd of reconnects when the DB recovers. Liveness checks should be process-internal only (event loop responsive, no deadlock); readiness checks should include dependency health.</p>`,
  },
  {
    id: "a9-observability-15",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `How does @nestjs/terminus's TypeOrmHealthIndicator (or similar DB indicator) actually verify database health, and what failure mode does a naive 'is the connection object truthy' check miss?`,
    answerHtml: `<p>Terminus's DB indicators run a lightweight real query (effectively a <code>SELECT 1</code>-equivalent ping) against the actual connection pool, via the indicator's <code>pingCheck()</code> method (you supply a timeout, since Terminus doesn't enforce one for you), and report unhealthy if it throws or times out. Checking only whether a connection/pool object exists in memory misses the common failure mode where the <b>object reference is alive but the underlying TCP connection is stale or the DB is unreachable</b> — e.g. after a network partition heals asymmetrically, or the DB process restarted and the pool hasn't detected the dead sockets yet.</p><p>A real ping with a timeout also catches the case where the DB is up but pathologically slow (lock contention, disk I/O stall) — a scenario a truthy-object check would report as healthy while every real request hangs.</p>`,
  },
  {
    id: "a9-observability-16",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `What's the risk of a Kubernetes readiness probe health check that itself makes network calls, and how should its timeout be configured relative to the probe's own timeout?`,
    answerHtml: `<p>If the health check's internal call to a dependency (DB, downstream API) has no timeout or a longer timeout than Kubernetes' probe timeout, a hung dependency call doesn't just mark the pod unhealthy — it can pile up concurrent health-check requests (each kubelet retry spawns another check before the last one resolves), consuming connection-pool slots and threads that legitimate traffic needs, making the outage worse than the dependency failure alone.</p><p>The check's internal timeout should be strictly shorter than the probe's <code>timeoutSeconds</code>, and the indicator should use a bounded connection (e.g. a dedicated low-concurrency pool, Terminus's <code>pingCheck(key, { timeout })</code> option, or a hard per-check timeout via <code>Promise.race</code>) so a stuck dependency fails fast and predictably rather than accumulating.</p>`,
  },
  {
    id: "a9-observability-17",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `Why does a multi-stage Dockerfile for a NestJS app typically need three stages (deps/build, then a slim runtime), not two?`,
    answerHtml: `<p>A common pattern is: <b>(1) a builder stage</b> that installs full dependencies (including devDependencies like the TypeScript compiler and Nest CLI) and runs <code>nest build</code> to produce <code>dist/</code>; <b>(2) a prod-deps stage</b> that does a clean <code>npm ci --omit=dev</code> (or <code>pnpm install --prod</code>) to get only runtime dependencies without the build tool weight; <b>(3) a runtime stage</b> that copies only <code>dist/</code> and the prod <code>node_modules</code> from the prior stages onto a minimal base image.</p><p>Collapsing build and prod-deps into one stage either forces you to ship devDependencies into the final image (bloats size, increases attack surface with build tooling) or forces a wasteful <code>npm prune --production</code> after build in the same layer, which doesn't shrink earlier layers in the image history the way copying into a fresh stage does.</p>`,
  },
  {
    id: "a9-observability-18",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `In a multi-stage Dockerfile, why does copying package.json and the lockfile before the rest of the source code (and running install in that earlier layer) matter for build speed?`,
    answerHtml: `<p>Docker layer caching invalidates a layer (and every layer after it) only when the inputs to that specific instruction change. If <code>COPY . .</code> happens before <code>npm ci</code>, then any source-code edit — even unrelated to dependencies — invalidates the dependency-install layer too, forcing a full reinstall on every build.</p><p>Copying only <code>package.json</code>/lockfile first, running <code>npm ci</code>, and <i>then</i> copying the rest of the source means the (usually expensive) install layer only re-runs when dependencies actually change, while source edits only invalidate the cheap, fast layers after it — turning a multi-minute rebuild into a multi-second one for the common case.</p>`,
  },
  {
    id: "a9-observability-19",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `Why run the final container as a non-root user (e.g. Node's built-in node user, or a custom USER directive), and what does it actually protect against in a NestJS image?`,
    answerHtml: `<p>By default, a Docker container's main process runs as root <i>inside the container</i>, which is fine in isolation but means a <b>container-breakout vulnerability</b> (in the kernel, the container runtime, or a misconfigured volume mount) hands an attacker root on the host, not just an unprivileged container user. It also means any RCE in the Node process itself (a deserialization bug, a vulnerable dependency) gives the attacker root within the container, able to modify the filesystem, install tooling, or pivot more easily.</p><p>Adding <code>USER node</code> (the official Node image ships this user, uid 1000, on both Debian and Alpine variants) before the final <code>CMD</code> — combined with <code>COPY --chown=node:node</code> so the app's files are actually readable by it — means the running process has no permission to write outside its assigned directories or install system packages, which doesn't prevent the vulnerability but meaningfully shrinks what an exploit can do with it — standard defense-in-depth, and increasingly required by platform security policies (e.g. Kubernetes Pod Security Standards' "restricted" profile).</p>`,
  },
  {
    id: "a9-observability-20",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `What's the difference between npm ci and npm install in a Docker build stage, and why does it matter for reproducible builds?`,
    answerHtml: `<p><code>npm ci</code> requires an existing lockfile, deletes <code>node_modules</code> first, and installs <b>exactly</b> what the lockfile specifies, failing outright if <code>package.json</code> and the lockfile are out of sync. <code>npm install</code> will happily resolve a new dependency graph, update the lockfile, and succeed even if they've drifted — which is useful locally but dangerous in CI/Docker, where you want the build to fail loudly on drift rather than silently bake a different dependency tree into the image than what's checked in.</p><p>It's also typically faster in a clean environment because it skips npm's full dependency-resolution pass (checking package.json ranges against the registry) and installs directly from the lockfile's pinned tree, which is part of why it's the standard choice for the dependency-install layer in a Dockerfile.</p>`,
  },
  {
    id: "a9-observability-21",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `How should signal handling (SIGTERM) be addressed in a NestJS Docker image, and why does the choice of base image / entrypoint matter for it?`,
    answerHtml: `<p>Kubernetes (and Docker's own stop) sends <code>SIGTERM</code> to PID 1 in the container and waits a grace period before <code>SIGKILL</code>. If PID 1 is a shell (e.g. an entrypoint written as <code>CMD npm start</code>, which spawns <code>node</code> as a child of <code>sh -c</code>), the shell often doesn't forward the signal to the actual Node process, so the app never gets a chance to drain in-flight requests and close DB connections before being hard-killed.</p><p>The fix is running Node directly as PID 1 (<code>CMD ["node", "dist/main.js"]</code>, exec form, not shell form) so it receives SIGTERM directly, combined with Nest's <code>app.enableShutdownHooks()</code> (shutdown listeners are <b>off by default</b> — you must opt in) so <code>OnModuleDestroy</code>/<code>OnApplicationShutdown</code> lifecycle hooks actually run — or using a minimal init process like <code>tini</code> (<code>docker run --init</code>, or <code>ENTRYPOINT ["tini", "--"]</code>) if you also need zombie-process reaping.</p>`,
  },
  {
    id: "a9-observability-22",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `What does NestJS's app.enableShutdownHooks() actually enable, and what's a concrete failure mode it prevents during a rolling deploy?`,
    answerHtml: `<p>It registers listeners for termination signals (SIGTERM and the rest of the <code>ShutdownSignal</code> enum by default, or a specific subset you pass in) that trigger Nest's shutdown lifecycle — calling <code>onModuleDestroy</code> / <code>beforeApplicationShutdown</code> / <code>onApplicationShutdown</code> hooks on every provider that implements them, in reverse module-initialization order. These listeners are <b>disabled unless you call this method</b> — without it, the process exits on the signal without running that cleanup at all.</p><p>Concretely, during a rolling deploy, Kubernetes sends SIGTERM to a pod being replaced while it may still have in-flight requests and open DB/queue connections. Without shutdown hooks, those connections are dropped mid-transaction and in-flight requests get connection-reset errors instead of completing; with hooks wired (closing the HTTP server to new connections, draining existing ones, closing DB pools cleanly), the pod terminates without dropping active work — assuming the grace period (<code>terminationGracePeriodSeconds</code>) is long enough for it to finish.</p>`,
  },
  {
    id: "a9-observability-23",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `Why might a NestJS Docker image choose a distroless or alpine base over the default node:XX (Debian-based) image, and what's the corresponding cost?`,
    answerHtml: `<p><code>node:XX-alpine</code> and distroless images are dramatically smaller (tens of MB vs. several hundred) because they ship a minimal or no general-purpose userland — fewer packages means less to download/cache/scan and a much smaller attack surface (fewer CVEs to track, no shell or package manager for an attacker to use post-compromise in distroless's case).</p><p>The cost: Alpine uses <b>musl libc</b> instead of glibc, which occasionally breaks native Node addons compiled against glibc assumptions (rare today but a known historical pain point, e.g. with some versions of <code>sharp</code> or <code>bcrypt</code>'s native bindings) and means you can't <code>apk add</code> a missing system tool at runtime for debugging. Distroless goes further — no shell at all — which is great for security but means you can't <code>kubectl exec</code> in and poke around; you have to rely entirely on logs/metrics/traces, which is part of why structured logging and tracing matter more once you adopt it.</p>`,
  },
  {
    id: "a9-observability-24",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `When instrumenting a NestJS app with OpenTelemetry, why does the choice of span processor (SimpleSpanProcessor vs BatchSpanProcessor) matter in production?`,
    answerHtml: `<p><code>SimpleSpanProcessor</code> exports each span synchronously the instant it ends, on the same execution path as the request — useful for local debugging where you want to see a span immediately, but in production it adds export latency (and failure risk) directly to request handling and creates one outbound call per span, which doesn't scale.</p><p><code>BatchSpanProcessor</code> buffers completed spans and exports them in batches on a timer/size threshold, off the request's critical path, which is the correct choice for most production services — it trades a small risk of losing the last batch on an ungraceful crash for dramatically lower overhead and export call volume. (The one common exception is short-lived serverless functions like AWS Lambda, where the process can freeze immediately after the response and never flush a pending batch — there, SimpleSpanProcessor or an explicit flush-before-freeze is preferred.) This is also why graceful shutdown matters for tracing specifically: an abrupt process kill can drop whatever's still sitting in the batch buffer unflushed.</p>`,
  },
];

export const ADVANCED9_QUIZ: QuizQuestion[] = [
  {
    id: "a9q-observability-1",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `A NestJS service uses AsyncLocalStorage to carry a correlation id, but a developer adds a setImmediate() call inside a request handler to defer some logging. What happens to the correlation id inside that setImmediate callback?`,
    options: [`It is lost because setImmediate breaks out of the async context entirely`, `It is preserved, because AsyncLocalStorage's context propagates through the full async resource chain, including timers and setImmediate`, `It is preserved only if the callback is wrapped in async/await`, `It is preserved only on Node versions before 16`],
    answer: 1,
    explanationHtml: `<p>AsyncLocalStorage is built on Node's <code>async_hooks</code> machinery, which tracks context across the full async resource chain — promises, <code>setTimeout</code>, <code>setImmediate</code>, and I/O callbacks all preserve the active store. This is precisely what makes it suitable for correlation-id propagation without manually passing context through every function signature.</p>`,
  },
  {
    id: "a9q-observability-2",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `A team configures their Kubernetes readiness probe to call the same Terminus endpoint as their liveness probe, and that endpoint checks both the event loop and a downstream Postgres connection. During a 90-second Postgres failover, what is the most likely observed behavior?`,
    options: [`Only traffic routing is affected; pods stay up and reconnect cleanly once Postgres recovers`, `Kubernetes kills and restarts every pod because the combined check fails liveness too, adding restart churn on top of the outage`, `Nothing happens because Terminus caches health results for 5 minutes by default`, `The pods automatically fall back to a read replica`],
    answer: 1,
    explanationHtml: `<p>Conflating liveness and readiness onto one check that includes an external dependency means a transient DB outage fails liveness too, so Kubernetes restarts every pod — not just routes around them. Liveness should only check process-internal health (event loop, deadlocks); dependency checks belong on readiness alone, so pods stay alive and simply rejoin the load-balancing pool once the dependency recovers.</p>`,
  },
  {
    id: "a9q-observability-3",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `Why does OpenTelemetry auto-instrumentation for a NestJS + Postgres service typically require its initialization file to run via node -r ./tracing.js dist/main.js rather than being imported inside an AppModule provider?`,
    options: [`NestJS providers cannot import OpenTelemetry packages at all`, `Auto-instrumentation patches a module's exports at require-time, so it must run before NestJS or the pg driver are first required, or the patches won't apply to already-cached module references`, `It's only a style convention with no functional effect`, `OpenTelemetry requires a separate Node process to run the Collector`],
    answer: 1,
    explanationHtml: `<p>Auto-instrumentation works by monkey-patching the target module's exports the moment that module is first <code>require()</code>'d. If NestJS and its dependencies (like the <code>pg</code> driver) load and cache references before the OTel SDK registers, those cached references are the unpatched originals — instrumentation silently no-ops for them. Running the SDK init as the very first thing in the process (via <code>-r</code> preload or the first line of the entrypoint) guarantees patches apply before anything else loads.</p>`,
  },
  {
    id: "a9q-observability-4",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `What is the core reason tail-based sampling for distributed traces requires routing spans through a Collector tier, rather than each service instance deciding independently?`,
    options: [`Collectors are required by the OpenTelemetry specification for any sampling at all`, `Tail-based sampling needs to see every span of a complete trace before deciding to keep or drop it, and no single service instance has visibility into spans produced by other services`, `It's purely a cost-optimization choice with no technical requirement`, `Head-based sampling is actually required for traces spanning more than one service`],
    answer: 1,
    explanationHtml: `<p>Tail-based sampling defers the keep/drop decision until a trace is complete so it can prioritize traces with errors or high latency. Since a trace typically spans multiple services, no single app instance sees the whole picture — a Collector tier buffers spans for a trace until it's complete (or a timeout) and only then makes the sampling decision, which is impossible to do correctly from a single service's local context.</p>`,
  },
  {
    id: "a9q-observability-5",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `In a multi-stage NestJS Dockerfile, what's the main reason for running COPY package.json pnpm-lock.yaml ./ and RUN pnpm install BEFORE COPY . .?`,
    options: [`It's required by pnpm and will error otherwise`, `It reduces the final image size by excluding source files from that layer`, `It lets Docker's layer cache reuse the dependency-install layer across builds where only application source changed, avoiding a full reinstall on every code edit`, `It prevents the lockfile from being included in the final image for security reasons`],
    answer: 2,
    explanationHtml: `<p>Docker invalidates a cached layer (and everything after it) only when that layer's own inputs change. Copying the lockfile and installing before copying the rest of the source means an unrelated source-code edit doesn't invalidate the install layer, so most builds skip the expensive reinstall entirely and only re-run the cheap copy/build steps after it.</p>`,
  },
  {
    id: "a9q-observability-6",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `A NestJS container's Dockerfile uses CMD npm start, where start runs node dist/main.js. During a rolling deploy, pods take the full terminationGracePeriodSeconds to die and in-flight requests get connection-reset errors despite app.enableShutdownHooks() being called in code. What is the most likely root cause?`,
    options: [`enableShutdownHooks() only works with HTTPS, not HTTP`, `npm start runs Node as a child of a shell process, so the shell (PID 1) may not forward SIGTERM to the actual Node process, meaning the app never gets to run its shutdown hooks`, `terminationGracePeriodSeconds is being ignored by Kubernetes for Node apps`, `Shutdown hooks are deprecated and have no effect in current NestJS versions`],
    answer: 1,
    explanationHtml: `<p>Shell-form CMD (<code>npm start</code>) makes a shell PID 1, which often does not propagate SIGTERM to its child Node process. Node never receives the signal, so the shutdown hooks never fire, and the container sits until SIGKILL after the grace period — dropping in-flight work instead of draining it. The fix is exec-form CMD running Node directly as PID 1 (<code>CMD ["node", "dist/main.js"]</code>), or an init process like tini that correctly forwards signals.</p>`,
  },
  {
    id: "a9q-observability-7",
    category: "deploy",
    categoryLabel: "Deploy & Ops",
    question: `Why is BatchSpanProcessor the recommended choice over SimpleSpanProcessor for OpenTelemetry tracing in a typical (non-serverless) production NestJS deployment?`,
    options: [`BatchSpanProcessor produces more accurate span timestamps`, `SimpleSpanProcessor exports each span synchronously on the request's own execution path, adding latency and a per-span network call; BatchSpanProcessor buffers and exports off the critical path, trading a small risk of losing unflushed spans on crash for much lower overhead`, `SimpleSpanProcessor is deprecated and no longer functions in current OpenTelemetry SDK versions`, `BatchSpanProcessor is required when using a Collector, and optional otherwise`],
    answer: 1,
    explanationHtml: `<p>SimpleSpanProcessor exports synchronously and inline with the request, which adds latency and creates one export call per span — fine for local debugging, not for production volume. BatchSpanProcessor buffers spans and flushes them in batches off the request path, which is far more efficient at scale; the tradeoff is that an abrupt crash can lose whatever spans were sitting unflushed in the buffer, which is part of why graceful shutdown (flushing on SIGTERM) matters for tracing too. The notable exception is short-lived serverless functions (e.g. Lambda), where SimpleSpanProcessor or an explicit pre-freeze flush is preferred instead.</p>`,
  },
];
